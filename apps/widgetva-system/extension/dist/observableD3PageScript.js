(function() {
	//#region ../../widgetva-kit/src/adapters/humanInteractionBindings.js
	function buildActionCall$1({ name, actionTargetRef, params }) {
		return {
			callId: `human_${Date.now()}`,
			name,
			actor: "human",
			targetRef: actionTargetRef || void 0,
			params: {
				targetRef: actionTargetRef || void 0,
				...params
			}
		};
	}
	function resolveCategoryField$1({ enc, interactionConfig }) {
		const preferredChannel = interactionConfig?.categoryFieldChannel;
		if (preferredChannel && enc?.[preferredChannel]?.field) return enc[preferredChannel].field;
		if (interactionConfig?.categoryField && typeof interactionConfig.categoryField === "string") return interactionConfig.categoryField;
		return enc.color?.field || enc.shape?.field || enc.detail?.field || enc.key?.field || enc.x?.field || enc.y?.field;
	}
	function resolveCellFields$1({ enc, interactionConfig }) {
		return {
			xField: interactionConfig?.xField || enc.x?.field || null,
			yField: interactionConfig?.yField || enc.y?.field || null
		};
	}
	function normalizeMultiBrushRules(rawRules) {
		if (!Array.isArray(rawRules)) return [];
		return rawRules.filter((rule) => typeof rule?.field === "string" && Array.isArray(rule?.range) && rule.range.length === 2).map((rule) => ({
			field: rule.field,
			range: [Math.min(...rule.range), Math.max(...rule.range)]
		}));
	}
	function buildFallbackSelection({ selectionSourceWidgetId, selectionType, domain, predicates, count, summary, fields, value, keyField, keys, field, values }) {
		return {
			selection_id: `sel_${Date.now()}`,
			source_widget_id: selectionSourceWidgetId || void 0,
			selection_type: selectionType,
			...domain ? { domain } : {},
			...Array.isArray(fields) ? { fields } : {},
			...value && typeof value === "object" && !Array.isArray(value) ? { value } : {},
			...typeof keyField === "string" ? { keyField } : {},
			...Array.isArray(keys) ? { keys } : {},
			...typeof field === "string" ? { field } : {},
			...Array.isArray(values) ? { values } : {},
			predicates,
			count,
			summary
		};
	}
	function bindWidgetHumanInteractions({ view, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
		const data = Array.isArray(spec?.data?.values) ? spec.data.values : [];
		const enc = spec?.encoding || {};
		const cleanups = [];
		if (!interactionConfig || interactionConfig.mode === "none") return () => {};
		function emitBrushSelection() {
			try {
				const xField = enc.x?.field ?? "x";
				const yField = enc.y?.field ?? "y";
				let xMin;
				let xMax;
				let yMin;
				let yMax;
				const brush = view.signal("brush");
				if (brush && (Array.isArray(brush) || brush.x && brush.y)) {
					const xr = Array.isArray(brush) ? brush : brush.x;
					const yr = Array.isArray(brush) ? brush : brush.y;
					if (xr && yr && xr.length >= 2 && yr.length >= 2) {
						xMin = Math.min(xr[0], xr[1]);
						xMax = Math.max(xr[0], xr[1]);
						yMin = Math.min(yr[0], yr[1]);
						yMax = Math.max(yr[0], yr[1]);
					}
				}
				if (xMin == null) {
					const x1 = view.signal("brush_x_1");
					const x2 = view.signal("brush_x_2");
					const y1 = view.signal("brush_y_1");
					const y2 = view.signal("brush_y_2");
					if (x1 != null && x2 != null && y1 != null && y2 != null) {
						xMin = Math.min(x1, x2);
						xMax = Math.max(x1, x2);
						yMin = Math.min(y1, y2);
						yMax = Math.max(y1, y2);
					}
				}
				if (xMin == null) {
					const tupleStore = view.data?.("brush_store");
					const tuple = Array.isArray(tupleStore) && tupleStore.length > 0 ? tupleStore[0] : null;
					const fields = tuple?.fields;
					const values = tuple?.values;
					if (Array.isArray(fields) && Array.isArray(values) && fields.length >= 2 && values.length >= 2) {
						const xVal = values[0];
						const yVal = values[1];
						if (Array.isArray(xVal) && xVal.length >= 2 && Array.isArray(yVal) && yVal.length >= 2) {
							xMin = Math.min(xVal[0], xVal[1]);
							xMax = Math.max(xVal[0], xVal[1]);
							yMin = Math.min(yVal[0], yVal[1]);
							yMax = Math.max(yVal[0], yVal[1]);
						}
					}
				}
				if (xMin == null || xMax == null || yMin == null || yMax == null) {
					onSelectionChange?.(null);
					return;
				}
				const filtered = data.filter((row) => {
					const x = row?.[xField];
					const y = row?.[yField];
					return x != null && y != null && x >= xMin && x <= xMax && y >= yMin && y <= yMax;
				});
				if (onActionCall) {
					onActionCall(buildActionCall$1({
						name: interactionConfig.actionName || "scatter.brushRegion",
						actionTargetRef,
						params: {
							xField,
							yField,
							xRange: [xMin, xMax],
							yRange: [yMin, yMax]
						}
					}));
					return;
				}
				onSelectionChange?.(buildFallbackSelection({
					selectionSourceWidgetId,
					selectionType: "interval",
					fields: [xField, yField],
					value: {
						[xField]: [xMin, xMax],
						[yField]: [yMin, yMax]
					},
					domain: {
						xDomain: [xMin, xMax],
						yDomain: [yMin, yMax]
					},
					predicates: [{
						field: xField,
						op: "between",
						value: [xMin, xMax]
					}, {
						field: yField,
						op: "between",
						value: [yMin, yMax]
					}],
					count: filtered.length,
					summary: `${xField} ${xMin.toFixed(1)}–${xMax.toFixed(1)}, ${yField} ${yMin.toFixed(1)}–${yMax.toFixed(1)}`
				}));
			} catch {
				onSelectionChange?.(null);
			}
		}
		function emitCategorySelection(_event, item) {
			try {
				const categoryField = resolveCategoryField$1({
					enc,
					interactionConfig
				});
				const categoryValue = categoryField ? item?.datum?.[categoryField] : void 0;
				if (!categoryField || categoryValue == null) return;
				const matched = data.filter((row) => row?.[categoryField] === categoryValue);
				if (onActionCall) {
					onActionCall(buildActionCall$1({
						name: interactionConfig.actionName || "bar.selectCategory",
						actionTargetRef,
						params: {
							field: categoryField,
							values: [categoryValue]
						}
					}));
					return;
				}
				onSelectionChange?.(buildFallbackSelection({
					selectionSourceWidgetId,
					selectionType: "category",
					field: categoryField,
					values: [categoryValue],
					predicates: [{
						field: categoryField,
						op: "in",
						value: [categoryValue]
					}],
					count: matched.length,
					summary: `${categoryField}: ${String(categoryValue)}`
				}));
			} catch {
				onSelectionChange?.(null);
			}
		}
		function emitCellSelection(_event, item) {
			try {
				const { xField, yField } = resolveCellFields$1({
					enc,
					interactionConfig
				});
				const xValue = xField ? item?.datum?.[xField] : void 0;
				const yValue = yField ? item?.datum?.[yField] : void 0;
				if (!xField || !yField || xValue == null || yValue == null) return;
				const matched = data.filter((row) => row?.[xField] === xValue && row?.[yField] === yValue);
				if (onActionCall) {
					onActionCall(buildActionCall$1({
						name: interactionConfig.actionName || "heatmap.selectCell",
						actionTargetRef,
						params: {
							xField,
							yField,
							xValue,
							yValue
						}
					}));
					return;
				}
				onSelectionChange?.(buildFallbackSelection({
					selectionSourceWidgetId,
					selectionType: "cell",
					fields: [xField, yField],
					value: {
						[xField]: xValue,
						[yField]: yValue
					},
					predicates: [{
						field: xField,
						op: "equals",
						value: xValue
					}, {
						field: yField,
						op: "equals",
						value: yValue
					}],
					count: matched.length,
					summary: `${xField}: ${String(xValue)}; ${yField}: ${String(yValue)}`
				}));
			} catch {
				onSelectionChange?.(null);
			}
		}
		function emitMultiBrushSelection(...args) {
			try {
				const rules = normalizeMultiBrushRules(args.find((value) => Array.isArray(value)) || interactionConfig?.rules);
				if (rules.length === 0) return;
				const matched = data.filter((row) => rules.every((rule) => {
					const value = row?.[rule.field];
					return typeof value === "number" && value >= rule.range[0] && value <= rule.range[1];
				}));
				if (onActionCall) {
					onActionCall(buildActionCall$1({
						name: interactionConfig.actionName || "parallelCoordinates.brushAxes",
						actionTargetRef,
						params: { rules }
					}));
					return;
				}
				onSelectionChange?.(buildFallbackSelection({
					selectionSourceWidgetId,
					selectionType: "interval",
					predicates: rules.map((rule) => ({
						field: rule.field,
						op: "between",
						value: rule.range
					})),
					count: matched.length,
					summary: rules.map((rule) => `${rule.field} ${rule.range[0]}–${rule.range[1]}`).join("; ")
				}));
			} catch {
				onSelectionChange?.(null);
			}
		}
		if (interactionConfig.mode === "brush2d") {
			try {
				view.addSignalListener("brush", emitBrushSelection);
				cleanups.push(() => view.removeSignalListener?.("brush", emitBrushSelection));
			} catch {}
			for (const signalName of [
				"brush_x_1",
				"brush_x_2",
				"brush_y_1",
				"brush_y_2"
			]) try {
				view.addSignalListener(signalName, emitBrushSelection);
				cleanups.push(() => view.removeSignalListener?.(signalName, emitBrushSelection));
			} catch {}
			try {
				view.addEventListener("mouseup", emitBrushSelection);
				cleanups.push(() => view.removeEventListener?.("mouseup", emitBrushSelection));
			} catch {}
		}
		if (interactionConfig.mode === "categoryClick") try {
			view.addEventListener("click", emitCategorySelection);
			cleanups.push(() => view.removeEventListener?.("click", emitCategorySelection));
		} catch {}
		if (interactionConfig.mode === "cellClick") try {
			view.addEventListener("click", emitCellSelection);
			cleanups.push(() => view.removeEventListener?.("click", emitCellSelection));
		} catch {}
		if (interactionConfig.mode === "multiBrush") for (const signalName of ["widgetva_multiBrush", "multiBrush"]) try {
			view.addSignalListener(signalName, emitMultiBrushSelection);
			cleanups.push(() => view.removeSignalListener?.(signalName, emitMultiBrushSelection));
		} catch {}
		return () => {
			cleanups.forEach((cleanup) => {
				try {
					cleanup();
				} catch {}
			});
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetAdapterContract.js
	function defaultHumanInteraction() {
		return {
			mode: "none",
			actionName: null,
			supportsDirectManipulation: false
		};
	}
	function assertFunction(name, value) {
		if (value == null) return;
		if (typeof value !== "function") throw new Error(`Widget adapter contract field ${name} must be a function when provided.`);
	}
	function assertRequiredFunction(name, value) {
		if (typeof value !== "function") throw new Error(`Widget adapter instance requires ${name}() to be provided.`);
	}
	function assertObject(name, value) {
		if (value == null) return;
		if (typeof value !== "object" || Array.isArray(value)) throw new Error(`Widget adapter contract field ${name} must be an object when provided.`);
	}
	function defaultProviderCapabilities() {
		return {
			supportedWidgetKinds: [],
			renderStrategy: "custom",
			stateApplyStrategy: "custom",
			interactionBindingStrategy: "custom",
			supportsRendererMount: false,
			supportsRendererUpdate: false,
			supportsRendererDispose: false,
			supportsSignalPatching: false,
			supportsOptionMerging: false,
			supportsImperativeRender: false,
			supportsPointSelection: false,
			supportsIntervalSelection: false,
			supportsZoomPan: false,
			supportsFocusReadback: false,
			supportsSelectionReadback: false,
			supportsViewportReadback: false,
			supportsHighlightProjection: false,
			supportsInteractionEvents: false
		};
	}
	function looksLikeWidgetState(value) {
		return value != null && typeof value === "object" && !Array.isArray(value) && (typeof value.widgetId === "string" || typeof value.ref === "string" || typeof value.kind === "string" || value.view != null || value.selections != null || value.data != null || value.feedback != null);
	}
	function normalizeApplyStateArgs(args) {
		if (args != null && typeof args === "object" && !Array.isArray(args) && Object.prototype.hasOwnProperty.call(args, "state")) return args;
		if (looksLikeWidgetState(args)) return { state: args };
		return args;
	}
	function createWidgetAdapterInstance({ definition, widgetRef, dataRef, getDescription, getState, humanInteraction, bindHumanInteractions, applyState, mount, update, dispose, readSelection, readViewport, metadata }) {
		assertRequiredFunction("getDescription", getDescription);
		assertRequiredFunction("getState", getState);
		const resolvedHumanInteraction = humanInteraction || definition.getHumanInteractionConfig?.() || defaultHumanInteraction();
		const resolvedBindHumanInteractions = bindHumanInteractions || definition.bindHumanInteractions || (() => {});
		const resolvedApplyState = applyState || definition.applyState || (() => {});
		const resolvedMount = mount || definition.mount || (() => null);
		const resolvedUpdate = update || definition.update || (() => null);
		const resolvedDispose = dispose || definition.dispose || (() => {});
		const resolvedReadSelection = readSelection || definition.readSelection || (() => null);
		const resolvedReadViewport = readViewport || definition.readViewport || (() => null);
		return {
			kind: definition.kind,
			provider: definition.provider || "custom",
			providerCapabilities: {
				...defaultProviderCapabilities(),
				...definition.providerCapabilities || {}
			},
			widgetRef,
			dataRef,
			metadata: metadata || {},
			getDescription,
			getState,
			buildActionDescriptors(args) {
				return definition.buildActionDescriptors?.(args) || [];
			},
			buildPerceptionDescriptors(args) {
				return definition.buildPerceptionDescriptors?.(args) || [];
			},
			registerActions(router) {
				definition.registerActions?.(router);
			},
			registerPerceptionQueries(registry) {
				definition.registerPerceptionQueries?.(registry);
			},
			bindHumanInteractions: resolvedBindHumanInteractions,
			applyState(args) {
				return resolvedApplyState(normalizeApplyStateArgs(args));
			},
			mount(args = {}) {
				return resolvedMount(args);
			},
			update(args = {}) {
				return resolvedUpdate(args);
			},
			dispose(args = {}) {
				return resolvedDispose(args);
			},
			readSelection(args = {}) {
				return resolvedReadSelection(args);
			},
			readViewport(args = {}) {
				return resolvedReadViewport(args);
			},
			describeCapabilities() {
				return {
					provider: definition.provider || "custom",
					providerCapabilities: {
						...defaultProviderCapabilities(),
						...definition.providerCapabilities || {}
					}
				};
			},
			getHumanInteractionConfig() {
				return resolvedHumanInteraction;
			}
		};
	}
	function createWidgetAdapterDefinition(definition) {
		if (!definition?.kind || typeof definition.kind !== "string") throw new Error("Widget adapter definition requires a string kind.");
		assertFunction("buildActionDescriptors", definition?.buildActionDescriptors);
		assertFunction("buildPerceptionDescriptors", definition?.buildPerceptionDescriptors);
		assertFunction("getHumanInteractionConfig", definition?.getHumanInteractionConfig);
		assertFunction("registerActions", definition?.registerActions);
		assertFunction("registerPerceptionQueries", definition?.registerPerceptionQueries);
		assertFunction("bindHumanInteractions", definition?.bindHumanInteractions);
		assertFunction("applyState", definition?.applyState);
		assertFunction("mount", definition?.mount);
		assertFunction("update", definition?.update);
		assertFunction("dispose", definition?.dispose);
		assertFunction("readSelection", definition?.readSelection);
		assertFunction("readViewport", definition?.readViewport);
		assertObject("providerCapabilities", definition?.providerCapabilities);
		return {
			provider: "custom",
			providerCapabilities: defaultProviderCapabilities(),
			buildActionDescriptors: () => [],
			buildPerceptionDescriptors: () => [],
			getHumanInteractionConfig: defaultHumanInteraction,
			registerActions: () => {},
			registerPerceptionQueries: () => {},
			bindHumanInteractions: () => () => {},
			applyState: () => {},
			mount: () => null,
			update: () => null,
			dispose: () => {},
			readSelection: () => null,
			readViewport: () => null,
			createInstance(args) {
				return createWidgetAdapterInstance({
					definition,
					humanInteraction: definition.getHumanInteractionConfig?.(),
					bindHumanInteractions: definition.bindHumanInteractions,
					applyState: definition.applyState,
					mount: definition.mount,
					update: definition.update,
					dispose: definition.dispose,
					readSelection: definition.readSelection,
					readViewport: definition.readViewport,
					...args
				});
			},
			...definition
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/actors.js
	function cloneValue$37(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var RUNTIME_ACTORS = [
		"agent",
		"human",
		"system"
	];
	function describeRuntimeActorSchema() {
		return cloneValue$37({
			type: "string",
			enum: RUNTIME_ACTORS
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/queryScope.js
	function cloneValue$36(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeString$6(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function makeQueryScope(scope = {}) {
		const normalizedScope = scope && typeof scope === "object" && !Array.isArray(scope) ? scope : {};
		return {
			widgetRef: normalizeString$6(normalizedScope.widgetRef || normalizedScope.widget_ref),
			dataRef: normalizeString$6(normalizedScope.dataRef || normalizedScope.data_ref),
			selectionRef: normalizeString$6(normalizedScope.selectionRef || normalizedScope.selection_ref),
			focusRef: normalizeString$6(normalizedScope.focusRef || normalizedScope.focus_ref),
			viewportRef: normalizeString$6(normalizedScope.viewportRef || normalizedScope.viewport_ref)
		};
	}
	function describeQueryScopeSchema() {
		return cloneValue$36({
			type: "object",
			additionalProperties: false,
			properties: {
				widgetRef: { type: "string" },
				dataRef: { type: "string" },
				selectionRef: { type: "string" },
				focusRef: { type: "string" },
				viewportRef: { type: "string" }
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/actions.js
	var ACTION_PRIMITIVES = [
		"filter",
		"sort",
		"aggregate",
		"reencode",
		"zoom",
		"select",
		"highlight",
		"navigate",
		"compare",
		"annotate",
		"reset",
		"undo",
		"redo"
	];
	var ACTION_CATEGORIES = [
		"dataTransform",
		"visualMapping",
		"viewTransform",
		"selection",
		"annotation",
		"navigation",
		"coordination"
	];
	var ACTION_EFFECT_KINDS = [
		"updatesSelection",
		"filtersWidget",
		"updatesViewDomain",
		"changesEncoding",
		"highlightsItems"
	];
	function cloneValue$35(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function withOptionalQueryScope$1(paramsSchema) {
		if (!paramsSchema || typeof paramsSchema !== "object" || Array.isArray(paramsSchema)) return {
			type: "object",
			properties: { queryScope: describeQueryScopeSchema() }
		};
		return {
			...cloneValue$35(paramsSchema),
			properties: {
				...cloneValue$35(paramsSchema.properties) || {},
				queryScope: describeQueryScopeSchema()
			}
		};
	}
	function describeActionPrimitiveSchema() {
		return cloneValue$35({
			type: "string",
			enum: ACTION_PRIMITIVES
		});
	}
	function describeActionCategorySchema() {
		return cloneValue$35({
			type: "string",
			enum: ACTION_CATEGORIES
		});
	}
	function makeActionDescriptor(descriptor) {
		const paramsSchema = withOptionalQueryScope$1(descriptor?.paramsSchema);
		return {
			scope: "local",
			supportedWidgetKinds: null,
			affectedRefs: [],
			affectedStatePaths: [],
			effects: [],
			reversible: false,
			preconditions: [],
			postconditions: [],
			examples: [],
			...descriptor,
			paramsSchema
		};
	}
	function describeActionEffectSchema() {
		return cloneValue$35({
			type: "object",
			properties: {
				kind: {
					type: "string",
					enum: ACTION_EFFECT_KINDS
				},
				ref: { type: ["string", "null"] },
				description: { type: "string" }
			}
		});
	}
	function describeActionEffectKindSchema() {
		return cloneValue$35({
			type: "string",
			enum: ACTION_EFFECT_KINDS
		});
	}
	function describeActionConditionSchema() {
		return cloneValue$35({
			type: "object",
			properties: {
				description: { type: "string" },
				checkHint: { type: "string" },
				failureMessage: { type: "string" }
			}
		});
	}
	function describeActionExampleSchema() {
		return cloneValue$35({
			type: "object",
			properties: {
				userGoal: { type: "string" },
				params: { type: "object" }
			}
		});
	}
	function describeActionDescriptorSchema() {
		return cloneValue$35({
			type: "object",
			required: [
				"name",
				"title",
				"description",
				"primitive",
				"category"
			],
			properties: {
				name: { type: "string" },
				title: { type: "string" },
				description: { type: "string" },
				primitive: describeActionPrimitiveSchema(),
				category: describeActionCategorySchema(),
				scope: { type: "string" },
				supportedWidgetKinds: { anyOf: [{ type: "null" }, {
					type: "array",
					items: { type: "string" }
				}] },
				targetRef: { type: ["string", "null"] },
				affectedRefs: {
					type: "array",
					items: { type: "string" }
				},
				affectedStatePaths: {
					type: "array",
					items: { type: "string" }
				},
				paramsSchema: { type: "object" },
				reversible: { type: "boolean" },
				preconditions: {
					type: "array",
					items: describeActionConditionSchema()
				},
				postconditions: {
					type: "array",
					items: describeActionConditionSchema()
				},
				effects: {
					type: "array",
					items: describeActionEffectSchema()
				},
				examples: {
					type: "array",
					items: describeActionExampleSchema()
				}
			}
		});
	}
	function describeActionCallSchema() {
		return cloneValue$35({
			type: "object",
			additionalProperties: false,
			required: ["name", "params"],
			properties: {
				callId: { type: "string" },
				name: { type: "string" },
				actor: describeRuntimeActorSchema(),
				reason: { type: "string" },
				queryScope: describeQueryScopeSchema(),
				params: { type: "object" }
			}
		});
	}
	function makeSelectionEffect(ref, description) {
		return {
			kind: "updatesSelection",
			ref,
			description
		};
	}
	function makeFilterEffect(ref, description) {
		return {
			kind: "filtersWidget",
			ref,
			description
		};
	}
	function makeDomainEffect(ref, description) {
		return {
			kind: "updatesViewDomain",
			ref,
			description
		};
	}
	function makeEncodingEffect(ref, description) {
		return {
			kind: "changesEncoding",
			ref,
			description
		};
	}
	function makeHighlightEffect(ref, description) {
		return {
			kind: "highlightsItems",
			ref,
			description
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/dataHandles.js
	var DATA_QUERY_KINDS = [
		"schema",
		"sampleRows",
		"filter",
		"aggregate",
		"groupBy",
		"sql",
		"summary",
		"computeCorrelation",
		"findExtremes",
		"findOutliers",
		"compareGroups"
	];
	function cloneValue$34(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function describeDataQueryPredicateSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				field: {
					type: "string",
					minLength: 1
				},
				op: {
					type: "string",
					minLength: 1
				},
				value: {}
			},
			required: [
				"field",
				"op",
				"value"
			]
		});
	}
	function withOptionalScopedDataTarget(schema) {
		const nextSchema = cloneValue$34(schema);
		const nextProperties = nextSchema.properties || {};
		return {
			...nextSchema,
			properties: {
				...nextProperties,
				queryScope: describeQueryScopeSchema()
			}
		};
	}
	function describeDataQueryMeasureSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				op: { type: "string" },
				field: { type: "string" },
				as: { type: "string" }
			},
			required: ["op"]
		});
	}
	function describeDataQuerySqlSpecSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				sql: {
					type: "string",
					minLength: 1
				},
				text: {
					type: "string",
					minLength: 1
				}
			},
			oneOf: [{ required: ["sql"] }, { required: ["text"] }]
		});
	}
	function describeDataQuerySummarySpecSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				fields: {
					type: "array",
					items: { type: "string" }
				},
				metrics: {
					type: "array",
					items: { type: "string" }
				},
				groupBy: {
					type: "array",
					items: { type: "string" }
				},
				measures: {
					type: "array",
					items: describeDataQueryMeasureSchema()
				},
				sortBy: {
					type: "object",
					properties: {
						field: { type: "string" },
						order: {
							type: "string",
							enum: ["ascending", "descending"]
						}
					}
				},
				limit: {
					type: "integer",
					minimum: 1,
					maximum: 500
				}
			}
		});
	}
	function describeDataQueryCorrelationSpecSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				xField: {
					type: "string",
					minLength: 1
				},
				yField: {
					type: "string",
					minLength: 1
				}
			},
			required: ["xField", "yField"]
		});
	}
	function describeDataQueryExtremesSpecSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				field: {
					type: "string",
					minLength: 1
				},
				direction: {
					type: "string",
					enum: [
						"min",
						"max",
						"both"
					]
				},
				limit: {
					type: "integer",
					minimum: 1,
					maximum: 200
				}
			},
			required: ["field"]
		});
	}
	function describeDataQueryOutliersSpecSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				field: {
					type: "string",
					minLength: 1
				},
				method: {
					type: "string",
					enum: ["iqr", "zscore"]
				},
				limit: {
					type: "integer",
					minimum: 1,
					maximum: 200
				}
			},
			required: ["field"]
		});
	}
	function describeDataQueryCompareGroupsSpecSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				groupField: {
					type: "string",
					minLength: 1
				},
				valueField: {
					type: "string",
					minLength: 1
				},
				groups: {
					type: "array",
					minItems: 2,
					items: {}
				},
				leftGroup: {},
				rightGroup: {}
			},
			required: ["groupField", "valueField"]
		});
	}
	function describeDataQueryAggregateSpecSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			properties: {
				groupBy: {
					type: "array",
					items: { type: "string" }
				},
				measures: {
					type: "array",
					items: describeDataQueryMeasureSchema()
				},
				metrics: {
					type: "array",
					items: describeDataQueryMeasureSchema()
				}
			}
		});
	}
	function describeDataQueryCallQuerySchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			required: ["kind"],
			properties: {
				kind: {
					type: "string",
					enum: DATA_QUERY_KINDS
				},
				spec: { type: "object" }
			}
		});
	}
	function describeDataRecordSchema() {
		return cloneValue$34({ type: "object" });
	}
	function describeDataSummaryRowSchema() {
		return cloneValue$34({ type: "object" });
	}
	function describeDataGroupComparisonSchema() {
		return cloneValue$34({ type: ["object", "null"] });
	}
	function describeDataSchemaResultSchema() {
		return cloneValue$34({
			type: "object",
			properties: { fields: {
				type: "array",
				items: describeDataFieldSchema()
			} }
		});
	}
	function describeDataRowsResultSchema() {
		return cloneValue$34({
			type: "array",
			items: describeDataRecordSchema()
		});
	}
	function describeDataSummaryTableSchema() {
		return cloneValue$34({
			type: "object",
			properties: { rows: {
				type: "array",
				items: describeDataSummaryRowSchema()
			} }
		});
	}
	function describeDataGroupComparisonResultSchema() {
		return cloneValue$34({
			type: "object",
			properties: {
				groupField: { type: ["string", "null"] },
				valueField: { type: ["string", "null"] },
				groups: {
					type: "array",
					items: describeDataSummaryRowSchema()
				},
				comparison: describeDataGroupComparisonSchema()
			}
		});
	}
	var DATA_QUERY_DESCRIPTOR_TEMPLATES = {
		schema: {
			title: "Inspect data schema",
			description: "Read the exposed field schema for the materialized data view.",
			resultKind: "schema",
			examples: [
				{ spec: {} },
				{ spec: { queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" } } },
				{ spec: { queryScope: {
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
				} } }
			]
		},
		sampleRows: {
			title: "Sample visible rows",
			description: "Read a bounded sample of rows from the shared current view or another materialized data view, optionally scoped to an active selection.",
			resultKind: "rowSample",
			examples: [
				{ spec: { limit: 10 } },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					limit: 10
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					limit: 10
				} }
			]
		},
		filter: {
			title: "Filter current data view",
			description: "Run a predicate filter over the shared current view or another materialized data view, optionally after scoping to an active selection, without mutating workspace state.",
			resultKind: "filteredRows",
			examples: [
				{ spec: { predicates: [{
					field: "Region",
					op: "in",
					value: ["North", "West"]
				}] } },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					predicates: [{
						field: "Region",
						op: "in",
						value: ["North", "West"]
					}]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					predicates: [{
						field: "Region",
						op: "in",
						value: ["North", "West"]
					}]
				} }
			]
		},
		aggregate: {
			title: "Aggregate current data view",
			description: "Aggregate the shared current view or another materialized data view, optionally scoped to an active selection, using group-by keys and measures.",
			resultKind: "aggregateTable",
			examples: [
				{ spec: {
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} }
			]
		},
		groupBy: {
			title: "Group current data view",
			description: "Group rows and compute summary measures over the shared current view or another materialized data view, optionally scoped to an active selection.",
			resultKind: "groupedTable",
			examples: [
				{ spec: {
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} }
			]
		},
		sql: {
			title: "Execute data-view SQL",
			description: "Run a SQL-like query supported by the underlying data engine over the current view or a scoped active selection.",
			resultKind: "queryTable",
			examples: [
				{ spec: { sql: "select Region, count(*) as recordCount from data group by Region" } },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					sql: "select Region, count(*) as recordCount from data group by Region"
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					sql: "select Region, count(*) as recordCount from data group by Region"
				} }
			]
		},
		summary: {
			title: "Summarize current data view",
			description: "Compute a compact summary over the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "summaryTable",
			examples: [
				{ spec: {
					fields: ["LatencyMs"],
					metrics: [
						"min",
						"max",
						"mean"
					]
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					fields: ["LatencyMs"],
					metrics: ["mean"]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					fields: ["LatencyMs"],
					metrics: ["mean"]
				} }
			]
		},
		computeCorrelation: {
			title: "Compute correlation",
			description: "Compute correlation statistics between two quantitative fields over the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "correlationStats",
			examples: [
				{ spec: {
					xField: "LatencyMs",
					yField: "ErrorRate"
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					xField: "LatencyMs",
					yField: "ErrorRate"
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					xField: "LatencyMs",
					yField: "ErrorRate"
				} }
			]
		},
		findExtremes: {
			title: "Find extremes",
			description: "Find records with the minimum, maximum, or both extremes for a field over the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "extremeRows",
			examples: [
				{ spec: {
					field: "Traffic",
					direction: "max",
					limit: 5
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					field: "Traffic",
					direction: "max",
					limit: 5
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					field: "Traffic",
					direction: "max",
					limit: 5
				} }
			]
		},
		findOutliers: {
			title: "Find outliers",
			description: "Find records that are statistically outlying for a field over the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "outlierRows",
			examples: [
				{ spec: {
					field: "ErrorRate",
					method: "iqr",
					limit: 10
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					field: "ErrorRate",
					method: "iqr",
					limit: 10
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					field: "ErrorRate",
					method: "iqr",
					limit: 10
				} }
			]
		},
		compareGroups: {
			title: "Compare groups",
			description: "Compare two groups over a quantitative value field in the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "groupComparison",
			examples: [
				{ spec: {
					groupField: "Region",
					valueField: "LatencyMs",
					groups: ["North", "South"]
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					groupField: "Region",
					valueField: "LatencyMs",
					groups: ["North", "South"]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					groupField: "Region",
					valueField: "LatencyMs",
					groups: ["North", "South"]
				} }
			]
		}
	};
	var DATA_QUERY_RESULT_SCHEMAS = {
		schema: describeDataSchemaResultSchema(),
		sampleRows: describeDataRowsResultSchema(),
		filter: {
			type: "object",
			properties: {
				rows: describeDataRowsResultSchema(),
				rowCount: { type: "integer" },
				predicates: {
					type: "array",
					items: describeDataQueryPredicateSchema()
				}
			}
		},
		aggregate: describeDataSummaryTableSchema(),
		groupBy: describeDataSummaryTableSchema(),
		sql: describeDataRowsResultSchema(),
		summary: describeDataSummaryTableSchema(),
		computeCorrelation: {
			type: "object",
			properties: {
				xField: { type: "string" },
				yField: { type: "string" },
				correlation: { type: ["number", "null"] },
				sampleSize: { type: "integer" }
			}
		},
		findExtremes: describeDataSummaryTableSchema(),
		findOutliers: describeDataSummaryTableSchema(),
		compareGroups: describeDataGroupComparisonResultSchema()
	};
	var DATA_QUERY_SCHEMAS = {
		schema: withOptionalScopedDataTarget({
			type: "object",
			additionalProperties: false,
			properties: {}
		}),
		sampleRows: withOptionalScopedDataTarget({
			type: "object",
			additionalProperties: false,
			properties: { limit: {
				type: "integer",
				minimum: 1,
				maximum: 500
			} }
		}),
		filter: withOptionalScopedDataTarget({
			type: "object",
			additionalProperties: false,
			properties: { predicates: {
				type: "array",
				minItems: 1,
				items: describeDataQueryPredicateSchema()
			} },
			required: ["predicates"]
		}),
		aggregate: withOptionalScopedDataTarget(describeDataQueryAggregateSpecSchema()),
		groupBy: withOptionalScopedDataTarget(describeDataQueryAggregateSpecSchema()),
		sql: withOptionalScopedDataTarget(describeDataQuerySqlSpecSchema()),
		summary: withOptionalScopedDataTarget(describeDataQuerySummarySpecSchema()),
		computeCorrelation: withOptionalScopedDataTarget(describeDataQueryCorrelationSpecSchema()),
		findExtremes: withOptionalScopedDataTarget(describeDataQueryExtremesSpecSchema()),
		findOutliers: withOptionalScopedDataTarget(describeDataQueryOutliersSpecSchema()),
		compareGroups: withOptionalScopedDataTarget(describeDataQueryCompareGroupsSpecSchema())
	};
	function makeDataHandle(handle) {
		return {
			sourceKind: "inline",
			schema: { fields: [] },
			stats: {},
			kind: "dataView",
			scope: "workspace",
			widgetRef: void 0,
			sourceSelectionRef: void 0,
			supportedQueries: ["sampleRows", "summary"],
			supportedQueryDescriptors: [],
			...handle
		};
	}
	function makeDataQueryDescriptor(descriptor) {
		return {
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {}
			},
			resultSchema: descriptor?.resultSchema || (descriptor?.name ? DATA_QUERY_RESULT_SCHEMAS[descriptor.name] : void 0),
			examples: [],
			...descriptor
		};
	}
	function describeDataFieldSchema() {
		return cloneValue$34({
			type: "object",
			required: ["name", "type"],
			properties: {
				name: { type: "string" },
				type: { type: "string" },
				nullable: { type: "boolean" },
				description: { type: "string" }
			}
		});
	}
	function describeDataQueryDescriptorSchema() {
		return cloneValue$34({
			type: "object",
			required: [
				"name",
				"title",
				"description",
				"resultKind"
			],
			properties: {
				name: {
					type: "string",
					enum: DATA_QUERY_KINDS
				},
				title: { type: "string" },
				description: { type: "string" },
				resultKind: { type: "string" },
				inputSchema: { type: "object" },
				resultSchema: { type: [
					"object",
					"array",
					"null"
				] },
				examples: {
					type: "array",
					items: {
						type: "object",
						properties: { spec: { type: "object" } }
					}
				}
			}
		});
	}
	function describeDataHandleSchema() {
		return cloneValue$34({
			type: "object",
			required: [
				"ref",
				"title",
				"sourceKind",
				"supportedQueries"
			],
			properties: {
				ref: { type: "string" },
				title: { type: "string" },
				description: { type: "string" },
				sourceKind: { type: "string" },
				kind: { type: "string" },
				scope: { type: "string" },
				widgetRef: { type: "string" },
				sourceSelectionRef: { type: "string" },
				schema: { ...describeDataSchemaResultSchema() },
				stats: {
					type: "object",
					properties: {
						rowCount: { type: "integer" },
						visibleCount: { type: "integer" },
						selectedCount: { type: "integer" }
					}
				},
				supportedQueries: {
					type: "array",
					items: {
						type: "string",
						enum: DATA_QUERY_KINDS
					}
				},
				supportedQueryDescriptors: {
					type: "array",
					items: describeDataQueryDescriptorSchema()
				}
			}
		});
	}
	function describeDataQueryCallSchema() {
		return cloneValue$34({
			type: "object",
			additionalProperties: false,
			required: ["query"],
			properties: {
				callId: { type: "string" },
				actor: describeRuntimeActorSchema(),
				dataRef: { type: "string" },
				query: describeDataQueryCallQuerySchema()
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/refs.js
	var REF_KINDS = [
		"widget",
		"selection",
		"data",
		"link",
		"action"
	];
	var REF_PATTERN = "^wl:\\/\\/[^/]+\\/workspace\\/[^/]+\\/.+$";
	function makeRef({ appId = "widgetva-app", workspaceId = "main", path }) {
		return `wl://${appId}/workspace/${workspaceId}/${String(path || "").replace(/^\/+/, "")}`;
	}
	function makeWidgetRef({ appId = "widgetva-app", workspaceId = "main", widgetId }) {
		return makeRef({
			appId,
			workspaceId,
			path: `widget/${widgetId}`
		});
	}
	function makeSelectionRef({ appId = "widgetva-app", workspaceId = "main", widgetId, selectionId }) {
		return makeRef({
			appId,
			workspaceId,
			path: `widget/${widgetId}/selection/${selectionId}`
		});
	}
	function makeDataRef({ appId = "widgetva-app", workspaceId = "main", dataId }) {
		return makeRef({
			appId,
			workspaceId,
			path: `data/${dataId}`
		});
	}
	function makeCurrentViewDataRef({ appId = "widgetva-app", workspaceId = "main" } = {}) {
		return makeDataRef({
			appId,
			workspaceId,
			dataId: "current_view"
		});
	}
	function makeCurrentSelectionDataRef({ appId = "widgetva-app", workspaceId = "main" } = {}) {
		return makeDataRef({
			appId,
			workspaceId,
			dataId: "current_selection"
		});
	}
	function makeWidgetSelectionDataRef({ appId = "widgetva-app", workspaceId = "main", widgetId }) {
		return makeDataRef({
			appId,
			workspaceId,
			dataId: `${widgetId}_selection`
		});
	}
	function makeSelectionScopedDataRef({ appId = "widgetva-app", workspaceId = "main", widgetId, selectionId }) {
		return makeDataRef({
			appId,
			workspaceId,
			dataId: `${widgetId}_selection_${selectionId}`
		});
	}
	function makeLinkRef({ appId = "widgetva-app", workspaceId = "main", linkId }) {
		return makeRef({
			appId,
			workspaceId,
			path: `link/${linkId}`
		});
	}
	function parseRef(ref) {
		const match = String(ref || "").match(/^wl:\/\/([^/]+)\/workspace\/([^/]+)\/(.+)$/);
		if (!match) return null;
		const [, appId, workspaceId, path] = match;
		const segments = path.split("/").filter(Boolean);
		return {
			appId,
			workspaceId,
			kind: segments.includes("selection") ? "selection" : segments[0] || null,
			path,
			localId: segments[segments.length - 1] || null,
			segments,
			widgetId: segments[0] === "widget" ? segments[1] || null : null,
			selectionId: segments.includes("selection") ? segments[segments.indexOf("selection") + 1] || null : null,
			dataId: segments[0] === "data" ? segments[1] || null : null,
			linkId: segments[0] === "link" ? segments[1] || null : null,
			actionId: segments[0] === "action" ? segments[1] || null : null
		};
	}
	function cloneValue$33(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function describeRefSchema() {
		return cloneValue$33({
			type: "string",
			pattern: REF_PATTERN
		});
	}
	function describeRefPartsSchema() {
		return cloneValue$33({
			type: "object",
			required: [
				"appId",
				"workspaceId",
				"kind",
				"path",
				"localId",
				"segments"
			],
			properties: {
				appId: { type: "string" },
				workspaceId: { type: "string" },
				kind: {
					type: "string",
					enum: REF_KINDS
				},
				path: { type: "string" },
				localId: { type: ["string", "null"] },
				segments: {
					type: "array",
					items: { type: "string" }
				},
				widgetId: { type: ["string", "null"] },
				selectionId: { type: ["string", "null"] },
				dataId: { type: ["string", "null"] },
				linkId: { type: ["string", "null"] },
				actionId: { type: ["string", "null"] }
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/interactionTrace.js
	function cloneValue$32(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var INTERACTION_TRACE_EVENT_KINDS = [
		"action",
		"perceptionQuery",
		"dataQuery",
		"systemTransition"
	];
	var INTERACTION_TRACE_QUERY_EVENT_KINDS = ["perceptionQuery", "dataQuery"];
	var INTERACTION_TRACE_EVENT_FAMILIES = [
		"action",
		"query",
		"systemTransition"
	];
	var INTERACTION_TRACE_QUERY_SURFACES = ["perception", "data"];
	function describeInteractionTraceActorSchema() {
		return describeRuntimeActorSchema();
	}
	function describeInteractionTraceEventKindSchema() {
		return cloneValue$32({
			type: "string",
			enum: INTERACTION_TRACE_EVENT_KINDS
		});
	}
	function describeInteractionTraceEventFamilySchema() {
		return cloneValue$32({
			type: "string",
			enum: INTERACTION_TRACE_EVENT_FAMILIES
		});
	}
	function describeInteractionTraceQuerySurfaceSchema() {
		return cloneValue$32({
			type: "string",
			enum: INTERACTION_TRACE_QUERY_SURFACES
		});
	}
	function getInteractionTraceEventFamily(recordOrEventKind) {
		const eventKind = typeof recordOrEventKind === "string" ? recordOrEventKind : recordOrEventKind?.eventKind || null;
		if (INTERACTION_TRACE_QUERY_EVENT_KINDS.includes(eventKind)) return "query";
		if (eventKind === "systemTransition") return "systemTransition";
		return "action";
	}
	function getInteractionTraceQuerySurface(recordOrEventKind) {
		const eventKind = typeof recordOrEventKind === "string" ? recordOrEventKind : recordOrEventKind?.eventKind || null;
		if (eventKind === "perceptionQuery") return "perception";
		if (eventKind === "dataQuery") return "data";
		return null;
	}
	function makeInteractionTraceRecord(record) {
		const hasAction = !!record?.action;
		const hasQuery = !!record?.query;
		const eventKind = record?.eventKind || (hasAction ? "action" : hasQuery ? "perceptionQuery" : "systemTransition");
		const eventFamily = record?.eventFamily || getInteractionTraceEventFamily(eventKind);
		const querySurface = record?.querySurface || getInteractionTraceQuerySurface(eventKind);
		return {
			timestamp: (/* @__PURE__ */ new Date()).toISOString(),
			actor: RUNTIME_ACTORS[0],
			eventKind,
			eventFamily,
			querySurface,
			affectedRefs: [],
			parentStateId: null,
			branchId: null,
			primitive: null,
			statePatch: {},
			notes: {},
			...record
		};
	}
	function describeInteractionTraceNotesSchema() {
		return cloneValue$32({
			type: "object",
			properties: {
				outcome: {
					type: "string",
					enum: ["success", "failure"]
				},
				errorCode: { type: "string" },
				errorMessage: { type: "string" },
				details: {},
				recoveryHints: {
					type: "array",
					items: { type: "string" }
				},
				rationale: { type: "string" },
				verification: { type: "string" },
				userVisibleSummary: { type: "string" }
			}
		});
	}
	function describeInteractionTraceActionSchema() {
		return cloneValue$32({ anyOf: [{
			type: "object",
			required: ["name"],
			properties: {
				name: { type: "string" },
				targetRef: describeRefSchema(),
				params: { type: "object" },
				actor: describeRuntimeActorSchema(),
				callId: { type: "string" }
			}
		}, describeActionCallSchema()] });
	}
	function describeInteractionTraceQuerySchema() {
		return cloneValue$32({ anyOf: [describeDataQueryCallSchema(), {
			type: "object",
			required: ["name"],
			properties: {
				name: { type: "string" },
				targetRef: describeRefSchema(),
				params: { type: "object" },
				actor: describeRuntimeActorSchema(),
				callId: { type: "string" }
			}
		}] });
	}
	function getInteractionTraceQueryName(query) {
		if (typeof query?.name === "string" && query.name.length > 0) return query.name;
		if (typeof query?.query?.kind === "string" && query.query.kind.length > 0) return `data.${query.query.kind}`;
		return null;
	}
	function describeInteractionTraceRecordSchema() {
		return cloneValue$32({
			type: "object",
			required: [
				"actor",
				"eventFamily",
				"eventKind",
				"affectedRefs",
				"stateId",
				"timestamp"
			],
			properties: {
				stateId: { type: "string" },
				parentStateId: { type: ["string", "null"] },
				branchId: { type: ["string", "null"] },
				actor: describeInteractionTraceActorSchema(),
				eventFamily: describeInteractionTraceEventFamilySchema(),
				eventKind: describeInteractionTraceEventKindSchema(),
				querySurface: { anyOf: [{ type: "null" }, describeInteractionTraceQuerySurfaceSchema()] },
				primitive: { type: ["string", "null"] },
				action: { anyOf: [{ type: "null" }, describeInteractionTraceActionSchema()] },
				query: { anyOf: [{ type: "null" }, describeInteractionTraceQuerySchema()] },
				affectedRefs: {
					type: "array",
					items: describeRefSchema()
				},
				statePatch: { type: "object" },
				timestamp: { type: "string" },
				notes: describeInteractionTraceNotesSchema()
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/state.js
	function cloneValue$31(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var SELECTION_KINDS = [
		"interval",
		"point",
		"category",
		"cell"
	];
	var ENCODING_TYPES = [
		"quantitative",
		"nominal",
		"ordinal",
		"temporal",
		"geo"
	];
	var TRANSFORM_KINDS$1 = [
		"filter",
		"sort",
		"aggregate",
		"derive",
		"sample",
		"syncDomain",
		"highlight"
	];
	var SELECTION_PREDICATE_OPERATIONS = [
		"equals",
		"in",
		"between"
	];
	function makeFieldEncoding(encoding) {
		return {
			field: "",
			type: "nominal",
			aggregate: void 0,
			bin: void 0,
			scale: void 0,
			...encoding
		};
	}
	function makeTransformState(transform) {
		return {
			kind: "derive",
			source: void 0,
			sourceWidgetId: void 0,
			sourceSelectionRef: void 0,
			linkId: void 0,
			spec: {},
			...transform
		};
	}
	function makeViewTransformState(view) {
		return {
			xDomain: void 0,
			yDomain: void 0,
			zoom: void 0,
			sort: void 0,
			highlight: void 0,
			drillDown: void 0,
			aggregate: void 0,
			reencode: void 0,
			annotate: void 0,
			addRemove: void 0,
			navigate: void 0,
			...view
		};
	}
	function makeSelectionState(selection) {
		return {
			selectionRef: null,
			selectionId: null,
			kind: "interval",
			sourceWidgetRef: null,
			sourceWidgetId: null,
			scope: "local",
			selectionDataRef: null,
			fields: [],
			value: {},
			domain: null,
			predicates: [],
			summary: "",
			aggregateName: null,
			...selection
		};
	}
	function makeInteractionFeedbackState(feedback) {
		return {
			hoveredItem: void 0,
			highlightedKeys: [],
			tooltip: void 0,
			inboundLinkIds: [],
			highlightLinkIds: [],
			linkedSourceRefs: [],
			sharedSelectionSourceWidgetId: void 0,
			...feedback
		};
	}
	function makeWorkspaceAnnotation(annotation) {
		return {
			annotationId: "",
			targetRef: void 0,
			kind: "note",
			text: "",
			actor: "agent",
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			...annotation
		};
	}
	function makeWidgetState(state) {
		return {
			version: 1,
			updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			...state,
			data: {
				sourceDataRef: null,
				currentDataRef: null,
				rowCount: 0,
				visibleCount: 0,
				selectedCount: 0,
				...state?.data || {}
			},
			encodings: { ...state?.encodings || {} },
			transforms: Array.isArray(state?.transforms) ? state.transforms.map((item) => makeTransformState(item)) : [],
			view: makeViewTransformState(state?.view),
			selections: Object.fromEntries(Object.entries(state?.selections || {}).map(([ref, selection]) => [ref, makeSelectionState(selection)])),
			feedback: makeInteractionFeedbackState(state?.feedback),
			humanInteraction: state?.humanInteraction || void 0
		};
	}
	function makeWorkspaceState(state) {
		return {
			stateId: "main:empty",
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			widgets: {},
			shared: {
				selections: {
					registry: {},
					views: {
						primary: null,
						byWidget: {}
					}
				},
				focus: null,
				highlight: null,
				viewport: null,
				globalFilters: {},
				focusedWidget: void 0,
				annotations: [],
				links: {
					definitions: [],
					topology: {}
				},
				...state?.shared || {}
			},
			taskContext: state?.taskContext || void 0,
			replayContext: state?.replayContext || void 0,
			delta: state?.delta || void 0,
			...state
		};
	}
	function describeFieldEncodingSchema() {
		return cloneValue$31({
			type: "object",
			required: ["field", "type"],
			properties: {
				field: { type: "string" },
				type: {
					type: "string",
					enum: ENCODING_TYPES
				},
				aggregate: { type: ["string", "null"] },
				bin: { type: ["boolean", "null"] },
				scale: { anyOf: [describeFieldScaleSchema(), { type: "null" }] }
			}
		});
	}
	function describeFieldScaleSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				domain: {},
				range: {},
				clamp: { type: "boolean" },
				nice: { type: "boolean" },
				zero: { type: "boolean" },
				type: { type: "string" }
			}
		});
	}
	function describeSelectionDomainAxisSchema() {
		return cloneValue$31({
			type: "array",
			minItems: 2,
			maxItems: 2,
			items: { anyOf: [
				{ type: "number" },
				{ type: "string" },
				{ type: "null" }
			] }
		});
	}
	function describeViewportStateSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				sourceWidgetRef: { type: ["string", "null"] },
				xDomain: { anyOf: [describeSelectionDomainAxisSchema(), { type: "null" }] },
				yDomain: { anyOf: [describeSelectionDomainAxisSchema(), { type: "null" }] },
				zoom: { anyOf: [{
					type: "object",
					properties: {
						level: { type: "number" },
						center: {
							type: "array",
							items: { type: "number" }
						}
					}
				}, { type: "null" }] }
			}
		});
	}
	function describeFocusStateSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				widgetRef: { type: ["string", "null"] },
				widgetId: { type: ["string", "null"] },
				source: { type: "string" }
			}
		});
	}
	function describeHighlightStateSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				entries: {
					type: "array",
					items: {
						type: "object",
						properties: {
							widgetRef: { type: ["string", "null"] },
							widgetId: { type: ["string", "null"] },
							highlightedKeys: {
								type: "array",
								items: {}
							},
							inboundLinkIds: {
								type: "array",
								items: { type: "string" }
							},
							highlightLinkIds: {
								type: "array",
								items: { type: "string" }
							},
							linkedSourceRefs: {
								type: "array",
								items: { type: "string" }
							}
						}
					}
				},
				activeWidgetRefs: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function describeSelectionDomainSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				xDomain: describeSelectionDomainAxisSchema(),
				yDomain: describeSelectionDomainAxisSchema()
			}
		});
	}
	function describeSelectionPredicateSchema() {
		return cloneValue$31({
			type: "object",
			required: ["field", "op"],
			properties: {
				field: { type: "string" },
				op: {
					type: "string",
					enum: SELECTION_PREDICATE_OPERATIONS
				},
				value: {}
			}
		});
	}
	function describeSelectionValueSchema() {
		return cloneValue$31({ type: "object" });
	}
	function describeTransformStateSchema() {
		return cloneValue$31({
			type: "object",
			required: ["kind", "spec"],
			properties: {
				kind: {
					type: "string",
					enum: TRANSFORM_KINDS$1
				},
				source: { type: ["string", "null"] },
				sourceWidgetId: { type: ["string", "null"] },
				sourceSelectionRef: { type: ["string", "null"] },
				linkId: { type: ["string", "null"] },
				spec: { type: "object" }
			}
		});
	}
	function describeViewZoomSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				level: { type: "number" },
				center: {
					type: "array",
					minItems: 2,
					maxItems: 2,
					items: { type: "number" }
				}
			}
		});
	}
	function describeViewSortSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				field: { type: "string" },
				order: {
					type: "string",
					enum: ["ascending", "descending"]
				}
			}
		});
	}
	function describeViewTransformStateSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				xDomain: { anyOf: [
					describeSelectionDomainAxisSchema(),
					{
						type: "array",
						items: { anyOf: [
							{ type: "number" },
							{ type: "string" },
							{ type: "null" }
						] }
					},
					{ type: "null" }
				] },
				yDomain: { anyOf: [
					describeSelectionDomainAxisSchema(),
					{
						type: "array",
						items: { anyOf: [
							{ type: "number" },
							{ type: "string" },
							{ type: "null" }
						] }
					},
					{ type: "null" }
				] },
				zoom: { anyOf: [describeViewZoomSchema(), { type: "null" }] },
				sort: { anyOf: [describeViewSortSchema(), { type: "null" }] }
			}
		});
	}
	function describeSelectionStateSchema() {
		return cloneValue$31({
			type: "object",
			required: ["kind"],
			properties: {
				selectionRef: { type: ["string", "null"] },
				selectionId: { type: ["string", "null"] },
				kind: {
					type: "string",
					enum: SELECTION_KINDS
				},
				sourceWidgetRef: { type: ["string", "null"] },
				sourceWidgetId: { type: ["string", "null"] },
				scope: { type: "string" },
				selectionDataRef: { type: ["string", "null"] },
				fields: {
					type: "array",
					items: { type: "string" }
				},
				value: describeSelectionValueSchema(),
				domain: { anyOf: [describeSelectionDomainSchema(), { type: "null" }] },
				predicates: {
					type: "array",
					items: describeSelectionPredicateSchema()
				},
				summary: { type: "string" },
				keyField: { type: "string" },
				keys: {
					type: "array",
					items: { type: ["string", "number"] }
				},
				field: { type: "string" },
				values: {
					type: "array",
					items: { type: ["string", "number"] }
				}
			}
		});
	}
	function describeInteractionTooltipSchema() {
		return cloneValue$31({ type: ["object", "null"] });
	}
	function describeInteractionHoveredItemSchema() {
		return cloneValue$31({ type: ["object", "null"] });
	}
	function describeInteractionFeedbackStateSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				hoveredItem: describeInteractionHoveredItemSchema(),
				highlightedKeys: {
					type: "array",
					items: { type: ["string", "number"] }
				},
				tooltip: describeInteractionTooltipSchema(),
				inboundLinkIds: {
					type: "array",
					items: { type: "string" }
				},
				highlightLinkIds: {
					type: "array",
					items: { type: "string" }
				},
				linkedSourceRefs: {
					type: "array",
					items: { type: "string" }
				},
				sharedSelectionSourceWidgetId: { type: ["string", "null"] }
			}
		});
	}
	function describeWidgetHumanInteractionStateSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				mode: { type: "string" },
				actionName: { type: ["string", "null"] },
				supportsDirectManipulation: { type: "boolean" }
			}
		});
	}
	function describeWidgetDataStateSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				sourceDataRef: { type: ["string", "null"] },
				currentDataRef: { type: ["string", "null"] },
				rowCount: { type: "number" },
				visibleCount: { type: "number" },
				selectedCount: { type: "number" }
			}
		});
	}
	function describeWorkspaceAnnotationSchema() {
		return cloneValue$31({
			type: "object",
			required: [
				"annotationId",
				"kind",
				"text",
				"actor",
				"createdAt"
			],
			properties: {
				annotationId: { type: "string" },
				targetRef: { type: ["string", "null"] },
				kind: { type: "string" },
				text: { type: "string" },
				actor: describeRuntimeActorSchema(),
				createdAt: { type: "string" }
			}
		});
	}
	function describeWidgetEncodingsStateSchema() {
		return cloneValue$31({
			type: "object",
			additionalProperties: describeFieldEncodingSchema()
		});
	}
	function describeWidgetSelectionsStateSchema() {
		return cloneValue$31({
			type: "object",
			additionalProperties: describeSelectionStateSchema()
		});
	}
	function describeWidgetStateSchema() {
		return cloneValue$31({
			type: "object",
			required: [
				"version",
				"updatedAt",
				"data",
				"encodings",
				"transforms",
				"view",
				"selections"
			],
			properties: {
				ref: { type: "string" },
				widgetId: { type: "string" },
				kind: { type: "string" },
				role: { type: "string" },
				version: { type: "integer" },
				updatedAt: { type: "string" },
				data: describeWidgetDataStateSchema(),
				encodings: describeWidgetEncodingsStateSchema(),
				transforms: {
					type: "array",
					items: describeTransformStateSchema()
				},
				view: describeViewTransformStateSchema(),
				selections: describeWidgetSelectionsStateSchema(),
				feedback: describeInteractionFeedbackStateSchema(),
				humanInteraction: { anyOf: [describeWidgetHumanInteractionStateSchema(), { type: "null" }] },
				rawSpec: { type: ["object", "null"] }
			}
		});
	}
	function describeWorkspaceTaskContextSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				taskId: { type: "string" },
				userQuery: { type: "string" },
				taskMode: { type: "string" },
				runMode: { type: "string" },
				coordinationScope: { type: "string" },
				expectedAnswerType: { type: "string" },
				interactionHorizon: { type: "string" },
				evidenceType: { type: "string" },
				complexityBudget: { type: "string" },
				targetWidgetRefs: {
					type: "array",
					items: describeRefSchema()
				}
			}
		});
	}
	function describeWorkspaceReplayContextSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				baselineSpec: { type: ["object", "null"] },
				currentSpec: { type: ["object", "null"] },
				workspaceSpec: { type: ["object", "null"] },
				planningRequest: { type: ["object", "null"] },
				userIntent: { type: ["string", "null"] },
				runMode: { type: ["string", "null"] }
			}
		});
	}
	function describeWorkspaceDeltaSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				baseStateId: { type: "string" },
				changedRefs: {
					type: "array",
					items: { type: "string" }
				},
				removedRefs: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function describeWorkspaceSharedStateSchema() {
		return cloneValue$31({
			type: "object",
			properties: {
				selections: {
					type: "object",
					properties: {
						registry: {
							type: "object",
							additionalProperties: describeSelectionStateSchema()
						},
						views: {
							type: "object",
							properties: {
								primary: { anyOf: [describeSelectionStateSchema(), { type: "null" }] },
								byWidget: {
									type: "object",
									additionalProperties: describeSelectionStateSchema()
								}
							}
						}
					}
				},
				globalFilters: {
					type: "object",
					additionalProperties: {
						type: "array",
						items: describeSelectionPredicateSchema()
					}
				},
				viewport: { anyOf: [describeViewportStateSchema(), { type: "null" }] },
				focus: { anyOf: [describeFocusStateSchema(), { type: "null" }] },
				highlight: { anyOf: [describeHighlightStateSchema(), { type: "null" }] },
				focusedWidget: { anyOf: [describeRefSchema(), { type: "null" }] },
				annotations: {
					type: "array",
					items: describeWorkspaceAnnotationSchema()
				},
				links: {
					type: "object",
					properties: {
						definitions: {
							type: "array",
							items: { type: "object" }
						},
						topology: { type: "object" }
					}
				}
			}
		});
	}
	function describeWorkspaceStateSchema() {
		return cloneValue$31({
			type: "object",
			required: [
				"stateId",
				"createdAt",
				"widgets",
				"shared"
			],
			properties: {
				stateId: { type: "string" },
				createdAt: { type: "string" },
				widgets: {
					type: "object",
					additionalProperties: describeWidgetStateSchema()
				},
				shared: describeWorkspaceSharedStateSchema(),
				taskContext: { anyOf: [describeWorkspaceTaskContextSchema(), { type: "null" }] },
				replayContext: { anyOf: [describeWorkspaceReplayContextSchema(), { type: "null" }] },
				delta: { anyOf: [describeWorkspaceDeltaSchema(), { type: "null" }] }
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/propagationReasons.js
	var PROPAGATION_SKIP_REASONS = [
		"missing_target_ref",
		"manual_activation_policy",
		"missing_target_widget",
		"unsupported_effect",
		"unsupported_advanced_response",
		"advanced_response_params_invalid",
		"advanced_response_verification_unavailable",
		"no_active_selection",
		"mapping_unresolved",
		"effect_not_applied",
		"no_applicable_link"
	];
	function normalizePropagationSkipReason(reason, fallback = "effect_not_applied") {
		return PROPAGATION_SKIP_REASONS.includes(reason) ? reason : fallback;
	}
	var CANONICAL_PROPAGATION_SKIP_REASONS = [
		"noApplicableLink",
		"manualLink",
		"sourceMismatch",
		"mappingUnavailable",
		"mappingFailed",
		"unsupportedEffect",
		"effectDowngradeUnavailable",
		"constraintBlocked"
	];
	var PROPAGATION_SKIP_REASON_ALIASES = {
		missing_target_ref: "sourceMismatch",
		manual_activation_policy: "manualLink",
		missing_target_widget: "sourceMismatch",
		unsupported_effect: "unsupportedEffect",
		unsupported_advanced_response: "unsupportedEffect",
		advanced_response_params_invalid: "mappingFailed",
		advanced_response_verification_unavailable: "mappingFailed",
		no_active_selection: "sourceMismatch",
		mapping_unresolved: "mappingUnavailable",
		effect_not_applied: "mappingFailed",
		no_applicable_link: "noApplicableLink"
	};
	function normalizeCanonicalPropagationSkipReason(reason, fallback = "mappingFailed") {
		if (CANONICAL_PROPAGATION_SKIP_REASONS.includes(reason)) return reason;
		if (typeof reason === "string" && reason in PROPAGATION_SKIP_REASON_ALIASES) return PROPAGATION_SKIP_REASON_ALIASES[reason];
		if (CANONICAL_PROPAGATION_SKIP_REASONS.includes(fallback)) return fallback;
		if (typeof fallback === "string" && fallback in PROPAGATION_SKIP_REASON_ALIASES) return PROPAGATION_SKIP_REASON_ALIASES[fallback];
		return "mappingFailed";
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/results.js
	function cloneValue$30(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function compactObject(value) {
		return Object.fromEntries(Object.entries(value).filter(([, entryValue]) => entryValue !== void 0));
	}
	var RESULT_ERROR_CODES = [
		"UNKNOWN_OPERATION",
		"UNSUPPORTED_TARGET",
		"INVALID_PARAMS",
		"PRECONDITION_FAILED",
		"RUNTIME_ERROR",
		"UNKNOWN_QUERY",
		"UNKNOWN_DATA_REF",
		"UNKNOWN_QUERY_KIND",
		"UNSUPPORTED_QUERY_KIND",
		"INVALID_QUERY_SPEC"
	];
	function describeResultErrorSchema() {
		return cloneValue$30({
			type: "object",
			required: ["code", "message"],
			properties: {
				code: {
					type: "string",
					enum: RESULT_ERROR_CODES
				},
				message: { type: "string" },
				details: {}
			}
		});
	}
	function makeResultError(error) {
		return {
			code: "RUNTIME_ERROR",
			message: "",
			...error
		};
	}
	function makeActionResult(result) {
		return compactObject({
			ok: false,
			callId: "",
			actionName: "unknown",
			updatedRefs: [],
			stateId: null,
			statePatch: {},
			expectedPostconditions: [],
			verificationHints: [],
			...result
		});
	}
	function describeActionResultSchema() {
		return cloneValue$30({
			type: "object",
			required: [
				"ok",
				"callId",
				"actionName"
			],
			properties: {
				ok: { type: "boolean" },
				callId: { type: "string" },
				actionName: { type: "string" },
				updatedRefs: {
					type: "array",
					items: { type: "string" }
				},
				stateId: { type: ["string", "null"] },
				statePatch: { type: "object" },
				result: {},
				expectedPostconditions: {
					type: "array",
					items: describeActionConditionSchema()
				},
				verificationHints: {
					type: "array",
					items: { type: "string" }
				},
				recoveryHints: {
					type: "array",
					items: { type: "string" }
				},
				error: describeResultErrorSchema()
			}
		});
	}
	function makePerceptionResult(result) {
		return compactObject({
			ok: false,
			callId: "",
			queryName: "unknown",
			result: null,
			...result
		});
	}
	function describePerceptionResultSchema() {
		return cloneValue$30({
			type: "object",
			required: [
				"ok",
				"callId",
				"queryName"
			],
			properties: {
				ok: { type: "boolean" },
				callId: { type: "string" },
				queryName: { type: "string" },
				result: {},
				recoveryHints: {
					type: "array",
					items: { type: "string" }
				},
				error: describeResultErrorSchema()
			}
		});
	}
	function makeActionVerificationResult(result) {
		return {
			verified: false,
			matchedStateId: null,
			matchedActionName: null,
			affectedRefs: [],
			missingRefs: [],
			expectedPostconditions: [],
			verificationHints: [],
			traceEvidence: null,
			statePatch: null,
			finalSnapshot: null,
			linkPropagation: [],
			...result
		};
	}
	function describeActionVerificationResultSchema() {
		return cloneValue$30({
			type: "object",
			properties: {
				verified: { type: "boolean" },
				matchedStateId: { type: ["string", "null"] },
				matchedActionName: { type: ["string", "null"] },
				affectedRefs: {
					type: "array",
					items: { type: "string" }
				},
				missingRefs: {
					type: "array",
					items: { type: "string" }
				},
				expectedPostconditions: {
					type: "array",
					items: describeActionConditionSchema()
				},
				verificationHints: {
					type: "array",
					items: { type: "string" }
				},
				traceEvidence: { anyOf: [describeInteractionTraceRecordSchema(), { type: "null" }] },
				statePatch: { type: ["object", "null"] },
				finalSnapshot: { anyOf: [describeWorkspaceSnapshotSchema(), { type: "null" }] },
				linkPropagation: {
					type: "array",
					items: describeLinkPropagationEvaluationSchema()
				}
			}
		});
	}
	function makeDataQueryResult(result) {
		return compactObject({
			ok: false,
			dataRef: null,
			result: null,
			...result
		});
	}
	function describeDataQueryResultSchema() {
		return cloneValue$30({
			type: "object",
			required: ["ok", "dataRef"],
			properties: {
				ok: { type: "boolean" },
				dataRef: { type: ["string", "null"] },
				result: {},
				recoveryHints: {
					type: "array",
					items: { type: "string" }
				},
				error: describeResultErrorSchema()
			}
		});
	}
	function makeStateSnapshotMeta(meta) {
		return {
			stateId: "",
			createdAt: null,
			baseStateId: null,
			branchId: null,
			transitionType: "continue",
			branchLabel: null,
			actor: "system",
			changedRefs: [],
			removedRefs: [],
			...meta
		};
	}
	function describeStateSnapshotMetaSchema() {
		return cloneValue$30({
			type: "object",
			required: ["stateId"],
			properties: {
				stateId: { type: "string" },
				createdAt: { type: ["string", "null"] },
				baseStateId: { type: ["string", "null"] },
				branchId: { type: ["string", "null"] },
				transitionType: { type: "string" },
				branchLabel: { type: ["string", "null"] },
				actor: describeRuntimeActorSchema(),
				changedRefs: {
					type: "array",
					items: { type: "string" }
				},
				removedRefs: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function describeWorkspaceSnapshotMetaSchema() {
		return cloneValue$30({
			type: "object",
			properties: {
				stateId: { type: ["string", "null"] },
				parentStateId: { type: ["string", "null"] },
				branchId: { type: ["string", "null"] },
				transitionType: { type: ["string", "null"] },
				branchLabel: { type: ["string", "null"] }
			}
		});
	}
	function describeWorkspaceSnapshotSchema() {
		return cloneValue$30({
			type: "object",
			required: [
				"stateId",
				"createdAt",
				"widgets",
				"shared"
			],
			properties: {
				stateId: { type: "string" },
				createdAt: { type: "string" },
				widgets: { type: "object" },
				shared: { type: "object" },
				taskContext: { type: ["object", "null"] },
				delta: { type: ["object", "null"] },
				replayContext: { anyOf: [describeWorkspaceReplayContextSchema(), { type: "null" }] },
				__meta: { anyOf: [describeWorkspaceSnapshotMetaSchema(), { type: "null" }] }
			}
		});
	}
	function makeBranchSummary(summary) {
		return {
			branchId: "",
			label: "",
			originStateId: null,
			parentBranchId: null,
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			...summary
		};
	}
	function describeBranchSummarySchema() {
		return cloneValue$30({
			type: "object",
			required: [
				"branchId",
				"label",
				"createdAt"
			],
			properties: {
				branchId: { type: "string" },
				label: { type: "string" },
				originStateId: { type: ["string", "null"] },
				parentBranchId: { type: ["string", "null"] },
				createdAt: { type: "string" }
			}
		});
	}
	function makeTraceGraphNode(node) {
		return {
			id: "",
			stateId: "",
			parentStateId: null,
			branchId: null,
			branchLabel: null,
			timestamp: null,
			actor: "system",
			eventFamily: "systemTransition",
			querySurface: null,
			actionName: null,
			queryName: null,
			responseId: null,
			responseActor: null,
			responsePreview: null,
			label: "",
			transitionType: "continue",
			current: false,
			...node
		};
	}
	function describeTraceGraphNodeSchema() {
		return cloneValue$30({
			type: "object",
			required: ["id", "stateId"],
			properties: {
				id: { type: "string" },
				stateId: { type: "string" },
				parentStateId: { type: ["string", "null"] },
				branchId: { type: ["string", "null"] },
				branchLabel: { type: ["string", "null"] },
				timestamp: { type: ["string", "null"] },
				actor: describeRuntimeActorSchema(),
				eventFamily: describeInteractionTraceEventFamilySchema(),
				querySurface: { anyOf: [{ type: "null" }, describeInteractionTraceQuerySurfaceSchema()] },
				actionName: { type: ["string", "null"] },
				queryName: { type: ["string", "null"] },
				responseId: { type: ["string", "null"] },
				responseActor: { anyOf: [describeRuntimeActorSchema(), { type: "null" }] },
				responsePreview: { type: ["string", "null"] },
				label: { type: "string" },
				transitionType: { type: "string" },
				current: { type: "boolean" }
			}
		});
	}
	function makeTraceGraphEdge(edge) {
		return {
			id: "",
			from_id: "",
			to_id: "",
			edge_type: "continue",
			branchId: null,
			label: "continue",
			timestamp: null,
			...edge
		};
	}
	function describeTraceGraphEdgeSchema() {
		return cloneValue$30({
			type: "object",
			required: [
				"id",
				"from_id",
				"to_id"
			],
			properties: {
				id: { type: "string" },
				from_id: { type: "string" },
				to_id: { type: "string" },
				edge_type: { type: "string" },
				branchId: { type: ["string", "null"] },
				label: { type: "string" },
				timestamp: { type: ["string", "null"] }
			}
		});
	}
	function makeTraceGraph(graph) {
		return {
			current_state_id: null,
			current_branch_id: null,
			sinceStateId: null,
			actors: [],
			...graph,
			branches: Array.isArray(graph?.branches) ? graph.branches.map((branch) => makeBranchSummary(branch)) : [],
			nodes: Array.isArray(graph?.nodes) ? graph.nodes.map((node) => makeTraceGraphNode(node)) : [],
			edges: Array.isArray(graph?.edges) ? graph.edges.map((edge) => makeTraceGraphEdge(edge)) : []
		};
	}
	function describeTraceGraphSchema() {
		return cloneValue$30({
			type: "object",
			required: [
				"current_state_id",
				"current_branch_id",
				"branches",
				"nodes",
				"edges"
			],
			properties: {
				current_state_id: { type: ["string", "null"] },
				current_branch_id: { type: ["string", "null"] },
				sinceStateId: { type: ["string", "null"] },
				actors: {
					type: "array",
					items: describeRuntimeActorSchema()
				},
				branches: {
					type: "array",
					items: describeBranchSummarySchema()
				},
				nodes: {
					type: "array",
					items: describeTraceGraphNodeSchema()
				},
				edges: {
					type: "array",
					items: describeTraceGraphEdgeSchema()
				}
			}
		});
	}
	function describeLinkPropagationCheckSchema() {
		return cloneValue$30({
			type: "object",
			required: ["name", "passed"],
			properties: {
				name: { type: "string" },
				passed: { type: "boolean" },
				actual: {},
				expected: {}
			}
		});
	}
	function describeLinkPropagationResultSchema() {
		return cloneValue$30({
			type: "object",
			required: [
				"linkRef",
				"primitive",
				"passed",
				"checks"
			],
			properties: {
				linkRef: { type: "string" },
				primitive: { type: "string" },
				targetRef: { type: ["string", "null"] },
				activationPolicy: { type: "string" },
				effectConstraint: { type: ["string", "null"] },
				responseSpec: { type: ["object", "null"] },
				reason: { type: ["string", "null"] },
				passed: { type: "boolean" },
				checks: {
					type: "array",
					items: describeLinkPropagationCheckSchema()
				}
			}
		});
	}
	function describeLinkPropagationEvaluationSchema() {
		return cloneValue$30({
			type: "object",
			required: [
				"ok",
				"sourceRef",
				"linkCount",
				"passedCount",
				"consistencyScore",
				"results"
			],
			properties: {
				ok: { type: "boolean" },
				sourceRef: { type: ["string", "null"] },
				activeSelection: { anyOf: [describeSelectionStateSchema(), { type: "null" }] },
				linkCount: { type: "integer" },
				passedCount: { type: "integer" },
				consistencyScore: { type: "number" },
				results: {
					type: "array",
					items: describeLinkPropagationResultSchema()
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/perception.js
	var PERCEPTION_CATEGORIES = [
		"inspect",
		"summarize",
		"compute",
		"verify",
		"explain"
	];
	function cloneValue$29(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function withOptionalQueryScope(paramsSchema) {
		if (!paramsSchema || typeof paramsSchema !== "object" || Array.isArray(paramsSchema)) return {
			type: "object",
			properties: { queryScope: describeQueryScopeSchema() }
		};
		return {
			...cloneValue$29(paramsSchema),
			properties: {
				...cloneValue$29(paramsSchema.properties) || {},
				queryScope: describeQueryScopeSchema()
			}
		};
	}
	function describePerceptionCategorySchema() {
		return cloneValue$29({
			type: "string",
			enum: PERCEPTION_CATEGORIES
		});
	}
	function describePerceptionInspectViewConfigResultSchema() {
		return cloneValue$29({
			type: "object",
			properties: {
				ref: { type: ["string", "null"] },
				kind: { type: ["string", "null"] },
				encodings: {
					type: "object",
					additionalProperties: describeFieldEncodingSchema()
				},
				transforms: {
					type: "array",
					items: describeTransformStateSchema()
				},
				view: describeViewTransformStateSchema(),
				selections: {
					type: "object",
					additionalProperties: describeSelectionStateSchema()
				},
				feedback: { anyOf: [describeInteractionFeedbackStateSchema(), { type: "null" }] }
			}
		});
	}
	function describePerceptionInspectVisibleRowsResultSchema() {
		return cloneValue$29({
			type: "object",
			properties: {
				ref: { type: ["string", "null"] },
				dataRef: { type: ["string", "null"] },
				visibleCount: { type: "integer" },
				rows: describeDataRowsResultSchema()
			}
		});
	}
	function describePerceptionSummaryGroupsSchema() {
		return cloneValue$29({
			type: "array",
			items: describeDataSummaryTableSchema().properties.rows.items
		});
	}
	function describePerceptionSummarizeSelectionResultSchema() {
		return cloneValue$29({
			type: "object",
			properties: {
				hasSelection: { type: "boolean" },
				selectionCount: { type: "integer" },
				selectionRefs: {
					type: "array",
					items: { type: "string" }
				},
				dataRef: { type: ["string", "null"] },
				selectedCount: { type: "integer" },
				summary: { type: "string" },
				selectionSummaries: {
					type: "array",
					items: { type: "string" }
				},
				groups: describePerceptionSummaryGroupsSchema(),
				aggregates: describePerceptionSummaryGroupsSchema(),
				predicates: {
					type: "array",
					items: { type: "object" }
				},
				selectionPredicates: {
					type: "array",
					items: {
						type: "array",
						items: { type: "object" }
					}
				}
			}
		});
	}
	function describePerceptionSummarizeVisibleResultSchema() {
		return cloneValue$29({
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				rowCount: { type: "integer" },
				groups: describePerceptionSummaryGroupsSchema(),
				aggregates: describePerceptionSummaryGroupsSchema(),
				summary: { type: ["string", "null"] }
			}
		});
	}
	function describePerceptionVerifyActionEffectParamsSchema() {
		return cloneValue$29({
			type: "object",
			additionalProperties: false,
			properties: {
				actionName: { type: "string" },
				stateId: { type: "string" },
				refs: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function describePerceptionVerifyActionEffectResultSchema() {
		return describeActionVerificationResultSchema();
	}
	var PERCEPTION_RETURN_SCHEMAS = {
		"perception.inspectViewConfig": describePerceptionInspectViewConfigResultSchema(),
		"perception.inspectVisibleRows": describePerceptionInspectVisibleRowsResultSchema(),
		"perception.summarizeSelection": describePerceptionSummarizeSelectionResultSchema(),
		"perception.summarizeVisible": describePerceptionSummarizeVisibleResultSchema(),
		"perception.verifyActionEffect": describePerceptionVerifyActionEffectResultSchema(),
		"perception.computeCorrelation": {
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				xField: { type: "string" },
				yField: { type: "string" },
				correlation: { type: ["number", "null"] },
				sampleSize: { type: "integer" }
			}
		},
		"perception.findExtremes": {
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				field: { type: ["string", "null"] },
				direction: { type: "string" },
				rows: describeDataRowsResultSchema()
			}
		},
		"perception.findOutliers": {
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				field: { type: ["string", "null"] },
				method: { type: "string" },
				rows: describeDataRowsResultSchema()
			}
		},
		"perception.compareGroups": {
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				groupField: { type: ["string", "null"] },
				valueField: { type: ["string", "null"] },
				groups: describeDataGroupComparisonResultSchema().properties.groups,
				comparison: describeDataGroupComparisonResultSchema().properties.comparison
			}
		}
	};
	function makePerceptionDescriptor(descriptor) {
		const returnsSchema = descriptor?.returnsSchema || (descriptor?.name ? PERCEPTION_RETURN_SCHEMAS[descriptor.name] : void 0);
		const paramsSchema = withOptionalQueryScope(descriptor?.paramsSchema || (descriptor?.name === "perception.verifyActionEffect" ? describePerceptionVerifyActionEffectParamsSchema() : void 0));
		return {
			...descriptor,
			targetRef: descriptor?.targetRef ?? null,
			paramsSchema: paramsSchema || withOptionalQueryScope({
				type: "object",
				properties: {}
			}),
			returnsSchema: returnsSchema || void 0,
			sideEffectFree: descriptor?.sideEffectFree !== false,
			evidenceKinds: Array.isArray(descriptor?.evidenceKinds) ? descriptor.evidenceKinds : [],
			verificationTargets: Array.isArray(descriptor?.verificationTargets) ? descriptor.verificationTargets : [],
			examples: Array.isArray(descriptor?.examples) ? descriptor.examples : []
		};
	}
	function describePerceptionExampleSchema() {
		return cloneValue$29({
			type: "object",
			properties: {
				userGoal: { type: "string" },
				params: { type: "object" }
			}
		});
	}
	function describePerceptionDescriptorSchema() {
		return cloneValue$29({
			type: "object",
			required: [
				"name",
				"title",
				"description",
				"category"
			],
			properties: {
				name: { type: "string" },
				title: { type: "string" },
				description: { type: "string" },
				category: describePerceptionCategorySchema(),
				targetRef: { type: ["string", "null"] },
				paramsSchema: { type: "object" },
				returnsSchema: { type: ["object", "null"] },
				sideEffectFree: { type: "boolean" },
				evidenceKinds: {
					type: "array",
					items: { type: "string" }
				},
				verificationTargets: {
					type: "array",
					items: { type: "string" }
				},
				examples: {
					type: "array",
					items: describePerceptionExampleSchema()
				}
			}
		});
	}
	function describePerceptionQueryCallSchema() {
		return cloneValue$29({
			type: "object",
			additionalProperties: false,
			required: ["name"],
			properties: {
				callId: { type: "string" },
				name: { type: "string" },
				actor: describeRuntimeActorSchema(),
				queryScope: describeQueryScopeSchema(),
				params: { type: "object" }
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/widgetLinks.js
	function cloneValue$28(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var WIDGET_LINK_KINDS = [
		"filter",
		"highlight",
		"syncDomain",
		"sharesSelection",
		"drillDown",
		"reencode",
		"aggregate",
		"structure",
		"contains",
		"usesData",
		"filters",
		"highlights",
		"syncsDomain",
		"comparesWith",
		"derivesFrom"
	];
	var WIDGET_LINK_KIND_ALIASES = {
		filters: "filter",
		highlights: "highlight",
		syncsDomain: "syncDomain"
	};
	var WIDGET_LINK_EFFECTS = [
		"applyFilter",
		"applyHighlight",
		"focusTarget",
		"syncDomain",
		"shareSelection",
		"transformView",
		"transformDataView",
		"transformStructure",
		"updateData",
		"compare"
	];
	var WIDGET_LINK_ACTIVATION_POLICIES = ["automatic", "manual"];
	var WIDGET_LINK_EFFECT_CONSTRAINTS = ["highlightOnly", "focusOnly"];
	var WIDGET_LINK_ADVANCED_RESPONSE_KINDS = [
		"drillDown",
		"reencode",
		"aggregate",
		"expand",
		"collapse"
	];
	function describeWidgetLinkKindSchema() {
		return cloneValue$28({
			type: "string",
			enum: WIDGET_LINK_KINDS
		});
	}
	function describeWidgetLinkEffectSchema() {
		return cloneValue$28({
			type: ["string", "null"],
			enum: [...WIDGET_LINK_EFFECTS, null]
		});
	}
	function describeWidgetLinkActivationPolicySchema() {
		return cloneValue$28({
			type: "string",
			enum: WIDGET_LINK_ACTIVATION_POLICIES
		});
	}
	function describeWidgetLinkEffectConstraintSchema() {
		return cloneValue$28({
			type: ["string", "null"],
			enum: [...WIDGET_LINK_EFFECT_CONSTRAINTS, null]
		});
	}
	function describeWidgetLinkAdvancedResponseSchema() {
		return cloneValue$28({
			type: "object",
			properties: {
				kind: {
					type: "string",
					enum: WIDGET_LINK_ADVANCED_RESPONSE_KINDS
				},
				params: { type: "object" },
				verificationHints: { type: "object" }
			}
		});
	}
	function readCompatibilityActivationPolicy(link) {
		const explicitActivationPolicy = typeof link?.activationPolicy === "string" ? link.activationPolicy : null;
		const explicitPropagationPolicy = typeof link?.propagationPolicy === "string" ? link.propagationPolicy : null;
		const explicitAutomatic = typeof link?.automatic === "boolean" ? link.automatic : null;
		if (WIDGET_LINK_ACTIVATION_POLICIES.includes(explicitActivationPolicy)) return explicitActivationPolicy;
		if (explicitPropagationPolicy === "manual") return "manual";
		if (explicitAutomatic === false) return "manual";
		return "automatic";
	}
	function readCompatibilityEffectConstraint(link) {
		const explicitEffectConstraint = typeof link?.effectConstraint === "string" ? link.effectConstraint : null;
		const explicitPropagationPolicy = typeof link?.propagationPolicy === "string" ? link.propagationPolicy : null;
		if (WIDGET_LINK_EFFECT_CONSTRAINTS.includes(explicitEffectConstraint)) return explicitEffectConstraint;
		if (explicitPropagationPolicy === "highlightOnly" || explicitPropagationPolicy === "focusOnly") return explicitPropagationPolicy;
		return null;
	}
	function makeWidgetLink(link) {
		const { propagationPolicy: _propagationPolicy, automatic: _automatic, trigger: _trigger, ...publicInput } = link || {};
		const explicitPrimitive = typeof link?.primitive === "string" ? link.primitive : null;
		const normalizedKind = WIDGET_LINK_KIND_ALIASES[link?.kind] || WIDGET_LINK_KIND_ALIASES[explicitPrimitive] || link?.kind || explicitPrimitive || null;
		const normalizedPrimitive = explicitPrimitive || normalizedKind;
		const normalizedActivationPolicy = readCompatibilityActivationPolicy(link);
		const normalizedEffectConstraint = readCompatibilityEffectConstraint(link);
		const explicitEffect = typeof link?.effect === "string" ? link.effect : null;
		const inferredEffect = normalizedPrimitive === "filter" ? "applyFilter" : normalizedPrimitive === "highlight" ? "applyHighlight" : normalizedPrimitive === "syncDomain" ? "syncDomain" : normalizedPrimitive === "sharesSelection" ? "shareSelection" : normalizedPrimitive === "drillDown" || normalizedPrimitive === "reencode" ? "transformView" : normalizedPrimitive === "aggregate" ? "transformDataView" : normalizedPrimitive === "structure" ? "transformStructure" : null;
		return {
			effect: inferredEffect,
			fieldMapping: [],
			activationPolicy: "automatic",
			effectConstraint: null,
			responseSpec: null,
			...publicInput,
			kind: normalizedKind,
			primitive: normalizedPrimitive,
			effect: explicitEffect || inferredEffect,
			activationPolicy: normalizedActivationPolicy,
			effectConstraint: normalizedEffectConstraint,
			responseSpec: link?.responseSpec && typeof link.responseSpec === "object" && !Array.isArray(link.responseSpec) ? cloneValue$28(link.responseSpec) : null
		};
	}
	function normalizeWidgetLink(link) {
		return makeWidgetLink(link);
	}
	function stripWidgetLinkCompatibilityFields(link) {
		if (!link || typeof link !== "object" || Array.isArray(link)) return link;
		const { propagationPolicy: _propagationPolicy, trigger: _trigger, automatic: _automatic, ...rest } = link;
		return rest;
	}
	function describeWidgetLinkSchema() {
		return cloneValue$28({
			type: "object",
			required: ["ref"],
			properties: {
				ref: describeRefSchema(),
				kind: describeWidgetLinkKindSchema(),
				from: { anyOf: [describeRefSchema(), { type: "null" }] },
				to: { anyOf: [describeRefSchema(), { type: "null" }] },
				sourceWidgetId: { type: "string" },
				targetWidgetId: { type: "string" },
				sourceRef: { anyOf: [describeRefSchema(), { type: "null" }] },
				targetRef: { anyOf: [describeRefSchema(), { type: "null" }] },
				sourceDataRef: { anyOf: [describeRefSchema(), { type: "null" }] },
				targetDataRef: { anyOf: [describeRefSchema(), { type: "null" }] },
				primitive: { type: ["string", "null"] },
				effect: describeWidgetLinkEffectSchema(),
				description: { type: "string" },
				fieldMapping: {
					type: "array",
					items: {
						type: "object",
						properties: {
							sourceField: { type: "string" },
							targetField: { type: "string" }
						}
					}
				},
				activationPolicy: describeWidgetLinkActivationPolicySchema(),
				effectConstraint: describeWidgetLinkEffectConstraintSchema(),
				responseSpec: { anyOf: [describeWidgetLinkAdvancedResponseSchema(), { type: "null" }] }
			}
		});
	}
	function describeWorkspaceTopologySummarySchema() {
		return cloneValue$28({
			type: "object",
			required: [
				"topology",
				"topologyLabel",
				"widgetCount",
				"edgeCount",
				"linkDensity",
				"sourceWidgetCount",
				"targetWidgetCount",
				"maxOutDegree",
				"maxInDegree",
				"rationale"
			],
			properties: {
				topology: { type: "string" },
				topologyLabel: { type: "string" },
				widgetCount: { type: "integer" },
				edgeCount: { type: "integer" },
				linkDensity: { type: "number" },
				sourceWidgetCount: { type: "integer" },
				targetWidgetCount: { type: "integer" },
				maxOutDegree: { type: "integer" },
				maxInDegree: { type: "integer" },
				rationale: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function makeWorkspaceTopologySummary(summary = {}) {
		return {
			topology: summary?.topology || "",
			topologyLabel: summary?.topologyLabel || "",
			widgetCount: summary?.widgetCount || 0,
			edgeCount: summary?.edgeCount || 0,
			linkDensity: summary?.linkDensity || 0,
			sourceWidgetCount: summary?.sourceWidgetCount || 0,
			targetWidgetCount: summary?.targetWidgetCount || 0,
			maxOutDegree: summary?.maxOutDegree || 0,
			maxInDegree: summary?.maxInDegree || 0,
			rationale: Array.isArray(summary?.rationale) ? [...summary.rationale] : []
		};
	}
	function makeLinkEnginePrimitiveEntry(entry = {}) {
		return {
			name: entry?.name || "",
			appliedStatePaths: Array.isArray(entry?.appliedStatePaths) ? [...entry.appliedStatePaths] : []
		};
	}
	function makeLinkEngineCapabilities(capabilities = {}) {
		return {
			propagationExecution: false,
			propagationPlan: false,
			effectCollection: false,
			consistencyEvaluation: false,
			...capabilities
		};
	}
	function makeLinkEngineSummary(summary = {}) {
		return {
			primitiveCount: summary?.primitiveCount || 0,
			primitives: Array.isArray(summary?.primitives) ? summary.primitives.map((entry) => makeLinkEnginePrimitiveEntry(entry)) : [],
			linkCount: summary?.linkCount || 0,
			coordinationLinkCount: summary?.coordinationLinkCount || 0,
			structuralLinkCount: summary?.structuralLinkCount || 0,
			automaticLinkCount: summary?.automaticLinkCount || 0,
			manualLinkCount: summary?.manualLinkCount || 0,
			topology: makeWorkspaceTopologySummary(summary?.topology),
			capabilities: makeLinkEngineCapabilities(summary?.capabilities)
		};
	}
	function describeLinkEngineSummarySchema() {
		return cloneValue$28({
			type: "object",
			required: [
				"primitiveCount",
				"primitives",
				"linkCount",
				"coordinationLinkCount",
				"structuralLinkCount",
				"automaticLinkCount",
				"manualLinkCount",
				"topology",
				"capabilities"
			],
			properties: {
				primitiveCount: { type: "integer" },
				primitives: {
					type: "array",
					items: {
						type: "object",
						required: ["name"],
						properties: {
							name: { type: "string" },
							appliedStatePaths: {
								type: "array",
								items: { type: "string" }
							}
						}
					}
				},
				linkCount: { type: "integer" },
				coordinationLinkCount: { type: "integer" },
				structuralLinkCount: { type: "integer" },
				automaticLinkCount: { type: "integer" },
				manualLinkCount: { type: "integer" },
				topology: describeWorkspaceTopologySummarySchema(),
				capabilities: {
					type: "object",
					properties: {
						propagationExecution: { type: "boolean" },
						propagationPlan: { type: "boolean" },
						effectCollection: { type: "boolean" },
						consistencyEvaluation: { type: "boolean" }
					}
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/planning.js
	function cloneValue$27(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeWorkspacePlanningDataField(field = {}) {
		return {
			name: "",
			type: "",
			...field
		};
	}
	function makeWorkspacePlanningDatasetSchema(schema = {}) {
		return {
			fields: Array.isArray(schema?.fields) ? schema.fields.map((field) => makeWorkspacePlanningDataField(field)) : [],
			...schema,
			fields: Array.isArray(schema?.fields) ? schema.fields.map((field) => makeWorkspacePlanningDataField(field)) : []
		};
	}
	var WORKSPACE_PLANNING_TOPOLOGIES = [
		"T1",
		"T2",
		"T3",
		"T4",
		"T5",
		"T6"
	];
	var WORKSPACE_PLANNING_BUDGETS = [
		"minimal",
		"standard",
		"extended"
	];
	var WORKSPACE_PLANNING_RUN_MODES = [
		"goal_oriented",
		"open_ended",
		"autonomous"
	];
	var WORKSPACE_PLANNING_TASK_FAMILIES = [
		"lookup",
		"filter",
		"compare",
		"rank",
		"distribution",
		"correlation",
		"outlier",
		"cluster",
		"trend",
		"flow",
		"multiViewCoordination",
		"drillDown"
	];
	var WORKSPACE_PLANNING_ANSWER_TYPES = [
		"exact",
		"bounded",
		"exploratory"
	];
	var WORKSPACE_PLANNING_INTERACTION_HORIZONS = ["single_step", "multi_step"];
	var WORKSPACE_PLANNING_COORDINATION_SCOPES = [
		"single_widget",
		"multi_widget",
		"workspace"
	];
	var WORKSPACE_PLANNING_EVIDENCE_TYPES = [
		"initial_view",
		"interaction_revealed",
		"cross_widget"
	];
	var WORKSPACE_PLAN_SOURCES = [
		"planner",
		"runtime_default",
		"workspace_spec"
	];
	var WORKSPACE_PLAN_MODES = [
		"topology_driven",
		"minimal_default",
		"explicit_spec"
	];
	function makeWorkspacePlanningTask(task = {}) {
		return {
			taskId: null,
			userQuery: null,
			taskMode: null,
			taskFamily: null,
			answerType: null,
			expectedAnswerType: null,
			interactionHorizon: null,
			coordinationScope: null,
			evidenceType: null,
			targetWidgetRefs: [],
			...task
		};
	}
	function makeWorkspaceWidgetPlanSource(source = {}) {
		return {
			kind: "baseSpec",
			spec: null,
			title: null,
			...source
		};
	}
	function makeWorkspaceWidgetPlanMetric(metric = {}) {
		return {
			field: "",
			op: "",
			as: "",
			...metric
		};
	}
	function makeWorkspaceWidgetPlanSort(sort = {}) {
		return {
			field: "",
			order: "descending",
			...sort
		};
	}
	function makeWorkspaceWidgetPlanTransform(transform = {}) {
		return {
			kind: "",
			groupBy: [],
			metrics: [],
			sortBy: null,
			...transform,
			metrics: Array.isArray(transform?.metrics) ? transform.metrics.map((metric) => makeWorkspaceWidgetPlanMetric(metric)) : [],
			sortBy: transform?.sortBy ? makeWorkspaceWidgetPlanSort(transform.sortBy) : null
		};
	}
	function makeWorkspaceWidgetDataBinding(binding = {}) {
		return {
			sourceWidgetId: null,
			transforms: [],
			...binding,
			transforms: Array.isArray(binding?.transforms) ? binding.transforms.map((transform) => makeWorkspaceWidgetPlanTransform(transform)) : []
		};
	}
	function makeWorkspaceLinkFieldMapping(mapping = {}) {
		return {
			sourceField: "",
			targetField: "",
			...mapping
		};
	}
	function makeWorkspaceLinkResponseSpec(responseSpec = {}) {
		return { ...responseSpec };
	}
	function makeWorkspaceWidgetPlan(plan = {}) {
		return {
			widgetId: "",
			role: "",
			kind: null,
			title: "",
			description: "",
			source: makeWorkspaceWidgetPlanSource(),
			dataBinding: null,
			analyticRoles: [],
			...plan,
			source: makeWorkspaceWidgetPlanSource(plan?.source),
			dataBinding: plan?.dataBinding ? makeWorkspaceWidgetDataBinding(plan.dataBinding) : null,
			analyticRoles: Array.isArray(plan?.analyticRoles) ? [...plan.analyticRoles] : []
		};
	}
	function makeWorkspaceLinkPlan(plan = {}) {
		const compatibilityInput = plan || {};
		const publicPlan = stripWidgetLinkCompatibilityFields(compatibilityInput);
		const normalizedLink = makeWidgetLink({
			...compatibilityInput,
			fieldMapping: Array.isArray(plan?.fieldMapping) ? plan.fieldMapping.map((mapping) => makeWorkspaceLinkFieldMapping(mapping)) : []
		});
		return {
			linkId: "",
			sourceWidgetId: "",
			targetWidgetId: "",
			kind: null,
			primitive: null,
			effect: null,
			activationPolicy: "automatic",
			effectConstraint: null,
			responseSpec: null,
			description: "",
			fieldMapping: [],
			...publicPlan,
			kind: normalizedLink.kind,
			primitive: normalizedLink.primitive,
			effect: normalizedLink.effect,
			activationPolicy: normalizedLink.activationPolicy,
			effectConstraint: normalizedLink.effectConstraint,
			responseSpec: normalizedLink.responseSpec ? makeWorkspaceLinkResponseSpec(normalizedLink.responseSpec) : null,
			description: normalizedLink.description,
			fieldMapping: normalizedLink.fieldMapping
		};
	}
	function makeWorkspacePlanningRequest(request) {
		return {
			task: null,
			datasetSchema: null,
			userIntent: null,
			runMode: "goal_oriented",
			complexityBudget: "standard",
			preferredTopology: null,
			...request,
			task: request?.task ? makeWorkspacePlanningTask(request.task) : null,
			datasetSchema: request?.datasetSchema ? makeWorkspacePlanningDatasetSchema(request.datasetSchema) : null
		};
	}
	function makeWorkspacePlanningResult(result) {
		return {
			topology: "T1",
			widgets: [],
			links: [],
			rationale: [],
			source: "planner",
			planningMode: "topology_driven",
			primaryWidgetId: null,
			title: "Workspace",
			...result,
			widgets: Array.isArray(result?.widgets) ? result.widgets.map((widget) => makeWorkspaceWidgetPlan(widget)) : [],
			links: Array.isArray(result?.links) ? result.links.map((link) => makeWorkspaceLinkPlan(link)) : []
		};
	}
	function describeWorkspacePlanningTaskSchema() {
		return cloneValue$27({
			type: "object",
			properties: {
				taskId: { type: "string" },
				userQuery: { type: "string" },
				taskMode: { type: "string" },
				taskFamily: {
					type: "string",
					enum: WORKSPACE_PLANNING_TASK_FAMILIES
				},
				answerType: {
					type: "string",
					enum: WORKSPACE_PLANNING_ANSWER_TYPES
				},
				expectedAnswerType: {
					type: "string",
					enum: WORKSPACE_PLANNING_ANSWER_TYPES
				},
				interactionHorizon: {
					type: "string",
					enum: WORKSPACE_PLANNING_INTERACTION_HORIZONS
				},
				coordinationScope: {
					type: "string",
					enum: WORKSPACE_PLANNING_COORDINATION_SCOPES
				},
				evidenceType: {
					type: "string",
					enum: WORKSPACE_PLANNING_EVIDENCE_TYPES
				},
				targetWidgetRefs: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function describeWorkspaceWidgetPlanSourceSchema() {
		return cloneValue$27({
			type: "object",
			required: ["kind"],
			properties: {
				kind: { type: "string" },
				spec: { type: "object" },
				title: { type: "string" }
			}
		});
	}
	function describeWorkspaceWidgetPlanMetricSchema() {
		return cloneValue$27({
			type: "object",
			properties: {
				field: { type: "string" },
				op: { type: "string" },
				as: { type: "string" }
			}
		});
	}
	function describeWorkspaceWidgetPlanSortSchema() {
		return cloneValue$27({
			type: "object",
			properties: {
				field: { type: "string" },
				order: { type: "string" }
			}
		});
	}
	function describeWorkspaceWidgetPlanTransformSchema() {
		return cloneValue$27({
			type: "object",
			properties: {
				kind: { type: "string" },
				groupBy: {
					type: "array",
					items: { type: "string" }
				},
				metrics: {
					type: "array",
					items: describeWorkspaceWidgetPlanMetricSchema()
				},
				sortBy: { anyOf: [describeWorkspaceWidgetPlanSortSchema(), { type: "null" }] }
			}
		});
	}
	function describeWorkspaceWidgetDataBindingSchema() {
		return cloneValue$27({
			type: "object",
			properties: {
				sourceWidgetId: { type: "string" },
				transforms: {
					type: "array",
					items: describeWorkspaceWidgetPlanTransformSchema()
				}
			}
		});
	}
	function describeWorkspaceLinkFieldMappingSchema() {
		return cloneValue$27({
			type: "object",
			properties: {
				sourceField: { type: "string" },
				targetField: { type: "string" }
			}
		});
	}
	function describeWorkspaceWidgetPlanSchema() {
		return cloneValue$27({
			type: "object",
			required: [
				"widgetId",
				"role",
				"source"
			],
			properties: {
				widgetId: { type: "string" },
				role: { type: "string" },
				kind: { type: "string" },
				title: { type: "string" },
				description: { type: "string" },
				source: describeWorkspaceWidgetPlanSourceSchema(),
				dataBinding: describeWorkspaceWidgetDataBindingSchema(),
				analyticRoles: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function describeWorkspaceLinkPlanSchema() {
		return cloneValue$27({
			type: "object",
			properties: {
				linkId: { type: "string" },
				sourceWidgetId: { type: "string" },
				targetWidgetId: { type: "string" },
				kind: { type: ["string", "null"] },
				primitive: { type: ["string", "null"] },
				effect: { type: ["string", "null"] },
				activationPolicy: { type: "string" },
				effectConstraint: { type: ["string", "null"] },
				responseSpec: {
					type: "object",
					properties: {
						kind: { type: "string" },
						params: { type: "object" },
						verificationHints: { type: "object" }
					}
				},
				description: { type: "string" },
				fieldMapping: {
					type: "array",
					items: describeWorkspaceLinkFieldMappingSchema()
				}
			}
		});
	}
	function describeWorkspacePlanningRequestSchema() {
		return cloneValue$27({
			type: "object",
			properties: {
				task: { anyOf: [{ type: "null" }, describeWorkspacePlanningTaskSchema()] },
				datasetSchema: { anyOf: [{ type: "null" }, {
					type: "object",
					properties: { fields: {
						type: "array",
						items: {
							type: "object",
							properties: {
								name: { type: "string" },
								type: { type: "string" },
								nullable: { type: "boolean" },
								description: { type: "string" }
							}
						}
					} }
				}] },
				userIntent: { type: ["string", "null"] },
				runMode: {
					type: "string",
					enum: WORKSPACE_PLANNING_RUN_MODES
				},
				complexityBudget: {
					type: "string",
					enum: WORKSPACE_PLANNING_BUDGETS
				},
				preferredTopology: {
					type: ["string", "null"],
					enum: [...WORKSPACE_PLANNING_TOPOLOGIES, null]
				}
			}
		});
	}
	function describeWorkspacePlanningResultSchema() {
		return cloneValue$27({
			type: "object",
			required: [
				"topology",
				"widgets",
				"links",
				"rationale",
				"source",
				"planningMode",
				"title"
			],
			properties: {
				topology: {
					type: "string",
					enum: WORKSPACE_PLANNING_TOPOLOGIES
				},
				widgets: {
					type: "array",
					items: describeWorkspaceWidgetPlanSchema()
				},
				links: {
					type: "array",
					items: describeWorkspaceLinkPlanSchema()
				},
				rationale: {
					type: "array",
					items: { type: "string" }
				},
				source: {
					type: "string",
					enum: WORKSPACE_PLAN_SOURCES
				},
				planningMode: {
					type: "string",
					enum: WORKSPACE_PLAN_MODES
				},
				primaryWidgetId: { type: ["string", "null"] },
				title: { type: "string" }
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/widgetAdapters.js
	function cloneValue$26(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var WIDGET_ADAPTER_PROVIDERS = [
		"vega-lite",
		"echarts",
		"d3",
		"custom"
	];
	function makeWidgetAdapterProviderCapabilities(capabilities = {}) {
		return {
			supportedWidgetKinds: [],
			renderStrategy: "custom",
			stateApplyStrategy: "custom",
			interactionBindingStrategy: "custom",
			supportsRendererMount: false,
			supportsRendererUpdate: false,
			supportsRendererDispose: false,
			supportsSignalPatching: false,
			supportsOptionMerging: false,
			supportsImperativeRender: false,
			supportsPointSelection: false,
			supportsIntervalSelection: false,
			supportsZoomPan: false,
			supportsFocusReadback: false,
			supportsSelectionReadback: false,
			supportsViewportReadback: false,
			supportsHighlightProjection: false,
			supportsInteractionEvents: false,
			...capabilities
		};
	}
	function makeWidgetAdapterHumanInteraction(interaction = {}) {
		return {
			mode: "none",
			actionName: null,
			supportsDirectManipulation: false,
			...interaction
		};
	}
	function makeWidgetAdapterCapabilities(capabilities = {}) {
		return {
			canDescribe: false,
			canReadState: false,
			canApplyState: false,
			canMount: false,
			canUpdate: false,
			canDisposeRenderer: false,
			canBindHumanInteractions: false,
			canReadSelection: false,
			canReadViewport: false,
			canRegisterActions: false,
			canRegisterPerceptionQueries: false,
			...capabilities
		};
	}
	function makeWidgetAdapterSummary(summary = {}) {
		return {
			widgetRef: null,
			dataRef: null,
			kind: null,
			title: "",
			description: "",
			analyticRoles: [],
			primaryDataRef: null,
			provider: "custom",
			providerCapabilities: makeWidgetAdapterProviderCapabilities(),
			role: null,
			sourceKind: null,
			supportsSpecMutation: false,
			metadata: {},
			usageNotes: [],
			actionNames: [],
			perceptionQueryNames: [],
			humanInteraction: makeWidgetAdapterHumanInteraction(),
			capabilities: makeWidgetAdapterCapabilities(),
			...summary,
			providerCapabilities: makeWidgetAdapterProviderCapabilities(summary?.providerCapabilities),
			humanInteraction: makeWidgetAdapterHumanInteraction(summary?.humanInteraction),
			capabilities: makeWidgetAdapterCapabilities(summary?.capabilities)
		};
	}
	function describeWidgetAdapterProviderCapabilitiesSchema() {
		return cloneValue$26({
			type: "object",
			properties: {
				supportedWidgetKinds: {
					type: "array",
					items: { type: "string" }
				},
				renderStrategy: { type: "string" },
				stateApplyStrategy: { type: "string" },
				interactionBindingStrategy: { type: "string" },
				supportsRendererMount: { type: "boolean" },
				supportsRendererUpdate: { type: "boolean" },
				supportsRendererDispose: { type: "boolean" },
				supportsSignalPatching: { type: "boolean" },
				supportsOptionMerging: { type: "boolean" },
				supportsImperativeRender: { type: "boolean" },
				supportsPointSelection: { type: "boolean" },
				supportsIntervalSelection: { type: "boolean" },
				supportsZoomPan: { type: "boolean" },
				supportsFocusReadback: { type: "boolean" },
				supportsSelectionReadback: { type: "boolean" },
				supportsViewportReadback: { type: "boolean" },
				supportsHighlightProjection: { type: "boolean" },
				supportsInteractionEvents: { type: "boolean" }
			}
		});
	}
	function describeWidgetAdapterHumanInteractionSchema() {
		return cloneValue$26({
			type: "object",
			properties: {
				mode: { type: "string" },
				actionName: { type: ["string", "null"] },
				supportsDirectManipulation: { type: "boolean" }
			}
		});
	}
	function describeWidgetAdapterCapabilitiesSchema() {
		return cloneValue$26({
			type: "object",
			properties: {
				canDescribe: { type: "boolean" },
				canReadState: { type: "boolean" },
				canApplyState: { type: "boolean" },
				canMount: { type: "boolean" },
				canUpdate: { type: "boolean" },
				canDisposeRenderer: { type: "boolean" },
				canBindHumanInteractions: { type: "boolean" },
				canReadSelection: { type: "boolean" },
				canReadViewport: { type: "boolean" },
				canRegisterActions: { type: "boolean" },
				canRegisterPerceptionQueries: { type: "boolean" }
			}
		});
	}
	function describeWidgetAdapterSummarySchema() {
		return cloneValue$26({
			type: "object",
			required: [
				"widgetRef",
				"provider",
				"capabilities",
				"humanInteraction"
			],
			properties: {
				widgetRef: describeRefSchema(),
				dataRef: { anyOf: [describeRefSchema(), { type: "null" }] },
				kind: { type: ["string", "null"] },
				title: { type: "string" },
				description: { type: "string" },
				analyticRoles: {
					type: "array",
					items: { type: "string" }
				},
				primaryDataRef: { anyOf: [describeRefSchema(), { type: "null" }] },
				provider: {
					type: "string",
					enum: WIDGET_ADAPTER_PROVIDERS
				},
				providerCapabilities: describeWidgetAdapterProviderCapabilitiesSchema(),
				role: { type: ["string", "null"] },
				sourceKind: { type: ["string", "null"] },
				supportsSpecMutation: { type: "boolean" },
				metadata: { type: "object" },
				usageNotes: {
					type: "array",
					items: { type: "string" }
				},
				actionNames: {
					type: "array",
					items: { type: "string" }
				},
				perceptionQueryNames: {
					type: "array",
					items: { type: "string" }
				},
				humanInteraction: describeWidgetAdapterHumanInteractionSchema(),
				capabilities: describeWidgetAdapterCapabilitiesSchema()
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/workspaceSpec.js
	function cloneValue$25(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var WORKSPACE_TOPOLOGIES = [
		"T1",
		"T2",
		"T3",
		"T4",
		"T5",
		"T6"
	];
	var WIDGET_SOURCE_KINDS = [
		"baseSpec",
		"templateSpec",
		"tableView"
	];
	var WIDGET_LINK_PRIMITIVES = [...WIDGET_LINK_KINDS];
	var TRANSFORM_KINDS = ["aggregate"];
	var LEGACY_WIDGET_LINK_PROPAGATION_POLICIES = [
		"automatic",
		"manual",
		"highlightOnly",
		"focusOnly"
	];
	function isPlainObject$3(value) {
		return !!value && typeof value === "object" && !Array.isArray(value);
	}
	function makeWorkspaceSpecSummary(summary = {}) {
		return {
			topology: typeof summary?.topology === "string" ? summary.topology : null,
			widgetCount: Number.isInteger(summary?.widgetCount) ? summary.widgetCount : 0,
			linkCount: Number.isInteger(summary?.linkCount) ? summary.linkCount : 0
		};
	}
	function validateWorkspaceSpec(spec) {
		if (spec == null) return {
			ok: true,
			issues: []
		};
		const issues = [];
		if (!isPlainObject$3(spec)) return {
			ok: false,
			issues: ["workspaceSpec must be a plain object."]
		};
		if (spec.topology != null && !WORKSPACE_TOPOLOGIES.includes(spec.topology)) issues.push(`workspaceSpec.topology must be one of: ${WORKSPACE_TOPOLOGIES.join(", ")}.`);
		if (spec.widgets != null && !Array.isArray(spec.widgets)) issues.push("workspaceSpec.widgets must be an array when provided.");
		if (spec.links != null && !Array.isArray(spec.links)) issues.push("workspaceSpec.links must be an array when provided.");
		if (Array.isArray(spec.widgets)) {
			const widgetIds = /* @__PURE__ */ new Set();
			spec.widgets.forEach((widget, index) => {
				if (!isPlainObject$3(widget)) {
					issues.push(`workspaceSpec.widgets[${index}] must be a plain object.`);
					return;
				}
				if (!widget.widgetId || typeof widget.widgetId !== "string") issues.push(`workspaceSpec.widgets[${index}].widgetId must be a non-empty string.`);
				else {
					if (widgetIds.has(widget.widgetId)) issues.push(`workspaceSpec.widgets[${index}].widgetId must be unique.`);
					widgetIds.add(widget.widgetId);
				}
				if (!widget.role || typeof widget.role !== "string") issues.push(`workspaceSpec.widgets[${index}].role must be a non-empty string.`);
				if (!isPlainObject$3(widget.source)) issues.push(`workspaceSpec.widgets[${index}].source must be a plain object.`);
				else if (!WIDGET_SOURCE_KINDS.includes(widget.source.kind)) issues.push(`workspaceSpec.widgets[${index}].source.kind must be one of: ${WIDGET_SOURCE_KINDS.join(", ")}.`);
				if (widget.source?.kind === "templateSpec" && !isPlainObject$3(widget.source.spec)) issues.push(`workspaceSpec.widgets[${index}].source.spec must be provided for templateSpec widgets.`);
				if (widget.dataBinding != null && !isPlainObject$3(widget.dataBinding)) issues.push(`workspaceSpec.widgets[${index}].dataBinding must be a plain object when provided.`);
				if (Array.isArray(widget.dataBinding?.transforms)) widget.dataBinding.transforms.forEach((transform, transformIndex) => {
					if (!isPlainObject$3(transform)) {
						issues.push(`workspaceSpec.widgets[${index}].dataBinding.transforms[${transformIndex}] must be a plain object.`);
						return;
					}
					if (!TRANSFORM_KINDS.includes(transform.kind)) issues.push(`workspaceSpec.widgets[${index}].dataBinding.transforms[${transformIndex}].kind must be one of: ${TRANSFORM_KINDS.join(", ")}.`);
				});
			});
		}
		if (Array.isArray(spec.links)) {
			const widgetIds = new Set(Array.isArray(spec.widgets) ? spec.widgets.map((widget) => widget?.widgetId).filter((id) => typeof id === "string" && id.length > 0) : []);
			spec.links.forEach((link, index) => {
				if (!isPlainObject$3(link)) {
					issues.push(`workspaceSpec.links[${index}] must be a plain object.`);
					return;
				}
				if (!link.linkId || typeof link.linkId !== "string") issues.push(`workspaceSpec.links[${index}].linkId must be a non-empty string.`);
				if (!link.sourceWidgetId || typeof link.sourceWidgetId !== "string") issues.push(`workspaceSpec.links[${index}].sourceWidgetId must be a non-empty string.`);
				else if (widgetIds.size > 0 && !widgetIds.has(link.sourceWidgetId)) issues.push(`workspaceSpec.links[${index}].sourceWidgetId must reference an existing widgetId.`);
				if (!link.targetWidgetId || typeof link.targetWidgetId !== "string") issues.push(`workspaceSpec.links[${index}].targetWidgetId must be a non-empty string.`);
				else if (widgetIds.size > 0 && !widgetIds.has(link.targetWidgetId)) issues.push(`workspaceSpec.links[${index}].targetWidgetId must reference an existing widgetId.`);
				if (!link.primitive || typeof link.primitive !== "string") issues.push(`workspaceSpec.links[${index}].primitive must be a non-empty string.`);
				else if (!WIDGET_LINK_PRIMITIVES.includes(link.primitive)) issues.push(`workspaceSpec.links[${index}].primitive must be one of: ${WIDGET_LINK_PRIMITIVES.join(", ")}.`);
				if (link.activationPolicy != null && !WIDGET_LINK_ACTIVATION_POLICIES.includes(link.activationPolicy)) issues.push(`workspaceSpec.links[${index}].activationPolicy must be one of: ${WIDGET_LINK_ACTIVATION_POLICIES.join(", ")}.`);
				if (link.effectConstraint != null && !WIDGET_LINK_EFFECT_CONSTRAINTS.includes(link.effectConstraint)) issues.push(`workspaceSpec.links[${index}].effectConstraint must be one of: ${WIDGET_LINK_EFFECT_CONSTRAINTS.join(", ")}.`);
				if (link.responseSpec != null) if (!isPlainObject$3(link.responseSpec)) issues.push(`workspaceSpec.links[${index}].responseSpec must be a plain object when provided.`);
				else {
					if (!link.responseSpec.kind || typeof link.responseSpec.kind !== "string") issues.push(`workspaceSpec.links[${index}].responseSpec.kind must be a non-empty string when responseSpec is provided.`);
					else if (!WIDGET_LINK_ADVANCED_RESPONSE_KINDS.includes(link.responseSpec.kind)) issues.push(`workspaceSpec.links[${index}].responseSpec.kind must be one of: ${WIDGET_LINK_ADVANCED_RESPONSE_KINDS.join(", ")}.`);
					if (link.responseSpec.params != null && !isPlainObject$3(link.responseSpec.params)) issues.push(`workspaceSpec.links[${index}].responseSpec.params must be a plain object when provided.`);
					if (link.responseSpec.verificationHints != null && !isPlainObject$3(link.responseSpec.verificationHints)) issues.push(`workspaceSpec.links[${index}].responseSpec.verificationHints must be a plain object when provided.`);
				}
				if (link.propagationPolicy != null && !LEGACY_WIDGET_LINK_PROPAGATION_POLICIES.includes(link.propagationPolicy)) issues.push(`workspaceSpec.links[${index}].propagationPolicy is a legacy compatibility field and, when provided, must be one of: ${LEGACY_WIDGET_LINK_PROPAGATION_POLICIES.join(", ")}.`);
			});
		}
		return {
			ok: issues.length === 0,
			issues
		};
	}
	function summarizeWorkspaceSpec(spec) {
		if (!isPlainObject$3(spec)) return null;
		return makeWorkspaceSpecSummary({
			topology: typeof spec.topology === "string" ? spec.topology : null,
			widgetCount: Array.isArray(spec.widgets) ? spec.widgets.length : 0,
			linkCount: Array.isArray(spec.links) ? spec.links.length : 0
		});
	}
	function describeWorkspaceSpecSummarySchema() {
		return cloneValue$25({
			type: "object",
			properties: {
				topology: {
					type: ["string", "null"],
					enum: [...WORKSPACE_TOPOLOGIES, null]
				},
				widgetCount: { type: "integer" },
				linkCount: { type: "integer" }
			}
		});
	}
	function describeWorkspaceSpecSchema() {
		return cloneValue$25({
			type: "object",
			properties: {
				topology: {
					type: "string",
					enum: WORKSPACE_TOPOLOGIES
				},
				widgets: {
					type: "array",
					items: {
						type: "object",
						required: [
							"widgetId",
							"role",
							"source"
						],
						properties: {
							widgetId: { type: "string" },
							role: { type: "string" },
							kind: { type: "string" },
							title: { type: "string" },
							description: { type: "string" },
							source: {
								type: "object",
								required: ["kind"],
								properties: {
									kind: {
										type: "string",
										enum: WIDGET_SOURCE_KINDS
									},
									spec: { type: "object" },
									title: { type: "string" }
								}
							},
							dataBinding: {
								type: "object",
								properties: {
									sourceDataId: { type: "string" },
									transforms: {
										type: "array",
										items: {
											type: "object",
											required: ["kind"],
											properties: {
												kind: {
													type: "string",
													enum: TRANSFORM_KINDS
												},
												groupBy: {
													type: "array",
													items: { type: "string" }
												},
												metrics: {
													type: "array",
													items: { type: "object" }
												},
												sortBy: { type: "object" },
												limit: { type: "integer" }
											}
										}
									}
								}
							},
							analyticRoles: {
								type: "array",
								items: { type: "string" }
							}
						}
					}
				},
				links: {
					type: "array",
					items: {
						type: "object",
						required: [
							"linkId",
							"sourceWidgetId",
							"targetWidgetId",
							"primitive"
						],
						properties: {
							linkId: { type: "string" },
							sourceWidgetId: { type: "string" },
							targetWidgetId: { type: "string" },
							primitive: {
								type: "string",
								enum: WIDGET_LINK_PRIMITIVES
							},
							activationPolicy: {
								type: "string",
								enum: WIDGET_LINK_ACTIVATION_POLICIES
							},
							effectConstraint: {
								type: ["string", "null"],
								enum: [...WIDGET_LINK_EFFECT_CONSTRAINTS, null]
							},
							responseSpec: {
								type: "object",
								properties: {
									kind: {
										type: "string",
										enum: WIDGET_LINK_ADVANCED_RESPONSE_KINDS
									},
									params: { type: "object" },
									verificationHints: { type: "object" }
								}
							},
							description: { type: "string" },
							fieldMapping: {
								type: "array",
								items: { type: "object" }
							}
						}
					}
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/description.js
	function cloneValue$24(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var WIDGET_KINDS = [
		"bar",
		"line",
		"scatter",
		"heatmap",
		"parallelCoordinates",
		"sankey",
		"map",
		"table",
		"custom"
	];
	var WIDGET_ANALYTIC_ROLES = [
		"lookup",
		"compare",
		"rank",
		"correlate",
		"cluster",
		"outlier",
		"trend",
		"distribution",
		"flow",
		"geoPattern"
	];
	var WORKSPACE_CAPABILITIES = [
		"singleWidgetAnalysis",
		"multiWidgetCoordination",
		"sharedSelection",
		"crossFilter",
		"domainSync",
		"traceReplay"
	];
	var WORKSPACE_TRANSPORT_TOOL_NAMES = [
		"workspace_describe",
		"view_read",
		"action_run",
		"perception_query",
		"interaction_trace_read"
	];
	var WORKSPACE_OPTIONAL_TRANSPORT_TOOL_NAMES = [
		"data_query",
		"workspace_plan",
		"agent_loop_describe",
		"trace_graph_read",
		"read_snapshot",
		"state_history_read",
		"branch_list",
		"verified_action_run",
		"jump_to_state",
		"branch_from_state",
		"response_recorder_describe",
		"agent_response_read",
		"agent_response_list",
		"agent_response_record"
	];
	var WIDGET_HUMAN_INTERACTION_MODES = [
		"none",
		"brush2d",
		"multiBrush",
		"categoryClick",
		"cellClick",
		"rowClick",
		"directManipulation"
	];
	function describeWidgetKindSchema() {
		return cloneValue$24({
			type: "string",
			enum: WIDGET_KINDS
		});
	}
	function describeWidgetAnalyticRoleSchema() {
		return cloneValue$24({
			type: "string",
			enum: WIDGET_ANALYTIC_ROLES
		});
	}
	function describeWorkspaceCapabilitySchema() {
		return cloneValue$24({
			type: "string",
			enum: WORKSPACE_CAPABILITIES
		});
	}
	function describeWorkspaceTransportToolNameSchema() {
		return cloneValue$24({
			type: "string",
			enum: WORKSPACE_TRANSPORT_TOOL_NAMES
		});
	}
	function describeWorkspaceOptionalTransportToolNameSchema() {
		return cloneValue$24({
			type: "string",
			enum: WORKSPACE_OPTIONAL_TRANSPORT_TOOL_NAMES
		});
	}
	function describeWidgetHumanInteractionModeSchema() {
		return cloneValue$24({
			type: "string",
			enum: WIDGET_HUMAN_INTERACTION_MODES
		});
	}
	function makeWidgetDescription(description) {
		return {
			title: "",
			description: "",
			analyticRoles: [],
			widgetId: null,
			role: "primary",
			sourceKind: null,
			supportsSpecMutation: false,
			primaryDataRef: null,
			actionNames: [],
			perceptionQueryNames: [],
			usageNotes: [],
			humanInteraction: {
				mode: "none",
				actionName: null,
				supportsDirectManipulation: false
			},
			...description
		};
	}
	function makeWorkspaceDescription(description) {
		return {
			appId: "widgetva-app",
			workspaceId: "main",
			generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			workspaceCapabilities: [],
			transportHints: null,
			runtimeTopology: null,
			taskContext: null,
			widgetAdapters: [],
			__schemas: null,
			widgets: [],
			dataHandles: [],
			links: [],
			actions: [],
			perceptionQueries: [],
			planning: null,
			...description,
			...description?.transportHints ? { transportHints: makeWorkspaceTransportHints(description.transportHints) } : {},
			...description?.planning ? { planning: makeWorkspaceDescriptionPlanning(description.planning) } : {}
		};
	}
	function makeWorkspaceTransportHints(transportHints) {
		return {
			recommendedTools: [],
			optionalTools: [],
			note: "",
			...transportHints
		};
	}
	function makeWorkspaceDescriptionPlanning(planning) {
		return {
			supportedTopologies: [],
			supportedRunModes: [],
			supportedComplexityBudgets: [],
			supportedPlanSources: [],
			supportedPlanningModes: [],
			requestedWorkspaceSpec: null,
			planningRequest: null,
			workspaceSpecStatus: null,
			workspaceSpecIssues: [],
			materializedFromSpec: false,
			materializedFromPlanner: false,
			planner: null,
			...planning,
			...planning?.requestedWorkspaceSpec ? { requestedWorkspaceSpec: makeWorkspaceSpecSummary(planning.requestedWorkspaceSpec) } : {},
			...planning?.planningRequest ? { planningRequest: makeWorkspacePlanningRequest(planning.planningRequest) } : {},
			...planning?.planner ? { planner: makeWorkspacePlanningResult(planning.planner) } : {}
		};
	}
	function describeWidgetDescriptionSchema() {
		return cloneValue$24({
			type: "object",
			required: [
				"title",
				"actionNames",
				"perceptionQueryNames",
				"humanInteraction"
			],
			properties: {
				ref: describeRefSchema(),
				kind: describeWidgetKindSchema(),
				title: { type: "string" },
				description: { type: "string" },
				analyticRoles: {
					type: "array",
					items: describeWidgetAnalyticRoleSchema()
				},
				widgetId: { type: ["string", "null"] },
				role: { type: "string" },
				sourceKind: { type: ["string", "null"] },
				supportsSpecMutation: { type: "boolean" },
				primaryDataRef: { anyOf: [describeRefSchema(), { type: "null" }] },
				actionNames: {
					type: "array",
					items: { type: "string" }
				},
				perceptionQueryNames: {
					type: "array",
					items: { type: "string" }
				},
				usageNotes: {
					type: "array",
					items: { type: "string" }
				},
				humanInteraction: {
					type: "object",
					properties: {
						mode: describeWidgetHumanInteractionModeSchema(),
						actionName: { type: ["string", "null"] },
						supportsDirectManipulation: { type: "boolean" }
					}
				}
			}
		});
	}
	function describeWorkspaceTransportHintsSchema() {
		return cloneValue$24({
			type: ["object", "null"],
			properties: {
				recommendedTools: {
					type: "array",
					items: describeWorkspaceTransportToolNameSchema()
				},
				optionalTools: {
					type: "array",
					items: describeWorkspaceOptionalTransportToolNameSchema()
				},
				note: { type: "string" }
			}
		});
	}
	function describeWorkspaceDescriptionPlanningSchema() {
		return cloneValue$24({
			type: ["object", "null"],
			properties: {
				supportedTopologies: {
					type: "array",
					items: {
						type: "string",
						enum: WORKSPACE_PLANNING_TOPOLOGIES
					}
				},
				supportedRunModes: {
					type: "array",
					items: {
						type: "string",
						enum: WORKSPACE_PLANNING_RUN_MODES
					}
				},
				supportedComplexityBudgets: {
					type: "array",
					items: {
						type: "string",
						enum: WORKSPACE_PLANNING_BUDGETS
					}
				},
				supportedPlanSources: {
					type: "array",
					items: {
						type: "string",
						enum: WORKSPACE_PLAN_SOURCES
					}
				},
				supportedPlanningModes: {
					type: "array",
					items: {
						type: "string",
						enum: WORKSPACE_PLAN_MODES
					}
				},
				requestedWorkspaceSpec: { anyOf: [describeWorkspaceSpecSummarySchema(), { type: "null" }] },
				planningRequest: { anyOf: [describeWorkspacePlanningRequestSchema(), { type: "null" }] },
				workspaceSpecStatus: { type: ["string", "null"] },
				workspaceSpecIssues: {
					type: "array",
					items: { type: "string" }
				},
				materializedFromSpec: { type: "boolean" },
				materializedFromPlanner: { type: "boolean" },
				planner: { anyOf: [describeWorkspacePlanningResultSchema(), { type: "null" }] }
			}
		});
	}
	function describeWorkspaceDescriptionSchema() {
		return cloneValue$24({
			type: "object",
			required: [
				"appId",
				"workspaceId",
				"generatedAt",
				"widgets",
				"dataHandles",
				"links",
				"actions",
				"perceptionQueries"
			],
			properties: {
				appId: { type: "string" },
				workspaceId: { type: "string" },
				generatedAt: { type: "string" },
				workspaceCapabilities: {
					type: "array",
					items: describeWorkspaceCapabilitySchema()
				},
				transportHints: describeWorkspaceTransportHintsSchema(),
				runtimeTopology: { anyOf: [describeWorkspaceTopologySummarySchema(), { type: "null" }] },
				taskContext: { anyOf: [describeWorkspaceTaskContextSchema(), { type: "null" }] },
				widgets: {
					type: "array",
					items: describeWidgetDescriptionSchema()
				},
				widgetAdapters: {
					type: "array",
					items: describeWidgetAdapterSummarySchema()
				},
				dataHandles: {
					type: "array",
					items: describeDataHandleSchema()
				},
				links: {
					type: "array",
					items: describeWidgetLinkSchema()
				},
				actions: {
					type: "array",
					items: describeActionDescriptorSchema()
				},
				perceptionQueries: {
					type: "array",
					items: describePerceptionDescriptorSchema()
				},
				planning: describeWorkspaceDescriptionPlanningSchema(),
				__schemas: { type: "object" }
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/agentLoop.js
	function cloneValue$23(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeAgentLoopHints(hints) {
		return {
			recommendedOrder: [],
			workspaceDescribeName: "describeWorkspace",
			workspacePlanName: null,
			stateReadName: "readState",
			viewReadName: "readView",
			actionRunName: "executeAction",
			perceptionQueryName: "queryPerception",
			verifyQueryName: "perception.verifyActionEffect",
			dataQueryRunName: null,
			dataQueryName: null,
			traceReadName: null,
			interactionTraceName: null,
			traceGraphName: null,
			snapshotName: null,
			stateHistoryName: null,
			branchListName: null,
			finalSnapshotName: null,
			verifiedActionName: "executeVerifiedAction",
			replayName: null,
			jumpToStateName: null,
			branchFromStateName: null,
			responseRecorderName: null,
			responseReadName: null,
			responseListName: null,
			responseRecordName: null,
			latestCoordinationResultReadName: null,
			verificationSurfaces: [],
			planningSurfaces: [],
			historySurfaces: [],
			replaySurfaces: [],
			answerSurfaces: [],
			humanInteractionHints: [],
			sharedDataViews: [],
			focusedDataViews: [],
			preferredEvidenceRefs: {
				currentViewRef: null,
				currentSelectionRef: null
			},
			workspaceTopology: null,
			...hints
		};
	}
	function describeAgentLoopHintsSchema() {
		const optionalNameSchema = { type: ["string", "null"] };
		return cloneValue$23({
			type: "object",
			properties: {
				recommendedOrder: {
					type: "array",
					items: { type: "string" }
				},
				workspaceDescribeName: { type: "string" },
				workspacePlanName: optionalNameSchema,
				stateReadName: { type: "string" },
				viewReadName: { type: "string" },
				actionRunName: { type: "string" },
				perceptionQueryName: { type: "string" },
				verifyQueryName: { type: "string" },
				dataQueryRunName: optionalNameSchema,
				dataQueryName: optionalNameSchema,
				traceReadName: optionalNameSchema,
				interactionTraceName: optionalNameSchema,
				traceGraphName: optionalNameSchema,
				snapshotName: optionalNameSchema,
				stateHistoryName: optionalNameSchema,
				branchListName: optionalNameSchema,
				finalSnapshotName: optionalNameSchema,
				verifiedActionName: { type: "string" },
				replayName: optionalNameSchema,
				jumpToStateName: optionalNameSchema,
				branchFromStateName: optionalNameSchema,
				responseRecorderName: optionalNameSchema,
				responseReadName: optionalNameSchema,
				responseListName: optionalNameSchema,
				responseRecordName: optionalNameSchema,
				latestCoordinationResultReadName: optionalNameSchema,
				verificationSurfaces: {
					type: "array",
					items: { type: "string" }
				},
				planningSurfaces: {
					type: "array",
					items: { type: "string" }
				},
				historySurfaces: {
					type: "array",
					items: { type: "string" }
				},
				replaySurfaces: {
					type: "array",
					items: { type: "string" }
				},
				answerSurfaces: {
					type: "array",
					items: { type: "string" }
				},
				humanInteractionHints: {
					type: "array",
					items: {
						type: "object",
						properties: {
							widgetRef: { type: "string" },
							mode: { type: "string" },
							actionName: { type: ["string", "null"] },
							supportsDirectManipulation: { type: "boolean" },
							focused: { type: "boolean" }
						}
					}
				},
				sharedDataViews: {
					type: "array",
					items: {
						type: "object",
						properties: {
							ref: { type: "string" },
							scope: { type: "string" },
							title: { type: "string" }
						}
					}
				},
				focusedDataViews: {
					type: "array",
					items: {
						type: "object",
						properties: {
							ref: { type: "string" },
							scope: { type: "string" },
							title: { type: "string" }
						}
					}
				},
				preferredEvidenceRefs: {
					type: "object",
					properties: {
						currentViewRef: { type: ["string", "null"] },
						currentSelectionRef: { type: ["string", "null"] }
					}
				},
				workspaceTopology: { anyOf: [describeWorkspaceTopologySummarySchema(), { type: "null" }] }
			}
		});
	}
	function makeVerifiedActionEvidence(evidence) {
		return {
			verificationHints: [],
			traceEvidence: null,
			finalSnapshot: null,
			linkPropagation: [],
			latestCoordinationResult: null,
			...evidence
		};
	}
	function describeVerifiedActionEvidenceSchema() {
		return cloneValue$23({
			type: "object",
			properties: {
				verificationHints: {
					type: "array",
					items: { type: "string" }
				},
				traceEvidence: { anyOf: [{ type: "null" }, describeInteractionTraceRecordSchema()] },
				finalSnapshot: { anyOf: [{ type: "null" }, describeWorkspaceSnapshotSchema()] },
				linkPropagation: {
					type: "array",
					items: describeLinkPropagationEvaluationSchema()
				},
				latestCoordinationResult: { anyOf: [{ type: "null" }, {
					type: "object",
					additionalProperties: true
				}] }
			}
		});
	}
	function makeAgentLoopContext(context) {
		return {
			workspace: null,
			view: null,
			latestCoordinationResult: null,
			loopHints: makeAgentLoopHints(),
			...context
		};
	}
	function describeAgentLoopContextSchema() {
		return cloneValue$23({
			type: "object",
			required: [
				"workspace",
				"view",
				"loopHints"
			],
			properties: {
				workspace: describeWorkspaceDescriptionSchema(),
				view: describeWorkspaceStateSchema(),
				latestCoordinationResult: { anyOf: [{ type: "null" }, {
					type: "object",
					additionalProperties: true
				}] },
				loopHints: describeAgentLoopHintsSchema()
			}
		});
	}
	function makeVerifiedActionResult(result) {
		return {
			ok: false,
			beforeStateId: null,
			actionResult: null,
			afterView: null,
			verification: null,
			...makeVerifiedActionEvidence(),
			...result
		};
	}
	function describeVerifiedActionResultSchema() {
		return cloneValue$23({
			type: "object",
			required: [
				"ok",
				"beforeStateId",
				"actionResult",
				"afterView",
				"verification"
			],
			properties: {
				ok: { type: "boolean" },
				beforeStateId: { type: ["string", "null"] },
				actionResult: { anyOf: [{ type: "null" }, describeActionResultSchema()] },
				afterView: { anyOf: [{ type: "null" }, describeWorkspaceStateSchema()] },
				verification: { anyOf: [{ type: "null" }, describePerceptionResultSchema()] },
				verificationHints: describeVerifiedActionEvidenceSchema().properties.verificationHints,
				traceEvidence: describeVerifiedActionEvidenceSchema().properties.traceEvidence,
				finalSnapshot: describeVerifiedActionEvidenceSchema().properties.finalSnapshot,
				linkPropagation: describeVerifiedActionEvidenceSchema().properties.linkPropagation,
				latestCoordinationResult: describeVerifiedActionEvidenceSchema().properties.latestCoordinationResult
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/agentContract.js
	var SINGLE_WIDGET_AGENT_CONTRACT_VERSION = "widgetva.single-widget.agent.v1";
	function clone$29(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function uniqueNames$1(names = []) {
		return [...new Set((Array.isArray(names) ? names : []).filter((name) => typeof name === "string" && name.length > 0))];
	}
	function makeSingleWidgetAgentContract(contract = {}) {
		const catalog = contract?.catalog && typeof contract.catalog === "object" ? contract.catalog : {};
		const actionDescriptors = Array.isArray(catalog.actionDescriptors) ? catalog.actionDescriptors : [];
		const perceptionDescriptors = Array.isArray(catalog.perceptionDescriptors) ? catalog.perceptionDescriptors : [];
		const availableActionNames = uniqueNames$1(Array.isArray(catalog.availableActionNames) ? catalog.availableActionNames : actionDescriptors.map((descriptor) => descriptor?.name));
		const availablePerceptionNames = uniqueNames$1(Array.isArray(catalog.availablePerceptionNames) ? catalog.availablePerceptionNames : perceptionDescriptors.map((descriptor) => descriptor?.name));
		return {
			version: SINGLE_WIDGET_AGENT_CONTRACT_VERSION,
			widget: {
				ref: null,
				widgetId: null,
				kind: null,
				title: null,
				role: null,
				...contract?.widget || {}
			},
			observe: {
				observationMethodName: "readObservation",
				stateMethodName: "readState",
				verificationStateMethodName: "readVerificationState",
				perceptionMethodName: "queryPerception",
				...contract?.observe || {}
			},
			act: {
				actionMethodName: "executeAction",
				verifiedActionMethodName: "executeVerifiedAction",
				...contract?.act || {}
			},
			verify: {
				verifyQueryName: "perception.verifyActionEffect",
				verificationStateMethodName: "readVerificationState",
				verificationContract: null,
				...contract?.verify || {}
			},
			catalog: {
				actionDescriptors: clone$29(actionDescriptors),
				perceptionDescriptors: clone$29(perceptionDescriptors),
				availableActionNames,
				availablePerceptionNames
			},
			schemas: {
				actionCall: describeActionCallSchema(),
				actionResult: describeActionResultSchema(),
				verifiedActionResult: describeVerifiedActionResultSchema(),
				perceptionResult: describePerceptionResultSchema(),
				actionDescriptor: describeActionDescriptorSchema(),
				perceptionDescriptor: describePerceptionDescriptorSchema(),
				...contract?.schemas || {}
			}
		};
	}
	function describeSingleWidgetAgentContractFromWidget(widget) {
		if (!widget || typeof widget.readObservation !== "function") throw new Error("describeSingleWidgetAgentContractFromWidget requires a widget with readObservation().");
		const observation = widget.readObservation() || {};
		const verificationState = typeof widget.readVerificationState === "function" ? widget.readVerificationState() : null;
		return makeSingleWidgetAgentContract({
			widget: {
				ref: observation?.widgetRef || null,
				widgetId: observation?.widgetId || null,
				kind: observation?.kind || null,
				title: observation?.title || null,
				role: observation?.role || null
			},
			verify: { verificationContract: clone$29(verificationState?.contract || observation?.verification?.contract || null) },
			catalog: {
				actionDescriptors: clone$29(observation?.actionDescriptors || []),
				perceptionDescriptors: clone$29(observation?.perceptionDescriptors || []),
				availableActionNames: clone$29(observation?.actionNames || []),
				availablePerceptionNames: clone$29(observation?.perceptionNames || [])
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/localSelectionContract.js
	function clone$28(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var WIDGET_LOCAL_SELECTION_CONTRACTS = {
		bar: {
			localSelectionFamily: "category",
			selectionKinds: ["category"],
			sourceActionNames: ["bar.selectCategory"],
			cardinality: "singleActiveSelection",
			selectionValueShape: "categoricalValues",
			observationFields: [
				"state.selections",
				"coordination.localSelectionRefs",
				"coordination.widgetSelectionRef",
				"selection.activeSelectionRef",
				"selection.activeSelectionKind",
				"selection.activeSelectionSummary"
			],
			perceptionExpectations: ["selection-scoped perception queries should accept queryScope.selectionRef when a bar selection is active", "selection summaries should describe selected categorical groups rather than raw pixel regions"]
		},
		line: {
			localSelectionFamily: "category",
			selectionKinds: ["category", "point"],
			sourceActionNames: ["line.selectSeries", "line.selectXValue"],
			cardinality: "singleActiveSelection",
			selectionValueShape: "categoricalValues",
			observationFields: [
				"state.selections",
				"coordination.localSelectionRefs",
				"coordination.widgetSelectionRef",
				"selection.activeSelectionRef",
				"selection.activeSelectionKind",
				"selection.activeSelectionSummary"
			],
			perceptionExpectations: ["selection-scoped perception queries should resolve the currently selected line series subset", "selection summaries should preserve the selected series identity for downstream comparison and anomaly checks"]
		},
		scatter: {
			localSelectionFamily: "interval",
			selectionKinds: ["interval"],
			sourceActionNames: ["scatter.brushRegion"],
			cardinality: "singleActiveSelection",
			selectionValueShape: "xAndYIntervals",
			observationFields: [
				"state.selections",
				"coordination.localSelectionRefs",
				"coordination.widgetSelectionRef",
				"selection.activeSelectionRef",
				"selection.activeSelectionKind",
				"selection.activeSelectionSummary",
				"selection.activeSelectionFields"
			],
			perceptionExpectations: ["selection-scoped perception queries should resolve rows inside the brushed x/y interval", "selection summaries should expose interval predicates rather than only selected row ids"]
		},
		heatmap: {
			localSelectionFamily: "cell",
			selectionKinds: ["cell", "region"],
			sourceActionNames: ["heatmap.selectCell", "heatmap.selectSubmatrix"],
			cardinality: "singleActiveSelection",
			selectionValueShape: "cellOrRegionCoordinates",
			observationFields: [
				"state.selections",
				"coordination.localSelectionRefs",
				"coordination.widgetSelectionRef",
				"selection.activeSelectionRef",
				"selection.activeSelectionKind",
				"selection.activeSelectionSummary",
				"selection.activeSelectionFields"
			],
			perceptionExpectations: ["selection-scoped perception queries should resolve either one cell or a selected submatrix subset", "selection summaries should distinguish cell selections from broader row/column region selections"]
		},
		parallelCoordinates: {
			localSelectionFamily: "interval",
			selectionKinds: ["interval", "record"],
			sourceActionNames: ["parallelCoordinates.brushAxes", "parallelCoordinates.selectRecord"],
			cardinality: "singleActiveSelection",
			selectionValueShape: "multivariateAxisIntervalsOrRecordId",
			observationFields: [
				"state.selections",
				"coordination.localSelectionRefs",
				"coordination.widgetSelectionRef",
				"selection.activeSelectionRef",
				"selection.activeSelectionKind",
				"selection.activeSelectionSummary",
				"selection.activeSelectionFields"
			],
			perceptionExpectations: [
				"selection-scoped perception queries should resolve rows that satisfy all brushed axis rules",
				"selection summaries should expose the brushed dimension list and interval predicates",
				"record-level selections should preserve the selected record identifier for downstream focus and comparison"
			]
		},
		sankey: {
			localSelectionFamily: "category",
			selectionKinds: ["category", "aggregate"],
			sourceActionNames: ["sankey.focusFlow", "sankey.selectAggregateNode"],
			cardinality: "singleActiveSelection",
			selectionValueShape: "nodeOrFlowCategoriesOrAggregateName",
			observationFields: [
				"state.selections",
				"coordination.localSelectionRefs",
				"coordination.widgetSelectionRef",
				"selection.activeSelectionRef",
				"selection.activeSelectionKind",
				"selection.activeSelectionSummary"
			],
			perceptionExpectations: [
				"selection-scoped perception queries should resolve rows or flows matching the selected node/link categories",
				"selection summaries should preserve whether the active focus refers to flow or node categories",
				"aggregate selections should preserve aggregateName even when they do not map to row-level predicates"
			]
		}
	};
	function describeLocalSelectionContract(kind) {
		return clone$28(WIDGET_LOCAL_SELECTION_CONTRACTS[kind] || null);
	}
	function buildLocalSelectionObservation({ kind, state, widgetSelectionView = null } = {}) {
		const contract = describeLocalSelectionContract(kind);
		const localSelections = state?.selections && typeof state.selections === "object" ? state.selections : {};
		const localSelectionRefs = Object.keys(localSelections);
		const activeSelectionRef = widgetSelectionView?.selectionRef || localSelectionRefs[0] || null;
		const activeSelectionState = activeSelectionRef ? localSelections[activeSelectionRef] || null : null;
		return {
			contract,
			localSelectionCount: localSelectionRefs.length,
			activeSelectionRef,
			activeSelectionKind: activeSelectionState?.kind || null,
			activeSelectionSummary: activeSelectionState?.summary || null,
			activeSelectionFields: Array.isArray(activeSelectionState?.fields) ? clone$28(activeSelectionState.fields) : []
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/verificationContract.js
	function clone$27(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var WIDGET_VERIFICATION_CONTRACTS = {
		bar: {
			preferredReadMethod: "readVerificationState",
			preferredObservationFields: [
				"verification.checks.selectionApplied",
				"verification.checks.filterApplied",
				"verification.checks.sortApplied",
				"verification.checks.reencodeApplied",
				"verification.checks.addRemoveApplied",
				"verification.checks.highlightApplied",
				"verification.encodings.channels",
				"verification.view.addRemove",
				"verification.view.reencode",
				"verification.view.sort",
				"verification.view.highlight",
				"verification.transforms.kinds"
			],
			supportedEffectTypes: [
				"selection",
				"filter",
				"sort",
				"reencode",
				"addRemove",
				"highlight",
				"encoding",
				"linkedPropagation"
			],
			effectChecks: {
				selection: [
					"checks.selectionApplied",
					"selections.count",
					"selections.activeSummary"
				],
				filter: [
					"checks.filterApplied",
					"data.rowCount",
					"data.visibleCount",
					"transforms.kinds"
				],
				sort: ["checks.sortApplied", "view.sort"],
				reencode: [
					"checks.reencodeApplied",
					"view.reencode",
					"encodings.channels"
				],
				addRemove: [
					"checks.addRemoveApplied",
					"view.addRemove",
					"data.visibleCount"
				],
				highlight: [
					"checks.highlightApplied",
					"view.highlight",
					"feedback.highlightKeyCount",
					"feedback.sharedSelectionSourceWidgetId"
				],
				encoding: [
					"checks.encodingReadable",
					"encodings.channels",
					"encodings.fieldsByChannel"
				],
				linkedPropagation: [
					"checks.linkedPropagationApplied",
					"feedback.linkedSourceRefCount",
					"feedback.sharedSelectionSourceWidgetId"
				]
			}
		},
		line: {
			preferredReadMethod: "readVerificationState",
			preferredObservationFields: [
				"verification.checks.selectionApplied",
				"verification.checks.filterApplied",
				"verification.checks.aggregateApplied",
				"verification.checks.drillDownApplied",
				"verification.checks.focusApplied",
				"verification.checks.annotateApplied",
				"verification.checks.navigateApplied",
				"verification.checks.zoomApplied",
				"verification.checks.highlightApplied",
				"verification.encodings.channels",
				"verification.view.aggregate",
				"verification.view.drillDown",
				"verification.view.focusKeys",
				"verification.view.annotate",
				"verification.view.navigate",
				"verification.view.highlight",
				"verification.transforms.kinds"
			],
			supportedEffectTypes: [
				"selection",
				"filter",
				"aggregate",
				"drillDown",
				"focus",
				"annotate",
				"navigate",
				"zoom",
				"highlight",
				"encoding",
				"linkedPropagation"
			],
			effectChecks: {
				selection: [
					"checks.selectionApplied",
					"selections.count",
					"selections.activeSummary"
				],
				filter: [
					"checks.filterApplied",
					"data.rowCount",
					"data.visibleCount",
					"transforms.kinds"
				],
				aggregate: [
					"checks.aggregateApplied",
					"view.aggregate",
					"encodings.aggregateChannels"
				],
				drillDown: [
					"checks.drillDownApplied",
					"view.drillDown",
					"transforms.kinds"
				],
				focus: ["checks.focusApplied", "view.focusKeys"],
				annotate: ["checks.annotateApplied", "view.annotate"],
				navigate: ["checks.navigateApplied", "view.navigate"],
				zoom: [
					"checks.zoomApplied",
					"view.xDomain",
					"view.zoom"
				],
				highlight: [
					"checks.highlightApplied",
					"view.highlight",
					"feedback.highlightKeyCount",
					"feedback.sharedSelectionSourceWidgetId"
				],
				encoding: [
					"checks.encodingReadable",
					"encodings.channels",
					"encodings.fieldsByChannel"
				],
				linkedPropagation: [
					"checks.linkedPropagationApplied",
					"feedback.linkedSourceRefCount",
					"feedback.sharedSelectionSourceWidgetId"
				]
			}
		},
		scatter: {
			preferredReadMethod: "readVerificationState",
			preferredObservationFields: [
				"verification.checks.selectionApplied",
				"verification.checks.filterApplied",
				"verification.checks.annotateApplied",
				"verification.checks.zoomApplied",
				"verification.encodings.channels",
				"verification.view.annotate",
				"verification.selections.activeKinds"
			],
			supportedEffectTypes: [
				"selection",
				"filter",
				"zoom",
				"highlight",
				"annotate",
				"encoding",
				"linkedPropagation"
			],
			effectChecks: {
				selection: [
					"checks.selectionApplied",
					"selections.count",
					"selections.activeKinds",
					"selections.activeSummary"
				],
				filter: [
					"checks.filterApplied",
					"data.rowCount",
					"data.visibleCount",
					"transforms.kinds"
				],
				zoom: [
					"checks.zoomApplied",
					"view.xDomain",
					"view.yDomain",
					"view.zoom"
				],
				annotate: ["checks.annotateApplied", "view.annotate"],
				highlight: [
					"checks.highlightApplied",
					"feedback.highlightKeyCount",
					"feedback.sharedSelectionSourceWidgetId"
				],
				encoding: [
					"checks.encodingReadable",
					"encodings.channels",
					"encodings.fieldsByChannel"
				],
				linkedPropagation: [
					"checks.linkedPropagationApplied",
					"feedback.linkedSourceRefCount",
					"feedback.sharedSelectionSourceWidgetId"
				]
			}
		},
		heatmap: {
			preferredReadMethod: "readVerificationState",
			preferredObservationFields: [
				"verification.checks.selectionApplied",
				"verification.checks.filterApplied",
				"verification.checks.aggregateApplied",
				"verification.checks.drillDownApplied",
				"verification.checks.reencodeApplied",
				"verification.checks.annotateApplied",
				"verification.checks.navigateApplied",
				"verification.checks.highlightApplied",
				"verification.encodings.channels",
				"verification.view.aggregate",
				"verification.view.drillDown",
				"verification.view.reencode",
				"verification.view.annotate",
				"verification.view.navigate",
				"verification.view.highlight",
				"verification.selections.activeKinds"
			],
			supportedEffectTypes: [
				"selection",
				"filter",
				"aggregate",
				"drillDown",
				"reencode",
				"annotate",
				"navigate",
				"highlight",
				"encoding",
				"linkedPropagation"
			],
			effectChecks: {
				selection: [
					"checks.selectionApplied",
					"selections.count",
					"selections.activeKinds",
					"selections.activeSummary"
				],
				filter: [
					"checks.filterApplied",
					"data.rowCount",
					"data.visibleCount",
					"transforms.kinds"
				],
				aggregate: [
					"checks.aggregateApplied",
					"view.aggregate",
					"view.sort"
				],
				drillDown: [
					"checks.drillDownApplied",
					"view.drillDown",
					"transforms.kinds"
				],
				reencode: [
					"checks.reencodeApplied",
					"view.reencode",
					"encodings.channels"
				],
				annotate: ["checks.annotateApplied", "view.annotate"],
				navigate: ["checks.navigateApplied", "view.navigate"],
				highlight: [
					"checks.highlightApplied",
					"view.highlight",
					"feedback.highlightKeyCount",
					"feedback.sharedSelectionSourceWidgetId"
				],
				encoding: [
					"checks.encodingReadable",
					"encodings.channels",
					"encodings.fieldsByChannel"
				],
				linkedPropagation: [
					"checks.linkedPropagationApplied",
					"feedback.linkedSourceRefCount",
					"feedback.sharedSelectionSourceWidgetId"
				]
			}
		},
		parallelCoordinates: {
			preferredReadMethod: "readVerificationState",
			preferredObservationFields: [
				"verification.checks.selectionApplied",
				"verification.checks.filterApplied",
				"verification.checks.highlightApplied",
				"verification.checks.reencodeApplied",
				"verification.checks.focusApplied",
				"verification.selections.activeKinds",
				"verification.view.highlight",
				"verification.view.reencode",
				"verification.transforms.kinds"
			],
			supportedEffectTypes: [
				"selection",
				"filter",
				"highlight",
				"focus",
				"reencode",
				"encoding",
				"linkedPropagation"
			],
			effectChecks: {
				selection: [
					"checks.selectionApplied",
					"selections.count",
					"selections.activeKinds",
					"selections.activeSummary"
				],
				filter: [
					"checks.filterApplied",
					"data.rowCount",
					"data.visibleCount",
					"transforms.kinds"
				],
				highlight: [
					"checks.highlightApplied",
					"view.highlight",
					"feedback.highlightKeyCount",
					"feedback.sharedSelectionSourceWidgetId"
				],
				focus: ["checks.focusApplied", "view.focusKeys"],
				reencode: [
					"checks.reencodeApplied",
					"view.reencode",
					"encodings.channels"
				],
				encoding: [
					"checks.encodingReadable",
					"encodings.channels",
					"encodings.fieldsByChannel"
				],
				linkedPropagation: [
					"checks.linkedPropagationApplied",
					"feedback.linkedSourceRefCount",
					"feedback.sharedSelectionSourceWidgetId"
				]
			}
		},
		sankey: {
			preferredReadMethod: "readVerificationState",
			preferredObservationFields: [
				"verification.checks.selectionApplied",
				"verification.checks.filterApplied",
				"verification.checks.aggregateApplied",
				"verification.checks.reencodeApplied",
				"verification.checks.navigateApplied",
				"verification.checks.highlightApplied",
				"verification.checks.focusApplied",
				"verification.view.aggregate",
				"verification.view.reencode",
				"verification.view.navigate",
				"verification.view.highlight",
				"verification.transforms.kinds"
			],
			supportedEffectTypes: [
				"selection",
				"filter",
				"aggregate",
				"reencode",
				"navigate",
				"highlight",
				"focus",
				"encoding",
				"linkedPropagation"
			],
			effectChecks: {
				selection: [
					"checks.selectionApplied",
					"selections.count",
					"selections.activeKinds",
					"selections.activeSummary"
				],
				filter: [
					"checks.filterApplied",
					"data.rowCount",
					"data.visibleCount",
					"transforms.kinds"
				],
				aggregate: ["checks.aggregateApplied", "view.aggregate"],
				reencode: [
					"checks.reencodeApplied",
					"view.reencode",
					"encodings.channels"
				],
				navigate: ["checks.navigateApplied", "view.navigate"],
				highlight: [
					"checks.highlightApplied",
					"view.highlight",
					"feedback.highlightKeyCount",
					"feedback.sharedSelectionSourceWidgetId"
				],
				focus: ["checks.focusApplied", "view.focusKeys"],
				encoding: [
					"checks.encodingReadable",
					"encodings.channels",
					"encodings.fieldsByChannel"
				],
				linkedPropagation: [
					"checks.linkedPropagationApplied",
					"feedback.linkedSourceRefCount",
					"feedback.sharedSelectionSourceWidgetId"
				]
			}
		}
	};
	function describeWidgetVerificationContract(kind) {
		return clone$27(WIDGET_VERIFICATION_CONTRACTS[kind] || null);
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/verificationState.js
	function clone$26(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function stableSerialize$3(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	function summarizeEncodings(encodings = {}) {
		const entries = Object.entries(encodings || {}).filter(([, encoding]) => encoding && typeof encoding === "object");
		const channels = entries.map(([channel]) => channel);
		const fieldsByChannel = Object.fromEntries(entries.map(([channel, encoding]) => [channel, encoding.field || null]));
		const typesByChannel = Object.fromEntries(entries.map(([channel, encoding]) => [channel, encoding.type || null]));
		const aggregateChannels = entries.filter(([, encoding]) => typeof encoding.aggregate === "string" && encoding.aggregate.length > 0).map(([channel]) => channel);
		return {
			channels,
			fieldsByChannel,
			typesByChannel,
			aggregateChannels,
			signature: stableSerialize$3({
				channels,
				fieldsByChannel,
				typesByChannel,
				aggregateChannels
			})
		};
	}
	function summarizeTransforms(transforms = []) {
		const normalized = Array.isArray(transforms) ? transforms : [];
		const kinds = normalized.map((transform) => transform?.kind).filter((kind) => typeof kind === "string" && kind.length > 0);
		const countsByKind = Object.fromEntries([...new Set(kinds)].map((kind) => [kind, kinds.filter((candidate) => candidate === kind).length]));
		return {
			count: normalized.length,
			kinds,
			countsByKind,
			hasFilterTransform: kinds.includes("filter"),
			hasSortTransform: kinds.includes("sort"),
			hasAggregateTransform: kinds.includes("aggregate"),
			hasHighlightTransform: kinds.includes("highlight"),
			signature: stableSerialize$3({
				kinds,
				countsByKind
			})
		};
	}
	function summarizeSelections(selections = {}) {
		const entries = Object.entries(selections || {}).filter(([ref, selection]) => Boolean(ref) && selection && typeof selection === "object");
		const refs = entries.map(([ref]) => ref);
		const activeKinds = entries.map(([, selection]) => selection.kind || null).filter((kind) => typeof kind === "string" && kind.length > 0);
		const activeSummary = entries.map(([, selection]) => selection.summary || null).find((summary) => typeof summary === "string" && summary.length > 0) || null;
		return {
			count: refs.length,
			refs,
			activeKinds,
			hasSelection: refs.length > 0,
			activeSummary,
			signature: stableSerialize$3({
				refs,
				activeKinds,
				activeSummary
			})
		};
	}
	function summarizeView(view = {}) {
		const xDomain = Array.isArray(view?.xDomain) ? clone$26(view.xDomain) : null;
		const yDomain = Array.isArray(view?.yDomain) ? clone$26(view.yDomain) : null;
		const zoom = view?.zoom && typeof view.zoom === "object" ? clone$26(view.zoom) : null;
		const sort = view?.sort && typeof view.sort === "object" ? clone$26(view.sort) : null;
		const highlight = view?.highlight && typeof view.highlight === "object" ? clone$26(view.highlight) : null;
		const drillDown = view?.drillDown && typeof view.drillDown === "object" ? clone$26(view.drillDown) : null;
		const aggregate = view?.aggregate && typeof view.aggregate === "object" ? clone$26(view.aggregate) : null;
		const reencode = view?.reencode && typeof view.reencode === "object" ? clone$26(view.reencode) : null;
		const annotate = view?.annotate && typeof view.annotate === "object" ? clone$26(view.annotate) : null;
		const addRemove = view?.addRemove && typeof view.addRemove === "object" ? clone$26(view.addRemove) : null;
		const navigate = view?.navigate && typeof view.navigate === "object" ? clone$26(view.navigate) : null;
		const focusKeys = Object.fromEntries(Object.entries({
			focusedCarId: view?.focusedCarId || null,
			focusedSeries: view?.focusedSeries || null,
			focusedNode: view?.focusedNode || null,
			focusedFlow: view?.focusedFlow || null
		}).filter(([, value]) => value != null));
		return {
			xDomain,
			yDomain,
			zoom,
			sort,
			highlight,
			drillDown,
			aggregate,
			reencode,
			annotate,
			addRemove,
			navigate,
			focusKeys,
			hasXDomainOverride: Array.isArray(xDomain),
			hasYDomainOverride: Array.isArray(yDomain),
			hasZoom: Boolean(zoom),
			hasSort: Boolean(sort),
			hasHighlight: Boolean(highlight),
			hasDrillDown: Boolean(drillDown),
			hasAggregate: Boolean(aggregate),
			hasReencode: Boolean(reencode),
			hasAnnotate: Boolean(annotate),
			hasAddRemove: Boolean(addRemove),
			hasNavigate: Boolean(navigate),
			hasFocus: Object.keys(focusKeys).length > 0,
			signature: stableSerialize$3({
				xDomain,
				yDomain,
				zoom,
				sort,
				highlight,
				drillDown,
				aggregate,
				reencode,
				annotate,
				addRemove,
				navigate,
				focusKeys
			})
		};
	}
	function summarizeFeedback(feedback = {}) {
		const linkedSourceRefs = Array.isArray(feedback?.linkedSourceRefs) ? [...feedback.linkedSourceRefs] : [];
		const highlightedKeys = Array.isArray(feedback?.highlightedKeys) ? [...feedback.highlightedKeys] : [];
		return {
			linkedSourceRefCount: linkedSourceRefs.length,
			highlightKeyCount: highlightedKeys.length,
			sharedSelectionSourceWidgetId: feedback?.sharedSelectionSourceWidgetId || null,
			signature: stableSerialize$3({
				linkedSourceRefs,
				highlightedKeys,
				sharedSelectionSourceWidgetId: feedback?.sharedSelectionSourceWidgetId || null
			})
		};
	}
	function buildWidgetVerificationState({ widgetRef = null, widgetId = null, kind = null, state = null } = {}) {
		const safeState = state && typeof state === "object" ? state : {};
		const data = safeState?.data && typeof safeState.data === "object" ? safeState.data : {};
		const encodings = summarizeEncodings(safeState.encodings || {});
		const transforms = summarizeTransforms(safeState.transforms || []);
		const selections = summarizeSelections(safeState.selections || {});
		const view = summarizeView(safeState.view || {});
		const feedback = summarizeFeedback(safeState.feedback || {});
		const xEncodingType = encodings.typesByChannel?.x || null;
		const yEncodingType = encodings.typesByChannel?.y || null;
		const xDomainCanRepresentZoom = xEncodingType === "quantitative" || xEncodingType === "temporal";
		const yDomainCanRepresentZoom = yEncodingType === "quantitative" || yEncodingType === "temporal";
		return {
			widgetRef,
			widgetId,
			kind,
			data: {
				rowCount: Number.isFinite(data?.rowCount) ? data.rowCount : 0,
				visibleCount: Number.isFinite(data?.visibleCount) ? data.visibleCount : 0,
				selectedCount: Number.isFinite(data?.selectedCount) ? data.selectedCount : 0,
				sourceDataRef: data?.sourceDataRef || null,
				currentDataRef: data?.currentDataRef || null
			},
			selections,
			transforms,
			view,
			encodings,
			feedback,
			checks: {
				selectionApplied: selections.hasSelection || Number.isFinite(data?.selectedCount) && data.selectedCount > 0,
				filterApplied: transforms.hasFilterTransform || Number.isFinite(data?.visibleCount) && Number.isFinite(data?.rowCount) && data.visibleCount > 0 && data.rowCount > 0 && data.visibleCount < data.rowCount,
				zoomApplied: view.hasZoom || view.hasXDomainOverride && xDomainCanRepresentZoom || view.hasYDomainOverride && yDomainCanRepresentZoom,
				sortApplied: view.hasSort || transforms.hasSortTransform,
				highlightApplied: view.hasHighlight || feedback.highlightKeyCount > 0,
				drillDownApplied: view.hasDrillDown,
				aggregateApplied: view.hasAggregate || transforms.hasAggregateTransform,
				reencodeApplied: view.hasReencode,
				annotateApplied: view.hasAnnotate,
				addRemoveApplied: view.hasAddRemove,
				navigateApplied: view.hasNavigate,
				focusApplied: view.hasFocus,
				linkedPropagationApplied: feedback.linkedSourceRefCount > 0,
				encodingReadable: encodings.channels.length > 0
			},
			signatures: {
				selections: selections.signature,
				transforms: transforms.signature,
				view: view.signature,
				encodings: encodings.signature,
				feedback: feedback.signature
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/VegaLiteWidgetAdapter.js
	function createVegaLiteWidgetAdapter(definition = {}) {
		return createWidgetAdapterDefinition({
			provider: "vega-lite",
			providerCapabilities: {
				supportedWidgetKinds: definition.kind ? [definition.kind] : [],
				renderStrategy: "vegaEmbed",
				stateApplyStrategy: "signalPatch",
				interactionBindingStrategy: "vegaViewListeners",
				supportsRendererMount: true,
				supportsRendererUpdate: true,
				supportsRendererDispose: true,
				supportsSignalPatching: true,
				supportsOptionMerging: false,
				supportsImperativeRender: false,
				supportsPointSelection: true,
				supportsIntervalSelection: true,
				supportsZoomPan: true,
				supportsSelectionReadback: true,
				supportsViewportReadback: true,
				supportsHighlightProjection: true,
				supportsInteractionEvents: true
			},
			mount({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			update({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			dispose({ view = null } = {}) {
				return view?.finalize?.();
			},
			bindHumanInteractions({ view, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
				return bindWidgetHumanInteractions({
					view,
					spec,
					interactionConfig,
					selectionSourceWidgetId,
					actionTargetRef,
					onActionCall,
					onSelectionChange
				});
			},
			async applyState({ view }) {
				try {
					await view.runAsync?.();
				} catch {}
			},
			readSelection() {
				return null;
			},
			readViewport() {
				return null;
			},
			...definition
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/shared/selectionResult.js
	function buildSelectionActionResult({ ctx, nextState, selectedCount, verificationHints }) {
		const targetRef = ctx.readQueryScope?.()?.widgetRef || null;
		const sourceWidgetRef = ctx.resolveTargetWidget({ targetRef })?.ref || null;
		const selectionRef = ctx.resolveSelectionRef(nextState, { targetRef });
		const rawPropagation = selectionRef ? ctx.propagate(selectionRef, {
			returnDetails: true,
			state: nextState
		}) : null;
		const propagation = Array.isArray(rawPropagation) ? {
			refs: rawPropagation,
			links: [],
			effects: [],
			nextState: ctx.readCurrentState()
		} : rawPropagation || ctx.collectPropagation(selectionRef, { state: nextState });
		const finalState = propagation.nextState || ctx.readCurrentState() || nextState;
		return {
			nextState: finalState,
			updatedRefs: ctx.collectUpdatedRefs(finalState, [
				...sourceWidgetRef ? [sourceWidgetRef] : [],
				...selectionRef ? [selectionRef] : [],
				...Array.isArray(propagation.refs) ? propagation.refs : []
			]),
			result: {
				selectedCount,
				propagated: propagation.links,
				propagationEffects: propagation.effects
			},
			verificationHints
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/bar/actions.js
	function replaceFilterTransformForField$1(transforms, field, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => transform?.filter?.field !== field);
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function replaceTaggedTransform$2(transforms, tag, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => transform?._widgetvaTag !== tag);
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function detectSubcategoryField(spec, explicitField) {
		if (typeof explicitField === "string" && explicitField.trim().length > 0) return explicitField;
		const encoding = spec?.encoding || {};
		return encoding?.xOffset?.field || encoding?.color?.field || null;
	}
	function detectCategoryField(spec, explicitField) {
		if (typeof explicitField === "string" && explicitField.trim().length > 0) return explicitField;
		const encoding = spec?.encoding || {};
		const xField = encoding?.x?.field || null;
		const yField = encoding?.y?.field || null;
		const xType = encoding?.x?.type || null;
		const yType = encoding?.y?.type || null;
		if ((xType === "nominal" || xType === "ordinal") && xField) return xField;
		if ((yType === "nominal" || yType === "ordinal") && yField) return yField;
		return xField || yField || null;
	}
	function detectBarChannels$1(spec, requestedChannel) {
		const encoding = spec?.encoding || {};
		const xType = encoding?.x?.type || null;
		const yType = encoding?.y?.type || null;
		const normalizedRequested = requestedChannel === "x" || requestedChannel === "y" ? requestedChannel : null;
		if (normalizedRequested) return {
			categoryChannel: normalizedRequested,
			valueChannel: normalizedRequested === "x" ? "y" : "x"
		};
		if ((yType === "nominal" || yType === "ordinal") && xType === "quantitative") return {
			categoryChannel: "y",
			valueChannel: "x"
		};
		return {
			categoryChannel: "x",
			valueChannel: "y"
		};
	}
	function aggregateBarValues(rows, { categoryField, valueField, aggregate, colorField, bySubcategory }) {
		const scores = /* @__PURE__ */ new Map();
		const normalizedAggregate = typeof aggregate === "string" && aggregate.trim().length > 0 ? aggregate.toLowerCase() : "mean";
		const groupedValues = /* @__PURE__ */ new Map();
		const pushValue = (key, value) => {
			if (!groupedValues.has(key)) groupedValues.set(key, []);
			groupedValues.get(key).push(value);
		};
		for (const row of rows) {
			if (!row || typeof row !== "object") continue;
			const category = row[categoryField];
			const value = row[valueField];
			if (category == null || typeof value !== "number" || Number.isNaN(value)) continue;
			if (bySubcategory != null) {
				if (!colorField || row[colorField] !== bySubcategory) continue;
				pushValue(category, value);
				continue;
			}
			if (colorField) {
				pushValue(JSON.stringify([category, row[colorField]]), value);
				continue;
			}
			pushValue(category, value);
		}
		const applyAggregate = (values) => {
			if (!Array.isArray(values) || values.length === 0) return 0;
			if (normalizedAggregate === "sum") return values.reduce((sum, value) => sum + value, 0);
			if (normalizedAggregate === "count") return values.length;
			if (normalizedAggregate === "median") {
				const sorted = [...values].sort((left, right) => left - right);
				const mid = Math.floor(sorted.length / 2);
				return sorted.length % 2 === 0 ? (sorted[mid - 1] + sorted[mid]) / 2 : sorted[mid];
			}
			if (normalizedAggregate === "min") return Math.min(...values);
			if (normalizedAggregate === "max") return Math.max(...values);
			return values.reduce((sum, value) => sum + value, 0) / values.length;
		};
		if (bySubcategory != null || !colorField) {
			for (const [key, values] of groupedValues.entries()) scores.set(key, applyAggregate(values));
			return scores;
		}
		const groupedByCategory = /* @__PURE__ */ new Map();
		for (const [compositeKey, values] of groupedValues.entries()) {
			const [category] = JSON.parse(compositeKey);
			groupedByCategory.set(category, (groupedByCategory.get(category) || 0) + applyAggregate(values));
		}
		return groupedByCategory;
	}
	function hasInlineObjectRows$1(spec) {
		return Array.isArray(spec?.data?.values) && spec.data.values.some((row) => row && typeof row === "object");
	}
	function isUrlBackedDataSpec$1(spec) {
		return typeof spec?.data?.url === "string" && spec.data.url.trim().length > 0;
	}
	function resolveBarRowsForAction(spec, ctx, widgetRef) {
		if (hasInlineObjectRows$1(spec)) return spec.data.values.filter((row) => row && typeof row === "object");
		if (isUrlBackedDataSpec$1(spec)) {
			const { rows } = ctx.readRowsForWidget(widgetRef);
			return Array.isArray(rows) ? rows.filter((row) => row && typeof row === "object") : [];
		}
		return [];
	}
	function buildBarActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
		const supportedWidgetKinds = ["bar"];
		return [
			makeActionDescriptor({
				name: "bar.selectCategory",
				title: "Select bar categories",
				description: "Select one or more categorical groups represented by bars.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						field: { type: "string" },
						values: {
							type: "array",
							items: { type: "string" }
						}
					},
					required: ["field", "values"]
				},
				postconditions: [{ description: "The active selection should contain the selected categorical values." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.selectCategory requires a valid bar target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a categorical selection on the bar chart."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeHighlightEffect(ref, "Linked widgets may highlight or filter the selected categories."))],
				examples: [{
					userGoal: "Select a subset of categories from the summary bar chart.",
					params: {
						field: "Origin",
						values: ["Japan", "USA"]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.sortBars",
				title: "Sort bars",
				description: "Sort bar groups by the requested order and optional field.",
				primitive: "sort",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "encodings" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						channel: { type: "string" },
						order: {
							type: "string",
							enum: ["ascending", "descending"]
						},
						field: { type: "string" },
						aggregate: { type: "string" },
						bySubcategory: { anyOf: [{ type: "string" }, { type: "number" }] }
					},
					required: ["channel", "order"]
				},
				postconditions: [{ description: "The bar chart encoding should include the requested sort rule." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.sortBars requires a valid bar target widget."
				}],
				examples: [{
					userGoal: "Sort the bar chart descending by the aggregated measure.",
					params: {
						channel: "x",
						order: "descending"
					}
				}, {
					userGoal: "Sort grouped or stacked categories using one specific subcategory as the ranking signal.",
					params: {
						channel: "x",
						order: "descending",
						bySubcategory: "Type1"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.highlightTopN",
				title: "Highlight top N bars",
				description: "Visually emphasize the top-N categories by measure while dimming the remaining bars.",
				primitive: "highlight",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						n: { type: "number" },
						order: {
							type: "string",
							enum: ["ascending", "descending"]
						},
						categoryField: { type: "string" },
						measureField: { type: "string" }
					},
					required: ["n"]
				},
				postconditions: [{ description: "The bar chart should visually emphasize the top-N categories by the requested measure." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.highlightTopN requires a valid bar target widget."
				}],
				examples: [{
					userGoal: "Highlight only the top 5 categories by value before comparing them.",
					params: {
						n: 5,
						order: "descending",
						categoryField: "category",
						measureField: "value"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.filterCategories",
				title: "Filter bar categories",
				description: "Filter the bar chart to a requested set of categories while leaving the other encodings intact.",
				primitive: "filter",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						categories: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						},
						field: { type: "string" }
					},
					required: ["categories"]
				},
				postconditions: [{ description: "The bar chart should retain only the requested categories through a categorical filter transform." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.filterCategories requires a valid bar target widget."
				}],
				effects: affectedRefs.map((ref) => makeHighlightEffect(ref, ref === widgetRef ? "The bar chart data is filtered down to the requested categories." : "Linked widgets may update to reflect the reduced category set.")),
				examples: [{
					userGoal: "Keep only a few categories before comparing them in detail.",
					params: {
						categories: ["A", "C"],
						field: "category"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.addBars",
				title: "Add bars back into view",
				description: "Expand the managed visible-category set of a bar chart by adding one or more category bars back into the visibility filter.",
				primitive: "addRemove",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						values: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						},
						field: { type: "string" }
					},
					required: ["values"]
				},
				postconditions: [{ description: "The bar chart visibility filter should expand to include the requested categories, and the stored bar visibility state should reflect the new visible set." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.addBars requires a valid bar target widget."
				}],
				effects: affectedRefs.map((ref) => makeHighlightEffect(ref, ref === widgetRef ? "The managed bar visibility filter expands to include the requested categories." : "Linked widgets may update to reflect the expanded category set.")),
				examples: [{
					userGoal: "Add previously hidden categories back into the current bar comparison without resetting the rest of the view.",
					params: {
						values: ["East", "West"],
						field: "category"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.removeBars",
				title: "Remove bars from view",
				description: "Shrink the managed visible-category set of a bar chart by removing one or more category bars from the visibility filter.",
				primitive: "addRemove",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						values: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						},
						field: { type: "string" }
					},
					required: ["values"]
				},
				postconditions: [{ description: "The bar chart visibility filter should exclude the requested categories, and the stored bar visibility state should reflect the reduced visible set." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.removeBars requires a valid bar target widget."
				}],
				effects: affectedRefs.map((ref) => makeHighlightEffect(ref, ref === widgetRef ? "The managed bar visibility filter contracts to exclude the requested categories." : "Linked widgets may update to reflect the reduced category set.")),
				examples: [{
					userGoal: "Temporarily remove several categories from the current bar comparison without resetting the rest of the view.",
					params: {
						values: ["East", "West"],
						field: "category"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.addBarItems",
				title: "Add grouped or stacked bar items back into view",
				description: "Expand the managed visible item set of a grouped or stacked bar chart by adding one or more (category, subcategory) pairs back into the visibility filter.",
				primitive: "addRemove",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						items: {
							type: "array",
							items: {
								type: "object",
								properties: {
									x: { anyOf: [{ type: "string" }, { type: "number" }] },
									sub: { anyOf: [{ type: "string" }, { type: "number" }] }
								},
								required: ["x", "sub"]
							}
						},
						xField: { type: "string" },
						subField: { type: "string" }
					},
					required: ["items"]
				},
				postconditions: [{ description: "The bar chart visibility filter should expand to include the requested (category, subcategory) items, and the stored visibility state should reflect the updated visible item set." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.addBarItems requires a valid bar target widget."
				}],
				effects: affectedRefs.map((ref) => makeHighlightEffect(ref, ref === widgetRef ? "The managed bar-item visibility filter expands to include the requested grouped or stacked members." : "Linked widgets may update to reflect the expanded item set.")),
				examples: [{
					userGoal: "Bring a few grouped or stacked members back into the current comparison without resetting the whole chart.",
					params: {
						items: [{
							x: "A",
							sub: "Type2"
						}],
						xField: "category",
						subField: "type"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.removeBarItems",
				title: "Remove grouped or stacked bar items from view",
				description: "Shrink the managed visible item set of a grouped or stacked bar chart by removing one or more (category, subcategory) pairs from the visibility filter.",
				primitive: "addRemove",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						items: {
							type: "array",
							items: {
								type: "object",
								properties: {
									x: { anyOf: [{ type: "string" }, { type: "number" }] },
									sub: { anyOf: [{ type: "string" }, { type: "number" }] }
								},
								required: ["x", "sub"]
							}
						},
						xField: { type: "string" },
						subField: { type: "string" }
					},
					required: ["items"]
				},
				postconditions: [{ description: "The bar chart visibility filter should exclude the requested (category, subcategory) items, and the stored visibility state should reflect the reduced visible item set." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.removeBarItems requires a valid bar target widget."
				}],
				effects: affectedRefs.map((ref) => makeHighlightEffect(ref, ref === widgetRef ? "The managed bar-item visibility filter contracts to exclude the requested grouped or stacked members." : "Linked widgets may update to reflect the reduced item set.")),
				examples: [{
					userGoal: "Temporarily remove a few grouped or stacked members from the current comparison without resetting the whole chart.",
					params: {
						items: [{
							x: "A",
							sub: "Type2"
						}],
						xField: "category",
						subField: "type"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.filterSubcategories",
				title: "Filter bar subcategories",
				description: "Exclude one or more grouped or stacked subcategories from the current bar chart while preserving the remaining categories.",
				primitive: "filter",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						subcategoriesToRemove: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						},
						subField: { type: "string" }
					},
					required: ["subcategoriesToRemove"]
				},
				postconditions: [{ description: "The bar chart transform list should exclude the requested subcategories, and the color domain should drop them when explicitly enumerated." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.filterSubcategories requires a valid bar target widget."
				}],
				effects: affectedRefs.map((ref) => makeHighlightEffect(ref, ref === widgetRef ? "The requested grouped or stacked subcategories are excluded from the current bar chart." : "Linked widgets may update to reflect the removed subcategories.")),
				examples: [{
					userGoal: "Remove several grouped or stacked subcategories before comparing the remaining composition.",
					params: {
						subcategoriesToRemove: ["Type2"],
						subField: "type"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.expandStack",
				title: "Expand a stacked bar into parallel bars",
				description: "Filter to one category from a stacked bar chart and expand its stacked segments into parallel bars for easier comparison.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { category: { anyOf: [{ type: "string" }, { type: "number" }] } },
					required: ["category"]
				},
				postconditions: [{ description: "The bar chart should filter to the requested category, move the stacked grouping field onto the x-axis, and remove y-axis stacking." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.expandStack requires a valid bar target widget."
				}],
				effects: affectedRefs.map((ref) => makeHighlightEffect(ref, ref === widgetRef ? "The stacked category is expanded into side-by-side bars for direct comparison." : "Linked widgets may update to reflect the expanded stacked-bar focus view.")),
				examples: [{
					userGoal: "Expand one stacked category to compare its internal composition without stacked baselines.",
					params: { category: "East China" }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "bar.toggleStackMode",
				title: "Toggle grouped or stacked mode",
				description: "Switch a grouped/stacked bar chart between grouped and stacked display modes.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { mode: {
						type: "string",
						enum: ["grouped", "stacked"]
					} },
					required: ["mode"]
				},
				postconditions: [{ description: "Grouped mode should add xOffset and remove y-axis stacking; stacked mode should remove xOffset and restore y-axis stacking." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid bar widget in the current workspace.",
					failureMessage: "bar.toggleStackMode requires a valid bar target widget."
				}],
				effects: affectedRefs.map((ref) => makeHighlightEffect(ref, ref === widgetRef ? "The bar chart display mode switches between grouped and stacked layouts." : "Linked widgets may update to reflect the new stacked/grouped comparison view.")),
				examples: [{
					userGoal: "Switch the current stacked bar chart into grouped mode for easier cross-category comparison.",
					params: { mode: "grouped" }
				}],
				reversible: true
			})
		];
	}
	function registerBarActions(actionExecutor) {
		if (!actionExecutor.has("bar.selectCategory")) actionExecutor.register({ name: "bar.selectCategory" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar"
			});
			const field = typeof params.field === "string" ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => typeof value === "string") : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("bar.selectCategory requires a bar target, field, and one or more values.");
			const { rows: visibleRows } = ctx.readRowsForWidget(targetWidget.ref);
			const matchedCount = visibleRows.filter((row) => values.includes(row?.[field])).length;
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "category",
					field,
					values,
					predicates: [{
						field,
						op: "in",
						value: values
					}],
					count: matchedCount,
					summary: `${field}: ${values.join(", ")}`
				}),
				selectedCount: matchedCount,
				verificationHints: ["Read the updated bar selection state.", "Read the linked widget feedback or visible rows to confirm highlight propagation."]
			});
		});
		if (!actionExecutor.has("bar.sortBars")) actionExecutor.register({ name: "bar.sortBars" }, async (call, ctx) => {
			const params = call?.params || {};
			const channel = typeof params.channel === "string" ? params.channel : null;
			const order = params.order === "ascending" || params.order === "descending" ? params.order : null;
			const field = typeof params.field === "string" ? params.field : null;
			const aggregate = typeof params.aggregate === "string" ? params.aggregate : null;
			const bySubcategory = params.bySubcategory;
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.sortBars requires a valid bar target widget."
			});
			const targetWidgetId = targetWidget?.widgetId || null;
			if (!channel || !order || !targetWidgetId) throw new Error("bar.sortBars requires a bar target, channel, and order.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for bar sort updates.");
					const { categoryChannel, valueChannel } = detectBarChannels$1(spec, channel);
					const currentCategoryChannel = spec.encoding?.[categoryChannel];
					const currentValueChannel = spec.encoding?.[valueChannel];
					if (!currentCategoryChannel || typeof currentCategoryChannel !== "object") throw new Error(`The active spec does not define encoding channel "${categoryChannel}".`);
					if (!currentValueChannel || typeof currentValueChannel !== "object") throw new Error(`The active spec does not define encoding channel "${valueChannel}".`);
					const categoryField = currentCategoryChannel.field;
					const measureField = field && field !== categoryField ? field : currentValueChannel.field;
					if (!categoryField || !measureField) throw new Error("bar.sortBars requires both categorical and quantitative fields on the active bar spec.");
					const rows = resolveBarRowsForAction(spec, ctx, targetWidget.ref);
					if (rows.length === 0) throw new Error("bar.sortBars requires bar rows from inline values or the current runtime data view.");
					const colorField = spec?.encoding?.color?.field || null;
					const sortScores = aggregateBarValues(rows, {
						categoryField,
						valueField: measureField,
						aggregate: aggregate || currentValueChannel.aggregate || "mean",
						colorField,
						bySubcategory
					});
					const sortedCategories = [...new Set(rows.map((row) => row[categoryField]).filter((value) => value != null))].sort((left, right) => {
						const leftScore = sortScores.get(left) ?? 0;
						const rightScore = sortScores.get(right) ?? 0;
						if (leftScore === rightScore) return String(left).localeCompare(String(right));
						return order === "ascending" ? leftScore - rightScore : rightScore - leftScore;
					});
					const nextEncoding = {
						...spec.encoding || {},
						[categoryChannel]: {
							...currentCategoryChannel,
							sort: sortedCategories
						}
					};
					return {
						...spec,
						encoding: nextEncoding
					};
				}),
				result: {
					widgetId: targetWidgetId,
					channel,
					order,
					...field ? { field } : {},
					...aggregate ? { aggregate } : {},
					...bySubcategory != null ? { bySubcategory } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the bar chart encoding now uses an explicit sorted category array.", "Read the target widget state to confirm the requested category ranking propagated to the view."]
			};
		});
		if (!actionExecutor.has("bar.highlightTopN")) actionExecutor.register({ name: "bar.highlightTopN" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.highlightTopN requires a valid bar target widget."
			});
			const n = Number.isFinite(params.n) ? Math.max(1, Math.floor(params.n)) : null;
			const order = params.order === "ascending" || params.order === "descending" ? params.order : "descending";
			if (!targetWidget || !n) throw new Error("bar.highlightTopN requires a bar target and a positive integer n.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for bar highlight updates.");
					const categoryField = typeof params.categoryField === "string" ? params.categoryField : spec?.encoding?.x?.field || spec?.encoding?.y?.field || null;
					const measureField = typeof params.measureField === "string" ? params.measureField : spec?.encoding?.y?.field || spec?.encoding?.x?.field || null;
					const rows = resolveBarRowsForAction(spec, ctx, targetWidget.ref);
					if (!categoryField || !measureField || rows.length === 0) throw new Error("bar.highlightTopN requires category/measure fields and bar rows from inline values or the current runtime data view.");
					const totals = /* @__PURE__ */ new Map();
					for (const row of rows) {
						const category = row?.[categoryField];
						const value = row?.[measureField];
						if (category == null || typeof value !== "number") continue;
						totals.set(category, (totals.get(category) || 0) + value);
					}
					const sortedCategories = [...totals.entries()].sort((a, b) => order === "ascending" ? a[1] - b[1] : b[1] - a[1]).slice(0, n).map(([category]) => category);
					if (sortedCategories.length === 0) throw new Error("bar.highlightTopN could not derive any categories from the current bar data.");
					const testExpr = sortedCategories.map((category) => `datum['${categoryField}'] == ${typeof category === "string" ? `'${category}'` : String(category)}`).join(" || ");
					return {
						...spec,
						encoding: {
							...spec.encoding || {},
							opacity: {
								condition: {
									test: testExpr,
									value: 1
								},
								value: .2
							}
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					n,
					order,
					...typeof params.categoryField === "string" ? { categoryField: params.categoryField } : {},
					...typeof params.measureField === "string" ? { measureField: params.measureField } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the bar chart opacity condition now highlights the top-N categories.", "Read the target widget view state to confirm the requested top categories are emphasized."]
			};
		});
		if (!actionExecutor.has("bar.filterCategories")) actionExecutor.register({ name: "bar.filterCategories" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.filterCategories requires a valid bar target widget."
			});
			const categories = Array.isArray(params.categories) ? params.categories.filter((value) => value != null) : [];
			if (!targetWidget || categories.length === 0) throw new Error("bar.filterCategories requires a bar target and one or more categories.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for bar category filtering.");
					const field = typeof params.field === "string" ? params.field : spec?.encoding?.x?.field || spec?.encoding?.y?.field || null;
					if (!field) throw new Error("bar.filterCategories requires a categorical field on the active bar spec.");
					return {
						...spec,
						transform: replaceFilterTransformForField$1(spec.transform, field, { filter: {
							field,
							oneOf: categories
						} })
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					field: typeof params.field === "string" ? params.field : void 0,
					categories
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the bar chart transform now filters to the requested categories.", "Read the target widget rows to confirm only the requested categories remain visible."]
			};
		});
		if (!actionExecutor.has("bar.addBars")) actionExecutor.register({ name: "bar.addBars" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.addBars requires a valid bar target widget."
			});
			const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
			if (!targetWidget || values.length === 0) throw new Error("bar.addBars requires a bar target and one or more category values.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for expanding bar visibility.");
					const field = detectCategoryField(spec, params.field);
					if (!field) throw new Error("bar.addBars requires a categorical field on the active bar spec.");
					const rows = resolveBarRowsForAction(spec, ctx, targetWidget.ref);
					const existingCategories = new Set(rows.map((row) => row[field]).filter((value) => value != null));
					const visibilityState = spec?._bar_visibility_state;
					const visible = visibilityState?.mode === "x" && visibilityState?.x_field === field && Array.isArray(visibilityState.visible_x) ? new Set(visibilityState.visible_x) : new Set(existingCategories);
					const missingValues = [];
					for (const value of values) if (existingCategories.has(value)) visible.add(value);
					else missingValues.push(value);
					const nextVisible = [...visible].sort((left, right) => String(left).localeCompare(String(right)));
					const nextSpec = {
						...spec,
						transform: replaceTaggedTransform$2(spec.transform, "bar.addBars", {
							filter: {
								field,
								oneOf: nextVisible
							},
							_widgetvaTag: "bar.addBars"
						}),
						_bar_visibility_state: {
							mode: "x",
							x_field: field,
							visible_x: nextVisible,
							last_operation: "add"
						}
					};
					if (missingValues.length > 0) nextSpec._bar_visibility_state = {
						...nextSpec._bar_visibility_state,
						missing_values: missingValues
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					values,
					...typeof params.field === "string" ? { field: params.field } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the managed bar visibility filter now includes the requested categories.", "Read the target widget view state to confirm the visible category set expanded without resetting unrelated bar encodings."]
			};
		});
		if (!actionExecutor.has("bar.removeBars")) actionExecutor.register({ name: "bar.removeBars" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.removeBars requires a valid bar target widget."
			});
			const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
			if (!targetWidget || values.length === 0) throw new Error("bar.removeBars requires a bar target and one or more category values.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for shrinking bar visibility.");
					const field = detectCategoryField(spec, params.field);
					if (!field) throw new Error("bar.removeBars requires a categorical field on the active bar spec.");
					const rows = resolveBarRowsForAction(spec, ctx, targetWidget.ref);
					const existingCategories = new Set(rows.map((row) => row[field]).filter((value) => value != null));
					const visibilityState = spec?._bar_visibility_state;
					const visible = visibilityState?.mode === "x" && visibilityState?.x_field === field && Array.isArray(visibilityState.visible_x) ? new Set(visibilityState.visible_x) : new Set(existingCategories);
					for (const value of values) visible.delete(value);
					const nextVisible = [...visible].sort((left, right) => String(left).localeCompare(String(right)));
					return {
						...spec,
						transform: replaceTaggedTransform$2(spec.transform, "bar.addBars", {
							filter: {
								field,
								oneOf: nextVisible
							},
							_widgetvaTag: "bar.addBars"
						}),
						_bar_visibility_state: {
							mode: "x",
							x_field: field,
							visible_x: nextVisible,
							last_operation: "remove"
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					values,
					...typeof params.field === "string" ? { field: params.field } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the managed bar visibility filter now excludes the requested categories.", "Read the target widget view state to confirm the visible category set shrank without resetting unrelated bar encodings."]
			};
		});
		if (!actionExecutor.has("bar.addBarItems")) actionExecutor.register({ name: "bar.addBarItems" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.addBarItems requires a valid bar target widget."
			});
			const items = Array.isArray(params.items) ? params.items.filter((item) => item && typeof item === "object" && item.x != null && item.sub != null) : [];
			if (!targetWidget || items.length === 0) throw new Error("bar.addBarItems requires a bar target and one or more { x, sub } items.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for expanding bar-item visibility.");
					const xField = detectCategoryField(spec, params.xField);
					const subField = detectSubcategoryField(spec, params.subField);
					if (!xField) throw new Error("bar.addBarItems requires a categorical x field on the active bar spec.");
					if (!subField) throw new Error("bar.addBarItems requires a grouped or stacked subcategory field on the active bar spec.");
					const rows = resolveBarRowsForAction(spec, ctx, targetWidget.ref);
					const existingPairs = new Set(rows.map((row) => row[xField] != null && row[subField] != null ? JSON.stringify([row[xField], row[subField]]) : null).filter(Boolean));
					const visibilityState = spec?._bar_visibility_state;
					const visiblePairs = visibilityState?.mode === "item" && visibilityState?.x_field === xField && visibilityState?.sub_field === subField && Array.isArray(visibilityState.visible_items) ? new Set(visibilityState.visible_items.map((pair) => JSON.stringify(pair))) : new Set(existingPairs);
					const missingItems = [];
					for (const item of items) {
						const pairKey = JSON.stringify([item.x, item.sub]);
						if (existingPairs.has(pairKey)) visiblePairs.add(pairKey);
						else missingItems.push({
							x: item.x,
							sub: item.sub
						});
					}
					const nextVisiblePairs = [...visiblePairs].map((pairKey) => JSON.parse(pairKey)).sort((left, right) => {
						const xCompare = String(left[0]).localeCompare(String(right[0]));
						if (xCompare !== 0) return xCompare;
						return String(left[1]).localeCompare(String(right[1]));
					});
					const filterExpr = nextVisiblePairs.length > 0 ? nextVisiblePairs.map(([xValue, subValue]) => `(datum['${xField}'] == ${JSON.stringify(xValue)} && datum['${subField}'] == ${JSON.stringify(subValue)})`).join(" || ") : "false";
					const nextSpec = {
						...spec,
						transform: replaceTaggedTransform$2(spec.transform, "bar.addBarItems", {
							filter: filterExpr,
							_widgetvaTag: "bar.addBarItems"
						}),
						_bar_visibility_state: {
							mode: "item",
							x_field: xField,
							sub_field: subField,
							visible_items: nextVisiblePairs,
							last_operation: "add"
						}
					};
					if (missingItems.length > 0) nextSpec._bar_visibility_state = {
						...nextSpec._bar_visibility_state,
						missing_items: missingItems
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					items,
					...typeof params.xField === "string" ? { xField: params.xField } : {},
					...typeof params.subField === "string" ? { subField: params.subField } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the managed bar-item visibility filter now includes the requested grouped or stacked members.", "Read the target widget view state to confirm the visible item set expanded without resetting unrelated bar encodings."]
			};
		});
		if (!actionExecutor.has("bar.removeBarItems")) actionExecutor.register({ name: "bar.removeBarItems" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.removeBarItems requires a valid bar target widget."
			});
			const items = Array.isArray(params.items) ? params.items.filter((item) => item && typeof item === "object" && item.x != null && item.sub != null) : [];
			if (!targetWidget || items.length === 0) throw new Error("bar.removeBarItems requires a bar target and one or more { x, sub } items.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for shrinking bar-item visibility.");
					const xField = detectCategoryField(spec, params.xField);
					const subField = detectSubcategoryField(spec, params.subField);
					if (!xField) throw new Error("bar.removeBarItems requires a categorical x field on the active bar spec.");
					if (!subField) throw new Error("bar.removeBarItems requires a grouped or stacked subcategory field on the active bar spec.");
					const rows = resolveBarRowsForAction(spec, ctx, targetWidget.ref);
					const existingPairs = new Set(rows.map((row) => row[xField] != null && row[subField] != null ? JSON.stringify([row[xField], row[subField]]) : null).filter(Boolean));
					const visibilityState = spec?._bar_visibility_state;
					const visiblePairs = visibilityState?.mode === "item" && visibilityState?.x_field === xField && visibilityState?.sub_field === subField && Array.isArray(visibilityState.visible_items) ? new Set(visibilityState.visible_items.map((pair) => JSON.stringify(pair))) : new Set(existingPairs);
					for (const item of items) visiblePairs.delete(JSON.stringify([item.x, item.sub]));
					const nextVisiblePairs = [...visiblePairs].map((pairKey) => JSON.parse(pairKey)).sort((left, right) => {
						const xCompare = String(left[0]).localeCompare(String(right[0]));
						if (xCompare !== 0) return xCompare;
						return String(left[1]).localeCompare(String(right[1]));
					});
					const filterExpr = nextVisiblePairs.length > 0 ? nextVisiblePairs.map(([xValue, subValue]) => `(datum['${xField}'] == ${JSON.stringify(xValue)} && datum['${subField}'] == ${JSON.stringify(subValue)})`).join(" || ") : "false";
					return {
						...spec,
						transform: replaceTaggedTransform$2(spec.transform, "bar.addBarItems", {
							filter: filterExpr,
							_widgetvaTag: "bar.addBarItems"
						}),
						_bar_visibility_state: {
							mode: "item",
							x_field: xField,
							sub_field: subField,
							visible_items: nextVisiblePairs,
							last_operation: "remove"
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					items,
					...typeof params.xField === "string" ? { xField: params.xField } : {},
					...typeof params.subField === "string" ? { subField: params.subField } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the managed bar-item visibility filter now excludes the requested grouped or stacked members.", "Read the target widget view state to confirm the visible item set shrank without resetting unrelated bar encodings."]
			};
		});
		if (!actionExecutor.has("bar.filterSubcategories")) actionExecutor.register({ name: "bar.filterSubcategories" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.filterSubcategories requires a valid bar target widget."
			});
			const subcategoriesToRemove = Array.isArray(params.subcategoriesToRemove) ? params.subcategoriesToRemove.filter((value) => value != null) : [];
			if (!targetWidget || subcategoriesToRemove.length === 0) throw new Error("bar.filterSubcategories requires a bar target and one or more subcategories to remove.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for bar subcategory filtering.");
					const subField = detectSubcategoryField(spec, params.subField);
					if (!subField) throw new Error("bar.filterSubcategories requires a subcategory field on the active bar spec.");
					const nextSpec = {
						...spec,
						transform: replaceTaggedTransform$2(spec.transform, "bar.filterSubcategories", {
							filter: {
								field: subField,
								notOneOf: [...subcategoriesToRemove]
							},
							_widgetvaTag: "bar.filterSubcategories"
						})
					};
					const colorScaleDomain = nextSpec?.encoding?.color?.scale?.domain;
					if (Array.isArray(colorScaleDomain)) nextSpec.encoding = {
						...nextSpec.encoding || {},
						color: {
							...nextSpec.encoding?.color || {},
							scale: {
								...nextSpec.encoding?.color?.scale || {},
								domain: colorScaleDomain.filter((value) => !subcategoriesToRemove.includes(value))
							}
						}
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					subcategoriesToRemove,
					...typeof params.subField === "string" ? { subField: params.subField } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the bar chart transform now excludes the requested subcategories.", "Read the target widget rows and color scale metadata to confirm the removed subcategories no longer appear."]
			};
		});
		if (!actionExecutor.has("bar.expandStack")) actionExecutor.register({ name: "bar.expandStack" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.expandStack requires a valid bar target widget."
			});
			const category = params.category;
			if (!targetWidget || category == null) throw new Error("bar.expandStack requires a bar target and a category value.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for stacked-bar expansion.");
					const encoding = spec.encoding || {};
					const xField = encoding?.x?.field || null;
					const colorEncoding = encoding?.color || {};
					const colorField = colorEncoding?.field || null;
					if (!xField) throw new Error("bar.expandStack requires an x encoding field on the active bar spec.");
					if (!colorField) throw new Error("bar.expandStack requires a color encoding field on the active stacked bar spec.");
					const nextEncoding = {
						...encoding || {},
						x: {
							field: colorField,
							type: "nominal",
							title: colorEncoding?.title || colorField,
							axis: { labelAngle: -45 },
							...colorEncoding?.scale?.domain ? { sort: colorEncoding.scale.domain } : {}
						},
						y: { ...encoding?.y || {} }
					};
					delete nextEncoding.y.stack;
					return {
						...spec,
						transform: replaceTaggedTransform$2(spec.transform, "bar.expandStack", {
							filter: `datum['${xField}'] == ${JSON.stringify(category)}`,
							_widgetvaTag: "bar.expandStack"
						}),
						encoding: nextEncoding,
						_bar_expand_stack_state: {
							category,
							category_field: xField,
							group_field: colorField
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					category
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the chart is filtered to the requested category and the x-axis now uses the stacked grouping field.", "Read the target widget view state to confirm y-axis stacking was removed for parallel comparison."]
			};
		});
		if (!actionExecutor.has("bar.toggleStackMode")) actionExecutor.register({ name: "bar.toggleStackMode" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "bar",
				message: "bar.toggleStackMode requires a valid bar target widget."
			});
			const mode = params.mode === "grouped" || params.mode === "stacked" ? params.mode : null;
			if (!targetWidget || !mode) throw new Error("bar.toggleStackMode requires a bar target and mode of either \"grouped\" or \"stacked\".");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for bar stack-mode toggling.");
					const colorField = spec?.encoding?.color?.field || null;
					if (!colorField) throw new Error("bar.toggleStackMode requires a color encoding field on the active bar spec.");
					const nextEncoding = {
						...spec.encoding || {},
						y: { ...spec.encoding?.y || {} }
					};
					if (mode === "grouped") {
						nextEncoding.xOffset = { field: colorField };
						delete nextEncoding.y.stack;
					} else {
						delete nextEncoding.xOffset;
						nextEncoding.y.stack = "zero";
					}
					return {
						...spec,
						encoding: nextEncoding,
						_stack_mode: mode
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					mode
				},
				verificationHints: ["Call perception.inspectViewConfig to verify grouped mode adds xOffset and stacked mode restores y-axis stacking.", "Read the target widget view state to confirm the stored stack-mode marker was updated."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/shared/dataPerception.js
	function runDataPerceptionQuery({ ctx, dataQueryExecutor, targetWidget, kind, spec }) {
		const { dataRef } = ctx.resolveRowsForWidget(targetWidget, spec || {});
		const queryResult = dataQueryExecutor.run({
			dataRef,
			query: {
				kind,
				spec: spec || {}
			}
		});
		return {
			dataRef,
			ok: queryResult?.ok === true,
			result: queryResult?.result || null,
			error: queryResult?.error || null
		};
	}
	function buildPerceptionDataResult({ dataQueryResult, fallbackDataRef = null }) {
		return {
			dataRef: dataQueryResult?.dataRef || fallbackDataRef || null,
			...dataQueryResult?.result || {}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/shared/perceptionScope.js
	function buildScopedPerceptionParamsSchema(extraProperties = {}) {
		return {
			type: "object",
			properties: { ...extraProperties || {} }
		};
	}
	function buildQueryScopeExample({ userGoal, params = {}, widgetRef = "wl://demo/workspace/main/widget/current", dataRef = "wl://demo/workspace/main/data/current_view", selectionRef = null } = {}) {
		return {
			userGoal,
			params: {
				...params || {},
				queryScope: {
					widgetRef,
					dataRef,
					...selectionRef ? { selectionRef } : {}
				}
			}
		};
	}
	function appendQueryScopeGuidance(description) {
		const base = typeof description === "string" ? description.trim() : "";
		const guidance = "Use queryScope for widget/data/selection targeting.";
		if (!base) return guidance;
		return `${base} ${guidance}`;
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/bar/perception.js
	function buildBarPerceptionDescriptors({ dataRef }) {
		return [makePerceptionDescriptor({
			name: "perception.compareGroups",
			title: "Compare groups",
			description: appendQueryScopeGuidance("Compare summary statistics for specified groups in the visible bar data."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				groupField: { type: "string" },
				valueField: { type: "string" },
				groups: {
					type: "array",
					items: { type: "string" }
				}
			}),
			sideEffectFree: true,
			evidenceKinds: ["groupComparison", "aggregateEvidence"],
			examples: [{
				userGoal: "Compare a few selected groups in the summary bars.",
				params: {
					groupField: "Origin",
					valueField: "count",
					groups: ["Japan", "USA"]
				}
			}, buildQueryScopeExample({
				userGoal: "Compare grouped bar summaries inside one filtered selection.",
				params: {
					groupField: "Origin",
					valueField: "count",
					groups: ["Japan", "USA"]
				},
				widgetRef: "wl://demo/workspace/main/widget/bar_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/bar_a/selection/current"
			})]
		}), makePerceptionDescriptor({
			name: "perception.findExtremes",
			title: "Find extremes",
			description: appendQueryScopeGuidance("Return top-k or bottom-k visible rows by a numeric field."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				field: { type: "string" },
				direction: { type: "string" },
				limit: { type: "number" }
			}),
			sideEffectFree: true,
			evidenceKinds: ["rankEvidence", "aggregateEvidence"],
			examples: [{
				userGoal: "Find the highest or lowest categories in the current summary view.",
				params: {
					field: "count",
					direction: "max",
					limit: 5
				}
			}, buildQueryScopeExample({
				userGoal: "Find the highest bars inside one filtered bar subset.",
				params: {
					field: "count",
					direction: "max",
					limit: 5
				},
				widgetRef: "wl://demo/workspace/main/widget/bar_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/bar_a/selection/current"
			})]
		})];
	}
	function registerBarPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.compareGroups", { supportedWidgetKinds: ["bar"] })) perceptionRegistry.register({
			name: "perception.compareGroups",
			supportedWidgetKinds: ["bar"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "bar" });
			const params = ctx.readCallParams();
			const comparison = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "compareGroups",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: comparison,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["bar"] });
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["bar"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["bar"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "bar" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["bar"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/bar/humanInteraction.js
	function getBarHumanInteractionConfig() {
		return {
			mode: "categoryClick",
			actionName: "bar.selectCategory",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/shared/applyVegaLiteState.js
	function setSignalSafely(view, name, value) {
		if (!view || typeof view.signal !== "function") return;
		try {
			view.signal(name, value);
		} catch {}
	}
	function normalizeSelections$3(state) {
		return Object.values(state?.selections || {}).filter(Boolean);
	}
	function resolveRepresentativeSelection$2(selections) {
		const normalizedSelections = Array.isArray(selections) ? selections.filter(Boolean) : [];
		if (normalizedSelections.length !== 1) return null;
		return normalizedSelections[0] || null;
	}
	function resolvePrimaryIntervalSelection$2(selections) {
		return (Array.isArray(selections) ? selections : []).find((selection) => selection?.kind === "interval") || null;
	}
	function selectionValuesFromPredicates(predicates) {
		if (!Array.isArray(predicates)) return [];
		return predicates.filter((predicate) => predicate?.op === "equals" || predicate?.op === "in").flatMap((predicate) => Array.isArray(predicate?.value) ? predicate.value : [predicate?.value]).filter((value) => value != null);
	}
	function setDomainSignals(view, state) {
		const xDomain = state?.view?.xDomain;
		const yDomain = state?.view?.yDomain;
		if (Array.isArray(xDomain)) {
			setSignalSafely(view, "xDomain", xDomain);
			setSignalSafely(view, "widgetva_xDomain", xDomain);
		}
		if (Array.isArray(yDomain)) {
			setSignalSafely(view, "yDomain", yDomain);
			setSignalSafely(view, "widgetva_yDomain", yDomain);
		}
	}
	function setIntervalSelectionSignals(view, selection) {
		const xDomain = selection?.domain?.xDomain;
		const yDomain = selection?.domain?.yDomain;
		if (!Array.isArray(xDomain) || !Array.isArray(yDomain)) {
			setSignalSafely(view, "brush", null);
			setSignalSafely(view, "brush_x_1", null);
			setSignalSafely(view, "brush_x_2", null);
			setSignalSafely(view, "brush_y_1", null);
			setSignalSafely(view, "brush_y_2", null);
			return;
		}
		setSignalSafely(view, "brush", {
			x: xDomain,
			y: yDomain
		});
		setSignalSafely(view, "brush_x_1", xDomain[0]);
		setSignalSafely(view, "brush_x_2", xDomain[1]);
		setSignalSafely(view, "brush_y_1", yDomain[0]);
		setSignalSafely(view, "brush_y_2", yDomain[1]);
	}
	function setSelectionSummarySignals(view, selection, selections = []) {
		const normalizedSelections = Array.isArray(selections) ? selections.filter(Boolean) : [];
		const predicates = selection ? Array.isArray(selection?.predicates) ? selection.predicates : [] : normalizedSelections.flatMap((entry) => Array.isArray(entry?.predicates) ? entry.predicates : []);
		const selectionSummaries = normalizedSelections.map((entry) => entry?.summary || "").filter(Boolean);
		const selectionPredicates = normalizedSelections.map((entry) => Array.isArray(entry?.predicates) ? entry.predicates : []);
		setSignalSafely(view, "widgetva_selectionKind", selection?.kind || null);
		setSignalSafely(view, "widgetva_selectionDomain", selection?.domain || null);
		setSignalSafely(view, "widgetva_selectedValues", selectionValuesFromPredicates(predicates));
		setSignalSafely(view, "widgetva_selectionCount", normalizedSelections.length);
		setSignalSafely(view, "widgetva_selections", normalizedSelections);
		setSignalSafely(view, "widgetva_selectionSummaries", selectionSummaries);
		setSignalSafely(view, "widgetva_selectionPredicateGroups", selectionPredicates);
	}
	function setWidgetVASignals(view, state, selection, selections) {
		const feedback = state?.feedback || {};
		setSignalSafely(view, "widgetva_selection", selection || null);
		setSelectionSummarySignals(view, selection, selections);
		const normalizedSelections = Array.isArray(selections) ? selections.filter(Boolean) : [];
		const aggregatedSummary = normalizedSelections.map((entry) => entry?.summary || "").filter(Boolean).join(" · ");
		const aggregatedPredicates = selection ? Array.isArray(selection?.predicates) ? selection.predicates : [] : normalizedSelections.flatMap((entry) => Array.isArray(entry?.predicates) ? entry.predicates : []);
		setSignalSafely(view, "widgetva_selectionSummary", selection?.summary || aggregatedSummary);
		setSignalSafely(view, "widgetva_selectionPredicates", aggregatedPredicates);
		setSignalSafely(view, "widgetva_selectedCount", state?.data?.selectedCount ?? 0);
		setSignalSafely(view, "widgetva_visibleCount", state?.data?.visibleCount ?? 0);
		setSignalSafely(view, "widgetva_highlightedKeys", Array.isArray(feedback?.highlightedKeys) ? feedback.highlightedKeys : []);
		setSignalSafely(view, "widgetva_inboundLinkIds", Array.isArray(feedback?.inboundLinkIds) ? feedback.inboundLinkIds : []);
		setSignalSafely(view, "widgetva_linkedSourceRefs", Array.isArray(feedback?.linkedSourceRefs) ? feedback.linkedSourceRefs : []);
	}
	async function applyVegaLiteRuntimeState({ view, state }) {
		if (!view || !state) return;
		const selections = normalizeSelections$3(state);
		const selection = resolveRepresentativeSelection$2(selections);
		const intervalSelection = resolvePrimaryIntervalSelection$2(selections);
		setDomainSignals(view, state);
		setIntervalSelectionSignals(view, intervalSelection);
		setWidgetVASignals(view, state, selection, selections);
		try {
			await view.runAsync?.();
		} catch {}
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/bar/state.js
	async function applyBarState(args) {
		return applyVegaLiteRuntimeState(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/barWidgetAdapter.js
	var barWidgetAdapter = createVegaLiteWidgetAdapter({
		kind: "bar",
		bindHumanInteractions({ view, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
			return bindWidgetHumanInteractions({
				view,
				spec,
				interactionConfig,
				selectionSourceWidgetId,
				actionTargetRef,
				onActionCall,
				onSelectionChange
			});
		},
		async applyState(args) {
			return applyBarState(args);
		},
		buildActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
			return buildBarActionDescriptors({
				widgetRef,
				selectionRef,
				scope,
				affectedRefs
			});
		},
		buildPerceptionDescriptors({ dataRef }) {
			return buildBarPerceptionDescriptors({ dataRef });
		},
		getHumanInteractionConfig() {
			return getBarHumanInteractionConfig();
		},
		registerActions(actionExecutor) {
			return registerBarActions(actionExecutor);
		},
		registerPerceptionQueries(perceptionRegistry) {
			return registerBarPerceptionQueries(perceptionRegistry);
		}
	});
	//#endregion
	//#region ../../widgetva-kit/src/adapters/providerFamilyBehavior.js
	function clone$25(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function uniqueValues$2(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => value != null))];
	}
	function normalizeSelections$2(state) {
		return Object.values(state?.selections || {}).filter(Boolean);
	}
	function resolveRepresentativeSelection$1(selections) {
		const normalizedSelections = Array.isArray(selections) ? selections.filter(Boolean) : [];
		if (normalizedSelections.length === 1) return normalizedSelections[0] || null;
		const pointSelections = normalizedSelections.filter((selection) => selection?.kind === "point");
		if (pointSelections.length === 1) return pointSelections[0] || null;
		return normalizedSelections[0] || null;
	}
	function resolvePrimaryIntervalSelection$1(selections) {
		return (Array.isArray(selections) ? selections : []).find((selection) => selection?.kind === "interval") || null;
	}
	function buildHumanActionCall({ name, actionTargetRef, params }) {
		return {
			callId: `human_${Date.now()}`,
			name,
			actor: "human",
			targetRef: actionTargetRef || void 0,
			params: {
				targetRef: actionTargetRef || void 0,
				...params
			}
		};
	}
	function resolveCategoryField({ spec, interactionConfig, selection = null }) {
		if (typeof selection?.field === "string" && selection.field.length > 0) return selection.field;
		const enc = spec?.encoding || {};
		const preferredChannel = interactionConfig?.categoryFieldChannel;
		if (preferredChannel && enc?.[preferredChannel]?.field) return enc[preferredChannel].field;
		if (typeof interactionConfig?.categoryField === "string" && interactionConfig.categoryField.length > 0) return interactionConfig.categoryField;
		return enc.color?.field || enc.shape?.field || enc.detail?.field || enc.key?.field || enc.x?.field || enc.y?.field || null;
	}
	function resolveCellFields({ spec, interactionConfig, selection = null }) {
		return {
			xField: selection?.xField || interactionConfig?.xField || spec?.encoding?.x?.field || null,
			yField: selection?.yField || interactionConfig?.yField || spec?.encoding?.y?.field || null
		};
	}
	function resolveTableKeyField({ interactionConfig, selection = null, eventData = null }) {
		return selection?.keyField || interactionConfig?.keyField || (eventData && typeof eventData === "object" ? Object.keys(eventData)[0] || null : null) || null;
	}
	function writeSurfaceState(surface, state, selection) {
		if (!surface) return;
		surface.dataset.widgetvaSelectedCount = String(state?.data?.selectedCount ?? 0);
		surface.dataset.widgetvaVisibleCount = String(state?.data?.visibleCount ?? 0);
		surface.dataset.widgetvaSelectionSummary = selection?.summary || "";
	}
	function resolveFocusPayload(state) {
		const view = state?.view && typeof state.view === "object" && !Array.isArray(state.view) ? state.view : null;
		if (!view) return null;
		const focusPayload = {
			...view.focusedCarId != null ? { focusedCarId: view.focusedCarId } : {},
			...view.focusedSeries != null ? { focusedSeries: clone$25(view.focusedSeries) } : {},
			...view.focusedNode != null ? { focusedNode: view.focusedNode } : {},
			...view.focusedFlow != null ? { focusedFlow: clone$25(view.focusedFlow) } : {}
		};
		return Object.keys(focusPayload).length > 0 ? focusPayload : null;
	}
	function resolveHighlightStatePayload(state) {
		const highlight = state?.view?.highlight;
		if (!highlight || typeof highlight !== "object" || Array.isArray(highlight)) return null;
		return clone$25(highlight);
	}
	function resolveAggregateStatePayload(state) {
		const aggregate = state?.view?.aggregate;
		if (!aggregate || typeof aggregate !== "object" || Array.isArray(aggregate)) return null;
		return clone$25(aggregate);
	}
	function resolveAddRemoveStatePayload(state) {
		const addRemove = state?.view?.addRemove;
		if (!addRemove || typeof addRemove !== "object" || Array.isArray(addRemove)) return null;
		return clone$25(addRemove);
	}
	function resolveAnnotateStatePayload(state) {
		const annotate = state?.view?.annotate;
		if (!annotate || typeof annotate !== "object" || Array.isArray(annotate)) return null;
		return clone$25(annotate);
	}
	function resolveDrillDownStatePayload(state) {
		const drillDown = state?.view?.drillDown;
		if (!drillDown || typeof drillDown !== "object" || Array.isArray(drillDown)) return null;
		return clone$25(drillDown);
	}
	function resolveNavigateStatePayload(state) {
		const navigate = state?.view?.navigate;
		if (!navigate || typeof navigate !== "object" || Array.isArray(navigate)) return null;
		return clone$25(navigate);
	}
	function resolveReencodeStatePayload(state) {
		const reencode = state?.view?.reencode;
		if (!reencode || typeof reencode !== "object" || Array.isArray(reencode)) return null;
		return clone$25(reencode);
	}
	function resolveSortPayload(state) {
		const sort = state?.view?.sort;
		if (!sort || typeof sort !== "object" || Array.isArray(sort)) return null;
		const payload = {
			...typeof sort.channel === "string" ? { channel: sort.channel } : {},
			...typeof sort.field === "string" ? { field: sort.field } : {},
			...typeof sort.mode === "string" ? { mode: sort.mode } : {},
			...typeof sort.order === "string" ? { order: sort.order } : {},
			...typeof sort.aggregate === "string" ? { aggregate: sort.aggregate } : {},
			...Array.isArray(sort.values) ? { values: clone$25(sort.values) } : {}
		};
		return Object.keys(payload).length > 0 ? payload : null;
	}
	function applyD3HostState({ view, surface, state }) {
		if (!state) return;
		const selections = normalizeSelections$2(state);
		const selection = resolveRepresentativeSelection$1(selections);
		const intervalSelection = resolvePrimaryIntervalSelection$1(selections);
		const xDomain = state?.view?.xDomain || intervalSelection?.domain?.xDomain || null;
		const yDomain = state?.view?.yDomain || intervalSelection?.domain?.yDomain || null;
		const aggregateState = resolveAggregateStatePayload(state);
		const addRemoveState = resolveAddRemoveStatePayload(state);
		const annotateState = resolveAnnotateStatePayload(state);
		const drillDownState = resolveDrillDownStatePayload(state);
		const highlightState = resolveHighlightStatePayload(state);
		const focusPayload = resolveFocusPayload(state);
		const navigateState = resolveNavigateStatePayload(state);
		const reencodeState = resolveReencodeStatePayload(state);
		const sortPayload = resolveSortPayload(state);
		const highlightedKeys = Array.isArray(state?.feedback?.highlightedKeys) ? state.feedback.highlightedKeys : [];
		writeSurfaceState(surface, state, selection);
		if (typeof view?.setBrush === "function") view.setBrush(intervalSelection || null);
		if (typeof view?.setViewport === "function") view.setViewport({
			...Array.isArray(xDomain) ? { xDomain: clone$25(xDomain) } : {},
			...Array.isArray(yDomain) ? { yDomain: clone$25(yDomain) } : {}
		});
		else if ((Array.isArray(xDomain) || Array.isArray(yDomain)) && typeof view?.setDomain === "function") view.setDomain(Array.isArray(xDomain) ? xDomain : null, Array.isArray(yDomain) ? yDomain : null);
		if (typeof view?.setSelection === "function") view.setSelection(selection || null);
		if (typeof view?.setHighlights === "function") view.setHighlights([...highlightedKeys]);
		if (highlightState && typeof view?.setHighlightState === "function") view.setHighlightState(highlightState);
		if (aggregateState && typeof view?.setAggregateState === "function") view.setAggregateState(aggregateState);
		if (addRemoveState && typeof view?.setAddRemoveState === "function") view.setAddRemoveState(addRemoveState);
		if (annotateState && typeof view?.setAnnotateState === "function") view.setAnnotateState(annotateState);
		if (drillDownState && typeof view?.setDrillDownState === "function") view.setDrillDownState(drillDownState);
		if (focusPayload && typeof view?.setFocus === "function") view.setFocus(focusPayload);
		if (navigateState && typeof view?.setNavigateState === "function") view.setNavigateState(navigateState);
		if (reencodeState && typeof view?.setReencodeState === "function") view.setReencodeState(reencodeState);
		if (sortPayload && typeof view?.setSort === "function") view.setSort(sortPayload);
	}
	async function renderD3FamilyState({ view, surface, state }) {
		if (!state) return;
		if (typeof view?.renderFromState === "function") return view.renderFromState(clone$25(state), {
			view,
			surface
		});
	}
	function bindD3FamilyInteractions({ view, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
		if (!view || !interactionConfig || interactionConfig.mode === "none") return () => {};
		if (interactionConfig.mode === "brush2d" && typeof view.onBrush === "function") {
			const cleanup = view.onBrush((selection) => {
				const normalizedSelection = selection || null;
				const [xField, yField] = Array.isArray(normalizedSelection?.fields) ? normalizedSelection.fields : [];
				const xRange = xField ? normalizedSelection?.value?.[xField] : null;
				const yRange = yField ? normalizedSelection?.value?.[yField] : null;
				if (typeof onActionCall === "function" && xField && yField && Array.isArray(xRange) && Array.isArray(yRange)) {
					onActionCall(buildHumanActionCall({
						name: interactionConfig.actionName || "scatter.brushRegion",
						actionTargetRef,
						params: {
							xField,
							yField,
							xRange,
							yRange
						}
					}));
					return;
				}
				if (typeof onSelectionChange === "function") onSelectionChange(normalizedSelection ? {
					...normalizedSelection,
					source_widget_id: selectionSourceWidgetId || void 0
				} : null);
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		if (interactionConfig.mode === "categoryClick" && typeof view.onCategoryClick === "function") {
			const cleanup = view.onCategoryClick((selection) => {
				const field = resolveCategoryField({
					spec,
					interactionConfig,
					selection
				});
				const values = uniqueValues$2(selection?.values || [selection?.value]).filter((value) => typeof value === "string" || typeof value === "number");
				if (!field || values.length === 0 || typeof onActionCall !== "function") return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "widget.selectCategory",
					actionTargetRef,
					params: {
						field,
						values
					}
				}));
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		if (interactionConfig.mode === "cellClick" && typeof view.onCellClick === "function") {
			const cleanup = view.onCellClick((selection) => {
				const { xField, yField } = resolveCellFields({
					spec,
					interactionConfig,
					selection
				});
				const xValue = selection?.xValue;
				const yValue = selection?.yValue;
				if (!xField || !yField || xValue == null || yValue == null || typeof onActionCall !== "function") return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "heatmap.filterCells",
					actionTargetRef,
					params: {
						xField,
						yField,
						xValue,
						yValue
					}
				}));
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		if (interactionConfig.mode === "multiBrush" && typeof view.onMultiBrush === "function") {
			const cleanup = view.onMultiBrush((rules) => {
				const normalizedRules = Array.isArray(rules) ? rules : [];
				if (normalizedRules.length === 0) return;
				if (typeof onActionCall === "function") {
					onActionCall(buildHumanActionCall({
						name: interactionConfig.actionName || "parallelCoordinates.brushAxes",
						actionTargetRef,
						params: { rules: clone$25(normalizedRules) }
					}));
					return;
				}
				if (typeof onSelectionChange === "function") onSelectionChange({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: selectionSourceWidgetId || void 0,
					selection_type: "interval",
					predicates: normalizedRules.map((rule) => ({
						field: rule.field,
						op: "between",
						value: rule.range
					}))
				});
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		if (interactionConfig.mode === "rowClick" && typeof view.onRowClick === "function") {
			const cleanup = view.onRowClick((selection) => {
				const keyField = resolveTableKeyField({
					interactionConfig,
					selection
				});
				const keys = uniqueValues$2(selection?.keys || [selection?.key]);
				if (!keyField || keys.length === 0 || typeof onActionCall !== "function") return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "table.focusRows",
					actionTargetRef,
					params: {
						keyField,
						keys
					}
				}));
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		return () => {};
	}
	function buildAxisPatch(domain, currentAxis) {
		if (!Array.isArray(domain)) return null;
		const nextAxis = {
			min: domain[0],
			max: domain[1]
		};
		if (Array.isArray(currentAxis)) return currentAxis.map(() => ({ ...nextAxis }));
		return nextAxis;
	}
	function buildDataZoomPatches(currentDataZoom, { xDomain, yDomain }) {
		const zoomItems = Array.isArray(currentDataZoom) ? currentDataZoom : currentDataZoom ? [currentDataZoom] : [];
		const patches = [];
		for (const zoomItem of zoomItems) {
			if (Array.isArray(xDomain) && zoomItem?.xAxisIndex != null) patches.push({
				xAxisIndex: zoomItem.xAxisIndex,
				startValue: xDomain[0],
				endValue: xDomain[1]
			});
			if (Array.isArray(yDomain) && zoomItem?.yAxisIndex != null) patches.push({
				yAxisIndex: zoomItem.yAxisIndex,
				startValue: yDomain[0],
				endValue: yDomain[1]
			});
		}
		return patches;
	}
	function readEChartsCurrentOption(view) {
		if (typeof view?.getOption !== "function") return null;
		try {
			return view.getOption() || null;
		} catch {
			return null;
		}
	}
	function buildEChartsFamilyOption({ state, view }) {
		if (!state) return null;
		const selections = normalizeSelections$2(state);
		const selection = resolveRepresentativeSelection$1(selections);
		const intervalSelection = resolvePrimaryIntervalSelection$1(selections);
		const xDomain = state?.view?.xDomain || intervalSelection?.domain?.xDomain || null;
		const yDomain = state?.view?.yDomain || intervalSelection?.domain?.yDomain || null;
		const currentOption = readEChartsCurrentOption(view);
		const option = { widgetva: {
			selection: clone$25(selection),
			selections: clone$25(selections),
			selectionSummary: selection?.summary || null,
			selectedCount: state?.data?.selectedCount ?? 0,
			visibleCount: state?.data?.visibleCount ?? 0,
			highlightedKeys: Array.isArray(state?.feedback?.highlightedKeys) ? [...state.feedback.highlightedKeys] : []
		} };
		const xAxisPatch = buildAxisPatch(xDomain, currentOption?.xAxis);
		if (xAxisPatch) option.xAxis = xAxisPatch;
		const yAxisPatch = buildAxisPatch(yDomain, currentOption?.yAxis);
		if (yAxisPatch) option.yAxis = yAxisPatch;
		const dataZoomPatches = buildDataZoomPatches(currentOption?.dataZoom, {
			xDomain,
			yDomain
		});
		if (dataZoomPatches.length > 0) option.dataZoom = dataZoomPatches;
		return option;
	}
	function bindEChartsFamilyInteractions({ view, spec, interactionConfig, actionTargetRef, onActionCall }) {
		if (!view || typeof view.on !== "function" || !interactionConfig || interactionConfig.mode === "none" || typeof onActionCall !== "function") return () => {};
		if (interactionConfig.mode === "brush2d") {
			const handler = (event) => {
				const selection = event?.widgetvaSelection || event?.selection || event?.batch?.[0]?.widgetvaSelection || null;
				const [xField, yField] = Array.isArray(selection?.fields) ? selection.fields : [];
				const xRange = xField ? selection?.value?.[xField] || selection?.xRange : null;
				const yRange = yField ? selection?.value?.[yField] || selection?.yRange : null;
				if (!xField || !yField || !Array.isArray(xRange) || !Array.isArray(yRange)) return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "scatter.brushRegion",
					actionTargetRef,
					params: {
						xField,
						yField,
						xRange,
						yRange
					}
				}));
			};
			view.on("brushselected", handler);
			return () => view.off?.("brushselected", handler);
		}
		if (interactionConfig.mode === "categoryClick") {
			const handler = (event) => {
				const field = resolveCategoryField({
					spec,
					interactionConfig,
					selection: event?.widgetvaSelection || event
				});
				const rawValue = event?.widgetvaSelection?.values?.[0] ?? event?.widgetvaSelection?.value ?? event?.data?.[field] ?? event?.name;
				if (!field || rawValue == null) return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "widget.selectCategory",
					actionTargetRef,
					params: {
						field,
						values: [rawValue]
					}
				}));
			};
			view.on("click", handler);
			return () => view.off?.("click", handler);
		}
		if (interactionConfig.mode === "cellClick") {
			const handler = (event) => {
				const selection = event?.widgetvaSelection || event;
				const { xField, yField } = resolveCellFields({
					spec,
					interactionConfig,
					selection
				});
				const xValue = selection?.xValue ?? event?.data?.[xField];
				const yValue = selection?.yValue ?? event?.data?.[yField];
				if (!xField || !yField || xValue == null || yValue == null) return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "heatmap.filterCells",
					actionTargetRef,
					params: {
						xField,
						yField,
						xValue,
						yValue
					}
				}));
			};
			view.on("click", handler);
			return () => view.off?.("click", handler);
		}
		if (interactionConfig.mode === "multiBrush") {
			const handler = (event) => {
				const rules = clone$25(event?.widgetvaSelection?.rules || event?.rules || event?.selection?.rules || []);
				if (!Array.isArray(rules) || rules.length === 0) return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "parallelCoordinates.brushAxes",
					actionTargetRef,
					params: { rules }
				}));
			};
			view.on("axisareaselected", handler);
			return () => view.off?.("axisareaselected", handler);
		}
		if (interactionConfig.mode === "rowClick") {
			const handler = (event) => {
				const eventData = event?.data && typeof event.data === "object" ? event.data : null;
				const selection = event?.widgetvaSelection || event;
				const keyField = resolveTableKeyField({
					interactionConfig,
					selection,
					eventData
				});
				const rawKey = selection?.keys?.[0] ?? selection?.key ?? (keyField ? eventData?.[keyField] : null);
				if (!keyField || rawKey == null) return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "table.focusRows",
					actionTargetRef,
					params: {
						keyField,
						keys: [rawKey]
					}
				}));
			};
			view.on("click", handler);
			return () => view.off?.("click", handler);
		}
		return () => {};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/D3WidgetAdapter.js
	function clone$24(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readCachedState$1(view) {
		return view?.__widgetvaLastAppliedState && typeof view.__widgetvaLastAppliedState === "object" ? view.__widgetvaLastAppliedState : null;
	}
	function resolveCachedSelection(view) {
		const selections = Object.values(readCachedState$1(view)?.selections || {}).filter(Boolean);
		if (selections.length === 1) return clone$24(selections[0]);
		return clone$24(selections.find((selection) => selection?.kind === "interval") || selections[0] || null);
	}
	function normalizeViewportCandidate(value) {
		if (!value || typeof value !== "object" || Array.isArray(value)) return null;
		const viewport = {
			...Array.isArray(value.xDomain) ? { xDomain: clone$24(value.xDomain) } : {},
			...Array.isArray(value.yDomain) ? { yDomain: clone$24(value.yDomain) } : {},
			...value.zoom && typeof value.zoom === "object" ? { zoom: clone$24(value.zoom) } : {}
		};
		return Object.keys(viewport).length > 0 ? viewport : null;
	}
	function createD3WidgetAdapter(definition = {}) {
		return createWidgetAdapterDefinition({
			provider: "d3",
			providerCapabilities: {
				supportedWidgetKinds: definition.kind ? [definition.kind] : [],
				renderStrategy: "providerView",
				stateApplyStrategy: "imperativeRender",
				interactionBindingStrategy: "providerEvents",
				supportsRendererMount: true,
				supportsRendererUpdate: true,
				supportsRendererDispose: true,
				supportsSignalPatching: false,
				supportsOptionMerging: false,
				supportsImperativeRender: true,
				supportsPointSelection: true,
				supportsIntervalSelection: true,
				supportsZoomPan: true,
				supportsSelectionReadback: true,
				supportsViewportReadback: true,
				supportsHighlightProjection: true,
				supportsInteractionEvents: true
			},
			mount({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			update({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			dispose() {},
			bindHumanInteractions() {
				return () => {};
			},
			async applyState(args) {
				const { view, state, surface } = args || {};
				if (view && state && typeof state === "object" && !Array.isArray(state)) view.__widgetvaLastAppliedState = clone$24(state);
				applyD3HostState({
					view,
					surface,
					state
				});
				return definition.renderFromState?.(args);
			},
			readSelection({ view } = {}) {
				if (typeof view?.getSelection === "function") return clone$24(view.getSelection() || null);
				if (typeof view?.getBrush === "function") return clone$24(view.getBrush() || null);
				return resolveCachedSelection(view);
			},
			readViewport({ view } = {}) {
				if (typeof view?.getViewport === "function") return clone$24(view.getViewport() || null);
				if (typeof view?.getDomain === "function") return normalizeViewportCandidate(view.getDomain());
				return normalizeViewportCandidate(readCachedState$1(view)?.view);
			},
			...definition
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/CustomWidgetAdapter.js
	function createCustomWidgetAdapter(definition = {}) {
		return createWidgetAdapterDefinition({
			provider: "custom",
			providerCapabilities: {
				supportedWidgetKinds: definition.kind ? [definition.kind] : [],
				renderStrategy: "custom",
				stateApplyStrategy: "custom",
				interactionBindingStrategy: "custom",
				supportsRendererMount: true,
				supportsRendererUpdate: true,
				supportsRendererDispose: true,
				supportsSignalPatching: false,
				supportsOptionMerging: false,
				supportsImperativeRender: false
			},
			...definition
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/defaultWidgetAdapter.js
	var defaultWidgetAdapter = createCustomWidgetAdapter({ kind: "custom" });
	//#endregion
	//#region ../../widgetva-kit/src/adapters/EChartsWidgetAdapter.js
	function clone$23(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readCachedState(view) {
		return view?.__widgetvaLastAppliedState && typeof view.__widgetvaLastAppliedState === "object" ? view.__widgetvaLastAppliedState : null;
	}
	function readCurrentOption(view) {
		if (typeof view?.getOption !== "function") return null;
		try {
			return view.getOption() || null;
		} catch {
			return null;
		}
	}
	function resolveRepresentativeSelection(selections = []) {
		const normalizedSelections = Array.isArray(selections) ? selections.filter(Boolean) : [];
		if (normalizedSelections.length === 1) return normalizedSelections[0] || null;
		const pointSelections = normalizedSelections.filter((selection) => selection?.kind === "point");
		if (pointSelections.length === 1) return pointSelections[0] || null;
		return normalizedSelections[0] || null;
	}
	function normalizeSelections$1(state) {
		return Object.values(state?.selections || {}).filter(Boolean);
	}
	function resolvePrimaryIntervalSelection(selections = []) {
		return (Array.isArray(selections) ? selections : []).find((selection) => selection?.kind === "interval") || null;
	}
	function resolveViewportFromState(state, intervalSelection) {
		const xDomain = state?.view?.xDomain || intervalSelection?.domain?.xDomain || null;
		const yDomain = state?.view?.yDomain || intervalSelection?.domain?.yDomain || null;
		const zoom = state?.view?.zoom && typeof state.view.zoom === "object" ? clone$23(state.view.zoom) : null;
		return normalizeViewport({
			...Array.isArray(xDomain) ? { xDomain } : {},
			...Array.isArray(yDomain) ? { yDomain } : {},
			...zoom ? { zoom } : {}
		});
	}
	function selectionValues(selection) {
		return (Array.isArray(selection?.values) ? selection.values : [selection?.value]).filter((value) => typeof value === "string" || typeof value === "number");
	}
	function readSeriesNames(option) {
		const seriesNames = Array.isArray(option?.series) ? option.series.map((series) => series?.name).filter((name) => typeof name === "string" && name.length > 0) : [];
		const legendNames = (Array.isArray(option?.legend) ? option.legend : option?.legend ? [option.legend] : []).flatMap((legend) => {
			return (Array.isArray(legend?.data) ? legend.data : []).map((entry) => typeof entry === "string" ? entry : entry?.name).filter((name) => typeof name === "string" && name.length > 0);
		});
		return [...new Set([...seriesNames, ...legendNames])];
	}
	function buildDataZoomActions(option, viewport) {
		const zoomItems = Array.isArray(option?.dataZoom) ? option.dataZoom : option?.dataZoom ? [option.dataZoom] : [];
		const actions = [];
		for (const [index, zoomItem] of zoomItems.entries()) {
			if (Array.isArray(viewport?.xDomain) && zoomItem?.xAxisIndex != null) actions.push({
				type: "dataZoom",
				dataZoomIndex: index,
				xAxisIndex: zoomItem.xAxisIndex,
				startValue: viewport.xDomain[0],
				endValue: viewport.xDomain[1]
			});
			if (Array.isArray(viewport?.yDomain) && zoomItem?.yAxisIndex != null) actions.push({
				type: "dataZoom",
				dataZoomIndex: index,
				yAxisIndex: zoomItem.yAxisIndex,
				startValue: viewport.yDomain[0],
				endValue: viewport.yDomain[1]
			});
		}
		return actions;
	}
	function resolveSeriesNamesFromSelection(selection, option) {
		if (!selection || selection.kind !== "point") return [];
		const knownSeriesNames = readSeriesNames(option);
		if (knownSeriesNames.length === 0) return [];
		const values = selectionValues(selection);
		if (values.length === 0) return [];
		const matchingValues = values.filter((value) => knownSeriesNames.includes(String(value)));
		if (matchingValues.length !== values.length) return [];
		const field = typeof selection?.field === "string" ? selection.field.toLowerCase() : "";
		if (field.includes("series") || field.length === 0) return matchingValues.map((value) => String(value));
		return matchingValues.length > 0 ? matchingValues.map((value) => String(value)) : [];
	}
	function resolveSeriesNamesFromHighlights(highlightedKeys, option) {
		const knownSeriesNames = readSeriesNames(option);
		if (knownSeriesNames.length === 0) return [];
		return [...new Set((Array.isArray(highlightedKeys) ? highlightedKeys : []).filter((key) => typeof key === "string" && knownSeriesNames.includes(key)))];
	}
	function readDataItemRefs(option) {
		const seriesList = Array.isArray(option?.series) ? option.series : [];
		const itemRefs = [];
		for (const [seriesIndex, series] of seriesList.entries()) {
			const dataItems = Array.isArray(series?.data) ? series.data : [];
			for (const [dataIndex, datum] of dataItems.entries()) {
				const name = typeof datum === "string" || typeof datum === "number" ? String(datum) : typeof datum?.name === "string" && datum.name.length > 0 ? datum.name : typeof datum?.id === "string" && datum.id.length > 0 ? datum.id : null;
				itemRefs.push({
					seriesIndex,
					dataIndex,
					name,
					datum: datum && typeof datum === "object" && !Array.isArray(datum) ? datum : null
				});
			}
		}
		return itemRefs;
	}
	function normalizeComparableValue(value) {
		return typeof value === "string" || typeof value === "number" ? String(value) : null;
	}
	function resolveDataItemRefsFromValues(values, option) {
		const normalizedValues = [...new Set((Array.isArray(values) ? values : []).map((value) => normalizeComparableValue(value)).filter(Boolean))];
		if (normalizedValues.length === 0) return [];
		const valueSet = new Set(normalizedValues);
		return readDataItemRefs(option).filter((itemRef) => valueSet.has(itemRef.name));
	}
	function normalizeFieldName$1(value) {
		return typeof value === "string" && value.length > 0 ? value.toLowerCase() : null;
	}
	function resolveDatumFieldValue(datum, field) {
		if (!datum || typeof datum !== "object" || Array.isArray(datum)) return null;
		const directValue = normalizeComparableValue(datum[field]);
		if (directValue != null) return directValue;
		const normalizedField = normalizeFieldName$1(field);
		if (!normalizedField) return null;
		const matchedEntry = Object.entries(datum).find(([candidateField]) => normalizeFieldName$1(candidateField) === normalizedField);
		return matchedEntry ? normalizeComparableValue(matchedEntry[1]) : null;
	}
	function resolveDataItemRefsFromSelection(selection, option) {
		const values = selectionValues(selection);
		if (values.length === 0) return [];
		const field = typeof selection?.field === "string" && selection.field.length > 0 ? selection.field : null;
		if (!field) return resolveDataItemRefsFromValues(values, option);
		const valueSet = new Set(values.map((value) => String(value)));
		const matchedRefs = readDataItemRefs(option).filter((itemRef) => {
			const datumValue = resolveDatumFieldValue(itemRef.datum, field);
			return datumValue != null && valueSet.has(datumValue);
		});
		if (matchedRefs.length > 0) return matchedRefs;
		return resolveDataItemRefsFromValues(values, option);
	}
	function parseHighlightKey(key) {
		if (typeof key !== "string" || key.length === 0) return null;
		const separatorIndex = key.indexOf(":");
		if (separatorIndex <= 0 || separatorIndex === key.length - 1) return null;
		return {
			field: key.slice(0, separatorIndex),
			value: key.slice(separatorIndex + 1)
		};
	}
	function resolveDataItemRefsFromHighlightKeys(highlightedKeys, option) {
		const itemRefs = readDataItemRefs(option);
		const directValueMatches = resolveDataItemRefsFromValues(highlightedKeys, option);
		const parsedPairs = (Array.isArray(highlightedKeys) ? highlightedKeys : []).map((key) => parseHighlightKey(key)).filter(Boolean);
		if (parsedPairs.length === 0) return directValueMatches;
		return mergeDataItemRefs(directValueMatches, itemRefs.filter((itemRef) => parsedPairs.some(({ field, value }) => resolveDatumFieldValue(itemRef.datum, field) === String(value))));
	}
	function mergeDataItemRefs(...groups) {
		const merged = /* @__PURE__ */ new Map();
		for (const group of groups) for (const itemRef of Array.isArray(group) ? group : []) {
			const key = `${itemRef.seriesIndex}:${itemRef.dataIndex}`;
			if (!merged.has(key)) merged.set(key, itemRef);
		}
		return [...merged.values()];
	}
	function dispatchEChartsNativeActions({ view, option, selection, highlightedKeys, viewport }) {
		if (typeof view?.dispatchAction !== "function") return;
		const selectedSeriesNames = resolveSeriesNamesFromSelection(selection, option);
		if (selectedSeriesNames.length > 0 && readSeriesNames(option).length > 0 && option?.legend) {
			const selectedSet = new Set(selectedSeriesNames);
			for (const name of readSeriesNames(option)) view.dispatchAction({
				type: selectedSet.has(name) ? "legendSelect" : "legendUnSelect",
				name
			});
		}
		const highlightedSeriesNames = resolveSeriesNamesFromHighlights(highlightedKeys, option);
		if (highlightedSeriesNames.length > 0) {
			for (const name of readSeriesNames(option)) view.dispatchAction({
				type: "downplay",
				seriesName: name
			});
			for (const name of highlightedSeriesNames) view.dispatchAction({
				type: "highlight",
				seriesName: name
			});
		}
		const activeItemRefs = mergeDataItemRefs(selectedSeriesNames.length === 0 ? resolveDataItemRefsFromSelection(selection, option) : [], resolveDataItemRefsFromHighlightKeys(highlightedKeys, option));
		if (activeItemRefs.length > 0) {
			for (const itemRef of readDataItemRefs(option)) view.dispatchAction({
				type: "downplay",
				seriesIndex: itemRef.seriesIndex,
				dataIndex: itemRef.dataIndex
			});
			for (const itemRef of activeItemRefs) view.dispatchAction({
				type: "highlight",
				seriesIndex: itemRef.seriesIndex,
				dataIndex: itemRef.dataIndex
			});
		}
		for (const action of buildDataZoomActions(option, viewport)) view.dispatchAction(action);
	}
	function readSelectionFromOption(option) {
		if (!option?.widgetva || typeof option.widgetva !== "object") return null;
		if (option.widgetva.selection && typeof option.widgetva.selection === "object") return clone$23(option.widgetva.selection);
		return clone$23(resolveRepresentativeSelection(Object.values(option.widgetva.selections || {}).filter(Boolean)));
	}
	function readSelectionFromCachedState(view) {
		return clone$23(resolveRepresentativeSelection(Object.values(readCachedState(view)?.selections || {}).filter(Boolean)));
	}
	function readAxisDomain(axis) {
		const axisObject = Array.isArray(axis) ? axis[0] : axis;
		if (!axisObject || typeof axisObject !== "object") return null;
		if (!Number.isFinite(axisObject.min) || !Number.isFinite(axisObject.max)) return null;
		return [axisObject.min, axisObject.max];
	}
	function readZoomDomain(dataZoom, axisKey) {
		const zoomItems = Array.isArray(dataZoom) ? dataZoom : dataZoom ? [dataZoom] : [];
		const axisIndexKey = axisKey === "x" ? "xAxisIndex" : "yAxisIndex";
		const candidate = zoomItems.find((zoomItem) => zoomItem?.[axisIndexKey] != null && Number.isFinite(zoomItem.startValue) && Number.isFinite(zoomItem.endValue)) || null;
		if (!candidate) return null;
		return [candidate.startValue, candidate.endValue];
	}
	function normalizeViewport(viewport) {
		if (!viewport || typeof viewport !== "object" || Array.isArray(viewport)) return null;
		const normalized = {
			...Array.isArray(viewport.xDomain) ? { xDomain: clone$23(viewport.xDomain) } : {},
			...Array.isArray(viewport.yDomain) ? { yDomain: clone$23(viewport.yDomain) } : {},
			...viewport.zoom && typeof viewport.zoom === "object" ? { zoom: clone$23(viewport.zoom) } : {}
		};
		return Object.keys(normalized).length > 0 ? normalized : null;
	}
	function createEChartsWidgetAdapter(definition = {}) {
		return createWidgetAdapterDefinition({
			provider: "echarts",
			providerCapabilities: {
				supportedWidgetKinds: definition.kind ? [definition.kind] : [],
				renderStrategy: "providerView",
				stateApplyStrategy: "optionMerge",
				interactionBindingStrategy: "providerEvents",
				supportsRendererMount: true,
				supportsRendererUpdate: true,
				supportsRendererDispose: true,
				supportsSignalPatching: false,
				supportsOptionMerging: true,
				supportsImperativeRender: false,
				supportsPointSelection: true,
				supportsIntervalSelection: true,
				supportsZoomPan: true,
				supportsSelectionReadback: true,
				supportsViewportReadback: true,
				supportsHighlightProjection: true,
				supportsInteractionEvents: true
			},
			mount({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			update({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			dispose({ view = null } = {}) {
				return view?.dispose?.();
			},
			bindHumanInteractions() {
				return () => {};
			},
			async applyState(args) {
				const { view, state } = args || {};
				if (!view) return;
				if (state && typeof state === "object" && !Array.isArray(state)) view.__widgetvaLastAppliedState = clone$23(state);
				const selections = normalizeSelections$1(state);
				const selection = resolveRepresentativeSelection(selections);
				const intervalSelection = resolvePrimaryIntervalSelection(selections);
				const aggregateState = resolveAggregateStatePayload(state);
				const addRemoveState = resolveAddRemoveStatePayload(state);
				const annotateState = resolveAnnotateStatePayload(state);
				const drillDownState = resolveDrillDownStatePayload(state);
				const highlightState = resolveHighlightStatePayload(state);
				const focusPayload = resolveFocusPayload(state);
				const navigateState = resolveNavigateStatePayload(state);
				const reencodeState = resolveReencodeStatePayload(state);
				const sortPayload = resolveSortPayload(state);
				const highlightedKeys = Array.isArray(state?.feedback?.highlightedKeys) ? [...state.feedback.highlightedKeys] : [];
				const viewport = resolveViewportFromState(state, intervalSelection);
				if (typeof view.setBrush === "function") view.setBrush(intervalSelection || null);
				if (typeof view.setSelection === "function") view.setSelection(selection || null);
				if (typeof view.setHighlights === "function") view.setHighlights(highlightedKeys);
				if (highlightState && typeof view.setHighlightState === "function") view.setHighlightState(highlightState);
				if (aggregateState && typeof view.setAggregateState === "function") view.setAggregateState(aggregateState);
				if (addRemoveState && typeof view.setAddRemoveState === "function") view.setAddRemoveState(addRemoveState);
				if (annotateState && typeof view.setAnnotateState === "function") view.setAnnotateState(annotateState);
				if (drillDownState && typeof view.setDrillDownState === "function") view.setDrillDownState(drillDownState);
				if (focusPayload && typeof view.setFocus === "function") view.setFocus(focusPayload);
				if (navigateState && typeof view.setNavigateState === "function") view.setNavigateState(navigateState);
				if (reencodeState && typeof view.setReencodeState === "function") view.setReencodeState(reencodeState);
				if (sortPayload && typeof view.setSort === "function") view.setSort(sortPayload);
				if (viewport && typeof view.setViewport === "function") view.setViewport(viewport);
				else if (viewport && typeof view.setDomain === "function") view.setDomain(Array.isArray(viewport.xDomain) ? viewport.xDomain : null, Array.isArray(viewport.yDomain) ? viewport.yDomain : null);
				if (typeof view.setOption !== "function") return;
				const nextOption = definition.buildOptionFromState?.(args) || null;
				if (nextOption) view.setOption(nextOption, {
					notMerge: false,
					lazyUpdate: true
				});
				dispatchEChartsNativeActions({
					view,
					option: readCurrentOption(view),
					selection,
					highlightedKeys,
					viewport
				});
			},
			readSelection({ view } = {}) {
				if (typeof view?.getSelection === "function") return clone$23(view.getSelection() || null);
				return readSelectionFromOption(readCurrentOption(view)) || readSelectionFromCachedState(view);
			},
			readViewport({ view } = {}) {
				if (typeof view?.getViewport === "function") return clone$23(view.getViewport() || null);
				const option = readCurrentOption(view);
				const xDomain = readZoomDomain(option?.dataZoom, "x") || readAxisDomain(option?.xAxis);
				const yDomain = readZoomDomain(option?.dataZoom, "y") || readAxisDomain(option?.yAxis);
				if (Array.isArray(xDomain) || Array.isArray(yDomain)) return {
					...Array.isArray(xDomain) ? { xDomain } : {},
					...Array.isArray(yDomain) ? { yDomain } : {}
				};
				return normalizeViewport(readCachedState(view)?.view);
			},
			...definition
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/heatmap/actions.js
	var MONTH_MAP = {
		Jan: 0,
		Feb: 1,
		Mar: 2,
		Apr: 3,
		May: 4,
		Jun: 5,
		Jul: 6,
		Aug: 7,
		Sep: 8,
		Oct: 9,
		Nov: 10,
		Dec: 11,
		January: 0,
		February: 1,
		March: 2,
		April: 3,
		June: 5,
		July: 6,
		August: 7,
		September: 8,
		October: 9,
		November: 10,
		December: 11
	};
	function datumRef(field) {
		return `datum['${String(field).replace(/\\/g, "\\\\").replace(/'/g, "\\'")}']`;
	}
	function cloneValue$22(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function replaceTaggedTransform$1(transforms, tag, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => transform?._widgetvaTag !== tag);
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function normalizeTimeUnitValues(field, values, timeUnit) {
		const safeValues = Array.isArray(values) ? values.filter((value) => value != null) : [];
		if (!timeUnit) return {
			valueListExpr: safeValues.map((value) => JSON.stringify(value)).join(","),
			datumExpr: datumRef(field)
		};
		const normalizedTimeUnit = String(timeUnit).toLowerCase().trim();
		if (normalizedTimeUnit === "date") {
			const numericValues = safeValues.map((value) => Number.parseInt(value, 10)).filter((value) => Number.isFinite(value));
			return {
				valueListExpr: (numericValues.length > 0 ? numericValues : safeValues).map((value) => JSON.stringify(value)).join(","),
				datumExpr: `date(${datumRef(field)})`
			};
		}
		if (normalizedTimeUnit === "month") {
			const monthValues = safeValues.map((value) => {
				if (typeof value === "string" && Object.prototype.hasOwnProperty.call(MONTH_MAP, value)) return MONTH_MAP[value];
				const parsed = Number.parseInt(value, 10);
				return Number.isFinite(parsed) ? parsed - 1 : null;
			}).filter((value) => Number.isFinite(value));
			return {
				valueListExpr: (monthValues.length > 0 ? monthValues : safeValues).map((value) => JSON.stringify(value)).join(","),
				datumExpr: `month(${datumRef(field)})`
			};
		}
		if (normalizedTimeUnit === "year") {
			const numericValues = safeValues.map((value) => Number.parseInt(value, 10)).filter((value) => Number.isFinite(value));
			return {
				valueListExpr: (numericValues.length > 0 ? numericValues : safeValues).map((value) => JSON.stringify(value)).join(","),
				datumExpr: `year(${datumRef(field)})`
			};
		}
		return {
			valueListExpr: safeValues.map((value) => JSON.stringify(value)).join(","),
			datumExpr: `${normalizedTimeUnit}(${datumRef(field)})`
		};
	}
	function buildHeatmapActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
		const supportedWidgetKinds = ["heatmap"];
		const filterCellDescriptor = makeActionDescriptor({
			name: "heatmap.filterCells",
			title: "Filter heatmap cells",
			description: "Filter the workspace through a heatmap cell identified by its x/y category pair.",
			primitive: "filter",
			category: "dataTransform",
			scope,
			supportedWidgetKinds,
			targetRef: selectionRef || widgetRef,
			affectedRefs,
			affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms"),
			paramsSchema: {
				type: "object",
				properties: {
					xField: { type: "string" },
					yField: { type: "string" },
					xValue: { type: "string" },
					yValue: { type: "string" }
				},
				required: [
					"xField",
					"yField",
					"xValue",
					"yValue"
				]
			},
			postconditions: [{ description: "The active selection should contain the selected x/y cell pair and linked widgets should be filterable from it." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
				failureMessage: "heatmap.filterCells requires a valid heatmap target widget."
			}],
			effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a cell selection on the heatmap."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeFilterEffect(ref, "Linked widgets may be filtered by the selected heatmap cell."))],
			examples: [{
				userGoal: "Select one heatmap cell and inspect linked detail views.",
				params: {
					xField: "Origin",
					yField: "Cylinders",
					xValue: "Japan",
					yValue: "4"
				}
			}],
			reversible: true
		});
		return [
			filterCellDescriptor,
			makeActionDescriptor({
				...cloneValue$22(filterCellDescriptor),
				name: "heatmap.selectCell",
				title: "Select heatmap cell",
				description: "Select a single heatmap cell through the canonical heatmap cell contract.",
				primitive: "select",
				category: "selection"
			}),
			makeActionDescriptor({
				name: "heatmap.selectSubmatrix",
				title: "Select heatmap submatrix",
				description: "Select a heatmap submatrix defined by one or more x-axis and/or y-axis coordinates without mutating the view spec.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						xValues: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						},
						yValues: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						}
					}
				},
				postconditions: [{ description: "The active selection should contain the selected heatmap rows, columns, or their intersection region." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.selectSubmatrix requires a valid heatmap target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a heatmap region selection over rows, columns, or an intersection."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeFilterEffect(ref, "Linked widgets may be filtered by the selected heatmap submatrix."))],
				examples: [{
					userGoal: "Select a rectangular region of the heatmap before inspecting linked views.",
					params: {
						xValues: ["Q1", "Q2"],
						yValues: ["A", "B"]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.drilldownAxis",
				title: "Drill down heatmap time axis",
				description: "Drill a temporal heatmap x-axis from year to month or from month to date by narrowing the visible period and refining the x-axis timeUnit.",
				primitive: "drillDown",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						level: { type: "string" },
						value: { anyOf: [{ type: "number" }, { type: "string" }] },
						parent: { type: "object" }
					},
					required: ["level", "value"]
				},
				postconditions: [{ description: "The heatmap x-axis should move to a finer temporal granularity while the transform list narrows the visible period." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.drilldownAxis requires a valid heatmap target widget."
				}],
				examples: [{
					userGoal: "Drill a yearly heatmap into monthly detail for one year.",
					params: {
						level: "year",
						value: 2024
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.resetDrilldown",
				title: "Reset heatmap drill-down",
				description: "Restore the original temporal x-axis encoding and remove drill-down filters from the heatmap.",
				primitive: "navigate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The heatmap should revert to its original temporal x-axis encoding and remove tagged drill-down filters." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.resetDrilldown requires a valid heatmap target widget."
				}],
				examples: [{
					userGoal: "Return a drilled heatmap back to its original yearly overview.",
					params: {}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.addMarginalBars",
				title: "Add heatmap marginal bars",
				description: "Compose a heatmap with optional top and right marginal bar charts that aggregate the heatmap value field along each axis.",
				primitive: "annotate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						op: { type: "string" },
						showTop: { type: "boolean" },
						showRight: { type: "boolean" },
						barSize: { type: "number" },
						barColor: { type: "string" }
					}
				},
				postconditions: [{ description: "The heatmap should be composed with marginal bars that aggregate the color/value field along rows and/or columns." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.addMarginalBars requires a valid heatmap target widget."
				}],
				examples: [{
					userGoal: "Add row and column marginal summaries before comparing the overall heatmap structure.",
					params: {
						op: "mean",
						showTop: true,
						showRight: true,
						barSize: 70,
						barColor: "#666666"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.highlightRegion",
				title: "Highlight heatmap region",
				description: "Highlight one or more heatmap rows, columns, or their intersection without filtering away the rest of the matrix.",
				primitive: "highlight",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						xValues: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						},
						yValues: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						}
					}
				},
				postconditions: [{ description: "The heatmap should visually emphasize the requested rows, columns, or region while dimming the rest." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.highlightRegion requires a valid heatmap target widget."
				}],
				examples: [{
					userGoal: "Highlight one row/column region before comparing extreme cells.",
					params: {
						xValues: ["Q1"],
						yValues: ["A"]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.adjustColorScale",
				title: "Adjust heatmap color scale",
				description: "Update the heatmap color scheme and optional numeric domain without changing the underlying data.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						scheme: { type: "string" },
						domain: {
							type: "array",
							minItems: 2,
							maxItems: 2,
							items: { anyOf: [{ type: "number" }, { type: "string" }] }
						}
					}
				},
				postconditions: [{ description: "The heatmap color encoding should reflect the requested scheme and optional domain." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.adjustColorScale requires a valid heatmap target widget."
				}],
				examples: [{
					userGoal: "Switch to a new color scheme and tighten the value range for comparison.",
					params: {
						scheme: "blues",
						domain: [0, 25]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.thresholdMask",
				title: "Mask heatmap values outside a threshold range",
				description: "Visually dim heatmap cells whose color values fall outside the requested inclusive threshold range.",
				primitive: "highlight",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						minValue: { anyOf: [{ type: "number" }, { type: "string" }] },
						maxValue: { anyOf: [{ type: "number" }, { type: "string" }] },
						outsideOpacity: { type: "number" }
					},
					required: ["minValue", "maxValue"]
				},
				postconditions: [{ description: "The heatmap opacity encoding should preserve cells inside the threshold range and dim cells outside it." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.thresholdMask requires a valid heatmap target widget."
				}],
				examples: [{
					userGoal: "Dim low-signal cells while keeping the full matrix visible.",
					params: {
						minValue: 10,
						maxValue: 25,
						outsideOpacity: .1
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.filterCellsByRegion",
				title: "Filter heatmap cells by region",
				description: "Exclude one or more heatmap rows, columns, or their intersection by writing a region filter into the heatmap spec.",
				primitive: "filter",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						xValue: { anyOf: [{ type: "string" }, { type: "number" }] },
						yValue: { anyOf: [{ type: "string" }, { type: "number" }] },
						xValues: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						},
						yValues: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						}
					}
				},
				postconditions: [{ description: "The heatmap transform list should exclude the requested rows, columns, or intersection region." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.filterCellsByRegion requires a valid heatmap target widget."
				}],
				effects: affectedRefs.map((ref) => makeFilterEffect(ref, ref === widgetRef ? "The heatmap excludes the requested rows, columns, or intersection region." : "Linked widgets may update to reflect the excluded heatmap region.")),
				examples: [{
					userGoal: "Remove one heatmap row-column intersection before inspecting the remaining structure.",
					params: {
						xValues: ["Q1"],
						yValues: ["A"]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.highlightRegionByValue",
				title: "Highlight heatmap cells by value range",
				description: "Visually emphasize cells whose displayed values fall inside a requested range, while dimming the rest without filtering data away.",
				primitive: "highlight",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						minValue: { anyOf: [{ type: "number" }, { type: "string" }] },
						maxValue: { anyOf: [{ type: "number" }, { type: "string" }] },
						outsideOpacity: { type: "number" }
					}
				},
				postconditions: [{ description: "The heatmap opacity encoding should emphasize values inside the requested range and dim values outside it." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.highlightRegionByValue requires a valid heatmap target widget."
				}],
				examples: [{
					userGoal: "Highlight only mid-range or high-value cells before comparing hotspots.",
					params: {
						minValue: 10,
						maxValue: 25,
						outsideOpacity: .12
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.clusterRowsCols",
				title: "Reorder heatmap rows or columns by aggregated value",
				description: "Reorder heatmap rows and/or columns by aggregated cell values so high-value bands are grouped toward the front.",
				primitive: "aggregate",
				category: "compute",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						clusterRows: { type: "boolean" },
						clusterCols: { type: "boolean" },
						method: { type: "string" }
					}
				},
				postconditions: [{ description: "The heatmap x/y encodings should carry explicit aggregate sort metadata for the requested rows and/or columns." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget with a color field on the current spec.",
					failureMessage: "heatmap.clusterRowsCols requires a valid heatmap target widget with a color encoding field."
				}],
				examples: [{
					userGoal: "Bring the hottest rows and columns toward the front of the matrix.",
					params: {
						clusterRows: true,
						clusterCols: true,
						method: "sum"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "heatmap.transpose",
				title: "Transpose heatmap axes",
				description: "Swap the heatmap x and y encodings to quickly inspect the matrix from the opposite orientation.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The heatmap x and y encodings should be swapped, and width/height should swap when both are defined." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid heatmap widget in the current workspace.",
					failureMessage: "heatmap.transpose requires a valid heatmap target widget."
				}],
				examples: [{
					userGoal: "Flip rows and columns to compare the matrix from the opposite orientation.",
					params: {}
				}],
				reversible: true
			})
		];
	}
	function registerHeatmapActions(actionExecutor) {
		const registerHeatmapCellAction = (name, invalidTargetMessage, invalidParamsMessage) => {
			if (actionExecutor.has(name)) return;
			actionExecutor.register({ name }, async (call, ctx) => {
				const params = call?.params || {};
				const targetWidget = ctx.requireTargetWidget({
					targetRef: call?.queryScope?.widgetRef || null,
					kind: "heatmap",
					message: invalidTargetMessage
				});
				const xField = typeof params.xField === "string" ? params.xField : null;
				const yField = typeof params.yField === "string" ? params.yField : null;
				const xValue = typeof params.xValue === "string" ? params.xValue : null;
				const yValue = typeof params.yValue === "string" ? params.yValue : null;
				if (!targetWidget || !xField || !yField || xValue == null || yValue == null) throw new Error(invalidParamsMessage);
				const { rows: visibleRows } = ctx.readRowsForWidget(targetWidget.ref);
				const matchedCount = visibleRows.filter((row) => row?.[xField] === xValue && row?.[yField] === yValue).length;
				if (name === "heatmap.filterCells") ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap cell filtering.");
					let nextTransforms = replaceTaggedTransform$1(spec.transform, "heatmap.filterCells.x", {
						filter: {
							field: xField,
							equal: xValue
						},
						_widgetvaTag: "heatmap.filterCells.x"
					});
					nextTransforms = replaceTaggedTransform$1(nextTransforms, "heatmap.filterCells.y", {
						filter: {
							field: yField,
							equal: yValue
						},
						_widgetvaTag: "heatmap.filterCells.y"
					});
					return {
						...spec,
						transform: nextTransforms
					};
				});
				return buildSelectionActionResult({
					ctx,
					nextState: ctx.commitSelection({
						selection_id: `sel_${Date.now()}`,
						source_widget_id: targetWidget.widgetId || void 0,
						selection_type: "cell",
						fields: [xField, yField],
						value: {
							[xField]: xValue,
							[yField]: yValue
						},
						predicates: [{
							field: xField,
							op: "equals",
							value: xValue
						}, {
							field: yField,
							op: "equals",
							value: yValue
						}],
						count: matchedCount,
						summary: `${xField}: ${xValue}; ${yField}: ${yValue}`
					}),
					selectedCount: matchedCount,
					verificationHints: [...name === "heatmap.filterCells" ? ["Call perception.inspectVisibleRows to verify only the requested heatmap cell remains visible.", "Call perception.inspectViewConfig to verify the active heatmap spec now includes x/y equality filters for the requested cell."] : ["Read the updated heatmap selection state.", "Read linked widgets to confirm cell-level filter propagation."]]
				});
			});
		};
		registerHeatmapCellAction("heatmap.filterCells", "heatmap.filterCells requires a valid heatmap target widget.", "heatmap.filterCells requires a heatmap target, xField, yField, xValue, and yValue.");
		registerHeatmapCellAction("heatmap.selectCell", "heatmap.selectCell requires a valid heatmap target widget.", "heatmap.selectCell requires a heatmap target, xField, yField, xValue, and yValue.");
		if (!actionExecutor.has("heatmap.selectSubmatrix")) actionExecutor.register({ name: "heatmap.selectSubmatrix" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.selectSubmatrix requires a valid heatmap target widget."
			});
			const xValues = Array.isArray(params.xValues) ? params.xValues.filter((value) => value != null) : [];
			const yValues = Array.isArray(params.yValues) ? params.yValues.filter((value) => value != null) : [];
			if (!targetWidget || xValues.length === 0 && yValues.length === 0) throw new Error("heatmap.selectSubmatrix requires a heatmap target and at least one of xValues or yValues.");
			const spec = ctx.readCurrentSpec();
			const xField = spec?.encoding?.x?.field || null;
			const yField = spec?.encoding?.y?.field || null;
			if (!xField || !yField) throw new Error("heatmap.selectSubmatrix requires x and y encodings on the active heatmap spec.");
			const { rows: visibleRows } = ctx.readRowsForWidget(targetWidget.ref);
			const matchedCount = visibleRows.filter((row) => {
				const xMatch = xValues.length === 0 || xValues.includes(row?.[xField]);
				const yMatch = yValues.length === 0 || yValues.includes(row?.[yField]);
				return xMatch && yMatch;
			}).length;
			const predicates = [];
			if (xValues.length > 0) predicates.push({
				field: xField,
				op: "in",
				value: xValues
			});
			if (yValues.length > 0) predicates.push({
				field: yField,
				op: "in",
				value: yValues
			});
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "cell",
					fields: [xField, yField],
					value: {
						...xValues.length > 0 ? { [xField]: xValues } : {},
						...yValues.length > 0 ? { [yField]: yValues } : {}
					},
					predicates,
					count: matchedCount,
					summary: `${xField}: ${xValues.length > 0 ? xValues.join(", ") : "all"}; ${yField}: ${yValues.length > 0 ? yValues.join(", ") : "all"}`
				}),
				selectedCount: matchedCount,
				verificationHints: ["Read the updated heatmap selection state.", "Read linked widgets to confirm submatrix-level filter propagation."]
			});
		});
		if (!actionExecutor.has("heatmap.drilldownAxis")) actionExecutor.register({ name: "heatmap.drilldownAxis" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.drilldownAxis requires a valid heatmap target widget."
			});
			const level = typeof params.level === "string" ? params.level.toLowerCase().trim() : null;
			const value = params.value;
			const parent = params.parent && typeof params.parent === "object" && !Array.isArray(params.parent) ? params.parent : {};
			if (!targetWidget || !level || value == null || value === "") throw new Error("heatmap.drilldownAxis requires a heatmap target plus level and value.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap axis drill-down.");
					const xEncoding = spec?.encoding?.x;
					const timeField = xEncoding?.field || null;
					const xType = xEncoding?.type || null;
					if (!timeField) throw new Error("heatmap.drilldownAxis requires a temporal x field on the active heatmap spec.");
					if (xType && xType !== "temporal") throw new Error(`heatmap.drilldownAxis requires encoding.x.type=temporal, received ${xType}.`);
					const heatmapState = spec._heatmap_state && typeof spec._heatmap_state === "object" ? { ...spec._heatmap_state } : {};
					if (!heatmapState.original_x_encoding) heatmapState.original_x_encoding = cloneValue$22(xEncoding);
					const nextTransforms = Array.isArray(spec.transform) ? spec.transform.filter((transform) => transform?._widgetvaTag !== "heatmap.drilldownAxis") : [];
					const mergedParent = {
						...heatmapState.parent && typeof heatmapState.parent === "object" ? heatmapState.parent : {},
						...parent
					};
					let nextTimeUnit = null;
					const filters = [];
					let nextParent;
					if (level === "year") {
						const yearValue = Number.parseInt(value, 10);
						if (!Number.isFinite(yearValue)) throw new Error(`heatmap.drilldownAxis requires an integer year value; received ${value}.`);
						nextParent = { year: yearValue };
						nextTimeUnit = "month";
						filters.push(`year(${datumRef(timeField)}) == ${yearValue}`);
					} else if (level === "month") {
						const yearValue = Number.parseInt(mergedParent.year, 10);
						const monthValue = Number.parseInt(value, 10);
						if (!Number.isFinite(yearValue)) throw new Error("heatmap.drilldownAxis month drill-down requires parent.year.");
						if (!Number.isFinite(monthValue)) throw new Error(`heatmap.drilldownAxis requires an integer month value; received ${value}.`);
						nextParent = {
							year: yearValue,
							month: monthValue
						};
						nextTimeUnit = "date";
						filters.push(`year(${datumRef(timeField)}) == ${yearValue}`);
						filters.push(`month(${datumRef(timeField)}) == ${monthValue - 1}`);
					} else if (level === "date") {
						const yearValue = Number.parseInt(mergedParent.year, 10);
						const monthValue = Number.parseInt(mergedParent.month, 10);
						const dateValue = Number.parseInt(value, 10);
						if (!Number.isFinite(yearValue) || !Number.isFinite(monthValue)) throw new Error("heatmap.drilldownAxis date drill-down requires parent.year and parent.month.");
						if (!Number.isFinite(dateValue)) throw new Error(`heatmap.drilldownAxis requires an integer date value; received ${value}.`);
						nextParent = {
							year: yearValue,
							month: monthValue,
							date: dateValue
						};
						nextTimeUnit = "date";
						filters.push(`year(${datumRef(timeField)}) == ${yearValue}`);
						filters.push(`month(${datumRef(timeField)}) == ${monthValue - 1}`);
						filters.push(`date(${datumRef(timeField)}) == ${dateValue}`);
					} else throw new Error(`heatmap.drilldownAxis does not support level "${level}".`);
					heatmapState.parent = nextParent;
					return {
						...spec,
						encoding: {
							...spec.encoding || {},
							x: {
								...spec.encoding?.x || {},
								field: timeField,
								type: "temporal",
								...nextTimeUnit ? { timeUnit: nextTimeUnit } : {}
							}
						},
						transform: [...nextTransforms, {
							filter: filters.join(" && "),
							_widgetvaTag: "heatmap.drilldownAxis"
						}],
						_heatmap_state: heatmapState
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					level,
					value,
					parent
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap x-axis timeUnit now reflects the finer drill-down level.", "Read the target widget view state to confirm the tagged drill-down filter now narrows the visible period."]
			};
		});
		if (!actionExecutor.has("heatmap.resetDrilldown")) actionExecutor.register({ name: "heatmap.resetDrilldown" }, async (call, ctx) => {
			call?.params;
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.resetDrilldown requires a valid heatmap target widget."
			});
			if (!targetWidget) throw new Error("heatmap.resetDrilldown requires a valid heatmap target widget.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap drill-down reset.");
					const heatmapState = spec._heatmap_state;
					const nextSpec = { ...spec };
					if (Array.isArray(nextSpec.transform)) nextSpec.transform = nextSpec.transform.filter((transform) => transform?._widgetvaTag !== "heatmap.drilldownAxis");
					const originalXEncoding = heatmapState && typeof heatmapState === "object" ? heatmapState.original_x_encoding : null;
					if (originalXEncoding && typeof originalXEncoding === "object") nextSpec.encoding = {
						...nextSpec.encoding || {},
						x: cloneValue$22(originalXEncoding)
					};
					delete nextSpec._heatmap_state;
					nextSpec._navigation_state = {
						mode: "reset",
						sourceAction: "heatmap.resetDrilldown"
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					reset: true
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the original heatmap x-axis encoding was restored.", "Read the target widget view state to confirm the drill-down filter and state marker were removed."]
			};
		});
		if (!actionExecutor.has("heatmap.addMarginalBars")) actionExecutor.register({ name: "heatmap.addMarginalBars" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.addMarginalBars requires a valid heatmap target widget."
			});
			const op = typeof params.op === "string" ? params.op.toLowerCase().trim() : "mean";
			const showTop = params.showTop !== false;
			const showRight = params.showRight !== false;
			const barSize = Number.isFinite(params.barSize) ? Number(params.barSize) : 70;
			const barColor = typeof params.barColor === "string" && params.barColor.length > 0 ? params.barColor : "#666666";
			const allowedAgg = new Set([
				"mean",
				"sum",
				"median",
				"max",
				"min",
				"count"
			]);
			if (!targetWidget) throw new Error("heatmap.addMarginalBars requires a valid heatmap target widget.");
			if (!showTop && !showRight) throw new Error("heatmap.addMarginalBars requires at least one of showTop/showRight to be true.");
			if (!allowedAgg.has(op)) throw new Error(`heatmap.addMarginalBars does not support op "${params.op}".`);
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap marginal bars.");
					const encoding = spec.encoding || {};
					const xEncoding = encoding.x || {};
					const yEncoding = encoding.y || {};
					const colorEncoding = encoding.color || {};
					const xField = xEncoding.field;
					const yField = yEncoding.field;
					const valueField = colorEncoding.field;
					if (!xField || !yField || !valueField) throw new Error("heatmap.addMarginalBars requires encoding.x.field, encoding.y.field, and encoding.color.field on the active heatmap spec.");
					const main = cloneValue$22(spec);
					const title = main.title;
					delete main.title;
					const defaultWidth = Number.isFinite(main.width) ? main.width : 400;
					const defaultHeight = Number.isFinite(main.height) ? main.height : 300;
					main.width = defaultWidth;
					main.height = defaultHeight;
					const baseData = cloneValue$22(main.data);
					const baseTransform = cloneValue$22(main.transform);
					const baseConfig = cloneValue$22(main.config);
					const baseBlock = () => ({
						...baseData !== void 0 ? { data: cloneValue$22(baseData) } : {},
						...baseTransform !== void 0 ? { transform: cloneValue$22(baseTransform) } : {},
						...baseConfig !== void 0 ? { config: cloneValue$22(baseConfig) } : {}
					});
					const topSpec = showTop ? {
						...baseBlock(),
						mark: {
							type: "bar",
							color: barColor
						},
						encoding: {
							x: cloneValue$22(xEncoding),
							y: {
								aggregate: op,
								field: valueField,
								type: "quantitative",
								title: null
							},
							tooltip: [{
								field: xField,
								...xEncoding.timeUnit ? { timeUnit: xEncoding.timeUnit } : {},
								type: xEncoding.type || "nominal",
								title: xEncoding.title || xField
							}, {
								aggregate: op,
								field: valueField,
								type: "quantitative",
								title: `${op}(${valueField})`
							}]
						},
						height: barSize,
						width: defaultWidth
					} : null;
					if (topSpec) {
						topSpec.encoding.x.axis = {
							labels: false,
							ticks: false,
							title: null,
							domain: false
						};
						topSpec.encoding.y.axis = {
							grid: false,
							ticks: false,
							title: null
						};
					}
					const rightSpec = showRight ? {
						...baseBlock(),
						mark: {
							type: "bar",
							color: barColor
						},
						encoding: {
							y: cloneValue$22(yEncoding),
							x: {
								aggregate: op,
								field: valueField,
								type: "quantitative",
								title: null
							},
							tooltip: [{
								field: yField,
								...yEncoding.timeUnit ? { timeUnit: yEncoding.timeUnit } : {},
								type: yEncoding.type || "nominal",
								title: yEncoding.title || yField
							}, {
								aggregate: op,
								field: valueField,
								type: "quantitative",
								title: `${op}(${valueField})`
							}]
						},
						width: barSize,
						height: defaultHeight
					} : null;
					if (rightSpec) {
						rightSpec.encoding.y.axis = {
							labels: false,
							ticks: false,
							title: null,
							domain: false
						};
						rightSpec.encoding.x.axis = {
							grid: false,
							ticks: false,
							title: null
						};
					}
					const row = {
						hconcat: [main, ...rightSpec ? [rightSpec] : []],
						resolve: { scale: { y: "shared" } }
					};
					return {
						$schema: spec.$schema || "https://vega.github.io/schema/vega-lite/v5.json",
						...title !== void 0 ? { title } : {},
						vconcat: [...topSpec ? [topSpec] : [], row],
						resolve: { scale: { x: "shared" } },
						_marginal_bars_state: {
							enabled: true,
							op,
							show_top: showTop,
							show_right: showRight,
							value_field: valueField,
							x_field: xField,
							y_field: yField
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					op,
					showTop,
					showRight
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap was composed with top and/or right marginal bar charts.", "Read the target widget view state to confirm the marginal bars aggregate the original heatmap value field."]
			};
		});
		if (!actionExecutor.has("heatmap.highlightRegion")) actionExecutor.register({ name: "heatmap.highlightRegion" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.highlightRegion requires a valid heatmap target widget."
			});
			const xValues = Array.isArray(params.xValues) ? params.xValues.filter((value) => value != null) : [];
			const yValues = Array.isArray(params.yValues) ? params.yValues.filter((value) => value != null) : [];
			if (!targetWidget || xValues.length === 0 && yValues.length === 0) throw new Error("heatmap.highlightRegion requires a heatmap target and at least one of xValues or yValues.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap region highlighting.");
					const xField = spec?.encoding?.x?.field;
					const yField = spec?.encoding?.y?.field;
					if (!xField || !yField) throw new Error("heatmap.highlightRegion requires x and y encodings on the active heatmap spec.");
					const tests = [];
					if (xValues.length > 0) tests.push(`indexof(${JSON.stringify(xValues)}, datum["${xField}"]) >= 0`);
					if (yValues.length > 0) tests.push(`indexof(${JSON.stringify(yValues)}, datum["${yField}"]) >= 0`);
					return {
						...spec,
						encoding: {
							...spec.encoding || {},
							opacity: {
								condition: {
									test: tests.join(" && "),
									value: 1
								},
								value: .15
							}
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					xValues,
					yValues
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap opacity condition was updated.", "Read the target widget view state to confirm the requested region is emphasized."]
			};
		});
		if (!actionExecutor.has("heatmap.adjustColorScale")) actionExecutor.register({ name: "heatmap.adjustColorScale" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.adjustColorScale requires a valid heatmap target widget."
			});
			const scheme = typeof params.scheme === "string" ? params.scheme.trim() : "";
			const domain = Array.isArray(params.domain) ? params.domain : null;
			if (!targetWidget || !scheme) throw new Error("heatmap.adjustColorScale requires a heatmap target and a non-empty scheme.");
			if (domain && domain.length !== 2) throw new Error("heatmap.adjustColorScale domain must contain exactly two values when provided.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap color-scale updates.");
					const nextEncoding = { ...spec.encoding || {} };
					nextEncoding.color = {
						...nextEncoding.color || {},
						scale: {
							...nextEncoding.color && nextEncoding.color.scale || {},
							scheme,
							...domain ? { domain } : {}
						}
					};
					return {
						...spec,
						encoding: nextEncoding,
						_color_scale_state: {
							channel: "color",
							scheme,
							...domain ? { domain } : {}
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					scheme,
					...domain ? { domain } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap color scale scheme was updated.", "Read the target widget view state to confirm the requested color domain values."]
			};
		});
		if (!actionExecutor.has("heatmap.thresholdMask")) actionExecutor.register({ name: "heatmap.thresholdMask" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.thresholdMask requires a valid heatmap target widget."
			});
			const minValue = Number(params.minValue);
			const maxValue = Number(params.maxValue);
			const outsideOpacity = Number.isFinite(params.outsideOpacity) ? Math.max(0, Math.min(1, params.outsideOpacity)) : .1;
			if (!targetWidget || !Number.isFinite(minValue) || !Number.isFinite(maxValue)) throw new Error("heatmap.thresholdMask requires a heatmap target plus finite minValue and maxValue.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap threshold masking.");
					const colorEncoding = spec?.encoding?.color;
					const colorField = colorEncoding?.field;
					if (!colorField) throw new Error("heatmap.thresholdMask requires a color encoding field on the active heatmap spec.");
					const aggregate = colorEncoding?.aggregate;
					const aggregateAs = colorEncoding?.as;
					const valueField = aggregate ? typeof aggregateAs === "string" && aggregateAs.trim().length > 0 ? aggregateAs.trim() : `${String(aggregate).toLowerCase()}_${colorField}` : colorField;
					return {
						...spec,
						encoding: {
							...spec.encoding || {},
							opacity: {
								condition: {
									test: `datum['${valueField}'] >= ${minValue} && datum['${valueField}'] <= ${maxValue}`,
									value: 1
								},
								value: outsideOpacity
							}
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					minValue,
					maxValue,
					outsideOpacity
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap opacity condition now masks values outside the requested threshold range.", "Read the target widget view state to confirm the requested threshold mask was applied."]
			};
		});
		if (!actionExecutor.has("heatmap.filterCellsByRegion")) actionExecutor.register({ name: "heatmap.filterCellsByRegion" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.filterCellsByRegion requires a valid heatmap target widget."
			});
			const xValues = Array.isArray(params.xValues) ? params.xValues.filter((value) => value != null) : params.xValue != null ? [params.xValue] : [];
			const yValues = Array.isArray(params.yValues) ? params.yValues.filter((value) => value != null) : params.yValue != null ? [params.yValue] : [];
			if (!targetWidget || xValues.length === 0 && yValues.length === 0) throw new Error("heatmap.filterCellsByRegion requires a heatmap target and at least one of xValues/yValues or xValue/yValue.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap region filtering.");
					const xField = spec?.encoding?.x?.field;
					const yField = spec?.encoding?.y?.field;
					const xTimeUnit = spec?.encoding?.x?.timeUnit;
					const yTimeUnit = spec?.encoding?.y?.timeUnit;
					if (!xField || !yField) throw new Error("heatmap.filterCellsByRegion requires x and y encodings on the active heatmap spec.");
					let filterSpec = null;
					if (xValues.length > 0 && yValues.length === 0 && !xTimeUnit) filterSpec = {
						field: xField,
						notOneOf: [...xValues]
					};
					else if (yValues.length > 0 && xValues.length === 0 && !yTimeUnit) filterSpec = {
						field: yField,
						notOneOf: [...yValues]
					};
					else {
						const excludeParts = [];
						if (xValues.length > 0) {
							const { valueListExpr, datumExpr } = normalizeTimeUnitValues(xField, xValues, xTimeUnit);
							excludeParts.push(`indexof([${valueListExpr}], ${datumExpr}) >= 0`);
						}
						if (yValues.length > 0) {
							const { valueListExpr, datumExpr } = normalizeTimeUnitValues(yField, yValues, yTimeUnit);
							excludeParts.push(`indexof([${valueListExpr}], ${datumExpr}) >= 0`);
						}
						filterSpec = `!(${excludeParts.join(" && ")})`;
					}
					return {
						...spec,
						transform: replaceTaggedTransform$1(spec.transform, "heatmap.filterCellsByRegion", {
							filter: filterSpec,
							_widgetvaTag: "heatmap.filterCellsByRegion"
						})
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					xValues,
					yValues
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap transform now excludes the requested region.", "Read the target widget rows to confirm the requested region no longer appears in the visible matrix."]
			};
		});
		if (!actionExecutor.has("heatmap.highlightRegionByValue")) actionExecutor.register({ name: "heatmap.highlightRegionByValue" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.highlightRegionByValue requires a valid heatmap target widget."
			});
			const hasMin = params.minValue != null && params.minValue !== "";
			const hasMax = params.maxValue != null && params.maxValue !== "";
			const minValue = hasMin ? Number(params.minValue) : null;
			const maxValue = hasMax ? Number(params.maxValue) : null;
			const outsideOpacity = Number.isFinite(params.outsideOpacity) ? Math.max(0, Math.min(1, params.outsideOpacity)) : .12;
			if (!targetWidget || !hasMin && !hasMax || hasMin && !Number.isFinite(minValue) || hasMax && !Number.isFinite(maxValue)) throw new Error("heatmap.highlightRegionByValue requires a heatmap target and at least one finite minValue or maxValue.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap value-range highlighting.");
					const colorEncoding = spec?.encoding?.color;
					const colorField = colorEncoding?.field;
					if (!colorField) throw new Error("heatmap.highlightRegionByValue requires a color encoding field on the active heatmap spec.");
					const aggregate = colorEncoding?.aggregate;
					const aggregateAs = colorEncoding?.as;
					const valueField = aggregate ? typeof aggregateAs === "string" && aggregateAs.trim().length > 0 ? aggregateAs.trim() : `${String(aggregate).toLowerCase()}_${colorField}` : colorField;
					const tests = [];
					if (hasMin) tests.push(`datum['${valueField}'] >= ${minValue}`);
					if (hasMax) tests.push(`datum['${valueField}'] <= ${maxValue}`);
					return {
						...spec,
						encoding: {
							...spec.encoding || {},
							opacity: {
								condition: {
									test: tests.join(" && "),
									value: 1
								},
								value: outsideOpacity
							}
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					...hasMin ? { minValue } : {},
					...hasMax ? { maxValue } : {},
					outsideOpacity
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap opacity condition now highlights values inside the requested range.", "Read the target widget view state to confirm the value-range highlight was applied without filtering data away."]
			};
		});
		if (!actionExecutor.has("heatmap.clusterRowsCols")) actionExecutor.register({ name: "heatmap.clusterRowsCols" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.clusterRowsCols requires a valid heatmap target widget."
			});
			const clusterRows = params.clusterRows !== false;
			const clusterCols = params.clusterCols !== false;
			const requestedMethod = typeof params.method === "string" ? params.method.toLowerCase().trim() : "sum";
			const method = [
				"sum",
				"mean",
				"max"
			].includes(requestedMethod) ? requestedMethod : "sum";
			if (!targetWidget || !clusterRows && !clusterCols) throw new Error("heatmap.clusterRowsCols requires a heatmap target and at least one of clusterRows or clusterCols to be true.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap row/column clustering.");
					const colorField = spec?.encoding?.color?.field;
					if (!colorField) throw new Error("heatmap.clusterRowsCols requires a color encoding field on the active heatmap spec.");
					const nextEncoding = cloneValue$22(spec.encoding || {});
					if (clusterRows && nextEncoding.y) nextEncoding.y = {
						...nextEncoding.y,
						sort: {
							op: method,
							field: colorField,
							order: "descending"
						}
					};
					if (clusterCols && nextEncoding.x) nextEncoding.x = {
						...nextEncoding.x,
						sort: {
							op: method,
							field: colorField,
							order: "descending"
						}
					};
					return {
						...spec,
						encoding: nextEncoding,
						_cluster_rows_cols_state: {
							cluster_rows: clusterRows,
							cluster_cols: clusterCols,
							method,
							color_field: colorField
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					clusterRows,
					clusterCols,
					method
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap x/y encodings now carry aggregate sort metadata.", "Read the target widget view state to confirm the requested rows and/or columns are reordered by aggregated value."]
			};
		});
		if (!actionExecutor.has("heatmap.transpose")) actionExecutor.register({ name: "heatmap.transpose" }, async (call, ctx) => {
			call?.params;
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "heatmap",
				message: "heatmap.transpose requires a valid heatmap target widget."
			});
			if (!targetWidget) throw new Error("heatmap.transpose requires a valid heatmap target widget.");
			const nextState = ctx.updateCurrentSpec((spec) => {
				if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for heatmap transpose.");
				const xEncoding = spec?.encoding?.x;
				const yEncoding = spec?.encoding?.y;
				if (!xEncoding || !yEncoding) throw new Error("heatmap.transpose requires both x and y encodings on the active heatmap spec.");
				return {
					...spec,
					encoding: {
						...spec.encoding || {},
						x: cloneValue$22(yEncoding),
						y: cloneValue$22(xEncoding)
					},
					...spec.width != null && spec.height != null ? {
						width: spec.height,
						height: spec.width
					} : {},
					_transpose_state: {
						...spec._transpose_state || { transposed: false },
						transposed: !(spec._transpose_state?.transposed === true)
					}
				};
			});
			return {
				nextState,
				result: {
					widgetId: targetWidget.widgetId,
					transposed: nextState?._transpose_state?.transposed === true
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap x and y encodings were swapped.", "Read the target widget view state to confirm the transpose marker and any width/height swap were applied."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/heatmap/perception.js
	function buildHeatmapPerceptionDescriptors({ dataRef }) {
		return [makePerceptionDescriptor({
			name: "perception.findExtremes",
			title: "Find extremes",
			description: appendQueryScopeGuidance("Return highest or lowest valued cells in the visible heatmap data."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				field: { type: "string" },
				direction: { type: "string" },
				limit: { type: "number" }
			}),
			sideEffectFree: true,
			evidenceKinds: ["matrixEvidence", "rankEvidence"],
			examples: [{
				userGoal: "Find the highest or lowest cells in the matrix view.",
				params: {
					field: "value",
					direction: "max",
					limit: 5
				}
			}, buildQueryScopeExample({
				userGoal: "Find extreme cells inside one selected heatmap region.",
				params: {
					field: "value",
					direction: "max",
					limit: 5
				},
				widgetRef: "wl://demo/workspace/main/widget/heatmap_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/heatmap_a/selection/region"
			})]
		}), makePerceptionDescriptor({
			name: "perception.findOutliers",
			title: "Find outliers",
			description: appendQueryScopeGuidance("Return likely outlier cells in the visible heatmap data."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				field: { type: "string" },
				zThreshold: { type: "number" },
				limit: { type: "number" }
			}),
			sideEffectFree: true,
			evidenceKinds: ["outlierEvidence", "matrixEvidence"],
			examples: [{
				userGoal: "Identify suspicious cells in the current heatmap view.",
				params: {
					field: "value",
					zThreshold: 2.5,
					limit: 10
				}
			}, buildQueryScopeExample({
				userGoal: "Identify outlier cells inside one selected heatmap region.",
				params: {
					field: "value",
					zThreshold: 2.5,
					limit: 10
				},
				widgetRef: "wl://demo/workspace/main/widget/heatmap_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/heatmap_a/selection/region"
			})]
		})];
	}
	function registerHeatmapPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["heatmap"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["heatmap"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "heatmap" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["heatmap"] });
		if (!perceptionRegistry.has("perception.findOutliers", { supportedWidgetKinds: ["heatmap"] })) perceptionRegistry.register({
			name: "perception.findOutliers",
			supportedWidgetKinds: ["heatmap"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "heatmap" });
			const params = ctx.readCallParams();
			const outliers = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findOutliers",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: outliers,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["heatmap"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/heatmap/humanInteraction.js
	function getHeatmapHumanInteractionConfig() {
		return {
			mode: "cellClick",
			actionName: "heatmap.filterCells",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/heatmap/state.js
	async function applyHeatmapState(args) {
		return applyVegaLiteRuntimeState(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/heatmapWidgetAdapter.js
	var heatmapWidgetAdapter = createVegaLiteWidgetAdapter({
		kind: "heatmap",
		async applyState(args) {
			return applyHeatmapState(args);
		},
		buildActionDescriptors(args) {
			return buildHeatmapActionDescriptors(args);
		},
		buildPerceptionDescriptors(args) {
			return buildHeatmapPerceptionDescriptors(args);
		},
		getHumanInteractionConfig() {
			return getHeatmapHumanInteractionConfig();
		},
		registerActions(actionExecutor) {
			return registerHeatmapActions(actionExecutor);
		},
		registerPerceptionQueries(perceptionRegistry) {
			return registerHeatmapPerceptionQueries(perceptionRegistry);
		}
	});
	//#endregion
	//#region ../../widgetva-kit/src/widgets/line/actions.js
	function replaceTaggedTransform(transforms, tag, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => transform?._widgetvaTag !== tag);
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function replaceTaggedLayer$1(layers, tag, nextLayer) {
		const nextLayers = (Array.isArray(layers) ? layers : []).filter((layer) => layer?._widgetvaTag !== tag);
		return nextLayer ? [...nextLayers, nextLayer] : nextLayers;
	}
	function inferLineRootEncoding(spec) {
		return spec?.layer?.[0]?.encoding || spec?.encoding || {};
	}
	function inferRawTimeField(spec, rootEncoding, originalTransforms) {
		const taggedTimeUnit = (Array.isArray(originalTransforms) ? originalTransforms : []).find((transform) => transform && typeof transform === "object" && "timeUnit" in transform && typeof transform.field === "string");
		if (taggedTimeUnit?.field) return taggedTimeUnit.field;
		const xField = rootEncoding?.x?.field;
		if (typeof xField !== "string" || xField.length === 0) return null;
		if (!xField.includes("_")) return xField;
		const candidate = xField.split("_").at(-1);
		const rows = spec?.data?.values;
		if (Array.isArray(rows) && rows.length > 0 && candidate && Object.prototype.hasOwnProperty.call(rows[0], candidate)) return candidate;
		return xField;
	}
	function inferRawValueField(spec, rootEncoding, originalTransforms) {
		const aggregateTransform = (Array.isArray(originalTransforms) ? originalTransforms : []).find((transform) => transform && typeof transform === "object" && Array.isArray(transform.aggregate) && transform.aggregate.length > 0);
		if (aggregateTransform?.aggregate?.[0]?.field) return aggregateTransform.aggregate[0].field;
		const yField = rootEncoding?.y?.field;
		if (typeof yField !== "string" || yField.length === 0) return null;
		if (yField.startsWith("total_")) return yField.slice(6);
		if (yField.startsWith("sum_")) return yField.slice(4);
		return yField;
	}
	function detectTemporalAxis(encoding, timeField) {
		if (encoding?.x?.field === timeField || encoding?.x?.type === "temporal") return {
			timeAxis: "x",
			valueAxis: "y"
		};
		return {
			timeAxis: "y",
			valueAxis: "x"
		};
	}
	function buildLineActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
		const supportedWidgetKinds = ["line"];
		return [
			makeActionDescriptor({
				name: "line.selectSeries",
				title: "Select line series",
				description: "Select one or more categorical series represented in the line chart.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						field: { type: "string" },
						values: {
							type: "array",
							items: { type: "string" }
						}
					},
					required: ["field", "values"]
				},
				postconditions: [{ description: "The active selection should contain the selected series values." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.selectSeries requires a valid line target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a categorical selection over line series."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeFilterEffect(ref, "Linked widgets may be filtered by the selected line series."))],
				examples: [{
					userGoal: "Focus one or more line series before comparing trends.",
					params: {
						field: "Origin",
						values: ["Japan"]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.selectXValue",
				title: "Select a line x-axis value",
				description: "Select all visible line rows that share one x-axis value, such as one year or one named category bucket.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						value: { anyOf: [{ type: "string" }, { type: "number" }] },
						field: { type: "string" }
					},
					required: ["value"]
				},
				postconditions: [{ description: "The active selection should contain one predicate targeting the chosen x-axis value." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.selectXValue requires a valid line target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a selection over one x-axis value in the current line view."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeFilterEffect(ref, "Linked widgets may update to the selected line x-axis slice."))],
				examples: [{
					userGoal: "Select one model year across the line view before checking linked distributions and details.",
					params: {
						field: "year",
						value: 1971
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.zoomXRegion",
				title: "Zoom line chart x-region",
				description: "Zoom the temporal x-axis of the line chart to a specific start/end range without discarding data.",
				primitive: "zoom",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						start: { type: "string" },
						end: { type: "string" }
					},
					required: ["start", "end"]
				},
				postconditions: [{ description: "The line chart x-axis domain should reflect the requested start/end range." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.zoomXRegion requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Zoom into a particular date range to inspect the detailed trend.",
					params: {
						start: "2024-01-01",
						end: "2024-02-01"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.focusLines",
				title: "Focus line series",
				description: "Emphasize one or more line series while dimming the remaining lines.",
				primitive: "focus",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						lines: {
							type: "array",
							items: { type: "string" }
						},
						lineField: { type: "string" },
						dimOpacity: { type: "number" }
					},
					required: ["lines"]
				},
				postconditions: [{ description: "The requested line series should be visually emphasized while the remaining lines are dimmed." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.focusLines requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Focus a subset of line series before comparing their trends.",
					params: {
						lines: ["A"],
						lineField: "series",
						dimOpacity: .08
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.highlightTrend",
				title: "Highlight line trend",
				description: "Add or refresh a regression trend line layer over the existing line chart.",
				primitive: "annotate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { trendType: { type: "string" } }
				},
				postconditions: [{ description: "The line chart should include a visible regression trend line layer over the current x/y encoding." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.highlightTrend requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Add a regression overlay before describing the overall trend direction.",
					params: { trendType: "increasing" }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.showMovingAverage",
				title: "Overlay moving average",
				description: "Add or replace a moving-average overlay line computed from the current temporal/value encodings, optionally grouped by line series.",
				primitive: "annotate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { windowSize: { type: "number" } }
				},
				postconditions: [{ description: "A tagged moving-average overlay layer should be present and should use a Vega window transform over the current line encodings." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.showMovingAverage requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Smooth a noisy line chart with a 3-period trailing moving average.",
					params: { windowSize: 3 }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.drillDownXAxis",
				title: "Drill down line x-axis",
				description: "Drill a temporal line chart from a coarser time aggregation into a more detailed x-axis view.",
				primitive: "drillDown",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						level: { type: "string" },
						value: { type: "number" },
						parent: { type: "object" }
					},
					required: ["level", "value"]
				},
				postconditions: [{ description: "The line chart should switch to a finer temporal aggregation with matching filters and updated x/y encodings." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.drillDownXAxis requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Drill a yearly trend into monthly detail for a specific year.",
					params: {
						level: "year",
						value: 2024
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.resetDrilldownXAxis",
				title: "Reset line x-axis drill-down",
				description: "Restore the original line chart encoding, transforms, and title after a temporal drill-down.",
				primitive: "navigate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The line chart should revert to its original temporal encoding, transforms, and title." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.resetDrilldownXAxis requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Return from a drilled monthly view back to the original yearly chart.",
					params: {}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.resampleXAxis",
				title: "Resample line x-axis",
				description: "Change the temporal aggregation granularity of a line chart and apply an aggregate to the value axis.",
				primitive: "aggregate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						granularity: { type: "string" },
						agg: { type: "string" }
					},
					required: ["granularity"]
				},
				postconditions: [{ description: "The line chart time axis should use the requested timeUnit and the value axis should use the requested aggregation." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.resampleXAxis requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Switch a dense daily series into monthly mean values before comparing long-term trends.",
					params: {
						granularity: "month",
						agg: "mean"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.resetResampleXAxis",
				title: "Reset line x-axis resample",
				description: "Restore the original temporal encoding after a line x-axis resampling operation.",
				primitive: "navigate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The line chart should revert to its original temporal encoding and clear the resample state." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.resetResampleXAxis requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Return a resampled monthly line chart back to its original daily granularity.",
					params: {}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.boldLines",
				title: "Bold line series",
				description: "Increase the stroke width of one or more line series while keeping the remaining lines thin.",
				primitive: "highlight",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						lineNames: {
							type: "array",
							items: { type: "string" }
						},
						lineField: { type: "string" },
						boldWidth: { type: "number" },
						baseWidth: { type: "number" }
					},
					required: ["lineNames"]
				},
				postconditions: [{ description: "The requested line series should be rendered with a thicker stroke than the remaining lines." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.boldLines requires a valid line target widget."
				}],
				examples: [{
					userGoal: "Make one or more line series stand out before comparing the trends.",
					params: {
						lineNames: ["A"],
						lineField: "series",
						boldWidth: 4,
						baseWidth: 1
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "line.filterLines",
				title: "Filter out line series",
				description: "Exclude one or more line series from the current chart by writing a series filter into the line spec.",
				primitive: "filter",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						linesToRemove: {
							type: "array",
							items: { type: "string" }
						},
						lineField: { type: "string" }
					},
					required: ["linesToRemove"]
				},
				postconditions: [{ description: "The line chart transform list should exclude the requested line series." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid line widget in the current workspace.",
					failureMessage: "line.filterLines requires a valid line target widget."
				}],
				effects: affectedRefs.map((ref) => makeFilterEffect(ref, ref === widgetRef ? "The requested line series are excluded from the current chart." : "Linked widgets may update to reflect the removed line series.")),
				examples: [{
					userGoal: "Remove noisy or irrelevant series before comparing the remaining trends.",
					params: {
						linesToRemove: ["B"],
						lineField: "series"
					}
				}],
				reversible: true
			})
		];
	}
	function registerLineActions(actionExecutor) {
		if (!actionExecutor.has("line.selectSeries")) actionExecutor.register({ name: "line.selectSeries" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line"
			});
			const field = typeof params.field === "string" ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => typeof value === "string") : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("line.selectSeries requires a line target, field, and one or more values.");
			const { rows: visibleRows } = ctx.readRowsForWidget(targetWidget.ref);
			const matchedCount = visibleRows.filter((row) => values.includes(row?.[field])).length;
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "category",
					field,
					values,
					predicates: [{
						field,
						op: "in",
						value: values
					}],
					count: matchedCount,
					summary: `${field}: ${values.join(", ")}`
				}),
				selectedCount: matchedCount,
				verificationHints: ["Read the updated line selection state.", "Read linked widgets to confirm series-level filter propagation."]
			});
		});
		if (!actionExecutor.has("line.zoomXRegion")) actionExecutor.register({ name: "line.zoomXRegion" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.zoomXRegion requires a valid line target widget."
			});
			const start = typeof params.start === "string" ? params.start : null;
			const end = typeof params.end === "string" ? params.end : null;
			if (!targetWidget || !start || !end) throw new Error("line.zoomXRegion requires a line target plus start and end values.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line x-domain updates.");
					const nextEncoding = { ...spec.encoding || {} };
					if (!nextEncoding.x) throw new Error("line.zoomXRegion requires an x encoding on the active line spec.");
					nextEncoding.x = {
						...nextEncoding.x,
						scale: {
							...nextEncoding.x.scale || {},
							domain: [start, end]
						}
					};
					const nextMark = typeof spec.mark === "string" ? {
						type: spec.mark,
						clip: true
					} : {
						...spec.mark || {},
						clip: true
					};
					return {
						...spec,
						mark: nextMark,
						encoding: nextEncoding
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					xDomain: [start, end]
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line chart x-domain was updated.", "Read the target widget view state to confirm the new temporal x-domain values."]
			};
		});
		if (!actionExecutor.has("line.selectXValue")) actionExecutor.register({ name: "line.selectXValue" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.selectXValue requires a valid line target widget."
			});
			const rawValue = params.value;
			const explicitField = typeof params.field === "string" && params.field.length > 0 ? params.field : null;
			if (!targetWidget || rawValue == null) throw new Error("line.selectXValue requires a line target and a non-null x-axis value.");
			const { rows: visibleRows } = ctx.readRowsForWidget(targetWidget.ref);
			const currentSpec = ctx.readCurrentSpec?.() || targetWidget.readState?.().currentSpec || targetWidget.readState?.().rawSpec || null;
			const rootEncoding = currentSpec?.layer?.[0]?.encoding || currentSpec?.encoding || {};
			const field = explicitField || rootEncoding?.x?.field || null;
			if (!field) throw new Error("line.selectXValue requires a resolvable x-axis field on the active line spec.");
			const matchedCount = visibleRows.filter((row) => row?.[field] === rawValue).length;
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "category",
					field,
					values: [rawValue],
					predicates: [{
						field,
						op: "in",
						value: [rawValue]
					}],
					count: matchedCount,
					summary: `${field}: ${rawValue}`
				}),
				selectedCount: matchedCount,
				verificationHints: ["Read the updated line selection state.", "Read linked widgets to confirm the selected x-axis slice propagated beyond the line chart."]
			});
		});
		if (!actionExecutor.has("line.focusLines")) actionExecutor.register({ name: "line.focusLines" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.focusLines requires a valid line target widget."
			});
			const lines = Array.isArray(params.lines) ? params.lines.filter((value) => typeof value === "string" && value.length > 0) : [];
			const dimOpacity = typeof params.dimOpacity === "number" ? params.dimOpacity : .08;
			if (!targetWidget || lines.length === 0) throw new Error("line.focusLines requires a line target and at least one line identifier.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line focus updates.");
					const encoding = spec.encoding || {};
					const lineField = typeof params.lineField === "string" ? params.lineField : encoding?.color?.field || encoding?.detail?.field || null;
					if (!lineField) throw new Error("line.focusLines requires a lineField or an existing color/detail grouping field on the active line spec.");
					const linesJson = JSON.stringify(lines);
					return {
						...spec,
						encoding: {
							...encoding,
							opacity: {
								condition: {
									test: `indexof(${linesJson}, datum['${lineField}']) >= 0`,
									value: 1
								},
								value: dimOpacity
							}
						},
						_line_focus_state: {
							lines,
							line_field: lineField
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					lines,
					lineField: params.lineField || null,
					dimOpacity
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line chart opacity condition now focuses the requested series.", "Read the target widget view state to confirm non-focused lines are dimmed."]
			};
		});
		if (!actionExecutor.has("line.highlightTrend")) actionExecutor.register({ name: "line.highlightTrend" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.highlightTrend requires a valid line target widget."
			});
			const trendType = typeof params.trendType === "string" && params.trendType.length > 0 ? params.trendType : "increasing";
			if (!targetWidget) throw new Error("line.highlightTrend requires a valid line target widget.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line trend highlighting.");
					const rootEncoding = spec?.layer?.[0]?.encoding || spec?.encoding || {};
					const xField = rootEncoding?.x?.field || null;
					const yField = rootEncoding?.y?.field || null;
					const xType = rootEncoding?.x?.type || "temporal";
					const yType = rootEncoding?.y?.type || "quantitative";
					if (!xField || !yField) throw new Error("line.highlightTrend requires x and y encodings on the active line spec.");
					const trendLayer = {
						_widgetvaTag: "line.highlightTrend",
						mark: {
							type: "line",
							color: "red",
							strokeDash: [5, 5],
							strokeWidth: 2
						},
						transform: [{
							regression: yField,
							on: xField
						}],
						encoding: {
							x: {
								field: xField,
								type: xType
							},
							y: {
								field: yField,
								type: yType
							}
						}
					};
					if (Array.isArray(spec.layer) && spec.layer.length > 0) return {
						...spec,
						layer: replaceTaggedLayer$1(spec.layer, "line.highlightTrend", trendLayer)
					};
					const nextSpec = { ...spec };
					const baseLayer = {
						mark: spec.mark || "line",
						encoding: spec.encoding || {}
					};
					delete nextSpec.mark;
					delete nextSpec.encoding;
					return {
						...nextSpec,
						layer: replaceTaggedLayer$1([baseLayer], "line.highlightTrend", trendLayer)
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					trendType,
					overlay: "regression"
				},
				verificationHints: ["Call perception.inspectViewConfig to verify a regression trend layer was added to the line chart.", "Read the target widget view state to confirm the trend overlay uses the current x/y encoding fields."]
			};
		});
		if (!actionExecutor.has("line.showMovingAverage")) actionExecutor.register({ name: "line.showMovingAverage" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.showMovingAverage requires a valid line target widget."
			});
			const windowSize = Number.isFinite(params.windowSize) ? Math.floor(Number(params.windowSize)) : 3;
			if (!targetWidget || windowSize < 1) throw new Error("line.showMovingAverage requires a valid line target widget and a windowSize >= 1.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line moving-average overlays.");
					const rootEncoding = spec?.layer?.[0]?.encoding || spec?.encoding || {};
					const xField = rootEncoding?.x?.field || null;
					const yField = rootEncoding?.y?.field || null;
					const xType = rootEncoding?.x?.type || "temporal";
					if (!xField || !yField) throw new Error("line.showMovingAverage requires x and y encodings on the active line spec.");
					const colorEncoding = rootEncoding?.color && typeof rootEncoding.color === "object" ? structuredClone(rootEncoding.color) : null;
					const detailEncoding = rootEncoding?.detail && typeof rootEncoding.detail === "object" ? structuredClone(rootEncoding.detail) : null;
					const groupField = colorEncoding?.field || detailEncoding?.field || null;
					const maField = `${yField}_ma`;
					const maLayer = {
						_widgetvaTag: "line.showMovingAverage",
						mark: {
							type: "line",
							color: "orange",
							strokeWidth: 3,
							opacity: .8
						},
						transform: [{
							window: [{
								op: "mean",
								field: yField,
								as: maField
							}],
							frame: [-(windowSize - 1), 0],
							sort: [{
								field: xField,
								order: "ascending"
							}],
							...groupField ? { groupby: [groupField] } : {}
						}],
						encoding: {
							x: {
								field: xField,
								type: xType
							},
							y: {
								field: maField,
								type: "quantitative"
							},
							...colorEncoding ? { color: colorEncoding } : {},
							...!colorEncoding && detailEncoding ? { detail: detailEncoding } : {}
						}
					};
					if (Array.isArray(spec.layer) && spec.layer.length > 0) return {
						...spec,
						layer: replaceTaggedLayer$1(spec.layer, "line.showMovingAverage", maLayer)
					};
					const nextSpec = { ...spec };
					const baseLayer = {
						mark: spec.mark || "line",
						encoding: spec.encoding || {}
					};
					delete nextSpec.mark;
					delete nextSpec.encoding;
					return {
						...nextSpec,
						layer: replaceTaggedLayer$1([baseLayer], "line.showMovingAverage", maLayer)
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					windowSize,
					overlay: "movingAverage"
				},
				verificationHints: ["Call perception.inspectViewConfig to verify a tagged moving-average overlay layer was added to the line chart.", "Read the target widget view state to confirm the overlay uses a window transform sorted by the current temporal axis."]
			};
		});
		if (!actionExecutor.has("line.drillDownXAxis")) actionExecutor.register({ name: "line.drillDownXAxis" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.drillDownXAxis requires a valid line target widget."
			});
			const level = typeof params.level === "string" ? params.level.toLowerCase().trim() : null;
			const numericValue = Number.isFinite(params.value) ? Number(params.value) : null;
			const parent = params.parent && typeof params.parent === "object" && !Array.isArray(params.parent) ? params.parent : {};
			if (!targetWidget || !level || numericValue === null) throw new Error("line.drillDownXAxis requires a line target plus level and numeric value.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line x-axis drill-down.");
					const drillState = spec._line_drilldown_state && typeof spec._line_drilldown_state === "object" ? { ...spec._line_drilldown_state } : {};
					if (!drillState.original_transform) {
						drillState.original_transform = Array.isArray(spec.transform) ? structuredClone(spec.transform) : [];
						drillState.original_encoding = structuredClone(spec.encoding || {});
						drillState.original_title = spec.title || "";
						const rootEncoding = inferLineRootEncoding(spec);
						drillState.raw_time_field = inferRawTimeField(spec, rootEncoding, drillState.original_transform);
						drillState.raw_value_field = inferRawValueField(spec, rootEncoding, drillState.original_transform);
						drillState.group_field = rootEncoding?.color?.field || null;
					}
					const rawDateField = drillState.raw_time_field;
					const rawValueField = drillState.raw_value_field;
					const groupField = drillState.group_field;
					if (!rawDateField || !rawValueField) throw new Error("line.drillDownXAxis requires x/y fields that can be resolved to raw date/value columns.");
					const nextTransforms = [];
					let nextEncoding;
					let nextParent = {};
					let titleSuffix = "";
					if (level === "year") {
						nextTransforms.push({
							filter: `year(datum.${rawDateField}) == ${numericValue}`,
							_widgetvaTag: "line.drillDownXAxis"
						});
						nextTransforms.push({
							timeUnit: "yearmonth",
							field: rawDateField,
							as: "month_date",
							_widgetvaTag: "line.drillDownXAxis"
						});
						nextTransforms.push({
							aggregate: [{
								op: "sum",
								field: rawValueField,
								as: "total_value"
							}],
							groupby: groupField ? ["month_date", groupField] : ["month_date"],
							_widgetvaTag: "line.drillDownXAxis"
						});
						nextParent = { year: numericValue };
						titleSuffix = `${numericValue} monthly trend`;
						nextEncoding = {
							x: {
								field: "month_date",
								type: "temporal",
								title: "Month",
								axis: { format: "%Y-%m" }
							},
							y: {
								field: "total_value",
								type: "quantitative",
								title: `Monthly total ${rawValueField}`
							},
							...groupField ? { color: drillState.original_encoding?.color || {
								field: groupField,
								type: "nominal"
							} } : {}
						};
					} else if (level === "month") {
						const yearValue = Number.isFinite(parent.year) ? Number(parent.year) : null;
						if (yearValue === null) throw new Error("line.drillDownXAxis month drill-down requires parent.year.");
						if (numericValue < 1 || numericValue > 12) throw new Error("line.drillDownXAxis month drill-down requires a month value between 1 and 12.");
						nextTransforms.push({
							filter: `year(datum.${rawDateField}) == ${yearValue} && month(datum.${rawDateField}) == ${numericValue - 1}`,
							_widgetvaTag: "line.drillDownXAxis"
						});
						nextTransforms.push({
							timeUnit: "yearmonthdate",
							field: rawDateField,
							as: "day_date",
							_widgetvaTag: "line.drillDownXAxis"
						});
						nextTransforms.push({
							aggregate: [{
								op: "sum",
								field: rawValueField,
								as: "total_value"
							}],
							groupby: groupField ? ["day_date", groupField] : ["day_date"],
							_widgetvaTag: "line.drillDownXAxis"
						});
						nextParent = {
							year: yearValue,
							month: numericValue
						};
						titleSuffix = `${yearValue}-${String(numericValue).padStart(2, "0")} daily trend`;
						nextEncoding = {
							x: {
								field: "day_date",
								type: "temporal",
								title: "Date",
								axis: { format: "%m-%d" }
							},
							y: {
								field: "total_value",
								type: "quantitative",
								title: `Daily total ${rawValueField}`
							},
							...groupField ? { color: drillState.original_encoding?.color || {
								field: groupField,
								type: "nominal"
							} } : {}
						};
					} else throw new Error(`line.drillDownXAxis does not support level "${level}".`);
					drillState.parent = nextParent;
					return {
						...spec,
						transform: nextTransforms,
						encoding: nextEncoding,
						title: titleSuffix,
						_line_drilldown_state: drillState
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					level,
					value: numericValue,
					parent
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line chart transform and x encoding now reflect the finer temporal drill-down.", "Read the target widget rows to confirm the line chart is now scoped to the drilled period."]
			};
		});
		if (!actionExecutor.has("line.resetDrilldownXAxis")) actionExecutor.register({ name: "line.resetDrilldownXAxis" }, async (call, ctx) => {
			call?.params;
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.resetDrilldownXAxis requires a valid line target widget."
			});
			if (!targetWidget) throw new Error("line.resetDrilldownXAxis requires a valid line target widget.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line drill-down reset.");
					const drillState = spec._line_drilldown_state;
					if (!drillState || typeof drillState !== "object") return spec;
					const nextSpec = { ...spec };
					if (Object.prototype.hasOwnProperty.call(drillState, "original_transform")) nextSpec.transform = Array.isArray(drillState.original_transform) ? structuredClone(drillState.original_transform) : [];
					else if (Array.isArray(nextSpec.transform)) nextSpec.transform = nextSpec.transform.filter((transform) => transform?._widgetvaTag !== "line.drillDownXAxis");
					if (drillState.original_encoding && typeof drillState.original_encoding === "object") nextSpec.encoding = structuredClone(drillState.original_encoding);
					if (Object.prototype.hasOwnProperty.call(drillState, "original_title")) nextSpec.title = drillState.original_title;
					delete nextSpec._line_drilldown_state;
					nextSpec._navigation_state = {
						mode: "reset",
						sourceAction: "line.resetDrilldownXAxis"
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					reset: true
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the original line x/y encoding and transforms were restored.", "Read the target widget view state to confirm the drill-down state marker has been removed."]
			};
		});
		if (!actionExecutor.has("line.resampleXAxis")) actionExecutor.register({ name: "line.resampleXAxis" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.resampleXAxis requires a valid line target widget."
			});
			const granularity = typeof params.granularity === "string" ? params.granularity.toLowerCase().trim() : null;
			const agg = typeof params.agg === "string" ? params.agg.toLowerCase().trim() : "mean";
			const granularityMap = {
				day: "yearmonthdate",
				week: "yearweek",
				month: "yearmonth",
				quarter: "yearquarter",
				year: "year"
			};
			const allowedAgg = new Set([
				"mean",
				"sum",
				"max",
				"min",
				"median",
				"count"
			]);
			if (!targetWidget || !granularity || !granularityMap[granularity]) throw new Error("line.resampleXAxis requires a line target and a supported granularity: day, week, month, quarter, or year.");
			if (!allowedAgg.has(agg)) throw new Error("line.resampleXAxis requires a supported aggregation: mean, sum, max, min, median, or count.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line x-axis resampling.");
					const nextSpec = { ...spec };
					const resampleState = nextSpec._resample_state && typeof nextSpec._resample_state === "object" ? { ...nextSpec._resample_state } : {};
					const rootEncoding = inferLineRootEncoding(nextSpec);
					const timeField = inferRawTimeField(nextSpec, rootEncoding, nextSpec.transform);
					if (!timeField) throw new Error("line.resampleXAxis requires a temporal field in the active line spec.");
					if (!resampleState.original_encoding) resampleState.original_encoding = structuredClone(rootEncoding);
					const applyEncodingUpdate = (encoding) => {
						const nextEncoding = structuredClone(encoding || {});
						const { timeAxis, valueAxis } = detectTemporalAxis(nextEncoding, timeField);
						if (!nextEncoding[timeAxis]) throw new Error("line.resampleXAxis requires a temporal axis encoding in the active line spec.");
						nextEncoding[timeAxis] = {
							...nextEncoding[timeAxis],
							timeUnit: granularityMap[granularity],
							type: "temporal"
						};
						if (nextEncoding[valueAxis]?.field) nextEncoding[valueAxis] = {
							...nextEncoding[valueAxis],
							aggregate: agg
						};
						return nextEncoding;
					};
					if (Array.isArray(nextSpec.layer) && nextSpec.layer.length > 0) nextSpec.layer = nextSpec.layer.map((layer, index) => {
						if (!layer?.encoding) return layer;
						if (index === 0 && !resampleState.original_encoding) resampleState.original_encoding = structuredClone(layer.encoding);
						return {
							...layer,
							encoding: applyEncodingUpdate(layer.encoding)
						};
					});
					else nextSpec.encoding = applyEncodingUpdate(nextSpec.encoding || {});
					resampleState.current_granularity = granularity;
					resampleState.current_agg = agg;
					nextSpec._resample_state = resampleState;
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					granularity,
					agg
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line chart time axis now uses the requested timeUnit.", "Read the target widget view state to confirm the value axis now carries the requested aggregation."]
			};
		});
		if (!actionExecutor.has("line.resetResampleXAxis")) actionExecutor.register({ name: "line.resetResampleXAxis" }, async (call, ctx) => {
			call?.params;
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.resetResampleXAxis requires a valid line target widget."
			});
			if (!targetWidget) throw new Error("line.resetResampleXAxis requires a valid line target widget.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line resample reset.");
					const resampleState = spec._resample_state;
					if (!resampleState || typeof resampleState !== "object") return spec;
					const nextSpec = { ...spec };
					const originalEncoding = resampleState.original_encoding;
					if (originalEncoding && typeof originalEncoding === "object") if (Array.isArray(nextSpec.layer) && nextSpec.layer.length > 0) nextSpec.layer = nextSpec.layer.map((layer, index) => {
						if (index !== 0 || !layer) return layer;
						return {
							...layer,
							encoding: structuredClone(originalEncoding)
						};
					});
					else nextSpec.encoding = structuredClone(originalEncoding);
					delete nextSpec._resample_state;
					nextSpec._navigation_state = {
						mode: "reset",
						sourceAction: "line.resetResampleXAxis"
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					reset: true
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the original line temporal encoding was restored.", "Read the target widget view state to confirm the resample state marker has been removed."]
			};
		});
		if (!actionExecutor.has("line.boldLines")) actionExecutor.register({ name: "line.boldLines" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.boldLines requires a valid line target widget."
			});
			const lineNames = Array.isArray(params.lineNames) ? params.lineNames.filter((value) => typeof value === "string" && value.length > 0) : [];
			const boldWidth = typeof params.boldWidth === "number" ? params.boldWidth : 4;
			const baseWidth = typeof params.baseWidth === "number" ? params.baseWidth : 1;
			if (!targetWidget || lineNames.length === 0) throw new Error("line.boldLines requires a line target and at least one line identifier.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line bolding updates.");
					const rootEncoding = spec?.layer?.[0]?.encoding || spec?.encoding || {};
					const lineField = typeof params.lineField === "string" ? params.lineField : rootEncoding?.color?.field || rootEncoding?.detail?.field || null;
					if (!lineField) throw new Error("line.boldLines requires a lineField or an existing color/detail grouping field on the active line spec.");
					const strokeWidth = {
						condition: {
							test: `indexof(${JSON.stringify(lineNames)}, datum['${lineField}']) >= 0`,
							value: boldWidth
						},
						value: baseWidth
					};
					if (Array.isArray(spec.layer) && spec.layer.length > 0) return {
						...spec,
						layer: spec.layer.map((layer) => {
							const mark = layer?.mark;
							if ((typeof mark === "string" ? mark : mark?.type) !== "line") return layer;
							return {
								...layer,
								encoding: {
									...layer?.encoding || {},
									strokeWidth
								}
							};
						})
					};
					return {
						...spec,
						encoding: {
							...spec.encoding || {},
							strokeWidth
						}
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					lineNames,
					lineField: params.lineField || null,
					boldWidth,
					baseWidth
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line chart strokeWidth condition now emphasizes the requested series.", "Read the target widget view state to confirm the requested line series are rendered with a thicker stroke."]
			};
		});
		if (!actionExecutor.has("line.filterLines")) actionExecutor.register({ name: "line.filterLines" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "line",
				message: "line.filterLines requires a valid line target widget."
			});
			const linesToRemove = Array.isArray(params.linesToRemove) ? params.linesToRemove.filter((value) => typeof value === "string" && value.length > 0) : [];
			if (!targetWidget || linesToRemove.length === 0) throw new Error("line.filterLines requires a line target and one or more line names to remove.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for line series filtering.");
					const rootEncoding = spec?.layer?.[0]?.encoding || spec?.encoding || {};
					const lineField = typeof params.lineField === "string" ? params.lineField : rootEncoding?.color?.field || rootEncoding?.detail?.field || null;
					if (!lineField) throw new Error("line.filterLines requires a line grouping field on the active line spec.");
					return {
						...spec,
						transform: replaceTaggedTransform(spec.transform, "line.filterLines", {
							filter: {
								field: lineField,
								notOneOf: linesToRemove
							},
							_widgetvaTag: "line.filterLines"
						})
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					linesToRemove,
					...typeof params.lineField === "string" ? { lineField: params.lineField } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line chart transform now excludes the requested line series.", "Read the target widget rows to confirm the removed series no longer appear in the visible line data."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/line/perception.js
	function inferFieldFromSpec(rawSpec, channel) {
		if (rawSpec?.encoding?.[channel]?.field) return rawSpec.encoding[channel].field;
		if (Array.isArray(rawSpec?.layer) && rawSpec.layer.length > 0) {
			for (const layer of rawSpec.layer) if (layer?.encoding?.[channel]?.field) return layer.encoding[channel].field;
		}
		return null;
	}
	function buildLineAnomalyResult({ rows, rawSpec, threshold = 2, yField = null, xField = null }) {
		const resolvedYField = yField || inferFieldFromSpec(rawSpec, "y");
		const resolvedXField = xField || inferFieldFromSpec(rawSpec, "x");
		if (!resolvedYField) throw new Error("perception.detectAnomalies requires a yField or a line spec with an encoded y field.");
		const numericValues = rows.map((row) => Number(row?.[resolvedYField])).filter((value) => Number.isFinite(value));
		if (numericValues.length < 3) throw new Error("perception.detectAnomalies requires at least 3 numeric values.");
		const mean = numericValues.reduce((sum, value) => sum + value, 0) / numericValues.length;
		const variance = numericValues.reduce((sum, value) => {
			const delta = value - mean;
			return sum + delta * delta;
		}, 0) / numericValues.length;
		const std = Math.sqrt(variance);
		const anomalies = rows.filter((row) => {
			const value = Number(row?.[resolvedYField]);
			return Number.isFinite(value) && Math.abs(value - mean) > threshold * std;
		}).map((row) => ({
			...resolvedXField && row?.[resolvedXField] != null ? { [resolvedXField]: row[resolvedXField] } : {},
			[resolvedYField]: row?.[resolvedYField]
		}));
		return {
			operation: "detect_anomalies",
			anomaly_count: anomalies.length,
			anomalies: anomalies.slice(0, 10),
			stats: {
				mean: Math.round(mean * 100) / 100,
				std: Math.round(std * 100) / 100,
				threshold,
				sample_size: numericValues.length,
				yField: resolvedYField,
				xField: resolvedXField
			},
			message: `Detected ${anomalies.length} anomalies (threshold=${threshold} std)`
		};
	}
	function buildLinePerceptionDescriptors({ dataRef }) {
		return [
			makePerceptionDescriptor({
				name: "perception.detectAnomalies",
				title: "Detect anomalies",
				description: appendQueryScopeGuidance("Detect likely anomalous points in the visible line data using a standard-deviation threshold on the y metric."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					threshold: { type: "number" },
					yField: { type: "string" },
					xField: { type: "string" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["outlierEvidence", "trendEvidence"],
				examples: [{
					userGoal: "Identify likely anomalous line points without changing the chart encoding.",
					params: { threshold: 2 }
				}, buildQueryScopeExample({
					userGoal: "Detect anomalies inside one selected line subset.",
					params: { threshold: 2 },
					widgetRef: "wl://demo/workspace/main/widget/line_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/line_a/selection/current"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findExtremes",
				title: "Find extremes",
				description: appendQueryScopeGuidance("Return top-k or bottom-k visible rows by a numeric field in the line view."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					field: { type: "string" },
					direction: { type: "string" },
					limit: { type: "number" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["trendEvidence", "rankEvidence"],
				examples: [{
					userGoal: "Find extrema in the visible line series data.",
					params: {
						field: "Miles_per_Gallon",
						direction: "max",
						limit: 5
					}
				}, buildQueryScopeExample({
					userGoal: "Find extremes inside one selected line subset.",
					params: {
						field: "Miles_per_Gallon",
						direction: "max",
						limit: 5
					},
					widgetRef: "wl://demo/workspace/main/widget/line_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/line_a/selection/current"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.compareGroups",
				title: "Compare groups",
				description: appendQueryScopeGuidance("Compare grouped trend values in the visible line data."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					groupField: { type: "string" },
					valueField: { type: "string" },
					groups: {
						type: "array",
						items: { type: "string" }
					}
				}),
				sideEffectFree: true,
				evidenceKinds: ["groupComparison", "trendEvidence"],
				examples: [{
					userGoal: "Compare the trends or magnitudes of a few line groups.",
					params: {
						groupField: "Origin",
						valueField: "Miles_per_Gallon",
						groups: ["Japan", "USA"]
					}
				}, buildQueryScopeExample({
					userGoal: "Compare grouped line values inside one selected subset.",
					params: {
						groupField: "Origin",
						valueField: "Miles_per_Gallon",
						groups: ["Japan", "USA"]
					},
					widgetRef: "wl://demo/workspace/main/widget/line_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/line_a/selection/current"
				})]
			})
		];
	}
	function registerLinePerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.detectAnomalies", { supportedWidgetKinds: ["line"] })) perceptionRegistry.register({
			name: "perception.detectAnomalies",
			supportedWidgetKinds: ["line"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "line" });
			const params = ctx.readCallParams();
			const { rows } = ctx.resolveRowsForWidget(targetWidget, params);
			const result = buildLineAnomalyResult({
				rows,
				rawSpec: targetWidget?.rawSpec || targetWidget?.spec || null,
				threshold: Number.isFinite(params?.threshold) ? Number(params.threshold) : 2,
				yField: params?.yField || null,
				xField: params?.xField || null
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result };
		}, { supportedWidgetKinds: ["line"] });
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["line"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["line"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "line" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["line"] });
		if (!perceptionRegistry.has("perception.compareGroups", { supportedWidgetKinds: ["line"] })) perceptionRegistry.register({
			name: "perception.compareGroups",
			supportedWidgetKinds: ["line"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "line" });
			const params = ctx.readCallParams();
			const comparison = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "compareGroups",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: comparison,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["line"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/line/humanInteraction.js
	function getLineHumanInteractionConfig() {
		return {
			mode: "categoryClick",
			actionName: "line.selectSeries",
			categoryFieldChannel: "color",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/line/state.js
	async function applyLineState(args) {
		return applyVegaLiteRuntimeState(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/lineWidgetAdapter.js
	var lineWidgetAdapter = createVegaLiteWidgetAdapter({
		kind: "line",
		async applyState(args) {
			return applyLineState(args);
		},
		buildActionDescriptors(args) {
			return buildLineActionDescriptors(args);
		},
		buildPerceptionDescriptors(args) {
			return buildLinePerceptionDescriptors(args);
		},
		getHumanInteractionConfig() {
			return getLineHumanInteractionConfig();
		},
		registerActions(actionExecutor) {
			return registerLineActions(actionExecutor);
		},
		registerPerceptionQueries(perceptionRegistry) {
			return registerLinePerceptionQueries(perceptionRegistry);
		}
	});
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/map/actions.js
	function buildMapActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
		return [makeActionDescriptor({
			name: "map.selectRegion",
			title: "Select map regions",
			description: "Select one or more geographic regions represented in the current map view.",
			primitive: "select",
			category: "selection",
			scope,
			supportedWidgetKinds: ["map"],
			targetRef: selectionRef || widgetRef,
			affectedRefs,
			affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "feedback"),
			paramsSchema: {
				type: "object",
				properties: {
					field: { type: "string" },
					values: {
						type: "array",
						items: {},
						minItems: 1
					}
				},
				required: ["field", "values"]
			},
			postconditions: [{ description: "The active selection should contain the requested geographic region values." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid map widget in the current workspace.",
				failureMessage: "map.selectRegion requires a valid map target widget."
			}],
			effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a categorical selection over map regions."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeHighlightEffect(ref, "Linked widgets may highlight or filter the selected regions."))],
			examples: [{
				userGoal: "Focus one or more regions before comparing downstream statistics.",
				params: {
					field: "State",
					values: ["California", "Texas"]
				}
			}],
			reversible: true
		})];
	}
	function registerMapActions(actionExecutor) {
		if (actionExecutor.has("map.selectRegion")) return;
		actionExecutor.register({ name: "map.selectRegion" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "map"
			});
			const field = typeof params.field === "string" ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("map.selectRegion requires a map target, field, and one or more values.");
			const { rows: visibleRows } = ctx.readRowsForWidget(targetWidget.ref);
			const matchedCount = visibleRows.filter((row) => values.includes(row?.[field])).length;
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "category",
					field,
					values,
					predicates: [{
						field,
						op: "in",
						value: values
					}],
					count: matchedCount,
					summary: `${field}: ${values.join(", ")}`
				}),
				selectedCount: matchedCount,
				verificationHints: ["Read the updated map selection state.", "Read linked widgets or visible rows to confirm region-level propagation."]
			});
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/map/humanInteraction.js
	function getMapHumanInteractionConfig() {
		return {
			mode: "categoryClick",
			actionName: "map.selectRegion",
			categoryFieldChannel: "color",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/map/perception.js
	function buildMapPerceptionDescriptors({ dataRef }) {
		return [makePerceptionDescriptor({
			name: "perception.compareGroups",
			title: "Compare groups",
			description: "Compare visible geographic groups in the current map view.",
			category: "compute",
			targetRef: dataRef,
			paramsSchema: {
				type: "object",
				properties: {
					groupField: { type: "string" },
					valueField: { type: "string" },
					groups: {
						type: "array",
						items: { type: "string" }
					},
					dataRef: { type: "string" }
				}
			},
			sideEffectFree: true,
			evidenceKinds: ["groupComparison", "geographicEvidence"],
			examples: [{
				userGoal: "Compare a few visible regions geographically.",
				params: {
					groupField: "State",
					valueField: "Population",
					groups: ["California", "Texas"]
				}
			}]
		})];
	}
	function registerMapPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.compareGroups", { supportedWidgetKinds: ["map"] })) perceptionRegistry.register({
			name: "perception.compareGroups",
			supportedWidgetKinds: ["map"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "map"
			});
			const comparison = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "compareGroups",
				spec: call?.params || {}
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: comparison,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, call?.params || {}).dataRef
			}) };
		}, { supportedWidgetKinds: ["map"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/map/state.js
	async function applyMapState(args) {
		return applyVegaLiteRuntimeState(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/mapWidgetAdapter.js
	var mapWidgetAdapter = createVegaLiteWidgetAdapter({
		kind: "map",
		async applyState(args) {
			return applyMapState(args);
		},
		buildActionDescriptors(args) {
			return buildMapActionDescriptors(args);
		},
		buildPerceptionDescriptors(args) {
			return buildMapPerceptionDescriptors(args);
		},
		getHumanInteractionConfig() {
			return getMapHumanInteractionConfig();
		},
		registerActions(actionExecutor) {
			return registerMapActions(actionExecutor);
		},
		registerPerceptionQueries(perceptionRegistry) {
			return registerMapPerceptionQueries(perceptionRegistry);
		}
	});
	//#endregion
	//#region ../../widgetva-kit/src/widgets/parallelCoordinates/actions.js
	function cloneValue$21(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function updateParallelXOrder(node, dimensionOrder) {
		if (!node || typeof node !== "object") return;
		if (Array.isArray(node)) {
			for (const entry of node) updateParallelXOrder(entry, dimensionOrder);
			return;
		}
		if (node.encoding && typeof node.encoding === "object") {
			const xEncoding = node.encoding.x;
			if (xEncoding && typeof xEncoding === "object" && [
				"dimension",
				"key",
				"variable"
			].includes(xEncoding.field)) node.encoding.x = {
				...xEncoding,
				sort: dimensionOrder,
				scale: {
					...xEncoding.scale || {},
					domain: dimensionOrder
				}
			};
		}
		for (const value of Object.values(node)) updateParallelXOrder(value, dimensionOrder);
	}
	function buildParallelCoordinatesActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
		const supportedWidgetKinds = ["parallelCoordinates"];
		return [
			makeActionDescriptor({
				name: "parallelCoordinates.brushAxes",
				title: "Brush parallel coordinate axes",
				description: "Select rows whose values fall inside one or more axis-aligned numeric ranges.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: { rules: {
						type: "array",
						minItems: 1,
						items: {
							type: "object",
							properties: {
								field: { type: "string" },
								range: {
									type: "array",
									items: { type: "number" },
									minItems: 2,
									maxItems: 2
								}
							},
							required: ["field", "range"]
						}
					} },
					required: ["rules"]
				},
				postconditions: [{ description: "The active selection should contain interval predicates for the requested axes." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid parallel coordinates widget in the current workspace.",
					failureMessage: "parallelCoordinates.brushAxes requires a valid parallel coordinates target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a multivariate interval selection over parallel axes."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeFilterEffect(ref, "Linked widgets may be filtered by the brushed multivariate range."))],
				examples: [{
					userGoal: "Restrict analysis to a multivariate value corridor.",
					params: { rules: [{
						field: "Horsepower",
						range: [80, 160]
					}, {
						field: "Weight_in_lbs",
						range: [1800, 3200]
					}] }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "parallelCoordinates.selectRecord",
				title: "Select one parallel-coordinate record",
				description: "Select one visible record by id so linked views can focus or compare that record without needing a brush gesture.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						recordId: { anyOf: [{ type: "string" }, { type: "number" }] },
						field: { type: "string" }
					},
					required: ["recordId"]
				},
				postconditions: [{ description: "The active selection should contain one equality predicate targeting the selected record id." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid parallel coordinates widget in the current workspace.",
					failureMessage: "parallelCoordinates.selectRecord requires a valid parallel coordinates target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a record-level selection anchored to one parallel-coordinates row."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeFilterEffect(ref, "Linked widgets may update to the selected record detail."))],
				examples: [{
					userGoal: "Select one car record in the parallel view before checking the same car across other linked views.",
					params: {
						field: "id",
						recordId: "toyota-corona-mark-ii"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "parallelCoordinates.reorderDimensions",
				title: "Reorder parallel-coordinate dimensions",
				description: "Reorder the visible dimension axes of a parallel coordinates view by rewriting the fold order and matching x-axis domain metadata.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { dimensionOrder: {
						type: "array",
						minItems: 1,
						items: { type: "string" }
					} },
					required: ["dimensionOrder"]
				},
				postconditions: [{ description: "The fold transform and any matching parallel-coordinate x-axis scale domain metadata should reflect the requested dimension order." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid parallel coordinates widget in the current workspace.",
					failureMessage: "parallelCoordinates.reorderDimensions requires a valid parallel coordinates target widget."
				}],
				examples: [{
					userGoal: "Move the most important dimensions to the front before comparing multivariate trends.",
					params: { dimensionOrder: [
						"Weight",
						"Horsepower",
						"Miles_per_Gallon"
					] }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "parallelCoordinates.filterDimension",
				title: "Filter a parallel-coordinate dimension",
				description: "Filter rows by a numeric range on one named dimension, inserting the predicate before the fold stage when the view is defined in wide format.",
				primitive: "filter",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						dimension: { type: "string" },
						range: {
							type: "array",
							minItems: 2,
							maxItems: 2,
							items: { type: "number" }
						}
					},
					required: ["dimension", "range"]
				},
				postconditions: [{ description: "The transform list should contain a numeric range predicate for the requested dimension, inserted ahead of the fold transform when present." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid parallel coordinates widget in the current workspace.",
					failureMessage: "parallelCoordinates.filterDimension requires a valid parallel coordinates target widget."
				}],
				examples: [{
					userGoal: "Keep only rows whose horsepower falls inside a specific corridor before comparing the remaining multivariate trajectories.",
					params: {
						dimension: "Horsepower",
						range: [80, 160]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "parallelCoordinates.filterByCategory",
				title: "Filter a parallel-coordinate category field",
				description: "Exclude rows whose category field matches one or more requested values, inserting the predicate before the fold stage when the view is defined in wide format.",
				primitive: "filter",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						field: { type: "string" },
						values: { oneOf: [{ type: "string" }, {
							type: "array",
							minItems: 1,
							items: { type: "string" }
						}] }
					},
					required: ["field", "values"]
				},
				postconditions: [{ description: "The transform list should contain a categorical exclusion predicate for the requested field, inserted ahead of the fold transform when present." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid parallel coordinates widget in the current workspace.",
					failureMessage: "parallelCoordinates.filterByCategory requires a valid parallel coordinates target widget."
				}],
				examples: [{
					userGoal: "Exclude one or more categories before comparing the remaining multivariate trajectories.",
					params: {
						field: "Origin",
						values: ["USA", "Japan"]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "parallelCoordinates.highlightCategory",
				title: "Highlight a parallel-coordinate category field",
				description: "Visually emphasize one or more category values by keeping matching trajectories fully opaque and dimming the rest.",
				primitive: "highlight",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						field: { type: "string" },
						values: { oneOf: [{ type: "string" }, {
							type: "array",
							minItems: 1,
							items: { type: "string" }
						}] }
					},
					required: ["field", "values"]
				},
				postconditions: [{ description: "The target line encoding should contain an opacity condition that keeps matching categories opaque and dims the rest." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid parallel coordinates widget in the current workspace.",
					failureMessage: "parallelCoordinates.highlightCategory requires a valid parallel coordinates target widget."
				}],
				examples: [{
					userGoal: "Highlight a few categories before comparing their multivariate trajectories against the background population.",
					params: {
						field: "Origin",
						values: ["USA", "Japan"]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "parallelCoordinates.hideDimensions",
				title: "Hide or restore parallel-coordinate dimensions",
				description: "Temporarily hide one or more dimensions from a parallel coordinates view while preserving the original full dimension order for later restoration.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						dimensions: {
							type: "array",
							minItems: 1,
							items: { type: "string" }
						},
						mode: {
							type: "string",
							enum: ["hide", "show"]
						}
					},
					required: ["dimensions"]
				},
				postconditions: [{ description: "The fold order and matching x-axis domain metadata should exclude hidden dimensions and preserve the original full dimension list in widget state." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid parallel coordinates widget in the current workspace.",
					failureMessage: "parallelCoordinates.hideDimensions requires a valid parallel coordinates target widget."
				}],
				examples: [{
					userGoal: "Temporarily hide weight from a crowded parallel-coordinates view.",
					params: { dimensions: ["Weight"] }
				}, {
					userGoal: "Restore one previously hidden dimension without resetting the full view.",
					params: {
						dimensions: ["Weight"],
						mode: "show"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "parallelCoordinates.resetHiddenDimensions",
				title: "Reset hidden parallel-coordinate dimensions",
				description: "Restore the full original dimension order after one or more dimensions have been hidden from a parallel coordinates view.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The full dimension list should be restored to the fold transform and matching x-axis domain metadata, and hidden-dimension widget state should be cleared." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid parallel coordinates widget in the current workspace.",
					failureMessage: "parallelCoordinates.resetHiddenDimensions requires a valid parallel coordinates target widget."
				}],
				examples: [{
					userGoal: "Restore every temporarily hidden dimension after a focused inspection pass.",
					params: {}
				}],
				reversible: true
			})
		];
	}
	function registerParallelCoordinatesActions(actionExecutor) {
		if (!actionExecutor.has("parallelCoordinates.brushAxes")) actionExecutor.register({ name: "parallelCoordinates.brushAxes" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "parallelCoordinates"
			});
			const rules = Array.isArray(params.rules) ? params.rules.filter((rule) => typeof rule?.field === "string" && Array.isArray(rule?.range) && rule.range.length === 2) : [];
			if (!targetWidget || rules.length === 0) throw new Error("parallelCoordinates.brushAxes requires a parallel coordinates target and at least one valid rule.");
			const { rows } = ctx.readRowsForWidget(targetWidget.ref);
			const normalizedRules = rules.map((rule) => ({
				field: rule.field,
				range: [Math.min(...rule.range), Math.max(...rule.range)]
			}));
			const filtered = rows.filter((row) => normalizedRules.every((rule) => {
				const value = row?.[rule.field];
				return typeof value === "number" && value >= rule.range[0] && value <= rule.range[1];
			}));
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "interval",
					fields: normalizedRules.map((rule) => rule.field),
					value: Object.fromEntries(normalizedRules.map((rule) => [rule.field, rule.range])),
					predicates: normalizedRules.map((rule) => ({
						field: rule.field,
						op: "between",
						value: rule.range
					})),
					count: filtered.length,
					summary: normalizedRules.map((rule) => `${rule.field} ${rule.range[0]}~${rule.range[1]}`).join("; ")
				}),
				selectedCount: filtered.length,
				verificationHints: ["Read the updated parallel coordinates selection state.", "Read linked widgets to confirm multivariate range propagation."]
			});
		});
		if (!actionExecutor.has("parallelCoordinates.reorderDimensions")) actionExecutor.register({ name: "parallelCoordinates.reorderDimensions" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "parallelCoordinates",
				message: "parallelCoordinates.reorderDimensions requires a valid parallel coordinates target widget."
			});
			const dimensionOrder = Array.isArray(params.dimensionOrder) ? params.dimensionOrder.filter((dimension) => typeof dimension === "string" && dimension.trim().length > 0) : [];
			if (!targetWidget || dimensionOrder.length === 0) throw new Error("parallelCoordinates.reorderDimensions requires a parallel coordinates target and a non-empty dimensionOrder.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for parallel-dimension reordering.");
					const nextSpec = cloneValue$21(spec);
					const transforms = Array.isArray(nextSpec.transform) ? nextSpec.transform : [];
					const foldIndex = transforms.findIndex((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
					if (foldIndex >= 0) {
						const currentFold = transforms[foldIndex].fold;
						const missing = dimensionOrder.filter((dimension) => !currentFold.includes(dimension));
						const extra = currentFold.filter((dimension) => !dimensionOrder.includes(dimension));
						if (missing.length > 0 || extra.length > 0) throw new Error("parallelCoordinates.reorderDimensions must provide a complete permutation of the current fold dimensions.");
						nextSpec.transform[foldIndex] = {
							...transforms[foldIndex],
							fold: dimensionOrder
						};
					}
					updateParallelXOrder(nextSpec, dimensionOrder);
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					dimensionOrder
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the fold order and x-axis domain now match the requested dimension ordering.", "Read the target widget view state to confirm the visible parallel-axis order was updated without changing the underlying data rows."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.selectRecord")) actionExecutor.register({ name: "parallelCoordinates.selectRecord" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "parallelCoordinates",
				message: "parallelCoordinates.selectRecord requires a valid parallel coordinates target widget."
			});
			const recordId = params.recordId;
			const field = typeof params.field === "string" && params.field.length > 0 ? params.field : "id";
			if (!targetWidget || recordId == null) throw new Error("parallelCoordinates.selectRecord requires a parallel coordinates target and a recordId.");
			const { rows } = ctx.readRowsForWidget(targetWidget.ref);
			const matchedRows = rows.filter((row) => row?.[field] === recordId);
			const summary = matchedRows[0]?.name ? `Record: ${matchedRows[0].name}` : `${field}: ${recordId}`;
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "category",
					field,
					values: [recordId],
					predicates: [{
						field,
						op: "equals",
						value: recordId
					}],
					count: matchedRows.length,
					summary
				}),
				selectedCount: matchedRows.length,
				verificationHints: ["Read the updated parallel-coordinates selection state.", "Read linked widgets to confirm the selected record became the current focus subset."]
			});
		});
		if (!actionExecutor.has("parallelCoordinates.filterDimension")) actionExecutor.register({ name: "parallelCoordinates.filterDimension" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "parallelCoordinates",
				message: "parallelCoordinates.filterDimension requires a valid parallel coordinates target widget."
			});
			const dimension = typeof params.dimension === "string" && params.dimension.trim().length > 0 ? params.dimension : null;
			const range = Array.isArray(params.range) && params.range.length === 2 ? params.range : null;
			if (!targetWidget || !dimension || !range || !range.every((value) => typeof value === "number" && Number.isFinite(value))) throw new Error("parallelCoordinates.filterDimension requires a parallel coordinates target, a dimension, and a two-number range.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for parallel-dimension filtering.");
					const nextSpec = cloneValue$21(spec);
					const transforms = Array.isArray(nextSpec.transform) ? [...nextSpec.transform] : [];
					const foldIndex = transforms.findIndex((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
					const [minValue, maxValue] = [Math.min(...range), Math.max(...range)];
					const filterTransform = {
						filter: {
							field: dimension,
							range: [minValue, maxValue]
						},
						_widgetvaTag: "parallelCoordinates.filterDimension"
					};
					const nextTransforms = transforms.filter((transform) => transform?._widgetvaTag !== "parallelCoordinates.filterDimension");
					if (foldIndex >= 0) nextTransforms.splice(foldIndex, 0, filterTransform);
					else nextTransforms.unshift(filterTransform);
					nextSpec.transform = nextTransforms;
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					dimension,
					range: [Math.min(...range), Math.max(...range)]
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the transform list now includes the requested numeric range filter ahead of the fold stage.", "Read the target widget rows to confirm only trajectories inside the requested dimension corridor remain visible."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.filterByCategory")) actionExecutor.register({ name: "parallelCoordinates.filterByCategory" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "parallelCoordinates",
				message: "parallelCoordinates.filterByCategory requires a valid parallel coordinates target widget."
			});
			const field = typeof params.field === "string" && params.field.trim().length > 0 ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => typeof value === "string" && value.length > 0) : typeof params.values === "string" && params.values.length > 0 ? [params.values] : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("parallelCoordinates.filterByCategory requires a parallel coordinates target, a field, and one or more category values.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for parallel-category filtering.");
					const nextSpec = cloneValue$21(spec);
					const transforms = Array.isArray(nextSpec.transform) ? [...nextSpec.transform] : [];
					const foldIndex = transforms.findIndex((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
					const filterTransform = {
						filter: {
							field,
							notOneOf: [...values]
						},
						_widgetvaTag: "parallelCoordinates.filterByCategory"
					};
					const nextTransforms = transforms.filter((transform) => transform?._widgetvaTag !== "parallelCoordinates.filterByCategory");
					if (foldIndex >= 0) nextTransforms.splice(foldIndex, 0, filterTransform);
					else nextTransforms.unshift(filterTransform);
					nextSpec.transform = nextTransforms;
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					field,
					values
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the transform list now includes the requested categorical exclusion filter ahead of the fold stage.", "Read the target widget rows to confirm trajectories belonging to the excluded categories no longer remain visible."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.highlightCategory")) actionExecutor.register({ name: "parallelCoordinates.highlightCategory" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "parallelCoordinates",
				message: "parallelCoordinates.highlightCategory requires a valid parallel coordinates target widget."
			});
			const field = typeof params.field === "string" && params.field.trim().length > 0 ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => typeof value === "string" && value.length > 0) : typeof params.values === "string" && params.values.length > 0 ? [params.values] : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("parallelCoordinates.highlightCategory requires a parallel coordinates target, a field, and one or more category values.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for parallel-category highlighting.");
					const nextSpec = cloneValue$21(spec);
					const opacityEncoding = {
						condition: {
							test: `indexof([${values.map((value) => JSON.stringify(value)).join(",")}], datum['${field}']) >= 0`,
							value: 1
						},
						value: .1
					};
					const lineLayer = Array.isArray(nextSpec.layer) ? nextSpec.layer.find((layer) => {
						const mark = layer?.mark;
						return mark === "line" || mark && typeof mark === "object" && mark.type === "line";
					}) : null;
					if (lineLayer && typeof lineLayer === "object") lineLayer.encoding = {
						...lineLayer.encoding || {},
						opacity: opacityEncoding
					};
					else nextSpec.encoding = {
						...nextSpec.encoding || {},
						opacity: opacityEncoding
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					field,
					values
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line encoding now contains an opacity condition for the requested category values.", "Read the target widget view state to confirm matching categories remain visually prominent while other trajectories are dimmed."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.hideDimensions")) actionExecutor.register({ name: "parallelCoordinates.hideDimensions" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "parallelCoordinates",
				message: "parallelCoordinates.hideDimensions requires a valid parallel coordinates target widget."
			});
			const dimensions = Array.isArray(params.dimensions) ? params.dimensions.filter((dimension) => typeof dimension === "string" && dimension.trim().length > 0) : [];
			const mode = params.mode === "show" ? "show" : "hide";
			if (!targetWidget || dimensions.length === 0) throw new Error("parallelCoordinates.hideDimensions requires a parallel coordinates target and at least one dimension.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for hiding parallel-coordinate dimensions.");
					const nextSpec = cloneValue$21(spec);
					const transforms = Array.isArray(nextSpec.transform) ? nextSpec.transform : [];
					const foldIndex = transforms.findIndex((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
					const storedState = nextSpec._pc_hidden_state && typeof nextSpec._pc_hidden_state === "object" ? nextSpec._pc_hidden_state : {};
					const allDimensions = Array.isArray(storedState.all_dimensions) && storedState.all_dimensions.length > 0 ? [...storedState.all_dimensions] : foldIndex >= 0 ? [...transforms[foldIndex].fold] : [];
					if (allDimensions.length === 0) throw new Error("parallelCoordinates.hideDimensions could not determine the full dimension list for the current view.");
					const hiddenSet = new Set(Array.isArray(storedState.hidden) ? storedState.hidden : []);
					for (const dimension of dimensions) {
						if (!allDimensions.includes(dimension)) throw new Error(`parallelCoordinates.hideDimensions cannot find dimension "${dimension}" in the current view.`);
						if (mode === "hide") hiddenSet.add(dimension);
						else hiddenSet.delete(dimension);
					}
					const visibleDimensions = allDimensions.filter((dimension) => !hiddenSet.has(dimension));
					if (visibleDimensions.length === 0) throw new Error("parallelCoordinates.hideDimensions must leave at least one visible dimension.");
					if (foldIndex >= 0) nextSpec.transform[foldIndex] = {
						...transforms[foldIndex],
						fold: visibleDimensions
					};
					updateParallelXOrder(nextSpec, visibleDimensions);
					nextSpec._pc_hidden_state = {
						hidden: allDimensions.filter((dimension) => hiddenSet.has(dimension)),
						all_dimensions: allDimensions
					};
					nextSpec._pc_reencode_state = {
						mode: "dimensionVisibility",
						sourceAction: "parallelCoordinates.hideDimensions",
						operation: mode,
						hidden_dimensions: allDimensions.filter((dimension) => hiddenSet.has(dimension)),
						visible_dimensions: visibleDimensions
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					dimensions,
					mode
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the visible fold order and x-axis domain no longer include the hidden dimensions.", "Read the target widget view state to confirm the original full dimension list remains stored for later restoration."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.resetHiddenDimensions")) actionExecutor.register({ name: "parallelCoordinates.resetHiddenDimensions" }, async (call, ctx) => {
			call?.params;
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "parallelCoordinates",
				message: "parallelCoordinates.resetHiddenDimensions requires a valid parallel coordinates target widget."
			});
			if (!targetWidget) throw new Error("parallelCoordinates.resetHiddenDimensions requires a valid parallel coordinates target widget.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for resetting hidden parallel-coordinate dimensions.");
					const nextSpec = cloneValue$21(spec);
					const hiddenState = nextSpec._pc_hidden_state && typeof nextSpec._pc_hidden_state === "object" ? nextSpec._pc_hidden_state : null;
					const allDimensions = Array.isArray(hiddenState?.all_dimensions) ? hiddenState.all_dimensions : [];
					if (allDimensions.length === 0) return nextSpec;
					const transforms = Array.isArray(nextSpec.transform) ? nextSpec.transform : [];
					const foldIndex = transforms.findIndex((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
					if (foldIndex >= 0) nextSpec.transform[foldIndex] = {
						...transforms[foldIndex],
						fold: [...allDimensions]
					};
					updateParallelXOrder(nextSpec, [...allDimensions]);
					nextSpec._pc_reencode_state = {
						mode: "dimensionVisibility",
						sourceAction: "parallelCoordinates.resetHiddenDimensions",
						operation: "reset",
						hidden_dimensions: [],
						visible_dimensions: [...allDimensions]
					};
					delete nextSpec._pc_hidden_state;
					return nextSpec;
				}),
				result: { widgetId: targetWidget.widgetId },
				verificationHints: ["Call perception.inspectViewConfig to verify the full original dimension order has been restored on the fold transform and x-axis domain metadata.", "Read the target widget view state to confirm the hidden-dimension bookkeeping state has been cleared."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/parallelCoordinates/perception.js
	function buildParallelCoordinatesPerceptionDescriptors({ dataRef }) {
		return [makePerceptionDescriptor({
			name: "perception.findOutliers",
			title: "Find outliers",
			description: appendQueryScopeGuidance("Return likely outlier rows in the visible parallel coordinates data."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				field: { type: "string" },
				zThreshold: { type: "number" },
				limit: { type: "number" }
			}),
			sideEffectFree: true,
			evidenceKinds: ["outlierEvidence", "multivariateEvidence"],
			examples: [{
				userGoal: "Find multivariate outliers in the visible parallel coordinates subset.",
				params: {
					field: "Horsepower",
					zThreshold: 2.5,
					limit: 10
				}
			}, buildQueryScopeExample({
				userGoal: "Find multivariate outliers inside one brushed parallel-coordinates subset.",
				params: {
					field: "Horsepower",
					zThreshold: 2.5,
					limit: 10
				},
				widgetRef: "wl://demo/workspace/main/widget/parallel_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/parallel_a/selection/brush"
			})]
		})];
	}
	function registerParallelCoordinatesPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.findOutliers", { supportedWidgetKinds: ["parallelCoordinates"] })) perceptionRegistry.register({
			name: "perception.findOutliers",
			supportedWidgetKinds: ["parallelCoordinates"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "parallelCoordinates" });
			const params = ctx.readCallParams();
			const outliers = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findOutliers",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: outliers,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["parallelCoordinates"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/parallelCoordinates/humanInteraction.js
	function getParallelCoordinatesHumanInteractionConfig() {
		return {
			mode: "multiBrush",
			actionName: "parallelCoordinates.brushAxes",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/parallelCoordinates/state.js
	async function applyParallelCoordinatesState(args) {
		return applyVegaLiteRuntimeState(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/parallelCoordinatesWidgetAdapter.js
	var parallelCoordinatesWidgetAdapter = createVegaLiteWidgetAdapter({
		kind: "parallelCoordinates",
		async applyState(args) {
			return applyParallelCoordinatesState(args);
		},
		buildActionDescriptors(args) {
			return buildParallelCoordinatesActionDescriptors(args);
		},
		buildPerceptionDescriptors(args) {
			return buildParallelCoordinatesPerceptionDescriptors(args);
		},
		getHumanInteractionConfig() {
			return getParallelCoordinatesHumanInteractionConfig();
		},
		registerActions(actionExecutor) {
			return registerParallelCoordinatesActions(actionExecutor);
		},
		registerPerceptionQueries(perceptionRegistry) {
			return registerParallelCoordinatesPerceptionQueries(perceptionRegistry);
		}
	});
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/selectionStateShape.js
	function cloneValue$20(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeScalarArray$1(values) {
		if (!Array.isArray(values)) return void 0;
		const normalized = values.filter((value) => typeof value === "string" || typeof value === "number");
		return normalized.length > 0 ? normalized : void 0;
	}
	function normalizeFieldArray(fields) {
		if (!Array.isArray(fields)) return void 0;
		const normalized = fields.filter((field) => typeof field === "string" && field.length > 0);
		return normalized.length > 0 ? normalized : void 0;
	}
	function normalizeString$5(value) {
		return typeof value === "string" && value.length > 0 ? value : void 0;
	}
	function buildSelectionStateInput(selection) {
		if (!selection || typeof selection !== "object") return null;
		const nextState = {
			selectionRef: normalizeString$5(selection.selection_ref) || normalizeString$5(selection.selectionRef),
			selectionId: normalizeString$5(selection.selection_id) || normalizeString$5(selection.selectionId),
			kind: selection.selection_type || selection.kind || "interval",
			sourceWidgetRef: normalizeString$5(selection.source_widget_ref) || normalizeString$5(selection.sourceWidgetRef),
			sourceWidgetId: normalizeString$5(selection.source_widget_id) || normalizeString$5(selection.sourceWidgetId),
			scope: normalizeString$5(selection.scope) || "local",
			selectionDataRef: normalizeString$5(selection.selection_data_ref) || normalizeString$5(selection.selectionDataRef),
			domain: selection.domain || null,
			predicates: Array.isArray(selection.predicates) ? cloneValue$20(selection.predicates) : [],
			summary: typeof selection.summary === "string" ? selection.summary : ""
		};
		if (typeof selection.aggregateName === "string" && selection.aggregateName.length > 0) nextState.aggregateName = selection.aggregateName;
		const fields = normalizeFieldArray(selection.fields);
		if (fields) nextState.fields = fields;
		if (selection.value && typeof selection.value === "object" && !Array.isArray(selection.value)) nextState.value = cloneValue$20(selection.value);
		if (typeof selection.keyField === "string" && selection.keyField.length > 0) nextState.keyField = selection.keyField;
		const keys = normalizeScalarArray$1(selection.keys);
		if (keys) nextState.keys = keys;
		if (typeof selection.field === "string" && selection.field.length > 0) nextState.field = selection.field;
		const values = normalizeScalarArray$1(selection.values);
		if (values) nextState.values = values;
		return nextState;
	}
	function buildSelectionPayloadFromState({ selectionRef, selectionState, sourceWidgetId, selectedCount = 0 }) {
		if (!selectionState) return null;
		const normalizedSelectionRef = typeof selectionRef === "string" ? selectionRef : selectionState.selectionRef || null;
		const normalizedSelectionId = selectionState.selectionId || (typeof normalizedSelectionRef === "string" ? normalizedSelectionRef.split("/").pop() || null : null) || `sel_${Date.now()}`;
		const normalizedSourceWidgetId = sourceWidgetId || selectionState.sourceWidgetId || void 0;
		const payload = {
			selection_ref: normalizedSelectionRef || void 0,
			selection_id: normalizedSelectionId,
			source_widget_ref: selectionState.sourceWidgetRef || void 0,
			source_widget_id: normalizedSourceWidgetId,
			selection_type: selectionState.kind || "interval",
			scope: selectionState.scope || "local",
			selection_data_ref: selectionState.selectionDataRef || void 0,
			domain: selectionState.domain || null,
			predicates: Array.isArray(selectionState.predicates) ? cloneValue$20(selectionState.predicates) : [],
			count: selectedCount,
			summary: selectionState.summary || ""
		};
		if (typeof selectionState.aggregateName === "string" && selectionState.aggregateName.length > 0) payload.aggregateName = selectionState.aggregateName;
		const fields = normalizeFieldArray(selectionState.fields);
		if (fields) payload.fields = fields;
		if (selectionState.value && typeof selectionState.value === "object" && !Array.isArray(selectionState.value) && Object.keys(selectionState.value).length > 0) payload.value = cloneValue$20(selectionState.value);
		if (typeof selectionState.keyField === "string" && selectionState.keyField.length > 0) payload.keyField = selectionState.keyField;
		const keys = normalizeScalarArray$1(selectionState.keys);
		if (keys) payload.keys = keys;
		if (typeof selectionState.field === "string" && selectionState.field.length > 0) payload.field = selectionState.field;
		const values = normalizeScalarArray$1(selectionState.values);
		if (values) payload.values = values;
		return payload;
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/scatter/actions.js
	function replaceTaggedLayer(layers, tag, nextLayer) {
		const nextLayers = (Array.isArray(layers) ? layers : []).filter((layer) => layer?._widgetvaTag !== tag);
		return nextLayer ? [...nextLayers, nextLayer] : nextLayers;
	}
	function euclideanDistanceSquared(a, b) {
		const dx = a[0] - b[0];
		const dy = a[1] - b[1];
		return dx * dx + dy * dy;
	}
	function assignPointsToCenters(points, centers) {
		return points.map((point) => {
			let bestIndex = 0;
			let bestDistance = Number.POSITIVE_INFINITY;
			centers.forEach((center, index) => {
				const distance = euclideanDistanceSquared(point, center);
				if (distance < bestDistance) {
					bestDistance = distance;
					bestIndex = index;
				}
			});
			return bestIndex;
		});
	}
	function recomputeCenters(points, labels, clusterCount, previousCenters) {
		return Array.from({ length: clusterCount }, (_, clusterIndex) => {
			const clusterPoints = points.filter((_, pointIndex) => labels[pointIndex] === clusterIndex);
			if (!clusterPoints.length) return previousCenters[clusterIndex];
			const sums = clusterPoints.reduce((acc, point) => [acc[0] + point[0], acc[1] + point[1]], [0, 0]);
			return [sums[0] / clusterPoints.length, sums[1] / clusterPoints.length];
		});
	}
	function runKMeans(points, clusterCount, maxIterations = 25) {
		const safeClusterCount = Math.max(1, Math.min(clusterCount, points.length));
		let centers = points.slice(0, safeClusterCount).map((point) => [...point]);
		let labels = assignPointsToCenters(points, centers);
		for (let iteration = 0; iteration < maxIterations; iteration += 1) {
			const nextCenters = recomputeCenters(points, labels, safeClusterCount, centers);
			const nextLabels = assignPointsToCenters(points, nextCenters);
			const unchanged = nextLabels.every((label, index) => label === labels[index]);
			centers = nextCenters;
			labels = nextLabels;
			if (unchanged) break;
		}
		return {
			labels,
			centers
		};
	}
	function hasInlineObjectRows(spec) {
		return Array.isArray(spec?.data?.values) && spec.data.values.some((row) => row && typeof row === "object");
	}
	function isUrlBackedDataSpec(spec) {
		return typeof spec?.data?.url === "string" && spec.data.url.trim().length > 0;
	}
	function buildScatterActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
		const supportedWidgetKinds = ["scatter"];
		return [
			makeActionDescriptor({
				name: "scatter.brushRegion",
				title: "Brush scatterplot region",
				description: "Select points inside a data-space rectangle on the scatterplot.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						xField: { type: "string" },
						yField: { type: "string" },
						xRange: {
							type: "array",
							items: { type: "number" },
							minItems: 2,
							maxItems: 2
						},
						yRange: {
							type: "array",
							items: { type: "number" },
							minItems: 2,
							maxItems: 2
						}
					},
					required: [
						"xField",
						"yField",
						"xRange",
						"yRange"
					]
				},
				postconditions: [{ description: "The active selection should contain an interval over the x and y fields." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid scatter widget in the current workspace.",
					failureMessage: "scatter.brushRegion requires a valid scatter target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates an interval selection on the scatterplot."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeFilterEffect(ref, "Linked widgets may be filtered or highlighted by the scatter selection."))],
				examples: [{
					userGoal: "Brush a region of interest in the scatterplot.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon",
						xRange: [80, 160],
						yRange: [20, 35]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "scatter.zoomDomain",
				title: "Zoom scatterplot domain",
				description: "Zoom the scatterplot to a specific x and/or y domain.",
				primitive: "zoom",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "transforms"),
				paramsSchema: {
					type: "object",
					properties: {
						xDomain: {
							type: "array",
							minItems: 2,
							maxItems: 2,
							items: { anyOf: [{ type: "number" }, { type: "string" }] }
						},
						yDomain: {
							type: "array",
							minItems: 2,
							maxItems: 2,
							items: { anyOf: [{ type: "number" }, { type: "string" }] }
						}
					}
				},
				postconditions: [{ description: "The scatterplot view should reflect the requested x and/or y domain." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid scatter widget in the current workspace.",
					failureMessage: "scatter.zoomDomain requires a valid scatter target widget."
				}],
				examples: [{
					userGoal: "Zoom into the high-risk cluster region of the scatterplot.",
					params: {
						xDomain: [80, 160],
						yDomain: [20, 35]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "scatter.identifyClusters",
				title: "Identify scatter clusters",
				description: "Cluster visible scatter points in the frontend and recolor the scatterplot by the derived cluster labels.",
				primitive: "annotate",
				category: "compute",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						nClusters: { type: "number" },
						method: { type: "string" }
					}
				},
				postconditions: [{ description: "The scatterplot data should include a derived cluster field and the color encoding should point to it." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid scatter widget with quantitative x/y encodings and enough visible rows.",
					failureMessage: "scatter.identifyClusters requires a valid scatter target widget with enough visible numeric points."
				}],
				examples: [{
					userGoal: "Color the visible scatter points by their inferred clusters.",
					params: {
						nClusters: 3,
						method: "kmeans"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "scatter.showRegression",
				title: "Overlay scatter regression",
				description: "Add or replace a regression-line overlay on the current scatterplot using the active x/y encodings.",
				primitive: "annotate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { method: { type: "string" } }
				},
				postconditions: [{ description: "A tagged regression overlay layer should be present and should use the current scatter x/y fields." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid scatter widget in the current workspace.",
					failureMessage: "scatter.showRegression requires a valid scatter target widget."
				}],
				examples: [{
					userGoal: "Overlay a regression line on the scatterplot to inspect the overall trend.",
					params: { method: "linear" }
				}],
				reversible: true
			})
		];
	}
	function registerScatterActions(actionExecutor) {
		if (!actionExecutor.has("scatter.brushRegion")) actionExecutor.register({ name: "scatter.brushRegion" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "scatter"
			});
			const xRange = Array.isArray(params.xRange) ? params.xRange : null;
			const yRange = Array.isArray(params.yRange) ? params.yRange : null;
			if (!targetWidget || !params.xField || !params.yField || !xRange || !yRange) throw new Error("scatter.brushRegion requires a scatter target, xField, yField, xRange, and yRange.");
			const { rows } = ctx.readRowsForWidget(targetWidget.ref);
			const [xMin, xMax] = [Math.min(...xRange), Math.max(...xRange)];
			const [yMin, yMax] = [Math.min(...yRange), Math.max(...yRange)];
			const filtered = rows.filter((row) => {
				const x = row?.[params.xField];
				const y = row?.[params.yField];
				return typeof x === "number" && typeof y === "number" && x >= xMin && x <= xMax && y >= yMin && y <= yMax;
			});
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "interval",
					fields: [params.xField, params.yField],
					value: {
						[params.xField]: [xMin, xMax],
						[params.yField]: [yMin, yMax]
					},
					domain: {
						xDomain: [xMin, xMax],
						yDomain: [yMin, yMax]
					},
					predicates: [{
						field: params.xField,
						op: "between",
						value: [xMin, xMax]
					}, {
						field: params.yField,
						op: "between",
						value: [yMin, yMax]
					}],
					count: filtered.length,
					summary: `${params.xField} ${xMin}~${xMax}; ${params.yField} ${yMin}~${yMax}`
				}),
				selectedCount: filtered.length,
				verificationHints: ["Read the updated widget selection state.", "Call perception.summarizeSelection to confirm the selected count."]
			});
		});
		if (!actionExecutor.has("scatter.zoomDomain")) actionExecutor.register({ name: "scatter.zoomDomain" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "scatter",
				message: "scatter.zoomDomain requires a valid scatter target widget."
			});
			const xDomain = Array.isArray(params.xDomain) ? params.xDomain : null;
			const yDomain = Array.isArray(params.yDomain) ? params.yDomain : null;
			if (!targetWidget || !xDomain && !yDomain) throw new Error("scatter.zoomDomain requires a scatter target and at least one domain range.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for scatter domain updates.");
					const nextEncoding = { ...spec.encoding || {} };
					if (xDomain && nextEncoding.x) nextEncoding.x = {
						...nextEncoding.x,
						scale: {
							...nextEncoding.x.scale || {},
							domain: xDomain
						}
					};
					if (yDomain && nextEncoding.y) nextEncoding.y = {
						...nextEncoding.y,
						scale: {
							...nextEncoding.y.scale || {},
							domain: yDomain
						}
					};
					const nextMark = typeof spec.mark === "string" ? {
						type: spec.mark,
						clip: true
					} : {
						...spec.mark || {},
						clip: true
					};
					return {
						...spec,
						mark: nextMark,
						encoding: nextEncoding
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					...xDomain ? { xDomain } : {},
					...yDomain ? { yDomain } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the scatterplot domain was updated.", "Read the target widget view state to confirm the new x/y domain values."]
			};
		});
		if (!actionExecutor.has("scatter.identifyClusters")) actionExecutor.register({ name: "scatter.identifyClusters" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "scatter",
				message: "scatter.identifyClusters requires a valid scatter target widget."
			});
			const method = typeof params.method === "string" && params.method.trim().length > 0 ? params.method.trim().toLowerCase() : "kmeans";
			const requestedClusters = Number.isFinite(params.nClusters) ? Math.floor(params.nClusters) : 3;
			const nClusters = Math.max(1, requestedClusters);
			if (method !== "kmeans") throw new Error(`scatter.identifyClusters currently only supports the kmeans method. Received: ${method}.`);
			const currentSpec = ctx.readCurrentSpec();
			const { rows: visibleRows } = ctx.readRowsForWidget(targetWidget.ref);
			const sourceRows = Array.isArray(visibleRows) && visibleRows.length > 0 ? visibleRows : Array.isArray(currentSpec?.data?.values) ? currentSpec.data.values : [];
			const rootEncoding = currentSpec?.layer?.[0]?.encoding || currentSpec?.encoding || {};
			const xField = rootEncoding?.x?.field || null;
			const yField = rootEncoding?.y?.field || null;
			if (!xField || !yField) throw new Error("scatter.identifyClusters requires x and y encodings on the active scatter spec.");
			const points = [];
			const validSourceIndices = [];
			sourceRows.forEach((row, rowIndex) => {
				const x = row?.[xField];
				const y = row?.[yField];
				if (typeof x === "number" && Number.isFinite(x) && typeof y === "number" && Number.isFinite(y)) {
					points.push([x, y]);
					validSourceIndices.push(rowIndex);
				}
			});
			if (points.length < nClusters) throw new Error(`scatter.identifyClusters requires at least ${nClusters} visible numeric points.`);
			const { labels, centers } = runKMeans(points, nClusters);
			const clusterField = `cluster_${nClusters}`;
			const clusterStatistics = centers.map((center, clusterId) => ({
				cluster_id: clusterId,
				size: labels.filter((label) => label === clusterId).length,
				center
			}));
			const sourceIndexToCluster = /* @__PURE__ */ new Map();
			validSourceIndices.forEach((rowIndex, index) => {
				sourceIndexToCluster.set(rowIndex, labels[index]);
			});
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for scatter cluster identification.");
					const nextSpec = { ...spec };
					const nextValues = (hasInlineObjectRows(spec) ? spec.data.values : isUrlBackedDataSpec(spec) ? sourceRows : []).map((row, rowIndex) => {
						const nextRow = { ...row };
						if (sourceIndexToCluster.has(rowIndex)) nextRow[clusterField] = sourceIndexToCluster.get(rowIndex);
						return nextRow;
					});
					nextSpec.data = {
						...spec.data || {},
						values: nextValues
					};
					const nextEncoding = { ...spec.encoding || {} };
					nextEncoding.color = {
						field: clusterField,
						type: "nominal",
						scale: { scheme: "category10" },
						legend: { title: "Cluster" }
					};
					nextSpec.encoding = nextEncoding;
					nextSpec._scatter_cluster_state = {
						method,
						cluster_field: clusterField,
						n_clusters: nClusters,
						centers
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					method,
					nClusters,
					clusterField,
					clusterStatistics,
					message: `Identified ${nClusters} clusters`
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the scatter color encoding now points to the derived cluster field.", "Read the target widget spec data values to confirm visible rows received cluster labels."]
			};
		});
		if (!actionExecutor.has("scatter.showRegression")) actionExecutor.register({ name: "scatter.showRegression" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "scatter",
				message: "scatter.showRegression requires a valid scatter target widget."
			});
			const method = typeof params.method === "string" && params.method.trim().length > 0 ? params.method.trim().toLowerCase() : "linear";
			if (!targetWidget) throw new Error("scatter.showRegression requires a valid scatter target widget.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for scatter regression overlays.");
					const rootEncoding = spec?.layer?.[0]?.encoding || spec?.encoding || {};
					const xField = rootEncoding?.x?.field || null;
					const yField = rootEncoding?.y?.field || null;
					if (!xField || !yField) throw new Error("scatter.showRegression requires x and y encodings on the active scatter spec.");
					const regressionTransform = {
						regression: yField,
						on: xField
					};
					if (method === "poly") {
						regressionTransform.method = "poly";
						regressionTransform.order = 3;
					} else if (method === "quad") {
						regressionTransform.method = "poly";
						regressionTransform.order = 2;
					} else if (method === "log" || method === "exp") regressionTransform.method = method;
					else regressionTransform.method = "linear";
					const regressionLayer = {
						_widgetvaTag: "scatter.showRegression",
						mark: {
							type: "line",
							color: "red",
							strokeWidth: 2
						},
						transform: [regressionTransform],
						encoding: {
							x: {
								field: xField,
								type: "quantitative"
							},
							y: {
								field: yField,
								type: "quantitative"
							}
						}
					};
					if (Array.isArray(spec.layer) && spec.layer.length > 0) return {
						...spec,
						layer: replaceTaggedLayer(spec.layer, "scatter.showRegression", regressionLayer)
					};
					const nextSpec = { ...spec };
					const baseLayer = {
						mark: spec.mark || "point",
						encoding: spec.encoding || {}
					};
					delete nextSpec.mark;
					delete nextSpec.encoding;
					return {
						...nextSpec,
						layer: replaceTaggedLayer([baseLayer], "scatter.showRegression", regressionLayer)
					};
				}),
				result: {
					widgetId: targetWidget.widgetId,
					method,
					overlay: "regression"
				},
				verificationHints: ["Call perception.inspectViewConfig to verify a tagged regression overlay layer was added to the scatterplot.", "Read the target widget view state to confirm the regression transform uses the current x/y fields and requested method."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/scatter/perception.js
	function buildScatterPerceptionDescriptors({ dataRef }) {
		return [
			makePerceptionDescriptor({
				name: "perception.computeCorrelation",
				title: "Compute correlation",
				description: appendQueryScopeGuidance("Compute a correlation coefficient over the visible rows of the target scatter widget."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					xField: { type: "string" },
					yField: { type: "string" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["statisticalEvidence", "correlationEvidence"],
				examples: [{
					userGoal: "Estimate the relationship between two numeric fields in the scatterplot.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon"
					}
				}, buildQueryScopeExample({
					userGoal: "Estimate the correlation inside one brushed scatter subset.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon"
					},
					widgetRef: "wl://demo/workspace/main/widget/scatter_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/scatter_a/selection/brush"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findOutliers",
				title: "Find outliers",
				description: appendQueryScopeGuidance("Return likely outlier rows in the visible scatter data by numeric fields."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					field: { type: "string" },
					xField: { type: "string" },
					yField: { type: "string" },
					zThreshold: { type: "number" },
					limit: { type: "number" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["outlierEvidence", "rowEvidence"],
				examples: [{
					userGoal: "Identify candidate outliers in the visible scatter subset.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon",
						limit: 5
					}
				}, buildQueryScopeExample({
					userGoal: "Identify outliers inside one brushed scatter subset.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon",
						limit: 5
					},
					widgetRef: "wl://demo/workspace/main/widget/scatter_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/scatter_a/selection/brush"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findExtremes",
				title: "Find extremes",
				description: appendQueryScopeGuidance("Return top-k or bottom-k visible rows by a numeric field."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					field: { type: "string" },
					direction: { type: "string" },
					limit: { type: "number" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["rankEvidence", "rowEvidence"],
				examples: [{
					userGoal: "Retrieve the highest or lowest visible rows by a metric.",
					params: {
						field: "Horsepower",
						direction: "max",
						limit: 5
					}
				}, buildQueryScopeExample({
					userGoal: "Retrieve extremes inside one brushed scatter subset.",
					params: {
						field: "Horsepower",
						direction: "max",
						limit: 5
					},
					widgetRef: "wl://demo/workspace/main/widget/scatter_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/scatter_a/selection/brush"
				})]
			})
		];
	}
	function registerScatterPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.computeCorrelation", { supportedWidgetKinds: ["scatter"] })) perceptionRegistry.register({
			name: "perception.computeCorrelation",
			supportedWidgetKinds: ["scatter"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "scatter" });
			const params = ctx.readCallParams();
			const correlation = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "computeCorrelation",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: correlation,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["scatter"] });
		if (!perceptionRegistry.has("perception.findOutliers", { supportedWidgetKinds: ["scatter"] })) perceptionRegistry.register({
			name: "perception.findOutliers",
			supportedWidgetKinds: ["scatter"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "scatter" });
			const params = ctx.readCallParams();
			const outliers = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findOutliers",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: outliers,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["scatter"] });
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["scatter"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["scatter"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "scatter" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["scatter"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/sankey/actions.js
	function cloneValue$19(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function findNamedDataSource$2(spec, name) {
		const data = Array.isArray(spec?.data) ? spec.data : [];
		const index = data.findIndex((entry) => entry && typeof entry === "object" && entry.name === name);
		if (index < 0) return {
			index: -1,
			values: null
		};
		return {
			index,
			values: Array.isArray(data[index]?.values) ? data[index].values : null
		};
	}
	function findNamedMark(spec, name) {
		const marks = Array.isArray(spec?.marks) ? spec.marks : [];
		for (const mark of marks) {
			if (mark?.name === name) return mark;
			if (mark?.type === "group" && Array.isArray(mark.marks)) {
				const nested = mark.marks.find((entry) => entry?.name === name);
				if (nested) return nested;
			}
		}
		return null;
	}
	function findNamedSignal(spec, name) {
		const signals = Array.isArray(spec?.signals) ? spec.signals : [];
		const index = signals.findIndex((entry) => entry && typeof entry === "object" && entry.name === name);
		if (index < 0) return {
			index: -1,
			signal: null
		};
		return {
			index,
			signal: signals[index]
		};
	}
	function computeNodeFlows$1(links) {
		const flows = /* @__PURE__ */ new Map();
		const ensure = (name) => {
			if (!flows.has(name)) flows.set(name, {
				inflow: 0,
				outflow: 0,
				total: 0
			});
			return flows.get(name);
		};
		for (const link of Array.isArray(links) ? links : []) {
			if (!link || typeof link !== "object") continue;
			const source = link.source;
			const target = link.target;
			const value = Number(link.value || 0);
			const sourceEntry = ensure(source);
			const targetEntry = ensure(target);
			sourceEntry.outflow += value;
			targetEntry.inflow += value;
		}
		for (const entry of flows.values()) entry.total = Math.max(entry.inflow, entry.outflow);
		return flows;
	}
	function buildSankeyActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
		const supportedWidgetKinds = ["sankey"];
		return [
			makeActionDescriptor({
				name: "sankey.focusFlow",
				title: "Focus Sankey flow",
				description: "Focus one or more flow categories or nodes in the current Sankey view.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						field: { type: "string" },
						values: {
							type: "array",
							items: {},
							minItems: 1
						}
					},
					required: ["field", "values"]
				},
				postconditions: [{ description: "The active selection should contain the requested flow or node categories." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.focusFlow requires a valid sankey target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates a categorical selection over Sankey flow nodes or links."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeHighlightEffect(ref, "Linked widgets may highlight or filter the selected flow categories."))],
				examples: [{
					userGoal: "Focus a subset of flows before investigating bottlenecks.",
					params: {
						field: "source",
						values: ["A"]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.selectAggregateNode",
				title: "Select a collapsed Sankey aggregate node",
				description: "Select one collapsed aggregate node by aggregate name so downstream context can focus that temporary group even when it does not map to row-level predicates.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: selectionRef || widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "selections" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { aggregateName: { type: "string" } },
					required: ["aggregateName"]
				},
				postconditions: [{ description: "The active selection should preserve the requested aggregateName even when no row-level predicates are available." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.selectAggregateNode requires a valid sankey target widget."
				}],
				effects: [makeSelectionEffect(selectionRef || widgetRef, "Creates or updates an aggregate-node selection over one collapsed Sankey group."), ...affectedRefs.filter((ref) => ref !== widgetRef).map((ref) => makeHighlightEffect(ref, "Linked widgets may use the selected Sankey aggregate as a focused comparison context."))],
				examples: [{
					userGoal: "Hold one collapsed aggregate group as the current focus before deciding whether to re-expand it.",
					params: { aggregateName: "collapsed:1:other" }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.filterFlow",
				title: "Filter Sankey flow by threshold",
				description: "Keep only links at or above a minimum flow value, preferably by updating the Sankey threshold signal when one exists.",
				primitive: "filter",
				category: "dataTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "transforms" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { minValue: { type: "number" } },
					required: ["minValue"]
				},
				postconditions: [{ description: "The Sankey threshold signal should reflect the requested minimum flow, or rawLinks/nodeConfig should be filtered to links at or above it." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace and the current Sankey view has rawLinks.",
					failureMessage: "sankey.filterFlow requires a valid sankey target widget with rawLinks."
				}],
				examples: [{
					userGoal: "Hide tiny flows so the main pathways stand out more clearly.",
					params: { minValue: 20 }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.collapseNodes",
				title: "Collapse Sankey nodes",
				description: "Collapse multiple nodes into one aggregate node by rewriting the raw link list and node configuration in the current Sankey view.",
				primitive: "aggregate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						nodes: {
							type: "array",
							minItems: 1,
							items: { type: "string" }
						},
						aggregateName: { type: "string" }
					},
					required: ["nodes"]
				},
				postconditions: [{ description: "The requested nodes should be replaced by one aggregate node, and rawLinks/nodeConfig should reflect the new topology." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.collapseNodes requires a valid sankey target widget."
				}],
				examples: [{
					userGoal: "Merge several low-signal source nodes into one aggregate before comparing downstream flow structure.",
					params: {
						nodes: ["A", "B"],
						aggregateName: "Other Sources"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.expandNode",
				title: "Expand a collapsed Sankey node",
				description: "Restore the original nodes and links for one previously collapsed aggregate node using the saved Sankey structural state.",
				primitive: "navigate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { aggregateName: { type: "string" } },
					required: ["aggregateName"]
				},
				postconditions: [{ description: "The aggregate node should be replaced by its original nodes and links, and the saved collapsed-group entry should be removed." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.expandNode requires a valid sankey target widget."
				}],
				examples: [{
					userGoal: "Re-expand one aggregate group after an earlier structural simplification pass.",
					params: { aggregateName: "Other Sources" }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.highlightPath",
				title: "Highlight a Sankey path",
				description: "Visually emphasize a multi-step path by increasing opacity for edges and nodes on the path and dimming unrelated structure.",
				primitive: "highlight",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { path: { oneOf: [{ type: "string" }, {
						type: "array",
						minItems: 2,
						items: { type: "string" }
					}] } },
					required: ["path"]
				},
				postconditions: [{ description: "The edge and node marks should contain path-sensitive opacity/stroke updates that emphasize the requested path and dim unrelated structure." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.highlightPath requires a valid sankey target widget."
				}],
				examples: [{
					userGoal: "Trace one conversion route through the Sankey graph while dimming everything else.",
					params: { path: [
						"A",
						"B",
						"C"
					] }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.traceNode",
				title: "Trace Sankey node connections",
				description: "Highlight all edges directly connected to one node and visually emphasize that node while dimming unrelated structure.",
				primitive: "focus",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { nodeName: { type: "string" } },
					required: ["nodeName"]
				},
				postconditions: [{ description: "Edges touching the requested node should remain prominent while unrelated edges and nodes are dimmed, or the selectedNode signal should be updated when the provider exposes it." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.traceNode requires a valid sankey target widget."
				}],
				examples: [{
					userGoal: "Trace all direct inflows and outflows for one node before deciding whether to collapse or reorder the layer.",
					params: { nodeName: "Checkout" }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.colorFlows",
				title: "Color Sankey flows connected to nodes",
				description: "Recolor all edges directly connected to one or more nodes while leaving the unrelated edge color encoding as a fallback.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						nodes: {
							type: "array",
							minItems: 1,
							items: { type: "string" }
						},
						color: { type: "string" }
					},
					required: ["nodes"]
				},
				postconditions: [{ description: "Edges connected to the requested nodes should be recolored while unrelated edges still resolve through the prior fill encoding or a fallback color scale expression." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.colorFlows requires a valid sankey target widget."
				}],
				examples: [{
					userGoal: "Color all flows touching one or more nodes before presenting a focused Sankey story.",
					params: {
						nodes: ["Checkout"],
						color: "#e74c3c"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.reorderNodesInLayer",
				title: "Reorder Sankey nodes in one layer",
				description: "Rewrite node order values for one Sankey depth layer using an explicit top-to-bottom node order.",
				primitive: "reencode",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: {
						depth: { type: "number" },
						order: {
							type: "array",
							minItems: 1,
							items: { type: "string" }
						}
					},
					required: ["depth", "order"]
				},
				postconditions: [{ description: "The nodeConfig entries at the requested depth should have updated order values reflecting the requested top-to-bottom sequence." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.reorderNodesInLayer requires a valid sankey target widget."
				}],
				examples: [{
					userGoal: "Reorder one Sankey layer to make important nodes appear first from top to bottom.",
					params: {
						depth: 0,
						order: [
							"C",
							"A",
							"B"
						]
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "sankey.autoCollapseByRank",
				title: "Auto-collapse Sankey nodes by rank",
				description: "Keep only the top-N nodes per Sankey layer by flow volume and collapse the remainder into layer-specific aggregate nodes.",
				primitive: "aggregate",
				category: "viewTransform",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.map((ref) => ref === widgetRef ? "view" : "feedback"),
				paramsSchema: {
					type: "object",
					properties: { topN: { type: "number" } },
					required: ["topN"]
				},
				postconditions: [{ description: "Each layer should keep only the top-N nodes by total flow while lower-ranked nodes are replaced by per-layer aggregate nodes in rawLinks/nodeConfig." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid sankey widget in the current workspace.",
					failureMessage: "sankey.autoCollapseByRank requires a valid sankey target widget."
				}],
				examples: [{
					userGoal: "Simplify a large Sankey by keeping only the most important nodes in each layer.",
					params: { topN: 2 }
				}],
				reversible: true
			})
		];
	}
	function registerSankeyActions(actionExecutor) {
		if (!actionExecutor.has("sankey.focusFlow")) actionExecutor.register({ name: "sankey.focusFlow" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey"
			});
			const field = typeof params.field === "string" ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("sankey.focusFlow requires a sankey target, field, and one or more values.");
			const { rows: visibleRows } = ctx.readRowsForWidget(targetWidget.ref);
			const matchedCount = visibleRows.filter((row) => values.includes(row?.[field])).length;
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "category",
					field,
					values,
					predicates: [{
						field,
						op: "in",
						value: values
					}],
					count: matchedCount,
					summary: `${field}: ${values.join(", ")}`
				}),
				selectedCount: matchedCount,
				verificationHints: ["Read the updated Sankey selection state.", "Read linked widgets or feedback to confirm focused flow propagation."]
			});
		});
		if (!actionExecutor.has("sankey.selectAggregateNode")) actionExecutor.register({ name: "sankey.selectAggregateNode" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.selectAggregateNode requires a valid sankey target widget."
			});
			const aggregateName = typeof params.aggregateName === "string" && params.aggregateName.trim().length > 0 ? params.aggregateName.trim() : null;
			if (!targetWidget || !aggregateName) throw new Error("sankey.selectAggregateNode requires a sankey target and an aggregateName.");
			return buildSelectionActionResult({
				ctx,
				nextState: ctx.commitSelection({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "category",
					field: "aggregateName",
					values: [aggregateName],
					predicates: [],
					count: 1,
					summary: `aggregateName: ${aggregateName}`,
					aggregateName
				}),
				selectedCount: 1,
				verificationHints: ["Read the updated Sankey selection state and confirm the aggregateName is preserved on the active selection.", "Read the coordination state to verify the aggregate selection became the current focused Sankey context."]
			});
		});
		if (!actionExecutor.has("sankey.filterFlow")) actionExecutor.register({ name: "sankey.filterFlow" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.filterFlow requires a valid sankey target widget."
			});
			const minValue = Number(params.minValue);
			if (!targetWidget || !Number.isFinite(minValue)) throw new Error("sankey.filterFlow requires a sankey target and a finite minValue.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for sankey flow filtering.");
					const nextSpec = cloneValue$19(spec);
					const rawLinksSource = findNamedDataSource$2(nextSpec, "rawLinks");
					if (rawLinksSource.index < 0 || !rawLinksSource.values) throw new Error("sankey.filterFlow requires a rawLinks data source.");
					const survivingLinks = rawLinksSource.values.filter((link) => Number(link?.value || 0) >= minValue);
					if (survivingLinks.length === 0) throw new Error(`sankey.filterFlow found no links with value >= ${minValue}.`);
					const thresholdSignal = findNamedSignal(nextSpec, "threshold");
					if (thresholdSignal.index >= 0 && thresholdSignal.signal) {
						nextSpec.signals[thresholdSignal.index] = {
							...thresholdSignal.signal,
							value: minValue,
							...thresholdSignal.signal.bind && typeof thresholdSignal.signal.bind === "object" ? { bind: {
								...thresholdSignal.signal.bind,
								...Number.isFinite(thresholdSignal.signal.bind.max) && minValue > thresholdSignal.signal.bind.max ? { max: minValue * 1.5 } : {}
							} } : {}
						};
						nextSpec._sankey_filter_state = {
							mode: "threshold",
							min_value: minValue,
							source_action: "sankey.filterFlow"
						};
						return nextSpec;
					}
					nextSpec.data[rawLinksSource.index].values = survivingLinks;
					const nodeConfigSource = findNamedDataSource$2(nextSpec, "nodeConfig");
					if (nodeConfigSource.index >= 0 && nodeConfigSource.values) {
						const usedNodes = /* @__PURE__ */ new Set();
						survivingLinks.forEach((link) => {
							if (typeof link?.source === "string") usedNodes.add(link.source);
							if (typeof link?.target === "string") usedNodes.add(link.target);
						});
						nextSpec.data[nodeConfigSource.index].values = nodeConfigSource.values.filter((node) => usedNodes.has(node?.name));
					}
					nextSpec._sankey_filter_state = {
						mode: "threshold",
						min_value: minValue,
						source_action: "sankey.filterFlow"
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					minValue
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the threshold signal or rawLinks data now reflect the requested minimum flow.", "Read the target widget view state to confirm low-value links no longer participate in the visible Sankey structure."]
			};
		});
		if (!actionExecutor.has("sankey.collapseNodes")) actionExecutor.register({ name: "sankey.collapseNodes" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.collapseNodes requires a valid sankey target widget."
			});
			const nodes = Array.isArray(params.nodes) ? params.nodes.filter((value) => typeof value === "string" && value.trim().length > 0) : [];
			const aggregateName = typeof params.aggregateName === "string" && params.aggregateName.trim().length > 0 ? params.aggregateName : "Other";
			if (!targetWidget || nodes.length === 0) throw new Error("sankey.collapseNodes requires a sankey target and one or more node names.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for collapsing Sankey nodes.");
					const nextSpec = cloneValue$19(spec);
					const rawLinksSource = findNamedDataSource$2(nextSpec, "rawLinks");
					const nodeConfigSource = findNamedDataSource$2(nextSpec, "nodeConfig");
					if (rawLinksSource.index < 0 || nodeConfigSource.index < 0 || !rawLinksSource.values || !nodeConfigSource.values) throw new Error("sankey.collapseNodes requires rawLinks and nodeConfig data sources.");
					const collapseSet = new Set(nodes);
					const existingNames = new Set(nodeConfigSource.values.map((node) => node?.name).filter((name) => typeof name === "string"));
					const missing = nodes.filter((name) => !existingNames.has(name));
					if (missing.length > 0) throw new Error(`sankey.collapseNodes cannot find node(s): ${missing.join(", ")}`);
					if (!nextSpec._sankey_state || typeof nextSpec._sankey_state !== "object") nextSpec._sankey_state = {
						original_nodes: cloneValue$19(nodeConfigSource.values),
						original_links: cloneValue$19(rawLinksSource.values),
						collapsed_groups: {}
					};
					if (!nextSpec._sankey_state.collapsed_groups || typeof nextSpec._sankey_state.collapsed_groups !== "object") nextSpec._sankey_state.collapsed_groups = {};
					nextSpec._sankey_state.collapsed_groups[aggregateName] = [...nodes];
					nextSpec._sankey_aggregate_state = {
						mode: "collapseNodes",
						aggregate_name: aggregateName,
						collapsed_nodes: [...nodes]
					};
					let collapseDepth = 0;
					let maxOrder = 0;
					for (const node of nodeConfigSource.values) {
						if (!node || typeof node !== "object") continue;
						if (collapseSet.has(node.name)) collapseDepth = typeof node.depth === "number" ? node.depth : collapseDepth;
						if ((typeof node.depth === "number" ? node.depth : 0) === collapseDepth) maxOrder = Math.max(maxOrder, typeof node.order === "number" ? node.order : 0);
					}
					const newNodes = nodeConfigSource.values.filter((node) => !collapseSet.has(node?.name));
					newNodes.push({
						name: aggregateName,
						depth: collapseDepth,
						order: maxOrder + 1,
						_is_aggregate: true,
						_collapsed_nodes: [...nodes]
					});
					const linkAgg = /* @__PURE__ */ new Map();
					for (const link of rawLinksSource.values) {
						if (!link || typeof link !== "object") continue;
						const src = link.source;
						const tgt = link.target;
						const value = Number(link.value || 0);
						const newSrc = collapseSet.has(src) ? aggregateName : src;
						const newTgt = collapseSet.has(tgt) ? aggregateName : tgt;
						if (newSrc === aggregateName && newTgt === aggregateName) continue;
						const key = `${newSrc}-->${newTgt}`;
						linkAgg.set(key, {
							source: newSrc,
							target: newTgt,
							value: (linkAgg.get(key)?.value || 0) + value
						});
					}
					nextSpec.data[nodeConfigSource.index].values = newNodes;
					nextSpec.data[rawLinksSource.index].values = [...linkAgg.values()];
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					nodes,
					aggregateName
				},
				verificationHints: ["Call perception.inspectViewConfig to verify rawLinks and nodeConfig now include the aggregate node and collapsed topology.", "Read the target widget view state to confirm the original Sankey nodes and links were preserved in _sankey_state for later expansion."]
			};
		});
		if (!actionExecutor.has("sankey.expandNode")) actionExecutor.register({ name: "sankey.expandNode" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.expandNode requires a valid sankey target widget."
			});
			const aggregateName = typeof params.aggregateName === "string" && params.aggregateName.trim().length > 0 ? params.aggregateName : null;
			if (!targetWidget || !aggregateName) throw new Error("sankey.expandNode requires a sankey target and an aggregateName.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for expanding Sankey nodes.");
					const nextSpec = cloneValue$19(spec);
					const rawLinksSource = findNamedDataSource$2(nextSpec, "rawLinks");
					const nodeConfigSource = findNamedDataSource$2(nextSpec, "nodeConfig");
					if (rawLinksSource.index < 0 || nodeConfigSource.index < 0 || !rawLinksSource.values || !nodeConfigSource.values) throw new Error("sankey.expandNode requires rawLinks and nodeConfig data sources.");
					const sankeyState = nextSpec._sankey_state && typeof nextSpec._sankey_state === "object" ? nextSpec._sankey_state : null;
					const collapsedGroups = sankeyState?.collapsed_groups && typeof sankeyState.collapsed_groups === "object" ? sankeyState.collapsed_groups : null;
					const originalNodes = Array.isArray(sankeyState?.original_nodes) ? sankeyState.original_nodes : null;
					const originalLinks = Array.isArray(sankeyState?.original_links) ? sankeyState.original_links : null;
					if (!collapsedGroups || !originalNodes || !originalLinks) throw new Error("sankey.expandNode requires saved _sankey_state with original nodes, links, and collapsed groups.");
					if (!Array.isArray(collapsedGroups[aggregateName])) throw new Error(`sankey.expandNode cannot find collapsed group "${aggregateName}".`);
					const collapsedNodeNames = new Set(collapsedGroups[aggregateName]);
					const newNodes = nodeConfigSource.values.filter((node) => node?.name !== aggregateName);
					for (const originalNode of originalNodes) if (collapsedNodeNames.has(originalNode?.name)) newNodes.push(cloneValue$19(originalNode));
					const currentNodeNames = new Set(newNodes.map((node) => node?.name).filter((name) => typeof name === "string"));
					const restoredLinks = [];
					for (const originalLink of originalLinks) {
						const src = originalLink?.source;
						const tgt = originalLink?.target;
						if (currentNodeNames.has(src) && currentNodeNames.has(tgt)) restoredLinks.push(cloneValue$19(originalLink));
					}
					for (const link of rawLinksSource.values) {
						const src = link?.source;
						const tgt = link?.target;
						if (src === aggregateName || tgt === aggregateName) continue;
						if (collapsedNodeNames.has(src) || collapsedNodeNames.has(tgt)) continue;
						if (!restoredLinks.some((existing) => existing?.source === src && existing?.target === tgt)) restoredLinks.push(cloneValue$19(link));
					}
					nextSpec.data[nodeConfigSource.index].values = newNodes;
					nextSpec.data[rawLinksSource.index].values = restoredLinks;
					delete nextSpec._sankey_state.collapsed_groups[aggregateName];
					nextSpec._navigation_state = {
						mode: "expandNode",
						sourceAction: "sankey.expandNode",
						aggregateName
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					aggregateName
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the aggregate node has been replaced by its original nodes and links.", "Read the target widget view state to confirm the corresponding collapsed-group entry was removed from _sankey_state."]
			};
		});
		if (!actionExecutor.has("sankey.highlightPath")) actionExecutor.register({ name: "sankey.highlightPath" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.highlightPath requires a valid sankey target widget."
			});
			const path = Array.isArray(params.path) ? params.path.filter((value) => typeof value === "string" && value.trim().length > 0) : typeof params.path === "string" && params.path.trim().length > 0 ? params.path.split(",").map((value) => value.trim()).filter((value) => value.length > 0) : [];
			if (!targetWidget || path.length < 2) throw new Error("sankey.highlightPath requires a sankey target and a path with at least two nodes.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for highlighting a Sankey path.");
					const nextSpec = cloneValue$19(spec);
					const rawLinksSource = findNamedDataSource$2(nextSpec, "rawLinks");
					if (rawLinksSource.index < 0 || !rawLinksSource.values) throw new Error("sankey.highlightPath requires a rawLinks data source.");
					const linkSet = new Set(rawLinksSource.values.map((link) => `${link?.source}-->${link?.target}`));
					const highlightEdges = [];
					for (let index = 0; index < path.length - 1; index += 1) {
						const key = `${path[index]}-->${path[index + 1]}`;
						if (linkSet.has(key)) highlightEdges.push([path[index], path[index + 1]]);
					}
					if (highlightEdges.length === 0) throw new Error("sankey.highlightPath could not find any valid edges along the requested path.");
					const edgeMark = findNamedMark(nextSpec, "edgeMark");
					const nodeMark = findNamedMark(nextSpec, "nodeRect");
					const edgeConditions = highlightEdges.map(([source, target]) => `(datum.source === '${source}' && datum.target === '${target}')`);
					const nodeConditions = [...new Set(path)].map((node) => `datum.name === '${node}'`);
					if (edgeMark) {
						const update = (edgeMark.encode ||= {}).update ||= {};
						update.fillOpacity = { signal: `(${edgeConditions.join(" || ")}) ? 0.75 : 0.06` };
						update.strokeOpacity = { signal: `(${edgeConditions.join(" || ")}) ? 0.5 : 0.02` };
					}
					if (nodeMark) {
						const update = (nodeMark.encode ||= {}).update ||= {};
						update.fillOpacity = { signal: `(${nodeConditions.join(" || ")}) ? 1.0 : 0.15` };
						update.strokeWidth = { signal: `(${nodeConditions.join(" || ")}) ? 2.5 : 0.5` };
					}
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					path
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the Sankey edge and node marks now contain path-sensitive opacity and stroke updates.", "Read the target widget view state to confirm the requested path remains prominent while unrelated edges and nodes are dimmed."]
			};
		});
		if (!actionExecutor.has("sankey.traceNode")) actionExecutor.register({ name: "sankey.traceNode" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.traceNode requires a valid sankey target widget."
			});
			const nodeName = typeof params.nodeName === "string" && params.nodeName.trim().length > 0 ? params.nodeName.trim() : null;
			if (!targetWidget || !nodeName) throw new Error("sankey.traceNode requires a sankey target and a nodeName.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for tracing a Sankey node.");
					const nextSpec = cloneValue$19(spec);
					const rawLinksSource = findNamedDataSource$2(nextSpec, "rawLinks");
					if (rawLinksSource.index < 0 || !rawLinksSource.values) throw new Error("sankey.traceNode requires a rawLinks data source.");
					if (!rawLinksSource.values.some((link) => link?.source === nodeName || link?.target === nodeName)) throw new Error(`sankey.traceNode cannot find node "${nodeName}" in rawLinks.`);
					const selectedNodeSignal = findNamedSignal(nextSpec, "selectedNode");
					if (selectedNodeSignal.index >= 0 && selectedNodeSignal.signal) {
						nextSpec.signals[selectedNodeSignal.index] = {
							...selectedNodeSignal.signal,
							value: nodeName
						};
						nextSpec._sankey_focus_state = { node_name: nodeName };
						return nextSpec;
					}
					const edgeMark = findNamedMark(nextSpec, "edgeMark");
					const nodeMark = findNamedMark(nextSpec, "nodeRect");
					if (edgeMark) {
						const update = (edgeMark.encode ||= {}).update ||= {};
						update.fillOpacity = { signal: `datum.source === '${nodeName}' || datum.target === '${nodeName}' ? 0.75 : 0.08` };
					}
					if (nodeMark) {
						const update = (nodeMark.encode ||= {}).update ||= {};
						update.fillOpacity = { signal: `datum.name === '${nodeName}' ? 1.0 : 0.2` };
					}
					nextSpec._sankey_focus_state = { node_name: nodeName };
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					nodeName
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the selectedNode signal or mark encodings now target the requested node and its directly connected flows.", "Read the target widget view state to confirm unrelated Sankey edges and nodes are dimmed relative to the traced node."]
			};
		});
		if (!actionExecutor.has("sankey.colorFlows")) actionExecutor.register({ name: "sankey.colorFlows" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.colorFlows requires a valid sankey target widget."
			});
			const nodes = Array.isArray(params.nodes) ? params.nodes.filter((value) => typeof value === "string" && value.trim().length > 0) : [];
			const color = typeof params.color === "string" && params.color.trim().length > 0 ? params.color.trim() : "#e74c3c";
			if (!targetWidget || nodes.length === 0) throw new Error("sankey.colorFlows requires a sankey target and one or more node names.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for coloring Sankey flows.");
					const nextSpec = cloneValue$19(spec);
					const rawLinksSource = findNamedDataSource$2(nextSpec, "rawLinks");
					if (rawLinksSource.index < 0 || !rawLinksSource.values) throw new Error("sankey.colorFlows requires a rawLinks data source.");
					const nodesSet = new Set(nodes);
					const coloredEdges = rawLinksSource.values.filter((link) => link && typeof link === "object" && (nodesSet.has(link.source) || nodesSet.has(link.target))).map((link) => [link.source, link.target]);
					if (coloredEdges.length === 0) throw new Error(`sankey.colorFlows cannot find any flows connected to nodes: ${nodes.join(", ")}`);
					const edgeMark = findNamedMark(nextSpec, "edgeMark");
					if (!edgeMark) throw new Error("sankey.colorFlows requires an edgeMark in the current Sankey spec.");
					const update = (edgeMark.encode ||= {}).update ||= {};
					const originalFill = update.fill;
					let fallback = "scale('color', datum.source)";
					if (originalFill && typeof originalFill === "object" && !Array.isArray(originalFill)) {
						if (typeof originalFill.scale === "string" && typeof originalFill.field === "string") fallback = `scale('${originalFill.scale}', datum.${originalFill.field})`;
						else if (typeof originalFill.signal === "string" && originalFill.signal.trim().length > 0) fallback = `(${originalFill.signal})`;
						else if (Object.prototype.hasOwnProperty.call(originalFill, "value")) fallback = `'${originalFill.value}'`;
					}
					update.fill = { signal: `(${coloredEdges.map(([source, target]) => `(datum.source === '${source}' && datum.target === '${target}')`).join(" || ")}) ? '${color}' : ${fallback}` };
					nextSpec._sankey_reencode_state = {
						mode: "colorFlows",
						nodes: [...nodes],
						color
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					nodes,
					color
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the Sankey edgeMark fill encoding now recolors flows connected to the requested nodes.", "Read the target widget view state to confirm unrelated edge colors still resolve through the prior fill encoding fallback."]
			};
		});
		if (!actionExecutor.has("sankey.reorderNodesInLayer")) actionExecutor.register({ name: "sankey.reorderNodesInLayer" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.reorderNodesInLayer requires a valid sankey target widget."
			});
			const depth = typeof params.depth === "number" && Number.isFinite(params.depth) ? params.depth : null;
			const order = Array.isArray(params.order) ? params.order.filter((value) => typeof value === "string" && value.trim().length > 0) : [];
			if (!targetWidget || depth == null || order.length === 0) throw new Error("sankey.reorderNodesInLayer requires a sankey target, a numeric depth, and a non-empty order list.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for reordering Sankey nodes in one layer.");
					const nextSpec = cloneValue$19(spec);
					const nodeConfigSource = findNamedDataSource$2(nextSpec, "nodeConfig");
					if (nodeConfigSource.index < 0 || !nodeConfigSource.values) throw new Error("sankey.reorderNodesInLayer requires a nodeConfig data source.");
					if (nodeConfigSource.values.filter((node) => Number(node?.depth ?? 0) === depth).length === 0) throw new Error(`sankey.reorderNodesInLayer cannot find nodes at depth ${depth}.`);
					const orderMap = new Map(order.map((name, index) => [name, index]));
					nextSpec.data[nodeConfigSource.index].values = nodeConfigSource.values.map((node) => {
						if (Number(node?.depth ?? 0) !== depth) return node;
						if (!orderMap.has(node?.name)) return node;
						return {
							...node,
							order: orderMap.get(node.name)
						};
					});
					nextSpec._sankey_reencode_state = {
						mode: "reorderNodesInLayer",
						depth,
						order: [...order]
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					depth,
					order
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the nodeConfig entries at the requested depth now use the requested order values.", "Read the target widget view state to confirm the Sankey layer ordering changed without altering the underlying raw link topology."]
			};
		});
		if (!actionExecutor.has("sankey.autoCollapseByRank")) actionExecutor.register({ name: "sankey.autoCollapseByRank" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "sankey",
				message: "sankey.autoCollapseByRank requires a valid sankey target widget."
			});
			const topN = typeof params.topN === "number" && Number.isFinite(params.topN) ? params.topN : null;
			if (!targetWidget || topN == null || topN < 0) throw new Error("sankey.autoCollapseByRank requires a sankey target and a non-negative numeric topN.");
			return {
				nextState: ctx.updateCurrentSpec((spec) => {
					if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("No active base spec is available for auto-collapsing Sankey nodes by rank.");
					const nextSpec = cloneValue$19(spec);
					const rawLinksSource = findNamedDataSource$2(nextSpec, "rawLinks");
					const nodeConfigSource = findNamedDataSource$2(nextSpec, "nodeConfig");
					if (rawLinksSource.index < 0 || nodeConfigSource.index < 0 || !rawLinksSource.values || !nodeConfigSource.values) throw new Error("sankey.autoCollapseByRank requires rawLinks and nodeConfig data sources.");
					if (!nextSpec._sankey_state || typeof nextSpec._sankey_state !== "object") nextSpec._sankey_state = {
						original_nodes: cloneValue$19(nodeConfigSource.values),
						original_links: cloneValue$19(rawLinksSource.values),
						collapsed_groups: {}
					};
					if (!nextSpec._sankey_state.collapsed_groups || typeof nextSpec._sankey_state.collapsed_groups !== "object") nextSpec._sankey_state.collapsed_groups = {};
					const nodeFlows = computeNodeFlows$1(rawLinksSource.values);
					const depthGroups = /* @__PURE__ */ new Map();
					for (const node of nodeConfigSource.values) {
						const depth = Number(node?.depth ?? 0);
						if (!depthGroups.has(depth)) depthGroups.set(depth, []);
						depthGroups.get(depth).push(node);
					}
					const nodesToKeep = /* @__PURE__ */ new Set();
					const collapsedByLayer = /* @__PURE__ */ new Map();
					const nodeToAggregate = /* @__PURE__ */ new Map();
					for (const [depth, group] of depthGroups.entries()) {
						const sortedGroup = [...group].sort((left, right) => {
							const leftTotal = nodeFlows.get(left?.name)?.total || 0;
							return (nodeFlows.get(right?.name)?.total || 0) - leftTotal;
						});
						for (const node of sortedGroup.slice(0, topN)) nodesToKeep.add(node?.name);
						const collapsedNames = sortedGroup.slice(topN).map((node) => node?.name).filter((name) => typeof name === "string");
						if (collapsedNames.length > 0) {
							const aggregateName = `Others (Layer ${depth})`;
							collapsedByLayer.set(depth, {
								aggregateName,
								collapsedNodes: collapsedNames
							});
							nextSpec._sankey_state.collapsed_groups[aggregateName] = collapsedNames;
							for (const name of collapsedNames) nodeToAggregate.set(name, aggregateName);
						}
					}
					if (collapsedByLayer.size === 0) return nextSpec;
					const newNodes = nodeConfigSource.values.filter((node) => nodesToKeep.has(node?.name));
					for (const [depth, info] of collapsedByLayer.entries()) {
						const maxOrder = (depthGroups.get(depth) || []).reduce((max, node) => Math.max(max, Number(node?.order ?? 0)), 0);
						newNodes.push({
							name: info.aggregateName,
							depth,
							order: maxOrder + 1,
							_is_aggregate: true,
							_collapsed_nodes: info.collapsedNodes
						});
					}
					const linkAgg = /* @__PURE__ */ new Map();
					for (const link of rawLinksSource.values) {
						if (!link || typeof link !== "object") continue;
						const source = nodeToAggregate.get(link.source) || link.source;
						const target = nodeToAggregate.get(link.target) || link.target;
						const key = `${source}-->${target}`;
						linkAgg.set(key, {
							source,
							target,
							value: (linkAgg.get(key)?.value || 0) + Number(link.value || 0)
						});
					}
					nextSpec.data[nodeConfigSource.index].values = newNodes;
					nextSpec.data[rawLinksSource.index].values = [...linkAgg.values()];
					nextSpec._sankey_aggregate_state = {
						mode: "autoCollapseByRank",
						top_n: topN,
						collapsed_groups: [...collapsedByLayer.entries()].map(([depth, info]) => ({
							depth,
							aggregate_name: info.aggregateName,
							collapsed_nodes: [...info.collapsedNodes]
						}))
					};
					return nextSpec;
				}),
				result: {
					widgetId: targetWidget.widgetId,
					topN
				},
				verificationHints: ["Call perception.inspectViewConfig to verify lower-ranked nodes were replaced by per-layer aggregate nodes in rawLinks/nodeConfig.", "Read the target widget view state to confirm the original Sankey topology remains preserved in _sankey_state for later restoration."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/sankey/perception.js
	function buildNodeFlows(links) {
		const flows = /* @__PURE__ */ new Map();
		const ensure = (name) => {
			if (!flows.has(name)) flows.set(name, {
				inflow: 0,
				outflow: 0,
				total: 0
			});
			return flows.get(name);
		};
		for (const link of Array.isArray(links) ? links : []) {
			if (!link || typeof link !== "object") continue;
			const source = link.source;
			const target = link.target;
			const value = Number(link.value || 0);
			const sourceEntry = ensure(source);
			const targetEntry = ensure(target);
			sourceEntry.outflow += value;
			targetEntry.inflow += value;
		}
		for (const entry of flows.values()) entry.total = Math.max(entry.inflow, entry.outflow);
		return flows;
	}
	function findNamedDataSource$1(spec, name) {
		const entry = (Array.isArray(spec?.data) ? spec.data : []).find((item) => item && typeof item === "object" && item.name === name) || null;
		return Array.isArray(entry?.values) ? entry.values : null;
	}
	function buildSankeyNodeOptions(rawSpec) {
		const nodeConfig = findNamedDataSource$1(rawSpec, "nodeConfig");
		const rawLinks = findNamedDataSource$1(rawSpec, "rawLinks");
		const depthLabelsData = findNamedDataSource$1(rawSpec, "depthLabelsData") || [];
		if (!nodeConfig || !rawLinks) throw new Error("perception.getNodeOptions requires rawLinks and nodeConfig data sources.");
		const nodeFlows = buildNodeFlows(rawLinks);
		const depthLabelMap = /* @__PURE__ */ new Map();
		for (const entry of depthLabelsData) {
			if (!entry || typeof entry !== "object") continue;
			depthLabelMap.set(String(entry.depth), entry.label || `Layer ${entry.depth}`);
		}
		const nodesByDepth = {};
		const allNodes = [];
		for (const node of [...nodeConfig].sort((left, right) => {
			const leftDepth = Number(left?.depth ?? 0);
			const rightDepth = Number(right?.depth ?? 0);
			if (leftDepth !== rightDepth) return leftDepth - rightDepth;
			return Number(left?.order ?? 0) - Number(right?.order ?? 0);
		})) {
			const name = node?.name;
			if (typeof name !== "string" || name.length === 0) continue;
			const depthKey = String(Number(node?.depth ?? 0));
			const flow = nodeFlows.get(name) || { total: 0 };
			allNodes.push(name);
			if (!nodesByDepth[depthKey]) nodesByDepth[depthKey] = {
				label: depthLabelMap.get(depthKey) || `Layer ${depthKey}`,
				nodes: []
			};
			const entry = {
				name,
				order: Number(node?.order ?? 0),
				total: Math.round(Number(flow.total || 0) * 100) / 100
			};
			if (node?._is_aggregate) entry.is_aggregate = true;
			if (Array.isArray(node?._collapsed_nodes) && node._collapsed_nodes.length > 0) entry.collapsed_nodes = [...node._collapsed_nodes];
			nodesByDepth[depthKey].nodes.push(entry);
		}
		const adjacency = Object.fromEntries(allNodes.map((name) => [name, {
			upstream: [],
			downstream: []
		}]));
		const edges = [];
		const values = [];
		for (const link of rawLinks) {
			if (!link || typeof link !== "object") continue;
			const source = link.source;
			const target = link.target;
			const value = Number(link.value || 0);
			edges.push({
				source,
				target,
				value
			});
			values.push(value);
			if (adjacency[source] && !adjacency[source].downstream.includes(target)) adjacency[source].downstream.push(target);
			if (adjacency[target] && !adjacency[target].upstream.includes(source)) adjacency[target].upstream.push(source);
		}
		return {
			all_nodes: allNodes,
			nodes_by_depth: nodesByDepth,
			depth_count: Object.keys(nodesByDepth).length,
			depth_labels: Object.fromEntries(depthLabelMap.entries()),
			edges,
			adjacency,
			collapsed_groups: rawSpec?._sankey_state?.collapsed_groups || {},
			value_range: values.length > 0 ? {
				min: Math.round(Math.min(...values) * 100) / 100,
				max: Math.round(Math.max(...values) * 100) / 100
			} : {
				min: 0,
				max: 0
			}
		};
	}
	function buildSankeyConversionRate(rawSpec, nodeName = null) {
		const rawLinks = findNamedDataSource$1(rawSpec, "rawLinks");
		if (!rawLinks) throw new Error("perception.calculateConversionRate requires a rawLinks data source.");
		const nodeFlows = buildNodeFlows(rawLinks);
		const nodeOptions = buildSankeyNodeOptions(rawSpec);
		const conversions = [];
		for (const name of [...nodeFlows.keys()].sort()) {
			const info = nodeFlows.get(name) || {
				inflow: 0,
				outflow: 0
			};
			const inflow = Number(info.inflow || 0);
			const outflow = Number(info.outflow || 0);
			let type = "intermediate";
			let rate = inflow > 0 ? Math.round(outflow / inflow * 1e4) / 1e4 : 0;
			if (inflow === 0 && outflow > 0) {
				type = "source";
				rate = "source";
			} else if (outflow === 0 && inflow > 0) {
				type = "sink";
				rate = 0;
			}
			const conversion = {
				node: name,
				inflow: Math.round(inflow * 100) / 100,
				outflow: Math.round(outflow * 100) / 100,
				rate,
				type
			};
			if (type === "intermediate" && inflow > 0) {
				const loss = inflow - outflow;
				conversion.loss = Math.round(loss * 100) / 100;
				conversion.loss_rate = Math.round(loss / inflow * 1e4) / 1e4;
			}
			conversions.push(conversion);
		}
		if (typeof nodeName === "string" && nodeName.length > 0) {
			const target = conversions.find((entry) => entry.node === nodeName);
			if (!target) throw new Error(`perception.calculateConversionRate cannot find node "${nodeName}".`);
			const upstream = rawLinks.filter((link) => link?.target === nodeName).map((link) => ({
				from: link.source,
				value: Number(link.value || 0)
			}));
			const downstream = rawLinks.filter((link) => link?.source === nodeName).map((link) => ({
				to: link.target,
				value: Number(link.value || 0)
			}));
			return {
				operation: "calculate_conversion_rate",
				message: `Conversion analysis for ${nodeName}`,
				node: nodeName,
				conversion: target,
				upstream,
				downstream,
				ui_hints: nodeOptions
			};
		}
		const sources = conversions.filter((entry) => entry.type === "source");
		const sinks = conversions.filter((entry) => entry.type === "sink");
		const intermediates = conversions.filter((entry) => entry.type === "intermediate");
		const highLossNodes = [...intermediates].filter((entry) => Number(entry.loss_rate || 0) > 0).sort((left, right) => Number(right.loss_rate || 0) - Number(left.loss_rate || 0)).slice(0, 5);
		return {
			operation: "calculate_conversion_rate",
			message: `Calculated conversion rates for ${conversions.length} nodes`,
			summary: {
				total_nodes: conversions.length,
				source_nodes: sources.length,
				sink_nodes: sinks.length,
				intermediate_nodes: intermediates.length
			},
			conversions,
			high_loss_nodes: highLossNodes,
			ui_hints: nodeOptions
		};
	}
	function buildSankeyBottlenecks(rawSpec, topN = 3) {
		const rawLinks = findNamedDataSource$1(rawSpec, "rawLinks");
		if (!rawLinks) throw new Error("perception.findBottleneck requires a rawLinks data source.");
		const nodeFlows = buildNodeFlows(rawLinks);
		const nodeOptions = buildSankeyNodeOptions(rawSpec);
		const bottlenecks = [];
		for (const [name, info] of nodeFlows.entries()) {
			const inflow = Number(info.inflow || 0);
			const outflow = Number(info.outflow || 0);
			if (!(inflow > 0 && outflow > 0 && inflow > outflow)) continue;
			const loss = inflow - outflow;
			bottlenecks.push({
				node: name,
				inflow: Math.round(inflow * 100) / 100,
				outflow: Math.round(outflow * 100) / 100,
				loss: Math.round(loss * 100) / 100,
				loss_rate: Math.round(loss / inflow * 1e4) / 1e4
			});
		}
		bottlenecks.sort((left, right) => {
			if (right.loss_rate !== left.loss_rate) return right.loss_rate - left.loss_rate;
			if (right.loss !== left.loss) return right.loss - left.loss;
			return String(left.node).localeCompare(String(right.node));
		});
		const limit = Number.isFinite(topN) && topN > 0 ? Math.floor(topN) : 3;
		const top = bottlenecks.slice(0, limit);
		return {
			operation: "find_bottleneck",
			message: top.length > 0 ? `Found top ${top.length} bottleneck nodes with highest loss rates` : "No bottlenecks found (no intermediate nodes with loss)",
			bottlenecks: top,
			total_bottleneck_nodes: bottlenecks.length,
			ui_hints: nodeOptions
		};
	}
	function buildSankeyPerceptionDescriptors({ dataRef }) {
		return [
			makePerceptionDescriptor({
				name: "perception.getNodeOptions",
				title: "Get Sankey node options",
				description: appendQueryScopeGuidance("Return structured Sankey node, layer, adjacency, and collapsed-group metadata for UI controls and agent-side exploration."),
				category: "inspect",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({ includeEdges: { type: "boolean" } }),
				sideEffectFree: true,
				evidenceKinds: ["topologyEvidence", "uiMetadata"],
				examples: [{
					userGoal: "Inspect all current Sankey node and layer options before choosing a path, collapse target, or filter threshold.",
					params: {}
				}, buildQueryScopeExample({
					userGoal: "Inspect Sankey node options for one focused flow view.",
					params: {},
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_view"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.calculateConversionRate",
				title: "Calculate Sankey conversion rate",
				description: appendQueryScopeGuidance("Compute inflow, outflow, conversion rate, and loss information for all Sankey nodes or one target node."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({ nodeName: { type: "string" } }),
				sideEffectFree: true,
				evidenceKinds: [
					"flowEvidence",
					"conversionEvidence",
					"topologyEvidence"
				],
				examples: [{
					userGoal: "Inspect conversion and loss rates across the whole Sankey, or drill into one node.",
					params: {}
				}, buildQueryScopeExample({
					userGoal: "Inspect conversion rates inside one focused Sankey view.",
					params: {},
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_view"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findBottleneck",
				title: "Find Sankey bottlenecks",
				description: appendQueryScopeGuidance("Identify the intermediate Sankey nodes with the highest loss rates by comparing inflow and outflow."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({ topN: { type: "number" } }),
				sideEffectFree: true,
				evidenceKinds: [
					"flowEvidence",
					"conversionEvidence",
					"rankEvidence"
				],
				examples: [{
					userGoal: "Find the worst flow drop-off points in the current Sankey.",
					params: { topN: 3 }
				}, buildQueryScopeExample({
					userGoal: "Find bottlenecks inside one focused Sankey view.",
					params: { topN: 3 },
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_view"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findExtremes",
				title: "Find extremes",
				description: appendQueryScopeGuidance("Return the highest- or lowest-valued flows in the visible Sankey data."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					field: { type: "string" },
					direction: { type: "string" },
					limit: { type: "number" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["flowEvidence", "rankEvidence"],
				examples: [{
					userGoal: "Find the largest or smallest visible flows.",
					params: {
						field: "value",
						direction: "max",
						limit: 5
					}
				}, buildQueryScopeExample({
					userGoal: "Find extreme flows inside one focused Sankey selection.",
					params: {
						field: "value",
						direction: "max",
						limit: 5
					},
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/sankey_a/selection/current"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.compareGroups",
				title: "Compare flow groups",
				description: appendQueryScopeGuidance("Compare grouped flow magnitudes in the visible Sankey data."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					groupField: { type: "string" },
					valueField: { type: "string" },
					groups: {
						type: "array",
						items: { type: "string" }
					}
				}),
				sideEffectFree: true,
				evidenceKinds: ["groupComparison", "flowEvidence"],
				examples: [{
					userGoal: "Compare grouped flow magnitudes across categories.",
					params: {
						groupField: "source",
						valueField: "value",
						groups: ["A", "B"]
					}
				}, buildQueryScopeExample({
					userGoal: "Compare grouped flows inside one focused Sankey selection.",
					params: {
						groupField: "source",
						valueField: "value",
						groups: ["A", "B"]
					},
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/sankey_a/selection/current"
				})]
			})
		];
	}
	function registerSankeyPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.getNodeOptions", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.getNodeOptions",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const options = buildSankeyNodeOptions(targetWidget?.rawSpec || targetWidget?.spec || null);
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: {
				operation: "get_node_options",
				message: `Extracted ${options.all_nodes.length} nodes across ${options.depth_count} layers`,
				...options
			} };
		}, { supportedWidgetKinds: ["sankey"] });
		if (!perceptionRegistry.has("perception.calculateConversionRate", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.calculateConversionRate",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const result = buildSankeyConversionRate(targetWidget?.rawSpec || targetWidget?.spec || null, call?.params?.nodeName || null);
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result };
		}, { supportedWidgetKinds: ["sankey"] });
		if (!perceptionRegistry.has("perception.findBottleneck", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.findBottleneck",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const result = buildSankeyBottlenecks(targetWidget?.rawSpec || targetWidget?.spec || null, call?.params?.topN);
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result };
		}, { supportedWidgetKinds: ["sankey"] });
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["sankey"] });
		if (!perceptionRegistry.has("perception.compareGroups", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.compareGroups",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const params = ctx.readCallParams();
			const comparison = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "compareGroups",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: comparison,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["sankey"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/sankey/humanInteraction.js
	function getSankeyHumanInteractionConfig() {
		return {
			mode: "categoryClick",
			actionName: "sankey.focusFlow",
			categoryFieldChannel: "color",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/sankey/state.js
	async function applySankeyState(args) {
		return applyVegaLiteRuntimeState(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/sankeyWidgetAdapter.js
	var sankeyWidgetAdapter = createVegaLiteWidgetAdapter({
		kind: "sankey",
		async applyState(args) {
			return applySankeyState(args);
		},
		buildActionDescriptors(args) {
			return buildSankeyActionDescriptors(args);
		},
		buildPerceptionDescriptors(args) {
			return buildSankeyPerceptionDescriptors(args);
		},
		getHumanInteractionConfig() {
			return getSankeyHumanInteractionConfig();
		},
		registerActions(actionExecutor) {
			return registerSankeyActions(actionExecutor);
		},
		registerPerceptionQueries(perceptionRegistry) {
			return registerSankeyPerceptionQueries(perceptionRegistry);
		}
	});
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/scatter/humanInteraction.js
	function getScatterHumanInteractionConfig() {
		return {
			mode: "brush2d",
			actionName: "scatter.brushRegion",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/scatter/state.js
	async function applyScatterState(args) {
		return applyVegaLiteRuntimeState(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/scatterWidgetAdapter.js
	var scatterWidgetAdapter = createVegaLiteWidgetAdapter({
		kind: "scatter",
		bindHumanInteractions({ view, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
			return bindWidgetHumanInteractions({
				view,
				spec,
				interactionConfig,
				selectionSourceWidgetId,
				actionTargetRef,
				onActionCall,
				onSelectionChange
			});
		},
		async applyState({ view, state }) {
			return applyScatterState({
				view,
				state
			});
		},
		buildActionDescriptors({ widgetRef, selectionRef, scope = "local", affectedRefs = [widgetRef] }) {
			return buildScatterActionDescriptors({
				widgetRef,
				selectionRef,
				scope,
				affectedRefs
			});
		},
		buildPerceptionDescriptors({ dataRef }) {
			return buildScatterPerceptionDescriptors({ dataRef });
		},
		getHumanInteractionConfig() {
			return getScatterHumanInteractionConfig();
		},
		registerActions(actionExecutor) {
			return registerScatterActions(actionExecutor);
		},
		registerPerceptionQueries(perceptionRegistry) {
			return registerScatterPerceptionQueries(perceptionRegistry);
		}
	});
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/table/actions.js
	function resolveDefaultKeyField(widgetState) {
		return (Array.isArray(widgetState?.rawSpec?.columns) ? widgetState.rawSpec.columns : [])[0] || null;
	}
	function buildTableActionDescriptors({ widgetRef, scope = "local", affectedRefs = [widgetRef] }) {
		return [makeActionDescriptor({
			name: "table.focusRows",
			title: "Focus table rows",
			description: "Focus a set of rows in the current table using a key field and one or more keys.",
			primitive: "navigate",
			category: "navigation",
			scope,
			supportedWidgetKinds: ["table"],
			targetRef: widgetRef,
			affectedRefs,
			paramsSchema: {
				type: "object",
				properties: {
					keyField: { type: "string" },
					keys: {
						type: "array",
						items: {},
						minItems: 1
					}
				},
				required: ["keys"]
			},
			postconditions: [{ description: "The workspace focus should move to the table and the table should expose a point selection for the requested keys." }],
			preconditions: [{
				description: "The table exposes at least one stable key field for row identity.",
				failureMessage: "No usable key field is available for row focus."
			}],
			effects: [makeSelectionEffect(widgetRef, "Creates a point selection that identifies focused table rows.")],
			examples: [{
				userGoal: "Jump to one or more detail rows that should be examined closely.",
				params: {
					keyField: "Name",
					keys: ["ford pinto"]
				}
			}],
			reversible: true
		})];
	}
	function registerTableActions(actionExecutor) {
		if (!actionExecutor.has("table.focusRows")) actionExecutor.register({ name: "table.focusRows" }, async (call, ctx) => {
			const params = call?.params || {};
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "table",
				message: "table.focusRows requires a valid target table widget."
			});
			const widgetState = ctx.readCurrentState({ refs: [targetWidget.ref] })?.widgets?.[targetWidget.ref];
			const keyField = typeof params.keyField === "string" ? params.keyField : resolveDefaultKeyField(widgetState);
			const keys = Array.isArray(params.keys) ? params.keys.filter((key) => key != null) : [];
			if (!keyField || keys.length === 0) throw new Error("table.focusRows requires a keyField and at least one key.");
			const selection = {
				selection_id: `table_focus_${Date.now()}`,
				source_widget_id: targetWidget.widgetId,
				selection_type: "point",
				keyField,
				keys,
				predicates: [{
					field: keyField,
					op: keys.length === 1 ? "equals" : "in",
					value: keys.length === 1 ? keys[0] : keys
				}],
				count: keys.length,
				summary: `Focused ${keys.length} row${keys.length === 1 ? "" : "s"} in ${targetWidget.widgetId}.`
			};
			ctx.getAppState().setCurrentFocusedWidgetRef(targetWidget.ref);
			return {
				nextState: ctx.commitSelection(selection),
				propagateFromSelection: true,
				result: {
					widgetId: targetWidget.widgetId,
					keyField,
					keys
				},
				verificationHints: ["Read the workspace state and confirm shared.focusedWidget points to the table.", "Read the table widget state and confirm a point selection exists for the requested keys."],
				notes: { userVisibleSummary: `Focused ${keys.length} table row${keys.length === 1 ? "" : "s"} via ${keyField}.` }
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/table/humanInteraction.js
	function getTableHumanInteractionConfig() {
		return {
			mode: "rowClick",
			actionName: "table.focusRows",
			supportsDirectManipulation: true
		};
	}
	function bindTableHumanInteractions({ surface, state, interactionConfig, onActionCall }) {
		if (!surface || !interactionConfig || interactionConfig.mode !== "rowClick" || typeof onActionCall !== "function") return () => {};
		const handleClick = (event) => {
			const rowElement = event.target instanceof Element ? event.target.closest?.("[data-widgetva-row-index]") : null;
			if (!rowElement) return;
			const rowIndex = Number(rowElement.getAttribute("data-widgetva-row-index"));
			if (!Number.isInteger(rowIndex) || rowIndex < 0) return;
			const row = (Array.isArray(state?.rawSpec?.data?.values) ? state.rawSpec.data.values : [])[rowIndex];
			if (!row || typeof row !== "object") return;
			const columns = Array.isArray(state?.rawSpec?.columns) ? state.rawSpec.columns : [];
			const keyField = interactionConfig.keyField || columns[0] || null;
			const key = keyField ? row?.[keyField] : null;
			if (!keyField || key == null) return;
			onActionCall({
				callId: `human_${Date.now()}`,
				name: interactionConfig.actionName || "table.focusRows",
				actor: "human",
				targetRef: state?.ref || void 0,
				params: {
					targetRef: state?.ref || void 0,
					keyField,
					keys: [key]
				}
			});
		};
		surface.addEventListener("click", handleClick);
		return () => surface.removeEventListener("click", handleClick);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/table/perception.js
	function buildTablePerceptionDescriptors({ dataRef }) {
		return [makePerceptionDescriptor({
			name: "perception.findExtremes",
			title: "Find extremes",
			description: "Return top-k or bottom-k rows by a numeric field in the visible table data.",
			category: "compute",
			targetRef: dataRef,
			paramsSchema: {
				type: "object",
				properties: {
					field: { type: "string" },
					direction: { type: "string" },
					limit: { type: "number" },
					dataRef: { type: "string" }
				}
			},
			sideEffectFree: true,
			evidenceKinds: ["rowEvidence", "rankEvidence"],
			examples: [{
				userGoal: "Find the highest or lowest records directly from the visible table.",
				params: {
					field: "Horsepower",
					direction: "max",
					limit: 5
				}
			}]
		}), makePerceptionDescriptor({
			name: "perception.compareGroups",
			title: "Compare groups",
			description: "Compare grouped statistics directly from the visible table rows.",
			category: "compute",
			targetRef: dataRef,
			paramsSchema: {
				type: "object",
				properties: {
					groupField: { type: "string" },
					valueField: { type: "string" },
					groups: {
						type: "array",
						items: { type: "string" }
					},
					dataRef: { type: "string" }
				}
			},
			sideEffectFree: true,
			evidenceKinds: ["groupComparison", "rowEvidence"],
			examples: [{
				userGoal: "Compare grouped statistics directly from detail rows.",
				params: {
					groupField: "Origin",
					valueField: "Horsepower",
					groups: ["Japan", "USA"]
				}
			}]
		})];
	}
	function registerTablePerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["table"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["table"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "table"
			});
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: call?.params || {}
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, call?.params || {}).dataRef
			}) };
		}, { supportedWidgetKinds: ["table"] });
		if (!perceptionRegistry.has("perception.compareGroups", { supportedWidgetKinds: ["table"] })) perceptionRegistry.register({
			name: "perception.compareGroups",
			supportedWidgetKinds: ["table"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({
				targetRef: call?.queryScope?.widgetRef || null,
				kind: "table"
			});
			const comparison = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "compareGroups",
				spec: call?.params || {}
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: comparison,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, call?.params || {}).dataRef
			}) };
		}, { supportedWidgetKinds: ["table"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgets/table/state.js
	async function applyTableState({ view, surface, state }) {
		const pointSelections = Object.values(state?.selections || {}).filter((selection) => selection?.kind === "point");
		const focusedSelection = pointSelections.length === 1 ? pointSelections[0] : null;
		const selectionSummaries = pointSelections.map((selection) => selection?.summary || "").filter(Boolean);
		const selectionFields = pointSelections.map((selection) => Array.isArray(selection?.predicates) ? selection.predicates : []).flat().filter((predicate) => predicate?.field && (predicate.op === "equals" || predicate.op === "in")).map((predicate) => predicate.field);
		const selectionField = selectionFields.length === 1 ? selectionFields[0] : new Set(selectionFields).size === 1 ? selectionFields[0] : "";
		if (surface) {
			surface.dataset.widgetvaSelectedCount = String(state?.data?.selectedCount ?? 0);
			surface.dataset.widgetvaVisibleCount = String(state?.data?.visibleCount ?? 0);
			surface.dataset.widgetvaSelectionSummary = focusedSelection?.summary || selectionSummaries.join(" · ");
			surface.dataset.widgetvaSelectionField = selectionField;
			surface.dataset.widgetvaSelectionCount = String(pointSelections.length);
			surface.dataset.widgetvaSelectionSummaries = JSON.stringify(selectionSummaries);
		}
		if (!view) return;
		try {
			if (typeof view.signal === "function") {
				view.signal("widgetva_tableSelection", focusedSelection || null);
				view.signal("widgetva_tableSelections", pointSelections);
			}
			await view.runAsync?.();
		} catch {}
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/tableWidgetAdapter.js
	var tableWidgetAdapter = createCustomWidgetAdapter({
		kind: "table",
		bindHumanInteractions(args) {
			return bindTableHumanInteractions(args);
		},
		async applyState(args) {
			return applyTableState(args);
		},
		buildActionDescriptors(args) {
			return buildTableActionDescriptors(args);
		},
		buildPerceptionDescriptors(args) {
			return buildTablePerceptionDescriptors(args);
		},
		getHumanInteractionConfig() {
			return getTableHumanInteractionConfig();
		},
		registerActions(actionExecutor) {
			return registerTableActions(actionExecutor);
		},
		registerPerceptionQueries(perceptionRegistry) {
			return registerTablePerceptionQueries(perceptionRegistry);
		}
	});
	//#endregion
	//#region ../../widgetva-kit/src/adapters/widgetFamilies/index.js
	var familyAdapters = new Map([
		["scatter", scatterWidgetAdapter],
		["bar", barWidgetAdapter],
		["line", lineWidgetAdapter],
		["heatmap", heatmapWidgetAdapter],
		["parallelCoordinates", parallelCoordinatesWidgetAdapter],
		["sankey", sankeyWidgetAdapter],
		["map", mapWidgetAdapter],
		["table", tableWidgetAdapter],
		["custom", defaultWidgetAdapter]
	]);
	function getWidgetFamilyAdapter(kind) {
		return familyAdapters.get(kind) || defaultWidgetAdapter;
	}
	function bindIfFunction(target, name) {
		const value = target?.[name];
		return typeof value === "function" ? value.bind(target) : void 0;
	}
	function pickProviderSharedDefinition(kind, { includeProviderRenderHooks = true } = {}) {
		const familyAdapter = getWidgetFamilyAdapter(kind);
		const baseDefinition = {
			kind,
			buildActionDescriptors: bindIfFunction(familyAdapter, "buildActionDescriptors"),
			buildPerceptionDescriptors: bindIfFunction(familyAdapter, "buildPerceptionDescriptors"),
			getHumanInteractionConfig: bindIfFunction(familyAdapter, "getHumanInteractionConfig"),
			registerActions: bindIfFunction(familyAdapter, "registerActions"),
			registerPerceptionQueries: bindIfFunction(familyAdapter, "registerPerceptionQueries"),
			readSelection: bindIfFunction(familyAdapter, "readSelection"),
			readViewport: bindIfFunction(familyAdapter, "readViewport")
		};
		if (!includeProviderRenderHooks) return baseDefinition;
		return {
			...baseDefinition,
			bindHumanInteractions: bindIfFunction(familyAdapter, "bindHumanInteractions"),
			applyState: bindIfFunction(familyAdapter, "applyState"),
			mount: bindIfFunction(familyAdapter, "mount"),
			update: bindIfFunction(familyAdapter, "update"),
			dispose: bindIfFunction(familyAdapter, "dispose")
		};
	}
	function createProviderFamilyAdapter(kind, provider = "vega-lite") {
		const sharedDefinition = pickProviderSharedDefinition(kind, { includeProviderRenderHooks: provider === "vega-lite" });
		if (provider === "echarts") return createEChartsWidgetAdapter({
			...sharedDefinition,
			bindHumanInteractions(args) {
				return bindEChartsFamilyInteractions(args);
			},
			buildOptionFromState(args) {
				return buildEChartsFamilyOption(args);
			}
		});
		if (provider === "d3") return createD3WidgetAdapter({
			...sharedDefinition,
			bindHumanInteractions(args) {
				return bindD3FamilyInteractions(args);
			},
			async renderFromState(args) {
				return renderD3FamilyState(args);
			}
		});
		return createVegaLiteWidgetAdapter(sharedDefinition);
	}
	function registerWidgetFamilyAdapters({ actionExecutor, perceptionQueryRegistry }) {
		return registerWidgetAdapters(familyAdapters.values(), {
			actionExecutor,
			perceptionQueryRegistry
		});
	}
	function registerWidgetAdapters(adapters, { actionExecutor, perceptionQueryRegistry }) {
		for (const adapter of adapters || []) {
			adapter.registerActions?.(actionExecutor);
			adapter.registerPerceptionQueries?.(perceptionQueryRegistry);
		}
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/selectionHelpers.js
	function normalizePredicateValues(predicate) {
		if (!predicate) return [];
		return Array.isArray(predicate.value) ? predicate.value : [predicate.value];
	}
	function rowMatchesPredicate(row, predicate) {
		if (!predicate || !row || typeof row !== "object") return false;
		const field = predicate.field;
		if (typeof field !== "string" || field.length === 0) return false;
		const rowValue = row?.[field];
		if (predicate.op === "equals") return rowValue === predicate.value;
		if (predicate.op === "in") return normalizePredicateValues(predicate).includes(rowValue);
		if (predicate.op === "notIn") return !normalizePredicateValues(predicate).includes(rowValue);
		if (predicate.op === "between") {
			const range = normalizePredicateValues(predicate);
			if (range.length < 2) return false;
			const [minValue, maxValue] = [Math.min(...range), Math.max(...range)];
			return typeof rowValue === "number" && rowValue >= minValue && rowValue <= maxValue;
		}
		return false;
	}
	function rowMatchesSelection(row, activeSelection) {
		const predicates = Array.isArray(activeSelection?.predicates) ? activeSelection.predicates : [];
		if (predicates.length === 0) return false;
		return predicates.every((predicate) => rowMatchesPredicate(row, predicate));
	}
	function countSelectedRows(rows, activeSelection) {
		if (!Array.isArray(rows) || rows.length === 0 || !activeSelection) return 0;
		return rows.filter((row) => rowMatchesSelection(row, activeSelection)).length;
	}
	function rowMatchesAnySelection(row, activeSelections) {
		const selections = Array.isArray(activeSelections) ? activeSelections.filter(Boolean) : [];
		if (selections.length === 0) return false;
		return selections.some((selection) => rowMatchesSelection(row, selection));
	}
	function countSelectedRowsForSelections(rows, activeSelections) {
		if (!Array.isArray(rows) || rows.length === 0) return 0;
		return rows.filter((row) => rowMatchesAnySelection(row, activeSelections)).length;
	}
	function mapSelectionToTargetSelection(activeSelection, fieldMapping = []) {
		if (!activeSelection) return null;
		const mappingIndex = new Map((Array.isArray(fieldMapping) ? fieldMapping : []).filter((entry) => typeof entry?.sourceField === "string" && typeof entry?.targetField === "string").map((entry) => [entry.sourceField, entry.targetField]));
		if (mappingIndex.size === 0) return activeSelection;
		const predicates = Array.isArray(activeSelection.predicates) ? activeSelection.predicates.map((predicate) => ({
			...predicate,
			field: mappingIndex.get(predicate.field) || predicate.field
		})) : [];
		const fields = Array.isArray(activeSelection.fields) ? activeSelection.fields.map((field) => mappingIndex.get(field) || field) : activeSelection.fields;
		const value = activeSelection.value && typeof activeSelection.value === "object" && !Array.isArray(activeSelection.value) ? Object.fromEntries(Object.entries(activeSelection.value).map(([field, fieldValue]) => [mappingIndex.get(field) || field, fieldValue])) : activeSelection.value;
		const field = typeof activeSelection.field === "string" ? mappingIndex.get(activeSelection.field) || activeSelection.field : activeSelection.field;
		const keyField = typeof activeSelection.keyField === "string" ? mappingIndex.get(activeSelection.keyField) || activeSelection.keyField : activeSelection.keyField;
		return {
			...activeSelection,
			...fields ? { fields } : {},
			...value ? { value } : {},
			...field ? { field } : {},
			...keyField ? { keyField } : {},
			predicates
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/viewStateMetadata.js
	function isNumericDomain(domain) {
		return Array.isArray(domain) && domain.length >= 2 && typeof domain[0] === "number" && typeof domain[1] === "number";
	}
	function midpoint(domain) {
		if (!isNumericDomain(domain)) return null;
		return (domain[0] + domain[1]) / 2;
	}
	function span(domain) {
		if (!isNumericDomain(domain)) return null;
		return Math.abs(domain[1] - domain[0]);
	}
	function getDataExtent(rows, field) {
		if (!Array.isArray(rows) || typeof field !== "string" || field.length === 0) return null;
		const values = rows.map((row) => row?.[field]).filter((value) => typeof value === "number" && Number.isFinite(value));
		if (values.length === 0) return null;
		return [Math.min(...values), Math.max(...values)];
	}
	function inferZoomLevel({ currentDomain, baselineDomain }) {
		const currentSpan = span(currentDomain);
		const baselineSpan = span(baselineDomain);
		if (!currentSpan || !baselineSpan || currentSpan <= 0 || baselineSpan <= 0) return null;
		return Number((baselineSpan / currentSpan).toFixed(4));
	}
	function stableSerialize$2(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	function appendHighlightEntry(entries, nextEntry) {
		const signature = stableSerialize$2(nextEntry);
		if (entries.some((entry) => stableSerialize$2(entry) === signature)) return;
		entries.push(nextEntry);
	}
	function buildViewZoomState({ widgetSpec, xDomain, yDomain }) {
		const nextXDomain = Array.isArray(xDomain) ? xDomain : null;
		const nextYDomain = Array.isArray(yDomain) ? yDomain : null;
		if (!nextXDomain && !nextYDomain) return null;
		const rows = Array.isArray(widgetSpec?.data?.values) ? widgetSpec.data.values : [];
		const xField = widgetSpec?.encoding?.x?.field;
		const yField = widgetSpec?.encoding?.y?.field;
		const baselineXDomain = getDataExtent(rows, xField);
		const baselineYDomain = getDataExtent(rows, yField);
		const centerX = midpoint(nextXDomain);
		const centerY = midpoint(nextYDomain);
		const levelCandidates = [inferZoomLevel({
			currentDomain: nextXDomain,
			baselineDomain: baselineXDomain
		}), inferZoomLevel({
			currentDomain: nextYDomain,
			baselineDomain: baselineYDomain
		})].filter((value) => typeof value === "number" && Number.isFinite(value));
		const zoom = {};
		if (centerX != null && centerY != null) zoom.center = [centerX, centerY];
		if (levelCandidates.length > 0) zoom.level = Number((levelCandidates.reduce((sum, value) => sum + value, 0) / levelCandidates.length).toFixed(4));
		return Object.keys(zoom).length > 0 ? zoom : null;
	}
	function buildViewHighlightState({ widgetSpec }) {
		const entries = [];
		const inspectEncoding = (encoding, scope = "root", layerIndex = null) => {
			if (!encoding || typeof encoding !== "object" || Array.isArray(encoding)) return;
			for (const [channel, channelSpec] of Object.entries(encoding)) {
				if (!channelSpec || typeof channelSpec !== "object" || Array.isArray(channelSpec)) continue;
				if (!Object.prototype.hasOwnProperty.call(channelSpec, "condition")) continue;
				appendHighlightEntry(entries, {
					source: "encoding",
					channel,
					scope,
					...layerIndex != null ? { layerIndex } : {}
				});
			}
		};
		inspectEncoding(widgetSpec?.encoding, "root", null);
		if (Array.isArray(widgetSpec?.layer)) widgetSpec.layer.forEach((layer, layerIndex) => {
			inspectEncoding(layer?.encoding, "layer", layerIndex);
		});
		for (const mark of Array.isArray(widgetSpec?.marks) ? widgetSpec.marks : []) {
			const updateEncoding = mark?.encode?.update;
			if (!updateEncoding || typeof updateEncoding !== "object" || Array.isArray(updateEncoding)) continue;
			for (const [channel, channelSpec] of Object.entries(updateEncoding)) {
				if (!channelSpec || typeof channelSpec !== "object" || Array.isArray(channelSpec)) continue;
				if (!Object.prototype.hasOwnProperty.call(channelSpec, "signal")) continue;
				appendHighlightEntry(entries, {
					source: "mark",
					channel,
					markName: typeof mark?.name === "string" ? mark.name : null
				});
			}
		}
		if (entries.length === 0) return null;
		return {
			channels: [...new Set(entries.map((entry) => entry.channel))],
			entries
		};
	}
	function normalizeDrillParent(parent) {
		if (!parent || typeof parent !== "object" || Array.isArray(parent)) return null;
		const nextParent = {};
		if (Number.isFinite(parent.year)) nextParent.year = Number(parent.year);
		if (Number.isFinite(parent.month)) nextParent.month = Number(parent.month);
		if (Number.isFinite(parent.date)) nextParent.date = Number(parent.date);
		return Object.keys(nextParent).length > 0 ? nextParent : null;
	}
	function inferDrillLevel(parent) {
		if (!parent || typeof parent !== "object") return null;
		if (Number.isFinite(parent.date)) return "date";
		if (Number.isFinite(parent.month)) return "month";
		if (Number.isFinite(parent.year)) return "year";
		return null;
	}
	function buildViewDrillDownState({ widgetSpec }) {
		const lineDrillState = widgetSpec?._line_drilldown_state;
		if (lineDrillState && typeof lineDrillState === "object") {
			const parent = normalizeDrillParent(lineDrillState.parent);
			const level = inferDrillLevel(parent);
			const targetField = typeof widgetSpec?.encoding?.x?.field === "string" ? widgetSpec.encoding.x.field : null;
			if (!level || !targetField) return null;
			return {
				axis: "x",
				active: true,
				level,
				parent,
				targetField
			};
		}
		const heatmapState = widgetSpec?._heatmap_state;
		if (heatmapState && typeof heatmapState === "object") {
			const parent = normalizeDrillParent(heatmapState.parent);
			const level = inferDrillLevel(parent);
			const targetField = typeof widgetSpec?.encoding?.x?.field === "string" ? widgetSpec.encoding.x.field : null;
			if (!level || !targetField) return null;
			return {
				axis: "x",
				active: true,
				level,
				parent,
				targetField
			};
		}
		return null;
	}
	function buildViewAggregateState({ widgetSpec }) {
		const resampleState = widgetSpec?._resample_state;
		if (resampleState && typeof resampleState === "object") {
			const granularity = typeof resampleState.current_granularity === "string" ? resampleState.current_granularity : null;
			const agg = typeof resampleState.current_agg === "string" ? resampleState.current_agg : null;
			const timeField = typeof widgetSpec?.encoding?.x?.field === "string" ? widgetSpec.encoding.x.field : null;
			const valueField = typeof widgetSpec?.encoding?.y?.field === "string" ? widgetSpec.encoding.y.field : null;
			if (!granularity || !agg || !timeField || !valueField) return null;
			return {
				mode: "resample",
				axis: "x",
				granularity,
				agg,
				timeField,
				valueField
			};
		}
		const clusterState = widgetSpec?._cluster_rows_cols_state;
		if (clusterState && typeof clusterState === "object") {
			const method = typeof clusterState.method === "string" ? clusterState.method : null;
			const colorField = typeof clusterState.color_field === "string" ? clusterState.color_field : null;
			if (!method || !colorField) return null;
			return {
				mode: "clusterRowsCols",
				clusterRows: clusterState.cluster_rows !== false,
				clusterCols: clusterState.cluster_cols !== false,
				method,
				colorField
			};
		}
		const sankeyAggregateState = widgetSpec?._sankey_aggregate_state;
		if (sankeyAggregateState && typeof sankeyAggregateState === "object") {
			const mode = typeof sankeyAggregateState.mode === "string" ? sankeyAggregateState.mode : null;
			if (!mode) return null;
			if (mode === "collapseNodes") return {
				mode,
				aggregateName: sankeyAggregateState.aggregate_name || null,
				collapsedNodes: Array.isArray(sankeyAggregateState.collapsed_nodes) ? [...sankeyAggregateState.collapsed_nodes] : []
			};
			if (mode === "autoCollapseByRank") return {
				mode,
				topN: Number.isFinite(sankeyAggregateState.top_n) ? sankeyAggregateState.top_n : null,
				collapsedGroups: Array.isArray(sankeyAggregateState.collapsed_groups) ? sankeyAggregateState.collapsed_groups.map((group) => ({
					depth: group?.depth,
					aggregateName: group?.aggregate_name || null,
					collapsedNodes: Array.isArray(group?.collapsed_nodes) ? [...group.collapsed_nodes] : []
				})) : []
			};
		}
		return null;
	}
	function buildViewReencodeState({ widgetSpec }) {
		const stackMode = typeof widgetSpec?._stack_mode === "string" ? widgetSpec._stack_mode : null;
		if (stackMode) return {
			mode: "stackMode",
			layout: stackMode,
			colorField: typeof widgetSpec?.encoding?.color?.field === "string" ? widgetSpec.encoding.color.field : null
		};
		const colorScaleState = widgetSpec?._color_scale_state;
		if (colorScaleState && typeof colorScaleState === "object") {
			const scheme = typeof colorScaleState.scheme === "string" ? colorScaleState.scheme : null;
			const channel = typeof colorScaleState.channel === "string" ? colorScaleState.channel : "color";
			const domain = Array.isArray(colorScaleState.domain) ? [...colorScaleState.domain] : null;
			if (!scheme) return null;
			return {
				mode: "colorScale",
				channel,
				scheme,
				...domain ? { domain } : {}
			};
		}
		const transposeState = widgetSpec?._transpose_state;
		if (transposeState && typeof transposeState === "object" && transposeState.transposed === true) return {
			mode: "transpose",
			transposed: true
		};
		const barExpandStackState = widgetSpec?._bar_expand_stack_state;
		if (barExpandStackState && typeof barExpandStackState === "object") return {
			mode: "expandStack",
			category: barExpandStackState.category ?? null,
			categoryField: barExpandStackState.category_field || null,
			groupField: barExpandStackState.group_field || null
		};
		const parallelReencodeState = widgetSpec?._pc_reencode_state;
		if (parallelReencodeState && typeof parallelReencodeState === "object") return {
			mode: parallelReencodeState.mode || "dimensionVisibility",
			sourceAction: parallelReencodeState.sourceAction || null,
			operation: parallelReencodeState.operation || null,
			hiddenDimensions: Array.isArray(parallelReencodeState.hidden_dimensions) ? [...parallelReencodeState.hidden_dimensions] : [],
			visibleDimensions: Array.isArray(parallelReencodeState.visible_dimensions) ? [...parallelReencodeState.visible_dimensions] : []
		};
		const sankeyReencodeState = widgetSpec?._sankey_reencode_state;
		if (sankeyReencodeState && typeof sankeyReencodeState === "object") {
			const mode = typeof sankeyReencodeState.mode === "string" ? sankeyReencodeState.mode : null;
			if (!mode) return null;
			if (mode === "colorFlows") return {
				mode,
				nodes: Array.isArray(sankeyReencodeState.nodes) ? [...sankeyReencodeState.nodes] : [],
				color: sankeyReencodeState.color || null
			};
			if (mode === "reorderNodesInLayer") return {
				mode,
				depth: Number.isFinite(sankeyReencodeState.depth) ? sankeyReencodeState.depth : null,
				order: Array.isArray(sankeyReencodeState.order) ? [...sankeyReencodeState.order] : []
			};
		}
		return null;
	}
	function buildViewAnnotateState({ widgetSpec }) {
		const layers = Array.isArray(widgetSpec?.layer) ? widgetSpec.layer : [];
		if (layers.some((layer) => layer?._widgetvaTag === "line.highlightTrend")) return {
			mode: "regressionOverlay",
			sourceAction: "line.highlightTrend"
		};
		if (layers.some((layer) => layer?._widgetvaTag === "scatter.showRegression")) return {
			mode: "regressionOverlay",
			sourceAction: "scatter.showRegression"
		};
		if (layers.some((layer) => layer?._widgetvaTag === "line.showMovingAverage")) return {
			mode: "movingAverageOverlay",
			sourceAction: "line.showMovingAverage"
		};
		const scatterClusterState = widgetSpec?._scatter_cluster_state;
		if (scatterClusterState && typeof scatterClusterState === "object") return {
			mode: "clusterAnnotation",
			method: scatterClusterState.method || null,
			clusterField: scatterClusterState.cluster_field || null,
			nClusters: Number.isFinite(scatterClusterState.n_clusters) ? scatterClusterState.n_clusters : null
		};
		const marginalBarsState = widgetSpec?._marginal_bars_state;
		if (marginalBarsState && typeof marginalBarsState === "object" && marginalBarsState.enabled === true) return {
			mode: "marginalBars",
			op: marginalBarsState.op || "sum",
			showTop: marginalBarsState.show_top !== false,
			showRight: marginalBarsState.show_right !== false,
			valueField: marginalBarsState.value_field || null
		};
		return null;
	}
	function buildViewAddRemoveState({ widgetSpec }) {
		const visibilityState = widgetSpec?._bar_visibility_state;
		if (!visibilityState || typeof visibilityState !== "object") return null;
		if (visibilityState.mode === "x" && typeof visibilityState.x_field === "string" && Array.isArray(visibilityState.visible_x)) return {
			mode: "categoryVisibility",
			operation: visibilityState.last_operation === "add" ? "add" : "remove",
			field: visibilityState.x_field,
			visibleValues: [...visibilityState.visible_x]
		};
		if (visibilityState.mode === "item" && typeof visibilityState.x_field === "string" && typeof visibilityState.sub_field === "string" && Array.isArray(visibilityState.visible_items)) return {
			mode: "itemVisibility",
			operation: visibilityState.last_operation === "add" ? "add" : "remove",
			xField: visibilityState.x_field,
			subField: visibilityState.sub_field,
			visibleItems: visibilityState.visible_items.map((pair) => Array.isArray(pair) ? [...pair] : pair)
		};
		return null;
	}
	function buildViewFocusState({ widgetSpec }) {
		const lineFocusState = widgetSpec?._line_focus_state;
		if (lineFocusState && typeof lineFocusState === "object" && Array.isArray(lineFocusState.lines) && lineFocusState.lines.length > 0) {
			const focusedSeries = [...lineFocusState.lines];
			return {
				focusedSeries,
				focusKeys: { focusedSeries }
			};
		}
		const sankeyFocusState = widgetSpec?._sankey_focus_state;
		if (sankeyFocusState && typeof sankeyFocusState === "object" && typeof sankeyFocusState.node_name === "string") {
			const focusedNode = sankeyFocusState.node_name;
			return {
				focusedNode,
				focusKeys: { focusedNode }
			};
		}
		return {};
	}
	function buildViewNavigateState({ widgetSpec }) {
		const navigationState = widgetSpec?._navigation_state;
		if (!navigationState || typeof navigationState !== "object") return null;
		if (typeof navigationState.sourceAction !== "string" || typeof navigationState.mode !== "string") return null;
		return {
			mode: navigationState.mode,
			sourceAction: navigationState.sourceAction
		};
	}
	function buildViewSortState({ widgetSpec }) {
		const encoding = widgetSpec?.encoding;
		if (!encoding || typeof encoding !== "object") return null;
		for (const [channel, channelSpec] of Object.entries(encoding)) {
			if (!channelSpec || typeof channelSpec !== "object") continue;
			const sort = channelSpec.sort;
			const field = typeof channelSpec.field === "string" && channelSpec.field.length > 0 ? channelSpec.field : null;
			if (Array.isArray(sort) && sort.length > 0) return {
				channel,
				field,
				mode: "explicitOrder",
				values: [...sort]
			};
			if (typeof sort === "string") {
				if (sort !== "ascending" && sort !== "descending") continue;
				if (!field) continue;
				return {
					channel,
					field,
					mode: "direction",
					order: sort
				};
			}
			if (sort && typeof sort === "object" && !Array.isArray(sort)) {
				const sortField = typeof sort.field === "string" && sort.field.length > 0 ? sort.field : field;
				const order = typeof sort.order === "string" ? sort.order : null;
				if (!sortField || order !== "ascending" && order !== "descending") continue;
				return {
					channel,
					field: sortField,
					mode: "direction",
					order,
					...typeof sort.op === "string" && sort.op.length > 0 ? { aggregate: sort.op } : {}
				};
			}
		}
		return null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/transformHelpers.js
	function isPlainObject$2(value) {
		return !!value && typeof value === "object" && !Array.isArray(value);
	}
	function extractPredicatesFromFilterTransform(transform) {
		const filter = transform?.filter;
		if (!isPlainObject$2(filter)) return [];
		if (typeof filter.field === "string" && Array.isArray(filter.oneOf) && filter.oneOf.length > 0) return [{
			field: filter.field,
			op: "in",
			value: [...filter.oneOf]
		}];
		if (typeof filter.field === "string" && Array.isArray(filter.notOneOf) && filter.notOneOf.length > 0) return [{
			field: filter.field,
			op: "notIn",
			value: [...filter.notOneOf]
		}];
		if (typeof filter.field === "string" && Array.isArray(filter.range) && filter.range.length === 2) return [{
			field: filter.field,
			op: "between",
			value: [...filter.range]
		}];
		if (typeof filter.field === "string" && Object.prototype.hasOwnProperty.call(filter, "equal")) return [{
			field: filter.field,
			op: "equals",
			value: filter.equal
		}];
		return [];
	}
	function extractPredicatesFromTransforms(transforms = []) {
		if (!Array.isArray(transforms) || transforms.length === 0) return [];
		return transforms.flatMap((transform) => extractPredicatesFromFilterTransform(transform));
	}
	function applySupportedSpecTransforms({ rows, transforms = [], dataQueryEngine }) {
		let nextRows = Array.isArray(rows) ? rows : [];
		for (const transform of Array.isArray(transforms) ? transforms : []) {
			const predicates = extractPredicatesFromFilterTransform(transform);
			if (predicates.length > 0) {
				nextRows = dataQueryEngine.filter(nextRows, predicates);
				continue;
			}
			if (Array.isArray(transform?.aggregate) && transform.aggregate.length > 0) {
				const result = dataQueryEngine.aggregate(nextRows, {
					groupBy: Array.isArray(transform?.groupby) ? transform.groupby.filter(Boolean) : [],
					measures: transform.aggregate.map((measure) => ({
						op: measure?.op,
						field: measure?.field,
						as: measure?.as
					})).filter((measure) => typeof measure.op === "string" && typeof measure.as === "string")
				});
				nextRows = Array.isArray(result?.rows) ? result.rows : nextRows;
			}
		}
		return nextRows;
	}
	function inferTransformKind(transform) {
		if (extractPredicatesFromFilterTransform(transform).length > 0) return "filter";
		if (Array.isArray(transform?.aggregate) || transform?.joinaggregate) return "aggregate";
		if (transform?.calculate) return "derive";
		if (transform?.sample) return "sample";
		return "derive";
	}
	function normalizeSpecTransforms(transforms = []) {
		if (!Array.isArray(transforms)) return [];
		return transforms.map((transform) => makeTransformState({
			kind: inferTransformKind(transform),
			source: "widgetSpec",
			spec: {
				...transform,
				predicates: extractPredicatesFromFilterTransform(transform)
			}
		}));
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/widgetStateBuilders.js
	function cloneValue$18(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readMarkType(widgetSpec) {
		if (typeof widgetSpec?.mark === "string") return widgetSpec.mark;
		if (widgetSpec?.mark && typeof widgetSpec.mark === "object" && typeof widgetSpec.mark.type === "string") return widgetSpec.mark.type;
		return null;
	}
	function normalizeSelections(activeSelectionOrSelections) {
		if (Array.isArray(activeSelectionOrSelections)) return activeSelectionOrSelections.filter(Boolean);
		return activeSelectionOrSelections ? [activeSelectionOrSelections] : [];
	}
	function buildEncodings(spec) {
		const enc = spec?.encoding || {};
		const normalized = {};
		for (const [channel, value] of Object.entries(enc)) {
			if (!value || typeof value !== "object") continue;
			normalized[channel] = makeFieldEncoding({
				field: value.field,
				type: value.type,
				aggregate: value.aggregate,
				bin: value.bin,
				scale: value.scale
			});
		}
		return normalized;
	}
	function buildHighlightFeedback({ link, activeSelections, existingFeedback }) {
		const values = normalizeSelections(activeSelections).flatMap((selection) => Array.isArray(selection?.predicates) ? selection.predicates : []).filter((predicate) => predicate.op === "equals" || predicate.op === "in").flatMap((predicate) => Array.isArray(predicate.value) ? predicate.value : [predicate.value]);
		return {
			...makeInteractionFeedbackState(existingFeedback),
			highlightLinkIds: [...new Set([...existingFeedback?.highlightLinkIds || [], link.linkId])],
			highlightedKeys: [...new Set(values)]
		};
	}
	function getHighlightInfo({ link, activeSelections }) {
		const categoricalPredicates = normalizeSelections(activeSelections).flatMap((selection) => Array.isArray(selection?.predicates) ? selection.predicates : []).filter((predicate) => predicate.op === "equals" || predicate.op === "in");
		if (categoricalPredicates.length === 0) return null;
		const mapping = Array.isArray(link?.fieldMapping) && link.fieldMapping.length > 0 ? link.fieldMapping[0] : null;
		const sourceField = mapping?.sourceField || categoricalPredicates[0].field;
		const targetField = mapping?.targetField || sourceField;
		const matchedPredicate = categoricalPredicates.find((predicate) => predicate.field === sourceField) || categoricalPredicates[0];
		return {
			sourceField,
			targetField,
			values: Array.isArray(matchedPredicate.value) ? matchedPredicate.value : [matchedPredicate.value]
		};
	}
	function buildLinkedViewState({ widgetSpec, inboundLinks, activeSelections }) {
		const viewState = {
			xDomain: widgetSpec?.encoding?.x?.scale?.domain,
			yDomain: widgetSpec?.encoding?.y?.scale?.domain
		};
		const selections = normalizeSelections(activeSelections);
		for (const link of inboundLinks) if (link.primitive === "syncDomain") for (const activeSelection of selections) {
			if (!activeSelection?.domain) continue;
			if (Array.isArray(activeSelection.domain.xDomain)) viewState.xDomain = activeSelection.domain.xDomain;
			if (Array.isArray(activeSelection.domain.yDomain)) viewState.yDomain = activeSelection.domain.yDomain;
		}
		viewState.zoom = buildViewZoomState({
			widgetSpec,
			xDomain: viewState.xDomain,
			yDomain: viewState.yDomain
		});
		viewState.sort = buildViewSortState({ widgetSpec });
		viewState.highlight = buildViewHighlightState({ widgetSpec });
		viewState.drillDown = buildViewDrillDownState({ widgetSpec });
		viewState.aggregate = buildViewAggregateState({ widgetSpec });
		viewState.reencode = buildViewReencodeState({ widgetSpec });
		viewState.annotate = buildViewAnnotateState({ widgetSpec });
		viewState.addRemove = buildViewAddRemoveState({ widgetSpec });
		viewState.navigate = buildViewNavigateState({ widgetSpec });
		Object.assign(viewState, buildViewFocusState({ widgetSpec }));
		return viewState;
	}
	function applyLinkedViewStateToSpec({ widgetSpec, inboundLinks, activeSelections }) {
		if (!widgetSpec?.encoding) return widgetSpec;
		const hasDomainSync = inboundLinks.some((link) => link.primitive === "syncDomain");
		const selections = normalizeSelections(activeSelections).filter((selection) => selection?.domain);
		if (!hasDomainSync || selections.length === 0) return widgetSpec;
		const nextSpec = cloneValue$18(widgetSpec);
		const resolvedXDomain = [...selections].reverse().find((selection) => Array.isArray(selection?.domain?.xDomain))?.domain?.xDomain || null;
		const resolvedYDomain = [...selections].reverse().find((selection) => Array.isArray(selection?.domain?.yDomain))?.domain?.yDomain || null;
		if (Array.isArray(resolvedXDomain) && nextSpec.encoding?.x) nextSpec.encoding.x = {
			...nextSpec.encoding.x,
			scale: {
				...nextSpec.encoding.x.scale || {},
				domain: resolvedXDomain
			}
		};
		if (Array.isArray(resolvedYDomain) && nextSpec.encoding?.y) nextSpec.encoding.y = {
			...nextSpec.encoding.y,
			scale: {
				...nextSpec.encoding.y.scale || {},
				domain: resolvedYDomain
			}
		};
		return nextSpec;
	}
	function applyHighlightToSpec({ widgetSpec, inboundLinks, activeSelections }) {
		const highlightLink = inboundLinks.find((link) => link.primitive === "highlight");
		if (!highlightLink || !Array.isArray(widgetSpec?.data?.values)) return widgetSpec;
		const highlightInfo = getHighlightInfo({
			link: highlightLink,
			activeSelections
		});
		if (!highlightInfo || highlightInfo.values.length === 0) return widgetSpec;
		const nextSpec = cloneValue$18(widgetSpec);
		nextSpec.data = { values: nextSpec.data.values.map((row) => ({
			...row,
			__widgetva_highlight: highlightInfo.values.includes(row?.[highlightInfo.targetField])
		})) };
		if (nextSpec.kind === "table") return nextSpec;
		nextSpec.encoding = {
			...nextSpec.encoding || {},
			opacity: {
				condition: {
					test: "datum.__widgetva_highlight === true",
					value: .95
				},
				value: .18
			}
		};
		return nextSpec;
	}
	function applySelectionToSpec({ widgetSpec, activeSelections, selectionEnabled = false }) {
		const selections = normalizeSelections(activeSelections);
		if (!selectionEnabled || !Array.isArray(widgetSpec?.data?.values) || selections.length === 0) return widgetSpec;
		const nextSpec = cloneValue$18(widgetSpec);
		const selectedRows = nextSpec.data.values.map((row) => ({
			...row,
			__widgetva_selected: rowMatchesAnySelection(row, selections)
		}));
		nextSpec.data = {
			...nextSpec.data || {},
			values: selectedRows
		};
		if (nextSpec.kind === "table") return nextSpec;
		const nextEncoding = {
			...nextSpec.encoding || {},
			opacity: {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: .98
				},
				value: .22
			}
		};
		const markType = readMarkType(nextSpec);
		if (markType === "point" || markType === "circle") {
			nextEncoding.strokeOpacity = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: 1
				},
				value: .12
			};
			nextEncoding.strokeWidth = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: 1.8
				},
				value: .6
			};
			nextEncoding.fill = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: "#4c78a8"
				},
				value: "#4c78a8"
			};
			nextEncoding.fillOpacity = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: .9
				},
				value: .04
			};
		}
		nextSpec.encoding = nextEncoding;
		return nextSpec;
	}
	function buildTransforms({ widgetSpec, inboundLinks }) {
		const specTransforms = normalizeSpecTransforms(widgetSpec?.transform);
		const sankeyFilterState = widgetSpec?._sankey_filter_state;
		if (sankeyFilterState && typeof sankeyFilterState === "object") specTransforms.push(makeTransformState({
			kind: "filter",
			source: "widgetSpec",
			spec: {
				mode: sankeyFilterState.mode || "threshold",
				minValue: Number.isFinite(sankeyFilterState.min_value) ? sankeyFilterState.min_value : null,
				sourceAction: sankeyFilterState.source_action || null
			}
		}));
		return [...specTransforms, ...inboundLinks.filter((link) => link.primitive === "filter" || link.primitive === "syncDomain").map((link) => makeTransformState({
			kind: link.primitive,
			sourceWidgetId: link.sourceWidgetId,
			linkId: link.linkId,
			spec: {
				linkRef: link.ref || null,
				sourceWidgetId: link.sourceWidgetId,
				targetWidgetId: link.targetWidgetId
			}
		}))];
	}
	function buildWidgetState({ widgetRef, widgetId, role, kind, updatedAt, sourceDataRef, currentDataRef, rowCount, visibleCount, selectedCount, widgetSpec, inboundLinks, activeSelections, widgetSelectionRefs, mirroredSelectionEntries, humanInteraction, linkedRefs, existingFeedback }) {
		const inboundHighlightLink = inboundLinks.find((link) => link.primitive === "highlight");
		const inboundSharedSelectionLink = inboundLinks.find((link) => link.primitive === "sharesSelection");
		const normalizedSelections = normalizeSelections(activeSelections);
		const feedback = inboundHighlightLink ? buildHighlightFeedback({
			link: inboundHighlightLink,
			activeSelections: normalizedSelections,
			existingFeedback
		}) : {
			...makeInteractionFeedbackState(existingFeedback),
			inboundLinkIds: inboundLinks.length > 0 ? inboundLinks.map((link) => link.linkId) : makeInteractionFeedbackState(existingFeedback).inboundLinkIds,
			linkedSourceRefs: linkedRefs.length > 0 ? linkedRefs : makeInteractionFeedbackState(existingFeedback).linkedSourceRefs,
			...inboundSharedSelectionLink ? { sharedSelectionSourceWidgetId: inboundSharedSelectionLink.sourceWidgetId } : {}
		};
		const selectionStateMap = {};
		for (const entry of Array.isArray(widgetSelectionRefs) ? widgetSelectionRefs : []) {
			if (!entry?.ref || !entry?.selection) continue;
			selectionStateMap[entry.ref] = makeSelectionState(buildSelectionStateInput(entry.selection));
		}
		for (const entry of Array.isArray(mirroredSelectionEntries) ? mirroredSelectionEntries : []) {
			if (!entry?.ref || !entry?.selection) continue;
			selectionStateMap[entry.ref] = makeSelectionState(buildSelectionStateInput(entry.selection));
		}
		const state = makeWidgetState({
			ref: widgetRef,
			widgetId,
			role,
			kind,
			version: 1,
			updatedAt,
			data: {
				sourceDataRef,
				currentDataRef,
				rowCount,
				visibleCount,
				selectedCount: countSelectedRowsForSelections(widgetSpec?.data?.values || [], [...Array.isArray(widgetSelectionRefs) ? widgetSelectionRefs.map((entry) => entry?.selection) : [], ...Array.isArray(mirroredSelectionEntries) ? mirroredSelectionEntries.map((entry) => entry?.selection) : []]) || selectedCount
			},
			encodings: buildEncodings(widgetSpec),
			transforms: buildTransforms({
				widgetSpec,
				inboundLinks
			}),
			view: makeViewTransformState(buildLinkedViewState({
				widgetSpec,
				inboundLinks,
				activeSelections: normalizedSelections
			})),
			selections: selectionStateMap,
			feedback: makeInteractionFeedbackState(feedback),
			rawSpec: widgetSpec,
			humanInteraction
		});
		if (!state.feedback.inboundLinkIds) state.feedback = {
			...state.feedback || {},
			inboundLinkIds: inboundLinks.map((link) => link.linkId),
			linkedSourceRefs: linkedRefs
		};
		return state;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/actionExecutor.js
	function cloneValue$17(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function cloneArray$1(value) {
		return Array.isArray(value) ? [...value] : [];
	}
	function makeActionExecutorCounts(counts = {}) {
		return {
			descriptorCount: 0,
			handlerCount: 0,
			preconditionCount: 0,
			...counts
		};
	}
	function describeActionExecutorCountsSchema() {
		return cloneValue$17({
			type: "object",
			properties: {
				descriptorCount: { type: "integer" },
				handlerCount: { type: "integer" },
				preconditionCount: { type: "integer" }
			}
		});
	}
	function makeActionExecutorCapabilities(capabilities = {}) {
		return {
			paramsValidation: false,
			preconditionValidation: false,
			stateSync: false,
			traceRecording: false,
			linkPropagation: false,
			...capabilities
		};
	}
	function describeActionExecutorCapabilitiesSchema() {
		return cloneValue$17({
			type: "object",
			properties: {
				paramsValidation: { type: "boolean" },
				preconditionValidation: { type: "boolean" },
				stateSync: { type: "boolean" },
				traceRecording: { type: "boolean" },
				linkPropagation: { type: "boolean" }
			}
		});
	}
	function makeActionExecutorActionEntry(action = {}) {
		return {
			name: action?.name || "",
			primitive: action?.primitive ?? null,
			category: action?.category ?? null,
			targetRef: action?.targetRef ?? null,
			affectedRefs: cloneArray$1(action?.affectedRefs),
			affectedStatePaths: cloneArray$1(action?.affectedStatePaths),
			supportedWidgetKinds: Array.isArray(action?.supportedWidgetKinds) ? [...action.supportedWidgetKinds] : null,
			hasPreconditions: action?.hasPreconditions === true,
			preconditionDescriptorCount: action?.preconditionDescriptorCount || 0,
			preconditionHandlerRegistered: action?.preconditionHandlerRegistered === true,
			postconditionCount: action?.postconditionCount || 0,
			reversible: action?.reversible === true,
			effectCount: action?.effectCount || 0,
			effectKinds: cloneArray$1(action?.effectKinds)
		};
	}
	function describeActionExecutorActionEntrySchema() {
		return cloneValue$17({
			type: "object",
			required: ["name"],
			properties: {
				name: { type: "string" },
				primitive: { type: ["string", "null"] },
				category: { type: ["string", "null"] },
				targetRef: { type: ["string", "null"] },
				affectedRefs: {
					type: "array",
					items: { type: "string" }
				},
				affectedStatePaths: {
					type: "array",
					items: { type: "string" }
				},
				supportedWidgetKinds: { anyOf: [{ type: "null" }, {
					type: "array",
					items: { type: "string" }
				}] },
				hasPreconditions: { type: "boolean" },
				preconditionDescriptorCount: { type: "integer" },
				preconditionHandlerRegistered: { type: "boolean" },
				postconditionCount: { type: "integer" },
				reversible: { type: "boolean" },
				effectCount: { type: "integer" },
				effectKinds: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function makeActionExecutorSummary(summary = {}) {
		return {
			counts: makeActionExecutorCounts(summary?.counts),
			capabilities: makeActionExecutorCapabilities(summary?.capabilities),
			actions: Array.isArray(summary?.actions) ? summary.actions.map((action) => makeActionExecutorActionEntry(action)) : []
		};
	}
	function describeActionExecutorSummarySchema() {
		return cloneValue$17({
			type: "object",
			required: [
				"counts",
				"capabilities",
				"actions"
			],
			properties: {
				counts: describeActionExecutorCountsSchema(),
				capabilities: describeActionExecutorCapabilitiesSchema(),
				actions: {
					type: "array",
					items: describeActionExecutorActionEntrySchema()
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/actionContext.js
	function cloneValue$16(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeActionContextCapabilities(capabilities = {}) {
		return {
			workspaceRead: false,
			workspaceWrite: false,
			specMutation: false,
			snapshotReplay: false,
			runtimeDataRead: false,
			runtimeDataWrite: false,
			widgetTargetResolution: false,
			selectionMutation: false,
			annotationMutation: false,
			linkPropagation: false,
			...capabilities
		};
	}
	function makeActionContextIntegrations(integrations = {}) {
		return {
			store: false,
			appState: false,
			linkEngine: false,
			traceRecorder: false,
			...integrations
		};
	}
	function makeActionContextSummary(summary = {}) {
		return {
			methods: Array.isArray(summary?.methods) ? [...summary.methods] : [],
			capabilities: makeActionContextCapabilities(summary?.capabilities),
			integrations: makeActionContextIntegrations(summary?.integrations)
		};
	}
	function describeActionContextSummarySchema() {
		return cloneValue$16({
			type: "object",
			required: [
				"methods",
				"capabilities",
				"integrations"
			],
			properties: {
				methods: {
					type: "array",
					items: { type: "string" }
				},
				capabilities: {
					type: "object",
					properties: {
						workspaceRead: { type: "boolean" },
						workspaceWrite: { type: "boolean" },
						specMutation: { type: "boolean" },
						snapshotReplay: { type: "boolean" },
						runtimeDataRead: { type: "boolean" },
						runtimeDataWrite: { type: "boolean" },
						widgetTargetResolution: { type: "boolean" },
						selectionMutation: { type: "boolean" },
						annotationMutation: { type: "boolean" },
						linkPropagation: { type: "boolean" }
					}
				},
				integrations: {
					type: "object",
					properties: {
						store: { type: "boolean" },
						appState: { type: "boolean" },
						linkEngine: { type: "boolean" },
						traceRecorder: { type: "boolean" }
					}
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/actionUsage.js
	function cloneValue$15(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function describeActionUsageFieldExplanationSchema() {
		return cloneValue$15({
			type: "object",
			required: ["role", "reason"],
			properties: {
				role: { type: "string" },
				value: { type: ["string", "null"] },
				reason: { type: "string" }
			}
		});
	}
	function describeActionUsageFieldRoleMapSchema() {
		return cloneValue$15({
			type: "object",
			required: [
				"xField",
				"yField",
				"colorField",
				"categoryField",
				"measureField",
				"subCategoryField"
			],
			properties: {
				xField: { type: ["string", "null"] },
				yField: { type: ["string", "null"] },
				colorField: { type: ["string", "null"] },
				categoryField: { type: ["string", "null"] },
				measureField: { type: ["string", "null"] },
				subCategoryField: { type: ["string", "null"] }
			}
		});
	}
	function describeActionUsageFieldCandidatesSchema() {
		return cloneValue$15({
			type: "object",
			required: [
				"xFields",
				"yFields",
				"categoryFields",
				"measureFields",
				"groupFields"
			],
			properties: {
				xFields: {
					type: "array",
					items: { type: "string" }
				},
				yFields: {
					type: "array",
					items: { type: "string" }
				},
				categoryFields: {
					type: "array",
					items: { type: "string" }
				},
				measureFields: {
					type: "array",
					items: { type: "string" }
				},
				groupFields: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function describeActionUsageFieldRolesSchema() {
		return cloneValue$15({
			type: "object",
			required: [
				"resolved",
				"candidates",
				"explanations"
			],
			properties: {
				resolved: describeActionUsageFieldRoleMapSchema(),
				candidates: describeActionUsageFieldCandidatesSchema(),
				explanations: {
					type: "array",
					items: describeActionUsageFieldExplanationSchema()
				}
			}
		});
	}
	function describeActionUsageParamRoleSchema() {
		return cloneValue$15({
			type: "string",
			enum: [
				"structural",
				"intent",
				"optional"
			]
		});
	}
	function describeActionUsageParamRolesSchema() {
		return cloneValue$15({
			type: "object",
			additionalProperties: describeActionUsageParamRoleSchema()
		});
	}
	function describeActionUsageSpecContextSchema() {
		return cloneValue$15({
			type: "object",
			required: [
				"hasCurrentSpec",
				"dataSourceKind",
				"runtimeRowCount",
				"encodings"
			],
			properties: {
				hasCurrentSpec: { type: "boolean" },
				dataSourceKind: { type: ["string", "null"] },
				runtimeRowCount: {
					type: "integer",
					minimum: 0
				},
				encodings: {
					type: "object",
					required: [
						"x",
						"y",
						"color",
						"size",
						"shape"
					],
					properties: {
						x: { anyOf: [describeFieldEncodingSchema(), { type: "null" }] },
						y: { anyOf: [describeFieldEncodingSchema(), { type: "null" }] },
						color: { anyOf: [describeFieldEncodingSchema(), { type: "null" }] },
						size: { anyOf: [describeFieldEncodingSchema(), { type: "null" }] },
						shape: { anyOf: [describeFieldEncodingSchema(), { type: "null" }] }
					}
				}
			}
		});
	}
	function describeActionUsageEntrySchema() {
		return cloneValue$15({
			type: "object",
			required: [
				"name",
				"title",
				"description",
				"primitive",
				"category",
				"requiredParams",
				"paramRoles",
				"suggestedParams",
				"recommendedCall",
				"diagnostics"
			],
			properties: {
				name: { type: "string" },
				title: { type: "string" },
				description: { type: "string" },
				primitive: describeActionPrimitiveSchema(),
				category: describeActionCategorySchema(),
				targetRef: { type: ["string", "null"] },
				supportedWidgetKinds: { anyOf: [{ type: "null" }, {
					type: "array",
					items: describeWidgetKindSchema()
				}] },
				requiredParams: {
					type: "array",
					items: { type: "string" }
				},
				paramRoles: describeActionUsageParamRolesSchema(),
				suggestedParams: { type: "object" },
				recommendedCall: { type: "object" },
				diagnostics: {
					type: "array",
					items: { type: "string" }
				},
				paramsSchema: { anyOf: [{ type: "object" }, { type: "null" }] },
				examples: { anyOf: [{ type: "null" }, {
					type: "array",
					items: describeActionExampleSchema()
				}] },
				actionDescriptor: { anyOf: [describeActionDescriptorSchema(), { type: "null" }] }
			}
		});
	}
	function describeActionUsageSummarySchema() {
		return cloneValue$15({
			type: "object",
			required: [
				"targetRef",
				"widgetId",
				"widgetKind",
				"title",
				"diagnostics",
				"specContext",
				"fieldRoles",
				"actions"
			],
			properties: {
				targetRef: { type: ["string", "null"] },
				widgetId: { type: ["string", "null"] },
				widgetKind: { anyOf: [describeWidgetKindSchema(), { type: "null" }] },
				title: { type: ["string", "null"] },
				diagnostics: {
					type: "array",
					items: { type: "string" }
				},
				specContext: describeActionUsageSpecContextSchema(),
				fieldRoles: describeActionUsageFieldRolesSchema(),
				actions: {
					type: "array",
					items: describeActionUsageEntrySchema()
				}
			}
		});
	}
	function describeActionUsageRequestSchema() {
		return cloneValue$15({
			type: "object",
			additionalProperties: false,
			properties: {
				targetRef: { type: "string" },
				widgetRef: { type: "string" },
				widgetId: { type: "string" },
				actionName: { type: "string" },
				includeSchemas: { type: "boolean" },
				includeExamples: { type: "boolean" }
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/responses.js
	function cloneValue$14(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeAgentResponseUsage(usage) {
		return {
			promptTokens: null,
			completionTokens: null,
			totalTokens: null,
			tokenCost: null,
			tokenCostUnit: null,
			...usage
		};
	}
	function makeAgentResponseRecord(record) {
		return {
			responseId: "",
			runId: null,
			sessionId: null,
			workspaceId: null,
			actor: "agent",
			mode: null,
			query: null,
			content: "",
			stateId: null,
			branchId: null,
			evidenceRefs: [],
			coordinationEvidence: null,
			usage: makeAgentResponseUsage(),
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			...record,
			usage: record?.usage ? makeAgentResponseUsage(record.usage) : makeAgentResponseUsage()
		};
	}
	function makeResponseRecorderCapabilities(capabilities) {
		return {
			recordsFinalResponses: true,
			latestResponseRead: true,
			responseHistoryRead: true,
			workspaceScopedReads: true,
			lineageTracking: true,
			...capabilities
		};
	}
	function makeResponseRecorderCounters(counters) {
		return {
			workspaceId: null,
			responseCount: 0,
			latestResponseId: null,
			latestRunId: null,
			latestStateId: null,
			latestBranchId: null,
			latestActor: null,
			latestMode: null,
			latestQuery: null,
			latestEvidenceRefCount: 0,
			...counters
		};
	}
	function makeResponseRecorderSummary(summary) {
		return {
			...summary,
			capabilities: makeResponseRecorderCapabilities(summary?.capabilities),
			counters: makeResponseRecorderCounters(summary?.counters)
		};
	}
	function describeAgentResponseRecordSchema() {
		return cloneValue$14({
			type: "object",
			required: [
				"responseId",
				"actor",
				"content",
				"createdAt"
			],
			properties: {
				responseId: { type: "string" },
				runId: { type: ["string", "null"] },
				sessionId: { type: ["string", "null"] },
				workspaceId: { type: ["string", "null"] },
				actor: describeRuntimeActorSchema(),
				mode: { type: ["string", "null"] },
				query: { type: ["string", "null"] },
				content: { type: "string" },
				stateId: { type: ["string", "null"] },
				branchId: { type: ["string", "null"] },
				evidenceRefs: {
					type: "array",
					items: describeRefSchema()
				},
				coordinationEvidence: { anyOf: [{ type: "null" }, {
					type: "object",
					additionalProperties: true
				}] },
				usage: {
					type: "object",
					properties: {
						promptTokens: { type: ["number", "null"] },
						completionTokens: { type: ["number", "null"] },
						totalTokens: { type: ["number", "null"] },
						tokenCost: { type: ["number", "null"] },
						tokenCostUnit: { type: ["string", "null"] }
					}
				},
				createdAt: { type: "string" }
			}
		});
	}
	function describeResponseRecorderCapabilitiesSchema() {
		return cloneValue$14({
			type: "object",
			properties: {
				recordsFinalResponses: { type: "boolean" },
				latestResponseRead: { type: "boolean" },
				responseHistoryRead: { type: "boolean" },
				workspaceScopedReads: { type: "boolean" },
				lineageTracking: { type: "boolean" }
			}
		});
	}
	function describeResponseRecorderCountersSchema() {
		return cloneValue$14({
			type: "object",
			properties: {
				workspaceId: { type: ["string", "null"] },
				responseCount: { type: "integer" },
				latestResponseId: { type: ["string", "null"] },
				latestRunId: { type: ["string", "null"] },
				latestStateId: { type: ["string", "null"] },
				latestBranchId: { type: ["string", "null"] },
				latestActor: { anyOf: [describeRuntimeActorSchema(), { type: "null" }] },
				latestMode: { type: ["string", "null"] },
				latestQuery: { type: ["string", "null"] },
				latestEvidenceRefCount: { type: "integer" }
			}
		});
	}
	function describeResponseRecorderSummarySchema() {
		return cloneValue$14({
			type: "object",
			required: ["capabilities", "counters"],
			properties: {
				capabilities: describeResponseRecorderCapabilitiesSchema(),
				counters: describeResponseRecorderCountersSchema()
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/perceptionContext.js
	function cloneValue$13(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makePerceptionContextCapabilities(capabilities = {}) {
		return {
			workspaceRead: false,
			runtimeDataRead: false,
			widgetTargetResolution: false,
			dataHandleResolution: false,
			selectionScopedQueries: false,
			traceRecording: false,
			...capabilities
		};
	}
	function makePerceptionContextIntegrations(integrations = {}) {
		return {
			store: false,
			traceRecorder: false,
			...integrations
		};
	}
	function makePerceptionContextSummary(summary = {}) {
		return {
			methods: Array.isArray(summary?.methods) ? [...summary.methods] : [],
			capabilities: makePerceptionContextCapabilities(summary?.capabilities),
			integrations: makePerceptionContextIntegrations(summary?.integrations)
		};
	}
	function describePerceptionContextSummarySchema() {
		return cloneValue$13({
			type: "object",
			required: [
				"methods",
				"capabilities",
				"integrations"
			],
			properties: {
				methods: {
					type: "array",
					items: { type: "string" }
				},
				capabilities: {
					type: "object",
					properties: {
						workspaceRead: { type: "boolean" },
						runtimeDataRead: { type: "boolean" },
						widgetTargetResolution: { type: "boolean" },
						dataHandleResolution: { type: "boolean" },
						selectionScopedQueries: { type: "boolean" },
						traceRecording: { type: "boolean" }
					}
				},
				integrations: {
					type: "object",
					properties: {
						store: { type: "boolean" },
						traceRecorder: { type: "boolean" }
					}
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/dataQueryContext.js
	function cloneValue$12(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeDataQueryContextCapabilities(capabilities = {}) {
		return {
			workspaceRead: false,
			runtimeDataRead: false,
			widgetTargetResolution: false,
			dataHandleResolution: false,
			selectionTargetResolution: false,
			explicitTargetValidation: false,
			selectionScopedQueries: false,
			traceRecording: false,
			...capabilities
		};
	}
	function makeDataQueryContextIntegrations(integrations = {}) {
		return {
			store: false,
			traceRecorder: false,
			...integrations
		};
	}
	function makeDataQueryContextSummary(summary = {}) {
		return {
			methods: Array.isArray(summary?.methods) ? [...summary.methods] : [],
			capabilities: makeDataQueryContextCapabilities(summary?.capabilities),
			integrations: makeDataQueryContextIntegrations(summary?.integrations)
		};
	}
	function describeDataQueryContextSummarySchema() {
		return cloneValue$12({
			type: "object",
			required: [
				"methods",
				"capabilities",
				"integrations"
			],
			properties: {
				methods: {
					type: "array",
					items: { type: "string" }
				},
				capabilities: {
					type: "object",
					properties: {
						workspaceRead: { type: "boolean" },
						runtimeDataRead: { type: "boolean" },
						widgetTargetResolution: { type: "boolean" },
						dataHandleResolution: { type: "boolean" },
						selectionTargetResolution: { type: "boolean" },
						explicitTargetValidation: { type: "boolean" },
						selectionScopedQueries: { type: "boolean" },
						traceRecording: { type: "boolean" }
					}
				},
				integrations: {
					type: "object",
					properties: {
						store: { type: "boolean" },
						traceRecorder: { type: "boolean" }
					}
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/dataQueryEngine.js
	function cloneValue$11(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeDataQueryEngineEngineSummary(summary = {}) {
		return {
			kind: summary?.kind ?? null,
			className: summary?.className ?? null
		};
	}
	function describeDataQueryEngineCountsSchema() {
		return cloneValue$11({
			type: "object",
			properties: {
				supportedQueryKindCount: { type: "integer" },
				supportedQueryDescriptorCount: { type: "integer" }
			}
		});
	}
	function makeDataQueryEngineCounts(counts = {}) {
		return {
			supportedQueryKindCount: 0,
			supportedQueryDescriptorCount: 0,
			...counts
		};
	}
	function makeDataQueryEngineCapabilities(capabilities = {}) {
		return {
			localExecution: false,
			remoteExecution: false,
			sqlSupport: false,
			fallbackEngine: false,
			...capabilities
		};
	}
	function makeDataQueryEngineSummary(summary = {}) {
		return {
			engine: makeDataQueryEngineEngineSummary(summary?.engine),
			counts: makeDataQueryEngineCounts(summary?.counts),
			supportedQueryKinds: Array.isArray(summary?.supportedQueryKinds) ? [...summary.supportedQueryKinds] : [],
			supportedQueryDescriptors: Array.isArray(summary?.supportedQueryDescriptors) ? [...summary.supportedQueryDescriptors] : [],
			capabilities: makeDataQueryEngineCapabilities(summary?.capabilities)
		};
	}
	function describeDataQueryEngineSummarySchema() {
		return cloneValue$11({
			type: "object",
			required: [
				"engine",
				"counts",
				"supportedQueryKinds",
				"supportedQueryDescriptors",
				"capabilities"
			],
			properties: {
				engine: {
					type: "object",
					properties: {
						kind: { type: ["string", "null"] },
						className: { type: ["string", "null"] }
					}
				},
				counts: describeDataQueryEngineCountsSchema(),
				supportedQueryKinds: {
					type: "array",
					items: { type: "string" }
				},
				supportedQueryDescriptors: {
					type: "array",
					items: describeDataQueryDescriptorSchema()
				},
				capabilities: {
					type: "object",
					properties: {
						localExecution: { type: "boolean" },
						remoteExecution: { type: "boolean" },
						sqlSupport: { type: "boolean" },
						fallbackEngine: { type: "boolean" }
					}
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/runtimeCore.js
	function cloneValue$10(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeRuntimeCoreComponents(components) {
		return {
			workspacePlanner: false,
			widgetRegistry: false,
			actionExecutor: false,
			actionContext: false,
			agentLoopRuntime: false,
			perceptionQueryRegistry: false,
			perceptionContext: false,
			dataQueryExecutor: false,
			dataQueryContext: false,
			dataQueryEngine: false,
			linkEngine: false,
			stateManager: false,
			interactionTraceRecorder: false,
			responseRecorder: false,
			...components
		};
	}
	function describeRuntimeCoreComponentsSchema() {
		return cloneValue$10({
			type: "object",
			properties: {
				workspacePlanner: { type: "boolean" },
				widgetRegistry: { type: "boolean" },
				actionExecutor: { type: "boolean" },
				actionContext: { type: "boolean" },
				agentLoopRuntime: { type: "boolean" },
				perceptionQueryRegistry: { type: "boolean" },
				perceptionContext: { type: "boolean" },
				dataQueryExecutor: { type: "boolean" },
				dataQueryContext: { type: "boolean" },
				dataQueryEngine: { type: "boolean" },
				linkEngine: { type: "boolean" },
				stateManager: { type: "boolean" },
				interactionTraceRecorder: { type: "boolean" },
				responseRecorder: { type: "boolean" }
			}
		});
	}
	function makeRuntimeCoreRegistries(registries) {
		return {
			widgetCount: 0,
			dataHandleCount: 0,
			linkCount: 0,
			adapterCount: 0,
			snapshotCount: 0,
			branchCount: 0,
			actionDescriptorCount: 0,
			actionHandlerCount: 0,
			actionPreconditionCount: 0,
			perceptionDescriptorCount: 0,
			perceptionHandlerCount: 0,
			dataQueryKindCount: 0,
			dataQueryDescriptorCount: 0,
			linkPrimitiveCount: 0,
			traceRecordCount: 0,
			responseCount: 0,
			...registries
		};
	}
	function describeRuntimeCoreRegistriesSchema() {
		return cloneValue$10({
			type: "object",
			properties: {
				widgetCount: { type: "integer" },
				dataHandleCount: { type: "integer" },
				linkCount: { type: "integer" },
				adapterCount: { type: "integer" },
				snapshotCount: { type: "integer" },
				branchCount: { type: "integer" },
				actionDescriptorCount: { type: "integer" },
				actionHandlerCount: { type: "integer" },
				actionPreconditionCount: { type: "integer" },
				perceptionDescriptorCount: { type: "integer" },
				perceptionHandlerCount: { type: "integer" },
				dataQueryKindCount: { type: "integer" },
				dataQueryDescriptorCount: { type: "integer" },
				linkPrimitiveCount: { type: "integer" },
				traceRecordCount: { type: "integer" },
				responseCount: { type: "integer" }
			}
		});
	}
	function makeRuntimeCoreCapabilities(capabilities) {
		return {
			paramsValidation: false,
			perceptionReturnsValidation: false,
			dataQueryReturnsValidation: false,
			actionRun: false,
			perceptionQueryRun: false,
			dataQueryRun: false,
			workspacePlanning: false,
			verifiedActionRun: false,
			agentLoopContextRead: false,
			statePatching: false,
			stateDelta: false,
			workspaceDescriptionRead: false,
			runtimeStoreRead: false,
			widgetRegistryRead: false,
			widgetAdapterListRead: false,
			viewRead: false,
			snapshotRead: false,
			stateHistoryRead: false,
			branchListRead: false,
			interactionTraceRead: false,
			primitiveTraceRead: false,
			affectedRefsTraceRead: false,
			actionCallTraceRead: false,
			stateDeltaTraceRead: false,
			verifyQueryResultsRead: false,
			branchReplayEventsRead: false,
			traceGraphRead: false,
			stateJump: false,
			branchCreate: false,
			finalWorkspaceSnapshotRead: false,
			linkPropagationEvaluation: false,
			traceRecording: false,
			branchReplay: false,
			answerRecording: false,
			latestResponseRead: false,
			responseHistoryRead: false,
			protocolIntrospection: false,
			...capabilities
		};
	}
	function describeRuntimeCoreCapabilitiesSchema() {
		return cloneValue$10({
			type: "object",
			properties: {
				paramsValidation: { type: "boolean" },
				perceptionReturnsValidation: { type: "boolean" },
				dataQueryReturnsValidation: { type: "boolean" },
				actionRun: { type: "boolean" },
				perceptionQueryRun: { type: "boolean" },
				dataQueryRun: { type: "boolean" },
				workspacePlanning: { type: "boolean" },
				verifiedActionRun: { type: "boolean" },
				agentLoopContextRead: { type: "boolean" },
				statePatching: { type: "boolean" },
				stateDelta: { type: "boolean" },
				workspaceDescriptionRead: { type: "boolean" },
				runtimeStoreRead: { type: "boolean" },
				widgetRegistryRead: { type: "boolean" },
				widgetAdapterListRead: { type: "boolean" },
				viewRead: { type: "boolean" },
				snapshotRead: { type: "boolean" },
				stateHistoryRead: { type: "boolean" },
				branchListRead: { type: "boolean" },
				interactionTraceRead: { type: "boolean" },
				primitiveTraceRead: { type: "boolean" },
				affectedRefsTraceRead: { type: "boolean" },
				actionCallTraceRead: { type: "boolean" },
				stateDeltaTraceRead: { type: "boolean" },
				verifyQueryResultsRead: { type: "boolean" },
				branchReplayEventsRead: { type: "boolean" },
				traceGraphRead: { type: "boolean" },
				stateJump: { type: "boolean" },
				branchCreate: { type: "boolean" },
				finalWorkspaceSnapshotRead: { type: "boolean" },
				linkPropagationEvaluation: { type: "boolean" },
				traceRecording: { type: "boolean" },
				branchReplay: { type: "boolean" },
				answerRecording: { type: "boolean" },
				latestResponseRead: { type: "boolean" },
				responseHistoryRead: { type: "boolean" },
				protocolIntrospection: { type: "boolean" }
			}
		});
	}
	function makeRuntimeCoreSummary(summary) {
		return {
			...summary,
			components: makeRuntimeCoreComponents(summary?.components),
			registries: makeRuntimeCoreRegistries(summary?.registries),
			capabilities: makeRuntimeCoreCapabilities(summary?.capabilities)
		};
	}
	function describeRuntimeCoreSummarySchema() {
		return cloneValue$10({
			type: "object",
			required: [
				"components",
				"registries",
				"capabilities"
			],
			properties: {
				components: describeRuntimeCoreComponentsSchema(),
				registries: describeRuntimeCoreRegistriesSchema(),
				capabilities: describeRuntimeCoreCapabilitiesSchema()
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/runtimeStore.js
	function cloneValue$9(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeRuntimeStoreCurrentStateSummary(summary) {
		return {
			stateId: null,
			focusedWidgetRef: null,
			focusedWidgetKind: null,
			focusedWidgetTitle: null,
			visibleCount: null,
			selectedCount: null,
			selectionCount: 0,
			activeSelectionRefs: [],
			primarySelectionRef: null,
			primarySelectionSummary: "",
			primarySelectionPredicates: [],
			annotationCount: 0,
			comparisonTargetCount: 0,
			globalFilterCount: 0,
			taskMode: null,
			coordinationScope: null,
			evidenceType: null,
			interactionHorizon: null,
			replayRunMode: null,
			replayUserIntent: "",
			changedRefs: [],
			removedRefs: [],
			sharedChanged: false,
			taskContextChanged: false,
			replayContextChanged: false,
			...summary
		};
	}
	function describeRuntimeStoreCurrentStateSummarySchema() {
		return cloneValue$9({
			type: ["object", "null"],
			properties: {
				stateId: { type: ["string", "null"] },
				focusedWidgetRef: { type: ["string", "null"] },
				focusedWidgetKind: { type: ["string", "null"] },
				focusedWidgetTitle: { type: ["string", "null"] },
				visibleCount: { type: ["number", "null"] },
				selectedCount: { type: ["number", "null"] },
				selectionCount: { type: "integer" },
				activeSelectionRefs: {
					type: "array",
					items: { type: "string" }
				},
				primarySelectionRef: { type: ["string", "null"] },
				primarySelectionSummary: { type: "string" },
				primarySelectionPredicates: {
					type: "array",
					items: { type: "object" }
				},
				annotationCount: { type: "integer" },
				comparisonTargetCount: { type: "integer" },
				globalFilterCount: { type: "integer" },
				taskMode: { type: ["string", "null"] },
				coordinationScope: { type: ["string", "null"] },
				evidenceType: { type: ["string", "null"] },
				interactionHorizon: { type: ["string", "null"] },
				replayRunMode: { type: ["string", "null"] },
				replayUserIntent: { type: "string" },
				changedRefs: {
					type: "array",
					items: { type: "string" }
				},
				removedRefs: {
					type: "array",
					items: { type: "string" }
				},
				sharedChanged: { type: "boolean" },
				taskContextChanged: { type: "boolean" },
				replayContextChanged: { type: "boolean" }
			}
		});
	}
	function makeRuntimeStoreHistoryRetention(retention) {
		return {
			snapshotMax: 0,
			traceMax: 0,
			responseMax: 0,
			...retention
		};
	}
	function describeRuntimeStoreRetentionSchema() {
		return cloneValue$9({
			type: "object",
			properties: {
				snapshotMax: { type: "integer" },
				traceMax: { type: "integer" },
				responseMax: { type: "integer" }
			}
		});
	}
	function makeRuntimeStoreIdentity(identity) {
		return {
			appId: "",
			workspaceId: "",
			...identity
		};
	}
	function describeRuntimeStoreIdentitySchema() {
		return cloneValue$9({
			type: "object",
			properties: {
				appId: { type: "string" },
				workspaceId: { type: "string" }
			}
		});
	}
	function makeRuntimeStoreStateSummary(summary) {
		return {
			stateId: null,
			currentBranchId: null,
			previousStateId: null,
			version: 0,
			...summary
		};
	}
	function describeRuntimeStoreStateSummarySchema() {
		return cloneValue$9({
			type: "object",
			required: ["stateId", "currentBranchId"],
			properties: {
				stateId: { type: ["string", "null"] },
				currentBranchId: { type: ["string", "null"] },
				previousStateId: { type: ["string", "null"] },
				version: { type: "integer" }
			}
		});
	}
	function makeRuntimeStoreIndexes(indexes) {
		return {
			widgetCount: 0,
			dataHandleCount: 0,
			linkCount: 0,
			widgetAdapterCount: 0,
			actionDescriptorCount: 0,
			perceptionDescriptorCount: 0,
			widgetPatchCount: 0,
			...indexes
		};
	}
	function describeRuntimeStoreIndexesSchema() {
		return cloneValue$9({
			type: "object",
			properties: {
				widgetCount: { type: "integer" },
				dataHandleCount: { type: "integer" },
				linkCount: { type: "integer" },
				widgetAdapterCount: { type: "integer" },
				actionDescriptorCount: { type: "integer" },
				perceptionDescriptorCount: { type: "integer" },
				widgetPatchCount: { type: "integer" }
			}
		});
	}
	function makeRuntimeStoreHistory(history) {
		return {
			snapshotCount: 0,
			traceCount: 0,
			responseCount: 0,
			branchCount: 0,
			retention: makeRuntimeStoreHistoryRetention(),
			maxSnapshotRetention: 0,
			maxTraceRetention: 0,
			maxResponseRetention: 0,
			...history
		};
	}
	function describeRuntimeStoreHistorySchema() {
		return cloneValue$9({
			type: "object",
			properties: {
				snapshotCount: { type: "integer" },
				traceCount: { type: "integer" },
				responseCount: { type: "integer" },
				branchCount: { type: "integer" },
				retention: describeRuntimeStoreRetentionSchema(),
				maxSnapshotRetention: { type: "integer" },
				maxTraceRetention: { type: "integer" },
				maxResponseRetention: { type: "integer" }
			}
		});
	}
	function makeRuntimeStoreCapabilities(capabilities) {
		return {
			deltaTracking: false,
			snapshotHistory: false,
			actorScopedHistory: false,
			branchReplay: false,
			traceGraph: false,
			runtimeDataIndex: false,
			adapterRegistry: false,
			widgetStatePatching: false,
			...capabilities
		};
	}
	function describeRuntimeStoreCapabilitiesSchema() {
		return cloneValue$9({
			type: "object",
			properties: {
				deltaTracking: { type: "boolean" },
				snapshotHistory: { type: "boolean" },
				actorScopedHistory: { type: "boolean" },
				branchReplay: { type: "boolean" },
				traceGraph: { type: "boolean" },
				runtimeDataIndex: { type: "boolean" },
				adapterRegistry: { type: "boolean" },
				widgetStatePatching: { type: "boolean" }
			}
		});
	}
	function makeRuntimeStoreSummary(summary) {
		return {
			currentStateSummary: null,
			...summary,
			identity: makeRuntimeStoreIdentity(summary?.identity),
			state: makeRuntimeStoreStateSummary(summary?.state),
			currentStateSummary: summary?.currentStateSummary == null ? null : makeRuntimeStoreCurrentStateSummary(summary.currentStateSummary),
			indexes: makeRuntimeStoreIndexes(summary?.indexes),
			history: makeRuntimeStoreHistory(summary?.history),
			capabilities: makeRuntimeStoreCapabilities(summary?.capabilities)
		};
	}
	function describeRuntimeStoreSummarySchema() {
		return cloneValue$9({
			type: "object",
			required: [
				"identity",
				"state",
				"indexes",
				"history",
				"capabilities"
			],
			properties: {
				identity: describeRuntimeStoreIdentitySchema(),
				state: describeRuntimeStoreStateSummarySchema(),
				currentStateSummary: describeRuntimeStoreCurrentStateSummarySchema(),
				indexes: describeRuntimeStoreIndexesSchema(),
				history: describeRuntimeStoreHistorySchema(),
				capabilities: describeRuntimeStoreCapabilitiesSchema()
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/widgetRegistry.js
	function cloneValue$8(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeWidgetRegistryCounts(counts) {
		return {
			widgetCount: 0,
			dataHandleCount: 0,
			linkCount: 0,
			adapterCount: 0,
			...counts
		};
	}
	function describeWidgetRegistryCountsSchema() {
		return cloneValue$8({
			type: "object",
			properties: {
				widgetCount: { type: "integer" },
				dataHandleCount: { type: "integer" },
				linkCount: { type: "integer" },
				adapterCount: { type: "integer" }
			}
		});
	}
	function makeWidgetRegistryRefs(refs) {
		return {
			widgetRefs: [],
			dataRefs: [],
			linkRefs: [],
			...refs
		};
	}
	function describeWidgetRegistryRefsSchema() {
		return cloneValue$8({
			type: "object",
			properties: {
				widgetRefs: {
					type: "array",
					items: { type: "string" }
				},
				dataRefs: {
					type: "array",
					items: { type: "string" }
				},
				linkRefs: {
					type: "array",
					items: { type: "string" }
				}
			}
		});
	}
	function makeWidgetRegistrySummary(summary) {
		return {
			widgets: [],
			dataHandles: [],
			links: [],
			...summary,
			counts: makeWidgetRegistryCounts(summary?.counts),
			refs: makeWidgetRegistryRefs(summary?.refs)
		};
	}
	function describeWidgetRegistryEntrySchema() {
		return cloneValue$8({
			type: "object",
			required: [
				"ref",
				"widgetId",
				"kind",
				"role"
			],
			properties: {
				ref: { type: "string" },
				widgetId: { type: "string" },
				kind: { type: "string" },
				role: { type: "string" },
				title: { type: "string" },
				description: { type: "string" },
				analyticRoles: {
					type: "array",
					items: { type: "string" }
				},
				sourceKind: { type: ["string", "null"] },
				supportsSpecMutation: { type: "boolean" },
				primaryDataRef: { type: ["string", "null"] },
				sourceDataRef: { type: ["string", "null"] },
				currentDataRef: { type: ["string", "null"] },
				usageNotes: {
					type: "array",
					items: { type: "string" }
				},
				actionNames: {
					type: "array",
					items: { type: "string" }
				},
				perceptionQueryNames: {
					type: "array",
					items: { type: "string" }
				},
				adapterProvider: { type: ["string", "null"] },
				providerCapabilities: { anyOf: [{ type: "null" }, describeWidgetAdapterProviderCapabilitiesSchema()] },
				adapterCapabilities: { anyOf: [{ type: "null" }, describeWidgetAdapterCapabilitiesSchema()] },
				humanInteractionMode: { type: ["string", "null"] },
				humanInteractionActionName: { type: ["string", "null"] },
				supportsDirectManipulation: { type: "boolean" }
			}
		});
	}
	function describeWidgetRegistrySummarySchema() {
		return cloneValue$8({
			type: "object",
			required: [
				"counts",
				"refs",
				"widgets",
				"dataHandles",
				"links"
			],
			properties: {
				counts: describeWidgetRegistryCountsSchema(),
				refs: describeWidgetRegistryRefsSchema(),
				widgets: {
					type: "array",
					items: describeWidgetRegistryEntrySchema()
				},
				dataHandles: {
					type: "array",
					items: describeDataHandleSchema()
				},
				links: {
					type: "array",
					items: describeWidgetLinkSchema()
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/perceptionRegistry.js
	function cloneValue$7(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makePerceptionRegistryCounts(counts) {
		return {
			descriptorCount: 0,
			handlerEntryCount: 0,
			...counts
		};
	}
	function describePerceptionRegistryCountsSchema() {
		return cloneValue$7({
			type: "object",
			properties: {
				descriptorCount: { type: "integer" },
				handlerEntryCount: { type: "integer" }
			}
		});
	}
	function makePerceptionRegistryCapabilities(capabilities) {
		return {
			paramsValidation: true,
			returnsValidation: true,
			traceRecording: false,
			linkPropagationEvidence: false,
			...capabilities
		};
	}
	function describePerceptionRegistryCapabilitiesSchema() {
		return cloneValue$7({
			type: "object",
			properties: {
				paramsValidation: { type: "boolean" },
				returnsValidation: { type: "boolean" },
				traceRecording: { type: "boolean" },
				linkPropagationEvidence: { type: "boolean" }
			}
		});
	}
	function makePerceptionRegistryQueryEntry(entry) {
		return {
			name: "",
			category: null,
			targetRef: null,
			handlerVariantCount: 0,
			sideEffectFree: true,
			evidenceKinds: [],
			verificationTargets: [],
			supportedWidgetKinds: null,
			...entry
		};
	}
	function describePerceptionRegistryQueryEntrySchema() {
		return cloneValue$7({
			type: "object",
			required: ["name"],
			properties: {
				name: { type: "string" },
				category: { type: ["string", "null"] },
				targetRef: { type: ["string", "null"] },
				handlerVariantCount: { type: "integer" },
				sideEffectFree: { type: "boolean" },
				evidenceKinds: {
					type: "array",
					items: { type: "string" }
				},
				verificationTargets: {
					type: "array",
					items: { type: "string" }
				},
				supportedWidgetKinds: { anyOf: [{ type: "null" }, {
					type: "array",
					items: { type: "string" }
				}] }
			}
		});
	}
	function makePerceptionRegistrySummary(summary) {
		return {
			queries: [],
			...summary,
			counts: makePerceptionRegistryCounts(summary?.counts),
			capabilities: makePerceptionRegistryCapabilities(summary?.capabilities),
			queries: Array.isArray(summary?.queries) ? summary.queries.map((query) => makePerceptionRegistryQueryEntry(query)) : []
		};
	}
	function describePerceptionRegistrySummarySchema() {
		return cloneValue$7({
			type: "object",
			required: [
				"counts",
				"capabilities",
				"queries"
			],
			properties: {
				counts: describePerceptionRegistryCountsSchema(),
				capabilities: describePerceptionRegistryCapabilitiesSchema(),
				queries: {
					type: "array",
					items: describePerceptionRegistryQueryEntrySchema()
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/dataQueryExecutor.js
	function cloneValue$6(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeDataQueryExecutorEngineSummary(summary) {
		return {
			kind: null,
			className: null,
			...summary
		};
	}
	function describeDataQueryExecutorEngineSummarySchema() {
		return cloneValue$6({
			type: "object",
			properties: {
				kind: { type: ["string", "null"] },
				className: { type: ["string", "null"] }
			}
		});
	}
	function makeDataQueryExecutorCounts(counts) {
		return {
			supportedQueryKindCount: 0,
			supportedQueryDescriptorCount: 0,
			...counts
		};
	}
	function describeDataQueryExecutorCountsSchema() {
		return cloneValue$6({
			type: "object",
			properties: {
				supportedQueryKindCount: { type: "integer" },
				supportedQueryDescriptorCount: { type: "integer" }
			}
		});
	}
	function makeDataQueryExecutorCapabilities(capabilities) {
		return {
			schemaValidation: true,
			returnsValidation: true,
			traceRecording: false,
			runtimeDataReads: false,
			...capabilities
		};
	}
	function describeDataQueryExecutorCapabilitiesSchema() {
		return cloneValue$6({
			type: "object",
			properties: {
				schemaValidation: { type: "boolean" },
				returnsValidation: { type: "boolean" },
				traceRecording: { type: "boolean" },
				runtimeDataReads: { type: "boolean" }
			}
		});
	}
	function makeDataQueryExecutorSummary(summary) {
		return {
			supportedQueryKinds: [],
			supportedQueryDescriptors: [],
			...summary,
			engine: makeDataQueryExecutorEngineSummary(summary?.engine),
			counts: makeDataQueryExecutorCounts(summary?.counts),
			capabilities: makeDataQueryExecutorCapabilities(summary?.capabilities)
		};
	}
	function describeDataQueryExecutorSummarySchema() {
		return cloneValue$6({
			type: "object",
			required: [
				"engine",
				"counts",
				"capabilities",
				"supportedQueryKinds",
				"supportedQueryDescriptors"
			],
			properties: {
				engine: describeDataQueryExecutorEngineSummarySchema(),
				counts: describeDataQueryExecutorCountsSchema(),
				capabilities: describeDataQueryExecutorCapabilitiesSchema(),
				supportedQueryKinds: {
					type: "array",
					items: { type: "string" }
				},
				supportedQueryDescriptors: {
					type: "array",
					items: describeDataQueryDescriptorSchema()
				}
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/stateManager.js
	function cloneValue$5(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeStateManagerCapabilities(capabilities) {
		return {
			stateIdGeneration: true,
			deltaCreation: true,
			statePatchCreation: true,
			refScopedReads: true,
			...capabilities
		};
	}
	function describeStateManagerCapabilitiesSchema() {
		return cloneValue$5({
			type: "object",
			properties: {
				stateIdGeneration: { type: "boolean" },
				deltaCreation: { type: "boolean" },
				statePatchCreation: { type: "boolean" },
				refScopedReads: { type: "boolean" }
			}
		});
	}
	function makeStateManagerCounters(counters) {
		return {
			generatedStateCount: 0,
			lastGeneratedStateId: null,
			...counters
		};
	}
	function describeStateManagerCountersSchema() {
		return cloneValue$5({
			type: "object",
			properties: {
				generatedStateCount: { type: "integer" },
				lastGeneratedStateId: { type: ["string", "null"] }
			}
		});
	}
	function makeStateManagerSummary(summary) {
		return {
			capabilities: makeStateManagerCapabilities(),
			reservedRefs: [],
			counters: makeStateManagerCounters(),
			...summary
		};
	}
	function describeStateManagerSummarySchema() {
		return cloneValue$5({
			type: "object",
			required: ["capabilities", "counters"],
			properties: {
				capabilities: describeStateManagerCapabilitiesSchema(),
				reservedRefs: {
					type: "array",
					items: { type: "string" }
				},
				counters: describeStateManagerCountersSchema()
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/traceRecorder.js
	function cloneValue$4(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeTraceRecorderCapabilities(capabilities) {
		return {
			recordsActions: true,
			recordsPerceptionQueries: true,
			recordsPerceptionQueryFailures: true,
			recordsDataQueries: true,
			recordsDataQueryFailures: true,
			normalizedQueryFamily: true,
			recordsSystemTransitions: true,
			lineageTracking: true,
			...capabilities
		};
	}
	function describeTraceRecorderCapabilitiesSchema() {
		return cloneValue$4({
			type: "object",
			properties: {
				recordsActions: { type: "boolean" },
				recordsPerceptionQueries: { type: "boolean" },
				recordsPerceptionQueryFailures: { type: "boolean" },
				recordsDataQueries: { type: "boolean" },
				recordsDataQueryFailures: { type: "boolean" },
				normalizedQueryFamily: { type: "boolean" },
				recordsSystemTransitions: { type: "boolean" },
				lineageTracking: { type: "boolean" }
			}
		});
	}
	function makeTraceRecorderCounters(counters) {
		return {
			traceCount: 0,
			latestStateId: null,
			latestBranchId: null,
			latestEventKind: null,
			latestEventFamily: null,
			latestQuerySurface: null,
			latestOutcome: null,
			...counters
		};
	}
	function describeTraceRecorderCountersSchema() {
		return cloneValue$4({
			type: "object",
			properties: {
				traceCount: { type: "integer" },
				latestStateId: { type: ["string", "null"] },
				latestBranchId: { type: ["string", "null"] },
				latestEventKind: { type: ["string", "null"] },
				latestEventFamily: { type: ["string", "null"] },
				latestQuerySurface: { type: ["string", "null"] },
				latestOutcome: { type: ["string", "null"] }
			}
		});
	}
	function makeTraceRecorderSummary(summary) {
		return {
			eventKinds: [],
			eventFamilies: [],
			querySurfaces: [],
			...summary,
			capabilities: makeTraceRecorderCapabilities(summary?.capabilities),
			counters: makeTraceRecorderCounters(summary?.counters)
		};
	}
	function describeTraceRecorderEventKindsSchema() {
		return cloneValue$4({
			type: "array",
			items: { type: "string" }
		});
	}
	function describeTraceRecorderEventFamiliesSchema() {
		return cloneValue$4({
			type: "array",
			items: { type: "string" }
		});
	}
	function describeTraceRecorderQuerySurfacesSchema() {
		return cloneValue$4({
			type: "array",
			items: { type: "string" }
		});
	}
	function describeTraceRecorderSummarySchema() {
		return cloneValue$4({
			type: "object",
			required: ["capabilities", "counters"],
			properties: {
				capabilities: describeTraceRecorderCapabilitiesSchema(),
				counters: describeTraceRecorderCountersSchema(),
				eventKinds: describeTraceRecorderEventKindsSchema(),
				eventFamilies: describeTraceRecorderEventFamiliesSchema(),
				querySurfaces: describeTraceRecorderQuerySurfacesSchema()
			}
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/protocol/pagePort.js
	var BOOLEAN_SCHEMA = { type: "boolean" };
	var STRING_SCHEMA = { type: "string" };
	var REF_ARRAY_SCHEMA = {
		type: "array",
		items: { type: "string" }
	};
	function cloneValue$3(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeMethodDescriptor(descriptor) {
		return {
			stability: "stable",
			aliases: [],
			inputSchema: {
				type: "object",
				additionalProperties: true,
				properties: {}
			},
			returns: {
				kind: "result",
				description: ""
			},
			errors: [],
			...descriptor
		};
	}
	function describePagePortAliasMapSchema() {
		return cloneValue$3({
			type: "object",
			additionalProperties: { type: "string" }
		});
	}
	function describePagePortErrorDescriptorSchema() {
		return cloneValue$3({
			type: "object",
			required: ["code", "description"],
			properties: {
				code: { type: "string" },
				description: { type: "string" }
			}
		});
	}
	function describePagePortErrorCatalogSchema() {
		return cloneValue$3({
			type: "object",
			additionalProperties: {
				type: "array",
				items: describePagePortErrorDescriptorSchema()
			}
		});
	}
	function describePagePortMethodReturnSchema() {
		return cloneValue$3({
			type: ["object", "null"],
			properties: {
				kind: { type: "string" },
				description: { type: "string" },
				schema: {}
			}
		});
	}
	function describePagePortMethodExampleSchema() {
		return cloneValue$3({
			type: "object",
			properties: {
				input: {},
				output: {},
				note: { type: "string" }
			}
		});
	}
	function describePagePortMethodDescriptorSchema() {
		return cloneValue$3({
			type: "object",
			required: [
				"stability",
				"aliases",
				"inputSchema",
				"returns",
				"errors"
			],
			properties: {
				stability: { type: "string" },
				aliases: {
					type: "array",
					items: { type: "string" }
				},
				inputSchema: { type: ["object", "null"] },
				returns: describePagePortMethodReturnSchema(),
				errors: {
					type: "array",
					items: describePagePortErrorDescriptorSchema()
				},
				description: { type: "string" },
				examples: {
					type: "array",
					items: describePagePortMethodExampleSchema()
				}
			}
		});
	}
	function describePagePortMethodDescriptorMapSchema() {
		return cloneValue$3({
			type: "object",
			additionalProperties: describePagePortMethodDescriptorSchema()
		});
	}
	function makePagePortTransportHints(transportHints) {
		return {
			recommendedTools: [
				"workspace_describe",
				"view_read",
				"action_run",
				"perception_query",
				"interaction_trace_read"
			],
			optionalTools: [
				"data_query",
				"workspace_plan",
				"agent_loop_describe",
				"action_usage_describe",
				"trace_graph_read",
				"read_snapshot",
				"state_history_read",
				"branch_list",
				"verified_action_run",
				"jump_to_state",
				"branch_from_state",
				"link_propagation_evaluate",
				"response_recorder_describe",
				"agent_response_read",
				"agent_response_list",
				"agent_response_record"
			],
			note: "External transports should prefer the stable workspace/view/action/perception/trace surface.",
			...transportHints
		};
	}
	function makePagePortMethodDescriptor(descriptor) {
		return {
			stability: "stable",
			aliases: [],
			inputSchema: {
				type: "object",
				additionalProperties: true,
				properties: {}
			},
			returns: {
				kind: "result",
				description: ""
			},
			errors: [],
			...descriptor,
			aliases: Array.isArray(descriptor?.aliases) ? cloneValue$3(descriptor.aliases) : [],
			inputSchema: descriptor?.inputSchema ? cloneValue$3(descriptor.inputSchema) : {
				type: "object",
				additionalProperties: true,
				properties: {}
			},
			returns: descriptor?.returns ? cloneValue$3(descriptor.returns) : {
				kind: "result",
				description: ""
			},
			errors: Array.isArray(descriptor?.errors) ? cloneValue$3(descriptor.errors) : []
		};
	}
	function makePagePortDescription(description = {}) {
		return {
			version: "1.0.0",
			methods: [...PAGE_PORT_METHODS],
			aliases: cloneValue$3(PAGE_PORT_ALIASES),
			transportHints: makePagePortTransportHints(),
			methodDescriptors: Object.fromEntries(PAGE_PORT_METHODS.map((name) => [name, makePagePortMethodDescriptor(PAGE_PORT_METHOD_DESCRIPTORS[name])])),
			errorCatalog: cloneValue$3(PAGE_PORT_ERROR_CODES),
			schemas: describePagePortSchemasForMethods(PAGE_PORT_METHODS),
			...description,
			...description?.transportHints ? { transportHints: makePagePortTransportHints(description.transportHints) } : {},
			...description?.methodDescriptors ? { methodDescriptors: Object.fromEntries(Object.entries(description.methodDescriptors).map(([name, descriptor]) => [name, makePagePortMethodDescriptor(descriptor)])) } : {}
		};
	}
	function describePagePortDescriptionSchema() {
		return cloneValue$3({
			type: "object",
			required: [
				"version",
				"methods",
				"aliases",
				"methodDescriptors",
				"errorCatalog",
				"schemas",
				"transportHints"
			],
			properties: {
				version: { type: "string" },
				methods: {
					type: "array",
					items: { type: "string" }
				},
				aliases: describePagePortAliasMapSchema(),
				methodDescriptors: describePagePortMethodDescriptorMapSchema(),
				errorCatalog: describePagePortErrorCatalogSchema(),
				transportHints: describeWorkspaceTransportHintsSchema(),
				schemas: {
					type: "object",
					additionalProperties: {}
				},
				widgetAdapterIntrospection: { type: "boolean" },
				runtimeCoreIntrospection: { type: "boolean" },
				planner: { type: "boolean" },
				agentLoop: { type: "boolean" }
			}
		});
	}
	var PAGE_PORT_METHODS = [
		"describePagePort",
		"describeWorkspace",
		"parseRef",
		"describeRuntimeCore",
		"describeRuntimeStore",
		"describeWidgetRegistry",
		"describeActionExecutor",
		"describeActionContext",
		"describeActionUsage",
		"describePerceptionRegistry",
		"describePerceptionContext",
		"describeDataQueryExecutor",
		"describeDataQueryContext",
		"describeDataQueryEngine",
		"describeStateManager",
		"describeTraceRecorder",
		"describeResponseRecorder",
		"describeLinkEngine",
		"planWorkspace",
		"describeAgentLoop",
		"listWidgetAdapters",
		"readObservation",
		"readCoordinationState",
		"readPropagationSummary",
		"readLatestCoordinationResult",
		"listAvailableActions",
		"listAvailablePerceptions",
		"readState",
		"readView",
		"readSnapshot",
		"listStateHistory",
		"listBranches",
		"executeAction",
		"executeVerifiedAction",
		"jumpToState",
		"branchFromState",
		"queryPerception",
		"runDataQuery",
		"queryData",
		"readTrace",
		"getInteractionTrace",
		"replay",
		"getTraceGraph",
		"getLatestAgentResponse",
		"listAgentResponses",
		"evaluateLinkPropagation",
		"recordAgentResponse"
	];
	var PAGE_PORT_ALIASES = {
		page_port_describe: "describePagePort",
		workspace_describe: "describeWorkspace",
		ref_parse: "parseRef",
		runtime_core_describe: "describeRuntimeCore",
		runtime_store_describe: "describeRuntimeStore",
		widget_registry_describe: "describeWidgetRegistry",
		action_executor_describe: "describeActionExecutor",
		action_context_describe: "describeActionContext",
		action_usage_describe: "describeActionUsage",
		perception_registry_describe: "describePerceptionRegistry",
		perception_context_describe: "describePerceptionContext",
		data_query_executor_describe: "describeDataQueryExecutor",
		data_query_context_describe: "describeDataQueryContext",
		data_query_engine_describe: "describeDataQueryEngine",
		state_manager_describe: "describeStateManager",
		trace_recorder_describe: "describeTraceRecorder",
		response_recorder_describe: "describeResponseRecorder",
		link_engine_describe: "describeLinkEngine",
		workspace_plan: "planWorkspace",
		agent_loop_describe: "describeAgentLoop",
		widget_adapter_list: "listWidgetAdapters",
		observation_read: "readObservation",
		coordination_state_read: "readCoordinationState",
		propagation_summary_read: "readPropagationSummary",
		latest_coordination_result_read: "readLatestCoordinationResult",
		available_actions_list: "listAvailableActions",
		available_perceptions_list: "listAvailablePerceptions",
		state_read: "readState",
		view_read: "readView",
		read_snapshot: "readSnapshot",
		state_history_read: "listStateHistory",
		branch_list: "listBranches",
		action_run: "executeAction",
		jump_to_state: "jumpToState",
		branch_from_state: "branchFromState",
		verified_action_run: "executeVerifiedAction",
		perception_query: "queryPerception",
		data_query_run: "runDataQuery",
		data_query: "queryData",
		trace_read: "readTrace",
		interaction_trace_read: "getInteractionTrace",
		workspace_replay: "replay",
		trace_graph_read: "getTraceGraph",
		agent_response_read: "getLatestAgentResponse",
		agent_response_list: "listAgentResponses",
		link_propagation_evaluate: "evaluateLinkPropagation",
		agent_response_record: "recordAgentResponse"
	};
	var PAGE_PORT_ERROR_CODES = {
		pagePort: [{
			code: "METHOD_NOT_INSTALLED",
			description: "The requested page-port method is unavailable on the current page runtime."
		}],
		action: [
			{
				code: "UNKNOWN_OPERATION",
				description: "The requested WidgetVA action name is not registered in the current runtime."
			},
			{
				code: "UNSUPPORTED_TARGET",
				description: "The requested action exists but is not declared for the target widget or ref."
			},
			{
				code: "INVALID_PARAMS",
				description: "The action call failed params-schema validation before execution."
			},
			{
				code: "PRECONDITION_FAILED",
				description: "The action is declared, but the current runtime state does not satisfy its documented preconditions."
			},
			{
				code: "RUNTIME_ERROR",
				description: "The action handler threw during execution."
			}
		],
		perception: [
			{
				code: "UNKNOWN_QUERY",
				description: "The requested perception query name is not registered in the current runtime."
			},
			{
				code: "INVALID_PARAMS",
				description: "The perception query failed params-schema validation before execution."
			},
			{
				code: "RUNTIME_ERROR",
				description: "The perception handler threw during execution."
			}
		],
		dataQuery: [
			{
				code: "UNSUPPORTED_TARGET",
				description: "The requested data-query target ref does not resolve to a materialized widget or data handle."
			},
			{
				code: "UNKNOWN_DATA_REF",
				description: "The requested data ref is not materialized in the current runtime store."
			},
			{
				code: "UNKNOWN_QUERY_KIND",
				description: "The requested data query kind is unsupported by WidgetVA."
			},
			{
				code: "UNSUPPORTED_QUERY_KIND",
				description: "The requested data query kind is valid globally but not exposed by the current data handle."
			},
			{
				code: "INVALID_QUERY_SPEC",
				description: "The data query spec failed schema validation before execution."
			}
		]
	};
	var PAGE_PORT_METHOD_DESCRIPTORS = {
		describePagePort: makeMethodDescriptor({
			description: "Describe the installed WidgetVA page port, including method catalog, aliases, schemas, and error semantics.",
			returns: {
				kind: "pagePortDescription",
				description: "A stable description of the external WidgetVA page API.",
				schema: describePagePortDescriptionSchema()
			},
			examples: [{
				userGoal: "Discover the stable WidgetVA page-port surface before connecting an external agent.",
				input: {}
			}]
		}),
		describeWorkspace: makeMethodDescriptor({
			aliases: ["workspace_describe"],
			description: "Describe the current workspace including widgets, data handles, links, actions, and perception queries.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					includeSchemas: BOOLEAN_SCHEMA,
					includeExamples: BOOLEAN_SCHEMA,
					includeProtocolSchemas: BOOLEAN_SCHEMA
				}
			},
			returns: {
				kind: "workspaceDescription",
				description: "A workspace-level capability description with widget and runtime metadata.",
				schema: describeWorkspaceDescriptionSchema()
			}
		}),
		parseRef: makeMethodDescriptor({
			aliases: ["ref_parse"],
			description: "Parse a WidgetVA ref string into structured ref parts.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { ref: describeRefSchema() },
				required: ["ref"]
			},
			returns: {
				kind: "refParts",
				description: "Structured app/workspace/kind/id components parsed from a WidgetVA ref.",
				schema: { anyOf: [describeRefPartsSchema(), { type: "null" }] }
			}
		}),
		describeRuntimeCore: makeMethodDescriptor({
			aliases: ["runtime_core_describe"],
			description: "Describe the installed WidgetVA runtime core components, handler registries, and protocol-level execution capabilities.",
			returns: {
				kind: "runtimeCoreSummary",
				description: "A compact summary of the runtime core component graph and registered execution/query/link capabilities.",
				schema: describeRuntimeCoreSummarySchema()
			}
		}),
		describeRuntimeStore: makeMethodDescriptor({
			aliases: ["runtime_store_describe"],
			description: "Describe the current runtime-store indexes, history buffers, and replay capabilities.",
			returns: {
				kind: "runtimeStoreSummary",
				description: "A compact summary of runtime store state, registries, and replay/trace capacities.",
				schema: describeRuntimeStoreSummarySchema()
			}
		}),
		describeWidgetRegistry: makeMethodDescriptor({
			aliases: ["widget_registry_describe"],
			description: "Describe the materialized widget/data/link/adapter registry maintained by the runtime core.",
			returns: {
				kind: "widgetRegistrySummary",
				description: "A compact registry summary covering widget refs, data refs, link refs, and per-widget registry entries.",
				schema: describeWidgetRegistrySummarySchema()
			}
		}),
		describeActionExecutor: makeMethodDescriptor({
			aliases: ["action_executor_describe"],
			description: "Describe the installed ActionExecutor, including registered actions, handlers, and precondition coverage.",
			returns: {
				kind: "actionExecutorSummary",
				description: "A compact summary of ActionExecutor registrations, capabilities, and action surface.",
				schema: describeActionExecutorSummarySchema()
			}
		}),
		describeActionContext: makeMethodDescriptor({
			aliases: ["action_context_describe"],
			description: "Describe the ActionContext contract that action handlers receive at runtime.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {}
			},
			returns: {
				kind: "actionContextSummary",
				description: "A stable summary of ActionContext methods, capabilities, and integrations.",
				schema: describeActionContextSummarySchema()
			}
		}),
		describeActionUsage: makeMethodDescriptor({
			aliases: ["action_usage_describe"],
			description: "Describe how one action should be used on the current widget, including inferred fields, recommended params, and diagnostic guidance.",
			inputSchema: describeActionUsageRequestSchema(),
			returns: {
				kind: "actionUsageSummary",
				description: "Widget-scoped action-usage guidance with inferred field roles, recommended params, and diagnostics.",
				schema: describeActionUsageSummarySchema()
			}
		}),
		describePerceptionRegistry: makeMethodDescriptor({
			aliases: ["perception_registry_describe"],
			description: "Describe the installed PerceptionQueryRegistry, including registered query handlers and supported widget-kind routing.",
			returns: {
				kind: "perceptionRegistrySummary",
				description: "A compact summary of perception-query registrations and evidence capabilities.",
				schema: describePerceptionRegistrySummarySchema()
			}
		}),
		describePerceptionContext: makeMethodDescriptor({
			aliases: ["perception_context_describe"],
			description: "Describe the PerceptionContext contract that perception-query handlers receive at runtime.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {}
			},
			returns: {
				kind: "perceptionContextSummary",
				description: "A stable summary of PerceptionContext methods, capabilities, and integrations.",
				schema: describePerceptionContextSummarySchema()
			}
		}),
		describeDataQueryExecutor: makeMethodDescriptor({
			aliases: ["data_query_executor_describe"],
			description: "Describe the installed DataQueryExecutor and its backing query-engine surface.",
			returns: {
				kind: "dataQueryExecutorSummary",
				description: "A compact summary of supported data-query kinds and query-engine capabilities.",
				schema: describeDataQueryExecutorSummarySchema()
			}
		}),
		describeDataQueryContext: makeMethodDescriptor({
			aliases: ["data_query_context_describe"],
			description: "Describe the DataQueryContext contract that data-query executions receive at runtime.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {}
			},
			returns: {
				kind: "dataQueryContextSummary",
				description: "A stable summary of DataQueryContext methods, capabilities, and integrations.",
				schema: describeDataQueryContextSummarySchema()
			}
		}),
		describeDataQueryEngine: makeMethodDescriptor({
			aliases: ["data_query_engine_describe"],
			description: "Describe the backing DataQueryEngine implementation and its supported query capabilities.",
			returns: {
				kind: "dataQueryEngineSummary",
				description: "A compact summary of the underlying data query engine implementation, supported query kinds, and execution capabilities.",
				schema: describeDataQueryEngineSummarySchema()
			}
		}),
		describeStateManager: makeMethodDescriptor({
			aliases: ["state_manager_describe"],
			description: "Describe the installed StateManager capabilities and state-id generation counters.",
			returns: {
				kind: "stateManagerSummary",
				description: "A compact summary of StateManager responsibilities and generated state-id counters.",
				schema: describeStateManagerSummarySchema()
			}
		}),
		describeTraceRecorder: makeMethodDescriptor({
			aliases: ["trace_recorder_describe"],
			description: "Describe the installed InteractionTraceRecorder capabilities and current trace counters.",
			returns: {
				kind: "traceRecorderSummary",
				description: "A compact summary of unified trace-recording capabilities and current interaction-trace counts.",
				schema: describeTraceRecorderSummarySchema()
			}
		}),
		describeResponseRecorder: makeMethodDescriptor({
			aliases: ["response_recorder_describe"],
			description: "Describe the runtime response recorder used for final-answer capture and retrieval.",
			returns: {
				kind: "responseRecorderSummary",
				description: "Summary of final-response recording capabilities and counters.",
				schema: describeResponseRecorderSummarySchema()
			}
		}),
		describeLinkEngine: makeMethodDescriptor({
			aliases: ["link_engine_describe"],
			description: "Describe the current LinkEngine primitive registry and materialized workspace link counts.",
			returns: {
				kind: "linkEngineSummary",
				description: "A compact summary of available link primitives and current workspace link topology size.",
				schema: describeLinkEngineSummarySchema()
			}
		}),
		planWorkspace: makeMethodDescriptor({
			aliases: ["workspace_plan"],
			description: "Generate a workspace topology plan for the current specification and task context.",
			inputSchema: describeWorkspacePlanningRequestSchema(),
			returns: {
				kind: "workspacePlanningResult",
				description: "A planner-generated workspace topology with widgets, links, and rationale.",
				schema: describeWorkspacePlanningResultSchema()
			}
		}),
		describeAgentLoop: makeMethodDescriptor({
			aliases: ["agent_loop_describe"],
			description: "Read a runtime-prepared observe-plan-act-verify-reason context bundle.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { viewOptions: {
					type: "object",
					additionalProperties: false,
					properties: {
						refs: REF_ARRAY_SCHEMA,
						deltaSince: STRING_SCHEMA
					}
				} }
			},
			returns: {
				kind: "agentLoopContext",
				description: "A structured runtime bundle for closed-loop action planning and verification.",
				schema: describeAgentLoopContextSchema()
			}
		}),
		listWidgetAdapters: makeMethodDescriptor({
			aliases: ["widget_adapter_list"],
			description: "List materialized widget adapter instances and their provider/human-interaction capabilities.",
			returns: {
				kind: "widgetAdapterSummary[]",
				description: "Per-widget adapter metadata for runtime introspection.",
				schema: {
					type: "array",
					items: describeWidgetAdapterSummarySchema()
				}
			}
		}),
		readObservation: makeMethodDescriptor({
			aliases: ["observation_read"],
			description: "Read a workspace-level observation envelope with workspace description, current state, coordination state, action/perception surfaces, and propagation summary.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					readStateOptions: {
						type: "object",
						additionalProperties: false,
						properties: {
							refs: REF_ARRAY_SCHEMA,
							deltaSince: STRING_SCHEMA
						}
					},
					propagationOptions: {
						type: "object",
						additionalProperties: false,
						properties: { sourceRef: STRING_SCHEMA }
					}
				}
			},
			returns: {
				kind: "runtimeObservation",
				description: "A workspace-level observation bundle for agent-facing environment reads.",
				schema: {
					type: "object",
					additionalProperties: true
				}
			}
		}),
		readCoordinationState: makeMethodDescriptor({
			aliases: ["coordination_state_read"],
			description: "Read the current shared coordination state including focus, selections, highlight, viewport, filters, and annotations.",
			returns: {
				kind: "coordinationState",
				description: "A shared coordination-state projection derived from the runtime workspace state.",
				schema: {
					type: "object",
					additionalProperties: true
				}
			}
		}),
		readPropagationSummary: makeMethodDescriptor({
			aliases: ["propagation_summary_read"],
			description: "Read a propagation summary over workspace links, optionally centered on one source ref.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { sourceRef: STRING_SCHEMA }
			},
			returns: {
				kind: "propagationSummary",
				description: "A propagation summary bundle including candidate source refs, outgoing propagation entries, and optional evaluation results.",
				schema: {
					type: "object",
					additionalProperties: true
				}
			}
		}),
		readLatestCoordinationResult: makeMethodDescriptor({
			aliases: ["latest_coordination_result_read"],
			description: "Read the most recent coordination-driving runtime result bundle, including propagation and verification summaries when available.",
			returns: {
				kind: "latestCoordinationResult",
				description: "An ephemeral runtime result bundle for the most recent coordination-driving interaction.",
				schema: {
					type: ["object", "null"],
					additionalProperties: true
				}
			}
		}),
		listAvailableActions: makeMethodDescriptor({
			aliases: ["available_actions_list"],
			description: "List the currently available workspace and widget action descriptors.",
			returns: {
				kind: "actionDescriptor[]",
				description: "Available action descriptors exposed by the current runtime workspace.",
				schema: {
					type: "array",
					items: describeActionDescriptorSchema()
				}
			}
		}),
		listAvailablePerceptions: makeMethodDescriptor({
			aliases: ["available_perceptions_list"],
			description: "List the currently available perception-query descriptors.",
			returns: {
				kind: "perceptionDescriptor[]",
				description: "Available perception descriptors exposed by the current runtime workspace.",
				schema: {
					type: "array",
					items: describePerceptionDescriptorSchema()
				}
			}
		}),
		readState: makeMethodDescriptor({
			aliases: ["state_read"],
			description: "Read the current workspace state or a delta restricted to selected refs using the stable agent-facing state surface.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					refs: REF_ARRAY_SCHEMA,
					deltaSince: STRING_SCHEMA
				}
			},
			returns: {
				kind: "workspaceState",
				description: "The current workspace state snapshot, optionally scoped by refs and/or delta base.",
				schema: describeWorkspaceStateSchema()
			}
		}),
		readView: makeMethodDescriptor({
			aliases: ["view_read"],
			description: "Read the current workspace state or a delta restricted to selected refs.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					refs: REF_ARRAY_SCHEMA,
					deltaSince: STRING_SCHEMA
				}
			},
			returns: {
				kind: "workspaceState",
				description: "The current workspace state snapshot, optionally scoped by refs and/or delta base.",
				schema: describeWorkspaceStateSchema()
			}
		}),
		readSnapshot: makeMethodDescriptor({
			aliases: ["read_snapshot"],
			description: "Read a historical workspace snapshot by stateId, optionally scoped to refs and enriched with metadata.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					stateId: STRING_SCHEMA,
					refs: REF_ARRAY_SCHEMA,
					includeMeta: BOOLEAN_SCHEMA
				},
				required: ["stateId"]
			},
			returns: {
				kind: "workspaceSnapshot",
				description: "A historical workspace snapshot, optionally annotated with branch and transition metadata.",
				schema: describeWorkspaceSnapshotSchema()
			}
		}),
		listStateHistory: makeMethodDescriptor({
			aliases: ["state_history_read"],
			description: "List recent runtime state snapshots in reverse chronological order.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					limit: {
						type: "integer",
						minimum: 1,
						maximum: 500
					},
					sinceStateId: STRING_SCHEMA,
					actors: {
						type: "array",
						items: describeRuntimeActorSchema()
					}
				}
			},
			returns: {
				kind: "stateSnapshotMeta[]",
				description: "Recent state-history entries with branch/transition metadata.",
				schema: {
					type: "array",
					items: describeStateSnapshotMetaSchema()
				}
			}
		}),
		listBranches: makeMethodDescriptor({
			aliases: ["branch_list"],
			description: "List known runtime branches and their lineage metadata.",
			returns: {
				kind: "branchSummary[]",
				description: "Branch records maintained by the runtime trace store.",
				schema: {
					type: "array",
					items: describeBranchSummarySchema()
				}
			}
		}),
		getFinalWorkspaceSnapshot: makeMethodDescriptor({
			aliases: ["workspace_snapshot_read"],
			description: "Read the latest workspace snapshot for replay, inspection, or runtime history workflows.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { refs: REF_ARRAY_SCHEMA }
			},
			returns: {
				kind: "workspaceSnapshot",
				description: "The latest workspace snapshot after current runtime state synchronization.",
				schema: describeWorkspaceSnapshotSchema()
			}
		}),
		executeAction: makeMethodDescriptor({
			aliases: ["action_run"],
			description: "Execute an intent-level WidgetVA action against a target ref or widget.",
			inputSchema: describeActionCallSchema(),
			returns: {
				kind: "actionResult",
				description: "An action execution envelope with updated refs, state patch, result payload, and verification hints.",
				schema: describeActionResultSchema()
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		executeVerifiedAction: makeMethodDescriptor({
			aliases: ["verified_action_run"],
			description: "Execute an action and immediately return runtime verification evidence.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					call: describeActionCallSchema(),
					options: {
						type: "object",
						additionalProperties: false,
						properties: {
							verify: BOOLEAN_SCHEMA,
							includeDeltaSince: BOOLEAN_SCHEMA,
							includeFinalSnapshotRefs: BOOLEAN_SCHEMA
						}
					}
				},
				required: ["call"]
			},
			returns: {
				kind: "verifiedActionResult",
				description: "An action execution envelope augmented with post-action verification evidence.",
				schema: describeVerifiedActionResultSchema()
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		jumpToState: makeMethodDescriptor({
			aliases: ["jump_to_state"],
			description: "Jump the runtime back to a historical state and replay it into the live workspace.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					callId: STRING_SCHEMA,
					actor: describeRuntimeActorSchema(),
					stateId: STRING_SCHEMA
				},
				required: ["stateId"]
			},
			returns: {
				kind: "actionResult",
				description: "A workspace.jumpToState action result.",
				schema: describeActionResultSchema()
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		branchFromState: makeMethodDescriptor({
			aliases: ["branch_from_state"],
			description: "Create and switch to a new runtime branch seeded from a historical state.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					callId: STRING_SCHEMA,
					actor: describeRuntimeActorSchema(),
					stateId: STRING_SCHEMA,
					branchLabel: STRING_SCHEMA
				},
				required: ["stateId"]
			},
			returns: {
				kind: "actionResult",
				description: "A workspace.branchFromState action result.",
				schema: describeActionResultSchema()
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		queryPerception: makeMethodDescriptor({
			aliases: ["perception_query"],
			description: "Execute a WidgetVA perception query against the current view or a target widget/data ref.",
			inputSchema: describePerceptionQueryCallSchema(),
			returns: {
				kind: "perceptionResult",
				description: "A perception query envelope carrying structured evidence.",
				schema: describePerceptionResultSchema()
			},
			errors: PAGE_PORT_ERROR_CODES.perception
		}),
		runDataQuery: makeMethodDescriptor({
			aliases: ["data_query_run"],
			description: "Execute a lower-level WidgetVA data query against a materialized data handle using the stable agent-facing act surface.",
			inputSchema: describeDataQueryCallSchema(),
			returns: {
				kind: "dataQueryResult",
				description: "A data-query envelope with rows, aggregates, or diagnostics depending on query kind.",
				schema: describeDataQueryResultSchema()
			},
			errors: PAGE_PORT_ERROR_CODES.dataQuery
		}),
		queryData: makeMethodDescriptor({
			aliases: ["data_query"],
			description: "Execute a lower-level WidgetVA data query against a materialized data handle.",
			inputSchema: describeDataQueryCallSchema(),
			returns: {
				kind: "dataQueryResult",
				description: "A data-query envelope with rows, aggregates, or diagnostics depending on query kind.",
				schema: describeDataQueryResultSchema()
			},
			errors: PAGE_PORT_ERROR_CODES.dataQuery
		}),
		readTrace: makeMethodDescriptor({
			aliases: ["trace_read"],
			description: "Read the unified human+agent interaction trace from the runtime store using the stable verification surface.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					limit: {
						type: "integer",
						minimum: 1,
						maximum: 1e3
					},
					sinceStateId: STRING_SCHEMA,
					actors: {
						type: "array",
						items: describeRuntimeActorSchema()
					}
				}
			},
			returns: {
				kind: "interactionTraceRecord[]",
				description: "Recent runtime interaction trace records.",
				schema: {
					type: "array",
					items: describeInteractionTraceRecordSchema()
				}
			}
		}),
		getInteractionTrace: makeMethodDescriptor({
			aliases: ["interaction_trace_read"],
			description: "Read the unified human+agent interaction trace from the runtime store.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					limit: {
						type: "integer",
						minimum: 1,
						maximum: 1e3
					},
					sinceStateId: STRING_SCHEMA,
					actors: {
						type: "array",
						items: describeRuntimeActorSchema()
					}
				}
			},
			returns: {
				kind: "interactionTraceRecord[]",
				description: "Recent runtime interaction trace records.",
				schema: {
					type: "array",
					items: describeInteractionTraceRecordSchema()
				}
			}
		}),
		replay: makeMethodDescriptor({
			aliases: ["workspace_replay"],
			description: "Replay a historical state into the live workspace using the stable replay surface.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					callId: STRING_SCHEMA,
					actor: describeRuntimeActorSchema(),
					stateId: STRING_SCHEMA
				},
				required: ["stateId"]
			},
			returns: {
				kind: "actionResult",
				description: "A workspace.jumpToState action result returned through the stable replay surface.",
				schema: describeActionResultSchema()
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		getTraceGraph: makeMethodDescriptor({
			aliases: ["trace_graph_read"],
			description: "Read the runtime trace graph with branch and transition relationships.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					limit: {
						type: "integer",
						minimum: 1,
						maximum: 1e3
					},
					sinceStateId: STRING_SCHEMA,
					actors: {
						type: "array",
						items: describeRuntimeActorSchema()
					}
				}
			},
			returns: {
				kind: "traceGraph",
				description: "A graph-friendly projection of runtime states and transitions.",
				schema: describeTraceGraphSchema()
			}
		}),
		getLatestAgentResponse: makeMethodDescriptor({
			aliases: ["agent_response_read"],
			description: "Read the latest recorded final response for the current or specified workspace.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { workspaceId: STRING_SCHEMA }
			},
			returns: {
				kind: "agentResponseRecord",
				description: "Latest recorded final response, if available.",
				schema: { anyOf: [describeAgentResponseRecordSchema(), { type: "null" }] }
			}
		}),
		listAgentResponses: makeMethodDescriptor({
			aliases: ["agent_response_list"],
			description: "List recent recorded final responses for the current or specified workspace.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					limit: {
						type: "integer",
						minimum: 1,
						maximum: 1e3
					},
					workspaceId: STRING_SCHEMA
				}
			},
			returns: {
				kind: "agentResponseRecord[]",
				description: "Recent recorded final responses for the current or specified workspace.",
				schema: {
					type: "array",
					items: describeAgentResponseRecordSchema()
				}
			}
		}),
		evaluateLinkPropagation: makeMethodDescriptor({
			aliases: ["link_propagation_evaluate"],
			description: "Evaluate the current propagation consistency for all outgoing links from a given source ref.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { sourceRef: STRING_SCHEMA },
				required: ["sourceRef"]
			},
			returns: {
				kind: "linkPropagationEvaluation",
				description: "Per-link consistency checks for runtime propagation semantics.",
				schema: describeLinkPropagationEvaluationSchema()
			}
		}),
		recordAgentResponse: makeMethodDescriptor({
			aliases: ["agent_response_record"],
			description: "Record a final response for the current workspace, produced by the agent or another runtime actor, for later history and inspection workflows.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				required: ["content"],
				properties: {
					responseId: STRING_SCHEMA,
					runId: STRING_SCHEMA,
					sessionId: STRING_SCHEMA,
					actor: describeRuntimeActorSchema(),
					mode: STRING_SCHEMA,
					query: STRING_SCHEMA,
					content: STRING_SCHEMA,
					evidenceRefs: REF_ARRAY_SCHEMA
				}
			},
			returns: {
				kind: "agentResponseRecord",
				description: "The recorded final-response record with runtime lineage metadata.",
				schema: describeAgentResponseRecordSchema()
			}
		})
	};
	var PAGE_PORT_METHOD_SCHEMA_KEYS = {
		describePagePort: ["pagePortDescription"],
		describeWorkspace: ["workspaceDescription"],
		parseRef: ["refParts"],
		describeRuntimeCore: ["runtimeCoreSummary"],
		describeRuntimeStore: ["runtimeStoreSummary"],
		describeWidgetRegistry: ["widgetRegistrySummary"],
		describeActionExecutor: ["actionExecutorSummary"],
		describeActionContext: ["actionContextSummary"],
		describeActionUsage: [
			"actionUsageSummary",
			"actionUsageSpecContext",
			"actionUsageFieldRoles",
			"actionUsageFieldRoleMap",
			"actionUsageFieldCandidates",
			"actionUsageFieldExplanation",
			"actionUsageParamRole",
			"actionUsageParamRoles",
			"actionUsageEntry"
		],
		describePerceptionRegistry: ["perceptionRegistrySummary"],
		describePerceptionContext: ["perceptionContextSummary"],
		describeDataQueryExecutor: ["dataQueryExecutorSummary"],
		describeDataQueryContext: ["dataQueryContextSummary"],
		describeDataQueryEngine: ["dataQueryEngineSummary"],
		describeStateManager: ["stateManagerSummary"],
		describeTraceRecorder: ["traceRecorderSummary"],
		describeResponseRecorder: ["responseRecorderSummary"],
		describeLinkEngine: ["linkEngineSummary"],
		planWorkspace: ["workspacePlanningRequest", "workspacePlanningResult"],
		describeAgentLoop: ["agentLoopContext"],
		listWidgetAdapters: ["widgetAdapterSummary"],
		readObservation: [],
		readCoordinationState: [],
		readPropagationSummary: [],
		readLatestCoordinationResult: [],
		listAvailableActions: ["actionDescriptor"],
		listAvailablePerceptions: ["perceptionDescriptor"],
		readState: ["workspaceState"],
		readView: ["workspaceState"],
		readSnapshot: ["workspaceSnapshot"],
		listStateHistory: ["stateSnapshotMeta"],
		listBranches: ["branchSummary"],
		getFinalWorkspaceSnapshot: ["workspaceSnapshot"],
		executeAction: ["actionCall", "actionResult"],
		executeVerifiedAction: ["verifiedActionResult"],
		jumpToState: ["actionResult"],
		branchFromState: ["actionResult"],
		queryPerception: ["perceptionQueryCall", "perceptionResult"],
		runDataQuery: ["dataQueryCall", "dataQueryResult"],
		queryData: ["dataQueryCall", "dataQueryResult"],
		readTrace: ["interactionTraceRecord"],
		getInteractionTrace: ["interactionTraceRecord"],
		replay: ["actionResult"],
		getTraceGraph: ["traceGraph"],
		getLatestAgentResponse: ["agentResponseRecord"],
		listAgentResponses: ["agentResponseRecord"],
		evaluateLinkPropagation: ["linkPropagationEvaluation"],
		recordAgentResponse: ["agentResponseRecord"]
	};
	function describePagePortSchemas() {
		return cloneValue$3({
			pagePortDescription: describePagePortDescriptionSchema(),
			pagePortErrorDescriptor: describePagePortErrorDescriptorSchema(),
			pagePortMethodReturn: describePagePortMethodReturnSchema(),
			pagePortMethodExample: describePagePortMethodExampleSchema(),
			pagePortMethodDescriptor: describePagePortMethodDescriptorSchema(),
			pagePortMethodDescriptorMap: describePagePortMethodDescriptorMapSchema(),
			pagePortAliasMap: describePagePortAliasMapSchema(),
			pagePortErrorCatalog: describePagePortErrorCatalogSchema(),
			runtimeActor: describeRuntimeActorSchema(),
			widgetDescription: describeWidgetDescriptionSchema(),
			widgetKind: describeWidgetKindSchema(),
			widgetAnalyticRole: describeWidgetAnalyticRoleSchema(),
			widgetHumanInteractionMode: describeWidgetHumanInteractionModeSchema(),
			workspaceDescription: describeWorkspaceDescriptionSchema(),
			workspaceCapability: describeWorkspaceCapabilitySchema(),
			workspaceTransportHints: describeWorkspaceTransportHintsSchema(),
			workspaceDescriptionPlanning: describeWorkspaceDescriptionPlanningSchema(),
			actionDescriptor: describeActionDescriptorSchema(),
			actionPrimitive: describeActionPrimitiveSchema(),
			actionCategory: describeActionCategorySchema(),
			actionEffectKind: describeActionEffectKindSchema(),
			actionCall: describeActionCallSchema(),
			perceptionDescriptor: describePerceptionDescriptorSchema(),
			perceptionCategory: describePerceptionCategorySchema(),
			perceptionSummaryGroups: describePerceptionSummaryGroupsSchema(),
			perceptionInspectViewConfigResult: describePerceptionInspectViewConfigResultSchema(),
			perceptionInspectVisibleRowsResult: describePerceptionInspectVisibleRowsResultSchema(),
			perceptionSummarizeSelectionResult: describePerceptionSummarizeSelectionResultSchema(),
			perceptionSummarizeVisibleResult: describePerceptionSummarizeVisibleResultSchema(),
			perceptionVerifyActionEffectParams: describePerceptionVerifyActionEffectParamsSchema(),
			perceptionVerifyActionEffectResult: describePerceptionVerifyActionEffectResultSchema(),
			perceptionQueryCall: describePerceptionQueryCallSchema(),
			dataHandle: describeDataHandleSchema(),
			dataRecord: describeDataRecordSchema(),
			dataSummaryRow: describeDataSummaryRowSchema(),
			dataSchemaResult: describeDataSchemaResultSchema(),
			dataRowsResult: describeDataRowsResultSchema(),
			dataSummaryTable: describeDataSummaryTableSchema(),
			dataGroupComparisonResult: describeDataGroupComparisonResultSchema(),
			dataQueryPredicate: describeDataQueryPredicateSchema(),
			dataQueryMeasure: describeDataQueryMeasureSchema(),
			dataQuerySqlSpec: describeDataQuerySqlSpecSchema(),
			dataQuerySummarySpec: describeDataQuerySummarySpecSchema(),
			dataQueryCorrelationSpec: describeDataQueryCorrelationSpecSchema(),
			dataQueryExtremesSpec: describeDataQueryExtremesSpecSchema(),
			dataQueryOutliersSpec: describeDataQueryOutliersSpecSchema(),
			dataQueryCompareGroupsSpec: describeDataQueryCompareGroupsSpecSchema(),
			dataQueryAggregateSpec: describeDataQueryAggregateSpecSchema(),
			dataQueryCallQuery: describeDataQueryCallQuerySchema(),
			dataQueryDescriptor: describeDataQueryDescriptorSchema(),
			dataQueryCall: describeDataQueryCallSchema(),
			runtimeCoreSummary: describeRuntimeCoreSummarySchema(),
			runtimeCoreComponents: describeRuntimeCoreComponentsSchema(),
			runtimeCoreRegistries: describeRuntimeCoreRegistriesSchema(),
			runtimeCoreCapabilities: describeRuntimeCoreCapabilitiesSchema(),
			runtimeStoreSummary: describeRuntimeStoreSummarySchema(),
			runtimeStoreIdentity: describeRuntimeStoreIdentitySchema(),
			runtimeStoreStateSummary: describeRuntimeStoreStateSummarySchema(),
			runtimeStoreIndexes: describeRuntimeStoreIndexesSchema(),
			runtimeStoreHistory: describeRuntimeStoreHistorySchema(),
			runtimeStoreRetention: describeRuntimeStoreRetentionSchema(),
			runtimeStoreCapabilities: describeRuntimeStoreCapabilitiesSchema(),
			widgetRegistrySummary: describeWidgetRegistrySummarySchema(),
			widgetRegistryCounts: describeWidgetRegistryCountsSchema(),
			widgetRegistryRefs: describeWidgetRegistryRefsSchema(),
			actionExecutorSummary: describeActionExecutorSummarySchema(),
			actionExecutorCounts: describeActionExecutorCountsSchema(),
			actionExecutorCapabilities: describeActionExecutorCapabilitiesSchema(),
			actionExecutorActionEntry: describeActionExecutorActionEntrySchema(),
			actionContextSummary: describeActionContextSummarySchema(),
			actionUsageSummary: describeActionUsageSummarySchema(),
			actionUsageSpecContext: describeActionUsageSpecContextSchema(),
			actionUsageFieldRoles: describeActionUsageFieldRolesSchema(),
			actionUsageFieldRoleMap: describeActionUsageFieldRoleMapSchema(),
			actionUsageFieldCandidates: describeActionUsageFieldCandidatesSchema(),
			actionUsageFieldExplanation: describeActionUsageFieldExplanationSchema(),
			actionUsageParamRole: describeActionUsageParamRoleSchema(),
			actionUsageParamRoles: describeActionUsageParamRolesSchema(),
			actionUsageEntry: describeActionUsageEntrySchema(),
			perceptionRegistrySummary: describePerceptionRegistrySummarySchema(),
			perceptionContextSummary: describePerceptionContextSummarySchema(),
			perceptionRegistryCounts: describePerceptionRegistryCountsSchema(),
			perceptionRegistryCapabilities: describePerceptionRegistryCapabilitiesSchema(),
			perceptionRegistryQueryEntry: describePerceptionRegistryQueryEntrySchema(),
			dataQueryExecutorSummary: describeDataQueryExecutorSummarySchema(),
			dataQueryContextSummary: describeDataQueryContextSummarySchema(),
			dataQueryExecutorEngineSummary: describeDataQueryExecutorEngineSummarySchema(),
			dataQueryExecutorCounts: describeDataQueryExecutorCountsSchema(),
			dataQueryExecutorCapabilities: describeDataQueryExecutorCapabilitiesSchema(),
			dataQueryEngineSummary: describeDataQueryEngineSummarySchema(),
			stateManagerSummary: describeStateManagerSummarySchema(),
			stateManagerCapabilities: describeStateManagerCapabilitiesSchema(),
			stateManagerCounters: describeStateManagerCountersSchema(),
			traceRecorderSummary: describeTraceRecorderSummarySchema(),
			traceRecorderCapabilities: describeTraceRecorderCapabilitiesSchema(),
			traceRecorderCounters: describeTraceRecorderCountersSchema(),
			traceRecorderEventKinds: describeTraceRecorderEventKindsSchema(),
			responseRecorderSummary: describeResponseRecorderSummarySchema(),
			responseRecorderCapabilities: describeResponseRecorderCapabilitiesSchema(),
			responseRecorderCounters: describeResponseRecorderCountersSchema(),
			agentResponseRecord: describeAgentResponseRecordSchema(),
			linkEngineSummary: describeLinkEngineSummarySchema(),
			workspaceSpec: describeWorkspaceSpecSchema(),
			ref: describeRefSchema(),
			refParts: describeRefPartsSchema(),
			fieldEncoding: describeFieldEncodingSchema(),
			fieldScale: describeFieldScaleSchema(),
			transformState: describeTransformStateSchema(),
			selectionDomain: describeSelectionDomainSchema(),
			selectionPredicate: describeSelectionPredicateSchema(),
			selectionValue: describeSelectionValueSchema(),
			selectionState: describeSelectionStateSchema(),
			interactionHoveredItem: describeInteractionHoveredItemSchema(),
			interactionTooltip: describeInteractionTooltipSchema(),
			interactionFeedbackState: describeInteractionFeedbackStateSchema(),
			widgetDataState: describeWidgetDataStateSchema(),
			widgetEncodingsState: describeWidgetEncodingsStateSchema(),
			widgetSelectionsState: describeWidgetSelectionsStateSchema(),
			viewTransformState: describeViewTransformStateSchema(),
			workspaceAnnotation: describeWorkspaceAnnotationSchema(),
			widgetState: describeWidgetStateSchema(),
			workspaceSharedState: describeWorkspaceSharedStateSchema(),
			workspaceTaskContext: describeWorkspaceTaskContextSchema(),
			workspaceDelta: describeWorkspaceDeltaSchema(),
			workspaceState: describeWorkspaceStateSchema(),
			widgetLink: describeWidgetLinkSchema(),
			widgetLinkKind: describeWidgetLinkKindSchema(),
			widgetLinkEffect: describeWidgetLinkEffectSchema(),
			workspacePlanningTask: describeWorkspacePlanningTaskSchema(),
			workspaceWidgetPlanSource: describeWorkspaceWidgetPlanSourceSchema(),
			workspaceWidgetPlanMetric: describeWorkspaceWidgetPlanMetricSchema(),
			workspaceWidgetPlanSort: describeWorkspaceWidgetPlanSortSchema(),
			workspaceWidgetPlanTransform: describeWorkspaceWidgetPlanTransformSchema(),
			workspaceWidgetDataBinding: describeWorkspaceWidgetDataBindingSchema(),
			workspaceLinkFieldMapping: describeWorkspaceLinkFieldMappingSchema(),
			workspacePlanningRequest: describeWorkspacePlanningRequestSchema(),
			workspacePlanningResult: describeWorkspacePlanningResultSchema(),
			agentLoopContext: describeAgentLoopContextSchema(),
			agentLoopHints: describeAgentLoopHintsSchema(),
			verifiedActionEvidence: describeVerifiedActionEvidenceSchema(),
			verifiedActionResult: describeVerifiedActionResultSchema(),
			resultError: describeResultErrorSchema(),
			actionResult: describeActionResultSchema(),
			perceptionResult: describePerceptionResultSchema(),
			dataQueryResult: describeDataQueryResultSchema(),
			stateSnapshotMeta: describeStateSnapshotMetaSchema(),
			workspaceSnapshot: describeWorkspaceSnapshotSchema(),
			workspaceSnapshotMeta: describeWorkspaceSnapshotMetaSchema(),
			workspaceReplayContext: describeWorkspaceReplayContextSchema(),
			branchSummary: describeBranchSummarySchema(),
			traceGraph: describeTraceGraphSchema(),
			traceGraphNode: describeTraceGraphNodeSchema(),
			traceGraphEdge: describeTraceGraphEdgeSchema(),
			linkPropagationCheck: describeLinkPropagationCheckSchema(),
			linkPropagationResult: describeLinkPropagationResultSchema(),
			linkPropagationEvaluation: describeLinkPropagationEvaluationSchema(),
			widgetAdapterSummary: describeWidgetAdapterSummarySchema(),
			widgetAdapterProviderCapabilities: describeWidgetAdapterProviderCapabilitiesSchema(),
			widgetAdapterHumanInteraction: describeWidgetAdapterHumanInteractionSchema(),
			widgetAdapterCapabilities: describeWidgetAdapterCapabilitiesSchema(),
			interactionTraceActor: describeInteractionTraceActorSchema(),
			interactionTraceEventKind: describeInteractionTraceEventKindSchema(),
			interactionTraceRecord: describeInteractionTraceRecordSchema()
		});
	}
	function describePagePortSchemasForMethods(methods = PAGE_PORT_METHODS) {
		const allSchemas = describePagePortSchemas();
		const selectedKeys = new Set((Array.isArray(methods) ? methods : []).flatMap((methodName) => PAGE_PORT_METHOD_SCHEMA_KEYS[methodName] || []));
		return Object.fromEntries(Object.entries(allSchemas).filter(([schemaKey]) => selectedKeys.has(schemaKey)));
	}
	function describePagePortCapabilities(overrides = {}) {
		return makePagePortDescription(overrides);
	}
	//#endregion
	//#region ../../widgetva-kit/src/transports/pagePortBridge.js
	var TRANSPORT_PAGE_PORT_ALIASES = Object.keys(PAGE_PORT_ALIASES);
	function resolvePagePortMethod(port, alias) {
		if (!port || typeof alias !== "string" || alias.length === 0) return null;
		const aliasedMethod = port[alias];
		if (typeof aliasedMethod === "function") return aliasedMethod;
		const stableMethodName = PAGE_PORT_ALIASES[alias];
		if (typeof stableMethodName === "string" && typeof port[stableMethodName] === "function") return port[stableMethodName];
		return null;
	}
	function getInstalledPagePort(root = window) {
		const port = root?.__widgetVA;
		if (!port) throw new Error("WidgetVA page port is not installed.");
		return port;
	}
	function invokePagePortAlias(alias, args = [], root = window) {
		const method = resolvePagePortMethod(getInstalledPagePort(root), alias);
		if (typeof method !== "function") throw new Error(`WidgetVA page port alias is unavailable: ${alias}`);
		return method(...Array.isArray(args) ? args : [args]);
	}
	var WIDGETVA_EXTENSION_RESPONSE_TYPE = "widgetva:response";
	var WIDGETVA_EXTENSION_RESPONSE_SOURCE = "widgetva-page";
	function buildErrorPayload(error) {
		if (error instanceof Error) return {
			name: error.name,
			message: error.message
		};
		return {
			name: "Error",
			message: String(error || "Unknown WidgetVA bridge error")
		};
	}
	function isWidgetVABridgeRequest(message) {
		return message && typeof message === "object" && message.source === "widgetva-extension" && message.type === "widgetva:request" && typeof message.id === "string" && typeof message.alias === "string";
	}
	function installBrowserExtensionBridge({ root = window, targetOrigin = "*", allowedAliases = [...TRANSPORT_PAGE_PORT_ALIASES] } = {}) {
		const allowedAliasSet = new Set(Array.isArray(allowedAliases) ? allowedAliases : [...TRANSPORT_PAGE_PORT_ALIASES]);
		const handleMessage = async (event) => {
			const message = event?.data;
			if (!isWidgetVABridgeRequest(message)) return;
			if (!allowedAliasSet.has(message.alias)) {
				event.source?.postMessage({
					source: WIDGETVA_EXTENSION_RESPONSE_SOURCE,
					type: WIDGETVA_EXTENSION_RESPONSE_TYPE,
					id: message.id,
					ok: false,
					error: {
						name: "UnsupportedAliasError",
						message: `Unsupported WidgetVA transport alias: ${message.alias}`
					}
				}, targetOrigin);
				return;
			}
			try {
				const result = await invokePagePortAlias(message.alias, Array.isArray(message.args) ? message.args : [], root);
				event.source?.postMessage({
					source: WIDGETVA_EXTENSION_RESPONSE_SOURCE,
					type: WIDGETVA_EXTENSION_RESPONSE_TYPE,
					id: message.id,
					ok: true,
					result
				}, targetOrigin);
			} catch (error) {
				event.source?.postMessage({
					source: WIDGETVA_EXTENSION_RESPONSE_SOURCE,
					type: WIDGETVA_EXTENSION_RESPONSE_TYPE,
					id: message.id,
					ok: false,
					error: buildErrorPayload(error)
				}, targetOrigin);
			}
		};
		root.addEventListener("message", handleMessage);
		return () => root.removeEventListener("message", handleMessage);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/pagePortAgentLoop.js
	function clone$22(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function summarizeStateForFormalObserve(observation = {}) {
		const coordination = observation?.coordination || null;
		const latestCoordinationResult = observation?.latestCoordinationResult || null;
		const parts = [];
		if (typeof coordination?.focusedWidgetRef === "string" && coordination.focusedWidgetRef.length > 0) parts.push(`focus ${coordination.focusedWidgetRef}`);
		if (typeof coordination?.selections?.views?.primary?.summary === "string" && coordination.selections.views.primary.summary.length > 0) parts.push(coordination.selections.views.primary.summary);
		if (typeof latestCoordinationResult?.verification?.summary === "string" && latestCoordinationResult.verification.summary.length > 0) parts.push(latestCoordinationResult.verification.summary);
		return parts.join(" · ") || null;
	}
	function deriveFormalActOk(result = {}) {
		if (typeof result?.actionResult?.ok === "boolean") return result.actionResult.ok;
		if (typeof result?.ok === "boolean") return result.ok;
		if (typeof result?.success === "boolean") return result.success;
		return true;
	}
	function deriveFormalStateId(result = {}) {
		return result?.actionResult?.stateId || result?.stateId || null;
	}
	function deriveFormalUpdatedRefs(result = {}) {
		if (Array.isArray(result?.actionResult?.updatedRefs)) return result.actionResult.updatedRefs;
		if (Array.isArray(result?.updatedRefs)) return result.updatedRefs;
		return [];
	}
	function deriveFormalVerificationOk(verification = null) {
		if (!verification || typeof verification !== "object") return null;
		if (typeof verification.ok === "boolean") return verification.ok;
		if (typeof verification?.result?.verified === "boolean") return verification.result.verified;
		if (typeof verification?.result?.passed === "boolean") return verification.result.passed;
		if (typeof verification?.passed === "boolean") return verification.passed;
		return null;
	}
	function summarizeFormalRuntimePayload(payload = null) {
		if (!payload || typeof payload !== "object") return null;
		if (typeof payload.summary === "string" && payload.summary.length > 0) return payload.summary;
		if (typeof payload.message === "string" && payload.message.length > 0) return payload.message;
		if (typeof payload.result === "string" && payload.result.length > 0) return payload.result;
		if (payload?.result && typeof payload.result === "object") return JSON.stringify(payload.result).slice(0, 220);
		return null;
	}
	function isPlainObject$1(value) {
		return Boolean(value) && typeof value === "object" && !Array.isArray(value);
	}
	function normalizeQueryScope(call = {}, fallbackWidgetRef = null) {
		const queryScope = isPlainObject$1(call?.queryScope) ? { ...call.queryScope } : {};
		const widgetRef = call?.targetRef || queryScope.widgetRef || fallbackWidgetRef || null;
		if (widgetRef && !queryScope.widgetRef) queryScope.widgetRef = widgetRef;
		return Object.keys(queryScope).length > 0 ? queryScope : null;
	}
	function normalizeOperation$1(operation = {}, { fallbackWidgetRef = null } = {}) {
		if (!isPlainObject$1(operation)) return null;
		const queryScope = normalizeQueryScope(operation, fallbackWidgetRef);
		const normalized = { ...operation };
		if (queryScope) normalized.queryScope = queryScope;
		return normalized;
	}
	function buildActionCall(operation = {}, { callId = "agent_step_action", actor = "agent" } = {}) {
		const queryScope = normalizeQueryScope(operation);
		return {
			callId,
			name: operation.name,
			actor,
			...queryScope ? { queryScope } : {},
			params: clone$22(operation.params || {})
		};
	}
	function buildVerificationCall(actionCall = {}, result = {}) {
		const queryScope = normalizeQueryScope(actionCall);
		const stateId = result?.actionResult?.stateId || result?.stateId || null;
		const refs = Array.isArray(result?.actionResult?.updatedRefs) ? result.actionResult.updatedRefs : Array.isArray(result?.updatedRefs) ? result.updatedRefs : [];
		return {
			callId: `${actionCall.callId || "agent_step_action"}_verify`,
			name: "perception.verifyActionEffect",
			actor: actionCall.actor || "agent",
			...queryScope ? { queryScope } : {},
			params: {
				actionName: actionCall.name || null,
				stateId,
				refs
			}
		};
	}
	async function maybeInvoke(port, methodName, ...args) {
		if (typeof port?.[methodName] !== "function") return null;
		return port[methodName](...args);
	}
	function resolveMethod(port, methodNames = []) {
		for (const methodName of methodNames) if (typeof port?.[methodName] === "function") return port[methodName].bind(port);
		return null;
	}
	async function readObserveStage(port, { workspaceOptions = {}, loopOptions = {}, observationOptions = {} } = {}) {
		const [workspace, loopContext, observation] = await Promise.all([
			port.describeWorkspace(workspaceOptions),
			port.describeAgentLoop(loopOptions),
			maybeInvoke(port, "readObservation", observationOptions)
		]);
		return {
			workspace,
			loopContext,
			observation
		};
	}
	async function readWorkspacePlan(port, planningRequest = null) {
		const planWorkspace = resolveMethod(port, ["planWorkspace"]);
		if (!planningRequest || typeof planWorkspace !== "function") return null;
		return planWorkspace(planningRequest);
	}
	async function readActionUsage(port, operation = null) {
		if (!operation || operation.kind !== "action" || typeof port?.describeActionUsage !== "function") return null;
		const targetRef = operation?.queryScope?.widgetRef || operation?.targetRef || null;
		if (!targetRef || typeof operation?.name !== "string" || operation.name.length === 0) return null;
		return port.describeActionUsage({
			targetRef,
			actionName: operation.name
		});
	}
	function defaultReasoner({ plan, result, verification, latestCoordinationResult }) {
		const actionApplied = Boolean(result?.actionResult?.ok ?? result?.ok ?? result?.success ?? false);
		const verificationPassed = Boolean(verification?.ok ?? verification?.result?.passed ?? result?.verification?.ok ?? false);
		const operationKind = plan?.operation?.kind || null;
		const runtimeSummary = summarizeFormalRuntimePayload(result);
		return {
			answer: operationKind === "perception" || operationKind === "data_query" ? runtimeSummary || plan?.assistantMessage || "Completed one agent loop step." : plan?.assistantMessage || runtimeSummary || "Completed one agent loop step.",
			rationale: plan?.rationale || "",
			success: actionApplied,
			verificationPassed,
			latestCoordinationResult: clone$22(latestCoordinationResult)
		};
	}
	function buildPlanStage({ objective = null, workspacePlan = null, planningResult = null, operation = null, actionUsage = null } = {}) {
		return {
			objective,
			workspacePlan: clone$22(workspacePlan),
			assistantMessage: planningResult?.assistantMessage || "",
			rationale: planningResult?.rationale || "",
			operation: clone$22(operation),
			actionUsage: clone$22(actionUsage),
			rawPlan: clone$22(planningResult)
		};
	}
	async function resolvePlanningResult({ planner, observe, workspacePlan, objective = null, operation = null, actor = "agent" } = {}) {
		if (operation) return {
			assistantMessage: "",
			rationale: "",
			operation: normalizeOperation$1(operation),
			actor,
			source: "provided_operation"
		};
		if (typeof planner !== "function") throw new Error("runPagePortAgentLoop requires either operation or planner.");
		const planningResult = await planner({
			objective,
			observe: clone$22(observe),
			workspacePlan: clone$22(workspacePlan),
			actor
		});
		if (!isPlainObject$1(planningResult)) throw new Error("planner must return an object containing operation.");
		const normalizedOperation = normalizeOperation$1(planningResult.operation);
		if (!normalizedOperation) throw new Error("planner must return a valid operation object.");
		return {
			...planningResult,
			operation: normalizedOperation,
			actor: planningResult.actor || actor,
			source: planningResult.source || "planner"
		};
	}
	async function executeOperation(port, operation, { actor = "agent", callId = "agent_step_action" } = {}) {
		if (!operation || typeof operation.kind !== "string") throw new Error("executeOperation requires an operation with kind.");
		if (operation.kind === "action") {
			const actionCall = buildActionCall(operation, {
				callId,
				actor
			});
			const runVerifiedAction = resolveMethod(port, ["executeVerifiedAction", "runVerifiedAction"]);
			if (typeof runVerifiedAction === "function") return {
				executionKind: "verified_action",
				actionCall,
				result: await runVerifiedAction(actionCall, {})
			};
			const executeAction = resolveMethod(port, ["executeAction", "runAction"]);
			if (typeof executeAction !== "function") throw new Error("Page port does not expose executeAction() or executeVerifiedAction().");
			return {
				executionKind: "action",
				actionCall,
				result: await executeAction(actionCall)
			};
		}
		if (operation.kind === "perception") {
			const queryPerception = resolveMethod(port, ["queryPerception"]);
			if (typeof queryPerception !== "function") throw new Error("Page port does not expose queryPerception().");
			const call = {
				callId,
				name: operation.name,
				actor,
				...operation.queryScope ? { queryScope: clone$22(operation.queryScope) } : {},
				params: clone$22(operation.params || {})
			};
			return {
				executionKind: "perception",
				actionCall: call,
				result: await queryPerception(call)
			};
		}
		if (operation.kind === "data_query") {
			const runDataQuery = resolveMethod(port, ["runDataQuery", "queryData"]);
			if (typeof runDataQuery !== "function") throw new Error("Page port does not expose runDataQuery().");
			const call = {
				callId,
				actor,
				dataRef: operation.dataRef || null,
				query: clone$22(operation.query || {})
			};
			return {
				executionKind: "data_query",
				actionCall: call,
				result: await runDataQuery(call)
			};
		}
		throw new Error(`Unsupported operation kind: ${operation.kind}`);
	}
	async function verifyOperation(port, execution = null, { verify = true } = {}) {
		if (!verify || !execution || execution.executionKind !== "action" && execution.executionKind !== "verified_action") return null;
		const resultVerification = execution?.result?.verification;
		if (resultVerification) return resultVerification;
		const queryPerception = resolveMethod(port, ["queryPerception"]);
		if (typeof queryPerception !== "function") return null;
		return queryPerception(buildVerificationCall(execution.actionCall, execution.result));
	}
	async function runPagePortAgentLoop(port, options = {}) {
		if (!port || typeof port !== "object") throw new Error("runPagePortAgentLoop requires a page port object.");
		if (typeof port.describeWorkspace !== "function") throw new Error("runPagePortAgentLoop requires pagePort.describeWorkspace().");
		if (typeof port.describeAgentLoop !== "function") throw new Error("runPagePortAgentLoop requires pagePort.describeAgentLoop().");
		const { objective = null, planner = null, reasoner = defaultReasoner, operation = null, planningRequest = null, verify = true, workspaceOptions = {}, loopOptions = {}, observationOptions = {}, callId = "agent_step_action", actor = "agent" } = options;
		const observe = await readObserveStage(port, {
			workspaceOptions,
			loopOptions,
			observationOptions
		});
		const workspacePlan = await readWorkspacePlan(port, planningRequest);
		const planningResult = await resolvePlanningResult({
			planner,
			observe,
			workspacePlan,
			objective,
			operation,
			actor
		});
		const actionUsage = await readActionUsage(port, planningResult.operation);
		const plan = buildPlanStage({
			objective,
			workspacePlan,
			planningResult,
			operation: planningResult.operation,
			actionUsage
		});
		const execution = await executeOperation(port, planningResult.operation, {
			actor: planningResult.actor || actor,
			callId
		});
		const verification = await verifyOperation(port, execution, { verify });
		const latestCoordinationResult = await maybeInvoke(port, "readLatestCoordinationResult");
		const reason = await reasoner({
			objective,
			observe: clone$22(observe),
			plan: clone$22(plan),
			result: clone$22(execution.result),
			verification: clone$22(verification),
			latestCoordinationResult: clone$22(latestCoordinationResult)
		});
		return {
			observe,
			plan,
			result: execution.result,
			verification,
			reason,
			latestCoordinationResult
		};
	}
	function buildFormalObservePayload(baseObserve = {}, { objective = null, previousTurnSummary = null, perception = null } = {}) {
		const observation = baseObserve?.observation || {};
		const coordination = observation?.coordination || null;
		const state = observation?.state || null;
		const stateSummary = summarizeStateForFormalObserve(observation);
		return {
			query: objective,
			previousTurnSummary,
			focusWidgetRef: coordination?.focusedWidgetRef || null,
			state: {
				summary: stateSummary,
				stateId: state?.stateId || null,
				rawRef: state?.stateId || null
			},
			view: {
				snapshot: {
					ref: state?.stateId ? `widgetva-view:${state.stateId}` : null,
					mimeType: "application/widgetva-view+json"
				},
				summary: stateSummary
			},
			perception: perception && typeof perception === "object" ? {
				name: perception.name || "perception",
				resultRef: perception.resultRef || null,
				summary: perception.summary || null
			} : null
		};
	}
	function buildFormalPlanPayload(plan = {}) {
		const operation = plan?.operation || {};
		return {
			objective: plan?.objective || null,
			step: {
				kind: operation.kind,
				...operation.name ? { name: operation.name } : {},
				...operation.queryScope ? { queryScope: clone$22(operation.queryScope) } : {},
				...operation.params && Object.keys(operation.params).length > 0 ? { params: clone$22(operation.params) } : {},
				...operation.dataRef ? { dataRef: operation.dataRef } : {},
				...operation.query ? { query: clone$22(operation.query) } : {}
			},
			rationale: plan?.rationale || ""
		};
	}
	function buildFormalActPayload(plan = {}, result = {}) {
		const operation = plan?.operation || {};
		return {
			kind: operation.kind,
			name: operation.name || operation.query?.kind || "data_query",
			...operation.params && Object.keys(operation.params).length > 0 ? { params: clone$22(operation.params) } : {},
			...operation.queryScope ? { queryScope: clone$22(operation.queryScope) } : {},
			ok: deriveFormalActOk(result),
			outputSummary: summarizeFormalRuntimePayload(result),
			stateId: deriveFormalStateId(result),
			updatedRefs: deriveFormalUpdatedRefs(result)
		};
	}
	function buildFormalVerifyPayload({ plan = {}, act = null, verification = null, observe = null } = {}) {
		const operation = plan?.operation || {};
		const beforeStateId = observe?.observation?.state?.stateId || null;
		const verificationOk = deriveFormalVerificationOk(verification);
		const verificationSummary = summarizeFormalRuntimePayload(verification);
		const stepChoice = act?.ok ? {
			status: act?.kind === "action" ? "uncertain" : "pass",
			reason: act?.kind === "action" ? "The action executed, but semantic fit to the user query was not separately judged in this turn." : "The turn executed a read-oriented step and produced an output."
		} : {
			status: "fail",
			reason: "The selected step did not execute successfully."
		};
		const params = act?.ok ? {
			status: "pass",
			reason: "Runtime accepted the parameters for this step."
		} : {
			status: "fail",
			reason: "The runtime rejected this step before parameters could be trusted."
		};
		const stateChange = (() => {
			if (act?.kind !== "action") return {
				status: "not_applicable",
				reason: "This turn did not execute a state-mutating action."
			};
			if (act?.stateId && beforeStateId && act.stateId !== beforeStateId) return {
				status: "pass",
				reason: `State changed from ${beforeStateId} to ${act.stateId}.`
			};
			if (!act?.ok) return {
				status: "fail",
				reason: "The action did not complete successfully, so no state change can be confirmed."
			};
			return {
				status: "uncertain",
				reason: "The action returned success, but a distinct state transition was not confirmed."
			};
		})();
		const visualChange = (() => {
			if (act?.kind !== "action") return {
				status: "not_applicable",
				reason: "This turn did not request a visual state change."
			};
			if (verificationOk === true) return {
				status: "pass",
				reason: verificationSummary || "Verification confirmed the expected visible effect."
			};
			if (verificationOk === false) return {
				status: "fail",
				reason: verificationSummary || "Verification reported that the visible effect was not achieved."
			};
			return {
				status: "uncertain",
				reason: "No explicit visual verification result was available for this action."
			};
		})();
		const ok = Boolean(act?.ok && params.status !== "fail" && stateChange.status !== "fail" && visualChange.status !== "fail");
		return {
			ok,
			summary: ok ? visualChange.status === "pass" ? visualChange.reason : act?.outputSummary || "The step executed successfully." : [
				params.reason,
				stateChange.reason,
				visualChange.reason
			].find(Boolean) || "The step did not verify cleanly.",
			checks: {
				stepChoice,
				params,
				stateChange,
				visualChange
			},
			nextStepHint: params.status === "fail" ? {
				kind: operation?.kind === "action" ? "action" : "perception",
				guidance: "Re-check required parameters before retrying this step."
			} : visualChange.status === "uncertain" ? {
				kind: "perception",
				guidance: "Inspect the current view or verification surface before deciding the next step."
			} : ok ? {
				kind: "answer",
				guidance: "This turn verified cleanly enough to answer unless the query explicitly requires another step."
			} : {
				kind: operation?.kind === "action" ? "action" : "stop",
				guidance: "Choose a simpler, better-scoped next step based on the current state."
			}
		};
	}
	function buildFormalReasonPayload(baseReason = {}, { act = null, verify = null, plan = null } = {}) {
		const prefersRuntimeSummary = act?.kind === "perception" || act?.kind === "data_query";
		if (verify?.ok) {
			const assistantText = baseReason?.answer || plan?.assistantMessage || null;
			const runtimeText = act?.outputSummary || null;
			return { answer: prefersRuntimeSummary ? [assistantText, runtimeText].filter((part, index, array) => typeof part === "string" && part.length > 0 && array.indexOf(part) === index).join(" ") || "Completed one agent loop step." : assistantText || runtimeText || "Completed one agent loop step." };
		}
		return { answer: verify?.summary || baseReason?.answer || plan?.assistantMessage || "The last step did not verify cleanly." };
	}
	async function runPagePortAgentTurn(port, options = {}) {
		const base = await runPagePortAgentLoop(port, options);
		const observe = buildFormalObservePayload(base.observe, {
			objective: options?.objective || null,
			previousTurnSummary: options?.previousTurnSummary || null,
			perception: options?.perception || null
		});
		const plan = buildFormalPlanPayload(base.plan);
		const act = buildFormalActPayload(base.plan, base.result);
		const verify = buildFormalVerifyPayload({
			plan: base.plan,
			act,
			verification: base.verification,
			observe: base.observe
		});
		return {
			observe,
			plan,
			act,
			verify,
			reason: buildFormalReasonPayload(base.reason, {
				act,
				verify,
				plan: base.plan
			})
		};
	}
	function summarizeTurnForSession(turn = {}, index = 0) {
		return {
			turnId: `turn_${index + 1}`,
			summary: turn?.reason?.answer || turn?.verify?.summary || turn?.act?.outputSummary || "Completed one agent turn."
		};
	}
	function buildPerceptionCarry(turn = null) {
		if (turn?.act?.kind !== "perception") return null;
		return {
			name: turn.act.name || "perception",
			resultRef: null,
			summary: turn.act.outputSummary || turn.verify?.summary || null
		};
	}
	async function runPagePortAgentSession(port, options = {}) {
		const { objective = null, maxTurns = 3 } = options;
		const safeMaxTurns = Number.isFinite(maxTurns) && maxTurns > 0 ? Math.max(1, Math.floor(maxTurns)) : 3;
		const turns = [];
		let previousTurnSummary = null;
		let previousPerception = null;
		let stopReason = "turn_budget_reached";
		for (let index = 0; index < safeMaxTurns; index += 1) {
			const turn = await runPagePortAgentTurn(port, {
				...options,
				objective,
				previousTurnSummary,
				perception: previousPerception,
				callId: `agent_step_${index + 1}`
			});
			turns.push(turn);
			previousTurnSummary = summarizeTurnForSession(turn, index).summary;
			previousPerception = buildPerceptionCarry(turn);
			const nextHintKind = turn?.verify?.nextStepHint?.kind || null;
			if (nextHintKind === "answer") {
				stopReason = "answered";
				break;
			}
			if (nextHintKind === "stop") {
				stopReason = "stopped";
				break;
			}
			if (index === safeMaxTurns - 1) stopReason = "turn_budget_reached";
		}
		const lastTurn = turns.at(-1) || null;
		return {
			objective: objective || null,
			ok: Boolean(lastTurn?.verify?.ok),
			answer: lastTurn?.reason?.answer || "",
			stopReason,
			turns
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/viewportStateModel.js
	function clone$21(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeString$4(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizeDomain(value) {
		return Array.isArray(value) ? clone$21(value) : null;
	}
	function normalizeZoom(value) {
		if (!value || typeof value !== "object" || Array.isArray(value)) return null;
		return clone$21(value);
	}
	function normalizeViewportState(viewport = null) {
		if (!viewport || typeof viewport !== "object" || Array.isArray(viewport)) return null;
		const normalized = clone$21(viewport);
		return {
			...normalized,
			sourceWidgetRef: normalizeString$4(normalized.sourceWidgetRef || normalized.source_widget_ref),
			xDomain: normalizeDomain(normalized.xDomain || normalized.x_domain),
			yDomain: normalizeDomain(normalized.yDomain || normalized.y_domain),
			zoom: normalizeZoom(normalized.zoom)
		};
	}
	function readViewportState(shared = {}) {
		return normalizeViewportState(shared?.viewport || null);
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/focusStateModel.js
	function normalizeString$3(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizeFocusState$1(focus = null) {
		if (!focus || typeof focus !== "object" || Array.isArray(focus)) return null;
		return {
			widgetRef: normalizeString$3(focus.widgetRef || focus.widget_ref),
			widgetId: normalizeString$3(focus.widgetId || focus.widget_id),
			source: normalizeString$3(focus.source) || "workspace"
		};
	}
	function readFocusState(shared = {}, widgets = {}) {
		const explicitFocus = normalizeFocusState$1(shared?.focus || null);
		if (explicitFocus) return explicitFocus;
		const focusedWidgetRef = normalizeString$3(shared?.focusedWidget);
		if (!focusedWidgetRef) return null;
		return {
			widgetRef: focusedWidgetRef,
			widgetId: normalizeString$3((widgets?.[focusedWidgetRef] || null)?.widgetId),
			source: "workspace"
		};
	}
	function withFocusSubmodel(shared = {}, focus = void 0, widgets = {}) {
		const nextFocus = focus === void 0 ? readFocusState(shared, widgets) : normalizeFocusState$1(focus);
		return {
			...shared || {},
			focusedWidget: nextFocus?.widgetRef || null,
			focus: nextFocus
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/highlightStateModel.js
	function clone$20(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeString$2(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizeStringArray(values) {
		return Array.isArray(values) ? values.filter((value, index) => typeof value === "string" && value.length > 0 && values.indexOf(value) === index) : [];
	}
	function normalizeScalarArray(values) {
		return Array.isArray(values) ? clone$20(values.filter((value, index) => value != null && values.findIndex((candidate) => JSON.stringify(candidate) === JSON.stringify(value)) === index)) : [];
	}
	function normalizeHighlightEntry$1(entry = {}) {
		if (!entry || typeof entry !== "object" || Array.isArray(entry)) return null;
		return {
			widgetRef: normalizeString$2(entry.widgetRef || entry.widget_ref),
			widgetId: normalizeString$2(entry.widgetId || entry.widget_id),
			sourceWidgetRef: normalizeString$2(entry.sourceWidgetRef || entry.source_widget_ref),
			sourceWidgetId: normalizeString$2(entry.sourceWidgetId || entry.source_widget_id),
			selectionRef: normalizeString$2(entry.selectionRef || entry.selection_ref),
			summary: normalizeString$2(entry.summary),
			predicates: Array.isArray(entry.predicates) ? clone$20(entry.predicates) : [],
			highlightedKeys: normalizeScalarArray(entry.highlightedKeys || entry.highlighted_keys),
			inboundLinkIds: normalizeStringArray(entry.inboundLinkIds || entry.inbound_link_ids),
			highlightLinkIds: normalizeStringArray(entry.highlightLinkIds || entry.highlight_link_ids),
			linkedSourceRefs: normalizeStringArray(entry.linkedSourceRefs || entry.linked_source_refs)
		};
	}
	function deriveHighlightState(state = {}) {
		const explicitHighlight = normalizeHighlightState$1(state?.shared?.highlight || null);
		if (explicitHighlight) return explicitHighlight;
		const widgets = state?.widgets && typeof state.widgets === "object" ? state.widgets : {};
		const entries = Object.values(widgets).map((widgetState) => normalizeHighlightEntry$1({
			widgetRef: widgetState?.ref || null,
			widgetId: widgetState?.widgetId || null,
			highlightedKeys: widgetState?.feedback?.highlightedKeys || [],
			inboundLinkIds: widgetState?.feedback?.inboundLinkIds || [],
			highlightLinkIds: widgetState?.feedback?.highlightLinkIds || [],
			linkedSourceRefs: widgetState?.feedback?.linkedSourceRefs || []
		})).filter((entry) => entry && (entry.predicates.length > 0 || Boolean(entry.summary) || entry.highlightedKeys.length > 0 || entry.inboundLinkIds.length > 0 || entry.highlightLinkIds.length > 0 || entry.linkedSourceRefs.length > 0));
		return {
			entries,
			activeWidgetRefs: entries.map((entry) => entry.widgetRef).filter(Boolean)
		};
	}
	function normalizeHighlightState$1(highlight = null) {
		if (!highlight || typeof highlight !== "object" || Array.isArray(highlight)) return null;
		const entries = Array.isArray(highlight.entries) ? highlight.entries.map((entry) => normalizeHighlightEntry$1(entry)).filter(Boolean) : [];
		return {
			entries,
			activeWidgetRefs: Array.isArray(highlight.activeWidgetRefs) ? highlight.activeWidgetRefs.filter((value, index) => typeof value === "string" && value.length > 0 && highlight.activeWidgetRefs.indexOf(value) === index) : entries.map((entry) => entry.widgetRef).filter(Boolean)
		};
	}
	function withHighlightSubmodel(shared = {}, highlight = void 0) {
		return {
			...shared || {},
			highlight: highlight === void 0 ? normalizeHighlightState$1(shared?.highlight || null) : normalizeHighlightState$1(highlight)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/data/DataQueryEngine.js
	function buildSupportedQueryDescriptors$2(kinds = []) {
		return kinds.map((kind) => makeDataQueryDescriptor({
			name: kind,
			...DATA_QUERY_DESCRIPTOR_TEMPLATES[kind],
			inputSchema: DATA_QUERY_SCHEMAS[kind] || {
				type: "object",
				additionalProperties: true,
				properties: {}
			}
		}));
	}
	function buildSummaryMeasures$1({ fields = [], metrics = [] } = {}) {
		const normalizedFields = Array.isArray(fields) ? fields.filter((field) => typeof field === "string" && field.length > 0) : [];
		const normalizedMetrics = Array.isArray(metrics) ? metrics.filter((metric) => typeof metric === "string" && metric.length > 0) : [];
		const measures = [];
		for (const metric of normalizedMetrics) {
			if (metric === "count") {
				measures.push({
					op: "count",
					as: "count"
				});
				continue;
			}
			for (const field of normalizedFields) measures.push({
				op: metric,
				field,
				as: `${field}_${metric}`
			});
		}
		return measures;
	}
	function normalizeAggregateSpec$1(spec = {}) {
		const groupBy = Array.isArray(spec.groupBy) ? spec.groupBy : [];
		const measures = Array.isArray(spec.measures) ? spec.measures : Array.isArray(spec.metrics) ? spec.metrics : [];
		return {
			...spec,
			groupBy,
			measures
		};
	}
	function normalizeSummarySpec$1(spec = {}) {
		const measures = Array.isArray(spec.measures) && spec.measures.length > 0 ? spec.measures : buildSummaryMeasures$1({
			fields: spec.fields,
			metrics: spec.metrics
		});
		return {
			...spec,
			measures
		};
	}
	function normalizeExtremesSpec$1(spec = {}) {
		const direction = typeof spec.direction === "string" ? spec.direction : null;
		return {
			...spec,
			order: direction === "min" ? "ascending" : "descending"
		};
	}
	function normalizeCompareGroupsSpec$1(spec = {}) {
		if (Array.isArray(spec.groups) && spec.groups.length >= 2) return spec;
		return {
			...spec,
			groups: [spec.leftGroup, spec.rightGroup].filter((value) => value != null)
		};
	}
	var DataQueryEngine = class {
		resolveQuerySource(source) {
			if (typeof source !== "string" || source.length === 0) return source;
			const resolver = typeof this.resolveSource === "function" ? this.resolveSource.bind(this) : typeof this.options?.resolveSource === "function" ? this.options.resolveSource.bind(this.options) : null;
			if (typeof resolver !== "function") return source;
			const resolvedSource = resolver(source);
			return resolvedSource == null ? source : resolvedSource;
		}
		query(source, query = {}) {
			const resolvedSource = this.resolveQuerySource(source);
			const kind = query?.kind || null;
			const spec = query?.spec || {};
			if (kind === "schema") return this.getSchema(resolvedSource);
			if (kind === "filter") return this.filter(resolvedSource, Array.isArray(spec?.predicates) ? spec.predicates : []);
			if (kind === "sampleRows") return this.sampleRows(resolvedSource, spec?.limit || 20);
			if (kind === "aggregate" || kind === "groupBy") return this.aggregate(resolvedSource, normalizeAggregateSpec$1(spec));
			if (kind === "summary") return this.summarize(resolvedSource, normalizeSummarySpec$1(spec));
			if (kind === "sql") return this.executeSql(resolvedSource, spec);
			if (kind === "computeCorrelation") return this.computeCorrelation(resolvedSource, spec);
			if (kind === "findExtremes") return this.findExtremes(resolvedSource, normalizeExtremesSpec$1(spec));
			if (kind === "findOutliers") return this.findOutliers(resolvedSource, spec);
			if (kind === "compareGroups") return this.compareGroups(resolvedSource, normalizeCompareGroupsSpec$1(spec));
			throw new Error(`Unsupported data query kind: ${kind || "missing"}.`);
		}
		describeEngine() {
			const supportedQueryKinds = typeof this.listSupportedQueryKinds === "function" ? this.listSupportedQueryKinds() : [];
			const supportedQueryDescriptors = buildSupportedQueryDescriptors$2(supportedQueryKinds);
			return makeDataQueryEngineSummary({
				engine: makeDataQueryEngineEngineSummary({
					kind: this.kind || null,
					className: this.constructor?.name || null
				}),
				counts: makeDataQueryEngineCounts({
					supportedQueryKindCount: supportedQueryKinds.length,
					supportedQueryDescriptorCount: supportedQueryDescriptors.length
				}),
				supportedQueryKinds,
				supportedQueryDescriptors,
				capabilities: makeDataQueryEngineCapabilities({
					localExecution: true,
					remoteExecution: false,
					sqlSupport: supportedQueryKinds.includes("sql"),
					fallbackEngine: false
				})
			});
		}
		listSupportedQueryKinds() {
			return [
				"schema",
				"sampleRows",
				"filter",
				"aggregate",
				"groupBy",
				"summary"
			];
		}
		getSchema() {
			throw new Error("DataQueryEngine.getSchema() must be implemented by subclasses.");
		}
		filter() {
			throw new Error("DataQueryEngine.filter() must be implemented by subclasses.");
		}
		sampleRows() {
			throw new Error("DataQueryEngine.sampleRows() must be implemented by subclasses.");
		}
		executeSql() {
			throw new Error("DataQueryEngine.executeSql() must be implemented by subclasses.");
		}
		aggregate() {
			throw new Error("DataQueryEngine.aggregate() must be implemented by subclasses.");
		}
		summarize() {
			throw new Error("DataQueryEngine.summarize() must be implemented by subclasses.");
		}
		computeCorrelation() {
			throw new Error("DataQueryEngine.computeCorrelation() must be implemented by subclasses.");
		}
		findExtremes() {
			throw new Error("DataQueryEngine.findExtremes() must be implemented by subclasses.");
		}
		findOutliers() {
			throw new Error("DataQueryEngine.findOutliers() must be implemented by subclasses.");
		}
		compareGroups() {
			throw new Error("DataQueryEngine.compareGroups() must be implemented by subclasses.");
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/JsArrayDataQueryEngine.js
	function inferFieldType(value) {
		if (typeof value === "number") return "quantitative";
		if (typeof value === "boolean") return "boolean";
		if (typeof value === "string") return "nominal";
		return "nominal";
	}
	function applyPredicate$2(row, predicate) {
		const value = row?.[predicate.field];
		if (predicate.op === "between" && Array.isArray(predicate.value)) return value >= predicate.value[0] && value <= predicate.value[1];
		if (predicate.op === "equals") return value === predicate.value;
		if (predicate.op === "in" && Array.isArray(predicate.value)) return predicate.value.includes(value);
		if (predicate.op === "notIn" && Array.isArray(predicate.value)) return !predicate.value.includes(value);
		return true;
	}
	function filterRows(rows, predicates = []) {
		if (!Array.isArray(predicates) || predicates.length === 0) return rows;
		return rows.filter((row) => predicates.every((predicate) => applyPredicate$2(row, predicate)));
	}
	function numericValuesForMeasure(rows, field) {
		if (!field) return [];
		return rows.map((row) => row?.[field]).filter((value) => typeof value === "number");
	}
	function median(values) {
		const sorted = [...values].sort((a, b) => a - b);
		if (sorted.length === 0) return null;
		const middle = Math.floor(sorted.length / 2);
		if (sorted.length % 2 === 1) return sorted[middle];
		return (sorted[middle - 1] + sorted[middle]) / 2;
	}
	function computeMeasureValue(rows, measure) {
		if (!measure?.op || !measure?.as) return void 0;
		if (measure.op === "count") return rows.length;
		const values = numericValuesForMeasure(rows, measure.field);
		if (measure.op === "sum") return values.reduce((sum, value) => sum + value, 0);
		if (measure.op === "mean") return values.length > 0 ? values.reduce((sum, value) => sum + value, 0) / values.length : null;
		if (measure.op === "min") return values.length > 0 ? Math.min(...values) : null;
		if (measure.op === "max") return values.length > 0 ? Math.max(...values) : null;
		if (measure.op === "median") return values.length > 0 ? median(values) : null;
	}
	function aggregateRows$1(rows, spec = {}) {
		const groupBy = Array.isArray(spec.groupBy) ? spec.groupBy.filter(Boolean) : [];
		const measures = Array.isArray(spec.measures) ? spec.measures : [];
		if (measures.length === 0) return {
			rows,
			rowCount: rows.length
		};
		if (groupBy.length === 0) {
			const summaryRow = {};
			measures.forEach((measure) => {
				if (!measure?.as) return;
				summaryRow[measure.as] = computeMeasureValue(rows, measure);
			});
			return {
				rows: [summaryRow],
				rowCount: 1
			};
		}
		const buckets = /* @__PURE__ */ new Map();
		for (const row of rows) {
			const key = JSON.stringify(groupBy.map((field) => row?.[field]));
			if (!buckets.has(key)) {
				const seed = { __widgetva_rows: [] };
				groupBy.forEach((field) => {
					seed[field] = row?.[field] ?? null;
				});
				buckets.set(key, seed);
			}
			buckets.get(key).__widgetva_rows.push(row);
		}
		let result = Array.from(buckets.values()).map((bucket) => {
			const nextBucket = { ...bucket };
			const bucketRows = Array.isArray(bucket.__widgetva_rows) ? bucket.__widgetva_rows : [];
			measures.forEach((measure) => {
				if (!measure?.as) return;
				nextBucket[measure.as] = computeMeasureValue(bucketRows, measure);
			});
			delete nextBucket.__widgetva_rows;
			return nextBucket;
		});
		if (spec.sortBy?.field) {
			const dir = spec.sortBy.order === "ascending" ? 1 : -1;
			const field = spec.sortBy.field;
			result = result.sort((a, b) => {
				const left = a?.[field];
				const right = b?.[field];
				if (left === right) return 0;
				return left > right ? dir : -dir;
			});
		}
		if (Number.isFinite(spec.limit) && spec.limit > 0) result = result.slice(0, spec.limit);
		return {
			rows: result,
			rowCount: result.length
		};
	}
	var JsArrayDataQueryEngine = class extends DataQueryEngine {
		constructor(options = {}) {
			super();
			this.options = options;
			this.kind = options.kind || "js_array";
		}
		listSupportedQueryKinds() {
			return [
				"schema",
				"sampleRows",
				"filter",
				"aggregate",
				"groupBy",
				"summary",
				"computeCorrelation",
				"findExtremes",
				"findOutliers",
				"compareGroups"
			];
		}
		getSchema(rows) {
			const resolvedRows = this.resolveQuerySource(rows);
			const sample = Array.isArray(resolvedRows) && resolvedRows.length > 0 ? resolvedRows[0] : {};
			return { fields: Object.keys(sample || {}).map((name) => ({
				name,
				type: inferFieldType(sample[name])
			})) };
		}
		filter(rows, predicates = []) {
			return filterRows(Array.isArray(rows) ? rows : [], predicates);
		}
		sampleRows(rows, limit = 20) {
			return (Array.isArray(rows) ? rows : []).slice(0, Math.max(0, limit));
		}
		executeSql() {
			return {
				ok: false,
				error: {
					code: "SQL_QUERY_UNSUPPORTED",
					message: "SQL queries are not supported by the in-memory JS array query engine."
				}
			};
		}
		aggregate(rows, spec = {}) {
			return aggregateRows$1(Array.isArray(rows) ? rows : [], spec);
		}
		summarize(rows, options = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			const groupBy = Array.isArray(options.groupBy) ? options.groupBy.filter(Boolean) : [];
			const measures = Array.isArray(options.measures) ? options.measures : [{
				op: "count",
				as: "count"
			}];
			return this.aggregate(safeRows, {
				groupBy,
				measures,
				sortBy: options.sortBy,
				limit: options.limit
			});
		}
		computeCorrelation(rows, { xField, yField } = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			if (!xField || !yField) return {
				ok: false,
				reason: "xField and yField are required."
			};
			const pairs = safeRows.map((row) => [row?.[xField], row?.[yField]]).filter(([x, y]) => typeof x === "number" && typeof y === "number");
			if (pairs.length < 2) return {
				ok: false,
				reason: "Not enough numeric rows to compute correlation."
			};
			const n = pairs.length;
			const sumX = pairs.reduce((acc, [x]) => acc + x, 0);
			const sumY = pairs.reduce((acc, [, y]) => acc + y, 0);
			const meanX = sumX / n;
			const meanY = sumY / n;
			let num = 0;
			let denX = 0;
			let denY = 0;
			for (const [x, y] of pairs) {
				const dx = x - meanX;
				const dy = y - meanY;
				num += dx * dy;
				denX += dx * dx;
				denY += dy * dy;
			}
			const denominator = Math.sqrt(denX * denY);
			return {
				ok: denominator > 0,
				coefficient: denominator > 0 ? num / denominator : null,
				sampleSize: n,
				fields: [xField, yField]
			};
		}
		findExtremes(rows, { field, order = "descending", limit = 5 } = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			if (!field) return { rows: [] };
			return { rows: safeRows.filter((row) => typeof row?.[field] === "number").sort((a, b) => {
				const diff = (a?.[field] || 0) - (b?.[field] || 0);
				return order === "ascending" ? diff : -diff;
			}).slice(0, Math.max(0, limit)) };
		}
		findOutliers(rows, { field, xField, yField, zThreshold = 2, limit = 10 } = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			const fields = [
				field,
				xField,
				yField
			].filter(Boolean);
			if (fields.length === 0) return {
				rows: [],
				summary: { reason: "No numeric field specified." }
			};
			const scored = [];
			for (const currentField of fields) {
				const numericValues = safeRows.map((row) => row?.[currentField]).filter((value) => typeof value === "number");
				if (numericValues.length < 2) continue;
				const mean = numericValues.reduce((acc, value) => acc + value, 0) / numericValues.length;
				const variance = numericValues.reduce((acc, value) => acc + (value - mean) ** 2, 0) / numericValues.length;
				const std = Math.sqrt(variance);
				if (!std) continue;
				for (const row of safeRows) {
					const value = row?.[currentField];
					if (typeof value !== "number") continue;
					const zScore = Math.abs((value - mean) / std);
					if (zScore >= zThreshold) scored.push({
						row,
						field: currentField,
						value,
						zScore
					});
				}
			}
			const deduped = [];
			const seen = /* @__PURE__ */ new Set();
			for (const item of scored.sort((a, b) => b.zScore - a.zScore)) {
				const key = JSON.stringify(item.row);
				if (seen.has(key)) continue;
				seen.add(key);
				deduped.push(item);
				if (deduped.length >= Math.max(0, limit)) break;
			}
			return {
				rows: deduped.map((item) => ({
					...item.row,
					__widgetva_outlier_field: item.field,
					__widgetva_outlier_score: item.zScore
				})),
				summary: {
					inspectedFields: fields,
					zThreshold,
					count: deduped.length
				}
			};
		}
		compareGroups(rows, { groupField, valueField, groups = [] } = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			if (!groupField || !valueField || groups.length < 2) return {
				ok: false,
				reason: "groupField, valueField, and at least two groups are required."
			};
			return {
				ok: true,
				groups: groups.map((group) => {
					const numericValues = safeRows.filter((row) => row?.[groupField] === group).map((row) => row?.[valueField]).filter((value) => typeof value === "number");
					const count = numericValues.length;
					const sum = numericValues.reduce((acc, value) => acc + value, 0);
					return {
						group,
						count,
						mean: count > 0 ? sum / count : null,
						min: count > 0 ? Math.min(...numericValues) : null,
						max: count > 0 ? Math.max(...numericValues) : null
					};
				})
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/DuckDbDataQueryEngine.js
	var DuckDbDataQueryEngine = class extends DataQueryEngine {
		constructor(options = {}) {
			super();
			this.options = options;
			this.kind = options.kind || "duckdb";
			this.execute = options.request || options.execute || null;
			this.fallbackEngine = new JsArrayDataQueryEngine(options);
		}
		unsupported() {
			return {
				ok: false,
				error: {
					code: "DUCKDB_ENGINE_UNAVAILABLE",
					message: "DuckDB-backed data queries are not configured in this frontend runtime build."
				}
			};
		}
		listSupportedQueryKinds() {
			return [
				"schema",
				"sampleRows",
				"filter",
				"aggregate",
				"groupBy",
				"sql",
				"summary",
				"computeCorrelation",
				"findExtremes",
				"findOutliers",
				"compareGroups"
			];
		}
		getSchema() {
			return this.fallbackEngine.getSchema(...arguments);
		}
		filter() {
			return this.fallbackEngine.filter(...arguments);
		}
		sampleRows() {
			return this.fallbackEngine.sampleRows(...arguments);
		}
		executeSql() {
			if (typeof this.execute === "function") return this.execute({
				kind: "sql",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.unsupported();
		}
		aggregate() {
			if (typeof this.execute === "function") return this.execute({
				kind: "aggregate",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.aggregate(...arguments);
		}
		summarize() {
			if (typeof this.execute === "function") return this.execute({
				kind: "summary",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.summarize(...arguments);
		}
		computeCorrelation() {
			if (typeof this.execute === "function") return this.execute({
				kind: "computeCorrelation",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.computeCorrelation(...arguments);
		}
		findExtremes() {
			if (typeof this.execute === "function") return this.execute({
				kind: "findExtremes",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.findExtremes(...arguments);
		}
		findOutliers() {
			if (typeof this.execute === "function") return this.execute({
				kind: "findOutliers",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.findOutliers(...arguments);
		}
		compareGroups() {
			if (typeof this.execute === "function") return this.execute({
				kind: "compareGroups",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.compareGroups(...arguments);
		}
		describeEngine() {
			const summary = super.describeEngine();
			return {
				...summary,
				capabilities: {
					...summary.capabilities,
					localExecution: true,
					remoteExecution: false,
					sqlSupport: typeof this.execute === "function",
					fallbackEngine: true
				}
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/RemoteDataQueryEngine.js
	var RemoteDataQueryEngine = class extends DataQueryEngine {
		constructor(options = {}) {
			super();
			const { request, execute } = options;
			this.request = request || execute || null;
			this.kind = "remote";
			this.fallbackEngine = new JsArrayDataQueryEngine(options);
		}
		unsupported() {
			return {
				ok: false,
				error: {
					code: "REMOTE_ENGINE_UNAVAILABLE",
					message: "Remote data query execution is not configured in this frontend runtime build."
				}
			};
		}
		listSupportedQueryKinds() {
			return [
				"schema",
				"sampleRows",
				"filter",
				"aggregate",
				"groupBy",
				"sql",
				"summary",
				"computeCorrelation",
				"findExtremes",
				"findOutliers",
				"compareGroups"
			];
		}
		getSchema() {
			return this.fallbackEngine.getSchema(...arguments);
		}
		filter() {
			return this.fallbackEngine.filter(...arguments);
		}
		sampleRows() {
			return this.fallbackEngine.sampleRows(...arguments);
		}
		executeSql() {
			if (typeof this.request === "function") return this.request({
				kind: "sql",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.unsupported();
		}
		aggregate() {
			if (typeof this.request === "function") return this.request({
				kind: "aggregate",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.aggregate(...arguments);
		}
		summarize() {
			if (typeof this.request === "function") return this.request({
				kind: "summary",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.summarize(...arguments);
		}
		computeCorrelation() {
			if (typeof this.request === "function") return this.request({
				kind: "computeCorrelation",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.computeCorrelation(...arguments);
		}
		findExtremes() {
			if (typeof this.request === "function") return this.request({
				kind: "findExtremes",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.findExtremes(...arguments);
		}
		findOutliers() {
			if (typeof this.request === "function") return this.request({
				kind: "findOutliers",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.findOutliers(...arguments);
		}
		compareGroups() {
			if (typeof this.request === "function") return this.request({
				kind: "compareGroups",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.compareGroups(...arguments);
		}
		describeEngine() {
			const summary = super.describeEngine();
			return {
				...summary,
				capabilities: {
					...summary.capabilities,
					localExecution: typeof this.request !== "function",
					remoteExecution: typeof this.request === "function",
					sqlSupport: typeof this.request === "function",
					fallbackEngine: true
				}
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/index.js
	function createDataQueryEngine({ kind = "js_array", ...options } = {}) {
		if (kind === "duckdb") return new DuckDbDataQueryEngine({
			kind,
			...options
		});
		if (kind === "remote") return new RemoteDataQueryEngine({
			kind,
			...options
		});
		return new JsArrayDataQueryEngine({
			kind,
			...options
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/workspaceDescriptorBuilders.js
	var dataQueryEngine$1 = createDataQueryEngine({ kind: "js_array" });
	var supportedQueryKinds = dataQueryEngine$1.listSupportedQueryKinds();
	function buildSupportedQueryDescriptors$1(kinds) {
		return kinds.map((kind) => makeDataQueryDescriptor({
			name: kind,
			...DATA_QUERY_DESCRIPTOR_TEMPLATES[kind],
			inputSchema: DATA_QUERY_SCHEMAS[kind] || {
				type: "object",
				additionalProperties: true,
				properties: {}
			}
		}));
	}
	function buildSelectionAffectedStatePaths(affectedRefs, widgetRef) {
		return affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms");
	}
	function buildHighlightAffectedStatePaths(affectedRefs) {
		return affectedRefs.map(() => "feedback");
	}
	function buildDataTransformAffectedStatePaths(affectedRefs) {
		return affectedRefs.flatMap(() => [
			"transforms",
			"data.currentDataRef",
			"data.visibleCount",
			"data.selectedCount"
		]);
	}
	function buildSortAffectedStatePaths(affectedRefs) {
		return affectedRefs.flatMap(() => ["encodings", "view.sort"]);
	}
	function buildEncodingAffectedStatePaths(affectedRefs) {
		return affectedRefs.map(() => "encodings");
	}
	function buildZoomAffectedStatePaths(affectedRefs) {
		return affectedRefs.flatMap(() => [
			"view.xDomain",
			"view.yDomain",
			"view.zoom"
		]);
	}
	function buildWorkspaceAffectedStatePaths(actionName, affectedRefs, widgetRef) {
		if (actionName === "workspace.focusWidget") return ["shared.focusedWidget"];
		if (actionName === "workspace.addAnnotation" || actionName === "workspace.clearAnnotations") return ["shared.annotations"];
		if (actionName === "workspace.jumpToState" || actionName === "workspace.branchFromState") return [
			"widgets",
			"shared",
			"replayContext"
		];
		if (actionName === "workspace.resetWorkspace") return [
			"widgets",
			"shared.selections.registry",
			"shared.globalFilters",
			"shared.annotations"
		];
		return affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms");
	}
	function buildDataHandle({ appId, workspaceId, spec }) {
		const rows = Array.isArray(spec?.data?.values) ? spec.data.values : [];
		const supportedQueries = supportedQueryKinds;
		return makeDataHandle({
			ref: `wl://${appId}/workspace/${workspaceId}/data/primary`,
			title: "Primary Data",
			description: "Current inline dataset bound to the active widget.",
			sourceKind: "inline",
			schema: dataQueryEngine$1.getSchema(rows),
			stats: { rowCount: rows.length },
			supportedQueries,
			supportedQueryDescriptors: buildSupportedQueryDescriptors$1(supportedQueries)
		});
	}
	function buildDerivedDataHandle({ ref, title, description, rows, selectedCount = 0, kind = "dataView", scope = "workspace", widgetRef = void 0, sourceSelectionRef = void 0 }) {
		const supportedQueries = supportedQueryKinds;
		return makeDataHandle({
			ref,
			title,
			description,
			sourceKind: "inline",
			kind,
			scope,
			widgetRef,
			sourceSelectionRef,
			schema: dataQueryEngine$1.getSchema(rows),
			stats: {
				rowCount: rows.length,
				visibleCount: rows.length,
				selectedCount
			},
			supportedQueries,
			supportedQueryDescriptors: buildSupportedQueryDescriptors$1(supportedQueries)
		});
	}
	function buildActionDescriptors({ widgetRef, selectionRef, widgetKind, widgetSourceKind, scope = "local", affectedRefs = [widgetRef] }) {
		const supportedWidgetKinds = typeof widgetKind === "string" ? [widgetKind] : null;
		const descriptors = [
			makeActionDescriptor({
				name: "widget.updateSelection",
				title: "Update widget selection",
				description: "Apply a runtime selection payload to the current widget through the shared action pipeline.",
				primitive: "select",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildSelectionAffectedStatePaths(affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {
						selection_id: { type: "string" },
						source_widget_id: { type: "string" },
						selection_type: { type: "string" },
						domain: { type: "object" },
						fields: {
							type: "array",
							items: { type: "string" }
						},
						value: { type: "object" },
						keyField: { type: "string" },
						keys: {
							type: "array",
							items: {}
						},
						field: { type: "string" },
						values: {
							type: "array",
							items: {}
						},
						predicates: {
							type: "array",
							items: { type: "object" }
						},
						count: { type: "integer" },
						summary: { type: "string" }
					},
					required: ["selection_type"]
				},
				postconditions: [{ description: "The widget selection state should match the supplied runtime selection payload." }],
				effects: [makeSelectionEffect(widgetRef, "Creates or updates the active selection through the shared runtime action path.")],
				examples: [{
					userGoal: "Apply a directly-manipulated runtime selection through the shared action pipeline.",
					params: {
						selection_type: "category",
						field: "Origin",
						values: ["USA"],
						predicates: [{
							field: "Origin",
							op: "in",
							value: ["USA"]
						}],
						count: 1,
						summary: "Origin: USA"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "widget.clearSelection",
				title: "Clear current selection",
				description: "Clear the active selection on the current widget.",
				primitive: "reset",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildSelectionAffectedStatePaths(affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The widget should have no active selection." }],
				effects: [makeSelectionEffect(widgetRef, "Clears the active selection on the target widget.")],
				examples: [{
					userGoal: "Clear the current focus region before starting a new analysis step.",
					params: {}
				}],
				reversible: false
			}),
			makeActionDescriptor({
				name: "widget.undoSelection",
				title: "Undo selection",
				description: "Revert to the previous selection state in the current workspace.",
				primitive: "undo",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildSelectionAffectedStatePaths(affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The previous selection state should be restored if history is available." }],
				preconditions: [{
					description: "A previous selection state exists in workspace history.",
					failureMessage: "No prior selection state is available to restore."
				}],
				effects: [makeSelectionEffect(widgetRef, "Restores the previous selection state on the target widget when available.")],
				reversible: false
			}),
			makeActionDescriptor({
				name: "widget.redoSelection",
				title: "Redo selection",
				description: "Reapply the next selection state in the current workspace when redo history is available.",
				primitive: "redo",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildSelectionAffectedStatePaths(affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The next selection state should be restored if redo history is available." }],
				preconditions: [{
					description: "A redo selection state exists in workspace history.",
					failureMessage: "No redo selection state is available to restore."
				}],
				effects: [makeSelectionEffect(widgetRef, "Reapplies the next selection state on the target widget when redo history is available.")],
				reversible: false
			}),
			makeActionDescriptor({
				name: "workspace.resetWorkspace",
				title: "Reset workspace state",
				description: "Reset interaction state for the current workspace back to its baseline state.",
				primitive: "reset",
				category: "coordination",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.resetWorkspace", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "Selections and derived interaction state should be cleared." }],
				effects: [makeSelectionEffect(widgetRef, "Clears active selections and restores baseline workspace interaction state.")],
				reversible: false
			}),
			makeActionDescriptor({
				name: "workspace.jumpToState",
				title: "Jump to previous state",
				description: "Restore the workspace interaction state from a previously recorded state snapshot.",
				primitive: "undo",
				category: "coordination",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.jumpToState", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: { stateId: { type: "string" } },
					required: ["stateId"]
				},
				postconditions: [{ description: "The workspace interaction state should match the target snapshot as closely as possible." }],
				preconditions: [{
					description: "The referenced state snapshot exists in runtime history.",
					failureMessage: "The requested stateId is not available in runtime history."
				}],
				examples: [{
					userGoal: "Return to the state before a failed exploratory branch.",
					params: { stateId: "main:previous-state" }
				}],
				reversible: false
			}),
			makeActionDescriptor({
				name: "workspace.focusWidget",
				title: "Focus workspace widget",
				description: "Move the workspace analytical focus to a target widget without changing data selection.",
				primitive: "navigate",
				category: "navigation",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.focusWidget", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The workspace focused widget should match the requested target." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid widget in the current workspace.",
					failureMessage: "workspace.focusWidget requires a valid target widget."
				}],
				examples: [{
					userGoal: "Move analytical attention from the overview chart to the detail table.",
					params: { queryScope: { widgetRef } }
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "workspace.addAnnotation",
				title: "Add workspace annotation",
				description: "Attach an explicit analysis note to a widget or the workspace for later reasoning and replay.",
				primitive: "annotate",
				category: "annotation",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.addAnnotation", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {
						text: { type: "string" },
						kind: { type: "string" }
					},
					required: ["text"]
				},
				postconditions: [{ description: "The workspace annotations list should contain the new note." }],
				preconditions: [{
					description: "If targetRef is provided, it resolves to a valid widget in the current workspace.",
					failureMessage: "workspace.addAnnotation targetRef must resolve to a valid widget when provided."
				}],
				examples: [{
					userGoal: "Record a hypothesis about the current cluster before continuing.",
					params: {
						text: "Potential high-risk subgroup worth validating.",
						queryScope: { widgetRef }
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "workspace.clearAnnotations",
				title: "Clear workspace annotations",
				description: "Remove all current workspace annotations.",
				primitive: "reset",
				category: "annotation",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.clearAnnotations", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The workspace annotations list should be empty." }],
				examples: [{
					userGoal: "Clear temporary analysis notes before starting a new line of reasoning.",
					params: {}
				}],
				reversible: false
			}),
			makeActionDescriptor({
				name: "workspace.branchFromState",
				title: "Branch from previous state",
				description: "Create a new branch from a previously recorded state snapshot and restore that interaction state.",
				primitive: "undo",
				category: "coordination",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.branchFromState", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {
						stateId: { type: "string" },
						branchLabel: { type: "string" }
					},
					required: ["stateId"]
				},
				postconditions: [{ description: "A new branch should be created and the workspace interaction state should match the branch origin snapshot." }],
				preconditions: [{
					description: "The referenced state snapshot exists in runtime history.",
					failureMessage: "The requested branch origin stateId is not available."
				}],
				reversible: false
			})
		];
		if (widgetSourceKind === "baseSpec") descriptors.push(makeActionDescriptor({
			name: "widget.highlightValues",
			title: "Highlight matching values",
			description: "Highlight rows/items in the target widget that match a categorical field-value set without changing the active selection.",
			primitive: "highlight",
			category: "coordination",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildHighlightAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					field: { type: "string" },
					values: {
						type: "array",
						items: {},
						minItems: 1
					}
				},
				required: ["field", "values"]
			},
			postconditions: [{ description: "The target widget should expose highlight feedback for the requested values." }, { description: "Matching rows/items should be marked as highlighted in runtime-visible state." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.highlightValues requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept highlight updates.",
				failureMessage: "No active base spec is available for highlight updates."
			}],
			effects: [makeHighlightEffect(widgetRef, "Applies direct runtime highlighting to matching values on the target widget.")],
			examples: [{
				userGoal: "Temporarily highlight records from one category before deciding whether to filter.",
				params: {
					field: "Origin",
					values: ["USA"]
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.aggregateData",
			title: "Aggregate current data view",
			description: "Apply or replace an aggregate transform on the current base specification to produce grouped summary rows.",
			primitive: "aggregate",
			category: "dataTransform",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildDataTransformAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					groupBy: {
						type: "array",
						items: { type: "string" },
						minItems: 1
					},
					measures: {
						type: "array",
						minItems: 1,
						items: {
							type: "object",
							properties: {
								op: { type: "string" },
								field: { type: "string" },
								as: { type: "string" }
							},
							required: ["op", "as"]
						}
					}
				},
				required: ["groupBy", "measures"]
			},
			postconditions: [{ description: "The target widget spec should include an aggregate transform with the requested groupBy and measure definitions." }, { description: "The target widget visible rows should represent grouped summary output rather than the raw record set." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.aggregateData requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept aggregate updates.",
				failureMessage: "No active base spec is available for aggregate updates."
			}],
			examples: [{
				userGoal: "Roll the current detail view up to counts by category before comparing groups.",
				params: {
					groupBy: ["Origin"],
					measures: [{
						op: "count",
						as: "count"
					}]
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.filterByValues",
			title: "Filter by categorical values",
			description: "Apply or replace a categorical filter transform on the current base specification.",
			primitive: "filter",
			category: "dataTransform",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildDataTransformAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					field: { type: "string" },
					values: {
						type: "array",
						items: {},
						minItems: 1
					}
				},
				required: ["field", "values"]
			},
			postconditions: [{ description: "The target widget spec should include a categorical filter transform for the requested field." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.filterByValues requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept transform updates.",
				failureMessage: "No active base spec is available for filter updates."
			}],
			effects: [makeFilterEffect(widgetRef, "Applies a categorical filter transform to the target widget specification.")],
			examples: [{
				userGoal: "Restrict the chart to records from a small set of categories.",
				params: {
					field: "Origin",
					values: ["Japan", "USA"]
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.filterByRange",
			title: "Filter by numeric range",
			description: "Apply or replace a numeric range filter transform on the current base specification.",
			primitive: "filter",
			category: "dataTransform",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildDataTransformAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					field: { type: "string" },
					range: {
						type: "array",
						items: { type: "number" },
						minItems: 2,
						maxItems: 2
					}
				},
				required: ["field", "range"]
			},
			postconditions: [{ description: "The target widget spec should include a range filter transform for the requested field." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.filterByRange requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept transform updates.",
				failureMessage: "No active base spec is available for filter updates."
			}],
			effects: [makeFilterEffect(widgetRef, "Applies a numeric range filter transform to the target widget specification.")],
			examples: [{
				userGoal: "Restrict the chart to a bounded numeric interval.",
				params: {
					field: "Horsepower",
					range: [80, 160]
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.sortEncoding",
			title: "Sort encoded values",
			description: "Apply a declarative sort rule to a target encoding channel in the current base specification.",
			primitive: "sort",
			category: "visualMapping",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildSortAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					channel: { type: "string" },
					field: { type: "string" },
					order: { type: "string" },
					aggregate: { type: "string" }
				},
				required: ["channel", "order"]
			},
			postconditions: [{ description: "The target widget encoding should expose the requested sort rule." }],
			preconditions: [
				{
					description: "The requested targetRef resolves to a valid widget in the current workspace.",
					failureMessage: "widget.sortEncoding requires a valid target widget."
				},
				{
					description: "The target widget is backed by an active base specification that can accept sort updates.",
					failureMessage: "No active base spec is available for sort updates."
				},
				{
					description: "The requested encoding channel already exists on the active spec.",
					failureMessage: "The active spec does not define the requested encoding channel."
				}
			],
			effects: [makeEncodingEffect(widgetRef, "Updates the target encoding channel sort rule in the base specification.")],
			examples: [{
				userGoal: "Sort the category axis by descending count.",
				params: {
					channel: "x",
					order: "descending",
					aggregate: "count"
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.changeEncoding",
			title: "Change visual encoding",
			description: "Rebind a widget encoding channel to a different field in the current base specification.",
			primitive: "reencode",
			category: "visualMapping",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildEncodingAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					channel: { type: "string" },
					field: { type: "string" },
					type: { type: "string" },
					aggregate: { type: "string" }
				},
				required: ["channel", "field"]
			},
			postconditions: [{ description: "The target widget encoding should point to the specified field." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.changeEncoding requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept encoding updates.",
				failureMessage: "No active base spec is available for encoding updates."
			}],
			effects: [makeEncodingEffect(widgetRef, "Rebinds a visual encoding channel to a new field in the base specification.")],
			examples: [{
				userGoal: "Switch the y channel to a different measure.",
				params: {
					channel: "y",
					field: "Miles_per_Gallon",
					type: "quantitative"
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.zoomDomain",
			title: "Zoom widget domain",
			description: "Update the data-space x/y domain of a widget without using pixel coordinates.",
			primitive: "zoom",
			category: "viewTransform",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildZoomAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					xDomain: {
						type: "array",
						items: { type: "number" },
						minItems: 2,
						maxItems: 2
					},
					yDomain: {
						type: "array",
						items: { type: "number" },
						minItems: 2,
						maxItems: 2
					}
				}
			},
			postconditions: [{ description: "The target widget view domain should match the requested x/y ranges." }],
			preconditions: [
				{
					description: "The requested targetRef resolves to a valid widget in the current workspace.",
					failureMessage: "widget.zoomDomain requires a valid target widget."
				},
				{
					description: "The target widget is backed by an active base specification that can accept domain updates.",
					failureMessage: "No active base spec is available for domain updates."
				},
				{
					description: "Any requested x/y domain corresponds to an encoding channel present on the active spec.",
					failureMessage: "The active spec does not define the requested zoom domain channel."
				}
			],
			effects: [makeDomainEffect(widgetRef, "Updates the x/y data-space domain of the target widget.")],
			examples: [{
				userGoal: "Zoom to a narrower value range without using pixel coordinates.",
				params: {
					xDomain: [0, 50],
					yDomain: [10, 100]
				}
			}],
			reversible: true
		}));
		const familyAdapter = getWidgetFamilyAdapter(widgetKind);
		descriptors.unshift(...familyAdapter.buildActionDescriptors({
			widgetRef,
			selectionRef,
			widgetKind,
			scope,
			affectedRefs
		}));
		return descriptors;
	}
	function buildPerceptionDescriptors({ appId = "widgetva-app", workspaceId = "main", widgetRef, dataRef, widgetKind, widgetDef, widgetSpec }) {
		const currentViewDataRef = makeCurrentViewDataRef({
			appId,
			workspaceId
		});
		const currentSelectionDataRef = makeCurrentSelectionDataRef({
			appId,
			workspaceId
		});
		const sampleSelectionRef = `${widgetRef}/selection/current`;
		const descriptors = [
			makePerceptionDescriptor({
				name: "perception.inspectViewConfig",
				title: "Inspect current view config",
				description: "Return encodings, transforms, domains, and selection state for the active widget.",
				category: "inspect",
				targetRef: widgetRef,
				paramsSchema: {
					type: "object",
					properties: {}
				},
				sideEffectFree: true,
				evidenceKinds: [
					"viewState",
					"encodingState",
					"selectionState"
				],
				examples: [{
					userGoal: "Check the current encoding and domain before deciding the next action.",
					params: {}
				}]
			}),
			makePerceptionDescriptor({
				name: "perception.inspectVisibleRows",
				title: "Inspect visible rows",
				description: "Return a sampled subset of the rows currently visible in the target widget.",
				category: "inspect",
				targetRef: currentViewDataRef,
				paramsSchema: {
					type: "object",
					properties: { limit: { type: "number" } }
				},
				sideEffectFree: true,
				evidenceKinds: ["rowSample", "visibleData"],
				examples: [
					{
						userGoal: "Look at representative visible records before aggregating.",
						params: { limit: 10 }
					},
					{
						userGoal: "Inspect rows from the shared current_view data view.",
						params: {
							queryScope: { dataRef: currentViewDataRef },
							limit: 10
						}
					},
					{
						userGoal: "Inspect one specific selection-scoped data view.",
						params: {
							queryScope: {
								dataRef: currentSelectionDataRef,
								selectionRef: sampleSelectionRef
							},
							limit: 10
						}
					}
				]
			}),
			makePerceptionDescriptor({
				name: "perception.summarizeSelection",
				title: "Summarize current selection",
				description: "Return a lightweight summary of the current selection, including the shared current_selection data view when available.",
				category: "summarize",
				targetRef: currentSelectionDataRef,
				paramsSchema: {
					type: "object",
					properties: {
						groupBy: {
							type: "array",
							items: { type: "string" }
						},
						measures: {
							type: "array",
							items: { type: "object" }
						},
						fields: {
							type: "array",
							items: { type: "string" }
						},
						metrics: {
							type: "array",
							items: { type: "string" }
						},
						sortBy: {
							type: "object",
							properties: {
								field: { type: "string" },
								order: {
									type: "string",
									enum: ["ascending", "descending"]
								}
							}
						},
						limit: { type: "integer" }
					}
				},
				sideEffectFree: true,
				evidenceKinds: ["selectionSummary", "aggregateEvidence"],
				verificationTargets: ["shared.selections.registry"],
				examples: [{
					userGoal: "Quantify the currently selected subset before answering.",
					params: {
						fields: ["Horsepower"],
						metrics: ["mean", "max"],
						groupBy: ["Origin"]
					}
				}, {
					userGoal: "Summarize one specific selection when multiple active selections are present.",
					params: {
						queryScope: {
							dataRef: currentSelectionDataRef,
							selectionRef: sampleSelectionRef
						},
						fields: ["Horsepower"],
						metrics: ["mean"]
					}
				}]
			}),
			makePerceptionDescriptor({
				name: "perception.summarizeVisible",
				title: "Summarize visible rows",
				description: "Return grouped or aggregated summaries over the rows currently visible in the target widget.",
				category: "summarize",
				targetRef: currentViewDataRef,
				paramsSchema: {
					type: "object",
					properties: {
						groupBy: {
							type: "array",
							items: { type: "string" }
						},
						measures: {
							type: "array",
							items: { type: "object" }
						},
						fields: {
							type: "array",
							items: { type: "string" }
						},
						metrics: {
							type: "array",
							items: { type: "string" }
						},
						sortBy: {
							type: "object",
							properties: {
								field: { type: "string" },
								order: {
									type: "string",
									enum: ["ascending", "descending"]
								}
							}
						},
						limit: { type: "integer" }
					}
				},
				sideEffectFree: true,
				evidenceKinds: ["aggregateEvidence", "visibleDataSummary"],
				examples: [
					{
						userGoal: "Get a grouped summary over the currently visible subset.",
						params: {
							fields: ["Horsepower"],
							metrics: ["mean"],
							groupBy: ["Origin"]
						}
					},
					{
						userGoal: "Summarize rows from the shared current_view data view without changing workspace state.",
						params: {
							queryScope: { dataRef: currentViewDataRef },
							fields: ["Horsepower"],
							metrics: ["mean"]
						}
					},
					{
						userGoal: "Summarize rows from the shared current_selection data view without changing workspace state.",
						params: {
							queryScope: {
								dataRef: currentSelectionDataRef,
								selectionRef: sampleSelectionRef
							},
							fields: ["Horsepower"],
							metrics: ["mean"]
						}
					}
				]
			}),
			makePerceptionDescriptor({
				name: "perception.verifyActionEffect",
				title: "Verify action effect",
				description: "Check whether a recent action produced the expected state change on the specified refs.",
				category: "verify",
				targetRef: widgetRef,
				paramsSchema: {
					type: "object",
					properties: {
						actionName: { type: "string" },
						stateId: { type: "string" },
						refs: {
							type: "array",
							items: { type: "string" }
						}
					}
				},
				sideEffectFree: true,
				evidenceKinds: ["verificationEvidence", "stateDelta"],
				verificationTargets: ["statePatch", "workspaceState"],
				examples: [{
					userGoal: "Verify that a selection or filter changed the intended widgets.",
					params: { actionName: "scatter.brushRegion" }
				}]
			})
		];
		const familyAdapter = getWidgetFamilyAdapter(widgetKind);
		descriptors.push(...familyAdapter.buildPerceptionDescriptors({
			widgetRef,
			dataRef,
			widgetKind,
			widgetDef,
			widgetSpec
		}));
		return descriptors;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/planning/WorkspacePlanner.js
	function getMarkType(spec) {
		return typeof spec?.mark === "string" ? spec.mark : spec?.mark?.type;
	}
	function inferWidgetKind$1(spec) {
		if (spec?.kind === "map") return "map";
		if (spec?.kind === "parallelCoordinates") return "parallelCoordinates";
		if (spec?.kind === "sankey") return "sankey";
		const mark = getMarkType(spec);
		if (mark === "point" || mark === "circle") return "scatter";
		if (mark === "bar") return "bar";
		if (mark === "line") return "line";
		if (mark === "rect") return "heatmap";
		return "custom";
	}
	function isNumber(value) {
		return typeof value === "number" && Number.isFinite(value);
	}
	function normalizeText(value) {
		return typeof value === "string" ? value.trim().toLowerCase() : "";
	}
	function includesAny(text, patterns) {
		return patterns.some((pattern) => text.includes(pattern));
	}
	function derivePlanningSignals({ task, userIntent, runMode, complexityBudget }) {
		const taskFamily = task?.taskFamily || null;
		const coordinationScope = task?.coordinationScope || task?.taskMode || null;
		const evidenceType = task?.evidenceType || null;
		const interactionHorizon = task?.interactionHorizon || null;
		const answerType = task?.answerType || task?.expectedAnswerType || null;
		const queryText = normalizeText(task?.userQuery || userIntent);
		const expansiveMode = runMode === "autonomous" || runMode === "open_ended" || complexityBudget === "extended";
		const asksForDetail = includesAny(queryText, [
			"detail",
			"record",
			"row",
			"exact",
			"which",
			"list",
			"show"
		]);
		const asksForComparison = includesAny(queryText, [
			"compare",
			"distribution",
			"breakdown",
			"summary",
			"group"
		]);
		const asksForCoordination = includesAny(queryText, [
			"linked",
			"across views",
			"cross-widget",
			"shared selection",
			"filter the other"
		]);
		return {
			taskFamily,
			coordinationScope,
			evidenceType,
			interactionHorizon,
			answerType,
			wantsMultiWidget: coordinationScope === "multi_widget" || coordinationScope === "workspace" || evidenceType === "cross_widget" || taskFamily === "multiViewCoordination" || asksForCoordination,
			wantsDetailEvidence: answerType === "exact" || evidenceType === "interaction_revealed" || evidenceType === "cross_widget" || interactionHorizon === "multi_step" || asksForDetail,
			wantsSummaryContext: answerType === "exploratory" || taskFamily === "compare" || taskFamily === "distribution" || taskFamily === "rank" || taskFamily === "trend" || asksForComparison,
			expansiveMode
		};
	}
	function appendPlanningSignals(plan, signals, extras = []) {
		const rationale = Array.isArray(plan?.rationale) ? [...plan.rationale] : [];
		if (signals.taskFamily) rationale.push(`Planner considered task family "${signals.taskFamily}" when choosing topology ${plan.topology}.`);
		if (signals.coordinationScope) rationale.push(`Requested coordination scope "${signals.coordinationScope}" influenced the workspace layout.`);
		if (signals.evidenceType) rationale.push(`Evidence target "${signals.evidenceType}" guided the choice of summary/detail coordination surfaces.`);
		if (signals.interactionHorizon) rationale.push(`Interaction horizon "${signals.interactionHorizon}" informed the workspace complexity level.`);
		return {
			...plan,
			rationale: [...rationale, ...extras]
		};
	}
	function normalizeSchemaFieldKind(type) {
		const kind = normalizeText(type);
		if (kind === "quantitative" || kind === "number" || kind === "integer" || kind === "float" || kind === "double" || kind === "numeric") return "quantitative";
		return "nominal";
	}
	function inferFieldKinds({ rows, datasetSchema }) {
		const fields = {};
		const sample = Array.isArray(rows) ? rows.slice(0, 50) : [];
		for (const row of sample) {
			if (!row || typeof row !== "object") continue;
			for (const [field, value] of Object.entries(row)) {
				if (!(field in fields)) fields[field] = {
					numeric: 0,
					string: 0,
					other: 0
				};
				if (isNumber(value)) fields[field].numeric += 1;
				else if (typeof value === "string") fields[field].string += 1;
				else fields[field].other += 1;
			}
		}
		if (sample.length === 0 && Array.isArray(datasetSchema?.fields)) return datasetSchema.fields.filter((field) => typeof field?.name === "string" && field.name).map((field) => ({
			field: field.name,
			kind: normalizeSchemaFieldKind(field.type)
		}));
		return Object.entries(fields).map(([field, counts]) => ({
			field,
			kind: counts.numeric >= counts.string ? "quantitative" : "nominal"
		}));
	}
	function findCategoricalField({ spec, rows, datasetSchema }) {
		const colorField = spec?.encoding?.color?.field;
		if (typeof colorField === "string") return colorField;
		const xField = spec?.encoding?.x?.field;
		const yField = spec?.encoding?.y?.field;
		return inferFieldKinds({
			rows,
			datasetSchema
		}).find((entry) => entry.kind === "nominal" && entry.field !== xField && entry.field !== yField)?.field || null;
	}
	function findSeriesField({ spec, rows, datasetSchema }) {
		const colorField = spec?.encoding?.color?.field;
		if (typeof colorField === "string") return colorField;
		const detailField = spec?.encoding?.detail?.field;
		if (typeof detailField === "string") return detailField;
		return inferFieldKinds({
			rows,
			datasetSchema
		}).find((entry) => entry.kind === "nominal")?.field || null;
	}
	function findValueField(spec) {
		const yField = spec?.encoding?.y?.field;
		if (typeof yField === "string") return yField;
		const colorField = spec?.encoding?.color?.field;
		if (typeof colorField === "string") return colorField;
		return null;
	}
	function hasCategoricalAxes(spec) {
		return !!spec?.encoding?.x?.field && !!spec?.encoding?.y?.field;
	}
	function hasQuantitativeXY(spec) {
		return spec?.encoding?.x?.type === "quantitative" && spec?.encoding?.y?.type === "quantitative";
	}
	function createCategorySummarySpec(field) {
		return {
			$schema: "https://vega.github.io/schema/vega-lite/v5.json",
			width: 280,
			height: 220,
			mark: {
				type: "bar",
				cornerRadiusTopLeft: 3,
				cornerRadiusTopRight: 3,
				color: "#4d7ea8"
			},
			encoding: {
				x: {
					field,
					type: "nominal",
					sort: "-y",
					axis: {
						title: field,
						labelAngle: -18
					}
				},
				y: {
					field: "recordCount",
					type: "quantitative",
					axis: { title: "Visible Records" }
				},
				tooltip: [{
					field,
					type: "nominal"
				}, {
					field: "recordCount",
					type: "quantitative"
				}]
			},
			config: {
				view: { stroke: "#d6dbe6" },
				axis: {
					labelColor: "#2d3a4e",
					titleColor: "#2d3a4e"
				}
			}
		};
	}
	function createLineSeriesSummarySpec(field, valueField) {
		return {
			$schema: "https://vega.github.io/schema/vega-lite/v5.json",
			width: 280,
			height: 220,
			mark: {
				type: "bar",
				cornerRadiusTopLeft: 3,
				cornerRadiusTopRight: 3,
				color: "#4d7ea8"
			},
			encoding: {
				x: {
					field,
					type: "nominal",
					sort: "-y",
					axis: {
						title: field,
						labelAngle: -18
					}
				},
				y: {
					field: valueField ? `total_${valueField}` : "recordCount",
					type: "quantitative",
					axis: { title: valueField ? `${valueField} Total` : "Visible Records" }
				},
				tooltip: [{
					field,
					type: "nominal"
				}, {
					field: valueField ? `total_${valueField}` : "recordCount",
					type: "quantitative"
				}]
			},
			config: {
				view: { stroke: "#d6dbe6" },
				axis: {
					labelColor: "#2d3a4e",
					titleColor: "#2d3a4e"
				}
			}
		};
	}
	function createDetailTableSource() {
		return {
			kind: "tableView",
			title: "Filtered Records"
		};
	}
	function buildMapDetailPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: [
					"geoPattern",
					"compare",
					"distribution"
				]
			}, {
				widgetId: `${primaryWidgetId}_table`,
				role: "detail",
				kind: "table",
				title: "Selected Region Records",
				description: "Detail rows corresponding to the currently selected map regions.",
				source: createDetailTableSource(),
				analyticRoles: [
					"lookup",
					"detail",
					"compare"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_table`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_table`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Selected map regions filter the detail table.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}, {
				linkId: `${primaryWidgetId}_share_selection_table`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_table`,
				primitive: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The detail table mirrors the active map-region selection context.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}],
			rationale: ["Map views benefit from a detail table so region-level selections can be inspected as exact records.", "This topology supports geographic overview plus row-level evidence."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Map Detail Workspace"
		});
	}
	function buildParallelCoordinatesDetailPlan({ sessionId, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: [
					"compare",
					"outlier",
					"distribution"
				]
			}, {
				widgetId: `${primaryWidgetId}_table`,
				role: "detail",
				kind: "table",
				title: "Brushed Records",
				description: "Detail rows corresponding to the currently brushed multivariate range.",
				source: createDetailTableSource(),
				analyticRoles: [
					"lookup",
					"detail",
					"compare"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_table`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_table`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Parallel coordinates brushing filters the detail table.",
				fieldMapping: []
			}, {
				linkId: `${primaryWidgetId}_share_selection_table`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_table`,
				primitive: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The detail table mirrors the active multivariate brush context.",
				fieldMapping: []
			}],
			rationale: ["Parallel coordinates often require row-level drill-down to interpret multivariate brushes.", "This topology supports multivariate filtering plus exact record inspection."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Parallel Coordinates Detail Workspace"
		});
	}
	function buildSankeyDetailPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: ["flow", "compare"]
			}, {
				widgetId: `${primaryWidgetId}_table`,
				role: "detail",
				kind: "table",
				title: "Focused Flow Records",
				description: "Detail rows corresponding to the currently focused Sankey flow categories.",
				source: createDetailTableSource(),
				analyticRoles: [
					"lookup",
					"detail",
					"compare"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_table`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_table`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Focused Sankey flows filter the detail table.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}, {
				linkId: `${primaryWidgetId}_share_selection_table`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_table`,
				primitive: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The detail table mirrors the active Sankey flow selection context.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}],
			rationale: ["Sankey views benefit from a detail table so focused flows can be verified against raw records.", "This topology supports flow overview plus exact downstream evidence."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Sankey Detail Workspace"
		});
	}
	function buildLineCoordinationPlan({ sessionId, field, valueField, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [
				{
					widgetId: primaryWidgetId,
					role: "primary",
					source: { kind: "baseSpec" },
					analyticRoles: ["trend", "compare"]
				},
				{
					widgetId: `${primaryWidgetId}_summary`,
					role: "context",
					title: `${field} Trend Summary`,
					description: `Aggregated summary for visible series grouped by ${field}.`,
					source: {
						kind: "templateSpec",
						spec: createLineSeriesSummarySpec(field, valueField)
					},
					dataBinding: {
						sourceDataId: "primary",
						transforms: [{
							kind: "aggregate",
							groupBy: [field],
							metrics: valueField ? [{
								op: "sum",
								field: valueField,
								as: `total_${valueField}`
							}] : [{
								op: "count",
								as: "recordCount"
							}],
							sortBy: {
								field: valueField ? `total_${valueField}` : "recordCount",
								order: "descending"
							}
						}]
					},
					analyticRoles: [
						"compare",
						"rank",
						"trend"
					]
				},
				{
					widgetId: `${primaryWidgetId}_table`,
					role: "detail",
					kind: "table",
					title: "Selected Series Rows",
					description: "Detail rows corresponding to the currently selected line series.",
					source: createDetailTableSource(),
					analyticRoles: [
						"lookup",
						"detail",
						"compare"
					]
				}
			],
			links: [
				{
					linkId: `${primaryWidgetId}_filter_summary`,
					sourceWidgetId: primaryWidgetId,
					targetWidgetId: `${primaryWidgetId}_summary`,
					primitive: "filter",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "Selected line series filter the summary view.",
					fieldMapping: []
				},
				{
					linkId: `${primaryWidgetId}_filter_table`,
					sourceWidgetId: primaryWidgetId,
					targetWidgetId: `${primaryWidgetId}_table`,
					primitive: "filter",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "Selected line series filter the detail table.",
					fieldMapping: []
				},
				{
					linkId: `${primaryWidgetId}_share_selection_table`,
					sourceWidgetId: primaryWidgetId,
					targetWidgetId: `${primaryWidgetId}_table`,
					primitive: "sharesSelection",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "The detail table mirrors the active line-series selection context.",
					fieldMapping: [{
						sourceField: field,
						targetField: field
					}]
				}
			],
			rationale: ["Line views benefit from a grouped summary plus row-level detail for the selected series.", "This topology supports trend comparison without losing record-level evidence."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Coordination` : "Line Coordination Workspace"
		});
	}
	function buildHeatmapDetailPlan({ sessionId, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: [
					"distribution",
					"outlier",
					"compare"
				]
			}, {
				widgetId: `${primaryWidgetId}_table`,
				role: "detail",
				kind: "table",
				title: "Selected Cell Records",
				description: "Detail rows corresponding to the currently selected heatmap cell.",
				source: createDetailTableSource(),
				analyticRoles: [
					"lookup",
					"detail",
					"compare"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_table`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_table`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Selected heatmap cell filters the detail table.",
				fieldMapping: []
			}, {
				linkId: `${primaryWidgetId}_share_selection_table`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_table`,
				primitive: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The detail table mirrors the active heatmap cell selection context.",
				fieldMapping: []
			}],
			rationale: ["Heatmaps benefit from row-level detail to inspect the records behind a selected cell.", "This topology supports matrix overview plus detailed evidence."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Heatmap Detail Workspace"
		});
	}
	function buildSingleViewPlan({ sessionId, spec }) {
		const widgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T1",
			widgets: [{
				widgetId,
				role: "primary",
				source: { kind: "baseSpec" }
			}],
			links: [],
			rationale: ["Single-view topology is sufficient for the current data and task context."],
			planningMode: "minimal_default",
			primaryWidgetId: widgetId,
			title: typeof spec?.title === "string" ? spec.title : "Active Widget"
		});
	}
	function buildCoordinatedPairPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: ["lookup", "compare"]
			}, {
				widgetId: `${primaryWidgetId}_summary`,
				role: "context",
				title: `${field} Distribution`,
				description: `Category summary for ${field} over the current visible rows.`,
				source: {
					kind: "templateSpec",
					spec: createCategorySummarySpec(field)
				},
				dataBinding: {
					sourceDataId: "primary",
					transforms: [{
						kind: "aggregate",
						groupBy: [field],
						metrics: [{
							op: "count",
							as: "recordCount"
						}],
						sortBy: {
							field: "recordCount",
							order: "descending"
						}
					}]
				},
				analyticRoles: [
					"compare",
					"rank",
					"distribution"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_summary`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_summary`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary selection filters the summary view.",
				fieldMapping: []
			}],
			rationale: ["A categorical summary view adds context to the primary analysis view.", "Filter propagation supports coordinated overview and summary inspection."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? spec.title : "Coordinated Workspace"
		});
	}
	function buildScatterDetailPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T3",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: [
					"correlate",
					"outlier",
					"cluster"
				]
			}, {
				widgetId: `${primaryWidgetId}_detail`,
				role: "detail",
				kind: "table",
				title: "Filtered Records",
				description: "Detail table showing the rows currently revealed by the active scatter selection.",
				source: createDetailTableSource(),
				analyticRoles: [
					"lookup",
					"detail",
					"compare"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary scatter selection filters the detail table.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}, {
				linkId: `${primaryWidgetId}_share_selection_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				primitive: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The detail table mirrors the active scatter selection context.",
				fieldMapping: [{
					sourceField: field,
					targetField: field
				}]
			}],
			rationale: [
				"Scatter data with quantitative x/y benefits from a linked detail table for exact record inspection.",
				"The overview scatter directly controls a dedicated detail surface for drill-down evidence.",
				"This workspace topology keeps overview and detail tightly coordinated without adding a third dashboard role."
			],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Scatter Detail Workspace"
		});
	}
	function buildTripleDashboardPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T4",
			widgets: [
				{
					widgetId: primaryWidgetId,
					role: "primary",
					source: { kind: "baseSpec" },
					analyticRoles: [
						"correlate",
						"outlier",
						"cluster"
					]
				},
				{
					widgetId: `${primaryWidgetId}_summary`,
					role: "context",
					title: `${field} Distribution`,
					description: `Category summary for ${field} under the current scatter selection.`,
					source: {
						kind: "templateSpec",
						spec: createCategorySummarySpec(field)
					},
					dataBinding: {
						sourceDataId: "primary",
						transforms: [{
							kind: "aggregate",
							groupBy: [field],
							metrics: [{
								op: "count",
								as: "recordCount"
							}],
							sortBy: {
								field: "recordCount",
								order: "descending"
							}
						}]
					},
					analyticRoles: [
						"compare",
						"rank",
						"distribution"
					]
				},
				{
					widgetId: `${primaryWidgetId}_table`,
					role: "detail",
					kind: "table",
					title: "Filtered Records",
					description: "Detail table showing the records currently revealed by the active scatter selection.",
					source: createDetailTableSource(),
					analyticRoles: [
						"lookup",
						"detail",
						"compare"
					]
				}
			],
			links: [
				{
					linkId: `${primaryWidgetId}_filter_summary`,
					sourceWidgetId: primaryWidgetId,
					targetWidgetId: `${primaryWidgetId}_summary`,
					primitive: "filter",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "Primary scatter selection filters the summary view.",
					fieldMapping: []
				},
				{
					linkId: `${primaryWidgetId}_filter_table`,
					sourceWidgetId: primaryWidgetId,
					targetWidgetId: `${primaryWidgetId}_table`,
					primitive: "filter",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "Primary scatter selection filters the detail table.",
					fieldMapping: []
				},
				{
					linkId: `${primaryWidgetId}_summary_highlight_table`,
					sourceWidgetId: `${primaryWidgetId}_summary`,
					targetWidgetId: `${primaryWidgetId}_table`,
					primitive: "highlight",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "Category selection in the summary view highlights matching records in the detail table.",
					fieldMapping: [{
						sourceField: field,
						targetField: field
					}]
				}
			],
			rationale: [
				"A triple dashboard supports overview, grouped comparison, and exact record inspection in parallel.",
				"Scatter brush drives both grouped summary and detail rows without hiding raw records.",
				"This topology is suitable when the task requires both aggregate comparison and table-level evidence."
			],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Dashboard` : "Triple Dashboard Workspace"
		});
	}
	function buildDrillDownChainPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T5",
			widgets: [
				{
					widgetId: primaryWidgetId,
					role: "primary",
					source: { kind: "baseSpec" },
					analyticRoles: [
						"correlate",
						"outlier",
						"cluster"
					]
				},
				{
					widgetId: `${primaryWidgetId}_summary`,
					role: "context",
					title: `${field} Distribution`,
					description: `Category summary for ${field} under the current scatter selection.`,
					source: {
						kind: "templateSpec",
						spec: createCategorySummarySpec(field)
					},
					dataBinding: {
						sourceDataId: "primary",
						transforms: [{
							kind: "aggregate",
							groupBy: [field],
							metrics: [{
								op: "count",
								as: "recordCount"
							}],
							sortBy: {
								field: "recordCount",
								order: "descending"
							}
						}]
					},
					analyticRoles: [
						"compare",
						"rank",
						"distribution"
					]
				},
				{
					widgetId: `${primaryWidgetId}_detail`,
					role: "detail",
					kind: "table",
					title: "Drill-down Records",
					description: "Detail rows reached through the staged summary-to-detail drill-down path.",
					source: createDetailTableSource(),
					analyticRoles: [
						"lookup",
						"detail",
						"compare"
					]
				}
			],
			links: [{
				linkId: `${primaryWidgetId}_filter_summary`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_summary`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary scatter selection filters the summary view.",
				fieldMapping: []
			}, {
				linkId: `${primaryWidgetId}_summary_filter_detail`,
				sourceWidgetId: `${primaryWidgetId}_summary`,
				targetWidgetId: `${primaryWidgetId}_detail`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Summary-category selections progressively narrow the detail table.",
				fieldMapping: [{
					sourceField: field,
					targetField: field
				}]
			}],
			rationale: ["A drill-down chain should narrow the analysis scope in stages rather than fan out from a single controller.", "The summary view sits between overview and detail so each interaction progressively refines the evidence surface."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Drill-down` : "Drill-down Chain Workspace"
		});
	}
	function buildGlobalControlPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T6",
			widgets: [
				{
					widgetId: primaryWidgetId,
					role: "primary",
					source: { kind: "baseSpec" },
					analyticRoles: [
						"correlate",
						"outlier",
						"cluster"
					]
				},
				{
					widgetId: `${primaryWidgetId}_summary`,
					role: "context",
					title: `${field} Distribution`,
					description: `Category summary for ${field} under the current global control selection.`,
					source: {
						kind: "templateSpec",
						spec: createCategorySummarySpec(field)
					},
					dataBinding: {
						sourceDataId: "primary",
						transforms: [{
							kind: "aggregate",
							groupBy: [field],
							metrics: [{
								op: "count",
								as: "recordCount"
							}],
							sortBy: {
								field: "recordCount",
								order: "descending"
							}
						}]
					},
					analyticRoles: [
						"compare",
						"rank",
						"distribution"
					]
				},
				{
					widgetId: `${primaryWidgetId}_detail`,
					role: "detail",
					kind: "table",
					title: "Filtered Records",
					description: "Detail rows directly controlled by the global selection surface.",
					source: createDetailTableSource(),
					analyticRoles: [
						"lookup",
						"detail",
						"compare"
					]
				}
			],
			links: [{
				linkId: `${primaryWidgetId}_filter_summary`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_summary`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary selection filters the grouped summary view.",
				fieldMapping: []
			}, {
				linkId: `${primaryWidgetId}_filter_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				primitive: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary selection simultaneously filters the detail table.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}],
			rationale: ["A global-control topology fans one interaction surface out to multiple dependent targets.", "Grouped context and detail rows update in parallel from the same primary control widget."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Global Control` : "Global Control Workspace"
		});
	}
	function planWorkspace({ sessionId, spec, task = null, datasetSchema = null, userIntent = null, runMode = "goal_oriented", complexityBudget = "standard", preferredTopology = null }) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		const rows = Array.isArray(spec?.data?.values) ? spec.data.values : [];
		const planningSignals = derivePlanningSignals({
			task,
			userIntent,
			runMode,
			complexityBudget
		});
		const widgetKind = inferWidgetKind$1(spec);
		const categoryField = findCategoricalField({
			spec,
			rows,
			datasetSchema
		});
		const seriesField = findSeriesField({
			spec,
			rows,
			datasetSchema
		});
		const valueField = findValueField(spec);
		const requestedTopology = typeof preferredTopology === "string" ? preferredTopology : null;
		const canUseScatterWorkspace = widgetKind === "scatter" && hasQuantitativeXY(spec) && categoryField;
		const canUseCoordinatedPair = !!categoryField;
		const canUseLineWorkspace = widgetKind === "line" && !!seriesField;
		const canUseHeatmapWorkspace = widgetKind === "heatmap" && hasCategoricalAxes(spec);
		const canUseMapWorkspace = widgetKind === "map" && !!categoryField;
		const canUseParallelWorkspace = widgetKind === "parallelCoordinates";
		const canUseSankeyWorkspace = widgetKind === "sankey" && !!categoryField;
		const expansiveMode = planningSignals.expansiveMode;
		const canUseTripleDashboard = canUseScatterWorkspace;
		const taskDrivenMultiWidget = planningSignals.wantsMultiWidget;
		const taskDrivenDetail = planningSignals.wantsDetailEvidence;
		const taskDrivenSummary = planningSignals.wantsSummaryContext;
		if (requestedTopology === "T2" && canUseLineWorkspace) return appendPlanningSignals(buildLineCoordinationPlan({
			sessionId,
			field: seriesField,
			valueField,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseHeatmapWorkspace) return appendPlanningSignals(buildHeatmapDetailPlan({
			sessionId,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseMapWorkspace) return appendPlanningSignals(buildMapDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseParallelWorkspace) return appendPlanningSignals(buildParallelCoordinatesDetailPlan({
			sessionId,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseSankeyWorkspace) return appendPlanningSignals(buildSankeyDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T4" && canUseTripleDashboard) return appendPlanningSignals(buildTripleDashboardPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T3" && canUseScatterWorkspace) return appendPlanningSignals(buildScatterDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T5" && canUseScatterWorkspace) return appendPlanningSignals(buildDrillDownChainPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T6" && canUseScatterWorkspace) return appendPlanningSignals(buildGlobalControlPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseCoordinatedPair) return appendPlanningSignals(buildCoordinatedPairPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T1") return appendPlanningSignals(buildSingleViewPlan({
			sessionId,
			spec
		}), planningSignals);
		if (canUseTripleDashboard && (complexityBudget === "extended" || taskDrivenMultiWidget && taskDrivenSummary && taskDrivenDetail)) return appendPlanningSignals(buildTripleDashboardPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals, ["Planner selected a triple-dashboard topology because the task requires cross-widget evidence, grouped context, and record-level detail together."]);
		if (canUseScatterWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildScatterDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals, ["Planner selected a scatter-detail topology because the current task requires interaction-driven evidence or coordinated follow-up views."]);
		if (canUseLineWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenSummary || taskDrivenMultiWidget)) return appendPlanningSignals(buildLineCoordinationPlan({
			sessionId,
			field: seriesField,
			valueField,
			spec
		}), planningSignals);
		if (canUseHeatmapWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildHeatmapDetailPlan({
			sessionId,
			spec
		}), planningSignals);
		if (canUseMapWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildMapDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (canUseParallelWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildParallelCoordinatesDetailPlan({
			sessionId,
			spec
		}), planningSignals);
		if (canUseSankeyWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildSankeyDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (canUseCoordinatedPair && (expansiveMode || taskDrivenSummary || taskDrivenMultiWidget)) return appendPlanningSignals(buildCoordinatedPairPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals, ["Planner selected a coordinated-pair topology to provide an overview view plus grouped context without adding full detail-table complexity."]);
		return appendPlanningSignals(buildSingleViewPlan({
			sessionId,
			spec
		}), planningSignals, ["Planner kept a single-view topology because the current task signals do not require additional coordination surfaces."]);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/workspacePlanBuilder.js
	function getTitleText$1(title) {
		if (typeof title === "string") return title;
		if (title && typeof title === "object" && typeof title.text === "string") return title.text;
		return null;
	}
	function buildBaseWidgetPlan({ sessionId, spec }) {
		const widgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T1",
			widgets: [{
				widgetId,
				role: "primary",
				source: { kind: "baseSpec" }
			}],
			links: [],
			source: "runtime_default",
			planningMode: "minimal_default",
			primaryWidgetId: widgetId,
			title: getTitleText$1(spec?.title) || "Active Widget"
		});
	}
	function normalizeWorkspacePlan({ sessionId, spec, workspaceSpec, planningRequest = null }) {
		const validation = validateWorkspaceSpec(workspaceSpec);
		if (!workspaceSpec || !validation.ok || !Array.isArray(workspaceSpec.widgets) || workspaceSpec.widgets.length === 0) {
			const planned = planWorkspace({
				sessionId,
				spec,
				...makeWorkspacePlanningRequest(planningRequest || {})
			});
			return {
				plan: planned || buildBaseWidgetPlan({
					sessionId,
					spec
				}),
				validation,
				summary: summarizeWorkspaceSpec(workspaceSpec),
				planningSummary: planned || null,
				materializedFromSpec: false,
				materializedFromPlanner: !!planned
			};
		}
		const primaryWidgetId = workspaceSpec.widgets[0]?.widgetId;
		return {
			plan: makeWorkspacePlanningResult({
				topology: workspaceSpec.topology || "T1",
				widgets: workspaceSpec.widgets,
				links: Array.isArray(workspaceSpec.links) ? workspaceSpec.links : [],
				source: "workspace_spec",
				planningMode: "explicit_spec",
				primaryWidgetId,
				title: getTitleText$1(spec?.title) || "Workspace"
			}),
			validation,
			summary: summarizeWorkspaceSpec(workspaceSpec),
			planningSummary: null,
			materializedFromSpec: true,
			materializedFromPlanner: false
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/selectionStateModel.js
	function clone$19(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function cloneObject$1(value) {
		return value && typeof value === "object" && !Array.isArray(value) ? clone$19(value) : {};
	}
	function normalizeString$1(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizePredicates(value) {
		return Array.isArray(value) ? clone$19(value) : [];
	}
	function normalizePrimarySelectionPayload(value) {
		if (!value || typeof value !== "object" || Array.isArray(value)) return null;
		return clone$19(value);
	}
	function normalizeWidgetGroupingKey(value) {
		return normalizeString$1(value);
	}
	function normalizePrimarySelectionView(primary = null, registry = {}) {
		const normalizedPrimary = normalizePrimarySelectionPayload(primary);
		if (!normalizedPrimary) return null;
		const normalizedRegistry = cloneObject$1(registry);
		const selectionRef = normalizeString$1(normalizedPrimary.selectionRef || normalizedPrimary.selection_ref);
		const registryEntry = selectionRef ? normalizedRegistry[selectionRef] || null : null;
		const merged = {
			...registryEntry && typeof registryEntry === "object" ? clone$19(registryEntry) : {},
			...normalizedPrimary
		};
		return {
			...merged,
			selectionRef: selectionRef || normalizeString$1(registryEntry?.selectionRef || registryEntry?.selection_ref),
			selectionId: normalizeString$1(merged.selectionId || merged.selection_id),
			sourceWidgetRef: normalizeString$1(merged.sourceWidgetRef || merged.source_widget_ref),
			sourceWidgetId: normalizeString$1(merged.sourceWidgetId || merged.source_widget_id),
			summary: typeof merged.summary === "string" ? merged.summary : "",
			predicates: normalizePredicates(merged.predicates),
			selectionDataRef: normalizeString$1(merged.selectionDataRef || merged.selection_data_ref),
			scope: normalizeString$1(merged.scope) || "local"
		};
	}
	function deriveByWidgetViewFromRegistry(registry = {}) {
		const normalizedRegistry = cloneObject$1(registry);
		const nextByWidget = {};
		for (const [selectionRef, selectionState] of Object.entries(normalizedRegistry)) {
			if (!selectionState || typeof selectionState !== "object") continue;
			const normalizedView = normalizePrimarySelectionView({
				...selectionState,
				selectionRef
			}, normalizedRegistry);
			const widgetKey = normalizeWidgetGroupingKey(normalizedView?.sourceWidgetId || normalizedView?.source_widget_id || normalizedView?.sourceWidgetRef || normalizedView?.source_widget_ref);
			if (!widgetKey || !normalizedView) continue;
			nextByWidget[widgetKey] = normalizedView;
		}
		return nextByWidget;
	}
	function readSelectionRegistry(shared = {}) {
		const canonicalRegistry = cloneObject$1(shared?.selections?.registry);
		if (Object.keys(canonicalRegistry).length > 0) return canonicalRegistry;
		return cloneObject$1(shared?.activeSelections);
	}
	function readSelectionPrimaryView(shared = {}) {
		const registry = readSelectionRegistry(shared);
		const explicitPrimary = shared?.selections?.views?.primary;
		if (explicitPrimary != null) return normalizePrimarySelectionView(explicitPrimary, registry);
		const legacyPrimarySelectionRef = normalizeString$1(shared?.primarySelectionRef);
		return legacyPrimarySelectionRef ? normalizePrimarySelectionView({ selectionRef: legacyPrimarySelectionRef }, registry) : null;
	}
	function readSelectionByWidgetView(shared = {}) {
		const registry = readSelectionRegistry(shared);
		const explicitByWidget = shared?.selections?.views?.byWidget;
		if (!explicitByWidget || typeof explicitByWidget !== "object" || Array.isArray(explicitByWidget)) return deriveByWidgetViewFromRegistry(registry);
		const normalizedByWidget = {};
		for (const [widgetKey, value] of Object.entries(explicitByWidget)) {
			const normalizedWidgetKey = normalizeWidgetGroupingKey(widgetKey);
			const normalizedView = normalizePrimarySelectionView(value, registry);
			if (!normalizedWidgetKey || !normalizedView) continue;
			normalizedByWidget[normalizedWidgetKey] = normalizedView;
		}
		return Object.keys(normalizedByWidget).length > 0 ? normalizedByWidget : deriveByWidgetViewFromRegistry(registry);
	}
	function resolvePrimarySelectionEntry(shared = {}) {
		const registry = readSelectionRegistry(shared);
		const primaryView = readSelectionPrimaryView(shared);
		const primarySelectionRef = primaryView?.selectionRef || null;
		if (primarySelectionRef && registry[primarySelectionRef]) return [primarySelectionRef, normalizePrimarySelectionView(primaryView, registry)];
		const selectionEntries = Object.entries(registry);
		if (selectionEntries.length === 0) return null;
		const [selectionRef, selectionState] = selectionEntries[0];
		return [selectionRef, normalizePrimarySelectionView({
			...selectionState && typeof selectionState === "object" ? selectionState : {},
			selectionRef
		}, registry)];
	}
	function withSelectionSubmodel(shared = {}, { registry, primary, byWidget } = {}) {
		const previousSelections = shared?.selections || {};
		const previousViews = previousSelections?.views || {};
		const nextRegistry = registry === void 0 ? readSelectionRegistry(shared) : cloneObject$1(registry);
		const nextPrimary = primary === void 0 ? normalizePrimarySelectionView(previousViews?.primary, nextRegistry) || (normalizeString$1(shared?.primarySelectionRef) ? normalizePrimarySelectionView({ selectionRef: shared.primarySelectionRef }, nextRegistry) : null) : normalizePrimarySelectionView(primary, nextRegistry);
		const nextByWidget = byWidget === void 0 ? readSelectionByWidgetView({
			...shared || {},
			selections: {
				...previousSelections,
				registry: nextRegistry,
				views: {
					...previousViews,
					byWidget: previousViews?.byWidget
				}
			}
		}) : (() => {
			const normalizedByWidget = {};
			const rawByWidget = cloneObject$1(byWidget);
			for (const [widgetKey, value] of Object.entries(rawByWidget)) {
				const normalizedWidgetKey = normalizeWidgetGroupingKey(widgetKey);
				const normalizedView = normalizePrimarySelectionView(value, nextRegistry);
				if (!normalizedWidgetKey || !normalizedView) continue;
				normalizedByWidget[normalizedWidgetKey] = normalizedView;
			}
			return normalizedByWidget;
		})();
		return {
			...shared || {},
			activeSelections: nextRegistry,
			primarySelectionRef: nextPrimary?.selectionRef || null,
			selections: {
				...previousSelections,
				registry: nextRegistry,
				views: {
					...previousViews,
					primary: nextPrimary,
					byWidget: nextByWidget
				}
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/sharedStateDerivation.js
	function uniquePredicates(predicates = []) {
		const seen = /* @__PURE__ */ new Set();
		const nextPredicates = [];
		for (const predicate of Array.isArray(predicates) ? predicates : []) {
			const key = JSON.stringify(predicate);
			if (seen.has(key)) continue;
			seen.add(key);
			nextPredicates.push(predicate);
		}
		return nextPredicates;
	}
	function resolveLinkedSelectionPredicates(transform, activeSelections = {}) {
		const sourceSelectionRef = transform?.source || transform?.sourceSelectionRef || transform?.spec?.sourceSelectionRef || null;
		if (sourceSelectionRef && Array.isArray(activeSelections?.[sourceSelectionRef]?.predicates)) return activeSelections[sourceSelectionRef].predicates;
		const sourceWidgetId = transform?.sourceWidgetId || transform?.spec?.sourceWidgetId || null;
		if (!sourceWidgetId) return [];
		return Object.entries(activeSelections || {}).filter(([selectionRef, selectionState]) => typeof selectionRef === "string" && selectionRef.includes(`/widget/${sourceWidgetId}/selection/`) && Array.isArray(selectionState?.predicates)).flatMap(([, selectionState]) => selectionState.predicates);
	}
	function deriveGlobalFiltersFromState(state) {
		const widgets = state?.widgets || {};
		const activeSelections = readSelectionRegistry(state?.shared || {});
		const globalFilters = {};
		for (const [widgetRef, widgetState] of Object.entries(widgets)) {
			const transforms = Array.isArray(widgetState?.transforms) ? widgetState.transforms : [];
			const transformPredicates = extractPredicatesFromTransforms(transforms.map((transform) => transform?.spec || transform));
			const linkedPredicates = transforms.filter((transform) => transform?.kind === "filter").flatMap((transform) => resolveLinkedSelectionPredicates(transform, activeSelections));
			const predicates = uniquePredicates([...transformPredicates, ...linkedPredicates]);
			if (predicates.length > 0) globalFilters[widgetRef] = predicates;
		}
		return globalFilters;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/deriveWorkspaceTopology.js
	var TOPOLOGY_LABELS = {
		T1: "Single View",
		T2: "Coordinated Pair",
		T3: "Overview + Detail",
		T4: "Triple Dashboard",
		T5: "Drill-down Chain",
		T6: "Global Control + Multiple Targets"
	};
	var TOPOLOGY_LINK_KINDS = new Set([
		"filter",
		"filters",
		"highlight",
		"highlights",
		"syncDomain",
		"syncsDomain",
		"sharesSelection",
		"comparesWith"
	]);
	function unique(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter(Boolean))];
	}
	function resolveWidgetId(ref) {
		return parseRef(ref)?.widgetId || null;
	}
	function buildDegreeMaps(widgetIds, links) {
		const outDegree = new Map(widgetIds.map((widgetId) => [widgetId, 0]));
		const inDegree = new Map(widgetIds.map((widgetId) => [widgetId, 0]));
		const adjacency = new Map(widgetIds.map((widgetId) => [widgetId, /* @__PURE__ */ new Set()]));
		for (const link of links) {
			const sourceWidgetId = link?.sourceWidgetId || resolveWidgetId(link?.from);
			const targetWidgetId = link?.targetWidgetId || resolveWidgetId(link?.to);
			if (!sourceWidgetId || !targetWidgetId || sourceWidgetId === targetWidgetId) continue;
			if (!adjacency.has(sourceWidgetId)) adjacency.set(sourceWidgetId, /* @__PURE__ */ new Set());
			if (!outDegree.has(sourceWidgetId)) outDegree.set(sourceWidgetId, 0);
			if (!inDegree.has(targetWidgetId)) inDegree.set(targetWidgetId, 0);
			const targets = adjacency.get(sourceWidgetId);
			if (targets.has(targetWidgetId)) continue;
			targets.add(targetWidgetId);
			outDegree.set(sourceWidgetId, (outDegree.get(sourceWidgetId) || 0) + 1);
			inDegree.set(targetWidgetId, (inDegree.get(targetWidgetId) || 0) + 1);
		}
		return {
			outDegree,
			inDegree,
			adjacency
		};
	}
	function isLinearChain(widgetIds, edgeCount, outDegree, inDegree) {
		if (widgetIds.length < 3) return false;
		if (edgeCount !== widgetIds.length - 1) return false;
		const starts = widgetIds.filter((widgetId) => (inDegree.get(widgetId) || 0) === 0 && (outDegree.get(widgetId) || 0) === 1);
		const ends = widgetIds.filter((widgetId) => (inDegree.get(widgetId) || 0) === 1 && (outDegree.get(widgetId) || 0) === 0);
		const middles = widgetIds.filter((widgetId) => (inDegree.get(widgetId) || 0) === 1 && (outDegree.get(widgetId) || 0) === 1);
		return starts.length === 1 && ends.length === 1 && middles.length === widgetIds.length - 2;
	}
	function hasDetailRole(widgets) {
		return (Array.isArray(widgets) ? widgets : []).some((widget) => widget?.role === "detail");
	}
	function hasContextRole(widgets) {
		return (Array.isArray(widgets) ? widgets : []).some((widget) => widget?.role === "context");
	}
	function isTopologyLink(link) {
		const normalizedLink = normalizeWidgetLink(link);
		return TOPOLOGY_LINK_KINDS.has(normalizedLink?.kind) || TOPOLOGY_LINK_KINDS.has(normalizedLink?.primitive);
	}
	function deriveWorkspaceTopology({ widgets = [], links = [] } = {}) {
		const widgetIds = unique((Array.isArray(widgets) ? widgets : []).map((widget) => widget?.widgetId || resolveWidgetId(widget?.ref)).filter(Boolean));
		const { outDegree, inDegree } = buildDegreeMaps(widgetIds, (Array.isArray(links) ? links : []).map((link) => normalizeWidgetLink(link)).filter(isTopologyLink));
		const edgeCount = Array.from(outDegree.values()).reduce((sum, value) => sum + value, 0);
		const maxOutDegree = Math.max(0, ...Array.from(outDegree.values()));
		const maxInDegree = Math.max(0, ...Array.from(inDegree.values()));
		const sourceWidgetCount = Array.from(outDegree.values()).filter((value) => value > 0).length;
		const targetWidgetCount = Array.from(inDegree.values()).filter((value) => value > 0).length;
		const possibleDirectedEdges = widgetIds.length > 1 ? widgetIds.length * (widgetIds.length - 1) : 1;
		const linkDensity = Number((edgeCount / possibleDirectedEdges).toFixed(3));
		let topology = "T1";
		const rationale = [];
		if (widgetIds.length <= 1) {
			topology = "T1";
			rationale.push("A single widget is active in the current workspace.");
		} else if (isLinearChain(widgetIds, edgeCount, outDegree, inDegree)) {
			topology = "T5";
			rationale.push("The link graph forms a linear multi-step chain.");
		} else if (widgetIds.length >= 3 && hasDetailRole(widgets) && hasContextRole(widgets) && sourceWidgetCount > 1) {
			topology = "T4";
			rationale.push("The workspace includes coordinated overview, context, and detail roles across multiple propagation stages.");
		} else if (maxOutDegree >= 2) {
			topology = "T6";
			rationale.push("One widget currently propagates to multiple targets.");
		} else if (hasDetailRole(widgets)) {
			topology = "T3";
			rationale.push("A detail-role widget is present in the current workspace plan.");
		} else if (widgetIds.length === 2) {
			topology = "T2";
			rationale.push("Two widgets are active, forming a coordinated pair.");
		} else {
			topology = "T4";
			rationale.push("Three or more widgets are active without a single fan-out controller.");
		}
		return makeWorkspaceTopologySummary({
			topology,
			topologyLabel: TOPOLOGY_LABELS[topology] || topology,
			widgetCount: widgetIds.length,
			edgeCount,
			linkDensity,
			sourceWidgetCount,
			targetWidgetCount,
			maxOutDegree,
			maxInDegree,
			rationale
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/summarizeWidgetAdapter.js
	function clone$18(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function summarizeWidgetAdapter(adapter) {
		if (!adapter) return null;
		const humanInteraction = adapter.getHumanInteractionConfig?.() || null;
		const description = adapter.getDescription?.() || null;
		return makeWidgetAdapterSummary({
			widgetRef: adapter.widgetRef,
			dataRef: adapter.dataRef || null,
			kind: adapter.kind || description?.kind || null,
			title: description?.title || "",
			description: description?.description || "",
			analyticRoles: Array.isArray(description?.analyticRoles) ? [...description.analyticRoles] : [],
			primaryDataRef: description?.primaryDataRef || null,
			provider: adapter.provider || "custom",
			providerCapabilities: makeWidgetAdapterProviderCapabilities(clone$18(adapter.providerCapabilities || {})),
			role: description?.role || null,
			sourceKind: description?.sourceKind || null,
			supportsSpecMutation: description?.supportsSpecMutation === true,
			metadata: clone$18(adapter.metadata || {}),
			usageNotes: Array.isArray(description?.usageNotes) ? [...description.usageNotes] : [],
			actionNames: Array.isArray(description?.actionNames) ? [...description.actionNames] : [],
			perceptionQueryNames: Array.isArray(description?.perceptionQueryNames) ? [...description.perceptionQueryNames] : [],
			humanInteraction: makeWidgetAdapterHumanInteraction(clone$18(humanInteraction)),
			capabilities: makeWidgetAdapterCapabilities({
				canDescribe: typeof adapter.getDescription === "function",
				canReadState: typeof adapter.getState === "function",
				canApplyState: typeof adapter.applyState === "function",
				canMount: typeof adapter.mount === "function",
				canUpdate: typeof adapter.update === "function",
				canDisposeRenderer: typeof adapter.dispose === "function",
				canBindHumanInteractions: typeof adapter.bindHumanInteractions === "function",
				canReadSelection: typeof adapter.readSelection === "function",
				canReadViewport: typeof adapter.readViewport === "function",
				canRegisterActions: typeof adapter.registerActions === "function",
				canRegisterPerceptionQueries: typeof adapter.registerPerceptionQueries === "function"
			})
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/WorkspaceMaterializer.js
	var dataQueryEngine = createDataQueryEngine({ kind: "js_array" });
	function getInlineRows(spec) {
		const rows = spec?.data?.values;
		return Array.isArray(rows) ? rows : [];
	}
	function getTitleText(title) {
		if (typeof title === "string") return title;
		if (title && typeof title === "object" && typeof title.text === "string") return title.text;
		return null;
	}
	function isSelectionPayload(selection) {
		return selection != null && typeof selection === "object" && !Array.isArray(selection);
	}
	function normalizeSelectionPayloadEntries({ selections, fallbackSelection }) {
		const entries = [];
		const pushSelection = (selection, fallbackKey = null) => {
			if (!isSelectionPayload(selection)) return;
			const sourceWidgetId = selection.source_widget_id || null;
			const selectionId = selection.selection_id || selection.id || null;
			if (!sourceWidgetId || !selectionId) return;
			entries.push([
				fallbackKey || `${sourceWidgetId}::${selectionId}`,
				selection,
				{
					sourceWidgetId,
					selectionId
				}
			]);
		};
		if (selections && typeof selections === "object" && !Array.isArray(selections)) for (const [key, value] of Object.entries(selections)) pushSelection(value, key || null);
		if (entries.length === 0 && isSelectionPayload(fallbackSelection)) pushSelection(fallbackSelection);
		return entries;
	}
	function buildActiveSelectionMap({ appId, workspaceId, selections, fallbackSelection }) {
		return Object.fromEntries(normalizeSelectionPayloadEntries({
			selections,
			fallbackSelection
		}).map(([, selection, selectionMeta]) => [makeSelectionRef({
			appId,
			workspaceId,
			widgetId: selectionMeta.sourceWidgetId,
			selectionId: selectionMeta.selectionId
		}), makeSelectionState(buildSelectionStateInput(selection))]));
	}
	function buildActiveSelectionEntries({ appId, workspaceId, selections, fallbackSelection }) {
		return normalizeSelectionPayloadEntries({
			selections,
			fallbackSelection
		}).map(([, selection, selectionMeta]) => ({
			ref: makeSelectionRef({
				appId,
				workspaceId,
				widgetId: selectionMeta.sourceWidgetId,
				selectionId: selectionMeta.selectionId
			}),
			widgetId: selectionMeta.sourceWidgetId,
			selectionId: selectionMeta.selectionId,
			selection
		}));
	}
	function resolvePrimarySelection({ selection, selections, preferredWidgetId = null }) {
		if (isSelectionPayload(selection)) return selection;
		const entries = normalizeSelectionPayloadEntries({
			selections,
			fallbackSelection: null
		});
		if (entries.length === 0) return null;
		if (preferredWidgetId) {
			const preferredEntry = entries.find(([, , selectionMeta]) => selectionMeta?.sourceWidgetId === preferredWidgetId);
			if (preferredEntry?.[1]) return preferredEntry[1];
		}
		if (entries.length === 1) return entries[0]?.[1] || null;
		return entries[entries.length - 1]?.[1] || null;
	}
	function buildPlanningTaskContext({ planningRequest, coordinationScope, targetWidgetRefs }) {
		const task = planningRequest?.task || null;
		const runMode = planningRequest?.runMode || null;
		const complexityBudget = planningRequest?.complexityBudget || null;
		const userIntent = planningRequest?.userIntent || null;
		const userQuery = task?.userQuery || userIntent || null;
		const expectedAnswerType = task?.answerType || task?.expectedAnswerType || null;
		const taskMode = task?.taskMode || runMode || "goal_oriented";
		return {
			taskId: task?.taskId || null,
			userQuery,
			taskMode,
			runMode,
			coordinationScope: task?.coordinationScope || coordinationScope,
			expectedAnswerType,
			interactionHorizon: task?.interactionHorizon || null,
			evidenceType: task?.evidenceType || null,
			complexityBudget,
			targetWidgetRefs
		};
	}
	function inferWidgetKind(spec) {
		if (spec?.kind === "table") return "table";
		if (spec?.kind === "parallelCoordinates") return "parallelCoordinates";
		if (spec?.kind === "sankey") return "sankey";
		if (spec?.kind === "map") return "map";
		const mark = typeof spec?.mark === "string" ? spec.mark : spec?.mark?.type;
		if (mark === "bar") return "bar";
		if (mark === "line") return "line";
		if (mark === "point" || mark === "circle") return "scatter";
		if (mark === "rect") return "heatmap";
		return "custom";
	}
	function inferAnalyticRoles(kind) {
		switch (kind) {
			case "scatter": return [
				"correlate",
				"cluster",
				"outlier",
				"distribution"
			];
			case "bar": return [
				"compare",
				"rank",
				"distribution"
			];
			case "line": return ["trend", "compare"];
			case "heatmap": return [
				"distribution",
				"outlier",
				"compare"
			];
			case "table": return [
				"lookup",
				"detail",
				"compare"
			];
			case "parallelCoordinates": return [
				"compare",
				"outlier",
				"distribution"
			];
			case "sankey": return ["flow", "compare"];
			case "map": return [
				"geoPattern",
				"compare",
				"distribution"
			];
			default: return ["lookup"];
		}
	}
	function resolveWidgetProvider(widgetDef = {}) {
		const sourceProvider = typeof widgetDef?.source?.provider === "string" ? widgetDef.source.provider : null;
		const providerSpecProvider = typeof widgetDef?.source?.providerSpec?.provider === "string" ? widgetDef.source.providerSpec.provider : null;
		const widgetProvider = typeof widgetDef?.provider === "string" ? widgetDef.provider : null;
		return sourceProvider || providerSpecProvider || widgetProvider || "vega-lite";
	}
	function aggregateRows(rows, transform) {
		return dataQueryEngine.aggregate(rows, {
			groupBy: Array.isArray(transform?.groupBy) ? transform.groupBy.filter(Boolean) : [],
			measures: Array.isArray(transform?.metrics) ? transform.metrics : [],
			sortBy: transform?.sortBy,
			limit: transform?.limit
		}).rows;
	}
	function buildLinkRef({ appId, workspaceId, linkId }) {
		return `wl://${appId}/workspace/${workspaceId}/link/${linkId}`;
	}
	function buildSpecFromSource({ source, baseSpec, rows }) {
		if (source?.kind === "baseSpec") {
			const nextSpec = JSON.parse(JSON.stringify(baseSpec));
			nextSpec.data = { values: rows };
			return nextSpec;
		}
		if (source?.kind === "templateSpec" && source.spec) {
			const nextSpec = JSON.parse(JSON.stringify(source.spec));
			nextSpec.data = { values: rows };
			return nextSpec;
		}
		if (source?.kind === "tableView") return {
			kind: "table",
			title: source.title || "Table Detail",
			columns: rows[0] && typeof rows[0] === "object" && !Array.isArray(rows[0]) ? Object.keys(rows[0]) : [],
			data: { values: rows }
		};
		return null;
	}
	function getSourceTransforms({ widgetDef, baseSpec }) {
		if (widgetDef?.source?.kind === "baseSpec") return Array.isArray(baseSpec?.transform) ? baseSpec.transform : [];
		if (widgetDef?.source?.kind === "templateSpec") return Array.isArray(widgetDef?.source?.spec?.transform) ? widgetDef.source.spec.transform : [];
		return [];
	}
	function materializeRows({ baseRows, widgetDef, inboundLinks, activeSelections, sourceTransforms = [] }) {
		let rows = applySupportedSpecTransforms({
			rows: baseRows,
			transforms: sourceTransforms,
			dataQueryEngine
		});
		const filteringLinks = inboundLinks.filter((link) => link.primitive === "filter");
		if (filteringLinks.length > 0 && Array.isArray(activeSelections) && activeSelections.length > 0) for (const link of filteringLinks) {
			const relevantSelections = activeSelections.filter((entry) => entry?.widgetId === link.sourceWidgetId);
			for (const selectionEntry of relevantSelections) {
				const mappedSelection = mapSelectionToTargetSelection(selectionEntry.selection, link.fieldMapping);
				rows = dataQueryEngine.filter(rows, mappedSelection?.predicates || []);
			}
		}
		const transforms = Array.isArray(widgetDef?.dataBinding?.transforms) ? widgetDef.dataBinding.transforms : [];
		for (const transform of transforms) if (transform?.kind === "aggregate") rows = aggregateRows(rows, transform);
		return rows;
	}
	function materializeWorkspace({ appId = "widgetva-app", workspaceId = "main", sessionId, spec, workspaceSpec = null, planningRequest = null, replayContext = null, selection, selections = null, focusedWidgetRef = null, focusState = null, highlightState = null, viewportState = null, annotations = [], previousState = null, currentBranchId = "main", stateManager = null }) {
		const { plan, validation: workspaceSpecValidation, summary: workspaceSpecSummary, planningSummary, materializedFromSpec, materializedFromPlanner } = normalizeWorkspacePlan({
			sessionId,
			spec,
			workspaceSpec,
			planningRequest
		});
		const workspaceSpecStatus = !workspaceSpecSummary ? materializedFromPlanner ? "planned" : "idle" : workspaceSpecValidation.ok ? materializedFromSpec ? "materialized" : "deferred" : "invalid";
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return {
			description: {
				appId,
				workspaceId,
				generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
				workspaceCapabilities: ["singleWidgetAnalysis"],
				taskContext: null,
				planning: {
					requestedWorkspaceSpec: workspaceSpecSummary,
					planningRequest: planningRequest || null,
					workspaceSpecStatus,
					workspaceSpecIssues: workspaceSpecValidation.issues,
					materializedFromSpec,
					materializedFromPlanner,
					planner: planningSummary
				},
				widgets: [],
				dataHandles: [],
				links: [],
				actions: [],
				perceptionQueries: []
			},
			state: {
				stateId: `${workspaceId}:empty`,
				createdAt: (/* @__PURE__ */ new Date()).toISOString(),
				widgets: {},
				shared: {
					selections: {
						registry: {},
						views: {
							primary: null,
							byWidget: {}
						}
					},
					globalFilters: {}
				}
			},
			replayContext
		};
		const dataHandle = buildDataHandle({
			appId,
			workspaceId,
			spec
		});
		const baseRows = getInlineRows(spec);
		const runtimeData = { [dataHandle.ref]: {
			ref: dataHandle.ref,
			rows: baseRows,
			handle: dataHandle
		} };
		const primarySelection = resolvePrimarySelection({
			selection,
			selections,
			preferredWidgetId: plan.primaryWidgetId || null
		});
		const activeSelections = buildActiveSelectionMap({
			appId,
			workspaceId,
			selections,
			fallbackSelection: primarySelection
		});
		const activeSelectionEntries = buildActiveSelectionEntries({
			appId,
			workspaceId,
			selections,
			fallbackSelection: primarySelection
		});
		const normalizedViewportState = normalizeViewportState(viewportState);
		const selectionSourceWidgetId = primarySelection?.source_widget_id || plan.primaryWidgetId;
		primarySelection?.selection_id && selectionSourceWidgetId && makeSelectionRef({
			appId,
			workspaceId,
			widgetId: selectionSourceWidgetId,
			selectionId: primarySelection.selection_id
		});
		const widgets = {};
		const widgetDescriptions = [];
		const widgetAdapters = [];
		const actionDescriptors = [];
		const perceptionDescriptors = [];
		const dataHandles = [dataHandle];
		const structuralLinks = [];
		const widgetRefsById = /* @__PURE__ */ new Map();
		for (const widgetDef of plan.widgets) {
			const widgetRef = makeWidgetRef({
				appId,
				workspaceId,
				widgetId: widgetDef.widgetId
			});
			widgetRefsById.set(widgetDef.widgetId, widgetRef);
		}
		for (const widgetDef of plan.widgets) {
			const widgetRef = widgetRefsById.get(widgetDef.widgetId);
			const inboundLinks = (plan.links || []).filter((link) => link.targetWidgetId === widgetDef.widgetId);
			const localSelectionEntries = activeSelectionEntries.filter((entry) => entry.widgetId === widgetDef.widgetId);
			const mirroredSelectionEntries = inboundLinks.filter((link) => link.primitive === "sharesSelection").flatMap((link) => activeSelectionEntries.filter((entry) => entry.widgetId === link.sourceWidgetId).map((entry) => ({
				ref: makeSelectionRef({
					appId,
					workspaceId,
					widgetId: widgetDef.widgetId,
					selectionId: `shared_from_${link.sourceWidgetId}_${entry.selectionId}`
				}),
				selection: mapSelectionToTargetSelection(entry.selection, link.fieldMapping),
				sourceWidgetId: link.sourceWidgetId
			})).filter((entry) => entry.selection));
			const widgetSelectionEntries = localSelectionEntries.map((entry) => ({
				ref: entry.ref,
				selection: entry.selection
			}));
			const widgetRuntimeSelections = [...widgetSelectionEntries.map((entry) => entry.selection), ...mirroredSelectionEntries.map((entry) => entry.selection)];
			const materializedRows = materializeRows({
				baseRows,
				widgetDef,
				inboundLinks,
				activeSelections: activeSelectionEntries,
				sourceTransforms: getSourceTransforms({
					widgetDef,
					baseSpec: spec
				})
			});
			const visibleDataRef = makeDataRef({
				appId,
				workspaceId,
				dataId: `${widgetDef.widgetId}_visible`
			});
			const widgetSpec = buildSpecFromSource({
				source: widgetDef.source,
				baseSpec: spec,
				rows: materializedRows
			});
			const declaredWidgetKind = widgetDef.kind || null;
			if (!widgetSpec && declaredWidgetKind !== "table") continue;
			const runtimeSpec = applyLinkedViewStateToSpec({
				widgetSpec,
				inboundLinks,
				activeSelections: activeSelectionEntries.filter((entry) => inboundLinks.some((link) => link.primitive === "syncDomain" && link.sourceWidgetId === entry.widgetId)).map((entry) => entry.selection)
			});
			const selectionVisibleOnWidget = widgetRuntimeSelections.length > 0;
			const materializedSelectedCount = selectionVisibleOnWidget ? countSelectedRowsForSelections(materializedRows, widgetRuntimeSelections) : 0;
			const selectedRows = selectionVisibleOnWidget ? materializedRows.filter((row) => rowMatchesAnySelection(row, widgetRuntimeSelections)) : [];
			const highlightedSpec = applyHighlightToSpec({
				widgetSpec: applySelectionToSpec({
					widgetSpec: runtimeSpec,
					activeSelections: widgetRuntimeSelections,
					selectionEnabled: selectionVisibleOnWidget
				}),
				inboundLinks,
				activeSelections: activeSelectionEntries.filter((entry) => inboundLinks.some((link) => link.primitive === "highlight" && link.sourceWidgetId === entry.widgetId)).map((entry) => mapSelectionToTargetSelection(entry.selection, inboundLinks.find((link) => link.primitive === "highlight" && link.sourceWidgetId === entry.widgetId)?.fieldMapping))
			});
			const visibleDataHandle = buildDerivedDataHandle({
				ref: visibleDataRef,
				title: `${widgetDef.title || widgetDef.widgetId} Visible Data`,
				description: `Current visible rows for widget ${widgetDef.widgetId}.`,
				rows: materializedRows,
				selectedCount: materializedSelectedCount,
				kind: "dataView",
				scope: "visible",
				widgetRef
			});
			runtimeData[visibleDataRef] = {
				ref: visibleDataRef,
				rows: materializedRows,
				baseRows: materializedRows,
				handle: visibleDataHandle,
				widgetRef
			};
			dataHandles.push(visibleDataHandle);
			structuralLinks.push(makeWidgetLink({
				ref: makeLinkRef({
					appId,
					workspaceId,
					linkId: `${widgetDef.widgetId}_uses_visible_data`
				}),
				kind: "usesData",
				from: widgetRef,
				to: visibleDataRef,
				sourceRef: widgetRef,
				targetRef: visibleDataRef,
				sourceDataRef: dataHandle.ref,
				targetDataRef: visibleDataRef,
				description: `${widgetDef.widgetId} reads from its current visible data view.`,
				activationPolicy: "manual"
			}));
			structuralLinks.push(makeWidgetLink({
				ref: makeLinkRef({
					appId,
					workspaceId,
					linkId: `${widgetDef.widgetId}_visible_derives_from_source`
				}),
				kind: "derivesFrom",
				from: visibleDataRef,
				to: dataHandle.ref,
				sourceRef: visibleDataRef,
				targetRef: dataHandle.ref,
				sourceDataRef: visibleDataRef,
				targetDataRef: dataHandle.ref,
				description: `${widgetDef.widgetId} visible data is derived from the primary workspace data handle.`,
				activationPolicy: "manual"
			}));
			if (selectionVisibleOnWidget) {
				const selectionDataRef = makeWidgetSelectionDataRef({
					appId,
					workspaceId,
					widgetId: widgetDef.widgetId
				});
				const selectionDataHandle = buildDerivedDataHandle({
					ref: selectionDataRef,
					title: `${widgetDef.title || widgetDef.widgetId} Selection Data`,
					description: `Current rows selected on widget ${widgetDef.widgetId}.`,
					rows: selectedRows,
					selectedCount: selectedRows.length,
					kind: "selectionData",
					scope: "combined",
					widgetRef
				});
				runtimeData[selectionDataRef] = {
					ref: selectionDataRef,
					rows: selectedRows,
					baseRows: materializedRows,
					handle: selectionDataHandle,
					widgetRef,
					kind: "selectionData",
					scope: "combined"
				};
				dataHandles.push(selectionDataHandle);
				structuralLinks.push(makeWidgetLink({
					ref: makeLinkRef({
						appId,
						workspaceId,
						linkId: `${widgetDef.widgetId}_selection_data_derives_from_visible`
					}),
					kind: "derivesFrom",
					from: selectionDataRef,
					to: visibleDataRef,
					sourceRef: selectionDataRef,
					targetRef: visibleDataRef,
					sourceDataRef: selectionDataRef,
					targetDataRef: visibleDataRef,
					description: `${widgetDef.widgetId} combined selection data is derived from the widget visible data view.`,
					activationPolicy: "manual"
				}));
				for (const entry of widgetSelectionEntries) {
					const selectionId = entry?.ref?.split("/").pop() || null;
					if (!selectionId || !entry?.selection) continue;
					const selectionRows = materializedRows.filter((row) => rowMatchesSelection(row, entry.selection));
					const selectionScopedDataRef = makeSelectionScopedDataRef({
						appId,
						workspaceId,
						widgetId: widgetDef.widgetId,
						selectionId
					});
					const selectionScopedDataHandle = buildDerivedDataHandle({
						ref: selectionScopedDataRef,
						title: `${widgetDef.title || widgetDef.widgetId} Selection ${selectionId} Data`,
						description: `Current rows for selection ${selectionId} on widget ${widgetDef.widgetId}.`,
						rows: selectionRows,
						selectedCount: selectionRows.length,
						kind: "selectionData",
						scope: "selection",
						widgetRef,
						sourceSelectionRef: entry.ref
					});
					runtimeData[selectionScopedDataRef] = {
						ref: selectionScopedDataRef,
						rows: selectionRows,
						baseRows: materializedRows,
						handle: selectionScopedDataHandle,
						widgetRef,
						sourceSelectionRef: entry.ref,
						kind: "selectionData",
						scope: "selection"
					};
					dataHandles.push(selectionScopedDataHandle);
					structuralLinks.push(makeWidgetLink({
						ref: makeLinkRef({
							appId,
							workspaceId,
							linkId: `${widgetDef.widgetId}_${selectionId}_contains_selection`
						}),
						kind: "contains",
						from: widgetRef,
						to: entry.ref,
						sourceRef: widgetRef,
						targetRef: entry.ref,
						description: `${widgetDef.widgetId} contains the local selection ${selectionId}.`,
						activationPolicy: "manual"
					}));
					structuralLinks.push(makeWidgetLink({
						ref: makeLinkRef({
							appId,
							workspaceId,
							linkId: `${widgetDef.widgetId}_${selectionId}_selection_data_derives_from_selection`
						}),
						kind: "derivesFrom",
						from: selectionScopedDataRef,
						to: entry.ref,
						sourceRef: selectionScopedDataRef,
						targetRef: entry.ref,
						sourceDataRef: visibleDataRef,
						description: `${widgetDef.widgetId} selection-scoped data is derived from selection ${selectionId}.`,
						activationPolicy: "manual"
					}));
				}
			}
			const widgetKind = declaredWidgetKind || inferWidgetKind(highlightedSpec);
			const linkedRefs = inboundLinks.map((link) => widgetRefsById.get(link.sourceWidgetId)).filter(Boolean);
			const outboundRefs = (plan.links || []).filter((link) => link.sourceWidgetId === widgetDef.widgetId).map((link) => widgetRefsById.get(link.targetWidgetId)).filter(Boolean);
			const affectedRefs = [widgetRef, ...new Set(outboundRefs)];
			const widgetActions = buildActionDescriptors({
				widgetRef,
				selectionRef: widgetSelectionEntries[0]?.ref || null,
				widgetKind,
				widgetSourceKind: widgetDef.source?.kind,
				scope: outboundRefs.length > 0 ? "workspace" : "local",
				affectedRefs
			});
			const widgetPerceptions = buildPerceptionDescriptors({
				appId,
				workspaceId,
				widgetRef,
				dataRef: visibleDataRef,
				widgetKind,
				widgetDef,
				widgetSpec: highlightedSpec
			});
			const familyAdapter = createProviderFamilyAdapter(widgetKind, resolveWidgetProvider(widgetDef));
			const humanInteraction = familyAdapter.getHumanInteractionConfig({
				widgetRef,
				widgetDef,
				widgetSpec: highlightedSpec
			});
			const widgetDescription = makeWidgetDescription({
				ref: widgetRef,
				widgetId: widgetDef.widgetId,
				role: widgetDef.role,
				kind: widgetKind,
				sourceKind: widgetDef.source?.kind || null,
				supportsSpecMutation: widgetDef.source?.kind === "baseSpec",
				title: widgetDef.title || getTitleText(highlightedSpec?.title) || widgetDef.widgetId,
				description: widgetDef.description || "Workspace visualization widget.",
				analyticRoles: Array.isArray(widgetDef.analyticRoles) && widgetDef.analyticRoles.length > 0 ? widgetDef.analyticRoles : inferAnalyticRoles(widgetKind),
				primaryDataRef: dataHandle.ref,
				actionNames: widgetActions.map((d) => d.name),
				perceptionQueryNames: widgetPerceptions.map((d) => d.name),
				usageNotes: widgetDef.usageNotes || [],
				humanInteraction
			});
			widgetDescriptions.push(widgetDescription);
			const widgetState = buildWidgetState({
				widgetRef,
				widgetId: widgetDef.widgetId,
				role: widgetDef.role,
				kind: widgetKind,
				updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
				sourceDataRef: dataHandle.ref,
				currentDataRef: visibleDataRef,
				rowCount: dataHandle.stats?.rowCount || 0,
				visibleCount: materializedRows.length,
				selectedCount: materializedSelectedCount,
				widgetSpec: highlightedSpec,
				inboundLinks,
				activeSelections: widgetRuntimeSelections,
				widgetSelectionRefs: widgetSelectionEntries,
				mirroredSelectionEntries,
				humanInteraction,
				linkedRefs,
				existingFeedback: previousState?.widgets?.[widgetRef]?.feedback
			});
			widgets[widgetRef] = widgetState;
			widgetAdapters.push(familyAdapter.createInstance({
				widgetRef,
				dataRef: visibleDataRef,
				getDescription: () => widgetDescription,
				getState: () => widgetState,
				humanInteraction,
				metadata: {
					widgetId: widgetDef.widgetId,
					role: widgetDef.role,
					primaryDataRef: dataHandle.ref,
					currentDataRef: visibleDataRef
				}
			}));
			actionDescriptors.push(...widgetActions);
			perceptionDescriptors.push(...widgetPerceptions);
		}
		const links = [...(plan.links || []).map((link) => makeWidgetLink({
			ref: buildLinkRef({
				appId,
				workspaceId,
				linkId: link.linkId
			}),
			kind: link.primitive,
			from: widgetRefsById.get(link.sourceWidgetId) || null,
			to: widgetRefsById.get(link.targetWidgetId) || null,
			description: link.description || `${link.sourceWidgetId} -> ${link.targetWidgetId}`,
			effect: link.effect || (link.primitive === "filter" ? "applyFilter" : link.primitive === "highlight" ? "applyHighlight" : link.primitive === "syncDomain" ? "syncDomain" : link.primitive === "sharesSelection" ? "shareSelection" : null),
			activationPolicy: link.activationPolicy,
			effectConstraint: link.effectConstraint,
			fieldMapping: Array.isArray(link.fieldMapping) ? link.fieldMapping : []
		})), ...structuralLinks];
		const defaultFocusedWidgetRef = widgetRefsById.get(plan.primaryWidgetId) || widgetDescriptions[0]?.ref || null;
		const runtimeTopology = deriveWorkspaceTopology({
			widgets: widgetDescriptions,
			links
		});
		const resolvedFocusedWidgetRef = focusedWidgetRef && widgets[focusedWidgetRef] ? focusedWidgetRef : defaultFocusedWidgetRef;
		const targetWidgetRefs = widgetDescriptions.map((widget) => widget.ref);
		const taskContext = buildPlanningTaskContext({
			planningRequest,
			coordinationScope: targetWidgetRefs.length > 1 ? "multi_widget" : "single_widget",
			targetWidgetRefs
		});
		const capabilities = new Set(["singleWidgetAnalysis", "traceReplay"]);
		if (targetWidgetRefs.length > 1) capabilities.add("multiWidgetCoordination");
		if (links.some((link) => link.kind === "filter")) capabilities.add("crossFilter");
		if (links.some((link) => link.kind === "sharesSelection")) capabilities.add("sharedSelection");
		if (links.some((link) => link.kind === "syncDomain")) capabilities.add("domainSync");
		const sharedStateWithFocus = withFocusSubmodel({
			activeSelections,
			viewport: normalizedViewportState,
			globalFilters: deriveGlobalFiltersFromState({
				widgets,
				shared: { activeSelections }
			}),
			focusedWidget: resolvedFocusedWidgetRef,
			annotations: Array.isArray(annotations) ? annotations.map((annotation) => makeWorkspaceAnnotation(annotation)) : []
		}, focusState == null ? void 0 : focusState, widgets);
		const sharedState = withHighlightSubmodel(sharedStateWithFocus, highlightState ?? deriveHighlightState({
			widgets,
			shared: sharedStateWithFocus
		}));
		const stateMeta = stateManager?.createStateMeta({
			workspaceId,
			previousState,
			nextWidgets: widgets,
			nextShared: sharedState,
			nextTaskContext: taskContext,
			nextReplayContext: replayContext,
			branchId: currentBranchId
		}) || {
			stateId: `${workspaceId}:${Date.now()}`,
			createdAt: (/* @__PURE__ */ new Date()).toISOString()
		};
		const currentSelectionDataRef = makeCurrentSelectionDataRef({
			appId,
			workspaceId
		});
		const currentViewDataRef = makeCurrentViewDataRef({
			appId,
			workspaceId
		});
		const focusedWidgetSelectionRef = resolvedFocusedWidgetRef ? Object.keys(widgets[resolvedFocusedWidgetRef]?.selections || {})[0] || null : null;
		const focusedWidgetVisibleDataRef = resolvedFocusedWidgetRef ? widgets[resolvedFocusedWidgetRef]?.data?.currentDataRef || null : null;
		const focusedWidgetVisibleDataEntry = focusedWidgetVisibleDataRef ? runtimeData[focusedWidgetVisibleDataRef] : null;
		const focusedWidgetSelectionDataRef = resolvedFocusedWidgetRef ? runtimeData[makeWidgetSelectionDataRef({
			appId,
			workspaceId,
			widgetId: widgets[resolvedFocusedWidgetRef]?.widgetId
		})] : null;
		if (focusedWidgetSelectionDataRef?.widgetRef && Array.isArray(focusedWidgetSelectionDataRef?.rows)) {
			const currentSelectionHandle = buildDerivedDataHandle({
				ref: currentSelectionDataRef,
				title: "Current Selection Data",
				description: "Current rows for the focused widget selection.",
				rows: focusedWidgetSelectionDataRef.rows,
				selectedCount: focusedWidgetSelectionDataRef.rows.length,
				kind: "selectionData",
				scope: "workspaceCurrent",
				widgetRef: focusedWidgetSelectionDataRef.widgetRef,
				sourceSelectionRef: focusedWidgetSelectionRef
			});
			runtimeData[currentSelectionDataRef] = {
				ref: currentSelectionDataRef,
				rows: focusedWidgetSelectionDataRef.rows,
				baseRows: focusedWidgetSelectionDataRef.baseRows,
				handle: currentSelectionHandle,
				widgetRef: focusedWidgetSelectionDataRef.widgetRef,
				sourceSelectionRef: focusedWidgetSelectionRef,
				kind: "selectionData",
				scope: "workspaceCurrent"
			};
			dataHandles.push(currentSelectionHandle);
		}
		if (focusedWidgetVisibleDataEntry?.widgetRef && Array.isArray(focusedWidgetVisibleDataEntry?.rows)) {
			const currentViewHandle = buildDerivedDataHandle({
				ref: currentViewDataRef,
				title: "Current View Data",
				description: "Current visible rows for the focused widget.",
				rows: focusedWidgetVisibleDataEntry.rows,
				selectedCount: focusedWidgetVisibleDataEntry.handle?.stats?.selectedCount || 0,
				kind: "dataView",
				scope: "workspaceCurrentView",
				widgetRef: focusedWidgetVisibleDataEntry.widgetRef
			});
			runtimeData[currentViewDataRef] = {
				ref: currentViewDataRef,
				rows: focusedWidgetVisibleDataEntry.rows,
				baseRows: focusedWidgetVisibleDataEntry.baseRows,
				handle: currentViewHandle,
				widgetRef: focusedWidgetVisibleDataEntry.widgetRef,
				kind: "dataView",
				scope: "workspaceCurrentView"
			};
			dataHandles.push(currentViewHandle);
		}
		const widgetAdapterSummaries = widgetAdapters.map((adapter) => summarizeWidgetAdapter(adapter)).filter(Boolean);
		return {
			description: makeWorkspaceDescription({
				appId,
				workspaceId,
				generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
				workspaceCapabilities: Array.from(capabilities),
				transportHints: {
					recommendedTools: [
						"workspace_describe",
						"view_read",
						"action_run",
						"perception_query",
						"interaction_trace_read"
					],
					optionalTools: [
						"data_query",
						"workspace_plan",
						"agent_loop_describe",
						"trace_graph_read",
						"read_snapshot",
						"state_history_read",
						"branch_list",
						"workspace_snapshot_read",
						"verified_action_run",
						"jump_to_state",
						"branch_from_state",
						"link_propagation_evaluate",
						"response_recorder_describe",
						"agent_response_read",
						"agent_response_list",
						"agent_response_record"
					],
					note: "External transports should prefer the stable workspace/view/action/perception/trace surface."
				},
				runtimeTopology,
				taskContext,
				planning: {
					supportedTopologies: [...WORKSPACE_PLANNING_TOPOLOGIES],
					supportedRunModes: [...WORKSPACE_PLANNING_RUN_MODES],
					supportedComplexityBudgets: [...WORKSPACE_PLANNING_BUDGETS],
					supportedPlanSources: [...WORKSPACE_PLAN_SOURCES],
					supportedPlanningModes: [...WORKSPACE_PLAN_MODES],
					requestedWorkspaceSpec: workspaceSpecSummary,
					planningRequest: planningRequest || null,
					workspaceSpecStatus,
					workspaceSpecIssues: workspaceSpecValidation.issues,
					materializedFromSpec,
					materializedFromPlanner,
					planner: planningSummary
				},
				widgets: widgetDescriptions,
				widgetAdapters: widgetAdapterSummaries,
				dataHandles,
				links,
				actions: actionDescriptors,
				perceptionQueries: perceptionDescriptors
			}),
			state: makeWorkspaceState({
				stateId: stateMeta.stateId,
				createdAt: stateMeta.createdAt,
				widgets,
				shared: sharedState,
				taskContext
			}),
			runtimeData,
			widgetAdapters,
			replayContext
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaSpecAdapter.js
	function buildSingleWidgetWorkspace(args) {
		return materializeWorkspace(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/actionErrors.js
	var ActionPreconditionError = class extends Error {
		constructor(message, details, recoveryHints) {
			super(message);
			this.name = "ActionPreconditionError";
			this.details = details;
			this.recoveryHints = Array.isArray(recoveryHints) ? recoveryHints : [];
		}
	};
	function throwActionPrecondition(message, details, recoveryHints) {
		throw new ActionPreconditionError(message, details, recoveryHints);
	}
	function isActionPreconditionError(error) {
		return error instanceof ActionPreconditionError;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/schemaValidation.js
	function describeExpectedTypes(type) {
		if (Array.isArray(type)) return type.join(" or ");
		return typeof type === "string" ? type : "valid value";
	}
	function isTypeMatch(value, type) {
		if (type === "string") return typeof value === "string";
		if (type === "number") return typeof value === "number" && Number.isFinite(value);
		if (type === "integer") return Number.isInteger(value);
		if (type === "boolean") return typeof value === "boolean";
		if (type === "array") return Array.isArray(value);
		if (type === "object") return !!value && typeof value === "object" && !Array.isArray(value);
		if (type === "null") return value === null;
		return true;
	}
	function validateCompositeSchema(value, schema, path) {
		if (Array.isArray(schema.anyOf) && schema.anyOf.length > 0) {
			const errors = [];
			for (const candidate of schema.anyOf) try {
				validateAgainstSchema(value, candidate, path);
				return;
			} catch (error) {
				errors.push(error instanceof Error ? error.message : String(error));
			}
			throw new Error(`${path} must satisfy at least one allowed schema. ${errors[0] || ""}`.trim());
		}
		if (Array.isArray(schema.oneOf) && schema.oneOf.length > 0) {
			let passedCount = 0;
			let lastError = null;
			for (const candidate of schema.oneOf) try {
				validateAgainstSchema(value, candidate, path);
				passedCount += 1;
			} catch (error) {
				lastError = error;
			}
			if (passedCount !== 1) throw new Error(passedCount === 0 ? `${path} must satisfy exactly one allowed schema. ${lastError instanceof Error ? lastError.message : ""}`.trim() : `${path} matches multiple incompatible schemas.`);
		}
	}
	function validateType(value, schema, path) {
		if (schema.type == null) return;
		if (!(Array.isArray(schema.type) ? schema.type : [schema.type]).some((type) => isTypeMatch(value, type))) throw new Error(`${path} must be ${describeExpectedTypes(schema.type)}.`);
	}
	function validateScalarConstraints(value, schema, path) {
		if (schema.const !== void 0 && value !== schema.const) throw new Error(`${path} must equal ${JSON.stringify(schema.const)}.`);
		if (Array.isArray(schema.enum) && schema.enum.length > 0 && !schema.enum.includes(value)) throw new Error(`${path} must be one of ${schema.enum.map((item) => JSON.stringify(item)).join(", ")}.`);
		if (typeof value === "number") {
			if (Number.isFinite(schema.minimum) && value < schema.minimum) throw new Error(`${path} must be >= ${schema.minimum}.`);
			if (Number.isFinite(schema.maximum) && value > schema.maximum) throw new Error(`${path} must be <= ${schema.maximum}.`);
		}
		if (typeof value === "string") {
			if (Number.isFinite(schema.minLength) && value.length < schema.minLength) throw new Error(`${path} must contain at least ${schema.minLength} characters.`);
			if (Number.isFinite(schema.maxLength) && value.length > schema.maxLength) throw new Error(`${path} must contain at most ${schema.maxLength} characters.`);
		}
	}
	function validateArray(value, schema, path) {
		if (!Array.isArray(value)) return;
		if (Number.isFinite(schema.minItems) && value.length < schema.minItems) throw new Error(`${path} must contain at least ${schema.minItems} items.`);
		if (Number.isFinite(schema.maxItems) && value.length > schema.maxItems) throw new Error(`${path} must contain at most ${schema.maxItems} items.`);
		if (schema.items && typeof schema.items === "object") value.forEach((item, index) => validateAgainstSchema(item, schema.items, `${path}[${index}]`));
	}
	function validateObject(value, schema, path) {
		if (!value || typeof value !== "object" || Array.isArray(value)) return;
		const required = Array.isArray(schema.required) ? schema.required : [];
		for (const key of required) if (!(key in value) || value[key] == null) throw new Error(`${path}.${key} is required.`);
		const properties = schema.properties && typeof schema.properties === "object" ? schema.properties : {};
		for (const [key, childSchema] of Object.entries(properties)) {
			if (!(key in value) || value[key] == null) continue;
			validateAgainstSchema(value[key], childSchema, `${path}.${key}`);
		}
		if (schema.additionalProperties === false) {
			const allowedKeys = new Set(Object.keys(properties));
			for (const key of Object.keys(value)) if (!allowedKeys.has(key)) throw new Error(`${path}.${key} is not allowed.`);
		}
	}
	function validateAgainstSchema(value, schema, path = "value") {
		if (!schema || typeof schema !== "object") return;
		if (value == null) {
			if (schema.nullable === true) return;
			if (schema.type === "null" || Array.isArray(schema.type) && schema.type.includes("null")) return;
		}
		validateCompositeSchema(value, schema, path);
		validateType(value, schema, path);
		validateScalarConstraints(value, schema, path);
		validateArray(value, schema, path);
		validateObject(value, schema, path);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/StateManager.js
	function clone$17(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function stableSerialize$1(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	var SHARED_STATE_REF = "shared";
	var TASK_CONTEXT_REF = "taskContext";
	var REPLAY_CONTEXT_REF = "replayContext";
	function readCurrentStateId(store) {
		if (typeof store?.stateId === "string" && store.stateId.length > 0) return store.stateId;
		if (typeof store?.state?.stateId === "string" && store.state.stateId.length > 0) return store.state.stateId;
		if (Array.isArray(store?.stateSnapshots) && store.stateSnapshots.length > 0) {
			const latestSnapshot = store.stateSnapshots[store.stateSnapshots.length - 1];
			if (typeof latestSnapshot?.stateId === "string" && latestSnapshot.stateId.length > 0) return latestSnapshot.stateId;
		}
		return null;
	}
	function hasStateManagerSurface(store) {
		return typeof store?.stateManager?.describeManager === "function" || typeof store?.stateManager?.createStateMeta === "function" || typeof store?.buildStatePatch === "function" || typeof store?.commitState === "function" || typeof store?.readState === "function" || typeof readCurrentStateId(store) === "string" || Array.isArray(store?.stateSnapshots);
	}
	function describeStateManagerFromStore(store) {
		if (typeof store?.stateManager?.describeManager === "function") return store.stateManager.describeManager();
		const currentStateId = readCurrentStateId(store);
		const snapshotCount = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots.length : 0;
		const generatedStateCount = Number.isFinite(store?.stateManager?.generatedStateCount) ? store.stateManager.generatedStateCount : snapshotCount > 0 ? snapshotCount : currentStateId ? 1 : 0;
		const lastGeneratedStateId = store?.stateManager?.lastGeneratedStateId ?? currentStateId ?? null;
		return makeStateManagerSummary({
			capabilities: makeStateManagerCapabilities({
				stateIdGeneration: typeof store?.commitState === "function" || snapshotCount > 0 || typeof currentStateId === "string",
				deltaCreation: typeof store?.stateManager?.createDelta === "function" || typeof store?.readState === "function" || snapshotCount > 0,
				statePatchCreation: typeof store?.stateManager?.buildStatePatch === "function" || typeof store?.buildStatePatch === "function" || typeof store?.readState === "function" || snapshotCount > 0,
				refScopedReads: typeof store?.stateManager?.pickRefs === "function" || typeof store?.readState === "function" || snapshotCount > 0 || Boolean(store?.widgets && typeof store.widgets === "object")
			}),
			reservedRefs: [
				SHARED_STATE_REF,
				TASK_CONTEXT_REF,
				REPLAY_CONTEXT_REF
			],
			counters: makeStateManagerCounters({
				generatedStateCount,
				lastGeneratedStateId
			})
		});
	}
	var StateManager = class {
		constructor() {
			this.generatedStateCount = 0;
			this.lastGeneratedStateId = null;
		}
		createStateMeta({ workspaceId = "main", previousState = null, nextWidgets = {}, nextShared = {}, nextTaskContext = void 0, nextReplayContext = void 0, branchId = "main" } = {}) {
			const nextStateSignature = stableSerialize$1({
				widgets: nextWidgets,
				shared: nextShared,
				taskContext: nextTaskContext,
				replayContext: nextReplayContext,
				branchId
			});
			const previousStateSignature = previousState ? stableSerialize$1({
				widgets: previousState.widgets || {},
				shared: previousState.shared || {},
				taskContext: previousState.taskContext,
				replayContext: previousState.replayContext,
				branchId
			}) : null;
			const createdAt = (/* @__PURE__ */ new Date()).toISOString();
			if (previousState?.stateId && previousStateSignature === nextStateSignature) return {
				stateId: previousState.stateId,
				createdAt: previousState.createdAt || createdAt,
				reused: true
			};
			this.generatedStateCount += 1;
			this.lastGeneratedStateId = `${workspaceId}:s${this.generatedStateCount}`;
			return {
				stateId: this.lastGeneratedStateId,
				createdAt,
				reused: false
			};
		}
		createDelta({ previousState, nextState, previousReplayContext = void 0, nextReplayContext = void 0 }) {
			const previousWidgets = previousState?.widgets || {};
			const nextWidgets = nextState?.widgets || {};
			const changedRefs = [];
			const removedRefs = [];
			const allRefs = new Set([...Object.keys(previousWidgets), ...Object.keys(nextWidgets)]);
			for (const ref of allRefs) {
				if (!(ref in nextWidgets)) {
					removedRefs.push(ref);
					continue;
				}
				if (!(ref in previousWidgets)) {
					changedRefs.push(ref);
					continue;
				}
				if (stableSerialize$1(previousWidgets[ref]) !== stableSerialize$1(nextWidgets[ref])) changedRefs.push(ref);
			}
			if (stableSerialize$1(previousState?.shared || null) !== stableSerialize$1(nextState?.shared || null)) changedRefs.push(SHARED_STATE_REF);
			const previousTaskContextExists = previousState?.taskContext !== void 0;
			const nextTaskContextExists = nextState?.taskContext !== void 0;
			if (previousTaskContextExists || nextTaskContextExists) {
				if (!nextTaskContextExists) removedRefs.push(TASK_CONTEXT_REF);
				else if (!previousTaskContextExists || stableSerialize$1(previousState?.taskContext) !== stableSerialize$1(nextState?.taskContext)) changedRefs.push(TASK_CONTEXT_REF);
			}
			const previousReplayContextExists = previousReplayContext !== void 0;
			const nextReplayContextExists = nextReplayContext !== void 0;
			if (previousReplayContextExists || nextReplayContextExists) {
				if (!nextReplayContextExists) removedRefs.push(REPLAY_CONTEXT_REF);
				else if (!previousReplayContextExists || stableSerialize$1(previousReplayContext) !== stableSerialize$1(nextReplayContext)) changedRefs.push(REPLAY_CONTEXT_REF);
			}
			return {
				baseStateId: previousState?.stateId || null,
				changedRefs,
				removedRefs
			};
		}
		buildStatePatch({ state, refs, replayContext = void 0 }) {
			const widgets = state?.widgets || {};
			const patch = {};
			for (const ref of refs || []) {
				if (ref === "shared") {
					patch[ref] = clone$17(state?.shared);
					continue;
				}
				if (ref === "taskContext") {
					patch[ref] = clone$17(state?.taskContext);
					continue;
				}
				if (ref === "replayContext") {
					patch[ref] = clone$17(replayContext);
					continue;
				}
				if (widgets[ref]) patch[ref] = clone$17(widgets[ref]);
			}
			return patch;
		}
		pickRefs({ state, refs, replayContext = void 0 }) {
			if (!Array.isArray(refs) || refs.length === 0) return clone$17(state);
			const widgets = {};
			const includeShared = refs.includes(SHARED_STATE_REF);
			const includeTaskContext = refs.includes(TASK_CONTEXT_REF);
			const includeReplayContext = refs.includes(REPLAY_CONTEXT_REF);
			for (const ref of refs) if (state?.widgets?.[ref]) widgets[ref] = clone$17(state.widgets[ref]);
			const nextState = {
				...clone$17(state),
				widgets
			};
			if (!includeShared) delete nextState.shared;
			if (!includeTaskContext) delete nextState.taskContext;
			if (includeReplayContext) nextState.replayContext = clone$17(replayContext);
			else delete nextState.replayContext;
			return nextState;
		}
		describeManager() {
			return makeStateManagerSummary({
				capabilities: makeStateManagerCapabilities({
					stateIdGeneration: true,
					deltaCreation: true,
					statePatchCreation: true,
					refScopedReads: true
				}),
				reservedRefs: [
					SHARED_STATE_REF,
					TASK_CONTEXT_REF,
					REPLAY_CONTEXT_REF
				],
				counters: makeStateManagerCounters({
					generatedStateCount: this.generatedStateCount,
					lastGeneratedStateId: this.lastGeneratedStateId
				})
			});
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/workspaceStoreReaders.js
	function clone$16(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeActorFilter$1(actors) {
		return Array.isArray(actors) ? [...new Set(actors.filter((actor) => typeof actor === "string" && actor.length > 0))] : [];
	}
	function getTraceRecordPriority$1(record) {
		const eventKind = record?.eventKind || null;
		if (eventKind === "action") return 3;
		if (eventKind === "perceptionQuery" || eventKind === "dataQuery") return 2;
		if (eventKind === "systemTransition") return 1;
		return 0;
	}
	function pickRepresentativeTraceRecord$1(currentRecord, nextRecord) {
		if (!currentRecord) return nextRecord || null;
		if (!nextRecord) return currentRecord;
		return getTraceRecordPriority$1(nextRecord) >= getTraceRecordPriority$1(currentRecord) ? nextRecord : currentRecord;
	}
	function buildResponsePreview$1(content, maxLength = 160) {
		const text = String(content || "").trim();
		if (!text) return null;
		if (!Number.isFinite(maxLength) || maxLength <= 0 || text.length <= maxLength) return text;
		return `${text.slice(0, maxLength - 3)}...`;
	}
	function stableSerialize(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	function createDeltaFallback({ previousState, nextState, previousReplayContext = void 0, nextReplayContext = void 0 }) {
		const previousWidgets = previousState?.widgets || {};
		const nextWidgets = nextState?.widgets || {};
		const changedRefs = [];
		const removedRefs = [];
		const allRefs = new Set([...Object.keys(previousWidgets), ...Object.keys(nextWidgets)]);
		for (const ref of allRefs) {
			if (!(ref in nextWidgets)) {
				removedRefs.push(ref);
				continue;
			}
			if (!(ref in previousWidgets)) {
				changedRefs.push(ref);
				continue;
			}
			if (stableSerialize(previousWidgets[ref]) !== stableSerialize(nextWidgets[ref])) changedRefs.push(ref);
		}
		if (stableSerialize(previousState?.shared || null) !== stableSerialize(nextState?.shared || null)) changedRefs.push(SHARED_STATE_REF);
		const previousTaskContextExists = previousState?.taskContext !== void 0;
		const nextTaskContextExists = nextState?.taskContext !== void 0;
		if (previousTaskContextExists || nextTaskContextExists) {
			if (!nextTaskContextExists) removedRefs.push(TASK_CONTEXT_REF);
			else if (!previousTaskContextExists || stableSerialize(previousState?.taskContext) !== stableSerialize(nextState?.taskContext)) changedRefs.push(TASK_CONTEXT_REF);
		}
		const previousReplayContextExists = previousReplayContext !== void 0;
		const nextReplayContextExists = nextReplayContext !== void 0;
		if (previousReplayContextExists || nextReplayContextExists) {
			if (!nextReplayContextExists) removedRefs.push(REPLAY_CONTEXT_REF);
			else if (!previousReplayContextExists || stableSerialize(previousReplayContext) !== stableSerialize(nextReplayContext)) changedRefs.push(REPLAY_CONTEXT_REF);
		}
		return {
			baseStateId: previousState?.stateId || null,
			changedRefs,
			removedRefs
		};
	}
	function pickRefsFromStateFallback({ state, refs, replayContext = void 0 }) {
		if (!Array.isArray(refs) || refs.length === 0) return clone$16(state);
		const widgets = {};
		const includeShared = refs.includes(SHARED_STATE_REF);
		const includeTaskContext = refs.includes(TASK_CONTEXT_REF);
		const includeReplayContext = refs.includes(REPLAY_CONTEXT_REF);
		for (const ref of refs) if (state?.widgets?.[ref]) widgets[ref] = clone$16(state.widgets[ref]);
		const nextState = {
			...clone$16(state),
			widgets
		};
		if (!includeShared) delete nextState.shared;
		if (!includeTaskContext) delete nextState.taskContext;
		if (includeReplayContext) nextState.replayContext = clone$16(replayContext);
		else delete nextState.replayContext;
		return nextState;
	}
	function deriveWorkspaceCapabilities({ widgets, links, store }) {
		const capabilities = /* @__PURE__ */ new Set();
		if (Array.isArray(widgets) && widgets.length > 0) capabilities.add("singleWidgetAnalysis");
		if (Array.isArray(widgets) && widgets.length > 1) capabilities.add("multiWidgetCoordination");
		if (Array.isArray(links)) {
			if (links.some((link) => link?.kind === "filter" || link?.kind === "filters")) capabilities.add("crossFilter");
			if (links.some((link) => link?.kind === "sharesSelection")) capabilities.add("sharedSelection");
			if (links.some((link) => link?.kind === "syncDomain" || link?.kind === "syncsDomain")) capabilities.add("domainSync");
		}
		if (Array.isArray(store?.interactionTrace) && store.interactionTrace.length > 0) capabilities.add("traceReplay");
		if (Array.isArray(store?.stateSnapshots) && store.stateSnapshots.length > 0) capabilities.add("traceReplay");
		return Array.from(capabilities);
	}
	function valuesFromRecord(record) {
		return record && typeof record === "object" && !Array.isArray(record) ? Object.values(record).filter(Boolean) : [];
	}
	function listResponsesFromStore(store, limit = 20, options = {}) {
		if (typeof store?.listResponses === "function") return store.listResponses(limit, options);
		const workspaceId = options.workspaceId || store?.workspaceId || null;
		const fullResponses = Array.isArray(store?.responseHistory) ? store.responseHistory : [];
		return (workspaceId ? fullResponses.filter((item) => item?.workspaceId === workspaceId) : fullResponses).slice(-(Number.isFinite(limit) ? limit : 20)).map((item) => clone$16(item));
	}
	function readLatestResponseFromStore(store, options = {}) {
		if (typeof store?.readLatestResponse === "function") return store.readLatestResponse(options);
		return listResponsesFromStore(store, 1, options)[0] || null;
	}
	function listStateSnapshotsFromStore(store, options = 50) {
		if (typeof store?.listStateSnapshots === "function") return store.listStateSnapshots(options);
		const normalizedOptions = typeof options === "number" ? { limit: options } : options || {};
		const limit = Number.isFinite(normalizedOptions.limit) ? normalizedOptions.limit : 50;
		const sinceStateId = normalizedOptions.sinceStateId || null;
		const allowedActors = normalizeActorFilter$1(normalizedOptions.actors);
		const fullSnapshots = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots : [];
		const traceByStateId = new Map((Array.isArray(store?.interactionTrace) ? store.interactionTrace : []).filter((record) => typeof record?.stateId === "string" && record.stateId.length > 0).map((record) => [record.stateId, record]));
		const scopedSnapshots = sinceStateId ? (() => {
			const idx = fullSnapshots.findIndex((snapshot) => snapshot?.stateId === sinceStateId);
			return idx >= 0 ? fullSnapshots.slice(idx + 1) : fullSnapshots;
		})() : fullSnapshots;
		return (allowedActors.length > 0 ? scopedSnapshots.filter((snapshot) => {
			const actor = traceByStateId.get(snapshot?.stateId)?.actor || "system";
			return allowedActors.includes(actor);
		}) : scopedSnapshots).slice(-limit).map((item) => {
			const representativeTrace = traceByStateId.get(item?.stateId);
			return makeStateSnapshotMeta({
				stateId: item?.stateId || "",
				createdAt: item?.createdAt || item?.state?.createdAt || null,
				baseStateId: (item?.parentStateId ?? item?.state?.delta?.baseStateId) || null,
				branchId: item?.branchId || null,
				transitionType: item?.transitionType || "continue",
				branchLabel: item?.branchLabel || null,
				actor: representativeTrace?.actor || "system",
				changedRefs: Array.isArray(item?.state?.delta?.changedRefs) ? [...item.state.delta.changedRefs] : [],
				removedRefs: Array.isArray(item?.state?.delta?.removedRefs) ? [...item.state.delta.removedRefs] : []
			});
		});
	}
	function listBranchesFromStore(store) {
		if (typeof store?.listBranches === "function") return store.listBranches();
		return valuesFromRecord(store?.branchRegistry).map((entry) => makeBranchSummary(clone$16(entry)));
	}
	function readInteractionTraceFromStore(store, options = {}) {
		if (typeof store?.readTraceWindow === "function") return store.readTraceWindow({
			limit: options.limit || 50,
			sinceStateId: options.sinceStateId || null,
			actors: options.actors || []
		});
		if (typeof store?.readTrace === "function") {
			const fullTrace = store.readTrace(options.limit || 50);
			const sinceStateId = options.sinceStateId || null;
			const allowedActors = normalizeActorFilter$1(options.actors);
			const scopedTrace = sinceStateId ? (() => {
				const idx = fullTrace.findIndex((record) => record?.stateId === sinceStateId);
				return idx >= 0 ? fullTrace.slice(idx + 1) : fullTrace;
			})() : fullTrace;
			return allowedActors.length > 0 ? scopedTrace.filter((record) => allowedActors.includes(record?.actor || "system")) : scopedTrace;
		}
		const fullTrace = Array.isArray(store?.interactionTrace) ? [...store.interactionTrace] : [];
		const sinceStateId = options.sinceStateId || null;
		const allowedActors = normalizeActorFilter$1(options.actors);
		const scopedTrace = sinceStateId ? (() => {
			const idx = fullTrace.findIndex((record) => record?.stateId === sinceStateId);
			return idx >= 0 ? fullTrace.slice(idx + 1) : fullTrace;
		})() : fullTrace;
		const actorScopedTrace = allowedActors.length > 0 ? scopedTrace.filter((record) => allowedActors.includes(record?.actor || "system")) : scopedTrace;
		const limit = options.limit || 50;
		if (!Number.isFinite(limit) || limit <= 0) return actorScopedTrace;
		return actorScopedTrace.slice(-limit);
	}
	function readSnapshotEntryFromStore(store, stateId) {
		if (!stateId || typeof stateId !== "string") return null;
		if (typeof store?.readSnapshotEntry === "function") return store.readSnapshotEntry(stateId);
		const snapshot = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots.find((item) => item?.stateId === stateId) || null : null;
		return snapshot ? clone$16(snapshot) : null;
	}
	function readCurrentSnapshotMetaFromStore(store) {
		if (typeof store?.readCurrentSnapshotMeta === "function") return store.readCurrentSnapshotMeta();
		const fullSnapshots = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots : [];
		if (fullSnapshots.length === 0) return null;
		const currentStateId = store?.state?.stateId || store?.stateId || null;
		return clone$16((currentStateId ? fullSnapshots.find((entry) => entry?.stateId === currentStateId) || null : null) || fullSnapshots[fullSnapshots.length - 1] || null);
	}
	function readSnapshotFromStore(store, stateId, options = {}) {
		const entry = readSnapshotEntryFromStore(store, stateId);
		const snapshot = entry?.state || null;
		if (!snapshot) return null;
		const scopedSnapshot = Array.isArray(options.refs) && options.refs.length > 0 ? typeof store?.stateManager?.pickRefs === "function" ? store.stateManager.pickRefs({
			state: snapshot,
			refs: options.refs,
			replayContext: entry?.replayContext || null
		}) : (() => {
			const refs = options.refs.filter(Boolean);
			const widgets = {};
			for (const ref of refs) if (snapshot?.widgets?.[ref]) widgets[ref] = clone$16(snapshot.widgets[ref]);
			const nextSnapshot = {
				...clone$16(snapshot),
				widgets
			};
			if (!refs.includes("shared")) delete nextSnapshot.shared;
			if (!refs.includes("taskContext")) delete nextSnapshot.taskContext;
			if (refs.includes("replayContext")) nextSnapshot.replayContext = clone$16(entry?.replayContext || null);
			else delete nextSnapshot.replayContext;
			return nextSnapshot;
		})() : {
			...clone$16(snapshot),
			replayContext: clone$16(entry?.replayContext || null)
		};
		return options.includeMeta ? {
			...scopedSnapshot,
			__meta: {
				stateId: entry?.stateId || null,
				parentStateId: entry?.parentStateId || null,
				branchId: entry?.branchId || null,
				transitionType: entry?.transitionType || "continue",
				branchLabel: entry?.branchLabel || null
			}
		} : scopedSnapshot;
	}
	function buildTraceGraphFromStore(store, options = 50) {
		if (typeof store?.buildTraceGraph === "function") return store.buildTraceGraph(options);
		const normalizedOptions = typeof options === "number" ? { limit: options } : options || {};
		const limit = Number.isFinite(normalizedOptions.limit) ? normalizedOptions.limit : 50;
		const sinceStateId = normalizedOptions.sinceStateId || null;
		const allowedActors = normalizeActorFilter$1(normalizedOptions.actors);
		const fullSnapshots = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots : [];
		const scopedSnapshots = sinceStateId ? (() => {
			const idx = fullSnapshots.findIndex((snapshot) => snapshot?.stateId === sinceStateId);
			return idx >= 0 ? fullSnapshots.slice(idx + 1) : fullSnapshots;
		})() : fullSnapshots;
		const traceByStateId = /* @__PURE__ */ new Map();
		for (const record of Array.isArray(store?.interactionTrace) ? store.interactionTrace : []) {
			if (!record?.stateId) continue;
			traceByStateId.set(record.stateId, pickRepresentativeTraceRecord$1(traceByStateId.get(record.stateId), record));
		}
		const snapshots = (allowedActors.length > 0 ? scopedSnapshots.filter((snapshot) => {
			const actor = traceByStateId.get(snapshot?.stateId)?.actor || "system";
			return allowedActors.includes(actor);
		}) : scopedSnapshots).slice(-limit);
		const latestResponseByStateId = /* @__PURE__ */ new Map();
		for (const response of Array.isArray(store?.responseHistory) ? store.responseHistory : []) {
			if (!response?.stateId) continue;
			latestResponseByStateId.set(response.stateId, response);
		}
		const nodes = snapshots.map((snapshot) => {
			const record = traceByStateId.get(snapshot?.stateId);
			const response = latestResponseByStateId.get(snapshot?.stateId);
			const queryName = getInteractionTraceQueryName(record?.query);
			return makeTraceGraphNode({
				id: snapshot?.stateId || "",
				stateId: snapshot?.stateId || "",
				parentStateId: snapshot?.parentStateId || null,
				branchId: snapshot?.branchId || "main",
				branchLabel: snapshot?.branchLabel || null,
				timestamp: snapshot?.createdAt || snapshot?.state?.createdAt || null,
				actor: record?.actor || "system",
				eventFamily: record?.eventFamily || getInteractionTraceEventFamily(record),
				querySurface: record?.querySurface || getInteractionTraceQuerySurface(record),
				actionName: record?.action?.name || null,
				queryName,
				responseId: response?.responseId || null,
				responseActor: response?.actor || null,
				responsePreview: buildResponsePreview$1(response?.content),
				label: record?.action?.name || queryName || snapshot?.transitionType || "state",
				transitionType: snapshot?.transitionType || "continue",
				current: snapshot?.stateId === (store?.state?.stateId || store?.stateId || null)
			});
		});
		const nodeIds = new Set(nodes.map((node) => node.id));
		const edges = snapshots.filter((snapshot) => snapshot?.parentStateId).filter((snapshot) => nodeIds.has(snapshot.stateId) && nodeIds.has(snapshot.parentStateId)).map((snapshot) => makeTraceGraphEdge({
			id: `${snapshot.parentStateId}->${snapshot.stateId}`,
			from_id: snapshot.parentStateId,
			to_id: snapshot.stateId,
			edge_type: snapshot.transitionType || "continue",
			branchId: snapshot.branchId || "main",
			label: snapshot.transitionType || "continue",
			timestamp: snapshot.createdAt || snapshot.state?.createdAt || null
		}));
		return makeTraceGraph({
			current_state_id: store?.state?.stateId || store?.stateId || null,
			current_branch_id: store?.currentBranchId || null,
			sinceStateId,
			actors: allowedActors,
			branches: listBranchesFromStore(store),
			nodes,
			edges
		});
	}
	function listStoreWidgets(store) {
		if (typeof store?.listWidgetDescriptions === "function") return store.listWidgetDescriptions();
		return valuesFromRecord(store?.descriptions);
	}
	function listStoreDataHandles(store) {
		if (typeof store?.listDataHandles === "function") return store.listDataHandles();
		return valuesFromRecord(store?.dataHandles);
	}
	function readDataHandleFromStore(store, ref) {
		if (!ref || typeof ref !== "string") return null;
		if (typeof store?.getDataHandle === "function") return store.getDataHandle(ref);
		return store?.dataHandles?.[ref] || null;
	}
	function listStoreLinks(store) {
		if (typeof store?.listLinks === "function") return (store.listLinks() || []).map((link) => stripWidgetLinkCompatibilityFields(normalizeWidgetLink(link)));
		return valuesFromRecord(store?.links).map((link) => stripWidgetLinkCompatibilityFields(normalizeWidgetLink(link)));
	}
	function listStoreActions(store, actionExecutor = null) {
		if (typeof store?.listActions === "function") {
			const listedActions = store.listActions();
			if (Array.isArray(listedActions) && listedActions.length > 0) return listedActions;
		}
		if (typeof actionExecutor?.list === "function") {
			const listedActions = actionExecutor.list();
			if (Array.isArray(listedActions) && listedActions.length > 0) return listedActions;
		}
		return valuesFromRecord(store?.actions);
	}
	function listStorePerceptionQueries(store, perceptionQueryRegistry = null) {
		if (typeof store?.listPerceptionQueries === "function") {
			const listedQueries = store.listPerceptionQueries();
			if (Array.isArray(listedQueries) && listedQueries.length > 0) return listedQueries;
		}
		if (typeof perceptionQueryRegistry?.list === "function") {
			const listedQueries = perceptionQueryRegistry.list();
			if (Array.isArray(listedQueries) && listedQueries.length > 0) return listedQueries;
		}
		return valuesFromRecord(store?.perceptionQueries);
	}
	function readRuntimeDataFromStore(store, dataRef) {
		if (!dataRef || typeof dataRef !== "string") return null;
		if (typeof store?.readRuntimeData === "function") return store.readRuntimeData(dataRef);
		return store?.runtimeData?.[dataRef] || null;
	}
	function resolveWidgetRecordFromStore(store, ref) {
		if (!ref || typeof ref !== "string") return null;
		if (typeof store?.getResolvedWidgetForTarget === "function") {
			const resolvedTarget = store.getResolvedWidgetForTarget(ref);
			if (resolvedTarget) return resolvedTarget;
		}
		if (typeof store?.getResolvedWidget === "function") {
			const resolvedWidget = store.getResolvedWidget(ref);
			if (resolvedWidget) return resolvedWidget;
		}
		if (typeof store?.getWidgetDescription === "function") {
			const widgetDescription = store.getWidgetDescription(ref);
			if (widgetDescription) return widgetDescription;
		}
		const widgetDescription = store?.descriptions?.[ref] || null;
		const widgetState = store?.widgets?.[ref] || null;
		if (widgetDescription || widgetState) return {
			...widgetDescription || {},
			...widgetState || {},
			ref,
			data: {
				...widgetDescription?.data || {},
				...widgetState?.data || {}
			}
		};
		const widgetByDataRef = listStoreWidgets(store).find((widget) => {
			const currentDataRef = widget?.data?.currentDataRef || null;
			const sourceDataRef = widget?.data?.sourceDataRef || null;
			const primaryDataRef = widget?.primaryDataRef || null;
			return currentDataRef === ref || sourceDataRef === ref || primaryDataRef === ref;
		}) || null;
		if (!widgetByDataRef?.ref) return null;
		const widgetStateByDataRef = store?.widgets?.[widgetByDataRef.ref] || null;
		return {
			...widgetByDataRef,
			...widgetStateByDataRef || {},
			ref: widgetByDataRef.ref,
			data: {
				...widgetByDataRef?.data || {},
				...widgetStateByDataRef?.data || {}
			}
		};
	}
	function resolveSelectionDataRefFromStore(store, selectionRef) {
		if (!selectionRef || typeof selectionRef !== "string") return null;
		if (typeof store?.resolveSelectionDataRef === "function") {
			const resolvedDataRef = store.resolveSelectionDataRef(selectionRef);
			if (resolvedDataRef) return resolvedDataRef;
		}
		const parts = parseRef(selectionRef);
		if (!parts?.widgetId || !parts?.selectionId) return null;
		return makeSelectionScopedDataRef({
			appId: parts.appId || store?.appId || "widgetva-app",
			workspaceId: parts.workspaceId || store?.workspaceId || "main",
			widgetId: parts.widgetId,
			selectionId: parts.selectionId
		});
	}
	function readWorkspaceDescriptionFromStore(store, { actionExecutor = null, perceptionQueryRegistry = null } = {}) {
		if (typeof store?.readDescription === "function") return store.readDescription();
		const widgets = listStoreWidgets(store);
		const links = listStoreLinks(store);
		const workspaceCapabilities = deriveWorkspaceCapabilities({
			widgets,
			links,
			store
		});
		return makeWorkspaceDescription({
			appId: store?.appId || null,
			workspaceId: store?.workspaceId || null,
			generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			workspaceCapabilities,
			transportHints: makeWorkspaceTransportHints({
				recommendedTools: [...WORKSPACE_TRANSPORT_TOOL_NAMES],
				optionalTools: [...WORKSPACE_OPTIONAL_TRANSPORT_TOOL_NAMES],
				note: "External transports should prefer the stable workspace/view/action/perception/trace surface."
			}),
			runtimeTopology: deriveWorkspaceTopology({
				widgets,
				links
			}),
			widgets,
			dataHandles: listStoreDataHandles(store),
			links,
			actions: listStoreActions(store, actionExecutor),
			perceptionQueries: listStorePerceptionQueries(store, perceptionQueryRegistry)
		});
	}
	function readWorkspaceStateFromStore(store, options = {}) {
		if (typeof store?.readState === "function") return store.readState(options);
		const normalizedRefs = Array.isArray(options?.refs) ? options.refs.filter(Boolean) : null;
		const allWidgets = store?.widgets && typeof store.widgets === "object" && !Array.isArray(store.widgets) ? store.widgets : {};
		const widgetEntries = normalizedRefs ? normalizedRefs.map((ref) => [ref, allWidgets?.[ref] || null]).filter(([, widgetState]) => Boolean(widgetState)) : Object.entries(allWidgets).filter(([, widgetState]) => Boolean(widgetState));
		const fallbackState = {
			stateId: store?.stateId || null,
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			widgets: Object.fromEntries(widgetEntries.map(([ref, widgetState]) => [ref, clone$16(widgetState)])),
			shared: {
				selections: {
					registry: {},
					views: {
						primary: null,
						byWidget: {}
					}
				},
				focus: null,
				highlight: null,
				viewport: null,
				globalFilters: {},
				focusedWidget: void 0,
				annotations: [],
				links: {
					definitions: [],
					topology: {}
				},
				...clone$16(store?.shared) || {}
			}
		};
		if (options?.deltaSince) {
			const baseEntry = readSnapshotEntryFromStore(store, options.deltaSince);
			if (baseEntry?.state) {
				const currentSnapshotEntry = readCurrentSnapshotMetaFromStore(store);
				const currentState = clone$16(currentSnapshotEntry?.state || {
					...fallbackState,
					...store?.taskContext !== void 0 ? { taskContext: clone$16(store.taskContext) } : {}
				});
				const delta = typeof store?.stateManager?.createDelta === "function" ? store.stateManager.createDelta({
					previousState: baseEntry.state,
					nextState: currentState,
					previousReplayContext: baseEntry.replayContext,
					nextReplayContext: currentSnapshotEntry?.replayContext ?? store?.replayContext
				}) : createDeltaFallback({
					previousState: baseEntry.state,
					nextState: currentState,
					previousReplayContext: baseEntry.replayContext,
					nextReplayContext: currentSnapshotEntry?.replayContext ?? store?.replayContext
				});
				const scopedRefs = normalizedRefs && normalizedRefs.length > 0 ? (delta.changedRefs || []).filter((ref) => normalizedRefs.includes(ref)) : null;
				const nextState = {
					...currentState,
					delta
				};
				return typeof store?.stateManager?.pickRefs === "function" ? store.stateManager.pickRefs({
					state: nextState,
					replayContext: currentSnapshotEntry?.replayContext ?? store?.replayContext,
					refs: scopedRefs || delta.changedRefs || []
				}) : pickRefsFromStateFallback({
					state: nextState,
					replayContext: currentSnapshotEntry?.replayContext ?? store?.replayContext,
					refs: scopedRefs || delta.changedRefs || []
				});
			}
		}
		return fallbackState;
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/workspaceSharedStateMutators.js
	function clone$15(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function ensureSelectionTimeline(store) {
		if (!store.__selectionTimeline || typeof store.__selectionTimeline !== "object") store.__selectionTimeline = {
			past: [],
			future: []
		};
		return store.__selectionTimeline;
	}
	function selectionSnapshot(store) {
		const state = readWorkspaceStateFromStore(store);
		return {
			widgets: clone$15(state?.widgets || {}),
			shared: clone$15(state?.shared || {})
		};
	}
	function nextStateId$1(currentStateId = "main:s0") {
		const prefix = typeof currentStateId === "string" && currentStateId.includes(":") ? currentStateId.split(":")[0] : "main";
		if (typeof globalThis.crypto?.randomUUID === "function") return `${prefix}:${globalThis.crypto.randomUUID()}`;
		return `${prefix}:${Date.now()}`;
	}
	function commitStateUpdate(store, { widgets, shared, transitionType = "continue" } = {}) {
		const currentState = readWorkspaceStateFromStore(store);
		const nextState = {
			...clone$15(currentState || {}),
			stateId: nextStateId$1(currentState?.stateId),
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			widgets: clone$15(widgets || currentState?.widgets || {}),
			shared: clone$15(shared || currentState?.shared || {})
		};
		return store.commitState(nextState, { transitionType });
	}
	function updateSharedStateInStore(store, updater) {
		if (!store || typeof updater !== "function") return readWorkspaceStateFromStore(store);
		const currentState = readWorkspaceStateFromStore(store);
		const nextShared = updater(clone$15(currentState?.shared || {}));
		if (!nextShared || typeof nextShared !== "object") return currentState;
		if (typeof store?.commitState === "function") return commitStateUpdate(store, {
			widgets: currentState?.widgets || {},
			shared: nextShared
		});
		if (store.state?.shared && typeof store.state === "object") store.state.shared = nextShared;
		store.shared = nextShared;
		store.version = (store.version || 0) + 1;
		store.stateId = nextStateId$1(store.stateId);
		return readWorkspaceStateFromStore(store);
	}
	function resetWorkspaceInteractionsInStore(store) {
		const currentState = readWorkspaceStateFromStore(store);
		const nextWidgets = {};
		for (const [ref, widgetState] of Object.entries(currentState?.widgets || {})) nextWidgets[ref] = {
			...clone$15(widgetState),
			version: (widgetState?.version || 0) + 1,
			updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			selections: {},
			data: {
				...widgetState?.data || {},
				selectedCount: 0
			}
		};
		store.widgets = nextWidgets;
		if (store.state?.widgets && typeof store.state === "object") store.state.widgets = nextWidgets;
		const nextShared = withHighlightSubmodel(withFocusSubmodel({ ...withSelectionSubmodel(currentState?.shared || {}, {
			registry: {},
			primary: null,
			byWidget: {}
		}) }, null), null);
		if (typeof store?.commitState === "function") return commitStateUpdate(store, {
			widgets: nextWidgets,
			shared: nextShared,
			transitionType: "reset"
		});
		if (store.state?.shared && typeof store.state === "object") store.state.shared = nextShared;
		store.shared = nextShared;
		store.version = (store.version || 0) + 1;
		store.stateId = nextStateId$1(store.stateId);
		return readWorkspaceStateFromStore(store);
	}
	function restoreSnapshotStateInStore(store, snapshot) {
		const snapshotState = snapshot?.state || snapshot || {};
		const nextWidgets = clone$15(snapshotState?.widgets || {});
		const nextShared = withHighlightSubmodel(withFocusSubmodel(withSelectionSubmodel({
			globalFilters: {},
			focusedWidget: null,
			...clone$15(snapshotState?.shared) || {}
		}, {
			registry: void 0,
			primary: void 0,
			byWidget: void 0
		}), void 0, nextWidgets), void 0);
		if (typeof store?.commitState === "function") return commitStateUpdate(store, {
			widgets: nextWidgets,
			shared: nextShared,
			transitionType: "continue"
		});
		if (store.state?.widgets && typeof store.state === "object") store.state.widgets = nextWidgets;
		if (store.state?.shared && typeof store.state === "object") store.state.shared = nextShared;
		store.widgets = nextWidgets;
		store.shared = nextShared;
		store.version = (store.version || 0) + 1;
		store.stateId = nextStateId$1(store.stateId);
		return readWorkspaceStateFromStore(store);
	}
	function recordSelectionHistorySnapshot(store) {
		if (!store) return;
		const timeline = ensureSelectionTimeline(store);
		timeline.past = [...timeline.past || [], selectionSnapshot(store)].slice(-50);
		timeline.future = [];
	}
	function hasUndoSelectionHistoryInStore(store) {
		return Array.isArray(store?.__selectionTimeline?.past) && store.__selectionTimeline.past.length > 0;
	}
	function hasRedoSelectionHistoryInStore(store) {
		return Array.isArray(store?.__selectionTimeline?.future) && store.__selectionTimeline.future.length > 0;
	}
	function undoSelectionInStore(store) {
		const timeline = ensureSelectionTimeline(store);
		if (!timeline.past.length) return readWorkspaceStateFromStore(store);
		const previous = timeline.past.pop();
		timeline.future = [...timeline.future || [], selectionSnapshot(store)].slice(-50);
		return restoreSnapshotStateInStore(store, previous);
	}
	function redoSelectionInStore(store) {
		const timeline = ensureSelectionTimeline(store);
		if (!timeline.future.length) return readWorkspaceStateFromStore(store);
		const next = timeline.future.pop();
		timeline.past = [...timeline.past || [], selectionSnapshot(store)].slice(-50);
		return restoreSnapshotStateInStore(store, next);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/workspaceStoreMutators.js
	function clone$14(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function nextStateId(currentStateId = "main:s0") {
		const prefix = typeof currentStateId === "string" && currentStateId.includes(":") ? currentStateId.split(":")[0] : "main";
		if (typeof globalThis.crypto?.randomUUID === "function") return `${prefix}:${globalThis.crypto.randomUUID()}`;
		return `${prefix}:${Date.now()}`;
	}
	function patchWidgetInStore(store, ref, patch = {}) {
		if (typeof store?.patchWidget === "function") return store.patchWidget(ref, patch);
		const currentWidget = store?.widgets?.[ref];
		if (!currentWidget) throw new Error(`Unknown widget: ${ref}`);
		const nextWidget = mergeRuntimePatch$1(clone$14(currentWidget), patch);
		nextWidget.version = (currentWidget.version || 0) + 1;
		nextWidget.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
		store.widgets = {
			...store.widgets || {},
			[ref]: nextWidget
		};
		store.version = (store.version || 0) + 1;
		store.stateId = nextStateId(store.stateId);
		return readWorkspaceStateFromStore(store);
	}
	function updateRuntimeDataInStore(store, ref, updater) {
		if (typeof store?.updateRuntimeData === "function") return store.updateRuntimeData(ref, updater);
		if (!ref || typeof updater !== "function") return null;
		const currentEntry = store?.runtimeData?.[ref];
		if (!currentEntry) return null;
		const nextEntry = updater(clone$14(currentEntry));
		if (!nextEntry || typeof nextEntry !== "object") return currentEntry;
		store.runtimeData = {
			...store.runtimeData || {},
			[ref]: nextEntry
		};
		if (nextEntry.handle) store.dataHandles = {
			...store.dataHandles || {},
			[ref]: nextEntry.handle
		};
		return nextEntry;
	}
	function beginBranchFromStateInStore(store, { stateId, label } = {}) {
		if (typeof store?.beginBranchFromState === "function") return store.beginBranchFromState({
			stateId,
			label
		});
		if (!stateId) throw new Error("beginBranchFromState requires a stateId.");
		const snapshotEntry = (typeof store?.readSnapshotEntry === "function" ? store.readSnapshotEntry(stateId) : null) || (Array.isArray(store?.stateSnapshots) ? store.stateSnapshots.find((entry) => entry?.stateId === stateId) || null : null);
		if (!snapshotEntry) throw new Error(`Unknown branch origin state: ${stateId}`);
		const branchId = `branch_${Date.now()}`;
		const branchLabel = label || `Branch from ${stateId}`;
		const parentBranchId = snapshotEntry?.branchId || store?.currentBranchId || "main";
		store.branchRegistry = {
			...store.branchRegistry || {},
			[branchId]: {
				branchId,
				label: branchLabel,
				originStateId: stateId,
				parentBranchId,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			}
		};
		store.currentBranchId = branchId;
		return store.branchRegistry[branchId];
	}
	function appendTraceRecordToStore(store, record) {
		if (typeof store?.appendTrace === "function") return store.appendTrace(record);
		if (!store || !record || typeof record !== "object") return null;
		const nextTrace = [...Array.isArray(store.interactionTrace) ? [...store.interactionTrace] : [], clone$14(record)];
		const retention = Number.isFinite(store.maxTraceRetention) ? store.maxTraceRetention : null;
		store.interactionTrace = retention && retention > 0 ? nextTrace.slice(-retention) : nextTrace;
		if (typeof store.emitChange === "function") store.emitChange();
		return record;
	}
	function mergeRuntimePatch$1(base, patch) {
		if (!patch || typeof patch !== "object" || Array.isArray(patch)) return base;
		const output = Array.isArray(base) ? [...base] : { ...base || {} };
		for (const [key, value] of Object.entries(patch)) {
			if (Array.isArray(value)) {
				output[key] = clone$14(value);
				continue;
			}
			if (value && typeof value === "object") {
				output[key] = mergeRuntimePatch$1(output[key], value);
				continue;
			}
			output[key] = value;
		}
		return output;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/readStatePatch.js
	function clone$13(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readStatePatch(store, refs = []) {
		const normalizedRefs = Array.isArray(refs) ? refs.filter(Boolean) : [];
		if (typeof store?.buildStatePatch === "function") return store.buildStatePatch(normalizedRefs);
		const state = readWorkspaceStateFromStore(store, { refs: normalizedRefs }) || {};
		const patch = {};
		for (const ref of normalizedRefs) {
			if (state?.widgets?.[ref]) {
				patch[ref] = clone$13(state.widgets[ref]);
				continue;
			}
			if (ref === "shared" && state?.shared !== void 0) {
				patch.shared = clone$13(state.shared);
				continue;
			}
			if (ref === "taskContext" && state?.taskContext !== void 0) {
				patch.taskContext = clone$13(state.taskContext);
				continue;
			}
			if (ref === "replayContext" && state?.replayContext !== void 0) patch.replayContext = clone$13(state.replayContext);
		}
		return patch;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/hostBridge.js
	function noop$1() {}
	function emptyArray() {
		return [];
	}
	function emptyObject() {
		return {};
	}
	function cloneValue$2(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function selectValue(selectors, name, readState, fallbackSelector) {
		const selector = selectors?.[name] || fallbackSelector;
		return typeof selector === "function" ? selector(readState()) : null;
	}
	function resolveAction(actions, name, fallbackFactory) {
		const action = actions?.[name];
		if (typeof action === "function") return action;
		return fallbackFactory;
	}
	function invokeResolvedAction(actions, name, readState, fallbackKey, args = []) {
		const action = actions?.[name];
		if (typeof action === "function") return action(...args);
		const state = readState() || {};
		const fallbackAction = state?.[fallbackKey];
		if (typeof fallbackAction === "function") return fallbackAction.call(state, ...args);
	}
	function firstNonNull(...values) {
		for (const value of values) if (value !== void 0 && value !== null) return value;
		return null;
	}
	function normalizeSelectionRegistry(registry) {
		if (!registry || typeof registry !== "object" || Array.isArray(registry)) return {};
		return cloneValue$2(registry);
	}
	function normalizePrimarySelectionRef(primary) {
		if (typeof primary === "string" && primary.length > 0) return primary;
		if (primary && typeof primary === "object") {
			if (typeof primary.ref === "string" && primary.ref.length > 0) return primary.ref;
			if (typeof primary.selectionRef === "string" && primary.selectionRef.length > 0) return primary.selectionRef;
		}
		return null;
	}
	function normalizeFocusState(focus) {
		if (!focus || typeof focus !== "object" || Array.isArray(focus)) return null;
		const widgetRef = typeof focus.widgetRef === "string" ? focus.widgetRef : typeof focus.widget_ref === "string" ? focus.widget_ref : null;
		const widgetId = typeof focus.widgetId === "string" ? focus.widgetId : typeof focus.widget_id === "string" ? focus.widget_id : null;
		const source = typeof focus.source === "string" && focus.source.length > 0 ? focus.source : "workspace";
		if (!widgetRef) return null;
		return {
			widgetRef,
			widgetId,
			source
		};
	}
	function normalizeUniqueStringArray(values) {
		return Array.isArray(values) ? values.filter((value, index) => typeof value === "string" && value.length > 0 && values.indexOf(value) === index) : [];
	}
	function normalizeUniqueScalarArray(values) {
		return Array.isArray(values) ? cloneValue$2(values.filter((value, index) => value != null && values.findIndex((candidate) => JSON.stringify(candidate) === JSON.stringify(value)) === index)) : [];
	}
	function normalizeHighlightEntry(entry) {
		if (!entry || typeof entry !== "object" || Array.isArray(entry)) return null;
		return {
			widgetRef: typeof entry.widgetRef === "string" ? entry.widgetRef : typeof entry.widget_ref === "string" ? entry.widget_ref : null,
			widgetId: typeof entry.widgetId === "string" ? entry.widgetId : typeof entry.widget_id === "string" ? entry.widget_id : null,
			highlightedKeys: normalizeUniqueScalarArray(entry.highlightedKeys || entry.highlighted_keys),
			inboundLinkIds: normalizeUniqueStringArray(entry.inboundLinkIds || entry.inbound_link_ids),
			highlightLinkIds: normalizeUniqueStringArray(entry.highlightLinkIds || entry.highlight_link_ids),
			linkedSourceRefs: normalizeUniqueStringArray(entry.linkedSourceRefs || entry.linked_source_refs)
		};
	}
	function normalizeHighlightState(highlight) {
		if (!highlight || typeof highlight !== "object" || Array.isArray(highlight)) return null;
		const entries = Array.isArray(highlight.entries) ? highlight.entries.map((entry) => normalizeHighlightEntry(entry)).filter(Boolean) : [];
		const explicitActiveWidgetRefs = normalizeUniqueStringArray(highlight.activeWidgetRefs || highlight.active_widget_refs);
		return {
			entries,
			activeWidgetRefs: explicitActiveWidgetRefs.length > 0 ? explicitActiveWidgetRefs : entries.map((entry) => entry.widgetRef).filter(Boolean)
		};
	}
	function deriveCurrentSelectionFromRegistry(registry, primarySelectionRef) {
		const normalizedRegistry = normalizeSelectionRegistry(registry);
		if (primarySelectionRef && normalizedRegistry[primarySelectionRef]) return cloneValue$2(normalizedRegistry[primarySelectionRef]);
		return cloneValue$2(Object.values(normalizedRegistry).find(Boolean) || null);
	}
	function defaultReadSelectionRegistry(state) {
		return firstNonNull(state?.shared?.selections?.registry, state?.selectionRegistry, state?.currentSelections, {});
	}
	function defaultReadPrimarySelectionRef(state) {
		return firstNonNull(state?.shared?.selections?.views?.primary?.ref, state?.shared?.selections?.views?.primary?.selectionRef, state?.primarySelectionRef, state?.currentSelectionRef);
	}
	function createDefaultWidgetVAHostBridge() {
		const defaultSelectionRegistry = emptyObject;
		const defaultPrimarySelectionRef = () => null;
		return {
			subscribe: () => noop$1,
			readSessionId: () => null,
			readBaselineSpec: () => null,
			readCurrentSpec: () => null,
			readWorkspaceSpec: () => null,
			readPlanningRequest: () => null,
			readRunMode: () => "goal_oriented",
			readUserIntent: () => "",
			readSelectionRegistry: defaultSelectionRegistry,
			readPrimarySelectionRef: defaultPrimarySelectionRef,
			readCurrentSelection: () => deriveCurrentSelectionFromRegistry(defaultSelectionRegistry(), defaultPrimarySelectionRef()),
			readCurrentSelections: defaultSelectionRegistry,
			readFocusedWidgetRef: () => null,
			readFocusState: () => null,
			readHighlightState: () => null,
			readSharedFilters: emptyObject,
			readViewportState: emptyObject,
			readWorkspaceAnnotations: emptyArray,
			readSelectionHistory: emptyArray,
			readSelectionsHistory: emptyArray,
			readSelectionFuture: emptyArray,
			readSelectionsFuture: emptyArray,
			writeCurrentSpec: void 0,
			resetCurrentSpec: void 0,
			writeWorkspaceSpec: void 0,
			writePlanningRequest: void 0,
			writeRunMode: void 0,
			writeUserIntent: void 0,
			writeCurrentSelection: void 0,
			writeCurrentSelections: void 0,
			resetSelectionHistory: void 0,
			setFocusedWidgetRef: void 0,
			setWorkspaceAnnotations: void 0,
			addWorkspaceAnnotation: void 0,
			clearWorkspaceAnnotations: void 0,
			undoSelection: void 0,
			redoSelection: void 0
		};
	}
	function createWidgetVAHostBridge(options = {}) {
		const { getState, getAppState, subscribe, subscribeAppState, selectors = {}, actions = {}, ...bridgeMethods } = options;
		const defaultBridge = createDefaultWidgetVAHostBridge();
		if (Object.keys(bridgeMethods).some((key) => typeof bridgeMethods[key] === "function")) return {
			...defaultBridge,
			subscribe: typeof subscribe === "function" ? subscribe : defaultBridge.subscribe,
			...bridgeMethods
		};
		const readState = typeof getState === "function" ? getState : typeof getAppState === "function" ? getAppState : () => ({});
		const subscribeToState = typeof subscribe === "function" ? subscribe : typeof subscribeAppState === "function" ? subscribeAppState : () => noop$1;
		const initialState = readState() || {};
		const readSelectionRegistry = () => normalizeSelectionRegistry(selectValue(selectors, "readSelectionRegistry", readState, defaultReadSelectionRegistry) || {});
		const readPrimarySelectionRef = () => normalizePrimarySelectionRef(selectValue(selectors, "readPrimarySelectionRef", readState, defaultReadPrimarySelectionRef));
		return {
			subscribe: subscribeToState,
			readSessionId: () => selectValue(selectors, "readSessionId", readState, (state) => state?.currentSessionId || null),
			readBaselineSpec: () => selectValue(selectors, "readBaselineSpec", readState, (state) => state?.baselineSpec || null),
			readCurrentSpec: () => selectValue(selectors, "readCurrentSpec", readState, (state) => state?.currentSpec || null),
			readWorkspaceSpec: () => selectValue(selectors, "readWorkspaceSpec", readState, (state) => state?.workspaceSpec || null),
			readPlanningRequest: () => selectValue(selectors, "readPlanningRequest", readState, (state) => state?.planningRequest || null),
			readRunMode: () => selectValue(selectors, "readRunMode", readState, (state) => state?.runMode || "goal_oriented") || "goal_oriented",
			readUserIntent: () => selectValue(selectors, "readUserIntent", readState, (state) => state?.userIntent || "") || "",
			readSelectionRegistry,
			readPrimarySelectionRef,
			readCurrentSelection: () => {
				const explicitSelection = selectValue(selectors, "readCurrentSelection", readState, (state) => state?.currentSelection || null);
				if (explicitSelection) return cloneValue$2(explicitSelection);
				return deriveCurrentSelectionFromRegistry(readSelectionRegistry(), readPrimarySelectionRef());
			},
			readCurrentSelections: () => {
				const explicitSelections = selectValue(selectors, "readCurrentSelections", readState, (state) => state?.currentSelections || null);
				if (explicitSelections && typeof explicitSelections === "object" && !Array.isArray(explicitSelections)) return cloneValue$2(explicitSelections);
				return readSelectionRegistry();
			},
			readFocusedWidgetRef: () => selectValue(selectors, "readFocusedWidgetRef", readState, (state) => state?.currentFocusedWidgetRef || null),
			readFocusState: () => normalizeFocusState(selectValue(selectors, "readFocusState", readState, (state) => state?.shared?.focus || state?.currentFocusState || null)),
			readHighlightState: () => normalizeHighlightState(selectValue(selectors, "readHighlightState", readState, (state) => state?.shared?.highlight || state?.currentHighlightState || null)),
			readSharedFilters: () => selectValue(selectors, "readSharedFilters", readState, (state) => state?.shared?.globalFilters || state?.sharedFilters || state?.currentSharedFilters || {}) || {},
			readViewportState: () => selectValue(selectors, "readViewportState", readState, (state) => state?.shared?.viewport || state?.viewportState || state?.currentViewportState || {}) || {},
			readWorkspaceAnnotations: () => selectValue(selectors, "readWorkspaceAnnotations", readState, (state) => state?.workspaceAnnotations || []) || [],
			readSelectionHistory: () => selectValue(selectors, "readSelectionHistory", readState, (state) => state?.currentSelectionHistory || []) || [],
			readSelectionsHistory: () => selectValue(selectors, "readSelectionsHistory", readState, (state) => state?.currentSelectionsHistory || []) || [],
			readSelectionFuture: () => selectValue(selectors, "readSelectionFuture", readState, (state) => state?.currentSelectionFuture || []) || [],
			readSelectionsFuture: () => selectValue(selectors, "readSelectionsFuture", readState, (state) => state?.currentSelectionsFuture || []) || [],
			writeCurrentSpec: typeof resolveAction(actions, "writeCurrentSpec", initialState?.setCurrentSpec) === "function" ? (nextSpec, options = {}) => invokeResolvedAction(actions, "writeCurrentSpec", readState, "setCurrentSpec", [nextSpec, options]) : void 0,
			resetCurrentSpec: typeof resolveAction(actions, "resetCurrentSpec", initialState?.resetCurrentSpec) === "function" ? () => invokeResolvedAction(actions, "resetCurrentSpec", readState, "resetCurrentSpec") : void 0,
			writeWorkspaceSpec: typeof resolveAction(actions, "writeWorkspaceSpec", initialState?.setWorkspaceSpec) === "function" ? (workspaceSpec) => invokeResolvedAction(actions, "writeWorkspaceSpec", readState, "setWorkspaceSpec", [workspaceSpec]) : void 0,
			writePlanningRequest: typeof resolveAction(actions, "writePlanningRequest", initialState?.setPlanningRequest) === "function" ? (planningRequest) => invokeResolvedAction(actions, "writePlanningRequest", readState, "setPlanningRequest", [planningRequest]) : void 0,
			writeRunMode: typeof resolveAction(actions, "writeRunMode", initialState?.setRunMode) === "function" ? (runMode) => invokeResolvedAction(actions, "writeRunMode", readState, "setRunMode", [runMode]) : void 0,
			writeUserIntent: typeof resolveAction(actions, "writeUserIntent", initialState?.setUserIntent) === "function" ? (userIntent) => invokeResolvedAction(actions, "writeUserIntent", readState, "setUserIntent", [userIntent]) : void 0,
			writeCurrentSelection: typeof resolveAction(actions, "writeCurrentSelection", initialState?.setCurrentSelection) === "function" ? (selection, nextOptions = {}) => invokeResolvedAction(actions, "writeCurrentSelection", readState, "setCurrentSelection", [selection, nextOptions]) : void 0,
			writeCurrentSelections: typeof resolveAction(actions, "writeCurrentSelections", initialState?.setCurrentSelections) === "function" ? (selections, nextOptions = {}) => invokeResolvedAction(actions, "writeCurrentSelections", readState, "setCurrentSelections", [selections, nextOptions]) : void 0,
			resetSelectionHistory: typeof resolveAction(actions, "resetSelectionHistory", initialState?.resetSelectionHistory) === "function" ? () => invokeResolvedAction(actions, "resetSelectionHistory", readState, "resetSelectionHistory") : void 0,
			setFocusedWidgetRef: typeof resolveAction(actions, "setFocusedWidgetRef", initialState?.setCurrentFocusedWidgetRef) === "function" ? (widgetRef) => invokeResolvedAction(actions, "setFocusedWidgetRef", readState, "setCurrentFocusedWidgetRef", [widgetRef]) : void 0,
			setWorkspaceAnnotations: typeof resolveAction(actions, "setWorkspaceAnnotations", initialState?.setWorkspaceAnnotations) === "function" ? (annotations) => invokeResolvedAction(actions, "setWorkspaceAnnotations", readState, "setWorkspaceAnnotations", [annotations]) : void 0,
			addWorkspaceAnnotation: typeof resolveAction(actions, "addWorkspaceAnnotation", initialState?.addWorkspaceAnnotation) === "function" ? (annotation) => invokeResolvedAction(actions, "addWorkspaceAnnotation", readState, "addWorkspaceAnnotation", [annotation]) : void 0,
			clearWorkspaceAnnotations: typeof resolveAction(actions, "clearWorkspaceAnnotations", initialState?.clearWorkspaceAnnotations) === "function" ? () => invokeResolvedAction(actions, "clearWorkspaceAnnotations", readState, "clearWorkspaceAnnotations") : void 0,
			undoSelection: typeof resolveAction(actions, "undoSelection", initialState?.undoCurrentSelection) === "function" ? () => invokeResolvedAction(actions, "undoSelection", readState, "undoCurrentSelection") : void 0,
			redoSelection: typeof resolveAction(actions, "redoSelection", initialState?.redoCurrentSelection) === "function" ? () => invokeResolvedAction(actions, "redoSelection", readState, "redoCurrentSelection") : void 0
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/queryScope.js
	function readNormalizedQueryScope({ call = null, descriptor = null, querySpec = null } = {}) {
		const scope = makeQueryScope(querySpec?.queryScope || call?.params?.queryScope || call?.queryScope || descriptor?.queryScope || null);
		return {
			widgetRef: scope.widgetRef || (typeof call?.targetRef === "string" && call.targetRef.length > 0 ? call.targetRef : null) || (typeof descriptor?.targetRef === "string" && descriptor.targetRef.length > 0 ? descriptor.targetRef : null),
			dataRef: scope.dataRef || (typeof call?.dataRef === "string" && call.dataRef.length > 0 ? call.dataRef : null),
			selectionRef: scope.selectionRef || (typeof call?.selectionRef === "string" && call.selectionRef.length > 0 ? call.selectionRef : null),
			focusRef: scope.focusRef,
			viewportRef: scope.viewportRef
		};
	}
	function buildScopedParams({ params = null, call = null, descriptor = null, querySpec = null, includeTopLevelLegacyTargeting = true } = {}) {
		const rawParams = params ?? call?.params ?? querySpec ?? null;
		const normalizedParams = rawParams && typeof rawParams === "object" && !Array.isArray(rawParams) ? { ...rawParams } : {};
		const scope = readNormalizedQueryScope({
			call: call ? {
				...call,
				...includeTopLevelLegacyTargeting ? {} : {
					targetRef: null,
					dataRef: null
				},
				params: normalizedParams
			} : { params: normalizedParams },
			descriptor,
			querySpec
		});
		if (Object.values(scope).some((value) => typeof value === "string" && value.length > 0)) normalizedParams.queryScope = scope;
		else if (normalizedParams.queryScope) normalizedParams.queryScope = makeQueryScope(normalizedParams.queryScope);
		delete normalizedParams.targetRef;
		delete normalizedParams.targetDataRef;
		delete normalizedParams.selectionRef;
		delete normalizedParams.dataRef;
		return normalizedParams;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/ActionContext.js
	function cloneValue$1(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var ActionContext = class {
		constructor({ store, descriptor, call, sync, hostBridge, getState, getAppState, subscribe, linkEngine, helpers = {} }) {
			this.store = store;
			this.descriptor = descriptor;
			this.call = call;
			this.sync = sync;
			this.hostBridge = hostBridge || createWidgetVAHostBridge({
				getState,
				getAppState,
				subscribe
			}) || createDefaultWidgetVAHostBridge();
			this.linkEngine = linkEngine;
			this.helpers = {
				cloneValue: cloneValue$1,
				primaryWidgetSpecFromSnapshot: helpers.primaryWidgetSpecFromSnapshot || (() => null),
				selectionPayloadFromSnapshot: helpers.selectionPayloadFromSnapshot || (() => null),
				selectionPayloadsFromSnapshot: helpers.selectionPayloadsFromSnapshot || (() => ({})),
				annotationsFromSnapshot: helpers.annotationsFromSnapshot || (() => [])
			};
		}
		static describeContract() {
			return makeActionContextSummary({
				methods: [
					"patchWidget",
					"propagate",
					"readStatePatch",
					"syncWorkspace",
					"hostBridge",
					"readDescription",
					"readCurrentState",
					"readCurrentSpec",
					"readBaselineSpec",
					"readFocusedWidgetRef",
					"readWorkspaceAnnotations",
					"readRuntimeData",
					"updateRuntimeData",
					"writeCurrentSpec",
					"updateCurrentSpec",
					"readSnapshot",
					"readSnapshotEntry",
					"resolveTargetWidget",
					"requireTargetWidget",
					"readWidgetAdapter",
					"resolveSelectionRef",
					"resolveSelectionRefs",
					"readRowsForWidget",
					"commitSelection",
					"commitSelections",
					"clearSelection",
					"setFocusedWidgetRef",
					"setWorkspaceAnnotations",
					"addWorkspaceAnnotation",
					"clearWorkspaceAnnotations",
					"undoSelection",
					"redoSelection",
					"resetWorkspace",
					"restoreSnapshotState",
					"collectUpdatedRefs",
					"collectPropagation"
				],
				capabilities: makeActionContextCapabilities({
					workspaceRead: true,
					workspaceWrite: true,
					specMutation: true,
					snapshotReplay: true,
					runtimeDataRead: true,
					runtimeDataWrite: true,
					widgetTargetResolution: true,
					selectionMutation: true,
					annotationMutation: true,
					linkPropagation: true
				}),
				integrations: makeActionContextIntegrations({
					store: true,
					appState: false,
					linkEngine: true,
					traceRecorder: false
				})
			});
		}
		syncWorkspace() {
			this.sync();
			return this.readCurrentState();
		}
		patchWidget(ref, patch) {
			return patchWidgetInStore(this.store, ref, patch);
		}
		propagate(changedRef, options = {}) {
			const propagation = this.collectPropagation(changedRef, options);
			return options?.returnDetails ? propagation : propagation.refs;
		}
		readStatePatch(refs) {
			return readStatePatch(this.store, refs);
		}
		readDescription() {
			return readWorkspaceDescriptionFromStore(this.store);
		}
		readCurrentState(options = {}) {
			return readWorkspaceStateFromStore(this.store, options);
		}
		readCurrentSpec() {
			return this.hostBridge.readCurrentSpec();
		}
		readBaselineSpec() {
			return this.hostBridge.readBaselineSpec();
		}
		readFocusedWidgetRef() {
			return this.hostBridge.readFocusedWidgetRef();
		}
		readWorkspaceAnnotations() {
			return this.hostBridge.readWorkspaceAnnotations();
		}
		readRuntimeData(dataRef) {
			return readRuntimeDataFromStore(this.store, dataRef);
		}
		updateRuntimeData(dataRef, updater) {
			return updateRuntimeDataInStore(this.store, dataRef, updater);
		}
		writeCurrentSpec(nextSpec) {
			if (typeof this.hostBridge.writeCurrentSpec !== "function") return this.readCurrentState();
			this.store.resetWidgetPatches?.();
			this.hostBridge.writeCurrentSpec(nextSpec, { trackHistory: true });
			this.sync();
			return this.readCurrentState();
		}
		updateCurrentSpec(updater) {
			const currentSpec = this.readCurrentSpec();
			const nextSpec = this.helpers.cloneValue(currentSpec);
			const updatedSpec = typeof updater === "function" ? updater(nextSpec) || nextSpec : nextSpec;
			return this.writeCurrentSpec(updatedSpec);
		}
		readSnapshot(stateId) {
			return readSnapshotFromStore(this.store, stateId) || this.store.readSnapshot?.(stateId) || null;
		}
		readSnapshotEntry(stateId) {
			return readSnapshotEntryFromStore(this.store, stateId);
		}
		readQueryScope() {
			return readNormalizedQueryScope({
				call: this.call,
				descriptor: this.descriptor
			});
		}
		resolveTargetWidget({ kind, targetRef } = {}) {
			const resolvedRef = targetRef || this.readQueryScope()?.widgetRef || null;
			if (resolvedRef) return resolveWidgetRecordFromStore(this.store, resolvedRef);
			const focusedWidgetRef = this.readCurrentState()?.shared?.focusedWidget || this.readFocusedWidgetRef() || null;
			if (focusedWidgetRef) {
				const focusedWidget = resolveWidgetRecordFromStore(this.store, focusedWidgetRef);
				if (focusedWidget && (!kind || focusedWidget.kind === kind)) return focusedWidget;
			}
			const widgets = listStoreWidgets(this.store);
			if (kind) {
				const match = widgets.find((widget) => widget.kind === kind) || null;
				return match ? resolveWidgetRecordFromStore(this.store, match.ref) || match : null;
			}
			const firstWidget = widgets[0] || null;
			return firstWidget ? resolveWidgetRecordFromStore(this.store, firstWidget.ref) || firstWidget : null;
		}
		requireTargetWidget({ kind = null, targetRef = null, message = null } = {}) {
			const targetWidget = this.resolveTargetWidget({
				kind,
				targetRef
			});
			if (targetWidget) return targetWidget;
			if (message) throw new Error(message);
			if (kind) throw new Error(`No active ${kind} widget in the current workspace.`);
			throw new Error("No active widget/spec in the current workspace.");
		}
		readWidgetAdapter(targetRef = null) {
			const widgetRef = this.resolveTargetWidget({ targetRef: targetRef || this.readQueryScope()?.widgetRef || null })?.ref || targetRef || null;
			return widgetRef ? this.store.getWidgetAdapter?.(widgetRef) || null : null;
		}
		resolveSelectionRefs(state = this.readCurrentState(), options = {}) {
			const activeSelections = readSelectionRegistry(state?.shared || {});
			const allRefs = Object.keys(activeSelections).filter(Boolean);
			if (allRefs.length === 0) return [];
			const explicitTargetRef = options?.targetRef || null;
			const targetWidgetRef = (explicitTargetRef ? resolveWidgetRecordFromStore(this.store, explicitTargetRef) : this.resolveTargetWidget({
				kind: options?.kind,
				targetRef: options?.targetRef
			}))?.ref || explicitTargetRef || null;
			if (!targetWidgetRef) return allRefs;
			const targetSelectionRefs = allRefs.filter((selectionRef) => state?.widgets?.[targetWidgetRef]?.selections?.[selectionRef]);
			return targetSelectionRefs.length > 0 ? targetSelectionRefs : allRefs;
		}
		resolveSelectionRef(state = this.readCurrentState(), options = {}) {
			return this.resolveSelectionRefs(state, options)[0] || null;
		}
		readRowsForWidget(widgetRef) {
			const widgetState = this.store.getWidgetState?.(widgetRef) || this.readCurrentState({ refs: [widgetRef] })?.widgets?.[widgetRef];
			const currentDataRef = widgetState?.data?.currentDataRef || widgetState?.data?.sourceDataRef;
			const runtimeData = this.readRuntimeData(currentDataRef);
			return {
				dataRef: currentDataRef,
				rows: Array.isArray(runtimeData?.rows) ? runtimeData.rows : []
			};
		}
		commitSelection(selection, options = {}) {
			if (typeof this.hostBridge.writeCurrentSelection !== "function") return this.commitSelectionFallback(selection, options);
			this.hostBridge.writeCurrentSelection(selection || null, options);
			return this.syncWorkspace();
		}
		commitSelections(selections, options = {}) {
			if (typeof this.hostBridge.writeCurrentSelections !== "function") return this.readCurrentState();
			this.hostBridge.writeCurrentSelections(selections || {}, options);
			return this.syncWorkspace();
		}
		clearSelection(options = {}) {
			if (typeof this.hostBridge.writeCurrentSelection !== "function") return this.clearSelectionFallback(options);
			const targetWidget = this.resolveTargetWidget();
			if (targetWidget?.widgetId) return this.commitSelection(null, {
				...options,
				removeSourceWidgetId: options?.removeSourceWidgetId || targetWidget.widgetId,
				clearAll: options?.clearAll === true
			});
			return this.commitSelection(null, options);
		}
		commitSelectionFallback(selection, options = {}) {
			if (!selection) return this.clearSelectionFallback(options);
			const targetWidget = this.resolveTargetWidget({ targetRef: this.readQueryScope()?.widgetRef || null });
			if (!targetWidget?.ref) return this.readCurrentState();
			const currentWidgetState = this.readCurrentState()?.widgets?.[targetWidget.ref] || this.store.getWidgetState?.(targetWidget.ref) || null;
			const selectionId = selection.selection_id || `sel_${Date.now()}`;
			const selectionRef = `${targetWidget.ref}/selection/${selectionId}`;
			const selectionState = makeSelectionState(buildSelectionStateInput({
				...selection,
				selectionRef,
				selectionId,
				sourceWidgetRef: targetWidget.ref,
				sourceWidgetId: targetWidget.widgetId || null
			}));
			const nextSelections = {
				...currentWidgetState?.selections || {},
				[selectionRef]: selectionState
			};
			recordSelectionHistorySnapshot(this.store);
			updateSharedStateInStore(this.store, (shared) => {
				const nextRegistry = {
					...readSelectionRegistry(shared),
					[selectionRef]: selectionState
				};
				const nextByWidget = {
					...readSelectionByWidgetView(shared),
					[targetWidget.widgetId || targetWidget.ref]: normalizePrimarySelectionView({
						...selectionState,
						selectionRef
					}, nextRegistry)
				};
				return {
					...withSelectionSubmodel(shared, {
						registry: nextRegistry,
						primary: normalizePrimarySelectionView({
							...selectionState,
							selectionRef
						}, nextRegistry),
						byWidget: nextByWidget
					}),
					focusedWidget: targetWidget.ref
				};
			});
			return this.patchWidget(targetWidget.ref, {
				selections: nextSelections,
				data: {
					...currentWidgetState?.data || {},
					...Number.isFinite(selection.count) ? { selectedCount: selection.count } : {}
				}
			});
		}
		clearSelectionFallback(_options = {}) {
			const targetWidget = this.resolveTargetWidget();
			if (!targetWidget?.ref) return this.readCurrentState();
			const currentWidgetState = this.readCurrentState()?.widgets?.[targetWidget.ref] || this.store.getWidgetState?.(targetWidget.ref) || null;
			const widgetSelectionRefs = Object.keys(currentWidgetState?.selections || {});
			const currentShared = this.readCurrentState()?.shared || {};
			const nextActiveSelections = { ...readSelectionRegistry(currentShared) };
			for (const selectionRef of widgetSelectionRefs) delete nextActiveSelections[selectionRef];
			const nextByWidget = { ...readSelectionByWidgetView(currentShared) };
			delete nextByWidget[targetWidget.widgetId || targetWidget.ref];
			if (widgetSelectionRefs.length > 0) recordSelectionHistorySnapshot(this.store);
			updateSharedStateInStore(this.store, (shared) => withSelectionSubmodel(shared, {
				registry: nextActiveSelections,
				primary: null,
				byWidget: nextByWidget
			}));
			return this.patchWidget(targetWidget.ref, {
				selections: {},
				data: {
					...currentWidgetState?.data || {},
					selectedCount: 0
				}
			});
		}
		setFocusedWidgetRef(widgetRef) {
			if (typeof this.hostBridge.setFocusedWidgetRef !== "function") {
				const nextWidget = widgetRef ? resolveWidgetRecordFromStore(this.store, widgetRef) : null;
				return updateSharedStateInStore(this.store, (shared) => withFocusSubmodel(shared, widgetRef ? {
					widgetRef,
					widgetId: nextWidget?.widgetId || null,
					source: "workspace"
				} : null));
			}
			this.hostBridge.setFocusedWidgetRef(widgetRef || null);
			return this.syncWorkspace();
		}
		setWorkspaceAnnotations(annotations) {
			if (typeof this.hostBridge.setWorkspaceAnnotations !== "function") return updateSharedStateInStore(this.store, (shared) => ({
				...shared,
				annotations: Array.isArray(annotations) ? annotations : []
			}));
			this.hostBridge.setWorkspaceAnnotations(Array.isArray(annotations) ? annotations : []);
			return this.syncWorkspace();
		}
		addWorkspaceAnnotation(annotation) {
			if (typeof this.hostBridge.addWorkspaceAnnotation !== "function") return updateSharedStateInStore(this.store, (shared) => ({
				...shared,
				annotations: [...Array.isArray(shared?.annotations) ? shared.annotations : [], annotation]
			}));
			this.hostBridge.addWorkspaceAnnotation(annotation);
			return this.syncWorkspace();
		}
		clearWorkspaceAnnotations() {
			if (typeof this.hostBridge.clearWorkspaceAnnotations !== "function") return updateSharedStateInStore(this.store, (shared) => ({
				...shared,
				annotations: []
			}));
			this.hostBridge.clearWorkspaceAnnotations();
			return this.syncWorkspace();
		}
		undoSelection() {
			if (typeof this.store?.beginTransition !== "function" || typeof this.hostBridge.undoSelection !== "function") return undoSelectionInStore(this.store);
			this.store.beginTransition({ transitionType: "undo" });
			this.hostBridge.undoSelection();
			return this.syncWorkspace();
		}
		redoSelection() {
			if (typeof this.store?.beginTransition !== "function" || typeof this.hostBridge.redoSelection !== "function") return redoSelectionInStore(this.store);
			this.store.beginTransition({ transitionType: "redo" });
			this.hostBridge.redoSelection();
			return this.syncWorkspace();
		}
		hasUndoSelectionHistory() {
			const history = this.hostBridge.readSelectionHistory();
			const selectionsHistory = this.hostBridge.readSelectionsHistory();
			return Array.isArray(history) && history.length > 0 || Array.isArray(selectionsHistory) && selectionsHistory.length > 0 || hasUndoSelectionHistoryInStore(this.store);
		}
		hasRedoSelectionHistory() {
			const future = this.hostBridge.readSelectionFuture();
			const selectionsFuture = this.hostBridge.readSelectionsFuture();
			return Array.isArray(future) && future.length > 0 || Array.isArray(selectionsFuture) && selectionsFuture.length > 0 || hasRedoSelectionHistoryInStore(this.store);
		}
		resetWorkspace() {
			if (typeof this.store?.beginTransition !== "function" || typeof this.hostBridge.resetCurrentSpec !== "function" || typeof this.hostBridge.writeCurrentSelection !== "function" || typeof this.hostBridge.resetSelectionHistory !== "function" || typeof this.hostBridge.setFocusedWidgetRef !== "function" || typeof this.hostBridge.clearWorkspaceAnnotations !== "function") return resetWorkspaceInteractionsInStore(this.store);
			this.store.beginTransition({ transitionType: "reset" });
			this.store.resetWidgetPatches?.();
			this.hostBridge.resetCurrentSpec();
			this.hostBridge.writeCurrentSelection(null, { trackHistory: false });
			this.hostBridge.resetSelectionHistory();
			this.hostBridge.setFocusedWidgetRef(null);
			this.hostBridge.clearWorkspaceAnnotations();
			return this.syncWorkspace();
		}
		restoreSnapshotState(snapshot) {
			const snapshotState = snapshot?.state || snapshot;
			const replayContext = snapshot?.replayContext || null;
			const restoredSpec = this.helpers.primaryWidgetSpecFromSnapshot(snapshotState);
			if (typeof this.hostBridge.resetSelectionHistory !== "function" || typeof this.hostBridge.writeCurrentSelections !== "function" || typeof this.hostBridge.setFocusedWidgetRef !== "function" || typeof this.hostBridge.setWorkspaceAnnotations !== "function" || typeof this.hostBridge.writeCurrentSpec !== "function") return restoreSnapshotStateInStore(this.store, snapshot);
			this.store.resetWidgetPatches?.();
			if (Object.prototype.hasOwnProperty.call(replayContext || {}, "baselineSpec")) this.hostBridge.writeCurrentSpec(replayContext?.baselineSpec ?? null, {
				replaceBaseline: true,
				trackHistory: false
			});
			this.hostBridge.writeWorkspaceSpec?.(replayContext?.workspaceSpec ?? null);
			this.hostBridge.writePlanningRequest?.(replayContext?.planningRequest ?? null);
			if (replayContext?.runMode) this.hostBridge.writeRunMode?.(replayContext.runMode);
			this.hostBridge.writeUserIntent?.(replayContext?.userIntent ?? "");
			if (restoredSpec) this.hostBridge.writeCurrentSpec(restoredSpec, { trackHistory: false });
			else if (Object.prototype.hasOwnProperty.call(replayContext || {}, "currentSpec")) this.hostBridge.writeCurrentSpec(replayContext?.currentSpec ?? null, { trackHistory: false });
			const selection = this.helpers.selectionPayloadFromSnapshot(snapshotState);
			const selections = this.helpers.selectionPayloadsFromSnapshot(snapshotState);
			this.hostBridge.resetSelectionHistory();
			this.hostBridge.writeCurrentSelections(selections, {
				fallbackSelection: selection,
				trackHistory: false
			});
			this.hostBridge.setFocusedWidgetRef(snapshotState?.shared?.focusedWidget || null);
			this.hostBridge.setWorkspaceAnnotations(this.helpers.annotationsFromSnapshot(snapshotState));
			return this.syncWorkspace();
		}
		collectUpdatedRefs(nextState, extraRefs = []) {
			const changedRefs = nextState?.delta?.changedRefs?.length ? nextState.delta.changedRefs : Object.keys(nextState?.widgets || {});
			return Array.from(new Set([...changedRefs || [], ...extraRefs || []]));
		}
		collectPropagation(selectionRef, options = {}) {
			if (!selectionRef || !this.linkEngine) return {
				refs: [],
				links: [],
				effects: []
			};
			const propagation = this.linkEngine.propagate({
				sourceRef: selectionRef,
				state: options.state || this.readCurrentState()
			});
			return {
				nextState: propagation?.nextState || this.readCurrentState(),
				refs: Array.isArray(propagation?.affectedRefs) ? propagation.affectedRefs : [],
				links: Array.isArray(propagation?.links) ? propagation.links : [],
				effects: Array.isArray(propagation?.effects) ? propagation.effects : []
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/actionVerification.js
	function cloneValue(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function uniqueStrings$2(values) {
		return [...new Set(values.filter((value) => typeof value === "string" && value.trim().length > 0))];
	}
	function formatVerificationRef(ref) {
		if (ref === "shared") return "shared state";
		if (ref === "taskContext") return "task context";
		return ref;
	}
	function normalizeActionConditions(conditions) {
		if (!Array.isArray(conditions)) return [];
		return conditions.filter((condition) => condition && typeof condition === "object").map((condition) => ({
			description: typeof condition.description === "string" ? condition.description : "",
			...typeof condition.checkHint === "string" ? { checkHint: condition.checkHint } : {},
			...typeof condition.failureMessage === "string" ? { failureMessage: condition.failureMessage } : {}
		})).filter((condition) => condition.description || condition.checkHint || condition.failureMessage);
	}
	function buildPostconditionVerificationHints({ descriptor, updatedRefs = [] } = {}) {
		const refs = Array.isArray(updatedRefs) ? updatedRefs.filter((ref) => typeof ref === "string").map((ref) => formatVerificationRef(ref)) : [];
		const refSummary = refs.length > 0 ? refs.join(", ") : null;
		return normalizeActionConditions(descriptor?.postconditions).map((condition) => {
			if (condition.checkHint) return condition.checkHint;
			if (refSummary) return `Check ${refSummary} and confirm: ${condition.description}`;
			return `Confirm: ${condition.description}`;
		});
	}
	function buildActionVerificationPayload({ descriptor, updatedRefs = [], verificationHints = [] } = {}) {
		const expectedPostconditions = normalizeActionConditions(descriptor?.postconditions);
		const mergedHints = uniqueStrings$2([...buildPostconditionVerificationHints({
			descriptor,
			updatedRefs
		}), ...Array.isArray(verificationHints) ? verificationHints : []]);
		const verificationNote = expectedPostconditions.length === 0 ? null : expectedPostconditions.length === 1 ? `Expected postcondition: ${expectedPostconditions[0].description}` : `Expected postconditions: ${expectedPostconditions.map((condition) => condition.description).join("; ")}`;
		return {
			expectedPostconditions: cloneValue(expectedPostconditions),
			verificationHints: mergedHints,
			verificationNote
		};
	}
	function resolveActionDescriptorForVerification({ store, actionName, targetRef, affectedRefs = [] } = {}) {
		if (!store || !actionName) return null;
		const candidateRefs = [
			targetRef,
			...Array.isArray(affectedRefs) ? affectedRefs : [],
			null
		];
		for (const ref of candidateRefs) {
			const descriptor = store.getActionDescriptor(actionName, ref || void 0);
			if (descriptor) return descriptor;
		}
		return store.listActions().find((descriptor) => descriptor?.name === actionName) || null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/actionUsageSurface.js
	function clone$12(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeString(value) {
		return typeof value === "string" && value.trim().length > 0 ? value.trim() : null;
	}
	function uniqueStrings$1(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => typeof value === "string" && value.length > 0))];
	}
	function readRootEncoding(spec) {
		return (Array.isArray(spec?.layer) ? spec.layer.find((layer) => layer?.encoding)?.encoding : null) || spec?.encoding || {};
	}
	function cloneEncoding(encoding) {
		return encoding && typeof encoding === "object" && !Array.isArray(encoding) ? clone$12(encoding) : null;
	}
	function listParamNames(schema) {
		return Object.keys(schema?.properties || {}).filter((name) => name !== "queryScope");
	}
	function listRequiredParamNames(schema) {
		return (Array.isArray(schema?.required) ? schema.required : []).filter((name) => name !== "queryScope");
	}
	function buildBaseFieldRoles() {
		return {
			resolved: {
				xField: null,
				yField: null,
				colorField: null,
				categoryField: null,
				measureField: null,
				subCategoryField: null
			},
			candidates: {
				xFields: [],
				yFields: [],
				categoryFields: [],
				measureFields: [],
				groupFields: []
			},
			explanations: []
		};
	}
	function pushFieldExplanation(fieldRoles, role, value, reason) {
		fieldRoles.explanations.push({
			role,
			value: value ?? null,
			reason
		});
	}
	function detectBarChannels(encoding = {}) {
		const xType = encoding?.x?.type || null;
		const yType = encoding?.y?.type || null;
		if ((yType === "nominal" || yType === "ordinal") && xType === "quantitative") return {
			categoryChannel: "y",
			valueChannel: "x"
		};
		return {
			categoryChannel: "x",
			valueChannel: "y"
		};
	}
	function inferFieldRoles({ widgetKind, spec, rows = [] }) {
		const fieldRoles = buildBaseFieldRoles();
		const encoding = readRootEncoding(spec);
		const xEncoding = encoding?.x || null;
		const yEncoding = encoding?.y || null;
		const colorEncoding = encoding?.color || null;
		const sizeEncoding = encoding?.size || null;
		const shapeEncoding = encoding?.shape || null;
		const xOffsetEncoding = encoding?.xOffset || null;
		const xField = typeof xEncoding?.field === "string" ? xEncoding.field : null;
		const yField = typeof yEncoding?.field === "string" ? yEncoding.field : null;
		const colorField = typeof colorEncoding?.field === "string" ? colorEncoding.field : null;
		const xType = xEncoding?.type || null;
		const yType = yEncoding?.type || null;
		fieldRoles.resolved.xField = xField;
		fieldRoles.resolved.yField = yField;
		fieldRoles.resolved.colorField = colorField;
		fieldRoles.candidates.xFields = uniqueStrings$1([xField]);
		fieldRoles.candidates.yFields = uniqueStrings$1([yField]);
		fieldRoles.candidates.groupFields = uniqueStrings$1([typeof xOffsetEncoding?.field === "string" ? xOffsetEncoding.field : null, colorField]);
		if (xField) pushFieldExplanation(fieldRoles, "xField", xField, `encoding.x.field is "${xField}".`);
		if (yField) pushFieldExplanation(fieldRoles, "yField", yField, `encoding.y.field is "${yField}".`);
		if (colorField) pushFieldExplanation(fieldRoles, "colorField", colorField, `encoding.color.field is "${colorField}".`);
		if (widgetKind === "bar") {
			const { categoryChannel, valueChannel } = detectBarChannels(encoding);
			const categoryEncoding = encoding?.[categoryChannel] || null;
			const valueEncoding = encoding?.[valueChannel] || null;
			const categoryField = typeof categoryEncoding?.field === "string" ? categoryEncoding.field : null;
			const measureField = typeof valueEncoding?.field === "string" ? valueEncoding.field : null;
			const subCategoryField = typeof xOffsetEncoding?.field === "string" ? xOffsetEncoding.field : colorField;
			fieldRoles.resolved.categoryField = categoryField;
			fieldRoles.resolved.measureField = measureField;
			fieldRoles.resolved.subCategoryField = subCategoryField;
			fieldRoles.candidates.categoryFields = uniqueStrings$1([
				xType === "nominal" || xType === "ordinal" ? xField : null,
				yType === "nominal" || yType === "ordinal" ? yField : null,
				categoryField
			]);
			fieldRoles.candidates.measureFields = uniqueStrings$1([
				xType === "quantitative" ? xField : null,
				yType === "quantitative" ? yField : null,
				measureField
			]);
			if (categoryField) pushFieldExplanation(fieldRoles, "categoryField", categoryField, `Bar charts use the ${categoryChannel} channel as the categorical axis because encoding.${categoryChannel}.type is "${categoryEncoding?.type || "unknown"}".`);
			if (measureField) pushFieldExplanation(fieldRoles, "measureField", measureField, `Bar charts use the ${valueChannel} channel as the quantitative measure because encoding.${valueChannel}.type is "${valueEncoding?.type || "unknown"}".`);
			if (subCategoryField) pushFieldExplanation(fieldRoles, "subCategoryField", subCategoryField, typeof xOffsetEncoding?.field === "string" ? `encoding.xOffset.field is "${subCategoryField}", so grouped members can be addressed by that field.` : `encoding.color.field is "${subCategoryField}", so stacked or grouped members can be addressed by that field.`);
		} else if (widgetKind === "scatter") {
			fieldRoles.resolved.categoryField = colorField;
			fieldRoles.candidates.categoryFields = uniqueStrings$1([colorField]);
			fieldRoles.candidates.measureFields = uniqueStrings$1([xType === "quantitative" ? xField : null, yType === "quantitative" ? yField : null]);
		} else {
			fieldRoles.candidates.categoryFields = uniqueStrings$1([xType === "nominal" || xType === "ordinal" ? xField : null, yType === "nominal" || yType === "ordinal" ? yField : null]);
			fieldRoles.candidates.measureFields = uniqueStrings$1([xType === "quantitative" ? xField : null, yType === "quantitative" ? yField : null]);
		}
		if (fieldRoles.candidates.categoryFields.length === 0 && rows.length > 0) {
			const firstRow = rows.find((row) => row && typeof row === "object") || null;
			if (firstRow) {
				const fallbackCategories = Object.keys(firstRow).filter((field) => {
					const values = rows.map((row) => row?.[field]).filter((value) => value != null);
					if (values.length === 0) return false;
					const uniqueCount = new Set(values.map((value) => JSON.stringify(value))).size;
					return uniqueCount > 1 && uniqueCount <= Math.max(12, Math.ceil(values.length / 2));
				});
				fieldRoles.candidates.categoryFields = uniqueStrings$1(fallbackCategories);
			}
		}
		return {
			fieldRoles,
			encodingSummary: {
				x: cloneEncoding(xEncoding),
				y: cloneEncoding(yEncoding),
				color: cloneEncoding(colorEncoding),
				size: cloneEncoding(sizeEncoding),
				shape: cloneEncoding(shapeEncoding)
			}
		};
	}
	function buildSpecContext(spec, rows = []) {
		const { encodingSummary } = inferFieldRoles({
			widgetKind: null,
			spec,
			rows
		});
		let dataSourceKind = null;
		if (Array.isArray(spec?.data?.values)) dataSourceKind = "inline_values";
		else if (typeof spec?.data?.url === "string" && spec.data.url.trim().length > 0) dataSourceKind = "url";
		else if (Array.isArray(rows) && rows.length > 0) dataSourceKind = "runtime_only";
		return {
			hasCurrentSpec: Boolean(spec && typeof spec === "object" && !Array.isArray(spec)),
			dataSourceKind,
			runtimeRowCount: Array.isArray(rows) ? rows.length : 0,
			encodings: encodingSummary
		};
	}
	function isContinuousDomainEncoding(channelEncoding) {
		const type = channelEncoding?.type || null;
		return type === "quantitative" || type === "temporal";
	}
	function inferLineGroupingField(spec) {
		const encoding = readRootEncoding(spec);
		return encoding?.color?.field || encoding?.detail?.field || null;
	}
	function inferLineDimensionContext(spec, rows = []) {
		const encoding = readRootEncoding(spec);
		const xField = typeof encoding?.x?.field === "string" ? encoding.x.field : null;
		const yField = typeof encoding?.y?.field === "string" ? encoding.y.field : null;
		const lineField = inferLineGroupingField(spec);
		const fallbackCategoryField = lineField || xField || Object.keys(rows.find((row) => row && typeof row === "object") || {}).find((field) => rows.some((row) => typeof row?.[field] === "string")) || null;
		return {
			xField,
			yField,
			lineField: lineField || fallbackCategoryField
		};
	}
	function inferHeatmapFieldContext(spec) {
		const encoding = readRootEncoding(spec);
		return {
			xField: typeof encoding?.x?.field === "string" ? encoding.x.field : null,
			yField: typeof encoding?.y?.field === "string" ? encoding.y.field : null,
			colorField: typeof encoding?.color?.field === "string" ? encoding.color.field : null
		};
	}
	function inferParallelDimensions(spec, rows = []) {
		const foldTransform = (Array.isArray(spec?.transform) ? spec.transform : []).find((transform) => Array.isArray(transform?.fold));
		if (foldTransform?.fold?.length > 0) return [...foldTransform.fold];
		const firstRow = rows.find((row) => row && typeof row === "object") || null;
		return firstRow ? Object.keys(firstRow) : [];
	}
	function inferPrimaryCategoryFieldFromRows(rows = []) {
		const firstRow = rows.find((row) => row && typeof row === "object") || null;
		if (!firstRow) return null;
		return Object.keys(firstRow).find((field) => {
			const values = rows.map((row) => row?.[field]).filter((value) => value != null);
			return values.length > 0 && values.every((value) => typeof value === "string");
		}) || null;
	}
	function inferSankeyContext(rows = []) {
		const firstRow = rows.find((row) => row && typeof row === "object") || null;
		return { field: (firstRow ? Object.keys(firstRow).filter((field) => field !== "value") : [])[0] || null };
	}
	function setParamRole(paramRoles, param, role) {
		if (!param || typeof param !== "string") return;
		paramRoles[param] = role;
	}
	function setSuggestedParam({ suggestedParams, paramRoles, diagnostics, param, value, role = "structural", diagnostic = null }) {
		if (value == null) {
			if (diagnostic) diagnostics.push(diagnostic);
			return;
		}
		suggestedParams[param] = value;
		setParamRole(paramRoles, param, role);
	}
	function buildActionUsageModel({ descriptor, targetWidget, fieldRoles, spec, rows = [] }) {
		const suggestedParams = {};
		const diagnostics = [];
		const requiredParams = listRequiredParamNames(descriptor?.paramsSchema);
		const paramNames = listParamNames(descriptor?.paramsSchema);
		const paramRoles = Object.fromEntries(paramNames.map((param) => [param, requiredParams.includes(param) ? "intent" : "optional"]));
		const actionName = descriptor?.name || "";
		const encoding = readRootEncoding(spec);
		const lineContext = inferLineDimensionContext(spec, rows);
		const heatmapContext = inferHeatmapFieldContext(spec);
		const parallelDimensions = inferParallelDimensions(spec, rows);
		const fallbackCategoryField = inferPrimaryCategoryFieldFromRows(rows);
		const sankeyContext = inferSankeyContext(rows);
		if (actionName === "bar.sortBars") {
			const { categoryChannel, valueChannel } = detectBarChannels(encoding);
			const measureField = fieldRoles.resolved.measureField;
			const currentAggregate = encoding?.[valueChannel]?.aggregate || "mean";
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "channel",
				value: categoryChannel,
				diagnostic: "Could not resolve the categorical channel for bar.sortBars from the current encoding."
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "field",
				value: measureField,
				diagnostic: "Could not resolve the quantitative field for bar.sortBars from the current encoding."
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "aggregate",
				value: currentAggregate,
				role: "optional"
			});
			setParamRole(paramRoles, "bySubcategory", "optional");
		} else if (actionName === "bar.highlightTopN") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "categoryField",
				value: fieldRoles.resolved.categoryField,
				diagnostic: "Could not resolve categoryField for bar.highlightTopN from the current bar spec."
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "measureField",
				value: fieldRoles.resolved.measureField,
				diagnostic: "Could not resolve measureField for bar.highlightTopN from the current bar spec."
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "order",
				value: "descending",
				role: "optional"
			});
		} else if (actionName === "bar.filterCategories") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "field",
			value: fieldRoles.resolved.categoryField,
			diagnostic: "Could not resolve the categorical field for bar.filterCategories from the current bar spec."
		});
		else if (actionName === "scatter.brushRegion") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "xField",
				value: fieldRoles.resolved.xField,
				diagnostic: "Could not resolve xField for scatter.brushRegion from the current scatter spec."
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "yField",
				value: fieldRoles.resolved.yField,
				diagnostic: "Could not resolve yField for scatter.brushRegion from the current scatter spec."
			});
		} else if (actionName === "scatter.zoomDomain") {
			const xEncoding = encoding?.x || null;
			const yEncoding = encoding?.y || null;
			setParamRole(paramRoles, "xDomain", "intent");
			setParamRole(paramRoles, "yDomain", "intent");
			if (!isContinuousDomainEncoding(xEncoding)) diagnostics.push("The current scatter x channel is not continuous, so xDomain zoom may not be valid.");
			if (!isContinuousDomainEncoding(yEncoding)) diagnostics.push("The current scatter y channel is not continuous, so yDomain zoom may not be valid.");
			diagnostics.push("Provide at least one of xDomain or yDomain when calling scatter.zoomDomain.");
		} else if (actionName === "bar.selectCategory") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "field",
			value: fieldRoles.resolved.categoryField,
			diagnostic: "Could not resolve the categorical field for bar.selectCategory from the current bar spec."
		});
		else if (actionName === "bar.addBars" || actionName === "bar.removeBars") setSuggestedParam({
			suggestedParams,
			paramRoles,
			param: "field",
			value: fieldRoles.resolved.categoryField,
			diagnostic: `Could not resolve the categorical field for ${actionName} from the current bar spec.`
		});
		else if (actionName === "bar.addBarItems" || actionName === "bar.removeBarItems") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				param: "xField",
				value: fieldRoles.resolved.categoryField,
				diagnostic: `Could not resolve xField for ${actionName} from the current bar spec.`
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				param: "subField",
				value: fieldRoles.resolved.subCategoryField,
				diagnostic: `Could not resolve subField for ${actionName} from the current bar spec.`
			});
		} else if (actionName === "bar.filterSubcategories") setSuggestedParam({
			suggestedParams,
			paramRoles,
			param: "subField",
			value: fieldRoles.resolved.subCategoryField,
			diagnostic: "Could not resolve subField for bar.filterSubcategories from the current bar spec."
		});
		else if (actionName === "scatter.identifyClusters") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "method",
				value: "kmeans",
				role: "optional"
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "nClusters",
				value: 3,
				role: "optional"
			});
		} else if (actionName === "scatter.showRegression") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "method",
			value: "linear",
			role: "optional"
		});
		else if (actionName === "line.selectSeries") setSuggestedParam({
			suggestedParams,
			paramRoles,
			param: "field",
			value: lineContext.lineField,
			diagnostic: "Could not resolve field for line.selectSeries from the current line spec."
		});
		else if (actionName === "line.selectXValue") setSuggestedParam({
			suggestedParams,
			paramRoles,
			param: "field",
			value: lineContext.xField,
			diagnostic: "Could not resolve field for line.selectXValue from the current line spec."
		});
		else if (actionName === "line.zoomXRegion") {
			setParamRole(paramRoles, "start", "intent");
			setParamRole(paramRoles, "end", "intent");
		} else if (actionName === "line.focusLines") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				param: "lineField",
				value: lineContext.lineField,
				diagnostic: "Could not resolve lineField for line.focusLines from the current line spec."
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "dimOpacity",
				value: .08,
				role: "optional"
			});
		} else if (actionName === "line.highlightTrend") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "trendType",
			value: "increasing",
			role: "optional"
		});
		else if (actionName === "line.showMovingAverage") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "windowSize",
			value: 3,
			role: "optional"
		});
		else if (actionName === "line.drillDownXAxis") {
			setParamRole(paramRoles, "level", "intent");
			setParamRole(paramRoles, "value", "intent");
			setParamRole(paramRoles, "parent", "optional");
		} else if (actionName === "line.resampleXAxis") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "agg",
			value: "mean",
			role: "optional"
		});
		else if (actionName === "line.boldLines") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				param: "lineField",
				value: lineContext.lineField,
				diagnostic: "Could not resolve lineField for line.boldLines from the current line spec."
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "boldWidth",
				value: 4,
				role: "optional"
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "baseWidth",
				value: 1,
				role: "optional"
			});
		} else if (actionName === "line.filterLines") setSuggestedParam({
			suggestedParams,
			paramRoles,
			param: "lineField",
			value: lineContext.lineField,
			diagnostic: "Could not resolve lineField for line.filterLines from the current line spec."
		});
		else if (actionName === "heatmap.filterCells" || actionName === "heatmap.selectCell") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				param: "xField",
				value: heatmapContext.xField,
				diagnostic: `Could not resolve xField for ${actionName} from the current heatmap spec.`
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				param: "yField",
				value: heatmapContext.yField,
				diagnostic: `Could not resolve yField for ${actionName} from the current heatmap spec.`
			});
		} else if (actionName === "heatmap.selectSubmatrix" || actionName === "heatmap.highlightRegion") diagnostics.push(`Provide at least one of ${actionName === "heatmap.selectSubmatrix" ? "xValues or yValues" : "xValues or yValues"} when calling ${actionName}.`);
		else if (actionName === "heatmap.drilldownAxis") {
			setParamRole(paramRoles, "level", "intent");
			setParamRole(paramRoles, "value", "intent");
			setParamRole(paramRoles, "parent", "optional");
		} else if (actionName === "heatmap.addMarginalBars") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "op",
				value: "mean",
				role: "optional"
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "showTop",
				value: true,
				role: "optional"
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "showRight",
				value: true,
				role: "optional"
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "barSize",
				value: 70,
				role: "optional"
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "barColor",
				value: "#666666",
				role: "optional"
			});
		} else if (actionName === "heatmap.adjustColorScale") {
			setParamRole(paramRoles, "scheme", "intent");
			setParamRole(paramRoles, "domain", "optional");
		} else if (actionName === "heatmap.thresholdMask") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "outsideOpacity",
			value: .1,
			role: "optional"
		});
		else if (actionName === "heatmap.filterCellsByRegion") diagnostics.push("Provide at least one of xValue/xValues or yValue/yValues when calling heatmap.filterCellsByRegion.");
		else if (actionName === "heatmap.highlightRegionByValue") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "outsideOpacity",
				value: .12,
				role: "optional"
			});
			diagnostics.push("Provide at least one of minValue or maxValue when calling heatmap.highlightRegionByValue.");
		} else if (actionName === "heatmap.clusterRowsCols") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "clusterRows",
				value: true,
				role: "optional"
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "clusterCols",
				value: true,
				role: "optional"
			});
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "method",
				value: "sum",
				role: "optional"
			});
		} else if (actionName === "parallelCoordinates.selectRecord") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "field",
			value: "id",
			role: "structural"
		});
		else if (actionName === "parallelCoordinates.filterByCategory" || actionName === "parallelCoordinates.highlightCategory") setSuggestedParam({
			suggestedParams,
			paramRoles,
			param: "field",
			value: fallbackCategoryField,
			diagnostic: `Could not resolve field for ${actionName} from the current parallel-coordinates rows.`
		});
		else if (actionName === "parallelCoordinates.hideDimensions") {
			setSuggestedParam({
				suggestedParams,
				paramRoles,
				diagnostics,
				param: "mode",
				value: "hide",
				role: "optional"
			});
			if (parallelDimensions.length > 0) diagnostics.push(`Current visible dimensions: ${parallelDimensions.join(", ")}`);
		} else if (actionName === "sankey.focusFlow") setSuggestedParam({
			suggestedParams,
			paramRoles,
			param: "field",
			value: sankeyContext.field,
			diagnostic: "Could not resolve field for sankey.focusFlow from the current Sankey rows."
		});
		else if (actionName === "sankey.collapseNodes") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "aggregateName",
			value: "Other",
			role: "optional"
		});
		else if (actionName === "sankey.colorFlows") setSuggestedParam({
			suggestedParams,
			paramRoles,
			diagnostics,
			param: "color",
			value: "#e74c3c",
			role: "optional"
		});
		return {
			requiredParams,
			paramRoles,
			suggestedParams,
			recommendedCall: {
				name: descriptor?.name || null,
				targetRef: targetWidget?.ref || null,
				queryScope: targetWidget?.ref ? { widgetRef: targetWidget.ref } : void 0,
				params: {
					...Object.fromEntries(requiredParams.map((param) => [param, `<required:${param}>`])),
					...clone$12(suggestedParams)
				}
			},
			diagnostics
		};
	}
	function buildActionUsageEntry({ descriptor, targetWidget, fieldRoles, spec, rows, includeSchemas, includeExamples }) {
		const parameterResolution = buildActionUsageModel({
			descriptor,
			targetWidget,
			fieldRoles,
			spec,
			rows
		});
		return {
			name: descriptor?.name || null,
			title: descriptor?.title || null,
			description: descriptor?.description || null,
			primitive: descriptor?.primitive || null,
			category: descriptor?.category || null,
			targetRef: descriptor?.targetRef || null,
			supportedWidgetKinds: Array.isArray(descriptor?.supportedWidgetKinds) ? [...descriptor.supportedWidgetKinds] : null,
			requiredParams: parameterResolution.requiredParams,
			paramRoles: parameterResolution.paramRoles,
			suggestedParams: parameterResolution.suggestedParams,
			recommendedCall: parameterResolution.recommendedCall,
			diagnostics: parameterResolution.diagnostics,
			...includeSchemas ? { paramsSchema: clone$12(descriptor?.paramsSchema || null) } : {},
			...includeExamples ? { examples: clone$12(Array.isArray(descriptor?.examples) ? descriptor.examples : []) } : {},
			actionDescriptor: clone$12(descriptor || null)
		};
	}
	function resolveTargetWidget({ store, description, targetRef, widgetId, actionName }) {
		const explicitRef = normalizeString(targetRef);
		const explicitWidgetId = normalizeString(widgetId);
		if (explicitRef) return resolveWidgetRecordFromStore(store, explicitRef);
		const widgets = Array.isArray(description?.widgets) && description.widgets.length > 0 ? description.widgets : listStoreWidgets(store);
		if (explicitWidgetId) return widgets.find((entry) => entry?.widgetId === explicitWidgetId) || null;
		if (actionName) {
			const uniqueTargetRefs = uniqueStrings$1(listStoreActions(store).filter((descriptor) => descriptor?.name === actionName).map((descriptor) => descriptor?.targetRef || null));
			if (uniqueTargetRefs.length === 1) return resolveWidgetRecordFromStore(store, uniqueTargetRefs[0]);
		}
		return widgets.length === 1 ? widgets[0] : null;
	}
	function describeActionUsageSurface({ actionExecutor, options = {}, contextOverride = null } = {}) {
		const includeSchemas = options?.includeSchemas !== false;
		const includeExamples = options?.includeExamples !== false;
		const actionName = normalizeString(options?.actionName);
		const description = readWorkspaceDescriptionFromStore(actionExecutor?.store, { actionExecutor });
		const targetWidget = resolveTargetWidget({
			store: actionExecutor?.store,
			description,
			targetRef: options?.targetRef || options?.widgetRef || null,
			widgetId: options?.widgetId || null,
			actionName
		});
		const diagnostics = [];
		const syntheticCall = {
			name: actionName || "describeActionUsage",
			params: {},
			queryScope: targetWidget?.ref ? { widgetRef: targetWidget.ref } : {}
		};
		const ctx = typeof actionExecutor?.createContext === "function" ? actionExecutor.createContext(syntheticCall, null, contextOverride) : null;
		const spec = ctx?.readCurrentSpec?.() || null;
		const rowResult = targetWidget?.ref && typeof ctx?.readRowsForWidget === "function" ? ctx.readRowsForWidget(targetWidget.ref) : { rows: [] };
		const rows = Array.isArray(rowResult?.rows) ? rowResult.rows : [];
		const { fieldRoles } = inferFieldRoles({
			widgetKind: targetWidget?.kind || null,
			spec,
			rows
		});
		if (!targetWidget) diagnostics.push(actionName ? `Could not resolve a target widget for "${actionName}". Pass targetRef/widgetRef explicitly when multiple widgets are present.` : "Could not resolve a target widget. Pass targetRef/widgetRef explicitly when multiple widgets are present.");
		if (!spec) diagnostics.push("No current spec is available from the runtime host bridge, so field-role inference is limited.");
		const actions = (Array.isArray(description?.actions) && description.actions.length > 0 ? description.actions : listStoreActions(actionExecutor?.store, actionExecutor)).filter((descriptor) => {
			if (actionName && descriptor?.name !== actionName) return false;
			if (!targetWidget?.ref) return true;
			if (!descriptor?.targetRef) return true;
			return descriptor.targetRef === targetWidget.ref;
		}).map((descriptor) => buildActionUsageEntry({
			descriptor,
			targetWidget,
			fieldRoles,
			spec,
			rows,
			includeSchemas,
			includeExamples
		}));
		if (actionName && actions.length === 0) diagnostics.push(`No declared action descriptor matched "${actionName}" for the resolved widget scope.`);
		return {
			targetRef: targetWidget?.ref || null,
			widgetId: targetWidget?.widgetId || null,
			widgetKind: targetWidget?.kind || null,
			title: targetWidget?.title || null,
			diagnostics,
			specContext: buildSpecContext(spec, rows),
			fieldRoles,
			actions
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/ActionExecutor.js
	function buildActionError({ call, code, message, recoveryHints, details }) {
		return makeActionResult({
			ok: false,
			callId: call?.callId || `call_${Date.now()}`,
			actionName: call?.name || "unknown",
			error: makeResultError({
				code,
				message,
				...details !== void 0 ? { details } : {}
			}),
			...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {}
		});
	}
	function resolveRepresentativeActiveSelection(snapshot) {
		const activeSelections = readSelectionRegistry(snapshot?.shared || {});
		const activeSelectionEntries = Object.entries(activeSelections).filter(([, selectionState]) => Boolean(selectionState));
		if (activeSelectionEntries.length === 0) return [null, null];
		const focusedWidgetRef = snapshot?.shared?.focusedWidget || null;
		if (focusedWidgetRef) {
			const focusedSelectionRef = Object.keys(snapshot?.widgets?.[focusedWidgetRef]?.selections || {}).find((selectionRef) => activeSelections[selectionRef]);
			if (focusedSelectionRef) return [focusedSelectionRef, activeSelections[focusedSelectionRef]];
		}
		const primaryWidgetEntry = Object.entries(snapshot?.widgets || {}).find(([, widgetState]) => widgetState?.role === "primary");
		if (primaryWidgetEntry) {
			const [primaryWidgetRef] = primaryWidgetEntry;
			const primarySelectionRef = Object.keys(snapshot?.widgets?.[primaryWidgetRef]?.selections || {}).find((selectionRef) => activeSelections[selectionRef]);
			if (primarySelectionRef) return [primarySelectionRef, activeSelections[primarySelectionRef]];
		}
		if (activeSelectionEntries.length === 1) return activeSelectionEntries[0];
		return activeSelectionEntries[activeSelectionEntries.length - 1];
	}
	function selectionPayloadFromSnapshot(snapshot) {
		const [selectionRef, selectionState] = resolveRepresentativeActiveSelection(snapshot);
		if (!selectionRef || !selectionState) return null;
		const sourceWidget = Object.values(snapshot?.widgets || {}).find((widget) => widget?.selections?.[selectionRef]);
		return buildSelectionPayloadFromState({
			selectionRef,
			selectionState,
			sourceWidgetId: sourceWidget?.widgetId || void 0,
			selectedCount: sourceWidget?.data?.selectedCount || 0
		});
	}
	function selectionPayloadsFromSnapshot(snapshot) {
		const activeSelections = readSelectionRegistry(snapshot?.shared || {});
		const widgets = Object.values(snapshot?.widgets || {});
		return Object.fromEntries(Object.entries(activeSelections).map(([selectionRef, selectionState]) => {
			if (!selectionRef || !selectionState) return null;
			const sourceWidget = widgets.find((widget) => widget?.selections?.[selectionRef]) || null;
			return [selectionRef, buildSelectionPayloadFromState({
				selectionRef,
				selectionState,
				sourceWidgetId: sourceWidget?.widgetId || void 0,
				selectedCount: sourceWidget?.data?.selectedCount || 0
			})];
		}).filter(Boolean));
	}
	function primaryWidgetSpecFromSnapshot(snapshot) {
		const widgetEntries = Object.entries(snapshot?.widgets || {});
		if (widgetEntries.length === 0) return null;
		const focusedWidgetRef = snapshot?.shared?.focusedWidget;
		const focusedWidget = focusedWidgetRef ? snapshot.widgets?.[focusedWidgetRef] : null;
		if (focusedWidget?.rawSpec) return focusedWidget.rawSpec;
		const primaryWidget = widgetEntries.map(([, widgetState]) => widgetState).find((widgetState) => widgetState?.role === "primary" && widgetState?.rawSpec);
		if (primaryWidget?.rawSpec) return primaryWidget.rawSpec;
		return (widgetEntries[0]?.[1])?.rawSpec || null;
	}
	function isFilterTransformForField(transform, field) {
		return transform?.filter?.field === field;
	}
	function replaceFilterTransformForField(transforms, field, nextTransform) {
		return [...(Array.isArray(transforms) ? transforms : []).filter((transform) => !isFilterTransformForField(transform, field)), nextTransform];
	}
	function replaceAggregateTransforms(transforms, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !Array.isArray(transform?.aggregate));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function isActionHighlightTransformForField(transform, field) {
		return transform?.kind === "highlight" && transform?.source === "action:widget.highlightValues" && transform?.spec?.field === field;
	}
	function replaceActionHighlightTransformForField(transforms, field, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !isActionHighlightTransformForField(transform, field));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function resolvedWidgetRefForTarget(store, targetRef) {
		if (!targetRef) return null;
		return store?.getResolvedWidgetForTarget?.(targetRef)?.ref || store?.getResolvedWidget?.(targetRef)?.ref || null;
	}
	function uniqueValues$1(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => value != null))];
	}
	function markHighlightedRows(rows, field, values) {
		const highlightedValues = uniqueValues$1(values);
		return (Array.isArray(rows) ? rows : []).map((row) => ({
			...row,
			__widgetva_highlight: highlightedValues.includes(row?.[field])
		}));
	}
	function annotationsFromSnapshot(snapshot) {
		return Array.isArray(snapshot?.shared?.annotations) ? snapshot.shared.annotations : [];
	}
	function mergeUniqueRefs(...groups) {
		return Array.from(new Set(groups.flatMap((group) => Array.isArray(group) ? group : []).filter((ref) => typeof ref === "string" && ref.length > 0)));
	}
	function callRemovesSelection(output, descriptor) {
		if (output?.clearsSelection === true) return true;
		const actionName = descriptor?.name || null;
		return actionName === "widget.clearSelection" || actionName === "workspace.resetWorkspace" || actionName === "workspace.jumpToState" || actionName === "workspace.branchFromState";
	}
	function buildHandlerInput$1(call) {
		const params = buildScopedParams({
			params: call?.params || {},
			call
		});
		const queryScope = params?.queryScope || readNormalizedQueryScope({ call });
		const { targetRef: _legacyTargetRef, dataRef: _legacyDataRef, params: _legacyParams, queryScope: _legacyQueryScope, ...rawCall } = call && typeof call === "object" ? call : {};
		return {
			...params,
			...rawCall,
			...queryScope?.widgetRef ? { targetRef: queryScope.widgetRef } : {},
			params,
			queryScope
		};
	}
	function resolveActionTargetRef(call, descriptor = null) {
		return readNormalizedQueryScope({
			call,
			descriptor
		}).widgetRef || null;
	}
	function hasOwnParam(params, name) {
		return Boolean(params) && Object.prototype.hasOwnProperty.call(params, name);
	}
	function buildResolvedQueryScope(normalizedCall, usageSummary) {
		const currentScope = normalizedCall?.queryScope && typeof normalizedCall.queryScope === "object" ? { ...normalizedCall.queryScope } : {};
		if (!currentScope.widgetRef && typeof usageSummary?.targetRef === "string" && usageSummary.targetRef.length > 0) currentScope.widgetRef = usageSummary.targetRef;
		return currentScope;
	}
	function buildExecutableCall(call, usageSummary) {
		const normalizedCall = buildHandlerInput$1(call);
		const resolvedQueryScope = buildResolvedQueryScope(normalizedCall, usageSummary);
		const nextParams = { ...normalizedCall.params || {} };
		if (Object.keys(resolvedQueryScope).length > 0) nextParams.queryScope = resolvedQueryScope;
		return {
			...normalizedCall,
			...resolvedQueryScope?.widgetRef ? { targetRef: resolvedQueryScope.widgetRef } : {},
			params: nextParams,
			queryScope: resolvedQueryScope
		};
	}
	function buildInvalidParamsRecoveryHints({ call, usageSummary, usageEntry, executableCall }) {
		const hints = [];
		const params = executableCall?.params || {};
		const missingParams = (usageEntry?.requiredParams || []).filter((param) => !hasOwnParam(params, param));
		if (missingParams.length > 0) hints.push(`Provide the required params for ${call?.name || "this action"}: ${missingParams.join(", ")}.`);
		if (usageEntry && Object.keys(usageEntry.suggestedParams || {}).length > 0) hints.push(`The runtime suggests these params, but does not inject them automatically: ${Object.keys(usageEntry.suggestedParams).join(", ")}.`);
		if (typeof usageSummary?.targetRef === "string" && usageSummary.targetRef.length > 0 && call?.name) hints.push(`Call describeActionUsage({ targetRef: "${usageSummary.targetRef}", actionName: "${call.name}" }) to inspect recommended params.`);
		return hints;
	}
	var ActionExecutor = class {
		constructor({ store, sync, hostBridge, getState, getAppState, subscribe, linkEngine, traceRecorder } = {}) {
			this.store = store || { listActions() {
				return [];
			} };
			this.sync = typeof sync === "function" ? sync : () => {};
			this.hostBridge = hostBridge || createWidgetVAHostBridge({
				getState,
				getAppState,
				subscribe
			}) || createDefaultWidgetVAHostBridge();
			this.linkEngine = linkEngine || null;
			this.traceRecorder = traceRecorder || null;
			this.builtinDescriptors = /* @__PURE__ */ new Map();
			this.handlers = /* @__PURE__ */ new Map();
			this.preconditionHandlers = /* @__PURE__ */ new Map();
			this.registerBuiltinPreconditions();
			this.registerBuiltinHandlers();
		}
		register(descriptor, handler) {
			if (!descriptor?.name || typeof handler !== "function") throw new Error("ActionExecutor.register requires a descriptor.name and handler.");
			if (this.handlers.has(descriptor.name)) throw new Error(`Duplicate action handler registration: ${descriptor.name}`);
			this.builtinDescriptors.set(descriptor.name, descriptor);
			this.handlers.set(descriptor.name, handler);
		}
		list() {
			const descriptors = this.store?.listActions?.() || [];
			if (descriptors.length > 0) return descriptors.filter((descriptor) => this.handlers.has(descriptor.name));
			return Array.from(this.builtinDescriptors.values());
		}
		has(name) {
			return this.handlers.has(name);
		}
		describeExecutor() {
			const actions = this.list().map((descriptor) => makeActionExecutorActionEntry({
				name: descriptor.name,
				primitive: descriptor.primitive || null,
				category: descriptor.category || null,
				targetRef: descriptor.targetRef || null,
				affectedRefs: Array.isArray(descriptor.affectedRefs) ? [...descriptor.affectedRefs] : [],
				affectedStatePaths: Array.isArray(descriptor.affectedStatePaths) ? [...descriptor.affectedStatePaths] : [],
				supportedWidgetKinds: Array.isArray(descriptor.supportedWidgetKinds) ? [...descriptor.supportedWidgetKinds] : null,
				hasPreconditions: Array.isArray(descriptor.preconditions) && descriptor.preconditions.length > 0,
				preconditionDescriptorCount: Array.isArray(descriptor.preconditions) ? descriptor.preconditions.length : 0,
				preconditionHandlerRegistered: this.preconditionHandlers.has(descriptor.name),
				postconditionCount: Array.isArray(descriptor.postconditions) ? descriptor.postconditions.length : 0,
				reversible: descriptor.reversible === true,
				effectCount: Array.isArray(descriptor.effects) ? descriptor.effects.length : 0,
				effectKinds: Array.isArray(descriptor.effects) ? [...new Set(descriptor.effects.map((effect) => effect?.kind).filter((kind) => typeof kind === "string" && kind.length > 0))] : []
			}));
			return makeActionExecutorSummary({
				counts: makeActionExecutorCounts({
					descriptorCount: actions.length,
					handlerCount: this.handlers.size,
					preconditionCount: this.preconditionHandlers.size
				}),
				capabilities: makeActionExecutorCapabilities({
					paramsValidation: true,
					preconditionValidation: true,
					stateSync: typeof this.sync === "function",
					traceRecording: typeof this.traceRecorder?.recordAction === "function",
					linkPropagation: Boolean(this.linkEngine)
				}),
				actions
			});
		}
		describeContext() {
			return ActionContext.describeContract();
		}
		describeActionUsage(options = {}, contextOverride = null) {
			return describeActionUsageSurface({
				actionExecutor: this,
				options,
				contextOverride
			});
		}
		registerPrecondition(name, handler) {
			if (!name || typeof handler !== "function") throw new Error("ActionExecutor.registerPrecondition requires an action name and handler.");
			this.preconditionHandlers.set(name, handler);
		}
		findCompatibleDescriptor(actionName, targetRef, storeOverride = null) {
			const store = storeOverride || this.store;
			if (!actionName || !targetRef) return null;
			const exactDescriptor = store?.getActionDescriptor?.(actionName, targetRef);
			if (exactDescriptor) return exactDescriptor;
			const targetWidgetRef = resolvedWidgetRefForTarget(store, targetRef);
			if (!targetWidgetRef) return null;
			return (store?.listActions?.() || []).filter((descriptor) => descriptor?.name === actionName).find((descriptor) => {
				const descriptorTargetRef = descriptor?.targetRef || null;
				if (!descriptorTargetRef) return false;
				return resolvedWidgetRefForTarget(store, descriptorTargetRef) === targetWidgetRef;
			}) || null;
		}
		resolveDescriptor(call, storeOverride = null) {
			return this.findCompatibleDescriptor(call?.name, resolveActionTargetRef(call), storeOverride) || this.builtinDescriptors.get(call?.name) || null;
		}
		createContext(call, descriptor, contextOverride = null) {
			const normalizedCall = buildHandlerInput$1(call);
			if (contextOverride instanceof ActionContext) {
				contextOverride.descriptor = descriptor;
				contextOverride.call = normalizedCall;
				return contextOverride;
			}
			const store = contextOverride?.store || this.store;
			const hasStateAccessOverride = typeof contextOverride?.getState === "function" || typeof contextOverride?.getAppState === "function" || typeof contextOverride?.subscribe === "function" || typeof contextOverride?.subscribeAppState === "function";
			const overrideHostBridge = contextOverride?.hostBridge || (hasStateAccessOverride ? {
				...this.hostBridge,
				...createWidgetVAHostBridge({
					getState: contextOverride?.getState,
					getAppState: contextOverride?.getAppState,
					subscribe: contextOverride?.subscribe,
					subscribeAppState: contextOverride?.subscribeAppState
				})
			} : null);
			const ctx = new ActionContext({
				store,
				descriptor,
				call: normalizedCall,
				sync: contextOverride?.syncWorkspace || contextOverride?.sync || this.sync || (() => {}),
				hostBridge: overrideHostBridge || this.hostBridge,
				getAppState: contextOverride?.getAppState,
				linkEngine: contextOverride?.linkEngine || this.linkEngine || null,
				helpers: {
					primaryWidgetSpecFromSnapshot,
					selectionPayloadFromSnapshot,
					selectionPayloadsFromSnapshot,
					annotationsFromSnapshot
				}
			});
			if (typeof contextOverride?.patchWidget === "function") ctx.patchWidget = contextOverride.patchWidget.bind(contextOverride);
			if (typeof contextOverride?.propagate === "function") ctx.propagate = contextOverride.propagate.bind(contextOverride);
			if (typeof contextOverride?.readStatePatch === "function") ctx.readStatePatch = contextOverride.readStatePatch.bind(contextOverride);
			if (typeof contextOverride?.readCurrentState === "function") ctx.readCurrentState = contextOverride.readCurrentState.bind(contextOverride);
			if (typeof contextOverride?.readDescription === "function") ctx.readDescription = contextOverride.readDescription.bind(contextOverride);
			if (typeof contextOverride?.clearSelection === "function") ctx.clearSelection = contextOverride.clearSelection.bind(contextOverride);
			return ctx;
		}
		async checkPreconditions(call, descriptor, ctx) {
			const handler = this.preconditionHandlers.get(call?.name);
			if (!handler) return;
			const outcome = await handler(buildHandlerInput$1(call), ctx, descriptor);
			if (outcome == null || outcome === true) return;
			if (outcome === false) throwActionPrecondition(descriptor?.preconditions?.[0]?.failureMessage || `Preconditions failed for action ${call?.name || "unknown"}.`);
			if (typeof outcome === "string") throwActionPrecondition(outcome);
			if (typeof outcome === "object" && outcome.ok === false) throwActionPrecondition(outcome.message || descriptor?.preconditions?.[0]?.failureMessage || `Preconditions failed for action ${call?.name || "unknown"}.`, outcome.details, outcome.recoveryHints);
		}
		async run(call, contextOverride = null) {
			const effectiveStore = contextOverride?.store || this.store || null;
			const traceRecorder = contextOverride?.traceRecorder || this.traceRecorder || null;
			const resolvedDescriptor = this.resolveDescriptor(call, effectiveStore);
			const targetRef = resolveActionTargetRef(call, resolvedDescriptor);
			const storeDescriptor = this.findCompatibleDescriptor(call?.name, targetRef, effectiveStore);
			const descriptor = storeDescriptor || resolvedDescriptor;
			const storeHasActionSurface = (effectiveStore?.listActions?.() || []).length > 0;
			const handler = this.handlers.get(call?.name);
			if (!handler) {
				const result = buildActionError({
					call,
					code: "UNKNOWN_OPERATION",
					message: `Unsupported WidgetVA action: ${call?.name || "unknown"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect currently supported actions."]
				});
				traceRecorder?.recordActionFailure?.({
					call,
					code: result.error.code,
					message: result.error.message,
					recoveryHints: result.recoveryHints
				});
				return result;
			}
			if (storeHasActionSurface && targetRef && !storeDescriptor && !this.builtinDescriptors.has(call?.name)) {
				const result = buildActionError({
					call,
					code: "UNSUPPORTED_TARGET",
					message: `Action ${call?.name || "unknown"} is not declared for target ${targetRef}.`,
					recoveryHints: ["Call describeWorkspace() to inspect which actions are declared on the target widget."]
				});
				traceRecorder?.recordActionFailure?.({
					call,
					primitive: descriptor?.primitive || null,
					code: result.error.code,
					message: result.error.message,
					recoveryHints: result.recoveryHints
				});
				return result;
			}
			const usageSummary = this.describeActionUsage({
				targetRef,
				actionName: call?.name || null,
				includeSchemas: false,
				includeExamples: false
			}, contextOverride);
			const usageEntry = Array.isArray(usageSummary?.actions) ? usageSummary.actions.find((entry) => entry?.name === call?.name) || null : null;
			const executableCall = buildExecutableCall(call, usageSummary);
			if (descriptor?.paramsSchema) try {
				validateAgainstSchema(executableCall.params || {}, descriptor.paramsSchema, "params");
			} catch (error) {
				const result = buildActionError({
					call: executableCall,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: buildInvalidParamsRecoveryHints({
						call,
						usageSummary,
						usageEntry,
						executableCall
					}),
					details: {
						targetRef: usageSummary?.targetRef || targetRef || null,
						requiredParams: usageEntry?.requiredParams || [],
						suggestedParams: usageEntry?.suggestedParams || {},
						diagnostics: usageEntry?.diagnostics || usageSummary?.diagnostics || []
					}
				});
				traceRecorder?.recordActionFailure?.({
					call: executableCall,
					primitive: descriptor?.primitive || null,
					code: result.error.code,
					message: result.error.message,
					recoveryHints: result.recoveryHints
				});
				return result;
			}
			try {
				const ctx = this.createContext(executableCall, descriptor, contextOverride);
				const previousState = ctx.readCurrentState();
				await this.checkPreconditions(executableCall, descriptor, ctx);
				const rawOutput = await handler(buildHandlerInput$1(executableCall), ctx);
				const baseNextState = rawOutput?.nextState || ctx.readCurrentState();
				const output = this.enrichActionOutput({
					output: rawOutput,
					descriptor,
					ctx,
					previousState,
					nextState: baseNextState
				});
				const nextState = output?.nextState || baseNextState;
				const updatedRefs = Array.isArray(output?.updatedRefs) ? output.updatedRefs : ctx.collectUpdatedRefs(nextState, output?.extraRefs || []);
				const statePatch = output?.statePatch || ctx.readStatePatch(updatedRefs);
				const primitive = descriptor?.primitive || null;
				const verificationPayload = buildActionVerificationPayload({
					descriptor,
					updatedRefs,
					verificationHints: output?.verificationHints
				});
				traceRecorder?.recordAction?.({
					call: executableCall,
					updatedRefs,
					stateId: nextState.stateId,
					statePatch,
					primitive,
					notes: {
						...output?.notes || {},
						...typeof verificationPayload.verificationNote === "string" && verificationPayload.verificationNote.length > 0 ? { verification: verificationPayload.verificationNote } : {},
						...verificationPayload.verificationHints.length > 0 ? { verificationHints: verificationPayload.verificationHints } : {}
					}
				});
				if (output?.transition && typeof traceRecorder?.recordSystemTransition === "function") traceRecorder.recordSystemTransition({
					actor: call?.actor || "agent",
					transitionType: output.transition.type || "continue",
					stateId: nextState.stateId,
					affectedRefs: updatedRefs,
					statePatch,
					notes: output.transition.notes
				});
				return makeActionResult({
					ok: true,
					callId: call?.callId || `call_${Date.now()}`,
					actionName: call?.name || "unknown",
					updatedRefs,
					stateId: nextState.stateId,
					statePatch,
					result: output?.result,
					expectedPostconditions: verificationPayload.expectedPostconditions,
					verificationHints: verificationPayload.verificationHints.length > 0 ? verificationPayload.verificationHints : ["Call readState({ refs: updatedRefs }) to verify the state update.", "Call a perception query if the task requires numerical or aggregate evidence."]
				});
			} catch (error) {
				if (isActionPreconditionError(error)) {
					const result = buildActionError({
						call,
						code: "PRECONDITION_FAILED",
						message: error.message,
						details: error.details,
						recoveryHints: error.recoveryHints
					});
					traceRecorder?.recordActionFailure?.({
						call,
						primitive: descriptor?.primitive || null,
						code: result.error.code,
						message: result.error.message,
						details: result.error.details,
						recoveryHints: result.recoveryHints
					});
					return result;
				}
				const result = buildActionError({
					call,
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error)
				});
				traceRecorder?.recordActionFailure?.({
					call,
					primitive: descriptor?.primitive || null,
					code: result.error.code,
					message: result.error.message
				});
				return result;
			}
		}
		enrichActionOutput({ output, descriptor, ctx, previousState, nextState }) {
			if (!output || typeof output !== "object") return output;
			const selectionResolveOptions = {
				targetRef: resolveActionTargetRef(ctx?.call || null, descriptor),
				kind: descriptor?.supportedWidgetKinds?.[0] || null
			};
			const previousSelectionRef = ctx.resolveSelectionRef(previousState, selectionResolveOptions);
			const nextSelectionRef = ctx.resolveSelectionRef(nextState, selectionResolveOptions);
			const shouldPropagateSelection = Boolean(output.propagateFromSelection) || descriptor?.primitive === "select" && !Array.isArray(output.updatedRefs);
			const propagateFromRef = output.propagateFromRef || (shouldPropagateSelection ? nextSelectionRef : null) || (previousSelectionRef && !nextSelectionRef && (descriptor?.primitive === "select" || descriptor?.primitive === "reset" || descriptor?.primitive === "undo" || callRemovesSelection(output, descriptor)) ? previousSelectionRef : null);
			if (!propagateFromRef) return output;
			const propagation = ctx.collectPropagation(propagateFromRef, { state: nextState });
			return {
				...output,
				nextState: propagation.nextState || nextState,
				updatedRefs: mergeUniqueRefs(Array.isArray(output.updatedRefs) ? output.updatedRefs : ctx.collectUpdatedRefs(nextState, output?.extraRefs || []), propagation.refs),
				result: {
					...output.result || {},
					...output.result?.propagated ? {} : { propagated: propagation.links },
					...output.result?.propagationEffects ? {} : { propagationEffects: propagation.effects }
				}
			};
		}
		registerBuiltinPreconditions() {
			this.registerPrecondition("widget.undoSelection", (call, ctx) => {
				if (!ctx.hasUndoSelectionHistory()) return {
					ok: false,
					message: "No prior selection state is available to restore.",
					recoveryHints: ["Run a selection action first so there is history to restore.", "Call readState() or readTrace() to confirm whether any prior selection exists."]
				};
				return { ok: true };
			});
			this.registerPrecondition("widget.redoSelection", (call, ctx) => {
				if (!ctx.hasRedoSelectionHistory()) return {
					ok: false,
					message: "No redo selection state is available to restore.",
					recoveryHints: ["Run widget.undoSelection first so there is a redo selection state to restore.", "Avoid making a new selection before redo, because new selections clear redo history."]
				};
				return { ok: true };
			});
			this.registerPrecondition("workspace.jumpToState", (call, ctx) => {
				const targetStateId = call?.params?.stateId;
				if (!(targetStateId ? ctx.readSnapshot(targetStateId) : null)) return {
					ok: false,
					message: "The requested stateId is not available in runtime history.",
					details: { stateId: targetStateId || null },
					recoveryHints: ["Call listStateHistory() to inspect available stateIds.", "Choose a valid historical state before retrying jumpToState."]
				};
				return { ok: true };
			});
			this.registerPrecondition("workspace.branchFromState", (call, ctx) => {
				const targetStateId = call?.params?.stateId;
				if (!(targetStateId ? ctx.readSnapshot(targetStateId) : null)) return {
					ok: false,
					message: "The requested branch origin stateId is not available.",
					details: { stateId: targetStateId || null },
					recoveryHints: ["Call listStateHistory() to inspect available branch origin stateIds.", "Retry branchFromState with a valid historical state."]
				};
				return { ok: true };
			});
			this.registerPrecondition("table.focusRows", (call, ctx) => {
				const targetWidgetCheck = this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "table",
					message: "table.focusRows requires a valid target table widget.",
					recoveryHints: ["Call describeWorkspace() to inspect valid table widget refs.", "Retry table.focusRows with a targetRef that resolves to a table widget."]
				});
				if (targetWidgetCheck?.ok === false) return targetWidgetCheck;
				const params = call?.params || {};
				if (typeof params.keyField === "string" && params.keyField.trim()) return { ok: true };
				const targetWidget = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					kind: "table",
					message: "table.focusRows requires a valid target table widget."
				});
				const widgetState = targetWidget?.ref ? ctx.readCurrentState({ refs: [targetWidget.ref] })?.widgets?.[targetWidget.ref] : null;
				if ((Array.isArray(widgetState?.rawSpec?.columns) ? widgetState.rawSpec.columns : []).length === 0) return {
					ok: false,
					message: "No usable key field is available for row focus.",
					details: { targetRef: targetWidget?.ref || null },
					recoveryHints: ["Provide an explicit keyField in the action params if the table has one.", "Inspect the table schema to confirm whether a stable row identity field exists."]
				};
				return { ok: true };
			});
			this.registerPrecondition("scatter.brushRegion", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "scatter",
					message: "scatter.brushRegion requires a valid scatter target widget."
				});
			});
			this.registerPrecondition("bar.selectCategory", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "bar",
					message: "bar.selectCategory requires a valid bar target widget."
				});
			});
			this.registerPrecondition("bar.highlightTopN", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "bar",
					message: "bar.highlightTopN requires a valid bar target widget."
				});
			});
			this.registerPrecondition("bar.filterCategories", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "bar",
					message: "bar.filterCategories requires a valid bar target widget."
				});
			});
			this.registerPrecondition("bar.filterSubcategories", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "bar",
					message: "bar.filterSubcategories requires a valid bar target widget."
				});
			});
			this.registerPrecondition("bar.expandStack", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "bar",
					message: "bar.expandStack requires a valid bar target widget."
				});
			});
			this.registerPrecondition("bar.toggleStackMode", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "bar",
					message: "bar.toggleStackMode requires a valid bar target widget."
				});
			});
			this.registerPrecondition("line.selectSeries", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "line",
					message: "line.selectSeries requires a valid line target widget."
				});
			});
			this.registerPrecondition("line.zoomXRegion", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "line",
					message: "line.zoomXRegion requires a valid line target widget."
				});
			});
			this.registerPrecondition("line.focusLines", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "line",
					message: "line.focusLines requires a valid line target widget."
				});
			});
			this.registerPrecondition("line.filterLines", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "line",
					message: "line.filterLines requires a valid line target widget."
				});
			});
			this.registerPrecondition("heatmap.selectCell", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "heatmap",
					message: "heatmap.selectCell requires a valid heatmap target widget."
				});
			});
			this.registerPrecondition("heatmap.highlightRegion", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "heatmap",
					message: "heatmap.highlightRegion requires a valid heatmap target widget."
				});
			});
			this.registerPrecondition("heatmap.adjustColorScale", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "heatmap",
					message: "heatmap.adjustColorScale requires a valid heatmap target widget."
				});
			});
			this.registerPrecondition("heatmap.thresholdMask", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "heatmap",
					message: "heatmap.thresholdMask requires a valid heatmap target widget."
				});
			});
			this.registerPrecondition("heatmap.filterCellsByRegion", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "heatmap",
					message: "heatmap.filterCellsByRegion requires a valid heatmap target widget."
				});
			});
			this.registerPrecondition("heatmap.highlightRegionByValue", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "heatmap",
					message: "heatmap.highlightRegionByValue requires a valid heatmap target widget."
				});
			});
			this.registerPrecondition("heatmap.transpose", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "heatmap",
					message: "heatmap.transpose requires a valid heatmap target widget."
				});
			});
			this.registerPrecondition("map.selectRegion", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "map",
					message: "map.selectRegion requires a valid map target widget."
				});
			});
			this.registerPrecondition("sankey.focusFlow", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "sankey",
					message: "sankey.focusFlow requires a valid sankey target widget."
				});
			});
			this.registerPrecondition("parallelCoordinates.brushAxes", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					kind: "parallelCoordinates",
					message: "parallelCoordinates.brushAxes requires a valid parallel coordinates target widget."
				});
			});
			this.registerPrecondition("widget.highlightValues", (call, ctx) => {
				const targetWidgetCheck = this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "widget.highlightValues requires a valid target widget."
				});
				if (targetWidgetCheck?.ok === false) return targetWidgetCheck;
				return this.readBaseSpecPrecondition("highlight updates", ctx);
			});
			this.registerPrecondition("widget.aggregateData", (call, ctx) => {
				const targetWidgetCheck = this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "widget.aggregateData requires a valid target widget."
				});
				if (targetWidgetCheck?.ok === false) return targetWidgetCheck;
				return this.readBaseSpecPrecondition("aggregate updates", ctx);
			});
			this.registerPrecondition("widget.filterByRange", (call, ctx) => {
				const targetWidgetCheck = this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "widget.filterByRange requires a valid target widget."
				});
				if (targetWidgetCheck?.ok === false) return targetWidgetCheck;
				return this.readBaseSpecPrecondition("filter updates", ctx);
			});
			this.registerPrecondition("widget.sortEncoding", (call, ctx) => {
				const targetWidgetCheck = this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "widget.sortEncoding requires a valid target widget."
				});
				if (targetWidgetCheck?.ok === false) return targetWidgetCheck;
				const baseSpecCheck = this.readBaseSpecPrecondition("sort updates", ctx);
				if (baseSpecCheck?.ok === false) return baseSpecCheck;
				const channel = typeof call?.params?.channel === "string" ? call.params.channel : null;
				const spec = ctx.readCurrentSpec();
				const currentChannel = channel ? spec?.encoding?.[channel] : null;
				if (!currentChannel || typeof currentChannel !== "object") return {
					ok: false,
					message: `The active spec does not define encoding channel "${channel || "unknown"}".`,
					details: { channel: channel || null }
				};
				return { ok: true };
			});
			this.registerPrecondition("widget.changeEncoding", (call, ctx) => {
				const targetWidgetCheck = this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "widget.changeEncoding requires a valid target widget."
				});
				if (targetWidgetCheck?.ok === false) return targetWidgetCheck;
				return this.readBaseSpecPrecondition("encoding updates", ctx);
			});
			this.registerPrecondition("widget.zoomDomain", (call, ctx) => {
				const targetWidgetCheck = this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "widget.zoomDomain requires a valid target widget."
				});
				if (targetWidgetCheck?.ok === false) return targetWidgetCheck;
				const baseSpecCheck = this.readBaseSpecPrecondition("domain updates", ctx);
				if (baseSpecCheck?.ok === false) return baseSpecCheck;
				const spec = ctx.readCurrentSpec();
				const hasXDomain = Array.isArray(call?.params?.xDomain);
				const hasYDomain = Array.isArray(call?.params?.yDomain);
				if (hasXDomain && !spec?.encoding?.x) return {
					ok: false,
					message: "The active spec does not define the requested zoom domain channel.",
					details: { channel: "x" }
				};
				if (hasYDomain && !spec?.encoding?.y) return {
					ok: false,
					message: "The active spec does not define the requested zoom domain channel.",
					details: { channel: "y" }
				};
				return { ok: true };
			});
			this.registerPrecondition("workspace.focusWidget", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "workspace.focusWidget requires a valid target widget."
				});
			});
			this.registerPrecondition("workspace.addAnnotation", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					allowMissing: true,
					message: "workspace.addAnnotation targetRef must resolve to a valid widget when provided.",
					recoveryHints: ["Omit targetRef to create a workspace-level annotation, or use a valid widget ref from describeWorkspace()."]
				});
			});
			this.registerPrecondition("widget.filterByValues", (call, ctx) => {
				const targetWidgetCheck = this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "widget.filterByValues requires a valid target widget."
				});
				if (targetWidgetCheck?.ok === false) return targetWidgetCheck;
				return this.readBaseSpecPrecondition("filter updates", ctx);
			});
		}
		readBaseSpecPrecondition(operationName, ctx) {
			const spec = ctx?.readCurrentSpec?.();
			if (!spec || typeof spec !== "object" || Array.isArray(spec)) return {
				ok: false,
				message: `No active base spec is available for ${operationName}.`,
				recoveryHints: ["Retry the action on a base-spec widget rather than a derived or linked view.", "Call describeWorkspace() and inspect supportsSpecMutation/sourceKind before retrying."]
			};
			return { ok: true };
		}
		readTargetWidgetPrecondition({ call, ctx, kind = null, allowMissing = false, message = null, recoveryHints = null } = {}) {
			const targetRef = resolveActionTargetRef(call);
			if (!targetRef && allowMissing) return { ok: true };
			if (!(ctx?.resolveTargetWidget?.({
				targetRef,
				...kind ? { kind } : {}
			}) || null)?.ref) return {
				ok: false,
				message: message || "The requested target widget is not available in the current workspace.",
				details: {
					targetRef: targetRef || null,
					kind
				},
				recoveryHints: Array.isArray(recoveryHints) && recoveryHints.length > 0 ? recoveryHints : ["Call describeWorkspace() to inspect valid widget refs before retrying.", "Retry the action with a targetRef that resolves to a materialized widget."]
			};
			return { ok: true };
		}
		resolveTargetWidgetRefs(requestedRefs, ctx) {
			const seen = /* @__PURE__ */ new Set();
			return requestedRefs.map((ref) => ctx?.resolveTargetWidget?.({ targetRef: ref })?.ref || null).filter((ref) => {
				if (!ref || seen.has(ref)) return false;
				seen.add(ref);
				return true;
			});
		}
		registerBuiltinHandlers() {
			this.register({ name: "widget.updateSelection" }, async (call, ctx) => {
				const params = call?.params || {};
				const selectionType = typeof params.selection_type === "string" ? params.selection_type : null;
				if (!selectionType) throw new Error("widget.updateSelection requires a selection_type.");
				const targetWidget = ctx.resolveTargetWidget({ targetRef: resolveActionTargetRef(call) });
				const selection = {
					selection_id: typeof params.selection_id === "string" ? params.selection_id : `sel_${Date.now()}`,
					source_widget_id: typeof params.source_widget_id === "string" ? params.source_widget_id : targetWidget?.widgetId || void 0,
					selection_type: selectionType,
					...params.domain && typeof params.domain === "object" ? { domain: params.domain } : {},
					...Array.isArray(params.fields) ? { fields: params.fields } : {},
					...params.value && typeof params.value === "object" && !Array.isArray(params.value) ? { value: params.value } : {},
					...typeof params.keyField === "string" ? { keyField: params.keyField } : {},
					...Array.isArray(params.keys) ? { keys: params.keys } : {},
					...typeof params.field === "string" ? { field: params.field } : {},
					...Array.isArray(params.values) ? { values: params.values } : {},
					predicates: Array.isArray(params.predicates) ? params.predicates : [],
					count: Number.isFinite(params.count) ? params.count : 0,
					summary: typeof params.summary === "string" ? params.summary : ""
				};
				if (targetWidget?.ref) ctx.setFocusedWidgetRef(targetWidget.ref);
				return {
					nextState: ctx.commitSelection(selection),
					propagateFromSelection: true,
					result: {
						selection,
						widgetId: targetWidget?.widgetId || null
					},
					verificationHints: ["Read the widget selection state and confirm it matches the supplied runtime selection payload.", "Read linked widgets to confirm propagation follows the updated selection."],
					notes: { userVisibleSummary: selection.summary || "Human-driven selection was applied through the shared action pipeline." }
				};
			});
			this.register({ name: "widget.clearSelection" }, async (call, ctx) => {
				const targetWidget = ctx.resolveTargetWidget({ targetRef: resolveActionTargetRef(call) });
				const targetAdapter = targetWidget?.ref ? ctx.readWidgetAdapter(targetWidget.ref) : null;
				if (typeof targetAdapter?.applySelectionClear === "function") await Promise.resolve(targetAdapter.applySelectionClear(call?.params || {}));
				return {
					nextState: ctx.clearSelection(),
					result: { cleared: true },
					verificationHints: ["Read the widget state and confirm there is no active selection."]
				};
			});
			this.register({ name: "widget.undoSelection" }, async (call, ctx) => ({
				nextState: ctx.undoSelection(),
				result: { restored: true },
				verificationHints: ["Read the widget state and confirm the previous selection was restored."],
				transition: {
					type: "undo",
					notes: { userVisibleSummary: "Selection history was rolled back to the prior state." }
				}
			}));
			this.register({ name: "widget.redoSelection" }, async (call, ctx) => ({
				nextState: ctx.redoSelection(),
				result: { restored: true },
				verificationHints: ["Read the widget state and confirm the selection matches the next recorded selection state."],
				notes: { userVisibleSummary: "Selection history was replayed to the next state." },
				transition: {
					type: "redo",
					notes: { userVisibleSummary: "Selection history was replayed to the next state." }
				}
			}));
			this.register({ name: "workspace.resetWorkspace" }, async (call, ctx) => ({
				nextState: ctx.resetWorkspace(),
				result: { reset: true },
				verificationHints: ["Read the workspace state and confirm selections and derived interaction state are cleared."],
				transition: {
					type: "reset",
					notes: { userVisibleSummary: "Workspace interaction state was reset to its baseline." }
				}
			}));
			this.register({ name: "workspace.jumpToState" }, async (call, ctx) => {
				const targetStateId = call?.params?.stateId;
				const snapshot = targetStateId ? ctx.readSnapshotEntry(targetStateId) : null;
				if (typeof this.store?.beginTransition === "function") this.store.beginTransition({
					transitionType: "jump_back",
					parentStateId: targetStateId
				});
				return {
					nextState: ctx.restoreSnapshotState(snapshot),
					result: { restoredStateId: targetStateId },
					verificationHints: ["Read the workspace state and confirm the restored interaction state matches the target snapshot."],
					transition: {
						type: "jump_back",
						notes: { userVisibleSummary: `Jumped workspace state back to ${targetStateId}.` }
					}
				};
			});
			this.register({ name: "workspace.branchFromState" }, async (call, ctx) => {
				const targetStateId = call?.params?.stateId;
				const snapshot = targetStateId ? ctx.readSnapshotEntry(targetStateId) : null;
				const branch = beginBranchFromStateInStore(this.store, {
					stateId: targetStateId,
					label: call?.params?.branchLabel
				});
				return {
					nextState: ctx.restoreSnapshotState(snapshot),
					result: {
						restoredStateId: targetStateId,
						branchId: branch.branchId,
						branchLabel: branch.label
					},
					verificationHints: ["Read the workspace state and confirm the restored interaction state matches the branch origin snapshot.", "Read trace graph or state history to confirm a new branch was created."],
					transition: {
						type: "branch",
						notes: { userVisibleSummary: `Created branch ${branch.label} from ${targetStateId}.` }
					}
				};
			});
			this.register({ name: "workspace.focusWidget" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					message: "workspace.focusWidget requires a valid target widget."
				});
				const previousFocusedWidgetRef = ctx.readCurrentState()?.shared?.focusedWidget || null;
				return {
					nextState: ctx.setFocusedWidgetRef(targetWidget.ref),
					updatedRefs: mergeUniqueRefs([previousFocusedWidgetRef], [targetWidget.ref]),
					result: {
						focusedWidgetRef: targetWidget.ref,
						widgetId: targetWidget.widgetId
					},
					verificationHints: ["Read the workspace state and confirm shared.focusedWidget matches the requested target."],
					notes: { userVisibleSummary: `Focused widget set to ${targetWidget.widgetId}.` }
				};
			});
			this.register({ name: "workspace.addAnnotation" }, async (call, ctx) => {
				const params = call?.params || {};
				const text = typeof params.text === "string" ? params.text.trim() : "";
				const targetRef = resolveActionTargetRef(call);
				const targetWidget = targetRef ? ctx.requireTargetWidget({
					targetRef,
					message: "workspace.addAnnotation targetRef must resolve to a valid widget when provided."
				}) : null;
				if (!text) throw new Error("workspace.addAnnotation requires non-empty annotation text.");
				const annotation = {
					annotationId: `annotation_${Date.now()}`,
					targetRef: targetWidget?.ref || targetRef || ctx.readFocusedWidgetRef() || void 0,
					kind: typeof params.kind === "string" ? params.kind : "note",
					text,
					actor: call?.actor || "agent",
					createdAt: (/* @__PURE__ */ new Date()).toISOString()
				};
				return {
					nextState: ctx.addWorkspaceAnnotation(annotation),
					updatedRefs: annotation.targetRef ? [annotation.targetRef] : [],
					result: annotation,
					verificationHints: ["Read the workspace state and confirm shared.annotations contains the new annotation."],
					notes: { userVisibleSummary: `Annotation added${annotation.targetRef ? ` for ${annotation.targetRef}.` : "."}` }
				};
			});
			this.register({ name: "workspace.clearAnnotations" }, async (call, ctx) => {
				const affectedRefs = (Array.isArray(ctx.readWorkspaceAnnotations()) ? ctx.readWorkspaceAnnotations() : []).map((annotation) => annotation?.targetRef).filter((ref) => typeof ref === "string" && ref.length > 0);
				return {
					nextState: ctx.clearWorkspaceAnnotations(),
					updatedRefs: mergeUniqueRefs(affectedRefs),
					result: { cleared: true },
					verificationHints: ["Read the workspace state and confirm shared.annotations is empty."]
				};
			});
			this.register({ name: "widget.aggregateData" }, async (call, ctx) => {
				const params = call?.params || {};
				const groupBy = Array.isArray(params.groupBy) ? params.groupBy.filter((field) => typeof field === "string" && field.trim()) : [];
				const measures = Array.isArray(params.measures) ? params.measures.map((measure) => ({
					op: typeof measure?.op === "string" ? measure.op : null,
					field: typeof measure?.field === "string" ? measure.field : void 0,
					as: typeof measure?.as === "string" ? measure.as : null
				})).filter((measure) => measure.op && measure.as) : [];
				const supportedMeasureOps = new Set([
					"count",
					"sum",
					"mean",
					"min",
					"max",
					"median"
				]);
				if (groupBy.length === 0 || measures.length === 0 || measures.some((measure) => !supportedMeasureOps.has(measure.op))) throw new Error("widget.aggregateData requires non-empty groupBy and supported measures (count, sum, mean, min, max, or median) with explicit aliases.");
				const targetWidget = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					message: "widget.aggregateData requires a valid target widget."
				});
				return {
					nextState: ctx.updateCurrentSpec((spec) => {
						if (!spec || typeof spec !== "object" || Array.isArray(spec)) throwActionPrecondition("No active base spec is available for aggregate updates.");
						const aggregateTransform = {
							aggregate: measures.map((measure) => ({
								op: measure.op,
								...measure.field ? { field: measure.field } : {},
								as: measure.as
							})),
							groupby: groupBy
						};
						return {
							...spec,
							transform: replaceAggregateTransforms(spec.transform, aggregateTransform)
						};
					}),
					result: {
						widgetId: targetWidget.widgetId,
						groupBy,
						measures
					},
					verificationHints: ["Call perception.inspectViewConfig to verify the target spec now includes the requested aggregate transform.", "Call perception.inspectVisibleRows or summarizeVisible to confirm the widget now exposes grouped summary rows."]
				};
			});
			this.register({ name: "widget.highlightValues" }, async (call, ctx) => {
				const params = call?.params || {};
				const field = typeof params.field === "string" ? params.field : null;
				const values = uniqueValues$1(params.values);
				const targetWidget = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					message: "widget.highlightValues requires a valid target widget."
				});
				const targetWidgetRef = targetWidget?.ref || null;
				const currentDataRef = targetWidget?.data?.currentDataRef || targetWidget?.data?.sourceDataRef || null;
				if (!field || values.length === 0 || !targetWidgetRef || !currentDataRef) throw new Error("widget.highlightValues requires a target widget, field, and at least one value.");
				const runtimeDataEntry = ctx.readRuntimeData(currentDataRef);
				const highlightedRows = markHighlightedRows(runtimeDataEntry?.rows, field, values);
				if (runtimeDataEntry) ctx.updateRuntimeData(currentDataRef, (entry) => ({
					...entry,
					rows: markHighlightedRows(entry?.rows, field, values)
				}));
				const nextState = ctx.patchWidget(targetWidgetRef, {
					transforms: replaceActionHighlightTransformForField(targetWidget?.transforms, field, makeTransformState({
						kind: "highlight",
						source: "action:widget.highlightValues",
						spec: {
							field,
							values
						}
					})),
					feedback: {
						...targetWidget?.feedback || {},
						highlightedKeys: values
					},
					rawSpec: Array.isArray(targetWidget?.rawSpec?.data?.values) ? {
						...targetWidget.rawSpec,
						data: {
							...targetWidget.rawSpec.data,
							values: markHighlightedRows(targetWidget.rawSpec.data.values, field, values)
						}
					} : targetWidget?.rawSpec || null
				});
				return {
					nextState: updateSharedStateInStore(ctx.store, (shared) => withHighlightSubmodel(shared, deriveHighlightState(nextState))),
					result: {
						widgetId: targetWidget?.widgetId || null,
						field,
						values,
						highlightedCount: highlightedRows.filter((row) => row?.__widgetva_highlight === true).length
					},
					verificationHints: ["Call perception.inspectVisibleRows to verify matching rows now carry highlight markers.", "Read the target widget state and confirm feedback.highlightedKeys contains the requested values."]
				};
			});
			this.register({ name: "widget.filterByValues" }, async (call, ctx) => {
				const params = call?.params || {};
				const field = typeof params.field === "string" ? params.field : null;
				const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
				const targetWidgetId = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					message: "widget.filterByValues requires a valid target widget."
				})?.widgetId || null;
				if (!field || values.length === 0 || !targetWidgetId) throw new Error("widget.filterByValues requires a target widget, field, and at least one value.");
				return {
					nextState: ctx.updateCurrentSpec((spec) => {
						if (!spec || typeof spec !== "object" || Array.isArray(spec)) throwActionPrecondition("No active base spec is available for filter updates.");
						return {
							...spec,
							transform: replaceFilterTransformForField(spec.transform, field, { filter: {
								field,
								oneOf: values
							} })
						};
					}),
					result: {
						widgetId: targetWidgetId,
						field,
						values
					},
					verificationHints: ["Call perception.inspectViewConfig to verify the target spec now includes the requested categorical filter transform.", "Read the target widget state or visible rows to confirm the filtered subset propagated."]
				};
			});
			this.register({ name: "widget.filterByRange" }, async (call, ctx) => {
				const params = call?.params || {};
				const field = typeof params.field === "string" ? params.field : null;
				const range = Array.isArray(params.range) ? params.range : null;
				const targetWidgetId = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					message: "widget.filterByRange requires a valid target widget."
				})?.widgetId || null;
				if (!field || !Array.isArray(range) || range.length !== 2 || !targetWidgetId) throw new Error("widget.filterByRange requires a target widget, field, and a two-value range.");
				return {
					nextState: ctx.updateCurrentSpec((spec) => {
						if (!spec || typeof spec !== "object" || Array.isArray(spec)) throwActionPrecondition("No active base spec is available for filter updates.");
						return {
							...spec,
							transform: replaceFilterTransformForField(spec.transform, field, { filter: {
								field,
								range
							} })
						};
					}),
					result: {
						widgetId: targetWidgetId,
						field,
						range
					},
					verificationHints: ["Call perception.inspectViewConfig to verify the target spec now includes the requested range filter transform.", "Read the target widget state or visible rows to confirm the filtered numeric interval propagated."]
				};
			});
			this.register({ name: "widget.sortEncoding" }, async (call, ctx) => {
				const params = call?.params || {};
				const channel = typeof params.channel === "string" ? params.channel : null;
				const order = typeof params.order === "string" ? params.order : null;
				const field = typeof params.field === "string" ? params.field : null;
				const aggregate = typeof params.aggregate === "string" ? params.aggregate : null;
				const targetWidgetId = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					message: "widget.sortEncoding requires a valid target widget."
				})?.widgetId || null;
				if (!channel || !order || !targetWidgetId) throw new Error("widget.sortEncoding requires a target widget, channel, and order.");
				return {
					nextState: ctx.updateCurrentSpec((spec) => {
						if (!spec || typeof spec !== "object" || Array.isArray(spec)) throwActionPrecondition("No active base spec is available for sort updates.");
						const currentChannel = spec.encoding?.[channel];
						if (!currentChannel || typeof currentChannel !== "object") throwActionPrecondition(`The active spec does not define encoding channel "${channel}".`);
						const sortField = field || currentChannel.field;
						const nextEncoding = {
							...spec.encoding || {},
							[channel]: {
								...currentChannel,
								sort: sortField ? {
									field: sortField,
									order,
									...aggregate ? { op: aggregate } : {}
								} : order
							}
						};
						return {
							...spec,
							encoding: nextEncoding
						};
					}),
					result: {
						widgetId: targetWidgetId,
						channel,
						order,
						...field ? { field } : {},
						...aggregate ? { aggregate } : {}
					},
					verificationHints: ["Call perception.inspectViewConfig to verify the target encoding now includes the requested sort rule.", "Read the target widget state to confirm the sort update propagated."]
				};
			});
			this.register({ name: "widget.changeEncoding" }, async (call, ctx) => {
				const params = call?.params || {};
				const channel = typeof params.channel === "string" ? params.channel : null;
				const field = typeof params.field === "string" ? params.field : null;
				const targetWidget = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					message: "widget.changeEncoding requires a valid target widget."
				});
				const targetWidgetId = targetWidget?.widgetId || null;
				if (!channel || !field || !targetWidgetId) throw new Error("widget.changeEncoding requires a target widget, channel, and field.");
				const targetAdapter = ctx.readWidgetAdapter(targetWidget.ref);
				if (typeof targetAdapter?.applyEncodingChange === "function") await Promise.resolve(targetAdapter.applyEncodingChange(channel, field, params));
				return {
					nextState: ctx.updateCurrentSpec((spec) => {
						if (!spec || typeof spec !== "object" || Array.isArray(spec)) throwActionPrecondition("No active base spec is available for encoding updates.");
						const nextEncoding = {
							...spec.encoding || {},
							[channel]: {
								...spec.encoding?.[channel] || {},
								field,
								...typeof params.type === "string" ? { type: params.type } : {},
								...params.aggregate ? { aggregate: params.aggregate } : {}
							}
						};
						return {
							...spec,
							encoding: nextEncoding
						};
					}),
					result: {
						widgetId: targetWidgetId,
						channel,
						field
					},
					verificationHints: ["Call perception.inspectViewConfig to verify the encoding channel now points to the new field.", "Read the target widget state to confirm the encoding update propagated."]
				};
			});
			this.register({ name: "widget.zoomDomain" }, async (call, ctx) => {
				const params = call?.params || {};
				const targetWidget = ctx.requireTargetWidget({
					targetRef: resolveActionTargetRef(call),
					message: "widget.zoomDomain requires a valid target widget."
				});
				const xDomain = Array.isArray(params.xDomain) ? params.xDomain : null;
				const yDomain = Array.isArray(params.yDomain) ? params.yDomain : null;
				if (!targetWidget || !xDomain && !yDomain) throw new Error("widget.zoomDomain requires a target widget and at least one domain range.");
				const targetAdapter = ctx.readWidgetAdapter(targetWidget.ref);
				if (typeof targetAdapter?.applyDomainZoom === "function") await Promise.resolve(targetAdapter.applyDomainZoom({
					xDomain,
					yDomain,
					options: params
				}));
				return {
					nextState: ctx.updateCurrentSpec((spec) => {
						if (!spec || typeof spec !== "object" || Array.isArray(spec)) throwActionPrecondition("No active base spec is available for domain updates.");
						const nextEncoding = { ...spec.encoding || {} };
						if (xDomain && nextEncoding.x) nextEncoding.x = {
							...nextEncoding.x,
							scale: {
								...nextEncoding.x.scale || {},
								domain: xDomain
							}
						};
						if (yDomain && nextEncoding.y) nextEncoding.y = {
							...nextEncoding.y,
							scale: {
								...nextEncoding.y.scale || {},
								domain: yDomain
							}
						};
						return {
							...spec,
							encoding: nextEncoding
						};
					}),
					result: {
						widgetId: targetWidget.widgetId,
						...xDomain ? { xDomain } : {},
						...yDomain ? { yDomain } : {}
					},
					verificationHints: ["Call perception.inspectViewConfig to verify the target domain was updated.", "Read the target widget view state to confirm the new x/y domain values."]
				};
			});
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/AgentLoopRuntime.js
	function clone$11(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function isNonBlockingPropagationResult(entry = {}) {
		return entry?.primitive === "usesData" || entry?.reason === "manual_activation_policy";
	}
	function hasBlockingPropagationFailure(entry = {}) {
		const relevantResults = (Array.isArray(entry?.results) ? entry.results : []).filter((result) => !isNonBlockingPropagationResult(result));
		if (relevantResults.length === 0) return false;
		return relevantResults.some((result) => result?.passed === false);
	}
	function pickFocusedWidgetRef(description, state) {
		const widgetRefs = Object.keys(state?.widgets || {});
		return state?.shared?.focusedWidget || widgetRefs[0] || description?.widgets?.[0]?.ref || null;
	}
	function summarizeLoopDataViews(description, state) {
		const dataHandles = Array.isArray(description?.dataHandles) ? description.dataHandles : [];
		const focusedWidgetRef = pickFocusedWidgetRef(description, state);
		const focusedWidget = focusedWidgetRef ? state?.widgets?.[focusedWidgetRef] || null : null;
		const focusedWidgetDataRefs = new Set([focusedWidget?.data?.currentDataRef, focusedWidget?.data?.sourceDataRef].filter((ref) => typeof ref === "string" && ref.length > 0));
		const activeSelectionRefs = Object.keys(readSelectionRegistry(state?.shared || {}));
		const sharedDataViews = dataHandles.filter((handle) => handle?.scope === "workspaceCurrentView" || handle?.scope === "workspaceCurrent").map((handle) => ({
			ref: handle.ref,
			scope: handle.scope || "workspace",
			title: handle.title || handle.ref
		}));
		const focusedDataViews = dataHandles.filter((handle) => {
			if (!handle?.ref) return false;
			if (handle.widgetRef && focusedWidgetRef && handle.widgetRef === focusedWidgetRef) return true;
			return focusedWidgetDataRefs.has(handle.ref);
		}).slice(0, 6).map((handle) => ({
			ref: handle.ref,
			scope: handle.scope || "workspace",
			title: handle.title || handle.ref
		}));
		const currentViewHandle = sharedDataViews.find((handle) => handle.scope === "workspaceCurrentView") || null;
		const currentSelectionHandle = sharedDataViews.find((handle) => handle.scope === "workspaceCurrent") || null;
		return {
			sharedDataViews,
			focusedDataViews,
			preferredEvidenceRefs: {
				currentViewRef: currentViewHandle?.ref || null,
				currentSelectionRef: activeSelectionRefs.length > 0 ? currentSelectionHandle?.ref || null : null
			}
		};
	}
	function summarizeHumanInteractionHints(description, state) {
		const widgets = Array.isArray(description?.widgets) ? description.widgets : [];
		const focusedWidgetRef = pickFocusedWidgetRef(description, state);
		return widgets.filter((widget) => {
			const interaction = widget?.humanInteraction;
			return interaction && interaction.mode && interaction.mode !== "none";
		}).map((widget) => ({
			widgetRef: widget.ref,
			mode: widget.humanInteraction.mode,
			actionName: widget.humanInteraction.actionName || null,
			supportsDirectManipulation: Boolean(widget.humanInteraction.supportsDirectManipulation),
			focused: widget.ref === focusedWidgetRef
		}));
	}
	var AgentLoopRuntime = class {
		constructor({ store, planWorkspace = null, executeAction, queryPerception, queryData = null, readLatestCoordinationResult = null, responseRecorder = null, actionExecutor = null, linkEngine }) {
			this.store = store;
			this.planWorkspace = planWorkspace;
			this.executeAction = executeAction;
			this.queryPerception = queryPerception;
			this.queryData = queryData;
			this.readLatestCoordinationResult = readLatestCoordinationResult;
			this.responseRecorder = responseRecorder;
			this.actionExecutor = actionExecutor;
			this.linkEngine = linkEngine;
		}
		async describeStepContext(options = {}) {
			const description = readWorkspaceDescriptionFromStore(this.store);
			const state = readWorkspaceStateFromStore(this.store, options?.viewOptions || {});
			const workspaceTopology = description?.runtimeTopology || deriveWorkspaceTopology({
				widgets: description?.widgets || [],
				links: description?.links || []
			});
			const dataViewHints = summarizeLoopDataViews(description, state);
			const humanInteractionHints = summarizeHumanInteractionHints(description, state);
			const hasActionHandler = (actionName) => {
				if (!actionName) return false;
				if (typeof this.actionExecutor?.has === "function") return this.actionExecutor.has(actionName);
				if (this.actionExecutor?.handlers instanceof Map) return this.actionExecutor.handlers.has(actionName);
				return false;
			};
			const surfaceName = (available, name) => available ? name : null;
			const canPlanWorkspace = typeof this.planWorkspace === "function";
			const canQueryData = typeof this.queryData === "function";
			const canReadInteractionTrace = typeof this.store?.readTraceWindow === "function" || Array.isArray(this.store?.interactionTrace);
			const canReadTraceGraph = typeof this.store?.buildTraceGraph === "function" || Array.isArray(this.store?.stateSnapshots);
			const canReadSnapshot = typeof this.store?.readSnapshotEntry === "function" || Array.isArray(this.store?.stateSnapshots);
			const canReadStateHistory = typeof this.store?.listStateHistory === "function" || typeof this.store?.listStateSnapshots === "function" || Array.isArray(this.store?.stateSnapshots);
			const canListBranches = typeof this.store?.listBranches === "function" || Boolean(this.store?.branchRegistry && typeof this.store.branchRegistry === "object");
			const canJumpToState = hasActionHandler("workspace.jumpToState");
			const canBranchFromState = hasActionHandler("workspace.branchFromState");
			const canDescribeResponseRecorder = typeof this.responseRecorder?.describeRecorder === "function";
			const canReadLatestResponse = typeof this.responseRecorder?.readLatestResponse === "function" || typeof this.store?.readLatestResponse === "function" || Array.isArray(this.store?.responseHistory);
			const canListResponses = typeof this.responseRecorder?.listResponses === "function" || typeof this.store?.listResponses === "function" || Array.isArray(this.store?.responseHistory);
			const canRecordResponse = typeof this.responseRecorder?.recordResponse === "function";
			const canEvaluateLinkPropagation = typeof this.linkEngine?.evaluatePropagation === "function";
			const canReadLatestCoordinationResult = typeof this.readLatestCoordinationResult === "function" || typeof this.store?.readLatestCoordinationResult === "function";
			const latestCoordinationResult = canReadLatestCoordinationResult ? clone$11(await (typeof this.readLatestCoordinationResult === "function" ? this.readLatestCoordinationResult() : this.store.readLatestCoordinationResult())) : null;
			const recommendedOrder = [
				"describeWorkspace",
				...canPlanWorkspace ? ["planWorkspace(optional)"] : [],
				"readState",
				"queryPerception(optional)",
				...canQueryData ? ["runDataQuery(optional)"] : [],
				"executeAction",
				"readState(deltaSince)",
				"queryPerception(verify)",
				"produceAnswerOrContinue",
				...canRecordResponse ? ["recordAgentResponse(optional)"] : []
			];
			return makeAgentLoopContext({
				workspace: clone$11(description),
				view: clone$11(state),
				latestCoordinationResult,
				loopHints: makeAgentLoopHints({
					recommendedOrder,
					workspaceDescribeName: "describeWorkspace",
					workspacePlanName: surfaceName(canPlanWorkspace, "planWorkspace"),
					stateReadName: "readState",
					viewReadName: "readView",
					actionRunName: "executeAction",
					perceptionQueryName: "queryPerception",
					verifyQueryName: "perception.verifyActionEffect",
					dataQueryRunName: surfaceName(canQueryData, "runDataQuery"),
					dataQueryName: surfaceName(canQueryData, "queryData"),
					traceReadName: surfaceName(canReadInteractionTrace, "readTrace"),
					interactionTraceName: surfaceName(canReadInteractionTrace, "getInteractionTrace"),
					traceGraphName: surfaceName(canReadTraceGraph, "getTraceGraph"),
					snapshotName: surfaceName(canReadSnapshot, "readSnapshot"),
					stateHistoryName: surfaceName(canReadStateHistory, "listStateHistory"),
					branchListName: surfaceName(canListBranches, "listBranches"),
					verifiedActionName: "executeVerifiedAction",
					replayName: surfaceName(canJumpToState, "replay"),
					jumpToStateName: surfaceName(canJumpToState, "jumpToState"),
					branchFromStateName: surfaceName(canBranchFromState, "branchFromState"),
					responseRecorderName: surfaceName(canDescribeResponseRecorder, "describeResponseRecorder"),
					responseReadName: surfaceName(canReadLatestResponse, "getLatestAgentResponse"),
					responseListName: surfaceName(canListResponses, "listAgentResponses"),
					responseRecordName: surfaceName(canRecordResponse, "recordAgentResponse"),
					latestCoordinationResultReadName: surfaceName(canReadLatestCoordinationResult, "readLatestCoordinationResult"),
					verificationSurfaces: [
						"perception.verifyActionEffect",
						...canQueryData ? ["runDataQuery"] : [],
						...canReadInteractionTrace ? ["readTrace"] : [],
						...canEvaluateLinkPropagation ? ["linkPropagation"] : [],
						...canReadLatestCoordinationResult ? ["readLatestCoordinationResult"] : []
					],
					planningSurfaces: [
						...canPlanWorkspace ? ["planWorkspace"] : [],
						"workspace.runtimeTopology",
						"workspace.planning"
					],
					historySurfaces: [
						...canReadInteractionTrace ? ["readTrace", "getInteractionTrace"] : [],
						...canReadTraceGraph ? ["getTraceGraph"] : [],
						...canReadSnapshot ? ["readSnapshot"] : [],
						...canReadStateHistory ? ["listStateHistory"] : [],
						...canListBranches ? ["listBranches"] : [],
						...canReadLatestResponse ? ["getLatestAgentResponse"] : [],
						...canListResponses ? ["listAgentResponses"] : []
					],
					replaySurfaces: [
						"executeVerifiedAction",
						...canJumpToState ? ["replay"] : [],
						...canJumpToState ? ["jumpToState"] : [],
						...canBranchFromState ? ["branchFromState"] : []
					],
					answerSurfaces: [
						...canDescribeResponseRecorder ? ["describeResponseRecorder"] : [],
						...canReadLatestResponse ? ["getLatestAgentResponse"] : [],
						...canListResponses ? ["listAgentResponses"] : [],
						...canRecordResponse ? ["recordAgentResponse"] : []
					],
					humanInteractionHints: clone$11(humanInteractionHints),
					sharedDataViews: dataViewHints.sharedDataViews,
					focusedDataViews: dataViewHints.focusedDataViews,
					preferredEvidenceRefs: dataViewHints.preferredEvidenceRefs,
					workspaceTopology: clone$11(workspaceTopology)
				})
			});
		}
		collectTraceEvidence({ stateId, actionName }) {
			const matchingRecord = [...readInteractionTraceFromStore(this.store, { limit: 100 })].reverse().find((record) => {
				if (stateId && record.stateId !== stateId) return false;
				if (actionName && record.action?.name !== actionName) return false;
				return true;
			});
			return matchingRecord ? clone$11(matchingRecord) : null;
		}
		collectLinkPropagationEvidence({ snapshot }) {
			const evaluateLinkPropagation = typeof this.linkEngine?.evaluatePropagation === "function" ? ({ sourceRef, state }) => this.linkEngine.evaluatePropagation({
				sourceRef,
				state
			}) : null;
			if (!evaluateLinkPropagation) return [];
			return Object.keys(readSelectionRegistry(snapshot?.shared || {})).map((sourceRef) => evaluateLinkPropagation({
				sourceRef,
				state: snapshot
			}));
		}
		async executeVerifiedAction(call, options = {}) {
			const beforeStateId = readWorkspaceStateFromStore(this.store)?.stateId || null;
			const actionResult = await this.executeAction(call);
			const readLatestCoordinationResult = typeof this.readLatestCoordinationResult === "function" ? this.readLatestCoordinationResult : typeof this.store?.readLatestCoordinationResult === "function" ? () => this.store.readLatestCoordinationResult() : null;
			if (!actionResult?.ok) return makeVerifiedActionResult({
				ok: false,
				beforeStateId,
				actionResult,
				afterView: null,
				verification: null,
				latestCoordinationResult: readLatestCoordinationResult ? clone$11(await readLatestCoordinationResult()) : null
			});
			const afterView = readWorkspaceStateFromStore(this.store, {
				refs: actionResult.updatedRefs,
				deltaSince: options.includeDeltaSince ? beforeStateId : void 0
			});
			let verification = null;
			if (options.verify !== false) verification = await this.queryPerception({
				callId: `${call?.callId || "call"}_verify`,
				name: "perception.verifyActionEffect",
				actor: call?.actor || "agent",
				targetRef: call?.queryScope?.widgetRef || void 0,
				params: {
					actionName: call?.name,
					stateId: actionResult.stateId,
					refs: actionResult.updatedRefs
				}
			});
			const finalSnapshot = actionResult.stateId ? readSnapshotFromStore(this.store, actionResult.stateId, {
				refs: options.includeFinalSnapshotRefs === false ? void 0 : actionResult.updatedRefs,
				includeMeta: true
			}) : null;
			const fullFinalSnapshot = actionResult.stateId ? readSnapshotFromStore(this.store, actionResult.stateId, { includeMeta: true }) || finalSnapshot : finalSnapshot;
			const traceEvidence = this.collectTraceEvidence({
				stateId: actionResult.stateId,
				actionName: call?.name
			});
			const linkPropagation = fullFinalSnapshot ? this.collectLinkPropagationEvidence({ snapshot: fullFinalSnapshot }) : [];
			const verificationHints = Array.isArray(actionResult?.verificationHints) ? actionResult.verificationHints : [];
			const verificationOk = verification?.ok !== false && (verification?.result?.verified ?? true);
			const propagationOk = !linkPropagation.some((entry) => hasBlockingPropagationFailure(entry));
			const latestCoordinationResult = readLatestCoordinationResult ? clone$11(await readLatestCoordinationResult()) : null;
			return makeVerifiedActionResult({
				ok: Boolean(actionResult?.ok) && verificationOk && propagationOk,
				beforeStateId,
				actionResult,
				afterView: clone$11(afterView),
				verification: clone$11(verification),
				verificationHints: clone$11(verificationHints),
				traceEvidence,
				finalSnapshot,
				linkPropagation,
				latestCoordinationResult
			});
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/DataQueryContext.js
	var DataQueryContext = class {
		constructor({ store, call, traceRecorder, hostBridge }) {
			this.store = store;
			this.call = call;
			this.traceRecorder = traceRecorder;
			this.hostBridge = hostBridge || createDefaultWidgetVAHostBridge();
			this.pendingDataQueryTrace = null;
		}
		static describeContract() {
			return makeDataQueryContextSummary({
				methods: [
					"readDescription",
					"readCurrentState",
					"readSelectionRegistry",
					"readFocusState",
					"readHighlightState",
					"readQueryScope",
					"readNormalizedQuerySpec",
					"readFocusedWidgetRef",
					"readRuntimeData",
					"resolveTargetWidget",
					"resolveExplicitTarget",
					"resolveSelectionDataRef",
					"resolveDataRef",
					"resolveDataHandle",
					"resolveSelectionState",
					"resolveRows",
					"recordDataQuery"
				],
				capabilities: makeDataQueryContextCapabilities({
					workspaceRead: true,
					runtimeDataRead: true,
					widgetTargetResolution: true,
					dataHandleResolution: true,
					selectionTargetResolution: true,
					explicitTargetValidation: true,
					selectionScopedQueries: true,
					traceRecording: true
				}),
				integrations: makeDataQueryContextIntegrations({
					store: true,
					traceRecorder: true
				})
			});
		}
		readDescription() {
			return readWorkspaceDescriptionFromStore(this.store);
		}
		readCurrentState(options = {}) {
			return readWorkspaceStateFromStore(this.store, options);
		}
		readSelectionRegistry() {
			const hostRegistry = this.hostBridge.readSelectionRegistry?.();
			if (hostRegistry && typeof hostRegistry === "object" && !Array.isArray(hostRegistry) && Object.keys(hostRegistry).length > 0) return hostRegistry;
			return readSelectionRegistry(this.readCurrentState()?.shared || {});
		}
		readFocusState() {
			const hostFocusState = this.hostBridge.readFocusState?.();
			if (hostFocusState && typeof hostFocusState === "object" && !Array.isArray(hostFocusState)) return hostFocusState;
			const currentState = this.readCurrentState();
			return readFocusState(currentState?.shared || {}, currentState?.widgets || {});
		}
		readHighlightState() {
			const hostHighlightState = this.hostBridge.readHighlightState?.();
			if (hostHighlightState && typeof hostHighlightState === "object" && !Array.isArray(hostHighlightState)) return hostHighlightState;
			return deriveHighlightState(this.readCurrentState() || {});
		}
		readQueryScope() {
			return readNormalizedQueryScope({
				call: this.call,
				querySpec: this.readQuerySpec()
			});
		}
		readNormalizedQuerySpec() {
			return this.normalizeScopedParams(this.readQuerySpec(), {
				querySpec: this.readQuerySpec(),
				includeTopLevelLegacyTargeting: false
			});
		}
		readFocusedWidgetRef() {
			const hostFocusedWidgetRef = this.hostBridge.readFocusedWidgetRef?.();
			if (typeof hostFocusedWidgetRef === "string" && hostFocusedWidgetRef.length > 0) return hostFocusedWidgetRef;
			const hostFocusWidgetRef = this.readFocusState()?.widgetRef;
			if (typeof hostFocusWidgetRef === "string" && hostFocusWidgetRef.length > 0) return hostFocusWidgetRef;
			return this.readCurrentState()?.shared?.focusedWidget || null;
		}
		readRuntimeData(dataRef) {
			return readRuntimeDataFromStore(this.store, dataRef);
		}
		normalizeScopedParams(params = {}, options = {}) {
			return buildScopedParams({
				params,
				call: options?.call || this.call,
				querySpec: options?.querySpec || this.readQuerySpec(),
				includeTopLevelLegacyTargeting: options?.includeTopLevelLegacyTargeting ?? true
			});
		}
		resolveTargetWidget(targetRef = this.readQueryScope()?.widgetRef || null) {
			const resolvedTargetRef = targetRef || this.readQueryScope()?.widgetRef || null;
			if (!resolvedTargetRef) return null;
			return resolveWidgetRecordFromStore(this.store, resolvedTargetRef);
		}
		readQuerySpec() {
			return this.call?.query?.spec || {};
		}
		resolveExplicitTarget(options = {}) {
			const normalizedOptions = this.normalizeScopedParams(options, { querySpec: options });
			const dataRef = normalizedOptions?.queryScope?.dataRef || this.call?.dataRef || this.readQueryScope()?.dataRef;
			const targetRef = normalizedOptions?.queryScope?.widgetRef || this.readQueryScope()?.widgetRef || null;
			if (dataRef) {
				const selectionDataRef = this.resolveSelectionDataRef(dataRef);
				if (selectionDataRef) return {
					kind: "data",
					ref: selectionDataRef,
					sourceRef: dataRef
				};
				if (this.store.getDataHandle?.(dataRef) || this.readRuntimeData(dataRef)) return {
					kind: "data",
					ref: dataRef
				};
				return null;
			}
			if (!targetRef) return null;
			const selectionDataRef = this.resolveSelectionDataRef(targetRef);
			if (selectionDataRef) return {
				kind: "data",
				ref: selectionDataRef,
				sourceRef: targetRef
			};
			if (this.store.getDataHandle?.(targetRef) || this.readRuntimeData(targetRef)) return {
				kind: "data",
				ref: targetRef
			};
			const targetWidget = this.resolveTargetWidget(targetRef);
			if (targetWidget?.ref) return {
				kind: "widget",
				ref: targetWidget.ref,
				widget: targetWidget
			};
			return null;
		}
		resolveSelectionDataRef(selectionRef) {
			if (!selectionRef) return null;
			const resolvedDataRef = resolveSelectionDataRefFromStore(this.store, selectionRef);
			if (!resolvedDataRef) return null;
			if (readDataHandleFromStore(this.store, resolvedDataRef) || this.readRuntimeData(resolvedDataRef)) return resolvedDataRef;
			return null;
		}
		resolveDataRef(preferredDataRef = this.call?.dataRef) {
			const queryScope = this.readNormalizedQuerySpec()?.queryScope || this.readQueryScope();
			if (preferredDataRef) return preferredDataRef;
			if (typeof queryScope?.dataRef === "string" && queryScope.dataRef.length > 0) return queryScope.dataRef;
			const explicitTarget = this.resolveExplicitTarget();
			if (explicitTarget?.kind === "data") return explicitTarget.ref;
			if (explicitTarget?.kind === "widget") return explicitTarget.widget?.data?.currentDataRef || explicitTarget.widget?.data?.sourceDataRef || explicitTarget.widget?.primaryDataRef || null;
			const focusedWidgetRef = this.readFocusedWidgetRef();
			const focusedWidget = focusedWidgetRef ? this.store.getResolvedWidget?.(focusedWidgetRef) || null : null;
			return focusedWidget?.data?.currentDataRef || focusedWidget?.data?.sourceDataRef || focusedWidget?.primaryDataRef || null;
		}
		resolveDataHandle(dataRef = this.call?.dataRef) {
			const resolvedDataRef = this.resolveDataRef(dataRef);
			return resolvedDataRef ? readDataHandleFromStore(this.store, resolvedDataRef) : null;
		}
		resolveSelectionState(selectionRef) {
			if (!selectionRef) return null;
			return this.readSelectionRegistry()?.[selectionRef] || null;
		}
		resolveRows(dataRef = this.call?.dataRef, options = {}) {
			const resolvedDataRef = this.resolveDataRef(dataRef);
			const runtimeData = this.readRuntimeData(resolvedDataRef);
			const baseRows = Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
			const selectionRef = this.normalizeScopedParams(options, { querySpec: options })?.queryScope?.selectionRef || this.readQueryScope()?.selectionRef || null;
			if (!selectionRef) return baseRows;
			const selectionState = this.resolveSelectionState(selectionRef);
			if (!selectionState) return null;
			const predicates = Array.isArray(selectionState?.predicates) ? selectionState.predicates : [];
			if (predicates.length === 0) return baseRows;
			return baseRows.filter((row) => predicates.every((predicate) => applyPredicate$1(row, predicate)));
		}
		recordDataQuery({ affectedRefs = [], notes } = {}) {
			this.pendingDataQueryTrace = {
				call: this.call,
				affectedRefs,
				notes
			};
		}
		consumeRecordedDataQuery() {
			const pending = this.pendingDataQueryTrace;
			this.pendingDataQueryTrace = null;
			return pending;
		}
	};
	function applyPredicate$1(row, predicate) {
		const value = row?.[predicate?.field];
		if (predicate?.op === "between" && Array.isArray(predicate?.value)) return value >= predicate.value[0] && value <= predicate.value[1];
		if (predicate?.op === "equals") return value === predicate?.value;
		if (predicate?.op === "in" && Array.isArray(predicate?.value)) return predicate.value.includes(value);
		if (predicate?.op === "notIn" && Array.isArray(predicate?.value)) return !predicate.value.includes(value);
		return true;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/DataQueryExecutor.js
	function buildDataQueryError({ dataRef, code, message, details, recoveryHints }) {
		return makeDataQueryResult({
			ok: false,
			dataRef,
			error: makeResultError({
				code,
				message,
				...details !== void 0 ? { details } : {}
			}),
			...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {}
		});
	}
	function recordDataQueryFailure(traceRecorder, call, { code, message, details, recoveryHints }) {
		traceRecorder?.recordDataQueryFailure?.({
			call,
			code,
			message,
			details,
			recoveryHints
		});
	}
	function buildDataQueryTraceNotes({ kind, selectionRef = null, dataRef = null, rowCount = null } = {}) {
		return {
			userVisibleSummary: `Data query: ${kind}${selectionRef ? ` scoped to ${selectionRef}` : ""}${Number.isFinite(rowCount) ? ` over ${rowCount} row${rowCount === 1 ? "" : "s"}` : ""}`,
			rationale: selectionRef ? "A selection-scoped data query was requested for the current runtime data view." : "A data query was requested for the current runtime data view.",
			...dataRef ? { verification: `Query executed against ${dataRef}.` } : {}
		};
	}
	function buildSupportedQueryDescriptors(kinds = []) {
		return kinds.map((kind) => makeDataQueryDescriptor({
			name: kind,
			...DATA_QUERY_DESCRIPTOR_TEMPLATES[kind],
			inputSchema: DATA_QUERY_SCHEMAS[kind] || {
				type: "object",
				additionalProperties: true,
				properties: {}
			}
		}));
	}
	function supportsUnifiedEngineQuery(engine) {
		if (!engine || typeof engine.query !== "function") return false;
		if (!(engine instanceof DataQueryEngine)) return true;
		if (engine.query !== DataQueryEngine.prototype.query) return true;
		if (typeof engine.resolveSource === "function") return true;
		if (typeof engine.options?.resolveSource === "function") return true;
		return false;
	}
	var DataQueryExecutor = class {
		constructor({ store, dataQueryEngine, traceRecorder, hostBridge } = {}) {
			this.store = store || {
				readDescription() {
					return {
						widgets: [],
						dataHandles: []
					};
				},
				readState() {
					return { shared: { focusedWidget: null } };
				},
				getResolvedWidgetForTarget() {
					return null;
				},
				getResolvedWidget() {
					return null;
				},
				getDataHandle() {
					return null;
				},
				readRuntimeData() {
					return null;
				},
				resolveSelectionDataRef() {
					return null;
				}
			};
			this.dataQueryEngine = dataQueryEngine || {
				kind: "manual",
				listSupportedQueryKinds() {
					return [];
				}
			};
			this.traceRecorder = traceRecorder || null;
			this.hostBridge = hostBridge || createDefaultWidgetVAHostBridge();
		}
		run(call = {}) {
			const ctx = this.createContext(call);
			const querySpec = ctx.readNormalizedQuerySpec();
			const queryScope = ctx.readQueryScope();
			const explicitTargetRef = queryScope?.widgetRef || queryScope?.dataRef || null;
			const explicitTarget = ctx.resolveExplicitTarget({
				dataRef: call?.dataRef || querySpec?.queryScope?.dataRef || null,
				targetRef: queryScope?.widgetRef || null
			});
			if (!call?.dataRef && !querySpec?.queryScope?.dataRef && explicitTargetRef && !explicitTarget) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "UNSUPPORTED_TARGET",
					message: `The requested data-query target ref is not available: ${explicitTargetRef}.`,
					details: { targetRef: explicitTargetRef },
					recoveryHints: ["Call describeWorkspace() to inspect valid widget refs and data refs before retrying.", "Retry the data query with a targetRef that resolves to a materialized widget or data handle."]
				});
				return buildDataQueryError({
					dataRef: null,
					code: "UNSUPPORTED_TARGET",
					message: `The requested data-query target ref is not available: ${explicitTargetRef}.`,
					details: { targetRef: explicitTargetRef },
					recoveryHints: ["Call describeWorkspace() to inspect valid widget refs and data refs before retrying.", "Retry the data query with a targetRef that resolves to a materialized widget or data handle."]
				});
			}
			const dataRef = ctx.resolveDataRef(call?.dataRef);
			const query = call?.query || {};
			const selectionRef = querySpec?.queryScope?.selectionRef || null;
			const runtimeData = ctx.readRuntimeData(dataRef);
			const dataHandle = ctx.resolveDataHandle(dataRef);
			if (!runtimeData) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "UNKNOWN_DATA_REF",
					message: `Unknown data ref: ${dataRef || "missing"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect materialized data handles and valid data refs.", "Retry the query with an explicit dataRef, a targetRef, or after focusing the widget whose data you want to inspect."]
				});
				return buildDataQueryError({
					dataRef,
					code: "UNKNOWN_DATA_REF",
					message: `Unknown data ref: ${dataRef || "missing"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect materialized data handles and valid data refs.", "Retry the query with an explicit dataRef, a targetRef, or after focusing the widget whose data you want to inspect."]
				});
			}
			const rows = ctx.resolveRows(dataRef, { selectionRef });
			const kind = query.kind;
			if (!DATA_QUERY_KINDS.includes(kind)) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "UNKNOWN_QUERY_KIND",
					message: `Unsupported data query kind: ${kind || "missing"}.`,
					recoveryHints: ["Inspect supportedQueryDescriptors on the current data handle before retrying."]
				});
				return buildDataQueryError({
					dataRef,
					code: "UNKNOWN_QUERY_KIND",
					message: `Unsupported data query kind: ${kind || "missing"}.`,
					recoveryHints: ["Inspect supportedQueryDescriptors on the current data handle before retrying."]
				});
			}
			if (dataHandle && Array.isArray(dataHandle.supportedQueries) && !dataHandle.supportedQueries.includes(kind)) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "UNSUPPORTED_QUERY_KIND",
					message: `Data query kind ${kind} is not supported by the current data handle.`,
					details: {
						supportedQueries: dataHandle.supportedQueries,
						dataHandleRef: dataHandle.ref || null
					},
					recoveryHints: ["Inspect supportedQueries or supportedQueryDescriptors on the current data handle before retrying.", "Retry with a query kind exposed by the current widget data handle."]
				});
				return buildDataQueryError({
					dataRef,
					code: "UNSUPPORTED_QUERY_KIND",
					message: `Data query kind ${kind} is not supported by the current data handle.`,
					details: {
						supportedQueries: dataHandle.supportedQueries,
						dataHandleRef: dataHandle.ref || null
					},
					recoveryHints: ["Inspect supportedQueries or supportedQueryDescriptors on the current data handle before retrying.", "Retry with a query kind exposed by the current widget data handle."]
				});
			}
			if (selectionRef && rows == null) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "INVALID_QUERY_SPEC",
					message: `Selection ref ${selectionRef} is not active in the current workspace state.`,
					recoveryHints: ["Inspect shared.selections.registry in readState() or call perception.summarizeSelection() before retrying.", "Retry the data query with an active selectionRef or omit selectionRef to query the full current view."]
				});
				return buildDataQueryError({
					dataRef,
					code: "INVALID_QUERY_SPEC",
					message: `Selection ref ${selectionRef} is not active in the current workspace state.`,
					recoveryHints: ["Inspect shared.selections.registry in readState() or call perception.summarizeSelection() before retrying.", "Retry the data query with an active selectionRef or omit selectionRef to query the full current view."]
				});
			}
			const querySchema = DATA_QUERY_SCHEMAS[kind];
			if (querySchema) try {
				validateAgainstSchema(querySpec, querySchema, `query.spec.${kind}`);
			} catch (error) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "INVALID_QUERY_SPEC",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the selected data query descriptor inputSchema/examples before retrying."]
				});
				return buildDataQueryError({
					dataRef,
					code: "INVALID_QUERY_SPEC",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the selected data query descriptor inputSchema/examples before retrying."]
				});
			}
			const resultSchema = DATA_QUERY_RESULT_SCHEMAS[kind] || null;
			try {
				let result;
				const runUnifiedQuery = () => this.dataQueryEngine.query(dataRef, {
					kind,
					spec: querySpec
				});
				if (kind === "schema") result = runtimeData.handle?.schema || (supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.getSchema(rows));
				if (kind === "filter") {
					const predicates = Array.isArray(querySpec?.predicates) ? querySpec.predicates : [];
					const filteredRows = supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.filter(rows, predicates);
					result = {
						rows: Array.isArray(filteredRows?.rows) ? filteredRows.rows : filteredRows,
						rowCount: Array.isArray(filteredRows?.rows) ? filteredRows.rows.length : filteredRows.length,
						predicates
					};
				}
				if (kind === "sampleRows") result = supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.sampleRows(rows, querySpec?.limit || 20);
				if (kind === "summary") result = supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.summarize(rows, normalizeSummarySpec(querySpec));
				if (kind === "aggregate" || kind === "groupBy") {
					const aggregateSpec = normalizeAggregateSpec(querySpec);
					result = normalizeAggregateResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.aggregate(rows, aggregateSpec));
				}
				if (kind === "sql") result = supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.executeSql(rows, querySpec);
				if (kind === "computeCorrelation") result = normalizeCorrelationResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.computeCorrelation(rows, querySpec), querySpec);
				if (kind === "findExtremes") result = normalizeExtremesResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.findExtremes(rows, normalizeExtremesSpec(querySpec)), querySpec);
				if (kind === "findOutliers") result = normalizeOutliersResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.findOutliers(rows, querySpec), querySpec);
				if (kind === "compareGroups") result = normalizeCompareGroupsResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.compareGroups(rows, normalizeCompareGroupsSpec(querySpec)), querySpec);
				const output = makeDataQueryResult({
					ok: false,
					dataRef,
					error: null
				});
				if (result !== void 0) {
					if (result?.ok === false && result?.error) {
						recordDataQueryFailure(this.traceRecorder, call, {
							code: result.error.code || "RUNTIME_ERROR",
							message: result.error.message || "Data query execution failed.",
							...result?.error?.details !== void 0 ? { details: result.error.details } : {},
							recoveryHints: Array.isArray(result?.recoveryHints) ? result.recoveryHints : []
						});
						output.error = result.error;
						if (Array.isArray(result?.recoveryHints)) output.recoveryHints = result.recoveryHints;
						return output;
					}
					output.ok = true;
					output.result = result;
					if (resultSchema) validateAgainstSchema(output.result, resultSchema, `result.${kind}`);
					ctx.recordDataQuery({
						affectedRefs: runtimeData.widgetRef ? [runtimeData.widgetRef] : [dataRef],
						notes: buildDataQueryTraceNotes({
							kind,
							selectionRef,
							dataRef,
							rowCount: Array.isArray(rows) ? rows.length : null
						})
					});
					const tracePayload = ctx.consumeRecordedDataQuery?.() || null;
					if (tracePayload) this.traceRecorder?.recordDataQuery?.(tracePayload);
				} else {
					recordDataQueryFailure(this.traceRecorder, call, {
						code: "UNKNOWN_QUERY_KIND",
						message: `Unsupported data query kind: ${kind || "missing"}.`,
						recoveryHints: ["Inspect supportedQueryDescriptors on the current data handle before retrying."]
					});
					output.error = {
						code: "UNKNOWN_QUERY_KIND",
						message: `Unsupported data query kind: ${kind || "missing"}.`
					};
					output.recoveryHints = ["Inspect supportedQueryDescriptors on the current data handle before retrying."];
				}
				return output;
			} catch (error) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the current data handle, target ref, and query kind in describeWorkspace() before retrying.", "Inspect the selected data query descriptor inputSchema/resultSchema/examples if the runtime result shape does not match the declared contract."]
				});
				return buildDataQueryError({
					dataRef,
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the current data handle, target ref, and query kind in describeWorkspace() before retrying.", "Inspect the selected data query descriptor inputSchema/resultSchema/examples if the runtime result shape does not match the declared contract."]
				});
			}
		}
		createContext(call = {}) {
			return new DataQueryContext({
				store: this.store,
				call,
				traceRecorder: this.traceRecorder,
				hostBridge: this.hostBridge
			});
		}
		describeExecutor() {
			const supportedQueryKinds = typeof this.dataQueryEngine?.listSupportedQueryKinds === "function" ? this.dataQueryEngine.listSupportedQueryKinds() : [...DATA_QUERY_KINDS];
			const supportedQueryDescriptors = buildSupportedQueryDescriptors(supportedQueryKinds);
			return makeDataQueryExecutorSummary({
				engine: makeDataQueryExecutorEngineSummary({
					kind: this.dataQueryEngine?.kind || null,
					className: this.dataQueryEngine?.constructor?.name || null
				}),
				counts: makeDataQueryExecutorCounts({
					supportedQueryKindCount: supportedQueryKinds.length,
					supportedQueryDescriptorCount: supportedQueryDescriptors.length
				}),
				capabilities: makeDataQueryExecutorCapabilities({
					schemaValidation: true,
					returnsValidation: true,
					traceRecording: typeof this.traceRecorder?.recordDataQuery === "function",
					runtimeDataReads: typeof this.store?.readRuntimeData === "function"
				}),
				supportedQueryKinds,
				supportedQueryDescriptors
			});
		}
		describeContext() {
			return DataQueryContext.describeContract();
		}
	};
	function normalizeAggregateSpec(spec = {}) {
		const groupBy = Array.isArray(spec.groupBy) ? spec.groupBy : [];
		const measures = Array.isArray(spec.measures) ? spec.measures : Array.isArray(spec.metrics) ? spec.metrics : [];
		return {
			...spec,
			groupBy,
			measures
		};
	}
	function normalizeAggregateResult(result) {
		return {
			rows: Array.isArray(result?.rows) ? result.rows : [],
			rowCount: Number.isFinite(result?.rowCount) ? result.rowCount : Array.isArray(result?.rows) ? result.rows.length : 0
		};
	}
	function buildSummaryMeasures({ fields = [], metrics = [] } = {}) {
		const normalizedFields = Array.isArray(fields) ? fields.filter((field) => typeof field === "string" && field.length > 0) : [];
		const normalizedMetrics = Array.isArray(metrics) ? metrics.filter((metric) => typeof metric === "string" && metric.length > 0) : [];
		const measures = [];
		for (const metric of normalizedMetrics) {
			if (metric === "count") {
				measures.push({
					op: "count",
					as: "count"
				});
				continue;
			}
			for (const field of normalizedFields) measures.push({
				op: metric,
				field,
				as: `${field}_${metric}`
			});
		}
		return measures;
	}
	function normalizeSummarySpec(spec = {}) {
		const measures = Array.isArray(spec.measures) && spec.measures.length > 0 ? spec.measures : buildSummaryMeasures({
			fields: spec.fields,
			metrics: spec.metrics
		});
		return {
			...spec,
			measures
		};
	}
	function normalizeExtremesSpec(spec = {}) {
		const direction = typeof spec.direction === "string" ? spec.direction : null;
		return {
			...spec,
			order: direction === "min" ? "ascending" : "descending"
		};
	}
	function normalizeExtremesResult(result, spec = {}) {
		const direction = typeof spec.direction === "string" ? spec.direction : spec.order === "ascending" ? "min" : "max";
		return {
			field: typeof spec.field === "string" ? spec.field : null,
			direction,
			rows: Array.isArray(result?.rows) ? result.rows : []
		};
	}
	function normalizeOutliersResult(result, spec = {}) {
		return {
			field: typeof spec.field === "string" ? spec.field : null,
			method: typeof spec.method === "string" ? spec.method : "zscore",
			rows: Array.isArray(result?.rows) ? result.rows : [],
			...result?.summary ? { summary: result.summary } : {}
		};
	}
	function normalizeCompareGroupsSpec(spec = {}) {
		if (Array.isArray(spec.groups) && spec.groups.length >= 2) return spec;
		return {
			...spec,
			groups: [spec.leftGroup, spec.rightGroup].filter((value) => value != null)
		};
	}
	function normalizeCompareGroupsResult(result, spec = {}) {
		const groups = Array.isArray(result?.groups) ? result.groups : [];
		const groupField = typeof spec.groupField === "string" ? spec.groupField : null;
		const valueField = typeof spec.valueField === "string" ? spec.valueField : null;
		let comparison = null;
		if (groups.length >= 2) {
			const [left, right] = groups;
			const leftMean = typeof left?.mean === "number" ? left.mean : null;
			const rightMean = typeof right?.mean === "number" ? right.mean : null;
			comparison = {
				groupField,
				valueField,
				leftGroup: left?.group ?? null,
				rightGroup: right?.group ?? null,
				deltaMean: leftMean != null && rightMean != null ? leftMean - rightMean : null
			};
		}
		return {
			groupField,
			valueField,
			groups,
			comparison
		};
	}
	function normalizeCorrelationResult(result, spec = {}) {
		return {
			xField: typeof spec.xField === "string" ? spec.xField : result?.fields?.[0] || null,
			yField: typeof spec.yField === "string" ? spec.yField : result?.fields?.[1] || null,
			correlation: typeof result?.coefficient === "number" ? result.coefficient : null,
			sampleSize: Number.isFinite(result?.sampleSize) ? result.sampleSize : 0
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/RuntimeCoreIntrospector.js
	function describeRuntimeCore({ store, planWorkspace, actionExecutor, perceptionQueryRegistry, dataQueryExecutor, linkEngine, traceRecorder, responseRecorder, agentLoopRuntime } = {}) {
		const countPerceptionHandlers = () => {
			const entries = perceptionQueryRegistry?.handlerEntries;
			if (!(entries instanceof Map)) return 0;
			let count = 0;
			for (const handlerEntries of entries.values()) count += Array.isArray(handlerEntries) ? handlerEntries.length : 0;
			return count;
		};
		const describeDataQueryExecutor = () => typeof dataQueryExecutor?.describeExecutor === "function" ? dataQueryExecutor.describeExecutor() : null;
		const describeActionExecutor = () => typeof actionExecutor?.describeExecutor === "function" ? actionExecutor.describeExecutor() : null;
		const describePerceptionRegistry = () => typeof perceptionQueryRegistry?.describeRegistry === "function" ? perceptionQueryRegistry.describeRegistry() : null;
		const describeWidgetRegistry = () => {
			if (typeof store?.widgetRegistry?.describe === "function") return store.widgetRegistry.describe();
			if (typeof store?.describeWidgetRegistry === "function") return store.describeWidgetRegistry();
			return null;
		};
		const supportsStatePatching = typeof store?.buildStatePatch === "function" || typeof store?.stateManager?.buildStatePatch === "function" || typeof store?.readState === "function" || Boolean(store?.widgets && typeof store.widgets === "object");
		const supportsStateDelta = Boolean(store?.readState?.({ deltaSince: store?.previousState?.stateId || void 0 })?.delta || store?.state?.delta) || typeof store?.stateManager?.createDelta === "function" || Array.isArray(store?.stateSnapshots);
		const hasActionHandler = (actionName) => {
			if (!actionName) return false;
			if (actionExecutor?.handlers instanceof Map) return actionExecutor.handlers.has(actionName);
			if (typeof actionExecutor?.has === "function") return actionExecutor.has(actionName);
			return false;
		};
		const actionExecutorSummary = describeActionExecutor();
		const perceptionRegistrySummary = describePerceptionRegistry();
		const widgetRegistrySummary = describeWidgetRegistry();
		const dataQueryExecutorSummary = describeDataQueryExecutor();
		return makeRuntimeCoreSummary({
			components: makeRuntimeCoreComponents({
				workspacePlanner: typeof planWorkspace === "function",
				widgetRegistry: Boolean(store?.widgetRegistry),
				actionExecutor: Boolean(actionExecutor),
				actionContext: typeof actionExecutor?.describeContext === "function",
				agentLoopRuntime: Boolean(agentLoopRuntime),
				perceptionQueryRegistry: Boolean(perceptionQueryRegistry),
				perceptionContext: typeof perceptionQueryRegistry?.describeContext === "function",
				dataQueryExecutor: Boolean(dataQueryExecutor),
				dataQueryContext: typeof dataQueryExecutor?.describeContext === "function",
				dataQueryEngine: Boolean(dataQueryExecutor?.dataQueryEngine),
				linkEngine: Boolean(linkEngine),
				stateManager: hasStateManagerSurface(store),
				interactionTraceRecorder: Boolean(traceRecorder),
				responseRecorder: Boolean(responseRecorder)
			}),
			registries: makeRuntimeCoreRegistries({
				widgetCount: Number.isFinite(widgetRegistrySummary?.counts?.widgetCount) ? widgetRegistrySummary.counts.widgetCount : 0,
				dataHandleCount: Number.isFinite(widgetRegistrySummary?.counts?.dataHandleCount) ? widgetRegistrySummary.counts.dataHandleCount : 0,
				linkCount: Number.isFinite(widgetRegistrySummary?.counts?.linkCount) ? widgetRegistrySummary.counts.linkCount : 0,
				adapterCount: Number.isFinite(widgetRegistrySummary?.counts?.adapterCount) ? widgetRegistrySummary.counts.adapterCount : 0,
				snapshotCount: Array.isArray(store?.stateSnapshots) ? store.stateSnapshots.length : 0,
				branchCount: listBranchesFromStore(store).length,
				actionDescriptorCount: Number.isFinite(actionExecutorSummary?.counts?.descriptorCount) ? actionExecutorSummary.counts.descriptorCount : 0,
				actionHandlerCount: actionExecutor?.handlers instanceof Map ? actionExecutor.handlers.size : 0,
				actionPreconditionCount: actionExecutor?.preconditionHandlers instanceof Map ? actionExecutor.preconditionHandlers.size : 0,
				perceptionDescriptorCount: Number.isFinite(perceptionRegistrySummary?.counts?.descriptorCount) ? perceptionRegistrySummary.counts.descriptorCount : 0,
				perceptionHandlerCount: countPerceptionHandlers(),
				dataQueryKindCount: typeof dataQueryExecutor?.dataQueryEngine?.listSupportedQueryKinds === "function" ? dataQueryExecutor.dataQueryEngine.listSupportedQueryKinds().length : 0,
				dataQueryDescriptorCount: Number.isFinite(dataQueryExecutorSummary?.counts?.supportedQueryDescriptorCount) ? dataQueryExecutorSummary.counts.supportedQueryDescriptorCount : 0,
				linkPrimitiveCount: linkEngine?.primitiveHandlers instanceof Map ? linkEngine.primitiveHandlers.size : 0,
				traceRecordCount: Array.isArray(store?.interactionTrace) ? store.interactionTrace.length : 0,
				responseCount: Array.isArray(responseRecorder?.responseHistory) ? responseRecorder.responseHistory.length : Array.isArray(store?.responseHistory) ? store.responseHistory.length : 0
			}),
			capabilities: makeRuntimeCoreCapabilities({
				paramsValidation: true,
				perceptionReturnsValidation: Boolean(perceptionRegistrySummary?.capabilities?.returnsValidation),
				dataQueryReturnsValidation: Boolean(dataQueryExecutorSummary?.capabilities?.returnsValidation),
				actionRun: Boolean(actionExecutor),
				perceptionQueryRun: Boolean(perceptionQueryRegistry),
				dataQueryRun: Boolean(dataQueryExecutor),
				workspacePlanning: typeof planWorkspace === "function",
				verifiedActionRun: typeof agentLoopRuntime?.executeVerifiedAction === "function",
				agentLoopContextRead: typeof agentLoopRuntime?.describeStepContext === "function",
				statePatching: supportsStatePatching,
				stateDelta: supportsStateDelta,
				workspaceDescriptionRead: typeof store?.readDescription === "function" || Boolean(store?.appId || store?.workspaceId),
				runtimeStoreRead: typeof store?.describeStore === "function",
				widgetRegistryRead: typeof store?.describeWidgetRegistry === "function" || typeof store?.widgetRegistry?.describe === "function",
				widgetAdapterListRead: typeof store?.listWidgetAdapters === "function" || Boolean(store?.widgetAdapters && typeof store.widgetAdapters === "object"),
				viewRead: typeof store?.readState === "function" || Boolean(store?.widgets && typeof store.widgets === "object"),
				snapshotRead: typeof store?.readSnapshotEntry === "function" || Array.isArray(store?.stateSnapshots),
				stateHistoryRead: typeof store?.listStateHistory === "function" || typeof store?.listStateSnapshots === "function" || Array.isArray(store?.stateSnapshots),
				branchListRead: typeof store?.listBranches === "function" || Boolean(store?.branchRegistry && typeof store.branchRegistry === "object"),
				interactionTraceRead: typeof store?.readTraceWindow === "function" || Array.isArray(store?.interactionTrace),
				traceGraphRead: typeof store?.buildTraceGraph === "function" || Array.isArray(store?.stateSnapshots),
				stateJump: hasActionHandler("workspace.jumpToState"),
				branchCreate: hasActionHandler("workspace.branchFromState"),
				linkPropagationEvaluation: typeof linkEngine?.evaluatePropagation === "function",
				traceRecording: typeof traceRecorder?.recordAction === "function",
				branchReplay: (typeof store?.buildTraceGraph === "function" || Array.isArray(store?.stateSnapshots)) && (typeof store?.beginBranchFromState === "function" || Object.keys(store?.branchRegistry || {}).length > 0),
				answerRecording: typeof responseRecorder?.recordResponse === "function",
				latestResponseRead: typeof responseRecorder?.readLatestResponse === "function" || typeof store?.readLatestResponse === "function" || Array.isArray(store?.responseHistory),
				responseHistoryRead: typeof responseRecorder?.listResponses === "function" || typeof store?.listResponses === "function" || Array.isArray(store?.responseHistory),
				protocolIntrospection: true
			})
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/linkStateModel.js
	function clone$10(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function cloneArray(value) {
		return Array.isArray(value) ? clone$10(value) : [];
	}
	function cloneObject(value) {
		return value && typeof value === "object" && !Array.isArray(value) ? clone$10(value) : {};
	}
	function readLinkDefinitions(shared = {}) {
		return cloneArray(shared?.links?.definitions);
	}
	function readLinkTopologyState(shared = {}) {
		return cloneObject(shared?.links?.topology);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/agentFacingSurface.js
	function clone$9(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function stripCompatibilityFields(value) {
		if (Array.isArray(value)) return value.map((entry) => stripCompatibilityFields(entry));
		if (!value || typeof value !== "object") return value;
		const next = {};
		for (const [key, entryValue] of Object.entries(value)) {
			if (key === "propagationPolicy" || key === "trigger" || key === "automatic") continue;
			next[key] = stripCompatibilityFields(entryValue);
		}
		return next;
	}
	function normalizePublicPropagationEntries(entries = []) {
		return (Array.isArray(entries) ? entries : []).map((entry) => {
			if (!entry || typeof entry !== "object") return entry;
			if (typeof entry.skippedReason !== "string" || entry.skippedReason.length === 0) return entry;
			return {
				...entry,
				skippedReason: normalizeCanonicalPropagationSkipReason(entry.skippedReason, "mappingFailed")
			};
		});
	}
	function normalizePublicPropagationEvaluation(evaluation = null) {
		if (!evaluation || typeof evaluation !== "object") return evaluation;
		const results = Array.isArray(evaluation.results) ? evaluation.results : [];
		return {
			...evaluation,
			results: results.map((entry) => {
				if (!entry || typeof entry !== "object") return entry;
				if (typeof entry.reason !== "string" || entry.reason.length === 0) return entry;
				return {
					...entry,
					reason: normalizeCanonicalPropagationSkipReason(entry.reason, "mappingFailed")
				};
			})
		};
	}
	function uniqueStrings(values = []) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => typeof value === "string" && value.length > 0))];
	}
	function buildCoordinationStateFromWorkspaceState(state = {}, { currentBranchId = null, derivedTopology = {} } = {}) {
		const shared = state?.shared || {};
		return {
			stateId: state?.stateId || null,
			branchId: state?.branchId || currentBranchId || null,
			focusedWidgetRef: shared?.focusedWidget || null,
			selections: {
				registry: readSelectionRegistry(shared),
				views: {
					primary: readSelectionPrimaryView(shared),
					byWidget: readSelectionByWidgetView(shared)
				}
			},
			focus: readFocusState(shared, state?.widgets || {}),
			highlight: deriveHighlightState(state),
			viewport: readViewportState(shared),
			globalFilters: clone$9(shared?.globalFilters || {}),
			annotations: clone$9(state?.annotations || shared?.annotations || []),
			links: {
				definitions: readLinkDefinitions(shared),
				topology: (() => {
					const sharedTopology = readLinkTopologyState(shared);
					return Object.keys(sharedTopology).length > 0 ? sharedTopology : clone$9(derivedTopology || {});
				})()
			}
		};
	}
	function listAvailableActionsFromDescription(description = {}) {
		return clone$9(Array.isArray(description?.actions) ? description.actions : []);
	}
	function listAvailablePerceptionsFromDescription(description = {}) {
		return clone$9(Array.isArray(description?.perceptionQueries) ? description.perceptionQueries : []);
	}
	function buildPropagationSummary({ state = {}, description = {}, linkEngine = null, sourceRef = null } = {}) {
		const shared = state?.shared || {};
		const registry = readSelectionRegistry(shared);
		const primarySelection = readSelectionPrimaryView(shared);
		const engineSummary = typeof linkEngine?.describeEngine === "function" ? clone$9(linkEngine.describeEngine()) : null;
		const candidateSourceRefs = uniqueStrings([
			sourceRef,
			primarySelection?.selectionRef || null,
			...Object.keys(registry || {}),
			shared?.focusedWidget || null,
			...(engineSummary?.links || []).map((entry) => entry?.sourceRef || null)
		]);
		const resolvedSourceRef = sourceRef || candidateSourceRefs[0] || null;
		const propagation = resolvedSourceRef && typeof linkEngine?.describePropagation === "function" ? normalizePublicPropagationEntries(stripCompatibilityFields(clone$9(linkEngine.describePropagation({ sourceRef: resolvedSourceRef })))) : [];
		const evaluation = resolvedSourceRef && typeof linkEngine?.evaluatePropagation === "function" ? normalizePublicPropagationEvaluation(stripCompatibilityFields(clone$9(linkEngine.evaluatePropagation({
			sourceRef: resolvedSourceRef,
			state
		})))) : null;
		return {
			sourceRef: resolvedSourceRef,
			candidateSourceRefs,
			topology: clone$9(description?.runtimeTopology || engineSummary?.topology || null),
			linkSummary: stripCompatibilityFields(engineSummary),
			propagation,
			evaluation
		};
	}
	function buildRuntimeObservation({ description = {}, state = {}, coordinationState = null, availableActions = [], availablePerceptions = [], propagationSummary = null, latestCoordinationResult = null } = {}) {
		return {
			workspace: clone$9(description),
			state: clone$9(state),
			coordination: coordinationState ? clone$9(coordinationState) : null,
			availableActions: clone$9(availableActions),
			availablePerceptions: clone$9(availablePerceptions),
			propagation: propagationSummary ? clone$9(propagationSummary) : null,
			latestCoordinationResult: clone$9(latestCoordinationResult)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/installPagePort.js
	function clone$8(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function resolveWidgetAdapterSummaries(store, description) {
		return Array.isArray(description?.widgetAdapters) && description.widgetAdapters.length > 0 ? clone$8(description.widgetAdapters) : (typeof store?.listWidgetAdapters === "function" ? store.listWidgetAdapters() : valuesFromRecord(store?.widgetAdapters)).map((item) => summarizeWidgetAdapter(item)).filter(Boolean);
	}
	function installWidgetVAPagePort({ store, planWorkspace, executeAction, actionContext, readLatestCoordinationResult, queryPerception, queryData, actionExecutor, perceptionQueryRegistry, dataQueryExecutor, linkEngine, traceRecorder, responseRecorder, agentLoopRuntime }) {
		const executeActionFn = typeof executeAction === "function" ? executeAction : typeof actionExecutor?.run === "function" ? (call) => actionExecutor.run(call, actionContext || null) : null;
		const queryPerceptionFn = typeof queryPerception === "function" ? queryPerception : typeof perceptionQueryRegistry?.run === "function" ? (call) => perceptionQueryRegistry.run(call) : typeof perceptionQueryRegistry?.query === "function" ? (call) => perceptionQueryRegistry.query(call) : null;
		const queryDataFn = typeof queryData === "function" ? queryData : typeof dataQueryExecutor?.run === "function" ? (call) => dataQueryExecutor.run(call) : null;
		const hasAgentLoopMethod = (methodName) => typeof agentLoopRuntime?.[methodName] === "function";
		const hasStoreMethod = (methodName) => typeof store?.[methodName] === "function";
		const hasActionExecutorMethod = (methodName) => typeof actionExecutor?.[methodName] === "function";
		const hasPerceptionRegistryMethod = (methodName) => typeof perceptionQueryRegistry?.[methodName] === "function";
		const hasDataQueryExecutorMethod = (methodName) => typeof dataQueryExecutor?.[methodName] === "function";
		const hasLinkEngineMethod = (methodName) => typeof linkEngine?.[methodName] === "function";
		const hasTraceRecorderMethod = (methodName) => typeof traceRecorder?.[methodName] === "function";
		const hasResponseRecorderMethod = (methodName) => typeof responseRecorder?.[methodName] === "function";
		Array.isArray(store?.interactionTrace);
		const plannerInstalled = typeof planWorkspace === "function";
		const hasActionHandler = (actionName) => {
			if (!actionName) return false;
			if (typeof actionExecutor?.has === "function") return actionExecutor.has(actionName);
			if (actionExecutor?.handlers instanceof Map) return actionExecutor.handlers.has(actionName);
			return false;
		};
		const agentLoopInstalled = hasAgentLoopMethod("describeStepContext") || hasAgentLoopMethod("executeVerifiedAction");
		const installedPagePortMethods = new Set([
			["describePagePort", true],
			["describeWorkspace", hasStoreMethod("readDescription") || Boolean(store?.appId || store?.workspaceId)],
			["parseRef", true],
			["describeRuntimeCore", true],
			["describeRuntimeStore", hasStoreMethod("describeStore")],
			["describeWidgetRegistry", hasStoreMethod("describeWidgetRegistry")],
			["describeActionExecutor", hasActionExecutorMethod("describeExecutor")],
			["describeActionContext", hasActionExecutorMethod("describeContext")],
			["describeActionUsage", hasActionExecutorMethod("describeActionUsage")],
			["describePerceptionRegistry", hasPerceptionRegistryMethod("describeRegistry")],
			["describePerceptionContext", hasPerceptionRegistryMethod("describeContext")],
			["describeDataQueryExecutor", hasDataQueryExecutorMethod("describeExecutor")],
			["describeDataQueryContext", hasDataQueryExecutorMethod("describeContext")],
			["describeDataQueryEngine", typeof dataQueryExecutor?.dataQueryEngine?.describeEngine === "function"],
			["describeStateManager", hasStateManagerSurface(store)],
			["describeTraceRecorder", hasTraceRecorderMethod("describeRecorder")],
			["describeResponseRecorder", hasResponseRecorderMethod("describeRecorder")],
			["describeLinkEngine", hasLinkEngineMethod("describeEngine")],
			["planWorkspace", plannerInstalled],
			["describeAgentLoop", hasAgentLoopMethod("describeStepContext")],
			["listWidgetAdapters", hasStoreMethod("listWidgetAdapters") || Boolean(store?.widgetAdapters && typeof store.widgetAdapters === "object")],
			["readObservation", Boolean(store)],
			["readCoordinationState", Boolean(store)],
			["readPropagationSummary", Boolean(store)],
			["readLatestCoordinationResult", typeof readLatestCoordinationResult === "function"],
			["listAvailableActions", Boolean(store)],
			["listAvailablePerceptions", Boolean(store)],
			["readState", Boolean(store)],
			["readView", Boolean(store)],
			["readSnapshot", hasStoreMethod("readSnapshotEntry") || Array.isArray(store?.stateSnapshots)],
			["listStateHistory", hasStoreMethod("listStateSnapshots") || Array.isArray(store?.stateSnapshots)],
			["listBranches", hasStoreMethod("listBranches") || Boolean(store?.branchRegistry && typeof store.branchRegistry === "object")],
			["executeAction", typeof executeActionFn === "function"],
			["executeVerifiedAction", hasAgentLoopMethod("executeVerifiedAction")],
			["jumpToState", typeof executeActionFn === "function" && hasActionHandler("workspace.jumpToState")],
			["branchFromState", typeof executeActionFn === "function" && hasActionHandler("workspace.branchFromState")],
			["queryPerception", typeof queryPerceptionFn === "function"],
			["runDataQuery", typeof queryDataFn === "function"],
			["queryData", typeof queryDataFn === "function"],
			["readTrace", Boolean(store)],
			["getInteractionTrace", Boolean(store)],
			["replay", typeof executeActionFn === "function" && hasActionHandler("workspace.jumpToState")],
			["getTraceGraph", hasStoreMethod("buildTraceGraph") || Array.isArray(store?.stateSnapshots)],
			["getLatestAgentResponse", hasStoreMethod("readLatestResponse") || Array.isArray(store?.responseHistory)],
			["listAgentResponses", hasStoreMethod("listResponses") || Array.isArray(store?.responseHistory)],
			["evaluateLinkPropagation", hasLinkEngineMethod("evaluatePropagation")],
			["recordAgentResponse", hasResponseRecorderMethod("recordResponse")]
		].filter(([, installed]) => installed).map(([methodName]) => methodName));
		const installedPagePortAliases = new Set(Object.entries(PAGE_PORT_ALIASES).filter(([, methodName]) => installedPagePortMethods.has(methodName)).map(([alias]) => alias));
		const port = {
			async describePagePort() {
				const description = describePagePortCapabilities({
					widgetAdapterIntrospection: true,
					runtimeCoreIntrospection: true,
					planner: plannerInstalled,
					agentLoop: agentLoopInstalled
				});
				const methods = description.methods.filter((methodName) => installedPagePortMethods.has(methodName));
				const methodSet = new Set(methods);
				const aliases = Object.fromEntries(Object.entries(description.aliases || {}).filter(([, methodName]) => methodSet.has(methodName)));
				const methodDescriptors = Object.fromEntries(Object.entries(description.methodDescriptors || {}).filter(([methodName]) => methodSet.has(methodName)));
				return {
					...description,
					methods,
					aliases,
					methodDescriptors,
					schemas: describePagePortSchemasForMethods(methods),
					transportHints: {
						...description.transportHints,
						recommendedTools: Array.isArray(description.transportHints?.recommendedTools) ? description.transportHints.recommendedTools.filter((alias) => installedPagePortAliases.has(alias)) : [],
						optionalTools: Array.isArray(description.transportHints?.optionalTools) ? description.transportHints.optionalTools.filter((alias) => installedPagePortAliases.has(alias)) : []
					}
				};
			},
			async describeWorkspace(options = {}) {
				const description = readWorkspaceDescriptionFromStore(store, {
					actionExecutor,
					perceptionQueryRegistry
				});
				const includeSchemas = options.includeSchemas !== false;
				const includeExamples = options.includeExamples !== false;
				const includeProtocolSchemas = options.includeProtocolSchemas === true;
				const actions = (Array.isArray(description?.actions) && description.actions.length > 0 ? description.actions : listStoreActions(store, actionExecutor)).map((action) => {
					const next = clone$8(action);
					if (!includeSchemas) delete next.paramsSchema;
					if (!includeExamples) delete next.examples;
					return next;
				});
				const dataHandles = (Array.isArray(description?.dataHandles) && description.dataHandles.length > 0 ? description.dataHandles : listStoreDataHandles(store)).map((handle) => {
					const next = clone$8(handle);
					if (!includeSchemas) delete next.schema;
					if (Array.isArray(next.supportedQueryDescriptors)) next.supportedQueryDescriptors = next.supportedQueryDescriptors.map((descriptor) => {
						const queryDescriptor = clone$8(descriptor);
						if (!includeSchemas) delete queryDescriptor.inputSchema;
						if (!includeSchemas) delete queryDescriptor.resultSchema;
						if (!includeExamples) delete queryDescriptor.examples;
						return queryDescriptor;
					});
					return next;
				});
				const perceptionQueries = (Array.isArray(description?.perceptionQueries) && description.perceptionQueries.length > 0 ? description.perceptionQueries : listStorePerceptionQueries(store, perceptionQueryRegistry)).map((item) => {
					const next = clone$8(item);
					if (!includeExamples) delete next.examples;
					if (!includeSchemas) delete next.paramsSchema;
					if (!includeSchemas) delete next.returnsSchema;
					return next;
				});
				const workspaceDescription = clone$8(description);
				if (workspaceDescription?.transportHints) workspaceDescription.transportHints = {
					...workspaceDescription.transportHints,
					recommendedTools: Array.isArray(workspaceDescription.transportHints?.recommendedTools) ? workspaceDescription.transportHints.recommendedTools.filter((alias) => installedPagePortAliases.has(alias)) : [],
					optionalTools: Array.isArray(workspaceDescription.transportHints?.optionalTools) ? workspaceDescription.transportHints.optionalTools.filter((alias) => installedPagePortAliases.has(alias)) : []
				};
				return {
					...workspaceDescription,
					widgets: Array.isArray(description?.widgets) && description.widgets.length > 0 ? clone$8(description.widgets) : listStoreWidgets(store).map((item) => clone$8(item)),
					widgetAdapters: resolveWidgetAdapterSummaries(store, description),
					dataHandles,
					links: Array.isArray(description?.links) && description.links.length > 0 ? clone$8(description.links) : listStoreLinks(store).map((item) => clone$8(item)),
					actions,
					perceptionQueries,
					...includeProtocolSchemas ? { __schemas: describePagePortSchemasForMethods(Array.from(installedPagePortMethods)) } : {}
				};
			},
			async parseRef(options = {}) {
				return parseRef(options?.ref);
			},
			async describeRuntimeCore() {
				return describeRuntimeCore({
					store,
					planWorkspace,
					actionExecutor,
					perceptionQueryRegistry,
					dataQueryExecutor,
					linkEngine,
					traceRecorder,
					responseRecorder,
					agentLoopRuntime
				});
			},
			async describeRuntimeStore() {
				return store.describeStore();
			},
			async describeWidgetRegistry() {
				return store.describeWidgetRegistry();
			},
			async describeActionExecutor() {
				return actionExecutor.describeExecutor();
			},
			async describeActionContext() {
				return actionExecutor.describeContext();
			},
			async describeActionUsage(options = {}) {
				return actionExecutor.describeActionUsage(options, actionContext || null);
			},
			async describePerceptionRegistry() {
				return perceptionQueryRegistry.describeRegistry();
			},
			async describePerceptionContext() {
				return perceptionQueryRegistry.describeContext();
			},
			async describeDataQueryExecutor() {
				return dataQueryExecutor.describeExecutor();
			},
			async describeDataQueryContext() {
				return dataQueryExecutor.describeContext();
			},
			async describeDataQueryEngine() {
				return dataQueryExecutor?.dataQueryEngine?.describeEngine?.() || null;
			},
			async describeStateManager() {
				return describeStateManagerFromStore(store);
			},
			async describeTraceRecorder() {
				return traceRecorder.describeRecorder();
			},
			async describeResponseRecorder() {
				return responseRecorder.describeRecorder();
			},
			async describeLinkEngine() {
				return linkEngine.describeEngine();
			},
			async planWorkspace(options = {}) {
				return planWorkspace(options);
			},
			async describeAgentLoop(options = {}) {
				return agentLoopRuntime.describeStepContext(options);
			},
			async listWidgetAdapters() {
				return resolveWidgetAdapterSummaries(store, readWorkspaceDescriptionFromStore(store, {
					actionExecutor,
					perceptionQueryRegistry
				}));
			},
			async readObservation(options = {}) {
				const description = readWorkspaceDescriptionFromStore(store, {
					actionExecutor,
					perceptionQueryRegistry
				});
				const state = readWorkspaceStateFromStore(store, options.readStateOptions || options);
				return buildRuntimeObservation({
					description,
					state,
					coordinationState: buildCoordinationStateFromWorkspaceState(state, {
						currentBranchId: store?.currentBranchId || null,
						derivedTopology: description?.runtimeTopology || {}
					}),
					availableActions: listAvailableActionsFromDescription(description),
					availablePerceptions: listAvailablePerceptionsFromDescription(description),
					propagationSummary: buildPropagationSummary({
						state,
						description,
						linkEngine,
						sourceRef: options?.propagationOptions?.sourceRef || options?.sourceRef || null
					}),
					latestCoordinationResult: typeof readLatestCoordinationResult === "function" ? await readLatestCoordinationResult() : null
				});
			},
			async readCoordinationState() {
				const description = readWorkspaceDescriptionFromStore(store, {
					actionExecutor,
					perceptionQueryRegistry
				});
				return buildCoordinationStateFromWorkspaceState(readWorkspaceStateFromStore(store), {
					currentBranchId: store?.currentBranchId || null,
					derivedTopology: description?.runtimeTopology || {}
				});
			},
			async readPropagationSummary(options = {}) {
				return buildPropagationSummary({
					state: readWorkspaceStateFromStore(store),
					description: readWorkspaceDescriptionFromStore(store, {
						actionExecutor,
						perceptionQueryRegistry
					}),
					linkEngine,
					sourceRef: options?.sourceRef || null
				});
			},
			async readLatestCoordinationResult() {
				if (typeof readLatestCoordinationResult !== "function") return null;
				return clone$8(await readLatestCoordinationResult());
			},
			async listAvailableActions() {
				return listAvailableActionsFromDescription(readWorkspaceDescriptionFromStore(store, {
					actionExecutor,
					perceptionQueryRegistry
				}));
			},
			async listAvailablePerceptions() {
				return listAvailablePerceptionsFromDescription(readWorkspaceDescriptionFromStore(store, {
					actionExecutor,
					perceptionQueryRegistry
				}));
			},
			async readView(options = {}) {
				return readWorkspaceStateFromStore(store, options);
			},
			async readState(options = {}) {
				return readWorkspaceStateFromStore(store, options);
			},
			async readSnapshot(options = {}) {
				return options.stateId ? readSnapshotFromStore(store, options.stateId, options) : null;
			},
			async listStateHistory(options = {}) {
				return listStateSnapshotsFromStore(store, {
					limit: options.limit || 50,
					sinceStateId: options.sinceStateId || null,
					actors: options.actors || []
				});
			},
			async listBranches() {
				return listBranchesFromStore(store);
			},
			async executeAction(call) {
				return executeActionFn(call);
			},
			async executeVerifiedAction(call, options = {}) {
				return agentLoopRuntime.executeVerifiedAction(call, options);
			},
			async jumpToState(options = {}) {
				return executeActionFn({
					callId: options.callId || `jump_${Date.now()}`,
					name: "workspace.jumpToState",
					actor: options.actor || "agent",
					params: { stateId: options.stateId }
				});
			},
			async branchFromState(options = {}) {
				return executeActionFn({
					callId: options.callId || `branch_${Date.now()}`,
					name: "workspace.branchFromState",
					actor: options.actor || "agent",
					params: {
						stateId: options.stateId,
						branchLabel: options.branchLabel
					}
				});
			},
			async queryPerception(call) {
				return queryPerceptionFn(call);
			},
			async runDataQuery(call) {
				return queryDataFn(call);
			},
			async queryData(call) {
				return queryDataFn(call);
			},
			async readTrace(options = {}) {
				return readInteractionTraceFromStore(store, options);
			},
			async getInteractionTrace(options = {}) {
				return readInteractionTraceFromStore(store, options);
			},
			async replay(options = {}) {
				return executeActionFn({
					callId: options.callId || `replay_${Date.now()}`,
					name: "workspace.jumpToState",
					actor: options.actor || "agent",
					params: { stateId: options.stateId }
				});
			},
			async getTraceGraph(options = {}) {
				return buildTraceGraphFromStore(store, {
					limit: options.limit || 50,
					sinceStateId: options.sinceStateId || null,
					actors: options.actors || []
				});
			},
			async getLatestAgentResponse(options = {}) {
				return readLatestResponseFromStore(store, { workspaceId: options.workspaceId || null });
			},
			async listAgentResponses(options = {}) {
				return listResponsesFromStore(store, options.limit || 20, { workspaceId: options.workspaceId || null });
			},
			async evaluateLinkPropagation(options = {}) {
				return linkEngine.evaluatePropagation({ sourceRef: options.sourceRef });
			},
			async recordAgentResponse(record = {}) {
				return responseRecorder.recordResponse(record);
			}
		};
		for (const methodName of PAGE_PORT_METHODS) if (!installedPagePortMethods.has(methodName)) delete port[methodName];
		for (const [alias, methodName] of Object.entries(PAGE_PORT_ALIASES)) if (typeof port[methodName] === "function") port[alias] = port[methodName];
		window.__widgetVA = port;
		return () => {
			if (window.__widgetVA === port) delete window.__widgetVA;
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/InteractionTraceRecorder.js
	var InteractionTraceRecorder = class {
		constructor({ store }) {
			this.store = store;
		}
		buildLineageFields(stateId) {
			const snapshot = stateId ? readSnapshotEntryFromStore(this.store, stateId) : readCurrentSnapshotMetaFromStore(this.store);
			return {
				parentStateId: snapshot?.parentStateId || null,
				branchId: snapshot?.branchId || null
			};
		}
		appendTrace(record) {
			return appendTraceRecordToStore(this.store, record);
		}
		recordAction({ call, updatedRefs, stateId, statePatch, primitive, notes }) {
			const lineage = this.buildLineageFields(stateId);
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "action",
				action: call,
				primitive: primitive || null,
				affectedRefs: updatedRefs,
				stateId,
				statePatch,
				...lineage,
				notes: {
					outcome: "success",
					...notes || {}
				}
			}));
		}
		recordActionFailure({ call, primitive = null, code, message, details, recoveryHints = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			const targetRef = readNormalizedQueryScope({ call }).widgetRef || null;
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "action",
				action: call,
				primitive,
				affectedRefs: targetRef ? [targetRef] : [],
				stateId: currentStateId,
				statePatch: {},
				...lineage,
				notes: {
					outcome: "failure",
					errorCode: code || "RUNTIME_ERROR",
					errorMessage: message || "Action execution failed.",
					...details !== void 0 ? { details } : {},
					...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {},
					...notes || {}
				}
			}));
		}
		recordQuery({ call, affectedRefs = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "perceptionQuery",
				query: call,
				affectedRefs,
				stateId: currentStateId,
				...lineage,
				notes: {
					outcome: "success",
					...notes || {}
				}
			}));
		}
		recordQueryFailure({ call, code, message, details, recoveryHints = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			const targetRef = readNormalizedQueryScope({ call }).widgetRef || null;
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "perceptionQuery",
				query: call,
				affectedRefs: targetRef ? [targetRef] : [],
				stateId: currentStateId,
				statePatch: {},
				...lineage,
				notes: {
					outcome: "failure",
					errorCode: code || "RUNTIME_ERROR",
					errorMessage: message || "Perception query execution failed.",
					...details !== void 0 ? { details } : {},
					...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {},
					...notes || {}
				}
			}));
		}
		recordDataQuery({ call, affectedRefs = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "dataQuery",
				query: call,
				affectedRefs,
				stateId: currentStateId,
				...lineage,
				notes: {
					outcome: "success",
					...notes || {}
				}
			}));
		}
		recordDataQueryFailure({ call, code, message, details, recoveryHints = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			const queryScope = readNormalizedQueryScope({
				call,
				querySpec: call?.query?.spec || null
			});
			const dataRef = queryScope.dataRef || call?.dataRef || null;
			const affectedRefs = [queryScope.widgetRef || null, dataRef].filter(Boolean);
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "dataQuery",
				query: call,
				affectedRefs,
				stateId: currentStateId,
				statePatch: {},
				...lineage,
				notes: {
					outcome: "failure",
					errorCode: code || "RUNTIME_ERROR",
					errorMessage: message || "Data query execution failed.",
					...details !== void 0 ? { details } : {},
					...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {},
					...notes || {}
				}
			}));
		}
		recordSystemTransition({ actor = "system", transitionType = "continue", stateId, affectedRefs = [], statePatch = {}, notes }) {
			const currentStateId = stateId || readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			this.appendTrace(makeInteractionTraceRecord({
				actor,
				eventKind: "systemTransition",
				primitive: transitionType,
				affectedRefs,
				stateId: currentStateId,
				statePatch,
				...lineage,
				notes
			}));
		}
		describeRecorder() {
			const trace = Array.isArray(this.store?.interactionTrace) ? this.store.interactionTrace : [];
			const latestRecord = trace[trace.length - 1] || null;
			return makeTraceRecorderSummary({
				capabilities: makeTraceRecorderCapabilities({
					recordsActions: true,
					recordsPerceptionQueries: true,
					recordsPerceptionQueryFailures: true,
					recordsDataQueries: true,
					recordsDataQueryFailures: true,
					normalizedQueryFamily: true,
					recordsSystemTransitions: true,
					lineageTracking: true
				}),
				counters: makeTraceRecorderCounters({
					traceCount: trace.length,
					latestStateId: latestRecord?.stateId || null,
					latestBranchId: latestRecord?.branchId || null,
					latestEventKind: latestRecord?.eventKind || null,
					latestEventFamily: latestRecord?.eventFamily || null,
					latestQuerySurface: latestRecord?.querySurface || null,
					latestOutcome: latestRecord?.notes?.outcome || null
				}),
				eventKinds: [
					"action",
					"perceptionQuery",
					"dataQuery",
					"systemTransition"
				],
				eventFamilies: [
					"action",
					"query",
					"systemTransition"
				],
				querySurfaces: ["perception", "data"]
			});
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/linkSemantics.js
	function clone$7(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readLinkActivationPolicy(link) {
		return normalizeWidgetLink(link)?.activationPolicy === "manual" ? "manual" : "automatic";
	}
	function readLinkEffectConstraint(link) {
		const normalizedLink = normalizeWidgetLink(link);
		return normalizedLink?.effectConstraint === "highlightOnly" || normalizedLink?.effectConstraint === "focusOnly" ? normalizedLink.effectConstraint : null;
	}
	function readLinkResponseSpec(link) {
		const normalizedLink = normalizeWidgetLink(link);
		return normalizedLink?.responseSpec && typeof normalizedLink.responseSpec === "object" ? clone$7(normalizedLink.responseSpec) : null;
	}
	function readDeclaredLinkEffect(link) {
		const normalizedLink = normalizeWidgetLink(link);
		if (typeof normalizedLink?.effect === "string" && normalizedLink.effect.length > 0) return normalizedLink.effect;
		if (normalizedLink?.kind === "filter") return "applyFilter";
		if (normalizedLink?.kind === "highlight") return "applyHighlight";
		if (normalizedLink?.kind === "syncDomain") return "syncDomain";
		if (normalizedLink?.kind === "sharesSelection") return "shareSelection";
		if (normalizedLink?.kind === "drillDown" || normalizedLink?.kind === "reencode") return "transformView";
		if (normalizedLink?.kind === "aggregate") return "transformDataView";
		if (normalizedLink?.kind === "structure") return "transformStructure";
		return null;
	}
	function readAppliedLinkEffect(link) {
		const effectConstraint = readLinkEffectConstraint(link);
		if (effectConstraint === "highlightOnly") return "applyHighlight";
		if (effectConstraint === "focusOnly") return "focusTarget";
		return readDeclaredLinkEffect(link);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/LinkEngine.js
	function clone$6(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function arraysEqual(left, right) {
		return JSON.stringify(left ?? null) === JSON.stringify(right ?? null);
	}
	function uniqueValues(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => value != null))];
	}
	function stableKey(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	function updateEncodingDomain(encoding, channel, domain) {
		const channelSpec = encoding?.[channel];
		if (!channelSpec || typeof channelSpec !== "object") return channelSpec;
		const nextScale = { ...channelSpec.scale || {} };
		if (Array.isArray(domain)) nextScale.domain = domain;
		else delete nextScale.domain;
		return {
			...channelSpec,
			scale: nextScale
		};
	}
	function categoricalValuesFromSelection(selection) {
		return uniqueValues((Array.isArray(selection?.predicates) ? selection.predicates : []).filter((predicate) => predicate?.op === "equals" || predicate?.op === "in").flatMap((predicate) => Array.isArray(predicate?.value) ? predicate.value : [predicate?.value]));
	}
	function markRows(rows, marker, predicate) {
		return (Array.isArray(rows) ? rows : []).map((row) => ({
			...row,
			[marker]: Boolean(predicate?.(row))
		}));
	}
	function countRowsMatching(rows, predicate) {
		if (!Array.isArray(rows) || typeof predicate !== "function") return 0;
		return rows.filter((row) => predicate(row)).length;
	}
	function findNamedDataSource(spec, name) {
		const data = Array.isArray(spec?.data) ? spec.data : [];
		const index = data.findIndex((entry) => entry && typeof entry === "object" && entry.name === name);
		if (index < 0) return {
			index: -1,
			values: null
		};
		return {
			index,
			values: Array.isArray(data[index]?.values) ? data[index].values : null
		};
	}
	function computeNodeFlows(links) {
		const flows = /* @__PURE__ */ new Map();
		const ensure = (name) => {
			if (!flows.has(name)) flows.set(name, {
				inflow: 0,
				outflow: 0,
				total: 0
			});
			return flows.get(name);
		};
		for (const link of Array.isArray(links) ? links : []) {
			if (!link || typeof link !== "object") continue;
			const source = link.source;
			const target = link.target;
			const value = Number(link.value || 0);
			const sourceEntry = ensure(source);
			const targetEntry = ensure(target);
			sourceEntry.outflow += value;
			targetEntry.inflow += value;
		}
		for (const entry of flows.values()) entry.total = Math.max(entry.inflow, entry.outflow);
		return flows;
	}
	function normalizeFieldName(value) {
		return typeof value === "string" ? value.trim().toLowerCase() : null;
	}
	function readScalarPredicateValue(selection, candidateFields = []) {
		const predicates = Array.isArray(selection?.predicates) ? selection.predicates : [];
		const normalizedCandidates = candidateFields.map((field) => normalizeFieldName(field)).filter(Boolean);
		if (normalizedCandidates.length === 0) return null;
		const predicate = predicates.find((entry) => normalizedCandidates.includes(normalizeFieldName(entry?.field)));
		if (!predicate) return null;
		if (predicate.op === "equals") return predicate.value;
		if (predicate.op === "in" && Array.isArray(predicate.value) && predicate.value.length === 1) return predicate.value[0];
		return null;
	}
	function buildLineDrillDownSpec(spec, params = {}) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("Line drill-down requires an object spec.");
		const level = typeof params.level === "string" ? params.level.toLowerCase().trim() : null;
		const numericValue = Number.isFinite(params.value) ? Number(params.value) : null;
		const parent = params.parent && typeof params.parent === "object" && !Array.isArray(params.parent) ? params.parent : {};
		if (!level || numericValue === null) throw new Error("Line drill-down requires level and numeric value.");
		const drillState = spec._line_drilldown_state && typeof spec._line_drilldown_state === "object" ? { ...spec._line_drilldown_state } : {};
		if (!drillState.original_transform) {
			drillState.original_transform = Array.isArray(spec.transform) ? clone$6(spec.transform) : [];
			drillState.original_encoding = clone$6(spec.encoding || {});
			drillState.original_title = spec.title || "";
			drillState.raw_time_field = drillState.raw_time_field || spec?.encoding?.x?.field || null;
			drillState.raw_value_field = drillState.raw_value_field || spec?.encoding?.y?.field || null;
			drillState.group_field = drillState.group_field || spec?.encoding?.color?.field || null;
		}
		const rawDateField = drillState.raw_time_field;
		const rawValueField = drillState.raw_value_field;
		const groupField = drillState.group_field;
		if (!rawDateField || !rawValueField) throw new Error("Line drill-down requires x/y fields on the active spec.");
		const nextTransforms = [];
		let nextEncoding;
		let nextParent = {};
		let titleSuffix = "";
		if (level === "year") {
			nextTransforms.push({
				filter: `year(datum.${rawDateField}) == ${numericValue}`,
				_widgetvaTag: "line.drillDownXAxis"
			});
			nextTransforms.push({
				timeUnit: "yearmonth",
				field: rawDateField,
				as: "month_date",
				_widgetvaTag: "line.drillDownXAxis"
			});
			nextTransforms.push({
				aggregate: [{
					op: "sum",
					field: rawValueField,
					as: "total_value"
				}],
				groupby: groupField ? ["month_date", groupField] : ["month_date"],
				_widgetvaTag: "line.drillDownXAxis"
			});
			nextParent = { year: numericValue };
			titleSuffix = `${numericValue} monthly trend`;
			nextEncoding = {
				x: {
					field: "month_date",
					type: "temporal",
					title: "Month",
					axis: { format: "%Y-%m" }
				},
				y: {
					field: "total_value",
					type: "quantitative",
					title: `Monthly total ${rawValueField}`
				},
				...groupField ? { color: drillState.original_encoding?.color || {
					field: groupField,
					type: "nominal"
				} } : {}
			};
		} else if (level === "month") {
			const yearValue = Number.isFinite(parent.year) ? Number(parent.year) : null;
			if (yearValue === null) throw new Error("Line month drill-down requires parent.year.");
			if (numericValue < 1 || numericValue > 12) throw new Error("Line month drill-down requires a month value between 1 and 12.");
			nextTransforms.push({
				filter: `year(datum.${rawDateField}) == ${yearValue} && month(datum.${rawDateField}) == ${numericValue - 1}`,
				_widgetvaTag: "line.drillDownXAxis"
			});
			nextTransforms.push({
				timeUnit: "yearmonthdate",
				field: rawDateField,
				as: "day_date",
				_widgetvaTag: "line.drillDownXAxis"
			});
			nextTransforms.push({
				aggregate: [{
					op: "sum",
					field: rawValueField,
					as: "total_value"
				}],
				groupby: groupField ? ["day_date", groupField] : ["day_date"],
				_widgetvaTag: "line.drillDownXAxis"
			});
			nextParent = {
				year: yearValue,
				month: numericValue
			};
			titleSuffix = `${yearValue}-${String(numericValue).padStart(2, "0")} daily trend`;
			nextEncoding = {
				x: {
					field: "day_date",
					type: "temporal",
					title: "Date",
					axis: { format: "%m-%d" }
				},
				y: {
					field: "total_value",
					type: "quantitative",
					title: `Daily total ${rawValueField}`
				},
				...groupField ? { color: drillState.original_encoding?.color || {
					field: groupField,
					type: "nominal"
				} } : {}
			};
		} else throw new Error(`Line drill-down does not support level "${level}".`);
		drillState.parent = nextParent;
		return {
			...spec,
			transform: nextTransforms,
			encoding: nextEncoding,
			title: titleSuffix,
			_line_drilldown_state: drillState
		};
	}
	function buildHeatmapDrillDownSpec(spec, params = {}) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("Heatmap drill-down requires an object spec.");
		const level = typeof params.level === "string" ? params.level.toLowerCase().trim() : null;
		const value = params.value;
		const parent = params.parent && typeof params.parent === "object" && !Array.isArray(params.parent) ? params.parent : {};
		if (!level || value == null || value === "") throw new Error("Heatmap drill-down requires level and value.");
		const xEncoding = spec?.encoding?.x;
		const timeField = xEncoding?.field || null;
		const xType = xEncoding?.type || null;
		if (!timeField) throw new Error("Heatmap drill-down requires a temporal x field on the active spec.");
		if (xType && xType !== "temporal") throw new Error(`Heatmap drill-down requires encoding.x.type=temporal, received ${xType}.`);
		const heatmapState = spec._heatmap_state && typeof spec._heatmap_state === "object" ? { ...spec._heatmap_state } : {};
		if (!heatmapState.original_x_encoding) heatmapState.original_x_encoding = clone$6(xEncoding);
		const nextTransforms = Array.isArray(spec.transform) ? spec.transform.filter((transform) => transform?._widgetvaTag !== "heatmap.drilldownAxis") : [];
		const mergedParent = {
			...heatmapState.parent && typeof heatmapState.parent === "object" ? heatmapState.parent : {},
			...parent
		};
		let nextTimeUnit = null;
		const filters = [];
		let nextParent;
		if (level === "year") {
			const yearValue = Number.parseInt(value, 10);
			if (!Number.isFinite(yearValue)) throw new Error(`Heatmap drill-down requires an integer year value; received ${value}.`);
			nextParent = { year: yearValue };
			nextTimeUnit = "month";
			filters.push(`year(datum.${timeField}) == ${yearValue}`);
		} else if (level === "month") {
			const yearValue = Number.parseInt(mergedParent.year, 10);
			const monthValue = Number.parseInt(value, 10);
			if (!Number.isFinite(yearValue)) throw new Error("Heatmap month drill-down requires parent.year.");
			if (!Number.isFinite(monthValue)) throw new Error(`Heatmap drill-down requires an integer month value; received ${value}.`);
			nextParent = {
				year: yearValue,
				month: monthValue
			};
			nextTimeUnit = "date";
			filters.push(`year(datum.${timeField}) == ${yearValue}`);
			filters.push(`month(datum.${timeField}) == ${monthValue - 1}`);
		} else if (level === "date") {
			const yearValue = Number.parseInt(mergedParent.year, 10);
			const monthValue = Number.parseInt(mergedParent.month, 10);
			const dateValue = Number.parseInt(value, 10);
			if (!Number.isFinite(yearValue) || !Number.isFinite(monthValue)) throw new Error("Heatmap date drill-down requires parent.year and parent.month.");
			if (!Number.isFinite(dateValue)) throw new Error(`Heatmap drill-down requires an integer date value; received ${value}.`);
			nextParent = {
				year: yearValue,
				month: monthValue,
				date: dateValue
			};
			nextTimeUnit = "date";
			filters.push(`year(datum.${timeField}) == ${yearValue}`);
			filters.push(`month(datum.${timeField}) == ${monthValue - 1}`);
			filters.push(`date(datum.${timeField}) == ${dateValue}`);
		} else throw new Error(`Heatmap drill-down does not support level "${level}".`);
		heatmapState.parent = nextParent;
		return {
			...spec,
			encoding: {
				...spec.encoding || {},
				x: {
					...spec.encoding?.x || {},
					field: timeField,
					type: "temporal",
					...nextTimeUnit ? { timeUnit: nextTimeUnit } : {}
				}
			},
			transform: [...nextTransforms, {
				filter: filters.join(" && "),
				_widgetvaTag: "heatmap.drilldownAxis"
			}],
			_heatmap_state: heatmapState
		};
	}
	function buildHeatmapTransposeSpec(spec, params = {}) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("Heatmap re-encode transpose requires an object spec.");
		const xEncoding = spec?.encoding?.x;
		const yEncoding = spec?.encoding?.y;
		if (!xEncoding || !yEncoding) throw new Error("Heatmap transpose requires both x and y encodings on the active spec.");
		const desiredTransposed = params?.transposed !== false;
		if (desiredTransposed === (spec?._transpose_state?.transposed === true)) return clone$6(spec);
		return {
			...spec,
			encoding: {
				...spec.encoding || {},
				x: clone$6(yEncoding),
				y: clone$6(xEncoding)
			},
			...spec.width != null && spec.height != null ? {
				width: spec.height,
				height: spec.width
			} : {},
			_transpose_state: {
				...spec._transpose_state || { transposed: false },
				transposed: desiredTransposed
			}
		};
	}
	function buildSankeyAutoCollapseSpec(spec, params = {}) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("Sankey aggregate auto-collapse requires an object spec.");
		const topN = Number.isFinite(params.topN) ? Number(params.topN) : null;
		if (topN == null || topN < 0) throw new Error("Sankey aggregate auto-collapse requires a non-negative topN.");
		const nextSpec = clone$6(spec);
		const rawLinksSource = findNamedDataSource(nextSpec, "rawLinks");
		const nodeConfigSource = findNamedDataSource(nextSpec, "nodeConfig");
		if (rawLinksSource.index < 0 || nodeConfigSource.index < 0 || !rawLinksSource.values || !nodeConfigSource.values) throw new Error("Sankey aggregate auto-collapse requires rawLinks and nodeConfig data sources.");
		if (!nextSpec._sankey_state || typeof nextSpec._sankey_state !== "object") nextSpec._sankey_state = {
			original_nodes: clone$6(nodeConfigSource.values),
			original_links: clone$6(rawLinksSource.values),
			collapsed_groups: {}
		};
		if (!nextSpec._sankey_state.collapsed_groups || typeof nextSpec._sankey_state.collapsed_groups !== "object") nextSpec._sankey_state.collapsed_groups = {};
		const nodeFlows = computeNodeFlows(rawLinksSource.values);
		const depthGroups = /* @__PURE__ */ new Map();
		for (const node of nodeConfigSource.values) {
			const depth = Number(node?.depth ?? 0);
			if (!depthGroups.has(depth)) depthGroups.set(depth, []);
			depthGroups.get(depth).push(node);
		}
		const nodesToKeep = /* @__PURE__ */ new Set();
		const collapsedByLayer = /* @__PURE__ */ new Map();
		const nodeToAggregate = /* @__PURE__ */ new Map();
		for (const [depth, group] of depthGroups.entries()) {
			const sortedGroup = [...group].sort((left, right) => {
				const leftTotal = nodeFlows.get(left?.name)?.total || 0;
				return (nodeFlows.get(right?.name)?.total || 0) - leftTotal;
			});
			for (const node of sortedGroup.slice(0, topN)) nodesToKeep.add(node?.name);
			const collapsedNames = sortedGroup.slice(topN).map((node) => node?.name).filter((name) => typeof name === "string");
			if (collapsedNames.length > 0) {
				const aggregateName = `Others (Layer ${depth})`;
				collapsedByLayer.set(depth, {
					aggregateName,
					collapsedNodes: collapsedNames
				});
				nextSpec._sankey_state.collapsed_groups[aggregateName] = collapsedNames;
				for (const name of collapsedNames) nodeToAggregate.set(name, aggregateName);
			}
		}
		if (collapsedByLayer.size === 0) return nextSpec;
		const newNodes = nodeConfigSource.values.filter((node) => nodesToKeep.has(node?.name));
		for (const [depth, info] of collapsedByLayer.entries()) {
			const maxOrder = (depthGroups.get(depth) || []).reduce((max, node) => Math.max(max, Number(node?.order ?? 0)), 0);
			newNodes.push({
				name: info.aggregateName,
				depth,
				order: maxOrder + 1,
				_is_aggregate: true,
				_collapsed_nodes: info.collapsedNodes
			});
		}
		const linkAgg = /* @__PURE__ */ new Map();
		for (const link of rawLinksSource.values) {
			if (!link || typeof link !== "object") continue;
			const source = nodeToAggregate.get(link.source) || link.source;
			const target = nodeToAggregate.get(link.target) || link.target;
			const key = `${source}-->${target}`;
			linkAgg.set(key, {
				source,
				target,
				value: (linkAgg.get(key)?.value || 0) + Number(link.value || 0)
			});
		}
		nextSpec.data[nodeConfigSource.index].values = newNodes;
		nextSpec.data[rawLinksSource.index].values = [...linkAgg.values()];
		return nextSpec;
	}
	function buildSankeyCollapseNodesSpec(spec, params = {}) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("Sankey structure collapse requires an object spec.");
		const nodes = Array.isArray(params.nodes) ? params.nodes.filter((value) => typeof value === "string" && value.trim().length > 0) : [];
		const aggregateName = typeof params.aggregateName === "string" && params.aggregateName.trim().length > 0 ? params.aggregateName : "Other";
		if (nodes.length === 0) throw new Error("Sankey structure collapse requires one or more node names.");
		const nextSpec = clone$6(spec);
		const rawLinksSource = findNamedDataSource(nextSpec, "rawLinks");
		const nodeConfigSource = findNamedDataSource(nextSpec, "nodeConfig");
		if (rawLinksSource.index < 0 || nodeConfigSource.index < 0 || !rawLinksSource.values || !nodeConfigSource.values) throw new Error("Sankey structure collapse requires rawLinks and nodeConfig data sources.");
		const collapseSet = new Set(nodes);
		const existingNames = new Set(nodeConfigSource.values.map((node) => node?.name).filter((name) => typeof name === "string"));
		const missing = nodes.filter((name) => !existingNames.has(name));
		if (missing.length > 0) throw new Error(`Sankey structure collapse cannot find node(s): ${missing.join(", ")}`);
		if (!nextSpec._sankey_state || typeof nextSpec._sankey_state !== "object") nextSpec._sankey_state = {
			original_nodes: clone$6(nodeConfigSource.values),
			original_links: clone$6(rawLinksSource.values),
			collapsed_groups: {}
		};
		if (!nextSpec._sankey_state.collapsed_groups || typeof nextSpec._sankey_state.collapsed_groups !== "object") nextSpec._sankey_state.collapsed_groups = {};
		nextSpec._sankey_state.collapsed_groups[aggregateName] = [...nodes];
		let collapseDepth = 0;
		let maxOrder = 0;
		for (const node of nodeConfigSource.values) {
			if (!node || typeof node !== "object") continue;
			if (collapseSet.has(node.name)) collapseDepth = typeof node.depth === "number" ? node.depth : collapseDepth;
			if ((typeof node.depth === "number" ? node.depth : 0) === collapseDepth) maxOrder = Math.max(maxOrder, typeof node.order === "number" ? node.order : 0);
		}
		const newNodes = nodeConfigSource.values.filter((node) => !collapseSet.has(node?.name));
		newNodes.push({
			name: aggregateName,
			depth: collapseDepth,
			order: maxOrder + 1,
			_is_aggregate: true,
			_collapsed_nodes: [...nodes]
		});
		const linkAgg = /* @__PURE__ */ new Map();
		for (const link of rawLinksSource.values) {
			if (!link || typeof link !== "object") continue;
			const src = link.source;
			const tgt = link.target;
			const value = Number(link.value || 0);
			const newSrc = collapseSet.has(src) ? aggregateName : src;
			const newTgt = collapseSet.has(tgt) ? aggregateName : tgt;
			if (newSrc === aggregateName && newTgt === aggregateName) continue;
			const key = `${newSrc}-->${newTgt}`;
			linkAgg.set(key, {
				source: newSrc,
				target: newTgt,
				value: (linkAgg.get(key)?.value || 0) + value
			});
		}
		nextSpec.data[nodeConfigSource.index].values = newNodes;
		nextSpec.data[rawLinksSource.index].values = [...linkAgg.values()];
		return nextSpec;
	}
	function buildSankeyExpandNodeSpec(spec, params = {}) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error("Sankey structure expand requires an object spec.");
		const aggregateName = typeof params.aggregateName === "string" && params.aggregateName.trim().length > 0 ? params.aggregateName : null;
		if (!aggregateName) throw new Error("Sankey structure expand requires an aggregateName.");
		const nextSpec = clone$6(spec);
		const rawLinksSource = findNamedDataSource(nextSpec, "rawLinks");
		const nodeConfigSource = findNamedDataSource(nextSpec, "nodeConfig");
		if (rawLinksSource.index < 0 || nodeConfigSource.index < 0 || !rawLinksSource.values || !nodeConfigSource.values) throw new Error("Sankey structure expand requires rawLinks and nodeConfig data sources.");
		const sankeyState = nextSpec?._sankey_state && typeof nextSpec._sankey_state === "object" ? nextSpec._sankey_state : null;
		const collapsedGroups = sankeyState?.collapsed_groups && typeof sankeyState.collapsed_groups === "object" ? sankeyState.collapsed_groups : null;
		const originalNodes = Array.isArray(sankeyState?.original_nodes) ? sankeyState.original_nodes : null;
		const originalLinks = Array.isArray(sankeyState?.original_links) ? sankeyState.original_links : null;
		if (!collapsedGroups || !originalNodes || !originalLinks) throw new Error("Sankey structure expand requires saved _sankey_state with original nodes, links, and collapsed groups.");
		if (!Array.isArray(collapsedGroups[aggregateName])) throw new Error(`Sankey structure expand cannot find collapsed group "${aggregateName}".`);
		const collapsedNodeNames = new Set(collapsedGroups[aggregateName]);
		const newNodes = nodeConfigSource.values.filter((node) => node?.name !== aggregateName);
		for (const originalNode of originalNodes) if (collapsedNodeNames.has(originalNode?.name)) newNodes.push(clone$6(originalNode));
		const restoredLinks = [];
		for (const originalLink of originalLinks) {
			if (!originalLink || typeof originalLink !== "object") continue;
			const src = originalLink.source;
			const tgt = originalLink.target;
			if (collapsedNodeNames.has(src) || collapsedNodeNames.has(tgt)) restoredLinks.push(clone$6(originalLink));
		}
		for (const link of rawLinksSource.values) {
			if (!link || typeof link !== "object") continue;
			const src = link.source;
			const tgt = link.target;
			if (src === aggregateName || tgt === aggregateName) continue;
			if (collapsedNodeNames.has(src) || collapsedNodeNames.has(tgt)) continue;
			restoredLinks.push(clone$6(link));
		}
		nextSpec.data[nodeConfigSource.index].values = newNodes;
		nextSpec.data[rawLinksSource.index].values = restoredLinks;
		delete nextSpec._sankey_state.collapsed_groups[aggregateName];
		return nextSpec;
	}
	function resolveAdvancedResponsePlan(entry, targetState) {
		const responseSpec = entry?.responseSpec && typeof entry.responseSpec === "object" ? entry.responseSpec : null;
		const params = responseSpec?.params && typeof responseSpec.params === "object" ? responseSpec.params : {};
		if (!responseSpec?.kind) return {
			ok: false,
			reason: normalizePropagationSkipReason("unsupported_advanced_response")
		};
		if (responseSpec.kind === "aggregate" && entry?.appliedEffect === "transformDataView") {
			const variant = typeof params.variant === "string" ? params.variant : null;
			const baseSpec = clone$6(targetState?.currentSpec || targetState?.rawSpec || null);
			try {
				if (targetState?.kind === "sankey" && variant === "autoCollapseByRank") return {
					ok: true,
					nextSpec: buildSankeyAutoCollapseSpec(baseSpec, params),
					actionName: "sankey.autoCollapseByRank"
				};
			} catch {
				return {
					ok: false,
					reason: normalizePropagationSkipReason("effect_not_applied")
				};
			}
			return {
				ok: false,
				reason: normalizePropagationSkipReason("unsupported_advanced_response")
			};
		}
		if ((responseSpec.kind === "collapse" || responseSpec.kind === "expand") && entry?.appliedEffect === "transformStructure") {
			const variant = typeof params.variant === "string" ? params.variant : null;
			const baseSpec = clone$6(targetState?.currentSpec || targetState?.rawSpec || null);
			try {
				if (targetState?.kind === "sankey" && responseSpec.kind === "collapse" && variant === "collapseNodes") return {
					ok: true,
					nextSpec: buildSankeyCollapseNodesSpec(baseSpec, params),
					actionName: "sankey.collapseNodes"
				};
				if (targetState?.kind === "sankey" && responseSpec.kind === "expand" && variant === "expandNode") return {
					ok: true,
					nextSpec: buildSankeyExpandNodeSpec(baseSpec, params),
					actionName: "sankey.expandNode"
				};
			} catch (error) {
				if ((typeof error?.message === "string" ? error.message : "").length > 0) return {
					ok: false,
					reason: normalizePropagationSkipReason("advanced_response_params_invalid")
				};
				return {
					ok: false,
					reason: normalizePropagationSkipReason("effect_not_applied")
				};
			}
			return {
				ok: false,
				reason: normalizePropagationSkipReason("unsupported_advanced_response")
			};
		}
		if (entry?.appliedEffect !== "transformView") return {
			ok: false,
			reason: normalizePropagationSkipReason("unsupported_advanced_response")
		};
		const baseSpec = clone$6(targetState?.currentSpec || targetState?.rawSpec || null);
		if (responseSpec.kind === "reencode") {
			const variant = typeof params.variant === "string" ? params.variant : null;
			try {
				if (targetState?.kind === "heatmap" && variant === "transpose") return {
					ok: true,
					nextSpec: buildHeatmapTransposeSpec(baseSpec, params),
					actionName: "heatmap.transpose"
				};
			} catch {
				return {
					ok: false,
					reason: normalizePropagationSkipReason("effect_not_applied")
				};
			}
			return {
				ok: false,
				reason: normalizePropagationSkipReason("unsupported_advanced_response")
			};
		}
		if (responseSpec.kind !== "drillDown") return {
			ok: false,
			reason: normalizePropagationSkipReason("unsupported_advanced_response")
		};
		const level = typeof params.level === "string" ? params.level : typeof params.fromLevel === "string" ? params.fromLevel : null;
		const selectionField = params.selectionField || level;
		const parent = params.parent && typeof params.parent === "object" && !Array.isArray(params.parent) ? clone$6(params.parent) : {};
		const value = params.value != null ? params.value : readScalarPredicateValue(entry?.mappedSelection || null, [selectionField]);
		if (!level || value == null || value === "") return {
			ok: false,
			reason: normalizePropagationSkipReason("mapping_unresolved")
		};
		if (level === "month" && parent.year == null) parent.year = readScalarPredicateValue(entry?.mappedSelection || null, ["year"]);
		if (level === "date") {
			if (parent.year == null) parent.year = readScalarPredicateValue(entry?.mappedSelection || null, ["year"]);
			if (parent.month == null) parent.month = readScalarPredicateValue(entry?.mappedSelection || null, ["month"]);
		}
		const drillParams = {
			level,
			value,
			parent
		};
		try {
			if (targetState?.kind === "line") return {
				ok: true,
				nextSpec: buildLineDrillDownSpec(baseSpec, drillParams),
				actionName: "line.drillDownXAxis"
			};
			if (targetState?.kind === "heatmap") return {
				ok: true,
				nextSpec: buildHeatmapDrillDownSpec(baseSpec, drillParams),
				actionName: "heatmap.drilldownAxis"
			};
		} catch {
			return {
				ok: false,
				reason: normalizePropagationSkipReason("effect_not_applied")
			};
		}
		return {
			ok: false,
			reason: normalizePropagationSkipReason("unsupported_advanced_response")
		};
	}
	function describeLinkEffect(link) {
		const appliedEffect = readAppliedLinkEffect(link);
		if (appliedEffect === "applyFilter") return {
			kind: "filtersWidget",
			targetRef: link.to,
			description: link.description || "Filter propagation"
		};
		if (appliedEffect === "applyHighlight") return {
			kind: "highlightsItems",
			targetRef: link.to,
			description: link.description || "Highlight propagation"
		};
		if (appliedEffect === "focusTarget") return {
			kind: "focusesWidget",
			targetRef: link.to,
			description: link.description || "Focus propagation"
		};
		if (appliedEffect === "syncDomain") return {
			kind: "updatesViewDomain",
			targetRef: link.to,
			description: link.description || "Domain synchronization"
		};
		if (appliedEffect === "shareSelection") return {
			kind: "sharesSelectionState",
			targetRef: link.to,
			description: link.description || "Selection sharing"
		};
		if (appliedEffect === "transformView") return {
			kind: "transformsView",
			targetRef: link.to,
			description: link.description || "View transformation"
		};
		if (appliedEffect === "transformDataView") return {
			kind: "transformsDataView",
			targetRef: link.to,
			description: link.description || "Data-view transformation"
		};
		if (appliedEffect === "transformStructure") return {
			kind: "transformsStructure",
			targetRef: link.to,
			description: link.description || "Structure transformation"
		};
		return null;
	}
	function describeAppliedStatePathsForEffect(effect) {
		if (effect === "applyFilter") return [
			"transforms",
			"data.currentDataRef",
			"feedback.inboundLinkIds"
		];
		if (effect === "applyHighlight") return ["feedback.highlightedKeys", "feedback.inboundLinkIds"];
		if (effect === "focusTarget") return ["shared.focusedWidget", "shared.focus"];
		if (effect === "syncDomain") return [
			"view.xDomain",
			"view.yDomain",
			"feedback.inboundLinkIds"
		];
		if (effect === "shareSelection") return [
			"selections",
			"data.selectedCount",
			"feedback.linkedSourceRefs"
		];
		if (effect === "transformView") return [
			"currentSpec",
			"rawSpec",
			"transforms",
			"encoding",
			"feedback.inboundLinkIds"
		];
		if (effect === "transformDataView") return [
			"currentSpec",
			"rawSpec",
			"transforms",
			"data",
			"feedback.inboundLinkIds"
		];
		if (effect === "transformStructure") return [
			"currentSpec",
			"rawSpec",
			"feedback.inboundLinkIds"
		];
		return [];
	}
	var LinkEngine = class {
		constructor(args = {}) {
			const store = args?.store || args;
			this.store = store;
			this.primitiveHandlers = new Map([
				["filter", (link) => describeLinkEffect(link)],
				["highlight", (link) => describeLinkEffect(link)],
				["syncDomain", (link) => describeLinkEffect(link)],
				["sharesSelection", (link) => describeLinkEffect(link)],
				["drillDown", (link) => describeLinkEffect(link)],
				["reencode", (link) => describeLinkEffect(link)],
				["aggregate", (link) => describeLinkEffect(link)],
				["structure", (link) => describeLinkEffect(link)]
			]);
		}
		resolveWidgetRefById(widgetId) {
			if (typeof widgetId !== "string" || widgetId.length === 0) return null;
			const describedWidget = (this.store.readDescription?.()?.widgets || []).find((widget) => widget?.widgetId === widgetId);
			if (typeof describedWidget?.ref === "string" && describedWidget.ref.length > 0) return describedWidget.ref;
			const listedWidget = this.store.listWidgetDescriptions?.()?.find((widget) => widget?.widgetId === widgetId);
			return typeof listedWidget?.ref === "string" && listedWidget.ref.length > 0 ? listedWidget.ref : null;
		}
		listLinks() {
			const deduped = /* @__PURE__ */ new Map();
			for (const rawLink of this.store.listLinks?.() || []) {
				const normalizedLink = normalizeWidgetLink(rawLink);
				const hydratedLink = {
					...normalizedLink,
					from: normalizedLink?.from || this.resolveWidgetRefById(normalizedLink?.sourceWidgetId),
					to: normalizedLink?.to || this.resolveWidgetRefById(normalizedLink?.targetWidgetId)
				};
				const dedupeKey = [
					hydratedLink?.from || null,
					hydratedLink?.to || null,
					hydratedLink?.sourceWidgetId || null,
					hydratedLink?.targetWidgetId || null,
					hydratedLink?.kind || null,
					hydratedLink?.effect || null,
					hydratedLink?.activationPolicy || null,
					hydratedLink?.effectConstraint || null,
					stableKey(hydratedLink?.fieldMapping || [])
				].join("::");
				if (!deduped.has(dedupeKey)) deduped.set(dedupeKey, hydratedLink);
			}
			return Array.from(deduped.values());
		}
		listPrimitives() {
			return Array.from(this.primitiveHandlers.keys());
		}
		describeEngine() {
			const links = this.listLinks();
			const coordinationLinks = links.filter(isTopologyLink);
			const topology = deriveWorkspaceTopology({
				widgets: (this.store.readDescription?.() || {}).widgets || [],
				links
			});
			return makeLinkEngineSummary({
				primitiveCount: this.primitiveHandlers.size,
				primitives: this.listPrimitives().map((name) => makeLinkEnginePrimitiveEntry({
					name,
					appliedStatePaths: this.describeAppliedStatePaths(name)
				})),
				linkCount: links.length,
				coordinationLinkCount: coordinationLinks.length,
				structuralLinkCount: links.length - coordinationLinks.length,
				automaticLinkCount: links.filter((link) => readLinkActivationPolicy(link) !== "manual").length,
				manualLinkCount: links.filter((link) => readLinkActivationPolicy(link) === "manual").length,
				topology,
				capabilities: makeLinkEngineCapabilities({
					propagationExecution: true,
					propagationPlan: true,
					effectCollection: true,
					consistencyEvaluation: true
				})
			});
		}
		findOutgoing({ sourceRef, state = this.store.readState() }) {
			if (!sourceRef) return [];
			const activeSelection = this.resolveActiveSelection({
				sourceRef,
				state
			});
			return this.listLinks().filter((link) => {
				if (link.from === sourceRef) return true;
				if (!activeSelection) return false;
				if (link.sourceWidgetId && link.sourceWidgetId === activeSelection.sourceWidgetId) return true;
				if (link.from && activeSelection.sourceWidgetRef && link.from === activeSelection.sourceWidgetRef) return true;
				return false;
			});
		}
		findAffectedRefs({ sourceRef, state = this.store.readState() }) {
			const affected = /* @__PURE__ */ new Set();
			for (const link of this.findOutgoing({
				sourceRef,
				state
			})) if (link.to) affected.add(link.to);
			return Array.from(affected);
		}
		describePropagation({ sourceRef }) {
			const state = this.store.readState?.() || {};
			const activeSelection = this.resolveActiveSelection({
				sourceRef,
				state
			});
			return this.findOutgoing({
				sourceRef,
				state
			}).map((link) => {
				const mappedSelection = activeSelection ? clone$6(mapSelectionToTargetSelection(activeSelection, link.fieldMapping)) : null;
				const planEntry = {
					linkRef: link.ref,
					primitive: link.kind,
					declaredEffect: readDeclaredLinkEffect(link),
					appliedEffect: readAppliedLinkEffect(link),
					responseSpec: readLinkResponseSpec(link),
					targetRef: link.to,
					activationPolicy: readLinkActivationPolicy(link),
					effectConstraint: readLinkEffectConstraint(link),
					sourceSelectionRef: link.from || null,
					sourceWidgetId: link.sourceWidgetId || null,
					hasActiveSelection: Boolean(activeSelection),
					mappedSelection,
					effect: this.primitiveHandlers.get(link.kind)?.(link) || null
				};
				const applicability = this.assessPropagationEntry({
					entry: planEntry,
					state,
					activeSelection
				});
				return {
					...planEntry,
					canApply: applicability.canApply,
					skippedReason: applicability.reason
				};
			});
		}
		collectEffects({ sourceRef }) {
			return this.findOutgoing({ sourceRef }).map((link) => describeLinkEffect(link)).filter(Boolean);
		}
		describeAppliedStatePaths(primitive) {
			if (primitive === "filter") return [
				"transforms",
				"data.currentDataRef",
				"feedback.inboundLinkIds"
			];
			if (primitive === "highlight") return ["feedback.highlightedKeys", "feedback.inboundLinkIds"];
			if (primitive === "syncDomain") return [
				"view.xDomain",
				"view.yDomain",
				"feedback.inboundLinkIds"
			];
			if (primitive === "sharesSelection") return [
				"selections",
				"data.selectedCount",
				"feedback.linkedSourceRefs"
			];
			return [];
		}
		resolveActiveSelection({ sourceRef, state = this.store.readState() }) {
			return sourceRef ? readSelectionRegistry(state?.shared || {})?.[sourceRef] || null : null;
		}
		buildMirroredSelectionRef(targetRef, sourceSelectionRef) {
			return `${targetRef}/selection/shared_${String(sourceSelectionRef || "selection").split("/").pop()}`;
		}
		buildLinkedFeedback(targetState, entry) {
			const feedback = targetState?.feedback || {};
			return {
				feedback,
				inboundLinkIds: uniqueValues([...feedback.inboundLinkIds || [], this.resolveLinkId(entry.linkRef)]),
				linkedSourceRefs: uniqueValues([...feedback.linkedSourceRefs || [], entry.sourceSelectionRef])
			};
		}
		buildPropagationPlan({ sourceRef, state = this.store.readState() }) {
			const activeSelection = this.resolveActiveSelection({
				sourceRef,
				state
			});
			return this.findOutgoing({
				sourceRef,
				state
			}).map((link) => ({
				linkRef: link.ref,
				primitive: link.kind,
				declaredEffect: readDeclaredLinkEffect(link),
				appliedEffect: readAppliedLinkEffect(link),
				responseSpec: readLinkResponseSpec(link),
				targetRef: link.to,
				activationPolicy: readLinkActivationPolicy(link),
				effectConstraint: readLinkEffectConstraint(link),
				sourceSelectionRef: sourceRef,
				sourceWidgetId: link.sourceWidgetId || null,
				hasActiveSelection: Boolean(activeSelection),
				mappedSelection: activeSelection ? clone$6(mapSelectionToTargetSelection(activeSelection, link.fieldMapping)) : null,
				effect: this.primitiveHandlers.get(link.kind)?.(link) || null
			}));
		}
		buildTargetPrimitiveEntries({ targetRef, primitive, state = this.store.readState() }) {
			if (!targetRef || !primitive) return [];
			return this.listLinks().filter((link) => link.to === targetRef && link.kind === primitive && readLinkActivationPolicy(link) !== "manual").map((link) => {
				const sourceSelectionRef = link.from || null;
				const activeSelection = this.resolveActiveSelection({
					sourceRef: sourceSelectionRef,
					state
				});
				return {
					linkRef: link.ref,
					primitive: link.kind,
					declaredEffect: readDeclaredLinkEffect(link),
					appliedEffect: readAppliedLinkEffect(link),
					responseSpec: readLinkResponseSpec(link),
					targetRef,
					activationPolicy: readLinkActivationPolicy(link),
					effectConstraint: readLinkEffectConstraint(link),
					sourceSelectionRef,
					sourceWidgetId: link.sourceWidgetId || null,
					hasActiveSelection: Boolean(activeSelection),
					mappedSelection: activeSelection ? clone$6(mapSelectionToTargetSelection(activeSelection, link.fieldMapping)) : null,
					effect: this.primitiveHandlers.get(link.kind)?.(link) || null
				};
			});
		}
		buildTargetEffectEntries({ targetRef, effect, state = this.store.readState() }) {
			if (!targetRef || !effect) return [];
			return this.listLinks().filter((link) => link.to === targetRef && readAppliedLinkEffect(link) === effect && readLinkActivationPolicy(link) !== "manual").map((link) => {
				const sourceSelectionRef = link.from || null;
				const activeSelection = this.resolveActiveSelection({
					sourceRef: sourceSelectionRef,
					state
				});
				return {
					linkRef: link.ref,
					primitive: link.kind,
					declaredEffect: readDeclaredLinkEffect(link),
					appliedEffect: readAppliedLinkEffect(link),
					responseSpec: readLinkResponseSpec(link),
					targetRef,
					activationPolicy: readLinkActivationPolicy(link),
					effectConstraint: readLinkEffectConstraint(link),
					sourceSelectionRef,
					sourceWidgetId: link.sourceWidgetId || null,
					hasActiveSelection: Boolean(activeSelection),
					mappedSelection: activeSelection ? clone$6(mapSelectionToTargetSelection(activeSelection, link.fieldMapping)) : null,
					effect: this.primitiveHandlers.get(link.kind)?.(link) || null
				};
			});
		}
		assessPropagationEntry({ entry, state = this.store.readState(), activeSelection = null }) {
			if (!entry?.targetRef) return {
				canApply: false,
				reason: normalizePropagationSkipReason("missing_target_ref")
			};
			if (entry.activationPolicy === "manual") return {
				canApply: false,
				reason: normalizePropagationSkipReason("manual_activation_policy")
			};
			const targetState = this.store.getWidgetState?.(entry.targetRef) || state?.widgets?.[entry.targetRef] || null;
			if (!targetState) return {
				canApply: false,
				reason: normalizePropagationSkipReason("missing_target_widget")
			};
			if (!entry.appliedEffect) return {
				canApply: false,
				reason: normalizePropagationSkipReason("unsupported_effect")
			};
			if (!activeSelection) return {
				canApply: false,
				reason: normalizePropagationSkipReason("no_active_selection")
			};
			if (entry.appliedEffect === "transformView" || entry.appliedEffect === "transformDataView" || entry.appliedEffect === "transformStructure") {
				const advancedPlan = resolveAdvancedResponsePlan(entry, targetState);
				return {
					canApply: advancedPlan.ok === true,
					reason: advancedPlan.ok === true ? null : normalizePropagationSkipReason(advancedPlan.reason)
				};
			}
			if (entry.appliedEffect === "focusTarget") return {
				canApply: true,
				reason: null
			};
			if (entry.appliedEffect === "syncDomain") {
				const hasDomain = Array.isArray(entry.mappedSelection?.domain?.xDomain) || Array.isArray(entry.mappedSelection?.domain?.yDomain);
				return {
					canApply: hasDomain,
					reason: hasDomain ? null : normalizePropagationSkipReason("mapping_unresolved")
				};
			}
			if (!entry.mappedSelection) return {
				canApply: false,
				reason: normalizePropagationSkipReason("mapping_unresolved")
			};
			return {
				canApply: true,
				reason: null
			};
		}
		propagate(input) {
			const directSourceRef = typeof input === "string" ? input : null;
			const sourceRef = directSourceRef || input?.sourceRef || null;
			const state = directSourceRef ? this.store.readState() : input?.state || this.store.readState();
			const activeSelection = this.resolveActiveSelection({
				sourceRef,
				state
			});
			const plan = this.buildPropagationPlan({
				sourceRef,
				state
			});
			const candidateRefs = [...new Set(plan.map((entry) => entry.targetRef).filter(Boolean))];
			this.store.clearWidgetPatches?.(candidateRefs);
			let currentState = this.store.readState();
			const appliedLinks = [];
			const skippedTargets = [];
			for (const entry of plan) {
				const applicability = this.assessPropagationEntry({
					entry,
					state: currentState,
					activeSelection
				});
				if (!applicability.canApply) {
					skippedTargets.push({
						linkRef: entry.linkRef,
						primitive: entry.primitive,
						declaredEffect: entry.declaredEffect,
						appliedEffect: entry.appliedEffect,
						responseSpec: clone$6(entry.responseSpec),
						targetRef: entry.targetRef,
						activationPolicy: entry.activationPolicy,
						effectConstraint: entry.effectConstraint,
						reason: normalizePropagationSkipReason(applicability.reason)
					});
					continue;
				}
				const patchResult = this.applyPropagationEntry({
					entry,
					state: currentState,
					activeSelection
				});
				if (patchResult?.applied) currentState = patchResult.nextState || this.store.readState();
				else {
					skippedTargets.push({
						linkRef: entry.linkRef,
						primitive: entry.primitive,
						declaredEffect: entry.declaredEffect,
						appliedEffect: entry.appliedEffect,
						responseSpec: clone$6(entry.responseSpec),
						targetRef: entry.targetRef,
						activationPolicy: entry.activationPolicy,
						effectConstraint: entry.effectConstraint,
						reason: normalizePropagationSkipReason(patchResult?.reason)
					});
					continue;
				}
				appliedLinks.push({
					linkRef: entry.linkRef,
					primitive: entry.primitive,
					declaredEffect: entry.declaredEffect,
					appliedEffect: entry.appliedEffect,
					responseSpec: clone$6(entry.responseSpec),
					targetRef: entry.targetRef,
					activationPolicy: entry.activationPolicy,
					effectConstraint: entry.effectConstraint,
					hasActiveSelection: entry.hasActiveSelection,
					appliedStatePaths: describeAppliedStatePathsForEffect(entry.appliedEffect),
					effect: clone$6(entry.effect)
				});
			}
			const affectedRefs = [...new Set(appliedLinks.map((entry) => entry.targetRef).filter(Boolean))];
			const propagation = {
				sourceRef,
				activeSelection: clone$6(activeSelection),
				affectedRefs,
				linkCount: plan.length,
				appliedLinkCount: appliedLinks.length,
				skippedLinkCount: skippedTargets.length,
				nextState: currentState,
				links: appliedLinks,
				skippedTargets,
				effects: plan.map((entry) => clone$6(entry.effect)).filter(Boolean)
			};
			return directSourceRef ? propagation.affectedRefs : propagation;
		}
		applyPropagationEntry({ entry, state, activeSelection }) {
			const targetRef = entry?.targetRef;
			if (!targetRef) return {
				applied: false,
				nextState: state,
				reason: normalizePropagationSkipReason("missing_target_ref")
			};
			const targetState = this.store.getWidgetState?.(targetRef) || state?.widgets?.[targetRef] || null;
			if (!targetState) return {
				applied: false,
				nextState: state,
				reason: normalizePropagationSkipReason("missing_target_widget")
			};
			const { feedback, inboundLinkIds, linkedSourceRefs } = this.buildLinkedFeedback(targetState, entry);
			entry.mappedSelection;
			if (entry.appliedEffect === "applyFilter") {
				const filterEntries = this.buildTargetEffectEntries({
					targetRef,
					effect: "applyFilter",
					state
				});
				const activeFilterEntries = filterEntries.filter((item) => item.mappedSelection);
				const filterLinkRefs = new Set(filterEntries.map((item) => item.linkRef));
				const nextTransforms = (Array.isArray(targetState.transforms) ? targetState.transforms : []).filter((transform) => !filterLinkRefs.has(transform?.spec?.linkRef));
				const visibleDataRef = targetState.data?.currentDataRef || null;
				const runtimeData = visibleDataRef ? this.store.readRuntimeData(visibleDataRef) : null;
				const baselineRows = Array.isArray(runtimeData?.baseRows) ? runtimeData.baseRows : Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
				const nextVisibleRows = activeFilterEntries.reduce((rows, filterEntry) => rows.filter((row) => rowMatchesSelection(row, filterEntry.mappedSelection)), baselineRows);
				const selectedCount = countSelectedRowsForSelections(nextVisibleRows, Object.values(targetState.selections || {}).filter(Boolean));
				for (const filterEntry of activeFilterEntries) nextTransforms.push({
					kind: "filter",
					source: filterEntry.sourceSelectionRef,
					sourceWidgetId: filterEntry.sourceWidgetId || null,
					sourceSelectionRef: filterEntry.sourceSelectionRef,
					linkId: this.resolveLinkId(filterEntry.linkRef),
					spec: {
						linkRef: filterEntry.linkRef,
						sourceSelectionRef: filterEntry.sourceSelectionRef
					}
				});
				if (visibleDataRef) this.store.updateRuntimeData?.(visibleDataRef, (currentEntry) => ({
					...currentEntry,
					rows: nextVisibleRows,
					baseRows: Array.isArray(currentEntry?.baseRows) ? currentEntry.baseRows : baselineRows,
					handle: {
						...currentEntry?.handle || {},
						stats: {
							...currentEntry?.handle?.stats || {},
							rowCount: Array.isArray(baselineRows) ? baselineRows.length : 0,
							visibleCount: nextVisibleRows.length,
							selectedCount
						}
					}
				}));
				const nextState = this.store.patchWidget(targetRef, {
					transforms: nextTransforms,
					data: {
						...targetState.data || {},
						visibleCount: nextVisibleRows.length,
						selectedCount
					},
					rawSpec: targetState.rawSpec?.data ? {
						...clone$6(targetState.rawSpec),
						data: {
							...targetState.rawSpec.data || {},
							values: nextVisibleRows
						}
					} : targetState.rawSpec,
					feedback: {
						...feedback,
						inboundLinkIds,
						linkedSourceRefs
					}
				});
				this.store.syncSelectionRuntimeData?.(targetRef);
				return {
					applied: true,
					nextState
				};
			}
			if (entry.appliedEffect === "applyHighlight") {
				const activeHighlightEntries = this.buildTargetEffectEntries({
					targetRef,
					effect: "applyHighlight",
					state
				}).filter((item) => item.mappedSelection);
				const highlightedKeys = uniqueValues(activeHighlightEntries.flatMap((item) => categoricalValuesFromSelection(item.mappedSelection)));
				const isHighlightedRow = (row) => activeHighlightEntries.some((item) => rowMatchesSelection(row, item.mappedSelection));
				if (targetState.data?.currentDataRef) this.store.updateRuntimeData?.(targetState.data.currentDataRef, (currentEntry) => {
					const currentRows = Array.isArray(currentEntry?.rows) ? currentEntry.rows : [];
					return {
						...currentEntry,
						rows: markRows(currentRows, "__widgetva_highlight", isHighlightedRow)
					};
				});
				const nextState = this.store.patchWidget(targetRef, {
					rawSpec: targetState.rawSpec?.data ? {
						...clone$6(targetState.rawSpec),
						data: {
							...targetState.rawSpec.data || {},
							values: markRows(targetState.rawSpec.data.values, "__widgetva_highlight", isHighlightedRow)
						}
					} : targetState.rawSpec,
					feedback: {
						...feedback,
						inboundLinkIds,
						linkedSourceRefs,
						highlightedKeys
					}
				});
				return {
					applied: true,
					nextState: updateSharedStateInStore(this.store, (shared) => withHighlightSubmodel(shared, deriveHighlightState(nextState)))
				};
			}
			if (entry.appliedEffect === "focusTarget") {
				const nextState = this.store.patchWidget(targetRef, { feedback: {
					...feedback,
					inboundLinkIds,
					linkedSourceRefs,
					focusLinkIds: uniqueValues([...feedback.focusLinkIds || [], this.resolveLinkId(entry.linkRef)]),
					focusedBySelectionRef: entry.sourceSelectionRef || null
				} });
				return {
					applied: true,
					nextState: updateSharedStateInStore(this.store, (shared) => withFocusSubmodel(shared, {
						widgetRef: targetRef,
						widgetId: targetState.widgetId || null,
						source: "link"
					}, nextState?.widgets || state?.widgets || {}))
				};
			}
			if (entry.appliedEffect === "syncDomain") {
				const activeSyncEntries = this.buildTargetEffectEntries({
					targetRef,
					effect: "syncDomain",
					state
				}).filter((item) => item.mappedSelection?.domain);
				const resolvedXDomain = [...activeSyncEntries].reverse().find((item) => Array.isArray(item.mappedSelection?.domain?.xDomain))?.mappedSelection?.domain?.xDomain || null;
				const resolvedYDomain = [...activeSyncEntries].reverse().find((item) => Array.isArray(item.mappedSelection?.domain?.yDomain))?.mappedSelection?.domain?.yDomain || null;
				return {
					applied: true,
					nextState: this.store.patchWidget(targetRef, {
						view: {
							...targetState.view || {},
							xDomain: resolvedXDomain,
							yDomain: resolvedYDomain,
							zoom: buildViewZoomState({
								widgetSpec: targetState.rawSpec,
								xDomain: resolvedXDomain,
								yDomain: resolvedYDomain
							})
						},
						rawSpec: targetState.rawSpec?.encoding ? {
							...clone$6(targetState.rawSpec),
							encoding: {
								...targetState.rawSpec.encoding || {},
								...targetState.rawSpec.encoding?.x ? { x: updateEncodingDomain(targetState.rawSpec.encoding, "x", resolvedXDomain) } : {},
								...targetState.rawSpec.encoding?.y ? { y: updateEncodingDomain(targetState.rawSpec.encoding, "y", resolvedYDomain) } : {}
							}
						} : targetState.rawSpec,
						feedback: {
							...feedback,
							inboundLinkIds,
							linkedSourceRefs
						}
					})
				};
			}
			if (entry.appliedEffect === "shareSelection") {
				const shareEntries = this.buildTargetEffectEntries({
					targetRef,
					effect: "shareSelection",
					state
				});
				const activeShareEntries = shareEntries.filter((item) => item.mappedSelection);
				const runtimeData = this.store.readRuntimeData(targetState.data?.currentDataRef);
				const targetRows = Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
				const shareSelectionRefs = new Set(shareEntries.map((item) => this.buildMirroredSelectionRef(targetRef, item.sourceSelectionRef)));
				const isSelectedRow = (row) => activeShareEntries.some((item) => rowMatchesSelection(row, item.mappedSelection));
				const selectedCount = countRowsMatching(targetRows, isSelectedRow);
				if (targetState.data?.currentDataRef) this.store.updateRuntimeData?.(targetState.data.currentDataRef, (currentEntry) => ({
					...currentEntry,
					rows: markRows(currentEntry?.rows, "__widgetva_selected", isSelectedRow),
					handle: {
						...currentEntry?.handle || {},
						stats: {
							...currentEntry?.handle?.stats || {},
							selectedCount
						}
					}
				}));
				const nextState = this.store.patchWidget(targetRef, {
					selections: activeShareEntries.reduce((acc, shareEntry) => ({
						...acc,
						[this.buildMirroredSelectionRef(targetRef, shareEntry.sourceSelectionRef)]: makeSelectionState(buildSelectionStateInput(shareEntry.mappedSelection))
					}), Object.fromEntries(Object.entries(targetState.selections || {}).filter(([selectionRef]) => !shareSelectionRefs.has(selectionRef)))),
					data: {
						...targetState.data || {},
						selectedCount
					},
					rawSpec: targetState.rawSpec?.data ? {
						...clone$6(targetState.rawSpec),
						data: {
							...targetState.rawSpec.data || {},
							values: markRows(targetState.rawSpec.data.values, "__widgetva_selected", isSelectedRow)
						}
					} : targetState.rawSpec,
					feedback: {
						...feedback,
						inboundLinkIds,
						linkedSourceRefs,
						sharedSelectionSourceWidgetId: activeShareEntries[0]?.sourceWidgetId || null
					}
				});
				this.store.syncSelectionRuntimeData?.(targetRef);
				return {
					applied: true,
					nextState
				};
			}
			if (entry.appliedEffect === "transformView" || entry.appliedEffect === "transformDataView" || entry.appliedEffect === "transformStructure") {
				const advancedPlan = resolveAdvancedResponsePlan(entry, targetState);
				if (advancedPlan.ok !== true || !advancedPlan.nextSpec) return {
					applied: false,
					nextState: state,
					reason: normalizePropagationSkipReason(advancedPlan.reason)
				};
				return {
					applied: true,
					nextState: this.store.patchWidget(targetRef, {
						currentSpec: clone$6(advancedPlan.nextSpec),
						rawSpec: clone$6(advancedPlan.nextSpec),
						encodings: advancedPlan.nextSpec?.encoding && typeof advancedPlan.nextSpec.encoding === "object" ? clone$6(advancedPlan.nextSpec.encoding) : targetState.encodings || {},
						transforms: Array.isArray(advancedPlan.nextSpec.transform) ? clone$6(advancedPlan.nextSpec.transform) : targetState.transforms || [],
						feedback: {
							...feedback,
							inboundLinkIds,
							linkedSourceRefs,
							advancedResponseKinds: uniqueValues([...Array.isArray(feedback.advancedResponseKinds) ? feedback.advancedResponseKinds : [], entry?.responseSpec?.kind || null]),
							advancedActionName: advancedPlan.actionName || null
						}
					})
				};
			}
			return {
				applied: false,
				nextState: state,
				reason: normalizePropagationSkipReason("unsupported_effect")
			};
		}
		resolveLinkId(linkRef) {
			const link = this.store.getLink?.(linkRef) || this.listLinks().find((item) => item.ref === linkRef) || null;
			if (link?.linkId) return link.linkId;
			if (typeof linkRef === "string" && linkRef.includes("/link/")) return linkRef.split("/").pop() || null;
			return null;
		}
		evaluatePropagation({ sourceRef, state = this.store.readState() }) {
			const activeSelection = this.resolveActiveSelection({
				sourceRef,
				state
			});
			const results = this.buildPropagationPlan({
				sourceRef,
				state
			}).map((entry) => this.evaluatePlanEntry({
				entry,
				state,
				activeSelection
			}));
			const passedCount = results.filter((item) => item.passed).length;
			return {
				ok: results.every((item) => item.passed),
				sourceRef,
				activeSelection: clone$6(activeSelection),
				linkCount: results.length,
				passedCount,
				consistencyScore: results.length === 0 ? 1 : passedCount / results.length,
				results
			};
		}
		evaluatePlanEntry({ entry, state, activeSelection }) {
			const resolvedLinkId = this.resolveLinkId(entry.linkRef);
			const targetState = entry.targetRef ? state?.widgets?.[entry.targetRef] || null : null;
			const mappedSelection = entry.mappedSelection;
			const checks = [];
			const applicability = this.assessPropagationEntry({
				entry,
				state,
				activeSelection
			});
			checks.push({
				name: "targetExists",
				passed: Boolean(targetState),
				actual: Boolean(targetState),
				expected: true
			});
			if (targetState && applicability.canApply && entry.appliedEffect !== "focusTarget") checks.push({
				name: "inboundLinkRegistered",
				passed: Array.isArray(targetState.feedback?.inboundLinkIds) && targetState.feedback.inboundLinkIds.includes(resolvedLinkId),
				actual: targetState.feedback?.inboundLinkIds || [],
				expected: resolvedLinkId
			});
			if (targetState && applicability.canApply) {
				if (entry.appliedEffect === "applyFilter" && mappedSelection) {
					const runtimeData = this.store.readRuntimeData?.(targetState.data?.currentDataRef);
					const targetRows = Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
					const everyVisibleRowMatches = targetRows.every((row) => countSelectedRows([row], mappedSelection) === 1);
					checks.push({
						name: "visibleRowsMatchFilter",
						passed: everyVisibleRowMatches,
						actual: targetRows.length,
						expected: "all visible rows satisfy mapped selection predicates"
					});
				}
				if (entry.appliedEffect === "shareSelection" && mappedSelection) {
					const runtimeData = this.store.readRuntimeData?.(targetState.data?.currentDataRef);
					const targetRows = Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
					const mirroredSelection = Object.values(targetState.selections || {}).find((selection) => selection?.summary === mappedSelection.summary);
					const expectedSelectedCount = countSelectedRows(targetRows, mappedSelection);
					checks.push({
						name: "mirroredSelectionExists",
						passed: Boolean(mirroredSelection),
						actual: Boolean(mirroredSelection),
						expected: true
					});
					checks.push({
						name: "mirroredSelectionCountMatches",
						passed: targetState.data?.selectedCount === expectedSelectedCount,
						actual: targetState.data?.selectedCount ?? 0,
						expected: expectedSelectedCount
					});
				}
				if (entry.appliedEffect === "syncDomain") {
					checks.push({
						name: "xDomainSynced",
						passed: arraysEqual(targetState.view?.xDomain, activeSelection?.domain?.xDomain),
						actual: targetState.view?.xDomain || null,
						expected: activeSelection?.domain?.xDomain || null
					});
					checks.push({
						name: "yDomainSynced",
						passed: arraysEqual(targetState.view?.yDomain, activeSelection?.domain?.yDomain),
						actual: targetState.view?.yDomain || null,
						expected: activeSelection?.domain?.yDomain || null
					});
				}
				if (entry.appliedEffect === "applyHighlight") {
					const highlightedKeys = targetState.feedback?.highlightedKeys || [];
					const highlightedRows = Array.isArray(targetState.rawSpec?.data?.values) ? targetState.rawSpec.data.values.filter((row) => row?.__widgetva_highlight === true).length : 0;
					checks.push({
						name: "highlightFeedbackPresent",
						passed: highlightedKeys.length > 0 || highlightedRows > 0,
						actual: {
							highlightedKeys: highlightedKeys.length,
							highlightedRows
						},
						expected: "at least one highlight feedback channel should be populated"
					});
				}
				if (entry.appliedEffect === "focusTarget") {
					const focusState = state?.shared?.focus || null;
					checks.push({
						name: "focusedWidgetMatchesTarget",
						passed: state?.shared?.focusedWidget === entry.targetRef,
						actual: state?.shared?.focusedWidget || null,
						expected: entry.targetRef
					});
					checks.push({
						name: "focusStateMatchesTarget",
						passed: focusState?.widgetRef === entry.targetRef,
						actual: focusState?.widgetRef || null,
						expected: entry.targetRef
					});
				}
			}
			checks.push({
				name: "linkCanApply",
				passed: applicability.canApply,
				actual: applicability.reason || "applicable",
				expected: "applicable"
			});
			return {
				linkRef: entry.linkRef,
				primitive: entry.primitive,
				declaredEffect: entry.declaredEffect,
				appliedEffect: entry.appliedEffect,
				responseSpec: clone$6(entry.responseSpec),
				targetRef: entry.targetRef,
				activationPolicy: entry.activationPolicy,
				effectConstraint: entry.effectConstraint,
				reason: applicability.reason,
				passed: checks.every((check) => check.passed),
				checks
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/WidgetRegistry.js
	function clone$5(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function summarizeWidgetEntry(widgetDescription, widgetState, widgetAdapter) {
		const humanInteraction = {
			...widgetAdapter?.getHumanInteractionConfig?.() || {},
			...widgetState?.humanInteraction || {},
			...widgetDescription?.humanInteraction || {}
		};
		return {
			ref: widgetDescription?.ref || "",
			widgetId: widgetDescription?.widgetId || "",
			kind: widgetDescription?.kind || "",
			role: widgetDescription?.role || "",
			title: widgetDescription?.title || "",
			description: widgetDescription?.description || "",
			analyticRoles: Array.isArray(widgetDescription?.analyticRoles) ? [...widgetDescription.analyticRoles] : [],
			sourceKind: widgetDescription?.sourceKind || null,
			supportsSpecMutation: widgetDescription?.supportsSpecMutation === true,
			primaryDataRef: widgetDescription?.primaryDataRef || widgetState?.data?.sourceDataRef || null,
			sourceDataRef: widgetState?.data?.sourceDataRef || null,
			currentDataRef: widgetState?.data?.currentDataRef || null,
			usageNotes: Array.isArray(widgetDescription?.usageNotes) ? [...widgetDescription.usageNotes] : [],
			actionNames: Array.isArray(widgetDescription?.actionNames) ? [...widgetDescription.actionNames] : [],
			perceptionQueryNames: Array.isArray(widgetDescription?.perceptionQueryNames) ? [...widgetDescription.perceptionQueryNames] : [],
			adapterProvider: widgetAdapter?.provider || null,
			providerCapabilities: widgetAdapter?.providerCapabilities ? clone$5(widgetAdapter.providerCapabilities) : null,
			adapterCapabilities: widgetAdapter ? {
				canDescribe: typeof widgetAdapter.getDescription === "function",
				canReadState: typeof widgetAdapter.getState === "function",
				canApplyState: typeof widgetAdapter.applyState === "function",
				canBindHumanInteractions: typeof widgetAdapter.bindHumanInteractions === "function",
				canRegisterActions: typeof widgetAdapter.registerActions === "function",
				canRegisterPerceptionQueries: typeof widgetAdapter.registerPerceptionQueries === "function"
			} : null,
			humanInteractionMode: humanInteraction.mode || null,
			humanInteractionActionName: humanInteraction.actionName || null,
			supportsDirectManipulation: humanInteraction?.supportsDirectManipulation === true
		};
	}
	function buildResolvedWidget(widgetDescription, widgetState, widgetAdapter) {
		if (!widgetDescription && !widgetState) return null;
		return {
			...widgetDescription || {},
			...widgetState || {},
			ref: widgetDescription?.ref || widgetState?.ref || "",
			kind: widgetDescription?.kind || widgetState?.kind || "",
			title: widgetDescription?.title || "",
			description: widgetDescription?.description || "",
			primaryDataRef: widgetDescription?.primaryDataRef || widgetState?.data?.sourceDataRef || null,
			widgetId: widgetDescription?.widgetId || widgetState?.widgetId || "",
			role: widgetDescription?.role || widgetState?.role || "",
			actionNames: Array.isArray(widgetDescription?.actionNames) ? widgetDescription.actionNames : [],
			perceptionQueryNames: Array.isArray(widgetDescription?.perceptionQueryNames) ? widgetDescription.perceptionQueryNames : [],
			usageNotes: Array.isArray(widgetDescription?.usageNotes) ? widgetDescription.usageNotes : [],
			adapterProvider: widgetAdapter?.provider || null,
			providerCapabilities: widgetAdapter?.providerCapabilities || null,
			humanInteraction: widgetDescription?.humanInteraction || widgetState?.humanInteraction || null
		};
	}
	function widgetUsesDataRef(widgetDescription, widgetState, dataRef) {
		if (!dataRef) return false;
		return widgetDescription?.primaryDataRef === dataRef || widgetState?.data?.currentDataRef === dataRef || widgetState?.data?.sourceDataRef === dataRef;
	}
	var WidgetRegistry = class {
		constructor() {
			this.widgetDescriptions = {};
			this.widgetStates = {};
			this.dataHandles = {};
			this.links = {};
			this.widgetAdapters = {};
			this.widgetEntries = [];
		}
		rebuildWidgetEntries() {
			this.widgetEntries = Object.values(this.widgetDescriptions).map((widgetDescription) => summarizeWidgetEntry(widgetDescription, this.widgetStates?.[widgetDescription.ref] || null, this.widgetAdapters?.[widgetDescription.ref] || null));
		}
		setState(state) {
			this.widgetStates = Object.fromEntries(Object.entries(state?.widgets || {}).map(([ref, widgetState]) => {
				return [ref, makeWidgetState({
					ref,
					...widgetState
				})];
			}));
			this.rebuildWidgetEntries();
		}
		updateDataHandle(ref, handle) {
			if (!ref || !handle) return;
			this.dataHandles = {
				...this.dataHandles || {},
				[ref]: handle
			};
		}
		removeDataHandle(ref) {
			if (!ref || !this.dataHandles?.[ref]) return;
			const nextHandles = { ...this.dataHandles || {} };
			delete nextHandles[ref];
			this.dataHandles = nextHandles;
		}
		replaceWorkspace({ description, state, widgetAdapters } = {}) {
			this.widgetDescriptions = Object.fromEntries((description?.widgets || []).map((item) => {
				const normalizedDescription = makeWidgetDescription(item);
				return [normalizedDescription.ref, normalizedDescription];
			}));
			this.dataHandles = Object.fromEntries((description?.dataHandles || []).map((item) => [item.ref, item]));
			this.links = Object.fromEntries((description?.links || []).map((item) => {
				const normalizedLink = normalizeWidgetLink(item);
				return [normalizedLink.ref, normalizedLink];
			}));
			this.widgetAdapters = Object.fromEntries((Array.isArray(widgetAdapters) ? widgetAdapters : []).map((adapter) => [adapter.widgetRef, adapter]));
			this.setState(state);
		}
		listWidgetRefs() {
			return Object.keys(this.widgetDescriptions);
		}
		listDataRefs() {
			return Object.keys(this.dataHandles);
		}
		listLinkRefs() {
			return Object.keys(this.links);
		}
		getWidget(ref) {
			return ref ? this.widgetDescriptions?.[ref] || null : null;
		}
		getWidgetState(ref) {
			return ref ? this.widgetStates?.[ref] || null : null;
		}
		getWidgetEntry(ref) {
			if (!ref) return null;
			const description = this.getWidget(ref);
			if (!description) return null;
			return summarizeWidgetEntry(description, this.getWidgetState(ref), this.getWidgetAdapter(ref));
		}
		getResolvedWidget(ref) {
			if (!ref) return null;
			return buildResolvedWidget(this.getWidget(ref), this.getWidgetState(ref), this.getWidgetAdapter(ref));
		}
		findWidgetRefForDataRef(dataRef) {
			if (!dataRef) return null;
			const widgetRefs = this.listWidgetRefs();
			for (const widgetRef of widgetRefs) if (widgetUsesDataRef(this.getWidget(widgetRef), this.getWidgetState(widgetRef), dataRef)) return widgetRef;
			return null;
		}
		findWidgetRefForSelectionRef(selectionRef) {
			if (!selectionRef) return null;
			const selectionParts = parseRef(selectionRef);
			if (!selectionParts || selectionParts.kind !== "selection" || !selectionParts.widgetId) return null;
			const widgetRefs = this.listWidgetRefs();
			for (const widgetRef of widgetRefs) {
				const widgetDescription = this.getWidget(widgetRef);
				const widgetState = this.getWidgetState(widgetRef);
				if (widgetDescription?.widgetId === selectionParts.widgetId || widgetState?.widgetId === selectionParts.widgetId) return widgetRef;
			}
			return null;
		}
		getResolvedWidgetForTarget(ref) {
			if (!ref) return null;
			const directWidget = this.getResolvedWidget(ref);
			if (directWidget) return directWidget;
			const selectionWidgetRef = this.findWidgetRefForSelectionRef(ref);
			if (selectionWidgetRef) return this.getResolvedWidget(selectionWidgetRef);
			const widgetRef = this.findWidgetRefForDataRef(ref);
			return widgetRef ? this.getResolvedWidget(widgetRef) : null;
		}
		getDataHandle(ref) {
			return ref ? this.dataHandles?.[ref] || null : null;
		}
		getLink(ref) {
			return ref ? this.links?.[ref] || null : null;
		}
		getWidgetAdapter(ref) {
			return ref ? this.widgetAdapters?.[ref] || null : null;
		}
		describe() {
			return clone$5(makeWidgetRegistrySummary({
				counts: makeWidgetRegistryCounts({
					widgetCount: this.listWidgetRefs().length,
					dataHandleCount: this.listDataRefs().length,
					linkCount: this.listLinkRefs().length,
					adapterCount: Object.keys(this.widgetAdapters).length
				}),
				refs: makeWidgetRegistryRefs({
					widgetRefs: this.listWidgetRefs(),
					dataRefs: this.listDataRefs(),
					linkRefs: this.listLinkRefs()
				}),
				widgets: this.widgetEntries,
				dataHandles: Object.values(this.dataHandles || {}),
				links: Object.values(this.links || {})
			}));
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/summarizeWorkspaceState.js
	function clone$4(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function summarizeWorkspaceState(state) {
		if (!state || typeof state !== "object") return null;
		const widgets = Object.values(state.widgets || {});
		const focusedWidgetRef = state.shared?.focusedWidget || widgets[0]?.ref || null;
		const focusedWidget = focusedWidgetRef ? state.widgets?.[focusedWidgetRef] || null : widgets[0] || null;
		const activeSelections = readSelectionRegistry(state.shared || {});
		const selectionEntries = Object.entries(activeSelections);
		const primarySelectionEntry = resolvePrimarySelectionEntry(state.shared || {});
		const changedRefs = Array.isArray(state.delta?.changedRefs) ? state.delta.changedRefs : [];
		const removedRefs = Array.isArray(state.delta?.removedRefs) ? state.delta.removedRefs : [];
		return makeRuntimeStoreCurrentStateSummary({
			stateId: state.stateId || null,
			focusedWidgetRef,
			focusedWidgetKind: focusedWidget?.kind || null,
			focusedWidgetTitle: focusedWidget?.title || null,
			visibleCount: focusedWidget?.data?.visibleCount ?? focusedWidget?.data?.rowCount ?? null,
			selectedCount: focusedWidget?.data?.selectedCount ?? null,
			selectionCount: selectionEntries.length,
			activeSelectionRefs: selectionEntries.map(([selectionRef]) => selectionRef),
			primarySelectionRef: primarySelectionEntry?.[0] || null,
			primarySelectionSummary: primarySelectionEntry?.[1]?.summary || "",
			primarySelectionPredicates: Array.isArray(primarySelectionEntry?.[1]?.predicates) ? clone$4(primarySelectionEntry[1].predicates) : [],
			annotationCount: Array.isArray(state.shared?.annotations) ? state.shared.annotations.length : 0,
			globalFilterCount: Object.keys(state.shared?.globalFilters || {}).length,
			taskMode: state.taskContext?.taskMode || null,
			coordinationScope: state.taskContext?.coordinationScope || null,
			evidenceType: state.taskContext?.evidenceType || null,
			interactionHorizon: state.taskContext?.interactionHorizon || null,
			replayRunMode: state.replayContext?.runMode || null,
			replayUserIntent: state.replayContext?.userIntent || "",
			changedRefs,
			removedRefs,
			sharedChanged: changedRefs.includes("shared"),
			taskContextChanged: changedRefs.includes("taskContext"),
			replayContextChanged: changedRefs.includes("replayContext")
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/RuntimeStore.js
	function makeDescriptorKey(name, targetRef) {
		return `${name || "unknown"}::${targetRef || "workspace"}`;
	}
	function clone$3(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function isPlainObject(value) {
		return value != null && typeof value === "object" && !Array.isArray(value);
	}
	function mergeRuntimePatch(base, patch) {
		if (!isPlainObject(base) || !isPlainObject(patch)) return clone$3(patch);
		const nextValue = { ...clone$3(base) };
		for (const [key, value] of Object.entries(patch)) if (isPlainObject(value) && isPlainObject(nextValue[key])) nextValue[key] = mergeRuntimePatch(nextValue[key], value);
		else nextValue[key] = clone$3(value);
		return nextValue;
	}
	function applyWidgetStatePatches(state, patches = {}) {
		if (!state?.widgets || !isPlainObject(patches) || Object.keys(patches).length === 0) return state;
		const nextWidgets = { ...state.widgets || {} };
		let changed = false;
		for (const [ref, patch] of Object.entries(patches)) {
			if (!nextWidgets[ref] || !isPlainObject(patch)) continue;
			nextWidgets[ref] = mergeRuntimePatch(nextWidgets[ref], patch);
			changed = true;
		}
		return changed ? {
			...state,
			widgets: nextWidgets
		} : state;
	}
	function normalizeWidgetStateMap(widgets = {}) {
		return Object.fromEntries(Object.entries(widgets || {}).map(([ref, widgetState]) => [ref, makeWidgetState({
			ref,
			...clone$3(widgetState)
		})]));
	}
	function normalizeWorkspaceState(state) {
		return makeWorkspaceState({
			...clone$3(state),
			widgets: normalizeWidgetStateMap(state?.widgets || {})
		});
	}
	function getTraceRecordPriority(record) {
		const eventKind = record?.eventKind || null;
		if (eventKind === "action") return 3;
		if (eventKind === "perceptionQuery" || eventKind === "dataQuery") return 2;
		if (eventKind === "systemTransition") return 1;
		return 0;
	}
	function pickRepresentativeTraceRecord(currentRecord, nextRecord) {
		if (!currentRecord) return nextRecord || null;
		if (!nextRecord) return currentRecord;
		return getTraceRecordPriority(nextRecord) >= getTraceRecordPriority(currentRecord) ? nextRecord : currentRecord;
	}
	function buildResponsePreview(content, maxLength = 160) {
		const text = String(content || "").trim();
		if (!text) return null;
		if (!Number.isFinite(maxLength) || maxLength <= 0 || text.length <= maxLength) return text;
		return `${text.slice(0, maxLength - 3)}...`;
	}
	function normalizeActorFilter(actors) {
		if (!Array.isArray(actors) || actors.length === 0) return [];
		return Array.from(new Set(actors.filter((actor) => typeof actor === "string" && actor.length > 0)));
	}
	function createMutableMapFacade({ keys, getEntry, setEntry, deleteEntry }) {
		return new Proxy({}, {
			get(_target, prop) {
				if (typeof prop === "symbol") return void 0;
				return getEntry(prop);
			},
			set(_target, prop, value) {
				if (typeof prop === "symbol") return true;
				setEntry(prop, value);
				return true;
			},
			deleteProperty(_target, prop) {
				if (typeof prop === "symbol") return true;
				deleteEntry(prop);
				return true;
			},
			ownKeys() {
				return keys();
			},
			has(_target, prop) {
				if (typeof prop === "symbol") return false;
				return keys().includes(prop);
			},
			getOwnPropertyDescriptor(_target, prop) {
				if (typeof prop === "symbol") return void 0;
				const value = getEntry(prop);
				if (value == null) return void 0;
				return {
					configurable: true,
					enumerable: true,
					writable: true,
					value
				};
			}
		});
	}
	var WidgetVARuntimeStore = class {
		constructor({ appId = "widgetva-app", workspaceId = "main" } = {}) {
			this.stateManager = new StateManager();
			this.widgetRegistry = new WidgetRegistry();
			this.appId = appId;
			this.workspaceId = workspaceId;
			this.description = makeWorkspaceDescription({
				appId: this.appId,
				workspaceId: this.workspaceId,
				generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
				workspaceCapabilities: ["singleWidgetAnalysis"]
			});
			this.state = makeWorkspaceState({
				stateId: `${this.workspaceId}:empty`,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			});
			this.previousState = null;
			this.stateSnapshots = [];
			this.descriptionIndex = {};
			this.dataHandleIndex = {};
			this.linkIndex = {};
			this.widgetAdapterList = [];
			this.widgetAdapterIndex = {};
			this.actionDescriptors = [];
			this.actionIndex = {};
			this.actionNameIndex = {};
			this.perceptionDescriptors = [];
			this.perceptionIndex = {};
			this.perceptionNameIndex = {};
			this.runtimeData = {};
			this.widgetStatePatches = {};
			this.interactionTrace = [];
			this.responseHistory = [];
			this.replayContext = null;
			this.currentBranchId = "main";
			this.version = 0;
			this.maxSnapshotRetention = 50;
			this.maxTraceRetention = 200;
			this.maxResponseRetention = 100;
			this.branchRegistry = { main: {
				branchId: "main",
				label: "Main Branch",
				originStateId: null,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			} };
			this.pendingTransition = null;
			this.listeners = /* @__PURE__ */ new Set();
			this.descriptions = createMutableMapFacade({
				keys: () => Object.keys(this.descriptionIndex || {}),
				getEntry: (ref) => this.descriptionIndex?.[ref] || null,
				setEntry: (ref, description) => this.upsertWidgetDescription(ref, description),
				deleteEntry: (ref) => this.removeWidgetDescription(ref)
			});
			this.widgets = createMutableMapFacade({
				keys: () => Object.keys(this.state?.widgets || {}),
				getEntry: (ref) => this.state?.widgets?.[ref] || null,
				setEntry: (ref, widgetState) => this.upsertWidgetState(ref, widgetState),
				deleteEntry: (ref) => this.removeWidgetState(ref)
			});
			this.dataHandles = createMutableMapFacade({
				keys: () => Object.keys(this.dataHandleIndex || {}),
				getEntry: (ref) => this.dataHandleIndex?.[ref] || null,
				setEntry: (ref, handle) => this.upsertDataHandle(ref, handle),
				deleteEntry: (ref) => this.removeDataHandleDefinition(ref)
			});
			this.links = createMutableMapFacade({
				keys: () => Object.keys(this.linkIndex || {}),
				getEntry: (ref) => this.linkIndex?.[ref] || null,
				setEntry: (ref, link) => this.upsertLinkDefinition(ref, link),
				deleteEntry: (ref) => this.removeLinkDefinition(ref)
			});
			this.widgetAdapters = createMutableMapFacade({
				keys: () => Object.keys(this.widgetAdapterIndex || {}),
				getEntry: (ref) => this.widgetAdapterIndex?.[ref] || null,
				setEntry: (ref, adapter) => this.upsertWidgetAdapter(ref, adapter),
				deleteEntry: (ref) => this.removeWidgetAdapter(ref)
			});
			this.actions = createMutableMapFacade({
				keys: () => Object.keys(this.actionNameIndex || {}),
				getEntry: (name) => this.getActionDescriptor(name) || null,
				setEntry: (name, descriptor) => this.upsertActionDescriptor(name, descriptor),
				deleteEntry: (name) => this.removeActionDescriptor(name)
			});
			this.perceptionQueries = createMutableMapFacade({
				keys: () => Object.keys(this.perceptionNameIndex || {}),
				getEntry: (name) => this.getPerceptionDescriptor(name) || null,
				setEntry: (name, descriptor) => this.upsertPerceptionDescriptor(name, descriptor),
				deleteEntry: (name) => this.removePerceptionDescriptor(name)
			});
		}
		get stateId() {
			return this.state?.stateId || null;
		}
		subscribe(listener) {
			if (typeof listener !== "function") return () => {};
			this.listeners.add(listener);
			return () => {
				this.listeners.delete(listener);
			};
		}
		emitChange() {
			for (const listener of this.listeners) try {
				listener();
			} catch {}
		}
		syncRegistryWorkspace() {
			this.widgetRegistry.replaceWorkspace({
				description: this.description,
				state: this.state,
				widgetAdapters: this.widgetAdapterList
			});
		}
		upsertWidgetDescription(ref, description) {
			if (!ref || !description || typeof description !== "object") return null;
			const nextDescription = makeWidgetDescription({
				ref,
				...clone$3(description)
			});
			this.descriptionIndex = {
				...this.descriptionIndex || {},
				[ref]: nextDescription
			};
			this.description = makeWorkspaceDescription({
				...this.description,
				widgets: Object.values(this.descriptionIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
			return nextDescription;
		}
		removeWidgetDescription(ref) {
			if (!ref || !this.descriptionIndex?.[ref]) return;
			const nextIndex = { ...this.descriptionIndex || {} };
			delete nextIndex[ref];
			this.descriptionIndex = nextIndex;
			this.description = makeWorkspaceDescription({
				...this.description,
				widgets: Object.values(this.descriptionIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		upsertWidgetState(ref, widgetState) {
			if (!ref || !widgetState || typeof widgetState !== "object") return null;
			this.state = normalizeWorkspaceState({
				...clone$3(this.state),
				widgets: {
					...this.state?.widgets || {},
					[ref]: {
						ref,
						...clone$3(widgetState)
					}
				}
			});
			this.syncRegistryWorkspace();
			this.emitChange();
			return this.state.widgets[ref];
		}
		removeWidgetState(ref) {
			if (!ref || !this.state?.widgets?.[ref]) return;
			const nextWidgets = { ...this.state?.widgets || {} };
			delete nextWidgets[ref];
			this.state = makeWorkspaceState({
				...clone$3(this.state),
				widgets: nextWidgets
			});
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		upsertDataHandle(ref, handle) {
			if (!ref || !handle || typeof handle !== "object") return null;
			const nextHandle = makeDataHandle({
				ref,
				...clone$3(handle)
			});
			this.dataHandleIndex = {
				...this.dataHandleIndex || {},
				[ref]: nextHandle
			};
			this.description = makeWorkspaceDescription({
				...this.description,
				dataHandles: Object.values(this.dataHandleIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
			return nextHandle;
		}
		removeDataHandleDefinition(ref) {
			if (!ref || !this.dataHandleIndex?.[ref]) return;
			const nextIndex = { ...this.dataHandleIndex || {} };
			delete nextIndex[ref];
			this.dataHandleIndex = nextIndex;
			this.description = makeWorkspaceDescription({
				...this.description,
				dataHandles: Object.values(this.dataHandleIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		upsertLinkDefinition(ref, link) {
			if (!ref || !link || typeof link !== "object") return null;
			const nextLink = makeWidgetLink({
				ref,
				...clone$3(link)
			});
			this.linkIndex = {
				...this.linkIndex || {},
				[ref]: nextLink
			};
			this.description = makeWorkspaceDescription({
				...this.description,
				links: Object.values(this.linkIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
			return nextLink;
		}
		removeLinkDefinition(ref) {
			if (!ref || !this.linkIndex?.[ref]) return;
			const nextIndex = { ...this.linkIndex || {} };
			delete nextIndex[ref];
			this.linkIndex = nextIndex;
			this.description = makeWorkspaceDescription({
				...this.description,
				links: Object.values(this.linkIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		upsertWidgetAdapter(ref, adapter) {
			if (!ref || !adapter || typeof adapter !== "object") return null;
			const nextAdapter = adapter.widgetRef === ref ? adapter : {
				...adapter,
				widgetRef: ref
			};
			this.widgetAdapterIndex = {
				...this.widgetAdapterIndex || {},
				[ref]: nextAdapter
			};
			this.widgetAdapterList = Object.values(this.widgetAdapterIndex);
			this.syncRegistryWorkspace();
			this.emitChange();
			return nextAdapter;
		}
		removeWidgetAdapter(ref) {
			if (!ref || !this.widgetAdapterIndex?.[ref]) return;
			const nextIndex = { ...this.widgetAdapterIndex || {} };
			delete nextIndex[ref];
			this.widgetAdapterIndex = nextIndex;
			this.widgetAdapterList = Object.values(nextIndex);
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		rebuildActionIndexes(actionDescriptors = []) {
			this.actionDescriptors = actionDescriptors.map((item) => makeActionDescriptor(item));
			this.actionIndex = Object.fromEntries(this.actionDescriptors.map((item) => [makeDescriptorKey(item.name, item.targetRef), item]));
			this.actionNameIndex = this.actionDescriptors.reduce((acc, item) => {
				if (!acc[item.name]) acc[item.name] = [];
				acc[item.name].push(item);
				return acc;
			}, {});
		}
		rebuildPerceptionIndexes(perceptionDescriptors = []) {
			this.perceptionDescriptors = perceptionDescriptors.map((item) => makePerceptionDescriptor(item));
			this.perceptionIndex = Object.fromEntries(this.perceptionDescriptors.map((item) => [makeDescriptorKey(item.name, item.targetRef), item]));
			this.perceptionNameIndex = this.perceptionDescriptors.reduce((acc, item) => {
				if (!acc[item.name]) acc[item.name] = [];
				acc[item.name].push(item);
				return acc;
			}, {});
		}
		upsertActionDescriptor(name, descriptor) {
			const descriptorName = descriptor?.name || name;
			if (!descriptorName || !descriptor || typeof descriptor !== "object") return null;
			const nextDescriptor = {
				name: descriptorName,
				...clone$3(descriptor)
			};
			const remainingDescriptors = this.actionDescriptors.filter((item) => item?.name !== descriptorName);
			this.rebuildActionIndexes([...remainingDescriptors, nextDescriptor]);
			this.description = makeWorkspaceDescription({
				...this.description,
				actions: [...this.actionDescriptors]
			});
			this.emitChange();
			return nextDescriptor;
		}
		removeActionDescriptor(name) {
			if (!name || !this.actionNameIndex?.[name]?.length) return;
			this.rebuildActionIndexes(this.actionDescriptors.filter((item) => item?.name !== name));
			this.description = makeWorkspaceDescription({
				...this.description,
				actions: [...this.actionDescriptors]
			});
			this.emitChange();
		}
		upsertPerceptionDescriptor(name, descriptor) {
			const descriptorName = descriptor?.name || name;
			if (!descriptorName || !descriptor || typeof descriptor !== "object") return null;
			const nextDescriptor = {
				name: descriptorName,
				...clone$3(descriptor)
			};
			const remainingDescriptors = this.perceptionDescriptors.filter((item) => item?.name !== descriptorName);
			this.rebuildPerceptionIndexes([...remainingDescriptors, nextDescriptor]);
			this.description = makeWorkspaceDescription({
				...this.description,
				perceptionQueries: [...this.perceptionDescriptors]
			});
			this.emitChange();
			return nextDescriptor;
		}
		removePerceptionDescriptor(name) {
			if (!name || !this.perceptionNameIndex?.[name]?.length) return;
			this.rebuildPerceptionIndexes(this.perceptionDescriptors.filter((item) => item?.name !== name));
			this.description = makeWorkspaceDescription({
				...this.description,
				perceptionQueries: [...this.perceptionDescriptors]
			});
			this.emitChange();
		}
		registerWidget(description, widgetState) {
			const ref = description?.ref || widgetState?.ref || null;
			if (!ref || !description || !widgetState) return null;
			const nextDescription = makeWidgetDescription({
				ref,
				...clone$3(description)
			});
			const nextWidgetState = {
				ref,
				...clone$3(widgetState)
			};
			const nextDescriptionIndex = {
				...this.descriptionIndex || {},
				[ref]: nextDescription
			};
			const nextWidgets = {
				...this.state?.widgets || {},
				[ref]: nextWidgetState
			};
			const stateMeta = this.stateManager.createStateMeta({
				workspaceId: this.workspaceId,
				previousState: this.state,
				nextWidgets,
				nextShared: this.state?.shared || {},
				nextTaskContext: this.state?.taskContext,
				nextReplayContext: this.replayContext,
				branchId: this.currentBranchId
			});
			this.descriptionIndex = nextDescriptionIndex;
			this.description = makeWorkspaceDescription({
				...this.description,
				widgets: Object.values(nextDescriptionIndex)
			});
			const nextState = makeWorkspaceState({
				...clone$3(this.state),
				stateId: stateMeta.stateId,
				createdAt: stateMeta.createdAt,
				widgets: nextWidgets
			});
			nextState.shared = {
				...nextState.shared || {},
				globalFilters: deriveGlobalFiltersFromState(nextState)
			};
			this.commitState(nextState, { transitionType: "continue" });
			this.syncRegistryWorkspace();
			return this.state.widgets?.[ref] || null;
		}
		registerLink(link) {
			const ref = link?.ref || null;
			if (!ref || !link || typeof link !== "object") return null;
			const nextLink = makeWidgetLink({
				ref,
				...clone$3(link)
			});
			this.linkIndex = {
				...this.linkIndex || {},
				[ref]: nextLink
			};
			this.description = makeWorkspaceDescription({
				...this.description,
				links: Object.values(this.linkIndex)
			});
			this.syncRegistryWorkspace();
			this.version += 1;
			this.emitChange();
			return nextLink;
		}
		replaceWorkspace(nextWorkspace) {
			const transition = this.pendingTransition || {};
			const normalizedDescriptions = Array.isArray(nextWorkspace.description?.widgets) ? nextWorkspace.description.widgets.map((item) => makeWidgetDescription(item)) : [];
			const normalizedActions = Array.isArray(nextWorkspace.description?.actions) ? nextWorkspace.description.actions.map((item) => makeActionDescriptor(item)) : [];
			const normalizedPerceptionQueries = Array.isArray(nextWorkspace.description?.perceptionQueries) ? nextWorkspace.description.perceptionQueries.map((item) => makePerceptionDescriptor(item)) : [];
			this.appId = nextWorkspace.description?.appId || this.appId;
			this.workspaceId = nextWorkspace.description?.workspaceId || this.workspaceId;
			this.descriptionIndex = Object.fromEntries(normalizedDescriptions.map((item) => [item.ref, item]));
			this.dataHandleIndex = Object.fromEntries((nextWorkspace.description?.dataHandles || []).map((item) => {
				const normalizedHandle = makeDataHandle(item);
				return [normalizedHandle.ref, normalizedHandle];
			}));
			this.linkIndex = Object.fromEntries((nextWorkspace.description?.links || []).map((item) => {
				const normalizedLink = makeWidgetLink(item);
				return [normalizedLink.ref, normalizedLink];
			}));
			this.description = makeWorkspaceDescription({
				...nextWorkspace.description,
				widgets: Object.values(this.descriptionIndex),
				dataHandles: Object.values(this.dataHandleIndex),
				links: Object.values(this.linkIndex),
				actions: normalizedActions,
				perceptionQueries: normalizedPerceptionQueries
			});
			this.widgetAdapterList = Array.isArray(nextWorkspace.widgetAdapters) ? nextWorkspace.widgetAdapters : [];
			this.widgetAdapterIndex = Object.fromEntries(this.widgetAdapterList.map((adapter) => [adapter.widgetRef, adapter]));
			this.rebuildActionIndexes(normalizedActions);
			this.rebuildPerceptionIndexes(normalizedPerceptionQueries);
			this.runtimeData = nextWorkspace.runtimeData || {};
			this.replayContext = clone$3(nextWorkspace.replayContext || null);
			const patchedState = applyWidgetStatePatches(nextWorkspace.state, this.widgetStatePatches);
			this.widgetRegistry.replaceWorkspace({
				description: this.description,
				state: patchedState,
				widgetAdapters: this.widgetAdapterList
			});
			this.commitState(patchedState, transition);
			this.syncCurrentViewRuntimeData();
			this.syncCurrentSelectionRuntimeData();
		}
		readDescription() {
			return this.description;
		}
		listWidgetDescriptions() {
			return Object.values(this.descriptionIndex);
		}
		getWidgetDescription(ref) {
			return ref ? this.widgetRegistry.getWidget(ref) : null;
		}
		getWidgetState(ref) {
			return ref ? this.widgetRegistry.getWidgetState(ref) : null;
		}
		getWidgetEntry(ref) {
			return ref ? this.widgetRegistry.getWidgetEntry(ref) : null;
		}
		getResolvedWidget(ref) {
			return ref ? this.widgetRegistry.getResolvedWidget(ref) : null;
		}
		getResolvedWidgetForTarget(ref) {
			if (!ref) return null;
			const directTarget = this.widgetRegistry.getResolvedWidgetForTarget(ref);
			if (directTarget) return directTarget;
			const widgetRef = this.readRuntimeData(ref)?.widgetRef || null;
			return widgetRef ? this.widgetRegistry.getResolvedWidget(widgetRef) : null;
		}
		listDataHandles() {
			return Object.values(this.dataHandleIndex);
		}
		getDataHandle(ref) {
			return ref ? this.widgetRegistry.getDataHandle(ref) : null;
		}
		listWidgetAdapters() {
			return [...this.widgetAdapterList];
		}
		getWidgetAdapter(widgetRef) {
			return widgetRef ? this.widgetAdapterIndex?.[widgetRef] || null : null;
		}
		describeWidgetRegistry() {
			return this.widgetRegistry.describe();
		}
		listLinks() {
			return Object.values(this.linkIndex);
		}
		getLink(ref) {
			return ref ? this.widgetRegistry.getLink(ref) : null;
		}
		listActions() {
			return [...this.actionDescriptors];
		}
		listPerceptionQueries() {
			return [...this.perceptionDescriptors];
		}
		getActionDescriptor(name, targetRef) {
			if (!name) return null;
			if (targetRef) return this.actionIndex?.[makeDescriptorKey(name, targetRef)] || null;
			const matches = this.actionNameIndex?.[name] || [];
			if (matches.length === 1) return matches[0];
			return matches.find((item) => item.scope === "workspace") || matches[0] || null;
		}
		getPerceptionDescriptor(name, targetRef) {
			if (!name) return null;
			if (targetRef) return this.perceptionIndex?.[makeDescriptorKey(name, targetRef)] || null;
			const matches = this.perceptionNameIndex?.[name] || [];
			if (matches.length === 1) return matches[0];
			return matches.find((item) => item.scope === "workspace") || matches[0] || null;
		}
		readState(options = {}) {
			if (options.deltaSince) {
				const baseSnapshot = this.stateSnapshots.find((snapshot) => snapshot.stateId === options.deltaSince);
				if (baseSnapshot) {
					const delta = this.stateManager.createDelta({
						previousState: baseSnapshot.state,
						nextState: this.state
					});
					return this.stateManager.pickRefs({
						state: {
							...this.state,
							delta
						},
						replayContext: this.replayContext,
						refs: Array.isArray(options.refs) && options.refs.length > 0 ? (delta.changedRefs || []).filter((ref) => options.refs.includes(ref)) : delta.changedRefs || []
					});
				}
			}
			if (options.deltaSince && this.previousState?.stateId === options.deltaSince) return this.stateManager.pickRefs({
				state: this.state,
				replayContext: this.replayContext,
				refs: Array.isArray(options.refs) && options.refs.length > 0 ? (this.state?.delta?.changedRefs || []).filter((ref) => options.refs.includes(ref)) : this.state?.delta?.changedRefs || []
			});
			if (Array.isArray(options.refs) && options.refs.length > 0) return this.stateManager.pickRefs({
				state: this.state,
				replayContext: this.replayContext,
				refs: options.refs
			});
			return this.state;
		}
		buildStatePatch(refs) {
			return this.stateManager.buildStatePatch({
				state: this.state,
				replayContext: this.replayContext,
				refs
			});
		}
		summarizeState(state = this.state) {
			return summarizeWorkspaceState(state === this.state ? {
				...state,
				replayContext: clone$3(this.replayContext)
			} : state);
		}
		patchWidget(ref, patch = {}) {
			const currentWidget = this.state?.widgets?.[ref];
			if (!currentWidget) throw new Error(`Unknown widget: ${ref}`);
			this.widgetStatePatches = {
				...this.widgetStatePatches,
				[ref]: mergeRuntimePatch(this.widgetStatePatches?.[ref] || {}, patch)
			};
			const nextWidgets = {
				...this.state?.widgets || {},
				[ref]: mergeRuntimePatch({
					...clone$3(currentWidget),
					version: (currentWidget.version || 0) + 1,
					updatedAt: (/* @__PURE__ */ new Date()).toISOString()
				}, patch)
			};
			const stateMeta = this.stateManager.createStateMeta({
				workspaceId: this.workspaceId,
				previousState: this.state,
				nextWidgets,
				nextShared: this.state?.shared || {},
				nextTaskContext: this.state?.taskContext,
				nextReplayContext: this.replayContext,
				branchId: this.currentBranchId
			});
			const nextState = makeWorkspaceState({
				...clone$3(this.state),
				stateId: stateMeta.stateId,
				createdAt: stateMeta.createdAt,
				widgets: nextWidgets
			});
			nextState.shared = {
				...nextState.shared || {},
				globalFilters: deriveGlobalFiltersFromState(nextState)
			};
			this.commitState(nextState, { transitionType: "continue" });
			return nextState;
		}
		resetWidgetPatches() {
			this.widgetStatePatches = {};
		}
		clearWidgetPatches(refs = []) {
			if (!Array.isArray(refs) || refs.length === 0) return;
			const nextPatches = { ...this.widgetStatePatches || {} };
			let changed = false;
			for (const ref of refs) {
				if (!ref || !Object.prototype.hasOwnProperty.call(nextPatches, ref)) continue;
				delete nextPatches[ref];
				changed = true;
			}
			if (changed) this.widgetStatePatches = nextPatches;
		}
		readRuntimeData(ref) {
			return ref ? this.runtimeData?.[ref] || null : this.runtimeData;
		}
		resolveSelectionDataRef(selectionRef) {
			const parts = parseRef(selectionRef);
			if (!parts?.widgetId || !parts?.selectionId) return null;
			return makeSelectionScopedDataRef({
				appId: parts.appId || this.appId,
				workspaceId: parts.workspaceId || this.workspaceId,
				widgetId: parts.widgetId,
				selectionId: parts.selectionId
			});
		}
		updateRuntimeData(ref, updater) {
			if (!ref || typeof updater !== "function") return null;
			const currentEntry = this.runtimeData?.[ref];
			if (!currentEntry) return null;
			const nextEntry = updater(clone$3(currentEntry));
			if (!nextEntry || typeof nextEntry !== "object") return currentEntry;
			this.upsertRuntimeData(ref, nextEntry);
			this.emitChange();
			return nextEntry;
		}
		upsertRuntimeData(ref, entry) {
			if (!ref || !entry || typeof entry !== "object") return null;
			this.runtimeData = {
				...this.runtimeData || {},
				[ref]: entry
			};
			const nextHandle = entry.handle || this.dataHandleIndex?.[ref] || null;
			if (nextHandle) {
				this.dataHandleIndex = {
					...this.dataHandleIndex || {},
					[ref]: nextHandle
				};
				this.widgetRegistry.updateDataHandle?.(ref, nextHandle);
			}
			if (Array.isArray(this.description?.dataHandles)) {
				const hasExistingHandle = this.description.dataHandles.some((handle) => handle?.ref === ref);
				this.description = {
					...this.description,
					dataHandles: hasExistingHandle ? this.description.dataHandles.map((handle) => handle?.ref === ref ? nextHandle || handle : handle) : nextHandle ? [...this.description.dataHandles, nextHandle] : this.description.dataHandles
				};
			}
			return entry;
		}
		removeRuntimeData(ref) {
			if (!ref || !this.runtimeData?.[ref]) return;
			const nextRuntimeData = { ...this.runtimeData || {} };
			delete nextRuntimeData[ref];
			this.runtimeData = nextRuntimeData;
			if (this.dataHandleIndex?.[ref]) {
				const nextDataHandleIndex = { ...this.dataHandleIndex || {} };
				delete nextDataHandleIndex[ref];
				this.dataHandleIndex = nextDataHandleIndex;
				this.widgetRegistry.removeDataHandle?.(ref);
			}
			if (Array.isArray(this.description?.dataHandles)) this.description = {
				...this.description,
				dataHandles: this.description.dataHandles.filter((handle) => handle?.ref !== ref)
			};
		}
		syncCurrentSelectionRuntimeData() {
			const currentSelectionDataRef = makeCurrentSelectionDataRef({
				appId: this.appId,
				workspaceId: this.workspaceId
			});
			const focusedWidgetRef = this.state?.shared?.focusedWidget || Object.keys(this.state?.widgets || {})[0] || this.listWidgetDescriptions()[0]?.ref || null;
			const focusedWidgetId = (focusedWidgetRef ? this.getWidgetState(focusedWidgetRef) : null)?.widgetId || null;
			const focusedSelectionDataRef = focusedWidgetId ? makeWidgetSelectionDataRef({
				appId: this.appId,
				workspaceId: this.workspaceId,
				widgetId: focusedWidgetId
			}) : null;
			const focusedSelectionEntry = focusedSelectionDataRef ? this.readRuntimeData(focusedSelectionDataRef) : null;
			const focusedSelectionRef = focusedWidgetRef ? Object.keys(this.state?.widgets?.[focusedWidgetRef]?.selections || {})[0] || null : null;
			if (!focusedSelectionEntry?.widgetRef || !Array.isArray(focusedSelectionEntry?.rows)) {
				this.removeRuntimeData(currentSelectionDataRef);
				return null;
			}
			const currentSelectionHandle = buildDerivedDataHandle({
				ref: currentSelectionDataRef,
				title: "Current Selection Data",
				description: "Current rows for the focused widget selection.",
				rows: focusedSelectionEntry.rows,
				selectedCount: focusedSelectionEntry.rows.length,
				kind: "selectionData",
				scope: "workspaceCurrent",
				widgetRef: focusedSelectionEntry.widgetRef,
				sourceSelectionRef: focusedSelectionRef
			});
			const nextEntry = {
				ref: currentSelectionDataRef,
				rows: focusedSelectionEntry.rows,
				baseRows: focusedSelectionEntry.baseRows,
				handle: currentSelectionHandle,
				widgetRef: focusedSelectionEntry.widgetRef,
				sourceSelectionRef: focusedSelectionRef,
				kind: "selectionData",
				scope: "workspaceCurrent"
			};
			this.upsertRuntimeData(currentSelectionDataRef, nextEntry);
			return nextEntry;
		}
		syncCurrentViewRuntimeData() {
			const currentViewDataRef = makeCurrentViewDataRef({
				appId: this.appId,
				workspaceId: this.workspaceId
			});
			const focusedWidgetRef = this.state?.shared?.focusedWidget || Object.keys(this.state?.widgets || {})[0] || this.listWidgetDescriptions()[0]?.ref || null;
			const focusedWidget = focusedWidgetRef ? this.getWidgetState(focusedWidgetRef) : null;
			const focusedDataRef = focusedWidget?.data?.currentDataRef || focusedWidget?.data?.sourceDataRef || null;
			const focusedDataEntry = focusedDataRef ? this.readRuntimeData(focusedDataRef) : null;
			if (!focusedDataEntry?.widgetRef || !Array.isArray(focusedDataEntry?.rows)) {
				this.removeRuntimeData(currentViewDataRef);
				return null;
			}
			const currentViewHandle = buildDerivedDataHandle({
				ref: currentViewDataRef,
				title: "Current View Data",
				description: "Current visible rows for the focused widget.",
				rows: focusedDataEntry.rows,
				selectedCount: focusedDataEntry.handle?.stats?.selectedCount || 0,
				kind: "dataView",
				scope: "workspaceCurrentView",
				widgetRef: focusedDataEntry.widgetRef
			});
			const nextEntry = {
				ref: currentViewDataRef,
				rows: focusedDataEntry.rows,
				baseRows: focusedDataEntry.baseRows,
				handle: currentViewHandle,
				widgetRef: focusedDataEntry.widgetRef,
				kind: "dataView",
				scope: "workspaceCurrentView"
			};
			this.upsertRuntimeData(currentViewDataRef, nextEntry);
			return nextEntry;
		}
		syncSelectionRuntimeData(widgetRef) {
			if (!widgetRef) return null;
			const widgetState = this.getWidgetState(widgetRef);
			const widgetDescription = this.getWidgetDescription(widgetRef);
			const widgetId = widgetState?.widgetId || widgetDescription?.widgetId || null;
			const managedSelectionDataRefs = Object.values(this.runtimeData || {}).filter((entry) => entry?.widgetRef === widgetRef && entry?.kind === "selectionData").map((entry) => entry?.ref).filter((ref) => typeof ref === "string");
			const currentDataRef = widgetState?.data?.currentDataRef || null;
			const currentDataEntry = currentDataRef ? this.readRuntimeData(currentDataRef) : null;
			if (!widgetId || !currentDataEntry) {
				for (const managedRef of managedSelectionDataRefs) this.removeRuntimeData(managedRef);
				this.syncCurrentViewRuntimeData();
				this.syncCurrentSelectionRuntimeData();
				this.emitChange();
				return null;
			}
			const activeSelectionEntries = Object.entries(widgetState?.selections || {}).filter(([selectionRef, selectionState]) => Boolean(selectionRef) && Boolean(selectionState));
			const selectionDataRef = makeWidgetSelectionDataRef({
				appId: this.appId,
				workspaceId: this.workspaceId,
				widgetId
			});
			if (activeSelectionEntries.length === 0) {
				for (const managedRef of managedSelectionDataRefs) this.removeRuntimeData(managedRef);
				this.syncCurrentViewRuntimeData();
				this.syncCurrentSelectionRuntimeData();
				this.emitChange();
				return null;
			}
			const visibleRows = Array.isArray(currentDataEntry?.rows) ? currentDataEntry.rows : [];
			const selections = activeSelectionEntries.map(([, selectionState]) => selectionState);
			const selectedRows = visibleRows.filter((row) => rowMatchesAnySelection(row, selections));
			const nextManagedRefs = new Set([selectionDataRef]);
			const nextEntry = {
				ref: selectionDataRef,
				rows: selectedRows,
				baseRows: visibleRows,
				handle: buildDerivedDataHandle({
					ref: selectionDataRef,
					title: `${widgetDescription?.title || widgetId} Selection Data`,
					description: `Current rows selected on widget ${widgetId}.`,
					rows: selectedRows,
					selectedCount: selectedRows.length,
					kind: "selectionData",
					scope: "combined",
					widgetRef
				}),
				widgetRef,
				kind: "selectionData",
				scope: "combined"
			};
			this.upsertRuntimeData(selectionDataRef, nextEntry);
			for (const [selectionRef, selectionState] of activeSelectionEntries) {
				const selectionScopedDataRef = this.resolveSelectionDataRef(selectionRef);
				if (!selectionScopedDataRef) continue;
				nextManagedRefs.add(selectionScopedDataRef);
				const selectionId = selectionRef.split("/").pop() || "selection";
				const selectionRows = visibleRows.filter((row) => rowMatchesSelection(row, selectionState));
				const selectionScopedHandle = buildDerivedDataHandle({
					ref: selectionScopedDataRef,
					title: `${widgetDescription?.title || widgetId} Selection ${selectionId} Data`,
					description: `Current rows for selection ${selectionId} on widget ${widgetId}.`,
					rows: selectionRows,
					selectedCount: selectionRows.length,
					kind: "selectionData",
					scope: "selection",
					widgetRef,
					sourceSelectionRef: selectionRef
				});
				this.upsertRuntimeData(selectionScopedDataRef, {
					ref: selectionScopedDataRef,
					rows: selectionRows,
					baseRows: visibleRows,
					handle: selectionScopedHandle,
					widgetRef,
					sourceSelectionRef: selectionRef,
					kind: "selectionData",
					scope: "selection"
				});
			}
			for (const managedRef of managedSelectionDataRefs) if (!nextManagedRefs.has(managedRef)) this.removeRuntimeData(managedRef);
			this.syncCurrentViewRuntimeData();
			this.syncCurrentSelectionRuntimeData();
			this.emitChange();
			return nextEntry;
		}
		readSnapshot(stateId) {
			const snapshot = this.stateSnapshots.find((item) => item.stateId === stateId);
			return snapshot ? clone$3(snapshot.state) : null;
		}
		readSnapshotEntry(stateId) {
			const snapshot = this.stateSnapshots.find((item) => item.stateId === stateId);
			return snapshot ? clone$3(snapshot) : null;
		}
		readCurrentSnapshotMeta() {
			const stateId = this.state?.stateId;
			return stateId ? this.readSnapshotEntry(stateId) : null;
		}
		describeStore() {
			return makeRuntimeStoreSummary({
				identity: makeRuntimeStoreIdentity({
					appId: this.appId || null,
					workspaceId: this.workspaceId || null
				}),
				state: makeRuntimeStoreStateSummary({
					stateId: this.readState()?.stateId || null,
					currentBranchId: this.currentBranchId || null,
					previousStateId: this.previousState?.stateId || null,
					version: this.version || 0
				}),
				currentStateSummary: this.summarizeState(),
				indexes: makeRuntimeStoreIndexes({
					widgetCount: this.listWidgetDescriptions().length,
					dataHandleCount: this.listDataHandles().length,
					linkCount: this.listLinks().length,
					widgetAdapterCount: this.listWidgetAdapters().length,
					actionDescriptorCount: this.listActions().length,
					perceptionDescriptorCount: this.listPerceptionQueries().length,
					widgetPatchCount: Object.keys(this.widgetStatePatches || {}).length
				}),
				history: makeRuntimeStoreHistory({
					snapshotCount: Array.isArray(this.stateSnapshots) ? this.stateSnapshots.length : 0,
					traceCount: Array.isArray(this.interactionTrace) ? this.interactionTrace.length : 0,
					responseCount: Array.isArray(this.responseHistory) ? this.responseHistory.length : 0,
					branchCount: this.listBranches().length,
					retention: makeRuntimeStoreHistoryRetention({
						snapshotMax: this.maxSnapshotRetention,
						traceMax: this.maxTraceRetention,
						responseMax: this.maxResponseRetention
					}),
					maxSnapshotRetention: this.maxSnapshotRetention,
					maxTraceRetention: this.maxTraceRetention,
					maxResponseRetention: this.maxResponseRetention
				}),
				capabilities: makeRuntimeStoreCapabilities({
					deltaTracking: true,
					snapshotHistory: true,
					actorScopedHistory: true,
					branchReplay: true,
					traceGraph: true,
					runtimeDataIndex: true,
					adapterRegistry: true,
					widgetStatePatching: true
				})
			});
		}
		buildRepresentativeTraceMap() {
			const traceByStateId = /* @__PURE__ */ new Map();
			for (const record of this.interactionTrace) {
				if (!record?.stateId) continue;
				traceByStateId.set(record.stateId, pickRepresentativeTraceRecord(traceByStateId.get(record.stateId), record));
			}
			return traceByStateId;
		}
		listStateSnapshots(options = 50) {
			const normalizedOptions = typeof options === "number" ? { limit: options } : options || {};
			const limit = Number.isFinite(normalizedOptions.limit) ? normalizedOptions.limit : 50;
			const sinceStateId = normalizedOptions.sinceStateId || null;
			const allowedActors = normalizeActorFilter(normalizedOptions.actors);
			const traceByStateId = this.buildRepresentativeTraceMap();
			const fullSnapshots = Array.isArray(this.stateSnapshots) ? this.stateSnapshots : [];
			const scopedSnapshots = sinceStateId ? (() => {
				const idx = fullSnapshots.findIndex((snapshot) => snapshot?.stateId === sinceStateId);
				return idx >= 0 ? fullSnapshots.slice(idx + 1) : fullSnapshots;
			})() : fullSnapshots;
			return (allowedActors.length > 0 ? scopedSnapshots.filter((snapshot) => {
				const actor = traceByStateId.get(snapshot?.stateId)?.actor || "system";
				return allowedActors.includes(actor);
			}) : scopedSnapshots).slice(-limit).map((item) => {
				const representativeTrace = traceByStateId.get(item.stateId);
				return makeStateSnapshotMeta({
					stateId: item.stateId,
					createdAt: item.createdAt || item.state?.createdAt || null,
					baseStateId: (item.parentStateId ?? item.state?.delta?.baseStateId) || null,
					branchId: item.branchId || null,
					transitionType: item.transitionType || "continue",
					branchLabel: item.branchLabel || null,
					actor: representativeTrace?.actor || "system",
					changedRefs: Array.isArray(item.state?.delta?.changedRefs) ? [...item.state.delta.changedRefs] : [],
					removedRefs: Array.isArray(item.state?.delta?.removedRefs) ? [...item.state.delta.removedRefs] : []
				});
			});
		}
		commitState(nextState, transition = {}) {
			const previousReplayContext = clone$3(this.state?.replayContext);
			this.previousState = this.state;
			this.state = normalizeWorkspaceState(nextState);
			this.state.replayContext = clone$3(this.replayContext);
			this.widgetRegistry.setState(this.state);
			this.version += 1;
			this.state.delta = this.stateManager.createDelta({
				previousState: this.previousState,
				nextState: this.state,
				previousReplayContext,
				nextReplayContext: this.replayContext
			});
			const snapshotEntry = {
				stateId: this.state.stateId,
				state: clone$3(this.state),
				replayContext: clone$3(this.replayContext || null),
				branchId: transition.branchId || this.currentBranchId,
				parentStateId: transition.parentStateId ?? this.previousState?.stateId ?? null,
				transitionType: transition.transitionType || "continue",
				branchLabel: transition.branchLabel || null,
				createdAt: this.state.createdAt || (/* @__PURE__ */ new Date()).toISOString()
			};
			const hasStateChange = (this.state.delta?.changedRefs?.length || 0) > 0 || (this.state.delta?.removedRefs?.length || 0) > 0;
			const latestSnapshot = this.stateSnapshots[this.stateSnapshots.length - 1] || null;
			if (!latestSnapshot || latestSnapshot.stateId !== snapshotEntry.stateId || snapshotEntry.transitionType !== "continue" || hasStateChange) {
				this.stateSnapshots.push(snapshotEntry);
				if (this.stateSnapshots.length > this.maxSnapshotRetention) this.stateSnapshots = this.stateSnapshots.slice(-this.maxSnapshotRetention);
			}
			this.syncCurrentViewRuntimeData();
			this.syncCurrentSelectionRuntimeData();
			this.pendingTransition = null;
			this.emitChange();
			return this.state;
		}
		listBranches() {
			return Object.values(this.branchRegistry).map((entry) => makeBranchSummary(clone$3(entry)));
		}
		beginTransition({ transitionType = "continue", parentStateId = void 0, branchId = void 0, branchLabel = void 0 } = {}) {
			this.pendingTransition = {
				transitionType,
				parentStateId,
				branchId,
				branchLabel
			};
		}
		beginBranchFromState({ stateId, label } = {}) {
			if (!stateId) throw new Error("beginBranchFromState requires a stateId.");
			const existing = this.readSnapshotEntry(stateId);
			if (!existing) throw new Error(`Unknown branch origin state: ${stateId}`);
			const branchId = `branch_${Date.now()}`;
			const branchLabel = label || `Branch from ${stateId}`;
			this.branchRegistry[branchId] = {
				branchId,
				label: branchLabel,
				originStateId: stateId,
				parentBranchId: existing.branchId || this.currentBranchId,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			};
			this.currentBranchId = branchId;
			this.beginTransition({
				transitionType: "branch",
				parentStateId: stateId,
				branchId,
				branchLabel
			});
			return this.branchRegistry[branchId];
		}
		buildTraceGraph(options = 50) {
			const normalizedOptions = typeof options === "number" ? { limit: options } : options || {};
			const limit = Number.isFinite(normalizedOptions.limit) ? normalizedOptions.limit : 50;
			const sinceStateId = normalizedOptions.sinceStateId || null;
			const allowedActors = normalizeActorFilter(normalizedOptions.actors);
			const fullSnapshots = Array.isArray(this.stateSnapshots) ? this.stateSnapshots : [];
			const scopedSnapshots = sinceStateId ? (() => {
				const idx = fullSnapshots.findIndex((snapshot) => snapshot?.stateId === sinceStateId);
				return idx >= 0 ? fullSnapshots.slice(idx + 1) : fullSnapshots;
			})() : fullSnapshots;
			const traceByStateId = this.buildRepresentativeTraceMap();
			const snapshots = (allowedActors.length > 0 ? scopedSnapshots.filter((snapshot) => {
				const actor = traceByStateId.get(snapshot?.stateId)?.actor || "system";
				return allowedActors.includes(actor);
			}) : scopedSnapshots).slice(-limit);
			const latestResponseByStateId = /* @__PURE__ */ new Map();
			for (const response of this.responseHistory) {
				if (!response?.stateId) continue;
				latestResponseByStateId.set(response.stateId, response);
			}
			const nodes = snapshots.map((snapshot) => {
				const record = traceByStateId.get(snapshot.stateId);
				const response = latestResponseByStateId.get(snapshot.stateId);
				const queryName = getInteractionTraceQueryName(record?.query);
				return makeTraceGraphNode({
					id: snapshot.stateId,
					stateId: snapshot.stateId,
					parentStateId: snapshot.parentStateId || null,
					branchId: snapshot.branchId || "main",
					branchLabel: snapshot.branchLabel || null,
					timestamp: snapshot.createdAt || snapshot.state?.createdAt || null,
					actor: record?.actor || "system",
					eventFamily: record?.eventFamily || getInteractionTraceEventFamily(record),
					querySurface: record?.querySurface || getInteractionTraceQuerySurface(record),
					actionName: record?.action?.name || null,
					queryName,
					responseId: response?.responseId || null,
					responseActor: response?.actor || null,
					responsePreview: buildResponsePreview(response?.content),
					label: record?.action?.name || queryName || snapshot.transitionType || "state",
					transitionType: snapshot.transitionType || "continue",
					current: snapshot.stateId === this.state?.stateId
				});
			});
			const nodeIds = new Set(nodes.map((node) => node.id));
			const edges = snapshots.filter((snapshot) => snapshot.parentStateId).filter((snapshot) => nodeIds.has(snapshot.stateId) && nodeIds.has(snapshot.parentStateId)).map((snapshot) => makeTraceGraphEdge({
				id: `${snapshot.parentStateId}->${snapshot.stateId}`,
				from_id: snapshot.parentStateId,
				to_id: snapshot.stateId,
				edge_type: snapshot.transitionType || "continue",
				branchId: snapshot.branchId || "main",
				label: snapshot.transitionType || "continue",
				timestamp: snapshot.createdAt || snapshot.state?.createdAt || null
			}));
			return makeTraceGraph({
				current_state_id: this.state?.stateId || null,
				current_branch_id: this.currentBranchId,
				sinceStateId,
				actors: allowedActors,
				branches: this.listBranches(),
				nodes,
				edges
			});
		}
		readTrace(limit = 50) {
			return this.interactionTrace.slice(-limit);
		}
		readTraceWindow({ limit = 50, sinceStateId = null, actors = [] } = {}) {
			const fullTrace = Array.isArray(this.interactionTrace) ? this.interactionTrace : [];
			const scopedTrace = sinceStateId ? (() => {
				const idx = fullTrace.findIndex((record) => record?.stateId === sinceStateId);
				return idx >= 0 ? fullTrace.slice(idx + 1) : fullTrace;
			})() : fullTrace;
			const allowedActors = normalizeActorFilter(actors);
			const actorScopedTrace = allowedActors.length > 0 ? scopedTrace.filter((record) => allowedActors.includes(record?.actor || "system")) : scopedTrace;
			if (!Number.isFinite(limit) || limit <= 0) return [...actorScopedTrace];
			return actorScopedTrace.slice(-limit);
		}
		appendTrace(record) {
			this.interactionTrace.push(makeInteractionTraceRecord(record));
			if (this.interactionTrace.length > this.maxTraceRetention) this.interactionTrace = this.interactionTrace.slice(-this.maxTraceRetention);
			this.version += 1;
			this.emitChange();
		}
		listResponses(limit = 20, options = {}) {
			const workspaceId = options.workspaceId || this.workspaceId || null;
			return (workspaceId ? this.responseHistory.filter((item) => item?.workspaceId === workspaceId) : this.responseHistory).slice(-limit).map((item) => clone$3(item));
		}
		readLatestResponse(options = {}) {
			return this.listResponses(1, options)[0] || null;
		}
		appendResponse(record) {
			this.responseHistory.push(makeAgentResponseRecord({
				workspaceId: this.workspaceId || null,
				branchId: this.currentBranchId || null,
				...record
			}));
			if (this.responseHistory.length > this.maxResponseRetention) this.responseHistory = this.responseHistory.slice(-this.maxResponseRetention);
			this.version += 1;
			this.emitChange();
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/PerceptionContext.js
	var PerceptionContext = class {
		constructor({ store, descriptor, call, traceRecorder, hostBridge }) {
			this.store = store;
			this.descriptor = descriptor;
			this.call = call;
			this.traceRecorder = traceRecorder;
			this.hostBridge = hostBridge || createDefaultWidgetVAHostBridge();
			this.pendingQueryTrace = null;
		}
		static describeContract() {
			return makePerceptionContextSummary({
				methods: [
					"readDescription",
					"readCurrentState",
					"readSelectionRegistry",
					"readFocusState",
					"readHighlightState",
					"readQueryScope",
					"readCallParams",
					"readFocusedWidgetRef",
					"resolveTargetWidget",
					"requireTargetWidget",
					"resolveSelectionDataRef",
					"resolveDataRefForWidget",
					"resolveRowsForWidget",
					"recordQuery"
				],
				capabilities: makePerceptionContextCapabilities({
					workspaceRead: true,
					runtimeDataRead: true,
					widgetTargetResolution: true,
					dataHandleResolution: true,
					selectionScopedQueries: true,
					traceRecording: true
				}),
				integrations: makePerceptionContextIntegrations({
					store: true,
					traceRecorder: true
				})
			});
		}
		readDescription() {
			return readWorkspaceDescriptionFromStore(this.store);
		}
		readCurrentState(options = {}) {
			return readWorkspaceStateFromStore(this.store, options);
		}
		readSelectionRegistry() {
			const hostRegistry = this.hostBridge.readSelectionRegistry?.();
			if (hostRegistry && typeof hostRegistry === "object" && !Array.isArray(hostRegistry) && Object.keys(hostRegistry).length > 0) return hostRegistry;
			return readSelectionRegistry(this.readCurrentState()?.shared || {});
		}
		readFocusState() {
			const hostFocusState = this.hostBridge.readFocusState?.();
			if (hostFocusState && typeof hostFocusState === "object" && !Array.isArray(hostFocusState)) return hostFocusState;
			const currentState = this.readCurrentState();
			return readFocusState(currentState?.shared || {}, currentState?.widgets || {});
		}
		readHighlightState() {
			const hostHighlightState = this.hostBridge.readHighlightState?.();
			if (hostHighlightState && typeof hostHighlightState === "object" && !Array.isArray(hostHighlightState)) return hostHighlightState;
			return deriveHighlightState(this.readCurrentState() || {});
		}
		readQueryScope() {
			return readNormalizedQueryScope({
				call: this.call,
				descriptor: this.descriptor
			});
		}
		readCallParams() {
			return this.normalizeScopedParams(this.call?.params || {});
		}
		readFocusedWidgetRef() {
			const hostFocusedWidgetRef = this.hostBridge.readFocusedWidgetRef?.();
			if (typeof hostFocusedWidgetRef === "string" && hostFocusedWidgetRef.length > 0) return hostFocusedWidgetRef;
			const hostFocusWidgetRef = this.readFocusState()?.widgetRef;
			if (typeof hostFocusWidgetRef === "string" && hostFocusWidgetRef.length > 0) return hostFocusWidgetRef;
			return this.readCurrentState()?.shared?.focusedWidget || null;
		}
		resolveTargetWidget({ kind, targetRef } = {}) {
			const queryScope = this.readQueryScope();
			const resolvedRef = targetRef || queryScope?.widgetRef || null;
			if (resolvedRef) {
				const explicitTarget = resolveWidgetRecordFromStore(this.store, resolvedRef);
				if (explicitTarget && (!kind || explicitTarget.kind === kind)) return explicitTarget;
				return null;
			}
			const focusedWidgetRef = this.readFocusedWidgetRef();
			if (focusedWidgetRef) {
				const focusedWidget = resolveWidgetRecordFromStore(this.store, focusedWidgetRef);
				if (focusedWidget && (!kind || focusedWidget.kind === kind)) return focusedWidget;
			}
			const widgets = listStoreWidgets(this.store);
			if (kind) {
				const match = widgets.find((widget) => widget.kind === kind) || null;
				return match ? resolveWidgetRecordFromStore(this.store, match.ref) || match : null;
			}
			const firstWidget = widgets[0] || null;
			return firstWidget ? resolveWidgetRecordFromStore(this.store, firstWidget.ref) || firstWidget : null;
		}
		requireTargetWidget({ kind = null, targetRef = null, message = null } = {}) {
			const targetWidget = this.resolveTargetWidget({
				kind,
				targetRef
			});
			if (targetWidget) return targetWidget;
			if (message) throw new Error(message);
			if (kind) throw new Error(`No active ${kind} widget in the current workspace.`);
			throw new Error("No active widget/spec in the current workspace.");
		}
		resolveSelectionDataRef(selectionRef) {
			if (!selectionRef) return null;
			const resolvedDataRef = resolveSelectionDataRefFromStore(this.store, selectionRef);
			if (!resolvedDataRef) return null;
			if (readDataHandleFromStore(this.store, resolvedDataRef) || readRuntimeDataFromStore(this.store, resolvedDataRef)) return resolvedDataRef;
			return null;
		}
		normalizeScopedParams(params = {}, options = {}) {
			return buildScopedParams({
				params,
				call: options?.call || this.call,
				descriptor: options?.descriptor || this.descriptor,
				querySpec: options?.querySpec || null
			});
		}
		resolveDataRefForWidget(targetWidget, params = {}) {
			const queryScope = this.normalizeScopedParams(params)?.queryScope || null;
			const explicitDataRef = queryScope?.dataRef || null;
			if (typeof explicitDataRef === "string" && explicitDataRef.length > 0) return explicitDataRef;
			const callTargetRef = this.readQueryScope()?.widgetRef || null;
			const selectionScopedDataRef = this.resolveSelectionDataRef(queryScope?.selectionRef || callTargetRef);
			if (selectionScopedDataRef) return selectionScopedDataRef;
			if (typeof callTargetRef === "string" && (readDataHandleFromStore(this.store, callTargetRef) || readRuntimeDataFromStore(this.store, callTargetRef))) return callTargetRef;
			return targetWidget?.data?.currentDataRef || targetWidget?.data?.sourceDataRef || targetWidget?.primaryDataRef || null;
		}
		resolveRowsForWidget(targetWidget, params = {}) {
			const scopedParams = this.normalizeScopedParams(params);
			const queryScope = scopedParams?.queryScope || null;
			const targetDataRef = this.resolveDataRefForWidget(targetWidget, scopedParams);
			const runtimeData = readRuntimeDataFromStore(this.store, targetDataRef);
			let rows = Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
			const selectionRef = queryScope?.selectionRef || null;
			if (selectionRef) {
				const selectionState = this.readSelectionRegistry()?.[selectionRef] || null;
				const baseDataRef = targetWidget?.data?.currentDataRef || targetWidget?.data?.sourceDataRef || targetWidget?.primaryDataRef || null;
				const baseRuntimeData = readRuntimeDataFromStore(this.store, baseDataRef);
				const baseRows = Array.isArray(baseRuntimeData?.rows) ? baseRuntimeData.rows : [];
				const predicates = Array.isArray(selectionState?.predicates) ? selectionState.predicates : [];
				if ((!runtimeData || targetDataRef === baseDataRef) && predicates.length > 0) rows = baseRows.filter((row) => predicates.every((predicate) => applyPredicate(row, predicate)));
			}
			return {
				dataRef: targetDataRef,
				rows
			};
		}
		recordQuery({ affectedRefs = [], notes } = {}) {
			this.pendingQueryTrace = {
				call: this.call,
				affectedRefs,
				notes
			};
		}
		consumeRecordedQuery() {
			const pending = this.pendingQueryTrace;
			this.pendingQueryTrace = null;
			return pending;
		}
	};
	function applyPredicate(row, predicate) {
		const value = row?.[predicate?.field];
		if (predicate?.op === "between" && Array.isArray(predicate?.value)) return value >= predicate.value[0] && value <= predicate.value[1];
		if (predicate?.op === "equals") return value === predicate?.value;
		if (predicate?.op === "in" && Array.isArray(predicate?.value)) return predicate.value.includes(value);
		if (predicate?.op === "notIn" && Array.isArray(predicate?.value)) return !predicate.value.includes(value);
		return true;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/actionEffectVerification.js
	function evaluateActionEffectVerification({ store, getFinalWorkspaceSnapshot, evaluateLinkPropagation, actionName = null, stateId = null, refs = [], targetRef = null, traceLimit = 50 } = {}) {
		const trace = readInteractionTraceFromStore(store, { limit: traceLimit });
		const expectedRefs = Array.isArray(refs) ? refs : [];
		const matchingRecord = [...trace].reverse().find((record) => {
			if (stateId && record?.stateId !== stateId) return false;
			if (actionName && record?.action?.name !== actionName) return false;
			return true;
		});
		const affectedRefs = Array.isArray(matchingRecord?.affectedRefs) ? matchingRecord.affectedRefs : [];
		const missingRefs = expectedRefs.filter((ref) => !affectedRefs.includes(ref));
		const matchedActionName = matchingRecord?.action?.name || actionName || null;
		const verificationPayload = buildActionVerificationPayload({
			descriptor: resolveActionDescriptorForVerification({
				store,
				actionName: matchedActionName,
				targetRef: matchingRecord?.action?.targetRef || targetRef || null,
				affectedRefs
			}),
			updatedRefs: expectedRefs.length > 0 ? expectedRefs : affectedRefs
		});
		const finalSnapshot = getFinalWorkspaceSnapshot({
			stateId,
			refs: expectedRefs.length > 0 ? expectedRefs : void 0,
			includeMeta: true
		}) || null;
		const fullFinalSnapshot = getFinalWorkspaceSnapshot({
			stateId,
			includeMeta: true
		}) || finalSnapshot;
		const linkPropagation = Object.keys(readSelectionRegistry(fullFinalSnapshot?.shared || {})).map((sourceRef) => evaluateLinkPropagation({ sourceRef }) || {
			ok: false,
			sourceRef,
			linkCount: 0,
			passedCount: 0,
			consistencyScore: 0,
			results: []
		});
		const propagationOk = linkPropagation.every((entry) => entry.ok);
		const snapshotEntry = stateId ? readSnapshotEntryFromStore(store, stateId) : readCurrentSnapshotMetaFromStore(store);
		const updatedState = stateId ? snapshotEntry?.state || null : readWorkspaceStateFromStore(store);
		const replayContext = stateId ? snapshotEntry?.replayContext || null : store?.replayContext || null;
		const statePatch = expectedRefs.length > 0 ? store?.stateManager?.buildStatePatch?.({
			state: updatedState,
			replayContext,
			refs: expectedRefs
		}) || {} : {};
		return makeActionVerificationResult({
			verified: !!matchingRecord && missingRefs.length === 0 && propagationOk,
			matchedStateId: matchingRecord?.stateId || null,
			matchedActionName,
			affectedRefs,
			missingRefs,
			expectedPostconditions: verificationPayload.expectedPostconditions,
			verificationHints: verificationPayload.verificationHints,
			traceEvidence: matchingRecord || null,
			statePatch,
			finalSnapshot,
			linkPropagation
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/PerceptionQueryRegistry.js
	function buildQueryError({ call, code, message, details, recoveryHints }) {
		return makePerceptionResult({
			ok: false,
			callId: call?.callId || `query_${Date.now()}`,
			queryName: call?.name || "unknown",
			error: makeResultError({
				code,
				message,
				...details !== void 0 ? { details } : {}
			}),
			...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {}
		});
	}
	function resolveCallTargetRef(call, descriptor) {
		return readNormalizedQueryScope({
			call,
			descriptor
		}).widgetRef || null;
	}
	function resolveWidgetSelections(targetWidget) {
		return Object.entries(targetWidget?.selections || {}).map(([selectionRef, selectionState]) => ({
			ref: selectionRef,
			selection: selectionState
		})).filter((entry) => entry.ref && entry.selection);
	}
	function resolveRequestedSelectionEntries(targetWidget, requestedSelectionRef = null) {
		const selectionEntries = resolveWidgetSelections(targetWidget);
		if (!requestedSelectionRef) return selectionEntries;
		return selectionEntries.filter((entry) => entry.ref === requestedSelectionRef);
	}
	function formatSummaryText({ rowCount = 0, groupCount = 0, fallback = "" } = {}) {
		if (typeof fallback === "string" && fallback.trim().length > 0) return fallback.trim();
		if (groupCount > 0) return `${groupCount} grouped summaries over ${rowCount} rows`;
		return `${rowCount} rows`;
	}
	function buildPerceptionTraceNotes({ userVisibleSummary, rationale = null, verification = null } = {}) {
		return {
			...typeof userVisibleSummary === "string" && userVisibleSummary.trim().length > 0 ? { userVisibleSummary: userVisibleSummary.trim() } : {},
			...typeof rationale === "string" && rationale.trim().length > 0 ? { rationale: rationale.trim() } : {},
			...typeof verification === "string" && verification.trim().length > 0 ? { verification: verification.trim() } : {}
		};
	}
	function summarizeSupportedWidgetKinds(entries = []) {
		if (!Array.isArray(entries) || entries.length === 0) return null;
		const collectedKinds = Array.from(new Set(entries.flatMap((entry) => Array.isArray(entry?.supportedWidgetKinds) ? entry.supportedWidgetKinds : []).filter((kind) => typeof kind === "string" && kind.length > 0)));
		return collectedKinds.length > 0 ? collectedKinds : null;
	}
	function buildHandlerInput(call, descriptor) {
		const params = buildScopedParams({
			params: call?.params || {},
			call,
			descriptor
		});
		const { targetRef: _legacyTargetRef, dataRef: _legacyDataRef, params: _legacyParams, queryScope: _legacyQueryScope, ...rawCall } = call && typeof call === "object" ? call : {};
		const input = {
			...rawCall,
			...params,
			params,
			queryScope: params.queryScope || null
		};
		delete input.targetRef;
		delete input.dataRef;
		return input;
	}
	var PerceptionQueryRegistry = class {
		constructor({ store, dataQueryEngine, dataQueryExecutor, traceRecorder, linkEngine, hostBridge } = {}) {
			this.store = store || {
				listPerceptionQueries() {
					return [];
				},
				getPerceptionDescriptor() {
					return null;
				},
				listWidgetDescriptions() {
					return [];
				},
				getResolvedWidgetForTarget() {
					return null;
				},
				getResolvedWidget() {
					return null;
				},
				getWidgetDescription() {
					return null;
				},
				readState() {
					return {};
				}
			};
			this.dataQueryEngine = dataQueryEngine || null;
			this.traceRecorder = traceRecorder || null;
			this.hostBridge = hostBridge || createDefaultWidgetVAHostBridge();
			this.dataQueryExecutor = dataQueryExecutor || (this.dataQueryEngine ? new DataQueryExecutor({
				store: this.store,
				dataQueryEngine: this.dataQueryEngine,
				traceRecorder: this.traceRecorder,
				hostBridge: this.hostBridge
			}) : null);
			this.linkEngine = linkEngine || null;
			this.builtinDescriptors = /* @__PURE__ */ new Map();
			this.handlerEntries = /* @__PURE__ */ new Map();
			this.registerBuiltinQueries();
		}
		register(descriptor, handler, options = {}) {
			if (!descriptor?.name || typeof handler !== "function") throw new Error("PerceptionQueryRegistry.register requires a descriptor.name and handler.");
			const supportedWidgetKinds = normalizeSupportedWidgetKinds$1(options.supportedWidgetKinds || descriptor.supportedWidgetKinds || null);
			const entries = this.handlerEntries.get(descriptor.name) || [];
			if (entries.some((entry) => sameSupportedKinds(entry.supportedWidgetKinds, supportedWidgetKinds))) throw new Error(`Duplicate perception query registration: ${descriptor.name}`);
			this.builtinDescriptors.set(descriptor.name, descriptor);
			this.handlerEntries.set(descriptor.name, [...entries, {
				descriptor,
				handler,
				supportedWidgetKinds
			}]);
		}
		list() {
			const descriptors = this.store.listPerceptionQueries();
			if (descriptors.length > 0) return descriptors.filter((descriptor) => this.handlerEntries.has(descriptor.name));
			return Array.from(this.builtinDescriptors.values());
		}
		has(name, options = {}) {
			const entries = this.handlerEntries.get(name) || [];
			const supportedWidgetKinds = normalizeSupportedWidgetKinds$1(options.supportedWidgetKinds || null);
			if (!supportedWidgetKinds) return entries.length > 0;
			return entries.some((entry) => sameSupportedKinds(entry.supportedWidgetKinds, supportedWidgetKinds));
		}
		describeRegistry() {
			const queries = this.list().map((descriptor) => {
				const handlerEntries = this.handlerEntries.get(descriptor.name) || [];
				return makePerceptionRegistryQueryEntry({
					name: descriptor.name,
					category: descriptor.category || null,
					targetRef: descriptor.targetRef || null,
					sideEffectFree: descriptor.sideEffectFree !== false,
					evidenceKinds: Array.isArray(descriptor.evidenceKinds) ? [...descriptor.evidenceKinds] : [],
					verificationTargets: Array.isArray(descriptor.verificationTargets) ? [...descriptor.verificationTargets] : [],
					supportedWidgetKinds: summarizeSupportedWidgetKinds(handlerEntries),
					handlerVariantCount: handlerEntries.length
				});
			});
			let handlerEntryCount = 0;
			for (const entries of this.handlerEntries.values()) handlerEntryCount += Array.isArray(entries) ? entries.length : 0;
			return makePerceptionRegistrySummary({
				counts: makePerceptionRegistryCounts({
					descriptorCount: queries.length,
					handlerEntryCount
				}),
				capabilities: makePerceptionRegistryCapabilities({
					paramsValidation: true,
					returnsValidation: true,
					traceRecording: typeof this.traceRecorder?.recordQuery === "function",
					linkPropagationEvidence: Boolean(this.linkEngine)
				}),
				queries
			});
		}
		describeContext() {
			return PerceptionContext.describeContract();
		}
		resolveDescriptor(call) {
			return this.store.getPerceptionDescriptor(call?.name, resolveCallTargetRef(call)) || this.builtinDescriptors.get(call?.name) || null;
		}
		createContext(call, descriptor) {
			const normalizedCall = buildHandlerInput(call, descriptor);
			return new PerceptionContext({
				store: this.store,
				descriptor,
				call: normalizedCall,
				traceRecorder: this.traceRecorder,
				hostBridge: this.hostBridge
			});
		}
		resolveHandlerEntry(call, descriptor, targetWidget) {
			const entries = this.handlerEntries.get(call?.name) || [];
			if (entries.length === 0) return null;
			const widgetKind = targetWidget?.kind || null;
			const targetRef = resolveCallTargetRef(call, descriptor);
			if (widgetKind) {
				const exactKindEntry = entries.find((entry) => entry.supportedWidgetKinds?.includes(widgetKind));
				if (exactKindEntry) return exactKindEntry;
			}
			if (targetRef) {
				const scopedEntry = entries.find((entry) => entry.descriptor?.targetRef === targetRef);
				if (scopedEntry) return scopedEntry;
			}
			return entries.find((entry) => !entry.supportedWidgetKinds || entry.supportedWidgetKinds.length === 0) || entries[0];
		}
		async query(call) {
			const descriptor = this.resolveDescriptor(call);
			const ctx = this.createContext(call, descriptor);
			const targetWidget = ctx.resolveTargetWidget();
			const handlerEntry = this.resolveHandlerEntry(call, descriptor, targetWidget);
			if (!handlerEntry?.handler) {
				this.traceRecorder?.recordQueryFailure?.({
					call,
					code: "UNKNOWN_QUERY",
					message: `Unsupported WidgetVA perception query: ${call?.name || "unknown"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect currently supported perception queries."]
				});
				return buildQueryError({
					call,
					code: "UNKNOWN_QUERY",
					message: `Unsupported WidgetVA perception query: ${call?.name || "unknown"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect currently supported perception queries."]
				});
			}
			if (descriptor?.paramsSchema) try {
				validateAgainstSchema(ctx.readCallParams(), descriptor.paramsSchema, "params");
			} catch (error) {
				this.traceRecorder?.recordQueryFailure?.({
					call,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the query paramsSchema in describeWorkspace() before retrying."]
				});
				return buildQueryError({
					call,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the query paramsSchema in describeWorkspace() before retrying."]
				});
			}
			try {
				const output = await handlerEntry.handler(buildHandlerInput(call, descriptor), ctx);
				if (descriptor?.returnsSchema) validateAgainstSchema(output?.result, descriptor.returnsSchema, "result");
				const tracePayload = ctx.consumeRecordedQuery?.() || null;
				if (tracePayload) this.traceRecorder?.recordQuery?.(tracePayload);
				return makePerceptionResult({
					ok: true,
					callId: call?.callId || `call_${Date.now()}`,
					queryName: call?.name || "unknown",
					result: output?.result
				});
			} catch (error) {
				this.traceRecorder?.recordQueryFailure?.({
					call,
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Call readState() or describeWorkspace() to verify the current target widget and data refs.", "Retry with a focused widget/query combination that is declared in the current workspace."]
				});
				return buildQueryError({
					call,
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: [
						"Call readState() or describeWorkspace() to verify the current target widget and data refs.",
						"Retry with a focused widget/query combination that is declared in the current workspace.",
						"Inspect the query returnsSchema if the runtime result shape does not match the declared contract."
					]
				});
			}
		}
		async run(call) {
			return this.query(call);
		}
		registerBuiltinQueries() {
			this.register({ name: "perception.inspectViewConfig" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: `Inspected the ${targetWidget.kind || "widget"} view configuration.`,
						rationale: "View configuration evidence was requested for the current target widget."
					})
				});
				return { result: {
					ref: targetWidget.ref,
					kind: targetWidget.kind,
					encodings: targetWidget.encodings,
					transforms: targetWidget.transforms,
					view: targetWidget.view,
					selections: targetWidget.selections,
					feedback: targetWidget.feedback || null
				} };
			});
			this.register({ name: "perception.summarizeSelection" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const requestedSelectionRef = params?.queryScope?.selectionRef || null;
				const selectionEntries = resolveRequestedSelectionEntries(targetWidget, requestedSelectionRef);
				if (requestedSelectionRef && selectionEntries.length === 0) throw new Error(`Selection ref ${requestedSelectionRef} is not active on the target widget.`);
				const selections = selectionEntries.map((entry) => entry.selection);
				const { rows: visibleRows, dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				let result;
				if (selections.length > 0) {
					const filteredRows = selections.reduce((rows, activeSelection) => this.dataQueryEngine.filter(rows, activeSelection.predicates || []), visibleRows);
					const summary = this.dataQueryEngine.summarize(filteredRows, params);
					const summaryRows = Array.isArray(summary?.rows) ? summary.rows : [];
					const selectionSummaries = selections.map((selection) => selection.summary || "").filter(Boolean);
					result = {
						hasSelection: true,
						selectionCount: selections.length,
						selectionRefs: selectionEntries.map((entry) => entry.ref),
						dataRef,
						selectedCount: filteredRows.length,
						summary: formatSummaryText({
							rowCount: filteredRows.length,
							groupCount: summaryRows.length,
							fallback: selectionSummaries.join(" | ")
						}),
						selectionSummaries,
						predicates: selections.flatMap((selection) => selection.predicates || []),
						selectionPredicates: selections.map((selection) => selection.predicates || []),
						groups: summaryRows,
						aggregates: summaryRows
					};
				} else result = {
					hasSelection: false,
					selectionCount: 0,
					selectionRefs: [],
					dataRef,
					selectedCount: 0,
					summary: "",
					selectionSummaries: [],
					groups: [],
					aggregates: [],
					predicates: [],
					selectionPredicates: []
				};
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: result.hasSelection ? `Summarized ${result.selectedCount} selected rows across ${result.selectionCount} active selection${result.selectionCount === 1 ? "" : "s"}.` : "Confirmed that there is no active selection on the target widget.",
						rationale: "Selection evidence was requested for the target widget."
					})
				});
				return { result };
			});
			this.register({ name: "perception.summarizeVisible" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const { rows: visibleRows, dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				const summary = this.dataQueryExecutor.run({
					dataRef,
					query: {
						kind: "summary",
						spec: params
					}
				});
				const summaryRows = Array.isArray(summary?.result?.rows) ? summary.result.rows : [];
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: `Summarized ${visibleRows.length} visible rows for the target widget.`,
						rationale: "Visible-view evidence was requested for the target widget."
					})
				});
				return { result: {
					dataRef,
					rowCount: visibleRows.length,
					groups: summaryRows,
					aggregates: summaryRows,
					summary: formatSummaryText({
						rowCount: visibleRows.length,
						groupCount: summaryRows.length
					})
				} };
			});
			this.register({ name: "perception.findExtremes" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const { dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				const rows = this.dataQueryExecutor.run({
					dataRef,
					query: {
						kind: "findExtremes",
						spec: params
					}
				})?.result?.rows || [];
				const direction = typeof params?.direction === "string" ? params.direction : "max";
				const field = typeof params?.field === "string" ? params.field : "value";
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: `Found ${rows.length} ${direction} extreme row${rows.length === 1 ? "" : "s"} for ${field}.`,
						rationale: "An extremes query was requested for the current target widget."
					})
				});
				return { result: {
					dataRef,
					field: typeof params?.field === "string" ? params.field : null,
					direction,
					rows
				} };
			});
			this.register({ name: "perception.inspectVisibleRows" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const { rows: visibleRows, dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				const sampledRows = this.dataQueryExecutor.run({
					dataRef,
					query: {
						kind: "sampleRows",
						spec: { limit: Number.isFinite(params?.limit) ? params.limit : 20 }
					}
				});
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: `Inspected ${visibleRows.length} visible rows and returned a sample.`,
						rationale: "Visible-row evidence was requested for the target widget."
					})
				});
				return { result: {
					ref: targetWidget.ref,
					dataRef,
					visibleCount: visibleRows.length,
					rows: sampledRows?.result || []
				} };
			});
			this.register({ name: "perception.verifyActionEffect" }, async (call, ctx) => {
				const params = ctx.readCallParams();
				const verification = evaluateActionEffectVerification({
					store: this.store,
					getFinalWorkspaceSnapshot: () => null,
					evaluateLinkPropagation: (options) => this.linkEngine?.evaluatePropagation?.(options) || null,
					actionName: params?.actionName || null,
					stateId: params?.stateId || null,
					refs: params?.refs || [],
					targetRef: params?.queryScope?.widgetRef || null,
					traceLimit: 50
				});
				ctx.recordQuery({
					affectedRefs: Array.isArray(params?.refs) && params.refs.length > 0 ? params.refs : verification.affectedRefs,
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: verification.verified ? `Verified the effect of ${verification.matchedActionName || "the action"} on the requested runtime refs.` : `Verification for ${verification.matchedActionName || "the action"} found missing refs or propagation mismatches.`,
						rationale: "Post-action verification evidence was requested from the runtime trace and state patch.",
						verification: verification.verified ? "Trace evidence, state patch, and link propagation checks all matched the requested action effect." : `Missing refs: ${verification.missingRefs.length}. Propagation ok: ${verification.linkPropagation.every((entry) => entry.ok)}.`
					})
				});
				return { result: verification };
			});
		}
	};
	function normalizeSupportedWidgetKinds$1(value) {
		if (!Array.isArray(value) || value.length === 0) return null;
		return [...new Set(value.filter((item) => typeof item === "string" && item.length > 0))].sort();
	}
	function sameSupportedKinds(left, right) {
		const a = normalizeSupportedWidgetKinds$1(left);
		const b = normalizeSupportedWidgetKinds$1(right);
		if (!a && !b) return true;
		if (!a || !b || a.length !== b.length) return false;
		return a.every((value, index) => value === b[index]);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/ResponseRecorder.js
	var ResponseRecorder = class {
		constructor({ store }) {
			this.store = store;
		}
		recordResponse({ responseId, runId = null, sessionId = null, actor = "agent", mode = null, query = null, content, evidenceRefs = [], usage = null } = {}) {
			const safeContent = String(content || "").trim();
			if (!safeContent) throw new Error("ResponseRecorder.recordResponse requires non-empty content.");
			const state = this.store.readState();
			const record = makeAgentResponseRecord({
				responseId: responseId || `response_${Date.now()}`,
				runId: runId || null,
				sessionId: sessionId || this.store.workspaceId || null,
				workspaceId: this.store.workspaceId || null,
				actor,
				mode: mode || null,
				query: query ? String(query) : null,
				content: safeContent,
				stateId: state?.stateId || null,
				branchId: this.store.currentBranchId || null,
				evidenceRefs: Array.isArray(evidenceRefs) ? [...evidenceRefs] : [],
				usage: usage && typeof usage === "object" ? usage : null,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			});
			this.store.appendResponse(record);
			return record;
		}
		readLatestResponse(options = {}) {
			return this.store.readLatestResponse(options);
		}
		listResponses(limit = 20, options = {}) {
			return this.store.listResponses(limit, options);
		}
		describeRecorder() {
			const workspaceId = this.store.workspaceId || null;
			const latest = this.store.readLatestResponse({ workspaceId }) || null;
			const responses = Array.isArray(this.store?.responseHistory) ? this.store.responseHistory.filter((record) => {
				if (!workspaceId) return true;
				return record?.workspaceId === workspaceId;
			}) : [];
			return makeResponseRecorderSummary({
				capabilities: makeResponseRecorderCapabilities({
					recordsFinalResponses: true,
					latestResponseRead: true,
					responseHistoryRead: true,
					workspaceScopedReads: true,
					lineageTracking: true
				}),
				counters: makeResponseRecorderCounters({
					workspaceId,
					responseCount: responses.length,
					latestResponseId: latest?.responseId || null,
					latestRunId: latest?.runId || null,
					latestStateId: latest?.stateId || null,
					latestBranchId: latest?.branchId || null,
					latestActor: latest?.actor || null,
					latestMode: latest?.mode || null,
					latestQuery: latest?.query || null,
					latestEvidenceRefCount: Array.isArray(latest?.evidenceRefs) ? latest.evidenceRefs.length : 0
				})
			});
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/createWidgetRuntime.js
	function createRuntimeStore(options = {}) {
		return new WidgetVARuntimeStore(options);
	}
	function normalizeReplayOptions(input) {
		if (typeof input === "string") return { stateId: input };
		if (input && typeof input === "object") return { ...input };
		return {};
	}
	function resolveRuntimeWidgetAdapters(options = {}) {
		if (Array.isArray(options.widgetAdapters)) return options.widgetAdapters.filter(Boolean);
		if (options.widgetAdapter) return [options.widgetAdapter];
		return [];
	}
	function createWidgetVARuntime(options = {}) {
		const defaultHostBridge = createDefaultWidgetVAHostBridge();
		const providedHostBridge = options.hostBridge || null;
		const derivedHostBridge = providedHostBridge ? null : createWidgetVAHostBridge({
			getState: options.getState,
			getAppState: options.getAppState,
			subscribe: options.subscribe,
			subscribeAppState: options.subscribeAppState
		});
		const hostBridge = providedHostBridge ? {
			...defaultHostBridge,
			...providedHostBridge
		} : derivedHostBridge || defaultHostBridge;
		const store = createRuntimeStore();
		const traceRecorder = new InteractionTraceRecorder({ store });
		const responseRecorder = new ResponseRecorder({ store });
		const dataQueryEngine = options.dataQueryEngine || createDataQueryEngine({ kind: "js_array" });
		const dataQueryExecutor = new DataQueryExecutor({
			store,
			dataQueryEngine,
			traceRecorder,
			hostBridge
		});
		const linkEngine = new LinkEngine({ store });
		const planWorkspaceWithHostState = (nextOptions = {}) => {
			const request = makeWorkspacePlanningRequest({
				runMode: nextOptions.runMode || hostBridge.readRunMode(),
				userIntent: nextOptions.userIntent || hostBridge.readUserIntent() || null,
				complexityBudget: nextOptions.complexityBudget || "standard",
				preferredTopology: nextOptions.preferredTopology || null,
				task: nextOptions.task || null
			});
			const spec = nextOptions.spec || hostBridge.readCurrentSpec();
			return planWorkspace({
				sessionId: hostBridge.readSessionId() || "main",
				spec,
				...request
			});
		};
		const buildReplayContext = () => ({
			baselineSpec: hostBridge.readBaselineSpec() ?? null,
			currentSpec: hostBridge.readCurrentSpec() ?? null,
			workspaceSpec: hostBridge.readWorkspaceSpec() ?? null,
			planningRequest: hostBridge.readPlanningRequest() ?? null,
			runMode: hostBridge.readRunMode() || "goal_oriented",
			userIntent: hostBridge.readUserIntent() || ""
		});
		const sync = () => {
			const replayContext = buildReplayContext();
			const workspace = buildSingleWidgetWorkspace({
				appId: "widgetva-app",
				workspaceId: hostBridge.readSessionId() || "main",
				sessionId: hostBridge.readSessionId(),
				spec: hostBridge.readCurrentSpec(),
				workspaceSpec: hostBridge.readWorkspaceSpec(),
				planningRequest: {
					...hostBridge.readPlanningRequest() || {},
					runMode: hostBridge.readRunMode(),
					userIntent: hostBridge.readUserIntent() || null
				},
				selection: hostBridge.readCurrentSelection(),
				selections: hostBridge.readCurrentSelections(),
				focusedWidgetRef: hostBridge.readFocusedWidgetRef(),
				focusState: hostBridge.readFocusState(),
				highlightState: hostBridge.readHighlightState(),
				viewportState: normalizeViewportState(hostBridge.readViewportState()),
				annotations: hostBridge.readWorkspaceAnnotations(),
				previousState: store.readState(),
				currentBranchId: store.currentBranchId,
				stateManager: store.stateManager,
				replayContext
			});
			store.replaceWorkspace(workspace);
		};
		sync();
		const unsubscribe = hostBridge.subscribe(sync);
		const actionExecutor = new ActionExecutor({
			store,
			sync,
			hostBridge,
			linkEngine,
			traceRecorder
		});
		const executeAction = actionExecutor.run.bind(actionExecutor);
		const runDataQuery = dataQueryExecutor.run.bind(dataQueryExecutor);
		const perceptionQueryRegistry = new PerceptionQueryRegistry({
			store,
			dataQueryEngine,
			dataQueryExecutor,
			traceRecorder,
			linkEngine,
			hostBridge
		});
		const queryPerception = perceptionQueryRegistry.query.bind(perceptionQueryRegistry);
		const describeWorkspace = () => readWorkspaceDescriptionFromStore(store, {
			actionExecutor,
			perceptionQueryRegistry
		});
		const readView = (options = {}) => readWorkspaceStateFromStore(store, options);
		const readState = (options = {}) => readWorkspaceStateFromStore(store, options);
		const readCoordinationState = () => buildCoordinationStateFromWorkspaceState(readState(), {
			currentBranchId: store.currentBranchId,
			derivedTopology: describeWorkspace()?.runtimeTopology || {}
		});
		const listAvailableActions = () => listAvailableActionsFromDescription(describeWorkspace());
		const listAvailablePerceptions = () => listAvailablePerceptionsFromDescription(describeWorkspace());
		const readPropagationSummary = (options = {}) => buildPropagationSummary({
			state: readState(),
			description: describeWorkspace(),
			linkEngine,
			sourceRef: options?.sourceRef || null
		});
		const readObservation = (options = {}) => buildRuntimeObservation({
			description: describeWorkspace(),
			state: readState(options.readStateOptions || options),
			coordinationState: readCoordinationState(),
			availableActions: listAvailableActions(),
			availablePerceptions: listAvailablePerceptions(),
			propagationSummary: readPropagationSummary(options.propagationOptions || {}),
			latestCoordinationResult: readLatestCoordinationResult()
		});
		const readLatestCoordinationResult = () => typeof options.readLatestCoordinationResult === "function" ? options.readLatestCoordinationResult() : null;
		const readTrace = (options = {}) => readInteractionTraceFromStore(store, options);
		const jumpToState = async (options = {}) => executeAction({
			callId: options.callId || `jump_${Date.now()}`,
			name: "workspace.jumpToState",
			actor: options.actor || "agent",
			params: { stateId: options.stateId || null }
		});
		const branchFromState = async (options = {}) => executeAction({
			callId: options.callId || `branch_${Date.now()}`,
			name: "workspace.branchFromState",
			actor: options.actor || "agent",
			params: {
				stateId: options.stateId || null,
				branchLabel: options.branchLabel || null
			}
		});
		const replay = async (stateIdOrOptions) => jumpToState(normalizeReplayOptions(stateIdOrOptions));
		const widgetAdapters = resolveRuntimeWidgetAdapters(options);
		if (options.registerDefaultWidgetFamilies ?? false) registerWidgetFamilyAdapters({
			actionExecutor,
			perceptionQueryRegistry
		});
		if (widgetAdapters.length > 0) registerWidgetAdapters(widgetAdapters, {
			actionExecutor,
			perceptionQueryRegistry
		});
		const agentLoopRuntime = new AgentLoopRuntime({
			store,
			planWorkspace: planWorkspaceWithHostState,
			executeAction,
			queryPerception,
			queryData: (call) => dataQueryExecutor.run(call),
			readLatestCoordinationResult,
			responseRecorder,
			actionExecutor,
			linkEngine
		});
		const executeVerifiedAction = agentLoopRuntime.executeVerifiedAction.bind(agentLoopRuntime);
		const uninstallPagePort = installWidgetVAPagePort({
			store,
			planWorkspace: planWorkspaceWithHostState,
			executeAction,
			readLatestCoordinationResult,
			queryPerception,
			queryData: (call) => dataQueryExecutor.run(call),
			actionExecutor,
			perceptionQueryRegistry,
			dataQueryExecutor,
			linkEngine,
			traceRecorder,
			responseRecorder,
			agentLoopRuntime
		});
		function inferHumanActionName(selection) {
			if (!selection) return "widget.clearSelection";
			const sourceWidgetId = selection?.source_widget_id;
			const declaredAction = store.listWidgetDescriptions().find((widget) => widget.widgetId === sourceWidgetId)?.humanInteraction?.actionName;
			if (declaredAction) return declaredAction;
			return "widget.updateSelection";
		}
		async function applyHumanSelection(selection) {
			const actionName = inferHumanActionName(selection);
			const sourceWidget = selection?.source_widget_id ? store.listWidgetDescriptions().find((widget) => widget.widgetId === selection.source_widget_id) || null : null;
			const result = await executeAction({
				callId: `human_${Date.now()}`,
				name: actionName,
				actor: "human",
				targetRef: sourceWidget?.ref || void 0,
				params: selection || {}
			});
			return {
				updatedRefs: result?.updatedRefs || [],
				stateId: result?.stateId || null
			};
		}
		return {
			store,
			actionExecutor,
			linkEngine,
			dataQueryExecutor,
			perceptionQueryRegistry,
			agentLoopRuntime,
			responseRecorder,
			planWorkspace: planWorkspaceWithHostState,
			applyHumanSelection,
			describeWorkspace,
			readObservation,
			readCoordinationState,
			readPropagationSummary,
			readLatestCoordinationResult,
			listAvailableActions,
			listAvailablePerceptions,
			readView,
			readState,
			readTrace,
			executeAction,
			executeVerifiedAction,
			queryPerception,
			queryData: runDataQuery,
			runDataQuery,
			getTrace: readTrace,
			jumpToState,
			branchFromState,
			replay,
			dispose() {
				unsubscribe?.();
				uninstallPagePort?.();
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/rendering/WidgetRendererBridge.js
	function noop() {}
	function bindRuntimeStateSubscription({ runtime, widgetRef, adapter, view, surface, spec, interactionConfig, initialWidgetState }) {
		const store = runtime?.store || null;
		if (!store || typeof store.subscribe !== "function") return noop;
		if (!widgetRef || typeof adapter?.applyState !== "function") return noop;
		if (!view && !surface) return noop;
		let lastAppliedState = initialWidgetState || null;
		return store.subscribe(() => {
			const nextWidgetState = store.getWidgetState?.(widgetRef) || null;
			if (!nextWidgetState || nextWidgetState === lastAppliedState) return;
			lastAppliedState = nextWidgetState;
			Promise.resolve(adapter.applyState({
				runtime,
				view,
				surface,
				spec,
				state: nextWidgetState,
				interactionConfig
			})).catch(() => {});
		});
	}
	function attachWidgetRendererBridge({ runtime, widgetRef, widgetAdapter = null, widgetState, view, surface, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
		const adapter = widgetAdapter || runtime?.store?.getWidgetAdapter?.(widgetRef) || null;
		const bindCleanup = adapter?.bindHumanInteractions ? adapter.bindHumanInteractions({
			view,
			surface,
			spec,
			state: widgetState,
			interactionConfig,
			selectionSourceWidgetId,
			actionTargetRef,
			onActionCall,
			onSelectionChange
		}) : bindWidgetHumanInteractions({
			view,
			spec,
			interactionConfig,
			selectionSourceWidgetId,
			actionTargetRef,
			onActionCall,
			onSelectionChange
		});
		const unsubscribe = bindRuntimeStateSubscription({
			runtime,
			widgetRef,
			adapter,
			view,
			surface,
			spec,
			interactionConfig,
			initialWidgetState: widgetState
		});
		return {
			adapter,
			cleanup() {
				if (typeof bindCleanup === "function") bindCleanup();
				unsubscribe();
			}
		};
	}
	async function applyWidgetRuntimeState({ runtime, widgetRef, widgetAdapter = null, widgetState, view, surface, spec, interactionConfig }) {
		const adapter = widgetAdapter || runtime?.store?.getWidgetAdapter?.(widgetRef) || null;
		if (!adapter?.applyState || !widgetState || !view && !surface) return;
		await adapter.applyState({
			runtime,
			view,
			surface,
			spec,
			state: widgetState,
			interactionConfig
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/rendering/RendererAdapterRegistry.js
	function normalizeProvider(provider) {
		return typeof provider === "string" && provider.trim().length > 0 ? provider.trim() : null;
	}
	function normalizeSupportedWidgetKinds(value) {
		return Array.isArray(value) ? value.filter((entry) => typeof entry === "string" && entry.trim().length > 0) : [];
	}
	function normalizeRegistryEntry(entry = {}) {
		const provider = normalizeProvider(entry.provider);
		if (!provider) throw new Error("RendererAdapterRegistry entries require a non-empty provider string.");
		if (typeof entry.createAdapter !== "function") throw new Error(`RendererAdapterRegistry entry ${provider} requires createAdapter().`);
		return {
			provider,
			createAdapter: entry.createAdapter,
			createDefinition: typeof entry.createDefinition === "function" ? entry.createDefinition : null,
			supportedWidgetKinds: normalizeSupportedWidgetKinds(entry.supportedWidgetKinds),
			metadata: entry.metadata && typeof entry.metadata === "object" && !Array.isArray(entry.metadata) ? { ...entry.metadata } : {}
		};
	}
	function createRendererAdapterRegistry(entries = []) {
		const entryMap = /* @__PURE__ */ new Map();
		function register(entry) {
			const normalized = normalizeRegistryEntry(entry);
			entryMap.set(normalized.provider, normalized);
			return normalized;
		}
		function resolve(provider) {
			const normalized = normalizeProvider(provider);
			return normalized ? entryMap.get(normalized) || null : null;
		}
		function has(provider) {
			return Boolean(resolve(provider));
		}
		function list() {
			return [...entryMap.values()].map((entry) => ({
				provider: entry.provider,
				supportedWidgetKinds: [...entry.supportedWidgetKinds],
				metadata: { ...entry.metadata }
			}));
		}
		function supportsWidgetKind(provider, widgetKind) {
			const entry = resolve(provider);
			if (!entry || typeof widgetKind !== "string" || widgetKind.length === 0) return false;
			return entry.supportedWidgetKinds.length === 0 || entry.supportedWidgetKinds.includes(widgetKind);
		}
		function createAdapter(provider, args = {}) {
			const entry = resolve(provider);
			if (!entry) throw new Error(`Renderer adapter provider ${provider} is not registered.`);
			return entry.createAdapter(args);
		}
		for (const entry of Array.isArray(entries) ? entries : []) register(entry);
		return {
			register,
			resolve,
			has,
			list,
			supportsWidgetKind,
			createAdapter
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/installWidgetView.js
	function createDefaultProviderBackedAdapter(provider, definition = {}) {
		const normalizedDefinition = definition && typeof definition === "object" && !Array.isArray(definition) ? definition : {};
		const kind = typeof normalizedDefinition.kind === "string" ? normalizedDefinition.kind : null;
		if (kind && ![
			"vega-lite-view",
			"d3-view",
			"echarts-view"
		].includes(kind)) {
			const familyAdapter = createProviderFamilyAdapter(kind, provider);
			return {
				...familyAdapter,
				...normalizedDefinition,
				provider: familyAdapter.provider,
				providerCapabilities: {
					...familyAdapter.providerCapabilities || {},
					...normalizedDefinition.providerCapabilities || {}
				}
			};
		}
		if (provider === "d3") return createD3WidgetAdapter(normalizedDefinition);
		if (provider === "echarts") return createEChartsWidgetAdapter(normalizedDefinition);
		return createVegaLiteWidgetAdapter(normalizedDefinition);
	}
	var DEFAULT_RENDERER_ADAPTER_REGISTRY = createRendererAdapterRegistry([
		{
			provider: "vega-lite",
			createAdapter({ definition = {} } = {}) {
				return createDefaultProviderBackedAdapter("vega-lite", definition);
			}
		},
		{
			provider: "d3",
			createAdapter({ definition = {} } = {}) {
				return createDefaultProviderBackedAdapter("d3", definition);
			}
		},
		{
			provider: "echarts",
			createAdapter({ definition = {} } = {}) {
				return createDefaultProviderBackedAdapter("echarts", definition);
			}
		}
	]);
	async function installWidgetVAOnView({ runtime = null, view = null, surface = null, container = null, widgetRef = null, widgetState = null, spec = null, interactionConfig = null, selectionSourceWidgetId = null, actionTargetRef = null, onActionCall = null, onSelectionChange = null, widgetAdapter = null, provider = null, adapterDefinition = {}, adapterRegistry = DEFAULT_RENDERER_ADAPTER_REGISTRY } = {}) {
		const effectiveSurface = surface || container || null;
		if (!view && !effectiveSurface) throw new Error("installWidgetVAOnView requires either a view, surface, or container.");
		const resolvedAdapter = widgetAdapter || (provider ? adapterRegistry?.createAdapter?.(provider, {
			definition: adapterDefinition,
			runtime,
			view,
			surface: effectiveSurface,
			widgetRef,
			widgetState,
			spec,
			interactionConfig,
			selectionSourceWidgetId,
			actionTargetRef: actionTargetRef || widgetRef
		}) : null);
		if (!resolvedAdapter) throw new Error("installWidgetVAOnView requires a widgetAdapter or provider-backed adapterRegistry.");
		const resolvedInteractionConfig = interactionConfig || widgetState?.humanInteraction || resolvedAdapter.getHumanInteractionConfig?.() || null;
		const bridge = attachWidgetRendererBridge({
			runtime,
			widgetRef,
			widgetAdapter: resolvedAdapter,
			widgetState,
			view,
			surface: effectiveSurface,
			spec,
			interactionConfig: resolvedInteractionConfig,
			selectionSourceWidgetId,
			actionTargetRef: actionTargetRef || widgetRef,
			onActionCall,
			onSelectionChange
		});
		async function apply({ widgetState: nextWidgetState = widgetState, spec: nextSpec = spec, interactionConfig: nextInteractionConfig = resolvedInteractionConfig } = {}) {
			await applyWidgetRuntimeState({
				runtime,
				widgetRef,
				widgetAdapter: resolvedAdapter,
				widgetState: nextWidgetState,
				view,
				surface: effectiveSurface,
				spec: nextSpec,
				interactionConfig: nextInteractionConfig
			});
		}
		await apply();
		return {
			adapter: resolvedAdapter,
			apply,
			dispose: bridge.cleanup
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/widgetInstance.js
	var WIDGET_INSTANCE_PUBLIC_METHODS = [
		"mount",
		"unmount",
		"isMounted",
		"describe",
		"listAvailableActions",
		"listActionNames",
		"listAvailablePerceptions",
		"listPerceptionNames",
		"listActionDescriptors",
		"listPerceptionDescriptors",
		"readState",
		"readObservation",
		"readVerificationState",
		"describeAgentContract",
		"readWorkspaceState",
		"readCoordinationState",
		"readPropagationSummary",
		"executeAction",
		"executeVerifiedAction",
		"queryPerception",
		"runDataQuery",
		"readTrace",
		"getTrace",
		"readSnapshot",
		"readSnapshotEntry",
		"replay",
		"dispose"
	];
	function makeCallId(prefix = "widget_call") {
		return `${prefix}_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
	}
	function resolveWidgetDescription(runtime, { widgetRef = null, widgetId = null } = {}) {
		const descriptions = runtime?.store?.listWidgetDescriptions?.() || [];
		if (widgetRef) return descriptions.find((entry) => entry?.ref === widgetRef) || null;
		if (widgetId) return descriptions.find((entry) => entry?.widgetId === widgetId) || null;
		return descriptions[0] || null;
	}
	function normalizeActionCall(call, widgetRef) {
		if (!call || typeof call !== "object") throw new Error("WidgetInstance.executeAction requires an action call object.");
		const normalizedQueryScope = call.queryScope && typeof call.queryScope === "object" && !Array.isArray(call.queryScope) ? { ...call.queryScope } : {};
		if (widgetRef && !normalizedQueryScope.widgetRef && !normalizedQueryScope.widget_ref) normalizedQueryScope.widgetRef = widgetRef;
		const { targetRef: _legacyTargetRef, ...restCall } = call;
		return {
			actor: call.actor || "agent",
			callId: call.callId || makeCallId("widget_action"),
			...restCall,
			...Object.keys(normalizedQueryScope).length > 0 ? { queryScope: normalizedQueryScope } : {}
		};
	}
	function normalizePerceptionCall(call, widgetRef) {
		if (!call || typeof call !== "object") throw new Error("WidgetInstance.queryPerception requires a perception call object.");
		const normalizedQueryScope = call.queryScope && typeof call.queryScope === "object" && !Array.isArray(call.queryScope) ? { ...call.queryScope } : {};
		if (widgetRef && !normalizedQueryScope.widgetRef && !normalizedQueryScope.widget_ref) normalizedQueryScope.widgetRef = widgetRef;
		const { targetRef: _legacyTargetRef, ...restCall } = call;
		return {
			actor: call.actor || "agent",
			callId: call.callId || makeCallId("widget_perception"),
			...restCall,
			...Object.keys(normalizedQueryScope).length > 0 ? { queryScope: normalizedQueryScope } : {}
		};
	}
	function normalizeDataQueryCall(call, widgetRef) {
		if (!call || typeof call !== "object") throw new Error("WidgetInstance.runDataQuery requires a data-query call object.");
		const normalizedQueryScope = call.queryScope && typeof call.queryScope === "object" && !Array.isArray(call.queryScope) ? { ...call.queryScope } : {};
		if (widgetRef && !normalizedQueryScope.widgetRef && !normalizedQueryScope.widget_ref) normalizedQueryScope.widgetRef = widgetRef;
		const { targetRef: _legacyTargetRef, ...restCall } = call;
		return {
			actor: call.actor || "agent",
			callId: call.callId || makeCallId("widget_data_query"),
			...restCall,
			...Object.keys(normalizedQueryScope).length > 0 ? { queryScope: normalizedQueryScope } : {}
		};
	}
	function normalizeMountTarget({ mountTarget = null, renderTarget = null, view = null, surface = null, container = null } = {}) {
		const target = mountTarget || renderTarget || null;
		return {
			...target?.view || view ? { view: target?.view || view } : {},
			...target?.surface || surface || target?.container || container ? { surface: target?.surface || surface || target?.container || container } : {},
			...target?.container || container ? { container: target?.container || container } : {}
		};
	}
	function uniqueNames(descriptors = []) {
		return descriptors.map((descriptor) => descriptor?.name).filter((name, index, names) => typeof name === "string" && name.length > 0 && names.indexOf(name) === index);
	}
	function clone$2(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeWidgetInstanceOptions({ mountOptions = {}, mountTarget = null, renderTarget = null, view = null, surface = null, container = null, ...options } = {}) {
		return {
			...options,
			mountOptions: {
				...mountOptions,
				...normalizeMountTarget({
					mountTarget,
					renderTarget,
					view,
					surface,
					container
				})
			}
		};
	}
	var WidgetInstance = class {
		constructor({ runtime = null, ownsRuntime = false, widgetAdapter = null, widgetRef = null, widgetId = null, spec = null, widgetState = null, mountOptions = {} } = {}) {
			if (!runtime) throw new Error("WidgetInstance requires a runtime.");
			if (!widgetAdapter) throw new Error("WidgetInstance requires a widgetAdapter.");
			this.runtime = runtime;
			this.widgetAdapter = widgetAdapter;
			this.widgetRef = widgetRef;
			this.widgetId = widgetId;
			this.spec = spec;
			this.widgetState = widgetState;
			this.mountOptions = { ...mountOptions };
			this.ownsRuntime = ownsRuntime;
			this.installation = null;
		}
		static describeContract() {
			return {
				methods: [...WIDGET_INSTANCE_PUBLIC_METHODS],
				methodSemantics: {
					describe: "returns the normalized widget description registered on the shared runtime",
					readState: "returns the current widget-local state stored for this widget ref",
					readObservation: "returns a VLM-facing observation envelope including identity, state, action/perception surfaces, and lightweight coordination hints",
					readVerificationState: "returns a verification-oriented widget summary covering selection, transforms, view domains, encodings, and comparable signatures",
					describeAgentContract: "returns the single-widget agent-facing contract including action/perception surfaces, verification surface names, and stable call/result schemas",
					readWorkspaceState: "returns the current shared runtime state snapshot that contains this widget",
					readCoordinationState: "returns the shared coordination state from the enclosing runtime/workspace surface",
					readPropagationSummary: "returns a propagation summary centered on this widget ref when link coordination is available",
					executeAction: "executes a widget/runtime action call with this widget as the default queryScope target",
					executeVerifiedAction: "executes a widget action and returns post-action verification evidence through the stable single-widget observe-act-verify surface",
					queryPerception: "executes a widget/runtime perception query with this widget as the default queryScope target",
					runDataQuery: "executes a data query with this widget as the default queryScope target when one is not provided explicitly"
				},
				constructorOptions: {
					runtime: "optional shared runtime instance",
					widgetAdapter: "required widget/provider adapter",
					widgetRef: "optional explicit widget ref",
					widgetId: "optional explicit widget id",
					spec: "optional widget spec when runtime is created internally",
					widgetState: "optional initial widget state override",
					mountOptions: "optional persisted mount options",
					mountTarget: "optional persisted { view | surface | container } target",
					renderTarget: "optional alias for mountTarget",
					view: "optional direct render view target",
					surface: "optional direct DOM/surface target",
					container: "optional direct DOM container alias"
				}
			};
		}
		resolveWidgetDescription() {
			return resolveWidgetDescription(this.runtime, {
				widgetRef: this.widgetRef,
				widgetId: this.widgetId
			});
		}
		resolveWidgetRef() {
			return this.widgetRef || this.resolveWidgetDescription()?.ref || null;
		}
		resolveWidgetId() {
			return this.widgetId || this.resolveWidgetDescription()?.widgetId || null;
		}
		isMounted() {
			return Boolean(this.installation);
		}
		async mount(nextMountOptions = {}) {
			if (this.installation?.dispose) {
				this.installation.dispose();
				this.installation = null;
			}
			this.mountOptions = {
				...this.mountOptions,
				...normalizeMountTarget(nextMountOptions),
				...nextMountOptions
			};
			this.installation = await installWidgetVAOnView({
				runtime: this.runtime,
				widgetAdapter: this.widgetAdapter,
				widgetRef: this.resolveWidgetRef(),
				widgetState: this.widgetState || this.readState(),
				spec: this.mountOptions.spec || this.spec,
				...this.mountOptions
			});
			return this;
		}
		unmount() {
			this.installation?.dispose?.();
			this.installation = null;
		}
		describe() {
			return this.resolveWidgetDescription();
		}
		listActionDescriptors() {
			const widgetRef = this.resolveWidgetRef();
			const description = this.resolveWidgetDescription();
			const builder = this.widgetAdapter?.buildActionDescriptors;
			if (typeof builder === "function") return builder.call(this.widgetAdapter, {
				widgetRef,
				selectionRef: null,
				scope: "local",
				affectedRefs: widgetRef ? [widgetRef] : []
			}) || [];
			return (description?.actionNames || []).map((name) => ({
				name,
				targetRef: widgetRef || null
			}));
		}
		listPerceptionDescriptors() {
			const state = this.readState();
			const description = this.resolveWidgetDescription();
			const dataRef = state?.data?.currentDataRef || state?.data?.sourceDataRef || description?.primaryDataRef || null;
			const builder = this.widgetAdapter?.buildPerceptionDescriptors;
			if (typeof builder === "function") return builder.call(this.widgetAdapter, { dataRef }) || [];
			return (description?.perceptionQueryNames || []).map((name) => ({
				name,
				targetRef: dataRef || null
			}));
		}
		listActionNames() {
			return uniqueNames(this.listActionDescriptors());
		}
		listAvailableActions() {
			return this.listActionDescriptors();
		}
		listPerceptionNames() {
			return uniqueNames(this.listPerceptionDescriptors());
		}
		listAvailablePerceptions() {
			return this.listPerceptionDescriptors();
		}
		readState() {
			const widgetRef = this.resolveWidgetRef();
			return widgetRef ? this.runtime?.store?.getWidgetState?.(widgetRef) || null : null;
		}
		readObservation(options = {}) {
			const widgetRef = this.resolveWidgetRef();
			const widgetId = this.resolveWidgetId();
			const description = this.resolveWidgetDescription();
			const state = this.readState();
			const shared = this.readWorkspaceState(options.readStateOptions || {})?.shared || {};
			const primarySelection = shared?.selections?.views?.primary || null;
			const widgetSelectionView = widgetId ? shared?.selections?.views?.byWidget?.[widgetId] || null : null;
			const actionDescriptors = this.listActionDescriptors();
			const perceptionDescriptors = this.listPerceptionDescriptors();
			const verificationContract = describeWidgetVerificationContract(description?.kind || null);
			return {
				widgetRef,
				widgetId,
				title: description?.title || null,
				kind: description?.kind || null,
				role: description?.role || null,
				state: clone$2(state),
				actionNames: uniqueNames(actionDescriptors),
				perceptionNames: uniqueNames(perceptionDescriptors),
				actionDescriptors: clone$2(actionDescriptors),
				perceptionDescriptors: clone$2(perceptionDescriptors),
				selection: buildLocalSelectionObservation({
					kind: description?.kind || null,
					state,
					widgetSelectionView
				}),
				verification: { contract: verificationContract },
				coordination: {
					isFocused: shared?.focusedWidgetRef === widgetRef,
					sharedPrimarySelectionRef: primarySelection?.selectionRef || null,
					sharedPrimarySelectionSourceWidgetId: primarySelection?.sourceWidgetId || null,
					widgetSelectionRef: widgetSelectionView?.selectionRef || null,
					localSelectionRefs: Object.keys(state?.selections || {}),
					linkedSourceRefs: Array.isArray(state?.feedback?.linkedSourceRefs) ? [...state.feedback.linkedSourceRefs] : []
				}
			};
		}
		readVerificationState() {
			const widgetRef = this.resolveWidgetRef();
			const widgetId = this.resolveWidgetId();
			const description = this.resolveWidgetDescription();
			const state = this.readState();
			return {
				contract: describeWidgetVerificationContract(description?.kind || null),
				...buildWidgetVerificationState({
					widgetRef,
					widgetId,
					kind: description?.kind || null,
					state
				})
			};
		}
		describeAgentContract() {
			return describeSingleWidgetAgentContractFromWidget(this);
		}
		readWorkspaceState(options = {}) {
			return this.runtime?.store?.readState?.(options) || null;
		}
		readCoordinationState() {
			const readCoordinationState = this.runtime?.readCoordinationState;
			if (typeof readCoordinationState === "function") return readCoordinationState();
			return null;
		}
		readPropagationSummary(options = {}) {
			const readPropagationSummary = this.runtime?.readPropagationSummary;
			if (typeof readPropagationSummary !== "function") return null;
			return readPropagationSummary({ sourceRef: options?.sourceRef || this.resolveWidgetRef() || null });
		}
		async executeAction(call) {
			const executeAction = this.runtime?.executeAction || this.runtime?.actionExecutor?.run?.bind(this.runtime.actionExecutor);
			if (typeof executeAction !== "function") throw new Error("WidgetInstance.executeAction requires a runtime with executeAction support.");
			return executeAction(normalizeActionCall(call, this.resolveWidgetRef()));
		}
		async executeVerifiedAction(call, options = {}) {
			const normalizedCall = normalizeActionCall(call, this.resolveWidgetRef());
			const executeVerifiedAction = this.runtime?.executeVerifiedAction || this.runtime?.agentLoopRuntime?.executeVerifiedAction?.bind(this.runtime.agentLoopRuntime);
			if (typeof executeVerifiedAction === "function") return executeVerifiedAction(normalizedCall, options);
			const beforeStateId = this.readWorkspaceState()?.stateId || null;
			const actionResult = await this.executeAction(normalizedCall);
			if (!actionResult?.ok) return makeVerifiedActionResult({
				ok: false,
				beforeStateId,
				actionResult,
				afterView: null,
				verification: null
			});
			const afterView = this.readWorkspaceState({
				refs: Array.isArray(actionResult?.updatedRefs) ? actionResult.updatedRefs : void 0,
				deltaSince: options?.includeDeltaSince ? beforeStateId : void 0
			});
			const verification = options?.verify === false ? null : await this.queryPerception({
				callId: `${normalizedCall.callId}_verify`,
				actor: normalizedCall.actor || "agent",
				name: "perception.verifyActionEffect",
				params: {
					actionName: normalizedCall.name,
					stateId: actionResult?.stateId || null,
					refs: Array.isArray(actionResult?.updatedRefs) ? actionResult.updatedRefs : []
				}
			});
			const verificationOk = verification?.ok !== false && (verification?.result?.verified ?? true);
			return makeVerifiedActionResult({
				ok: Boolean(actionResult?.ok) && verificationOk,
				beforeStateId,
				actionResult,
				afterView: clone$2(afterView),
				verification: clone$2(verification),
				verificationHints: clone$2(Array.isArray(actionResult?.verificationHints) ? actionResult.verificationHints : [])
			});
		}
		async queryPerception(call) {
			const queryPerception = this.runtime?.queryPerception || this.runtime?.perceptionQueryRegistry?.query?.bind(this.runtime.perceptionQueryRegistry) || this.runtime?.perceptionQueryRegistry?.run?.bind(this.runtime.perceptionQueryRegistry);
			if (typeof queryPerception !== "function") throw new Error("WidgetInstance.queryPerception requires a runtime with queryPerception support.");
			return queryPerception(normalizePerceptionCall(call, this.resolveWidgetRef()));
		}
		async runDataQuery(call) {
			const runDataQuery = this.runtime?.runDataQuery || this.runtime?.queryData || this.runtime?.dataQueryExecutor?.run?.bind(this.runtime.dataQueryExecutor);
			if (typeof runDataQuery !== "function") throw new Error("WidgetInstance.runDataQuery requires a runtime with data-query support.");
			return runDataQuery(normalizeDataQueryCall(call, this.resolveWidgetRef()));
		}
		readTrace(options = {}) {
			return this.getTrace(options);
		}
		getTrace(options = {}) {
			return readInteractionTraceFromStore(this.runtime?.store, options);
		}
		readSnapshot(stateId, options = {}) {
			return readSnapshotFromStore(this.runtime?.store, stateId, options);
		}
		readSnapshotEntry(stateId) {
			return readSnapshotEntryFromStore(this.runtime?.store, stateId);
		}
		async replay(stateId) {
			return this.runtime.executeAction({
				callId: makeCallId("widget_replay"),
				actor: "agent",
				name: "workspace.jumpToState",
				params: { stateId }
			});
		}
		dispose() {
			this.unmount();
			if (this.ownsRuntime) this.runtime?.dispose?.();
		}
	};
	function createWidgetInstance({ runtime = null, widgetAdapter = null, widgetRef = null, widgetId = null, spec = null, widgetState = null, mountTarget = null, renderTarget = null, view = null, surface = null, container = null, mountOptions = {}, runtimeOptions = {} } = {}) {
		const normalizedOptions = normalizeWidgetInstanceOptions({
			runtime,
			widgetAdapter,
			widgetRef,
			widgetId,
			spec,
			widgetState,
			mountTarget,
			renderTarget,
			view,
			surface,
			container,
			mountOptions,
			runtimeOptions: runtime ? runtimeOptions : {
				...runtimeOptions,
				widgetAdapters: Array.isArray(runtimeOptions.widgetAdapters) ? runtimeOptions.widgetAdapters : [widgetAdapter].filter(Boolean)
			}
		});
		return new WidgetInstance({
			runtime: normalizedOptions.runtime || createWidgetVARuntime(normalizedOptions.runtimeOptions),
			ownsRuntime: !runtime,
			widgetAdapter: normalizedOptions.widgetAdapter,
			widgetRef: normalizedOptions.widgetRef,
			widgetId: normalizedOptions.widgetId,
			spec: normalizedOptions.spec,
			widgetState: normalizedOptions.widgetState,
			mountOptions: normalizedOptions.mountOptions
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/integrations/officialPages/observableD3Pages.js
	function tryParseUrl(value) {
		try {
			return new URL(value);
		} catch {
			return null;
		}
	}
	function normalizeRoot(root) {
		if (!root || typeof root !== "object") throw new Error("A browser-like root object is required.");
		return root;
	}
	function readDocument(root) {
		return root?.document || null;
	}
	function readIframes(root) {
		const doc = readDocument(root);
		if (!doc?.querySelectorAll) return [];
		return [...doc.querySelectorAll("iframe")];
	}
	function readFrameRect(frame) {
		try {
			if (typeof frame?.getBoundingClientRect === "function") {
				const rect = frame.getBoundingClientRect();
				return {
					width: Number(rect?.width) || 0,
					height: Number(rect?.height) || 0
				};
			}
		} catch {}
		return {
			width: 0,
			height: 0
		};
	}
	function computeFrameArea(frame) {
		const rect = readFrameRect(frame);
		return rect.width * rect.height;
	}
	function isObservableD3NotebookPage(pageUrl) {
		const parsedUrl = tryParseUrl(pageUrl);
		if (!parsedUrl) return false;
		return parsedUrl.hostname === "observablehq.com" && parsedUrl.pathname.startsWith("/@d3/");
	}
	function isObservableWorkerFrameUrl(frameUrl) {
		const parsedUrl = tryParseUrl(frameUrl);
		if (!parsedUrl) return false;
		return parsedUrl.hostname.endsWith(".observableusercontent.com") && /\/worker-[^/]+\.html$/.test(parsedUrl.pathname);
	}
	function parseObservableNotebookIdentity(pageUrl) {
		const parsedUrl = tryParseUrl(pageUrl);
		if (!parsedUrl || parsedUrl.hostname !== "observablehq.com") return null;
		const segments = parsedUrl.pathname.split("/").filter(Boolean);
		if (segments.length < 2 || segments[0] !== "@d3") return null;
		return {
			owner: segments[0],
			slug: segments[1],
			version: segments[2] || null,
			path: parsedUrl.pathname,
			url: parsedUrl.href
		};
	}
	function findObservableWorkerFrame(root = globalThis.window) {
		const iframes = readIframes(normalizeRoot(root));
		const candidates = [];
		for (const iframe of iframes) if (isObservableWorkerFrameUrl(iframe?.getAttribute?.("src") || iframe?.src || null)) candidates.push(iframe);
		if (candidates.length === 0) return null;
		return candidates.reduce((best, frame) => {
			if (!best) return frame;
			return computeFrameArea(frame) > computeFrameArea(best) ? frame : best;
		}, null);
	}
	async function waitForObservableWorkerFrame({ root = globalThis.window, timeoutMs = 5e3, pollMs = 25 } = {}) {
		const normalizedRoot = normalizeRoot(root);
		const startedAt = Date.now();
		while (Date.now() - startedAt <= timeoutMs) {
			const frame = findObservableWorkerFrame(normalizedRoot);
			if (frame) return frame;
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for an Observable worker iframe after ${timeoutMs}ms.`);
	}
	function describeObservableD3PageShape(root = globalThis.window) {
		const normalizedRoot = normalizeRoot(root);
		const pageUrl = normalizedRoot?.location?.href || null;
		const notebook = parseObservableNotebookIdentity(pageUrl);
		const iframes = readIframes(normalizedRoot);
		const workerFrame = findObservableWorkerFrame(normalizedRoot);
		const bodyText = normalizedRoot?.document?.body?.innerText || "";
		return {
			provider: "d3",
			source: "observablehq",
			pageUrl,
			notebook,
			iframeCount: iframes.length,
			workerFrame: workerFrame ? { src: workerFrame.getAttribute?.("src") || workerFrame.src || null } : null,
			bodyTextPreview: bodyText.slice(0, 400)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/integrations/officialPages/observableD3Examples.js
	function clone$1(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function buildObservableScatterSpec(rows = []) {
		return {
			data: { values: rows },
			mark: "point",
			encoding: {
				x: {
					field: "__screenX",
					type: "quantitative"
				},
				y: {
					field: "__screenY",
					type: "quantitative"
				}
			}
		};
	}
	function buildObservableBarSpec(rows = []) {
		return {
			data: { values: rows },
			mark: "bar",
			encoding: {
				x: {
					field: "category",
					type: "nominal"
				},
				y: {
					field: "__barHeight",
					type: "quantitative"
				}
			}
		};
	}
	function buildObservableLineSpec(rows = []) {
		return {
			data: { values: rows },
			mark: "line",
			encoding: {
				x: {
					field: "xValue",
					type: "nominal"
				},
				y: {
					field: "__seriesIndex",
					type: "quantitative"
				},
				color: {
					field: "series",
					type: "nominal"
				}
			}
		};
	}
	function buildSelectionFromRowSummary(summary) {
		if (!summary || summary.count === 0) return null;
		const xSpan = summary.xMax - summary.xMin;
		const ySpan = summary.yMax - summary.yMin;
		return {
			kind: "interval",
			domain: {
				xDomain: [summary.xMin + xSpan * .25, summary.xMax - xSpan * .25],
				yDomain: [summary.yMin + ySpan * .25, summary.yMax - ySpan * .25]
			}
		};
	}
	function createOfficialPageHostBridge({ sessionId, baselineSpec, currentSpecRef, userIntent = null }) {
		const listeners = /* @__PURE__ */ new Set();
		const emit = () => {
			for (const listener of listeners) try {
				listener();
			} catch {}
		};
		return {
			subscribe(listener) {
				if (typeof listener !== "function") return () => {};
				listeners.add(listener);
				return () => {
					listeners.delete(listener);
				};
			},
			readSessionId: () => sessionId,
			readBaselineSpec: () => clone$1(baselineSpec),
			readCurrentSpec: () => clone$1(currentSpecRef.current),
			writeCurrentSpec(nextSpec) {
				currentSpecRef.current = clone$1(nextSpec);
				emit();
			},
			readWorkspaceSpec: () => null,
			readPlanningRequest: () => null,
			readRunMode: () => "goal_oriented",
			readUserIntent: () => userIntent,
			readCurrentSelection: () => null,
			readCurrentSelections: () => ({}),
			readFocusedWidgetRef: () => null,
			readComparisonTargets: () => [],
			readWorkspaceAnnotations: () => []
		};
	}
	function createOfficialPageAgentLoopRunner(root) {
		return async function runOfficialPageAgentLoop(options = {}) {
			const port = root?.__widgetVA || null;
			if (!port || typeof port.describeWorkspace !== "function" || typeof port.describeAgentLoop !== "function") throw new Error("WidgetVA page port is not ready for official-page agent-loop execution.");
			return runPagePortAgentLoop(port, options);
		};
	}
	function createPostActionSyncProxy(target, syncAfterAction) {
		if (!target || typeof target !== "object") return target;
		return new Proxy(target, { get(obj, prop, receiver) {
			if (prop === "executeAction" || prop === "executeVerifiedAction") {
				const original = Reflect.get(obj, prop, receiver);
				if (typeof original !== "function") return original;
				return async (...args) => {
					const result = await original.apply(obj, args);
					await syncAfterAction?.({
						method: prop,
						args,
						result
					});
					return result;
				};
			}
			const value = Reflect.get(obj, prop, receiver);
			return typeof value === "function" ? value.bind(obj) : value;
		} });
	}
	var OBSERVABLE_D3_WORKER_REQUEST = "widgetva:observable-d3-worker-request";
	var OBSERVABLE_D3_WORKER_RESPONSE = "widgetva:observable-d3-worker-response";
	var OBSERVABLE_D3_TOP_SOURCE = "widgetva-observable-d3-top";
	var OBSERVABLE_D3_WORKER_SOURCE = "widgetva-observable-d3-worker";
	async function invokeObservableWorker(frame, method, params = null, { timeoutMs = 5e3 } = {}) {
		const targetWindow = frame?.contentWindow;
		if (!targetWindow || typeof targetWindow.postMessage !== "function") throw new Error("Observable worker frame is not available for postMessage RPC.");
		const listenerRoot = globalThis.window;
		const requestId = `observable_d3_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
		return new Promise((resolve, reject) => {
			const timeout = setTimeout(() => {
				cleanup();
				reject(/* @__PURE__ */ new Error(`Timed out waiting for Observable worker RPC: ${method}.`));
			}, timeoutMs);
			const cleanup = () => {
				clearTimeout(timeout);
				listenerRoot?.removeEventListener?.("message", handleMessage);
			};
			const handleMessage = (event) => {
				const message = event?.data;
				if (!message || message.source !== OBSERVABLE_D3_WORKER_SOURCE || message.type !== OBSERVABLE_D3_WORKER_RESPONSE || message.id !== requestId) return;
				cleanup();
				if (message.ok === false) {
					reject(new Error(message?.error?.message || `Observable worker RPC failed: ${method}.`));
					return;
				}
				resolve(message.result || null);
			};
			listenerRoot?.addEventListener?.("message", handleMessage);
			targetWindow.postMessage({
				source: OBSERVABLE_D3_TOP_SOURCE,
				type: OBSERVABLE_D3_WORKER_REQUEST,
				id: requestId,
				method,
				params
			}, "*");
		});
	}
	async function waitForObservableWorkerScatterSurface(frame, { timeoutMs = 5e3, pollMs = 25, notebook = null } = {}) {
		const startedAt = Date.now();
		let lastSurface = null;
		let lastRowCount = 0;
		while (Date.now() - startedAt <= timeoutMs) {
			try {
				const surface = await invokeObservableWorker(frame, "describeSurface", { notebook }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const workerRows = await invokeObservableWorker(frame, "readScatterRows", null, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const rows = Array.isArray(workerRows?.rows) ? workerRows.rows : [];
				lastSurface = surface || null;
				lastRowCount = rows.length;
				if (surface?.inferredKind === "scatter" && rows.length > 0) return {
					surface,
					rows
				};
			} catch {}
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for a ready Observable D3 scatter surface after ${timeoutMs}ms (last inferred kind: ${lastSurface?.inferredKind || "unknown"}, last row count: ${lastRowCount}).`);
	}
	async function waitForObservableWorkerBarSurface(frame, { timeoutMs = 5e3, pollMs = 25, notebook = null } = {}) {
		const startedAt = Date.now();
		let lastSurface = null;
		let lastRowCount = 0;
		while (Date.now() - startedAt <= timeoutMs) {
			try {
				const surface = await invokeObservableWorker(frame, "describeSurface", { notebook }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const workerRows = await invokeObservableWorker(frame, "readBarRows", null, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const rows = Array.isArray(workerRows?.rows) ? workerRows.rows : [];
				lastSurface = surface || null;
				lastRowCount = rows.length;
				if (surface?.inferredKind === "bar" && rows.length > 0) return {
					surface,
					rows
				};
			} catch {}
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for a ready Observable D3 bar surface after ${timeoutMs}ms (last inferred kind: ${lastSurface?.inferredKind || "unknown"}, last row count: ${lastRowCount}).`);
	}
	async function waitForObservableWorkerLineSurface(frame, { timeoutMs = 5e3, pollMs = 25, notebook = null } = {}) {
		const startedAt = Date.now();
		let lastSurface = null;
		let lastRowCount = 0;
		while (Date.now() - startedAt <= timeoutMs) {
			try {
				const surface = await invokeObservableWorker(frame, "describeSurface", { notebook }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const workerRows = await invokeObservableWorker(frame, "readLineRows", null, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const rows = Array.isArray(workerRows?.rows) ? workerRows.rows : [];
				lastSurface = surface || null;
				lastRowCount = rows.length;
				if (surface?.inferredKind === "line" && rows.length > 0) return {
					surface,
					rows
				};
			} catch {}
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for a ready Observable D3 line surface after ${timeoutMs}ms (last inferred kind: ${lastSurface?.inferredKind || "unknown"}, last row count: ${lastRowCount}).`);
	}
	function createObservableScatterSurfaceWrapper({ frame, currentSpecRef = null }) {
		let currentSelection = null;
		let currentViewport = null;
		let currentClusterAnnotation = null;
		let currentRegressionAnnotation = null;
		async function applySelection(selection = null) {
			currentSelection = selection && typeof selection === "object" ? clone$1(selection) : null;
			await invokeObservableWorker(frame, "applyScatterSelection", { selection: currentSelection });
		}
		async function applyViewport(viewport = null) {
			const normalizedViewport = viewport && typeof viewport === "object" ? {
				...Array.isArray(viewport.xDomain) ? { xDomain: clone$1(viewport.xDomain) } : {},
				...Array.isArray(viewport.yDomain) ? { yDomain: clone$1(viewport.yDomain) } : {}
			} : null;
			currentViewport = normalizedViewport && Object.keys(normalizedViewport).length > 0 ? normalizedViewport : null;
			await invokeObservableWorker(frame, "applyScatterViewport", { viewport: currentViewport });
		}
		async function applyClusterAnnotation(cluster = null) {
			currentClusterAnnotation = cluster && typeof cluster === "object" ? {
				...Number.isFinite(cluster.nClusters) ? { nClusters: Number(cluster.nClusters) } : {},
				...typeof cluster.clusterField === "string" ? { clusterField: cluster.clusterField } : {},
				...typeof cluster.method === "string" ? { method: cluster.method } : {}
			} : null;
			await invokeObservableWorker(frame, "applyScatterClusters", { cluster: currentClusterAnnotation });
		}
		async function applyRegressionAnnotation(regression = null) {
			currentRegressionAnnotation = regression && typeof regression === "object" ? { ...typeof regression.method === "string" ? { method: regression.method } : {} } : null;
			await invokeObservableWorker(frame, "applyScatterRegression", { regression: currentRegressionAnnotation });
		}
		function readClusterState(widgetState = {}) {
			const clusterState = widgetState?.rawSpec?._scatter_cluster_state || currentSpecRef?.current?._scatter_cluster_state;
			if (!clusterState || typeof clusterState !== "object") return null;
			return {
				nClusters: Number.isFinite(clusterState.n_clusters) ? Number(clusterState.n_clusters) : 3,
				clusterField: clusterState.cluster_field || null,
				method: clusterState.method || "kmeans"
			};
		}
		function readRegressionState(widgetState = {}) {
			const regressionLayer = (Array.isArray(widgetState?.rawSpec?.layer) ? widgetState.rawSpec.layer : Array.isArray(currentSpecRef?.current?.layer) ? currentSpecRef.current.layer : []).find((layer) => layer?._widgetvaTag === "scatter.showRegression");
			if (!regressionLayer) return null;
			return { method: (Array.isArray(regressionLayer.transform) ? regressionLayer.transform.find((transform) => typeof transform?.regression === "string") : null)?.method || "linear" };
		}
		return {
			getState() {
				return {
					view: {
						...currentViewport ? clone$1(currentViewport) : {},
						...currentClusterAnnotation ? { cluster: clone$1(currentClusterAnnotation) } : {},
						...currentRegressionAnnotation ? { regression: clone$1(currentRegressionAnnotation) } : {}
					},
					selections: currentSelection ? { localBrush: clone$1(currentSelection) } : {}
				};
			},
			async renderFromState(widgetState = {}) {
				await applyViewport(widgetState?.view && typeof widgetState.view === "object" ? {
					...Array.isArray(widgetState.view.xDomain) ? { xDomain: widgetState.view.xDomain } : {},
					...Array.isArray(widgetState.view.yDomain) ? { yDomain: widgetState.view.yDomain } : {}
				} : null);
				await applySelection(Object.values(widgetState?.selections || {}).find((entry) => entry?.kind === "interval") || null);
				await applyClusterAnnotation(readClusterState(widgetState));
				await applyRegressionAnnotation(readRegressionState(widgetState));
			},
			async setBrush(selection) {
				return applySelection(selection);
			},
			async setSelection(selection) {
				return applySelection(selection);
			},
			async setViewport(viewport) {
				return applyViewport(viewport);
			},
			async setClusterAnnotation(cluster) {
				return applyClusterAnnotation(cluster);
			},
			async setRegressionAnnotation(regression) {
				return applyRegressionAnnotation(regression);
			},
			getViewport() {
				return currentViewport ? clone$1(currentViewport) : null;
			},
			async readDebugSnapshot() {
				return invokeObservableWorker(frame, "readDebugSnapshot", null);
			}
		};
	}
	function createObservableBarSurfaceWrapper({ frame, currentSpecRef = null }) {
		let currentSelection = null;
		let currentFilter = null;
		let currentSort = null;
		async function applySelection(selection = null) {
			currentSelection = selection && typeof selection === "object" ? clone$1(selection) : null;
			await invokeObservableWorker(frame, "applyBarSelection", { selection: currentSelection });
		}
		async function applyFilter(filter = null) {
			const normalizedFilter = filter && typeof filter === "object" ? {
				field: typeof filter.field === "string" ? filter.field : "category",
				categories: Array.isArray(filter.categories) ? clone$1(filter.categories) : []
			} : null;
			currentFilter = normalizedFilter && currentFilter?.categories?.length !== 0 ? normalizedFilter : normalizedFilter;
			await invokeObservableWorker(frame, "applyBarFilter", { filter: currentFilter });
		}
		async function applySort(sort = null) {
			const normalizedSort = sort && typeof sort === "object" ? {
				...typeof sort.channel === "string" ? { channel: sort.channel } : {},
				...typeof sort.field === "string" ? { field: sort.field } : {},
				...typeof sort.mode === "string" ? { mode: sort.mode } : {},
				...typeof sort.order === "string" ? { order: sort.order } : {},
				...Array.isArray(sort.values) ? { values: clone$1(sort.values) } : {}
			} : null;
			currentSort = normalizedSort && Array.isArray(normalizedSort.values) && normalizedSort.values.length > 0 ? normalizedSort : null;
			await invokeObservableWorker(frame, "applyBarSort", { sort: currentSort });
		}
		function readFilterFromSpec() {
			const categoricalFilter = (Array.isArray(currentSpecRef?.current?.transform) ? currentSpecRef.current.transform : []).find((transform) => transform?.filter?.field && Array.isArray(transform?.filter?.oneOf));
			if (!categoricalFilter) return null;
			return {
				field: categoricalFilter.filter.field,
				categories: clone$1(categoricalFilter.filter.oneOf)
			};
		}
		function readFilterFromWidgetState(widgetState = {}) {
			const rawSpecFilter = (Array.isArray(widgetState?.rawSpec?.transform) ? widgetState.rawSpec.transform : []).find((transform) => transform?.filter?.field && Array.isArray(transform?.filter?.oneOf));
			if (rawSpecFilter) return {
				field: rawSpecFilter.filter.field,
				categories: clone$1(rawSpecFilter.filter.oneOf)
			};
			const runtimeFilter = (Array.isArray(widgetState?.transforms) ? widgetState.transforms : []).find((transform) => transform?.spec?.filter?.field && Array.isArray(transform?.spec?.filter?.oneOf));
			if (runtimeFilter) return {
				field: runtimeFilter.spec.filter.field,
				categories: clone$1(runtimeFilter.spec.filter.oneOf)
			};
			return null;
		}
		function readSortFromWidgetState(widgetState = {}) {
			const viewSort = widgetState?.view?.sort;
			if (viewSort && typeof viewSort === "object" && Array.isArray(viewSort.values) && viewSort.values.length > 0) return clone$1(viewSort);
			const rawSpecEncoding = widgetState?.rawSpec?.encoding || {};
			for (const channel of ["x", "y"]) if (Array.isArray(rawSpecEncoding?.[channel]?.sort) && rawSpecEncoding[channel].sort.length > 0) return {
				channel,
				field: rawSpecEncoding?.[channel]?.field || null,
				mode: "explicitOrder",
				values: clone$1(rawSpecEncoding[channel].sort)
			};
			return null;
		}
		return {
			getState() {
				return {
					view: currentSort ? { sort: clone$1(currentSort) } : {},
					selections: currentSelection ? { localSelection: clone$1(currentSelection) } : {}
				};
			},
			async renderFromState(widgetState = {}) {
				await applySelection(Object.values(widgetState?.selections || {}).find((entry) => Array.isArray(entry?.values)) || null);
				await applyFilter(readFilterFromWidgetState(widgetState) || readFilterFromSpec());
				await applySort(readSortFromWidgetState(widgetState));
			},
			async setSelection(selection) {
				return applySelection(selection);
			},
			async syncCurrentSpec() {
				return applyFilter(readFilterFromSpec());
			},
			async readDebugSnapshot() {
				return invokeObservableWorker(frame, "readDebugSnapshot", null);
			}
		};
	}
	function createObservableLineSurfaceWrapper({ frame, currentSpecRef = null }) {
		let currentSelection = null;
		let currentFocus = null;
		let currentTrend = null;
		let currentMovingAverage = null;
		let currentDrilldown = null;
		let currentViewport = null;
		async function applySelection(selection = null) {
			currentSelection = selection && typeof selection === "object" ? clone$1(selection) : null;
			await invokeObservableWorker(frame, "applyLineSelection", { selection: currentSelection });
		}
		async function applyFocus(focus = null) {
			const normalizedFocus = focus && typeof focus === "object" ? {
				...Array.isArray(focus.lines) ? { lines: clone$1(focus.lines) } : {},
				...typeof focus.lineField === "string" ? { lineField: focus.lineField } : {},
				...Number.isFinite(focus.dimOpacity) ? { dimOpacity: Number(focus.dimOpacity) } : {}
			} : null;
			currentFocus = normalizedFocus && Array.isArray(normalizedFocus.lines) && normalizedFocus.lines.length > 0 ? normalizedFocus : null;
			await invokeObservableWorker(frame, "applyLineFocus", { focus: currentFocus });
		}
		async function applyTrend(trend = null) {
			currentTrend = trend && typeof trend === "object" ? { ...typeof trend.trendType === "string" ? { trendType: trend.trendType } : {} } : null;
			await invokeObservableWorker(frame, "applyLineTrend", { trend: currentTrend });
		}
		async function applyMovingAverage(movingAverage = null) {
			currentMovingAverage = movingAverage && typeof movingAverage === "object" ? { ...Number.isFinite(movingAverage.windowSize) ? { windowSize: Number(movingAverage.windowSize) } : {} } : null;
			await invokeObservableWorker(frame, "applyLineMovingAverage", { movingAverage: currentMovingAverage });
		}
		async function applyDrilldown(drilldown = null) {
			const normalizedDrilldown = drilldown && typeof drilldown === "object" ? {
				...typeof drilldown.level === "string" ? { level: drilldown.level } : {},
				...Number.isFinite(drilldown.value) ? { value: Number(drilldown.value) } : {},
				...drilldown.parent && typeof drilldown.parent === "object" && !Array.isArray(drilldown.parent) ? { parent: clone$1(drilldown.parent) } : {},
				...typeof drilldown.title === "string" ? { title: drilldown.title } : {}
			} : null;
			currentDrilldown = normalizedDrilldown && typeof normalizedDrilldown.level === "string" ? normalizedDrilldown : null;
			await invokeObservableWorker(frame, "applyLineDrilldown", { drilldown: currentDrilldown });
		}
		async function applyViewport(viewport = null) {
			const normalizedViewport = viewport && typeof viewport === "object" ? { ...Array.isArray(viewport.xDomain) ? { xDomain: clone$1(viewport.xDomain) } : {} } : null;
			currentViewport = normalizedViewport && Array.isArray(normalizedViewport.xDomain) ? normalizedViewport : null;
			await invokeObservableWorker(frame, "applyLineViewport", { viewport: currentViewport });
		}
		function readFocusState(widgetState = {}) {
			const rawSpec = widgetState?.rawSpec || currentSpecRef?.current || {};
			const focusState = rawSpec?._line_focus_state;
			if (!focusState || typeof focusState !== "object") return null;
			const opacityCondition = rawSpec?.encoding?.opacity?.condition;
			const dimOpacity = Number(rawSpec?.encoding?.opacity?.value);
			return {
				lines: Array.isArray(focusState.lines) ? clone$1(focusState.lines) : [],
				lineField: focusState.line_field || "series",
				...Number.isFinite(dimOpacity) ? { dimOpacity } : {},
				...typeof opacityCondition?.test === "string" ? { test: opacityCondition.test } : {}
			};
		}
		function readTrendState(widgetState = {}) {
			const rawSpec = widgetState?.rawSpec || currentSpecRef?.current || {};
			if (!(Array.isArray(rawSpec?.layer) ? rawSpec.layer : []).find((layer) => layer?._widgetvaTag === "line.highlightTrend")) return null;
			return { trendType: "regression" };
		}
		function readMovingAverageState(widgetState = {}) {
			const rawSpec = widgetState?.rawSpec || currentSpecRef?.current || {};
			const maLayer = (Array.isArray(rawSpec?.layer) ? rawSpec.layer : []).find((layer) => layer?._widgetvaTag === "line.showMovingAverage");
			if (!maLayer) return null;
			const frameRange = (Array.isArray(maLayer.transform) ? maLayer.transform.find((transform) => Array.isArray(transform?.frame)) : null)?.frame;
			return { windowSize: Array.isArray(frameRange) ? Math.abs(Number(frameRange[0])) + 1 : 3 };
		}
		function readDrilldownState(widgetState = {}) {
			const rawSpec = widgetState?.rawSpec || currentSpecRef?.current || {};
			const drillState = rawSpec?._line_drilldown_state;
			if (!drillState || typeof drillState !== "object") return null;
			const parent = drillState.parent && typeof drillState.parent === "object" && !Array.isArray(drillState.parent) ? clone$1(drillState.parent) : {};
			if (Number.isFinite(parent.month)) return {
				level: "month",
				value: Number(parent.month),
				parent: Number.isFinite(parent.year) ? { year: Number(parent.year) } : {},
				title: rawSpec?.title || ""
			};
			if (Number.isFinite(parent.year)) return {
				level: "year",
				value: Number(parent.year),
				parent: {},
				title: rawSpec?.title || ""
			};
			return {
				level: "drilldown",
				parent,
				title: rawSpec?.title || ""
			};
		}
		function readViewportState(widgetState = {}) {
			const xDomain = (widgetState?.rawSpec || currentSpecRef?.current || {})?.encoding?.x?.scale?.domain;
			if (!Array.isArray(xDomain) || xDomain.length !== 2) return null;
			return { xDomain: clone$1(xDomain) };
		}
		return {
			getState() {
				return {
					view: {
						...currentViewport ? clone$1(currentViewport) : {},
						...currentFocus ? { focus: clone$1(currentFocus) } : {},
						...currentTrend ? { trend: clone$1(currentTrend) } : {},
						...currentMovingAverage ? { movingAverage: clone$1(currentMovingAverage) } : {},
						...currentDrilldown ? { drilldown: clone$1(currentDrilldown) } : {}
					},
					selections: currentSelection ? { localSelection: clone$1(currentSelection) } : {}
				};
			},
			async renderFromState(widgetState = {}) {
				await applySelection(Object.values(widgetState?.selections || {}).find((entry) => Array.isArray(entry?.values)) || null);
				await applyViewport(readViewportState(widgetState));
				await applyFocus(readFocusState(widgetState));
				await applyTrend(readTrendState(widgetState));
				await applyMovingAverage(readMovingAverageState(widgetState));
				await applyDrilldown(readDrilldownState(widgetState));
			},
			async setSelection(selection) {
				return applySelection(selection);
			},
			async readDebugSnapshot() {
				return invokeObservableWorker(frame, "readDebugSnapshot", null);
			}
		};
	}
	async function attachWidgetVAToObservableD3ScatterPage({ root = globalThis.window, sessionId = null, userIntent = "Analyze and manipulate the current official Observable D3 scatterplot through WidgetVA structured actions.", timeoutMs = 5e3, pollMs = 25 } = {}) {
		const pageUrl = root?.location?.href || "";
		if (!isObservableD3NotebookPage(pageUrl)) throw new Error(`Unsupported Observable D3 URL: ${pageUrl}`);
		const pageShape = describeObservableD3PageShape(root);
		const workerFrame = await waitForObservableWorkerFrame({
			root,
			timeoutMs,
			pollMs
		});
		const scatterSurface = await waitForObservableWorkerScatterSurface(workerFrame, {
			timeoutMs,
			pollMs,
			notebook: pageShape?.notebook || null
		});
		const surfaceDescription = scatterSurface?.surface || null;
		if (surfaceDescription?.inferredKind !== "scatter") throw new Error(`Observable D3 page is not yet supported for WidgetVA runtime attachment: inferred kind ${surfaceDescription?.inferredKind || "unknown"}.`);
		const rows = Array.isArray(scatterSurface?.rows) ? scatterSurface.rows : [];
		if (rows.length === 0) throw new Error("No scatter points were detected on the Observable D3 surface.");
		const spec = buildObservableScatterSpec(rows);
		const widgetAdapter = createProviderFamilyAdapter("scatter", "d3");
		const currentSpecRef = { current: clone$1(spec) };
		const wrapper = createObservableScatterSurfaceWrapper({
			frame: workerFrame,
			currentSpecRef
		});
		const widget = createWidgetInstance({
			widgetAdapter,
			spec,
			runtimeOptions: { hostBridge: createOfficialPageHostBridge({
				sessionId: sessionId || `official-observable-d3-${pageShape?.notebook?.slug || "scatterplot"}`,
				baselineSpec: spec,
				currentSpecRef,
				userIntent
			}) }
		});
		await widget.mount({
			view: wrapper,
			surface: root.document?.documentElement || null,
			spec
		});
		const syncSpecDrivenState = async (actionContext = null) => {
			const widgetState = typeof widget.readState === "function" ? widget.readState() : null;
			if (widgetState && typeof wrapper.renderFromState === "function") await wrapper.renderFromState(widgetState);
			else await wrapper.syncCurrentSpec?.();
			const actionCall = actionContext?.args?.[0];
			const actionResult = actionContext?.result;
			if (!actionCall || typeof actionCall !== "object") return;
			if (actionCall.name === "scatter.identifyClusters" && actionResult?.ok !== false) {
				const nClusters = actionResult?.result?.nClusters || actionCall?.params?.nClusters || 3;
				await wrapper.setClusterAnnotation?.({
					nClusters,
					clusterField: actionResult?.result?.clusterField || `cluster_${nClusters}`,
					method: actionResult?.result?.method || actionCall?.params?.method || "kmeans"
				});
			}
			if (actionCall.name === "scatter.showRegression" && actionResult?.ok !== false) await wrapper.setRegressionAnnotation?.({ method: actionResult?.result?.method || actionCall?.params?.method || "linear" });
		};
		const widgetApi = createPostActionSyncProxy(widget, syncSpecDrivenState);
		const pagePort = root?.__widgetVA ? createPostActionSyncProxy(root.__widgetVA, syncSpecDrivenState) : null;
		if (pagePort) root.__widgetVA = pagePort;
		await syncSpecDrivenState();
		async function readDebugSnapshot() {
			return {
				provider: "d3",
				kind: "scatter",
				pageShape,
				surface: surfaceDescription,
				route: "worker",
				...await wrapper.readDebugSnapshot()
			};
		}
		async function previewVisibleBrush() {
			const snapshot = await readDebugSnapshot();
			const selection = buildSelectionFromRowSummary(snapshot?.rowSummary || null);
			if (!selection) throw new Error("Unable to preview brush because no scatter rows were detected.");
			return {
				ok: true,
				selection,
				selectionResult: await wrapper.setSelection(selection) || null,
				snapshot
			};
		}
		async function renderDebugProbe() {
			return invokeObservableWorker(workerFrame, "renderDebugProbe", null, { timeoutMs });
		}
		return {
			provider: "d3",
			kind: "scatter",
			pageShape,
			surface: surfaceDescription,
			rows,
			widget: widgetApi,
			widgetAdapter,
			runAgentLoop: createOfficialPageAgentLoopRunner(root),
			readDebugSnapshot,
			previewVisibleBrush,
			renderDebugProbe,
			describeAgentContract() {
				return widgetApi.describeAgentContract();
			},
			dispose() {
				widget.dispose();
			}
		};
	}
	async function attachWidgetVAToObservableD3BarPage({ root = globalThis.window, sessionId = null, userIntent = "Analyze and manipulate the current official Observable D3 bar chart through WidgetVA structured actions.", timeoutMs = 5e3, pollMs = 25 } = {}) {
		const pageUrl = root?.location?.href || "";
		if (!isObservableD3NotebookPage(pageUrl)) throw new Error(`Unsupported Observable D3 URL: ${pageUrl}`);
		const pageShape = describeObservableD3PageShape(root);
		const workerFrame = await waitForObservableWorkerFrame({
			root,
			timeoutMs,
			pollMs
		});
		const barSurface = await waitForObservableWorkerBarSurface(workerFrame, {
			timeoutMs,
			pollMs,
			notebook: pageShape?.notebook || null
		});
		const surfaceDescription = barSurface?.surface || null;
		if (surfaceDescription?.inferredKind !== "bar") throw new Error(`Observable D3 page is not yet supported for WidgetVA runtime attachment: inferred kind ${surfaceDescription?.inferredKind || "unknown"}.`);
		const rows = Array.isArray(barSurface?.rows) ? barSurface.rows : [];
		if (rows.length === 0) throw new Error("No bars were detected on the Observable D3 surface.");
		const spec = buildObservableBarSpec(rows);
		const widgetAdapter = createProviderFamilyAdapter("bar", "d3");
		const currentSpecRef = { current: clone$1(spec) };
		const wrapper = createObservableBarSurfaceWrapper({
			frame: workerFrame,
			currentSpecRef
		});
		const widget = createWidgetInstance({
			widgetAdapter,
			spec,
			runtimeOptions: { hostBridge: createOfficialPageHostBridge({
				sessionId: sessionId || `official-observable-d3-${pageShape?.notebook?.slug || "bar-chart"}`,
				baselineSpec: spec,
				currentSpecRef,
				userIntent
			}) }
		});
		await widget.mount({
			view: wrapper,
			surface: root.document?.documentElement || null,
			spec
		});
		const syncSpecDrivenState = async () => {
			const widgetState = typeof widget.readState === "function" ? widget.readState() : null;
			if (widgetState && typeof wrapper.renderFromState === "function") {
				await wrapper.renderFromState(widgetState);
				return;
			}
			await wrapper.syncCurrentSpec?.();
		};
		const widgetApi = createPostActionSyncProxy(widget, syncSpecDrivenState);
		const pagePort = root?.__widgetVA ? createPostActionSyncProxy(root.__widgetVA, syncSpecDrivenState) : null;
		if (pagePort) root.__widgetVA = pagePort;
		await syncSpecDrivenState();
		async function readDebugSnapshot() {
			return {
				provider: "d3",
				kind: "bar",
				pageShape,
				surface: surfaceDescription,
				route: "worker",
				...await wrapper.readDebugSnapshot()
			};
		}
		async function renderDebugProbe() {
			return invokeObservableWorker(workerFrame, "renderDebugProbe", null, { timeoutMs });
		}
		return {
			provider: "d3",
			kind: "bar",
			pageShape,
			surface: surfaceDescription,
			rows,
			widget: widgetApi,
			widgetAdapter,
			runAgentLoop: createOfficialPageAgentLoopRunner(root),
			readDebugSnapshot,
			renderDebugProbe,
			describeAgentContract() {
				return widgetApi.describeAgentContract();
			},
			dispose() {
				widget.dispose();
			}
		};
	}
	async function attachWidgetVAToObservableD3LinePage({ root = globalThis.window, sessionId = null, userIntent = "Analyze and manipulate the current official Observable D3 line chart through WidgetVA structured actions.", timeoutMs = 5e3, pollMs = 25 } = {}) {
		const pageUrl = root?.location?.href || "";
		if (!isObservableD3NotebookPage(pageUrl)) throw new Error(`Unsupported Observable D3 URL: ${pageUrl}`);
		const pageShape = describeObservableD3PageShape(root);
		const workerFrame = await waitForObservableWorkerFrame({
			root,
			timeoutMs,
			pollMs
		});
		const lineSurface = await waitForObservableWorkerLineSurface(workerFrame, {
			timeoutMs,
			pollMs,
			notebook: pageShape?.notebook || null
		});
		const surfaceDescription = lineSurface?.surface || null;
		if (surfaceDescription?.inferredKind !== "line") throw new Error(`Observable D3 page is not yet supported for WidgetVA runtime attachment: inferred kind ${surfaceDescription?.inferredKind || "unknown"}.`);
		const rows = Array.isArray(lineSurface?.rows) ? lineSurface.rows : [];
		if (rows.length === 0) throw new Error("No line rows were detected on the Observable D3 surface.");
		const spec = buildObservableLineSpec(rows);
		const widgetAdapter = createProviderFamilyAdapter("line", "d3");
		const currentSpecRef = { current: clone$1(spec) };
		const wrapper = createObservableLineSurfaceWrapper({
			frame: workerFrame,
			currentSpecRef
		});
		const widget = createWidgetInstance({
			widgetAdapter,
			spec,
			runtimeOptions: { hostBridge: createOfficialPageHostBridge({
				sessionId: sessionId || `official-observable-d3-${pageShape?.notebook?.slug || "line-chart"}`,
				baselineSpec: spec,
				currentSpecRef,
				userIntent
			}) }
		});
		await widget.mount({
			view: wrapper,
			surface: root.document?.documentElement || null,
			spec
		});
		const syncState = async () => {
			const widgetState = typeof widget.readState === "function" ? widget.readState() : null;
			if (widgetState && typeof wrapper.renderFromState === "function") await wrapper.renderFromState(widgetState);
		};
		const widgetApi = createPostActionSyncProxy(widget, syncState);
		const pagePort = root?.__widgetVA ? createPostActionSyncProxy(root.__widgetVA, syncState) : null;
		if (pagePort) root.__widgetVA = pagePort;
		await syncState();
		async function readDebugSnapshot() {
			return {
				provider: "d3",
				kind: "line",
				pageShape,
				surface: surfaceDescription,
				route: "worker",
				...await wrapper.readDebugSnapshot()
			};
		}
		async function renderDebugProbe() {
			return invokeObservableWorker(workerFrame, "renderDebugProbe", null, { timeoutMs });
		}
		return {
			provider: "d3",
			kind: "line",
			pageShape,
			surface: surfaceDescription,
			rows,
			widget: widgetApi,
			widgetAdapter,
			readDebugSnapshot,
			renderDebugProbe,
			describeAgentContract() {
				return widgetApi.describeAgentContract();
			},
			dispose() {
				widget.dispose();
			}
		};
	}
	async function attachWidgetVAToObservableD3Page(options = {}) {
		const inferredSlug = (describeObservableD3PageShape(options?.root || globalThis.window)?.notebook || null)?.slug || "";
		if (inferredSlug.includes("bar")) return attachWidgetVAToObservableD3BarPage(options);
		if (inferredSlug.includes("line") || inferredSlug.includes("index-chart")) return attachWidgetVAToObservableD3LinePage(options);
		return attachWidgetVAToObservableD3ScatterPage(options);
	}
	async function bootstrapObservableD3Page({ root = globalThis.window, sessionId = null, userIntent, timeoutMs = 5e3, pollMs = 25, enableExtensionBridge = true } = {}) {
		const controller = await attachWidgetVAToObservableD3Page({
			root,
			sessionId,
			userIntent,
			timeoutMs,
			pollMs
		});
		const disposeBridge = enableExtensionBridge ? installBrowserExtensionBridge({ root }) : () => {};
		return {
			...controller,
			pagePort: root?.__widgetVA || null,
			runAgentLoop: createOfficialPageAgentLoopRunner(root),
			dispose() {
				try {
					disposeBridge?.();
				} finally {
					controller.dispose();
				}
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/naturalLanguagePlanner.js
	var DEFAULT_OPENROUTER_AGENT_MODEL = "deepseek/deepseek-v4-flash";
	function clone(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function buildAgentMessages({ objective, observe }) {
		const systemPrompt = [
			"You are an analyst agent operating a widget-based visual analytics workspace.",
			"Your job is to convert the user objective into exactly one next structured operation.",
			"Use only the actions, perceptions, and data-query surfaces already exposed by the workspace observation.",
			"Return JSON only.",
			"The JSON must contain assistantMessage, rationale, and operation.",
			"operation.kind must be exactly one of: action, perception, data_query.",
			"If operation.kind is action or perception, include operation.name.",
			"If possible, include operation.queryScope.widgetRef.",
			"For action/perception operations, put arguments in operation.params.",
			"For data_query operations, include operation.dataRef and operation.query.",
			"Do not return markdown fences."
		].join(" ");
		const userPrompt = JSON.stringify({
			objective,
			observe
		});
		return [{
			role: "system",
			content: systemPrompt
		}, {
			role: "user",
			content: userPrompt
		}];
	}
	function buildRepairMessages({ objective, observe, previousContent = "" }) {
		const systemPrompt = [
			"You previously returned an invalid plan for a widget-based visual analytics agent.",
			"Return JSON only.",
			"Your JSON must contain assistantMessage, rationale, and operation.",
			"operation.kind must be exactly one of: action, perception, data_query.",
			"If operation.kind is action or perception, include operation.name.",
			"If possible, include operation.queryScope.widgetRef.",
			"Do not include markdown fences or explanatory prose."
		].join(" ");
		const userPrompt = JSON.stringify({
			objective,
			observe,
			previousContent,
			requiredShape: {
				assistantMessage: "string",
				rationale: "string",
				operation: {
					kind: "action|perception|data_query",
					name: "string when kind is action/perception",
					queryScope: { widgetRef: "string" },
					params: {},
					dataRef: "string when kind is data_query",
					query: {}
				}
			}
		});
		return [{
			role: "system",
			content: systemPrompt
		}, {
			role: "user",
			content: userPrompt
		}];
	}
	function buildReasonMessages({ objective, observe, plan, result, verification, latestCoordinationResult } = {}) {
		const systemPrompt = [
			"You are the answer stage of a widget-based visual analytics agent.",
			"You have already observed, planned, executed, and verified one analysis step.",
			"Write the user-facing answer for this turn.",
			"Use the actual execution result and verification outcome.",
			"Do not invent computed values that are not present in the runtime result.",
			"Return JSON only.",
			"The JSON must contain answer."
		].join(" ");
		const userPrompt = JSON.stringify({
			objective,
			observe,
			plan,
			result,
			verification,
			latestCoordinationResult
		});
		return [{
			role: "system",
			content: systemPrompt
		}, {
			role: "user",
			content: userPrompt
		}];
	}
	function extractJsonObject(text = "") {
		if (typeof text !== "string") return null;
		try {
			return JSON.parse(text);
		} catch {
			const start = text.indexOf("{");
			const end = text.lastIndexOf("}");
			if (start >= 0 && end > start) try {
				return JSON.parse(text.slice(start, end + 1));
			} catch {
				return null;
			}
			return null;
		}
	}
	function readPlannedOperation(plan = {}) {
		if (plan?.operation) return plan.operation;
		for (const [key, value] of Object.entries(plan || {})) {
			const normalizedKey = key.toLowerCase();
			if (value && typeof value === "object" && (normalizedKey === "action" || normalizedKey === "perception" || normalizedKey === "data_query" || normalizedKey === "dataquery")) return {
				kind: normalizedKey === "dataquery" ? "data_query" : normalizedKey,
				...value
			};
		}
		return {};
	}
	function inferOperationKind(plan = {}, operation = {}) {
		const explicitKind = operation.kind || plan.kind;
		if (explicitKind) return explicitKind;
		if (operation.dataRef || operation.query) return "data_query";
		if (operation.name) {
			if (String(operation.name).startsWith("perception.")) return "perception";
			return "action";
		}
		return null;
	}
	function normalizeOperation(plan = {}, observe = {}) {
		const operation = readPlannedOperation(plan);
		const fallbackWidgetRef = (Array.isArray(observe?.workspace?.widgets) ? observe.workspace.widgets : [])[0]?.ref || null;
		const flatWidgetRef = operation["queryScope.widgetRef"] || operation["query_scope.widget_ref"] || null;
		const widgetRef = operation.queryScope?.widgetRef || operation.query_scope?.widget_ref || flatWidgetRef || fallbackWidgetRef || null;
		return {
			assistantMessage: typeof plan?.assistantMessage === "string" ? plan.assistantMessage : "Planned one next analysis step.",
			rationale: typeof plan?.rationale === "string" ? plan.rationale : "",
			kind: inferOperationKind(plan, operation),
			name: operation.name || null,
			queryScope: widgetRef ? { widgetRef } : void 0,
			params: clone(operation.params || {}),
			dataRef: operation.dataRef || null,
			query: clone(operation.query || null)
		};
	}
	function isExecutableOperation(operation = {}) {
		if (!operation || typeof operation !== "object") return false;
		if (operation.kind === "action" || operation.kind === "perception") return typeof operation.name === "string" && operation.name.trim().length > 0;
		if (operation.kind === "data_query") return typeof operation.dataRef === "string" && operation.dataRef.length > 0 && Boolean(operation.query);
		return false;
	}
	function buildSafeFallbackOperation(observe = {}) {
		const widgets = Array.isArray(observe?.workspace?.widgets) ? observe.workspace.widgets : [];
		const focusedWidgetRef = observe?.observation?.focusedWidgetRef || observe?.loopContext?.view?.shared?.focusedWidget || widgets[0]?.ref || null;
		return {
			assistantMessage: "I will first inspect the currently focused widget before taking a stronger interaction step.",
			rationale: "The model response did not yield a valid executable operation, so the runtime is falling back to a safe perception on the active widget.",
			kind: "perception",
			name: "perception.inspectViewConfig",
			...focusedWidgetRef ? { queryScope: { widgetRef: focusedWidgetRef } } : {},
			params: {},
			dataRef: null,
			query: null
		};
	}
	function createNaturalLanguagePlanner({ completeChat, model = DEFAULT_OPENROUTER_AGENT_MODEL, temperature = .2 } = {}) {
		if (typeof completeChat !== "function") throw new Error("createNaturalLanguagePlanner requires completeChat().");
		return async function planner({ objective = null, observe = null, actor = "agent" } = {}) {
			const safeObjective = typeof objective === "string" && objective.trim().length > 0 ? objective.trim() : "Inspect the current visualization workspace and take the next useful step.";
			const primaryResponse = await completeChat({
				model,
				temperature,
				messages: buildAgentMessages({
					objective: safeObjective,
					observe
				})
			});
			const primaryContent = primaryResponse?.content || "";
			const primaryPlan = extractJsonObject(primaryContent);
			if (!primaryPlan) throw new Error("Agent response did not contain valid JSON.");
			let normalizedOperation = normalizeOperation(primaryPlan, observe);
			let resolvedResponse = primaryResponse;
			let resolvedPlan = primaryPlan;
			if (!isExecutableOperation(normalizedOperation)) {
				const repairedResponse = await completeChat({
					model,
					temperature,
					messages: buildRepairMessages({
						objective: safeObjective,
						observe,
						previousContent: primaryContent
					})
				});
				const repairedPlan = extractJsonObject(repairedResponse?.content || "");
				const repairedOperation = repairedPlan ? normalizeOperation(repairedPlan, observe) : null;
				if (repairedPlan && isExecutableOperation(repairedOperation)) {
					resolvedResponse = repairedResponse;
					resolvedPlan = repairedPlan;
					normalizedOperation = repairedOperation;
				} else normalizedOperation = buildSafeFallbackOperation(observe);
			}
			return {
				assistantMessage: normalizedOperation.assistantMessage,
				rationale: normalizedOperation.rationale,
				operation: {
					kind: normalizedOperation.kind,
					...normalizedOperation.name ? { name: normalizedOperation.name } : {},
					...normalizedOperation.queryScope ? { queryScope: normalizedOperation.queryScope } : {},
					...normalizedOperation.params ? { params: normalizedOperation.params } : {},
					...normalizedOperation.dataRef ? { dataRef: normalizedOperation.dataRef } : {},
					...normalizedOperation.query ? { query: normalizedOperation.query } : {}
				},
				actor,
				source: "planner",
				model,
				rawResponse: clone(resolvedResponse?.raw || null),
				rawContent: resolvedResponse?.content || "",
				rawPlan: clone(resolvedPlan)
			};
		};
	}
	function createNaturalLanguageReasoner({ completeChat, model = DEFAULT_OPENROUTER_AGENT_MODEL, temperature = .2 } = {}) {
		if (typeof completeChat !== "function") throw new Error("createNaturalLanguageReasoner requires completeChat().");
		return async function reasoner({ objective = null, observe = null, plan = null, result = null, verification = null, latestCoordinationResult = null } = {}) {
			const response = await completeChat({
				model,
				temperature,
				messages: buildReasonMessages({
					objective,
					observe,
					plan,
					result,
					verification,
					latestCoordinationResult
				})
			});
			const content = response?.content || "";
			const parsed = extractJsonObject(content);
			if (parsed && typeof parsed.answer === "string" && parsed.answer.trim().length > 0) return {
				answer: parsed.answer.trim(),
				rawResponse: clone(response?.raw || null),
				rawContent: content
			};
			return {
				answer: (typeof result?.summary === "string" && result.summary.length > 0 ? result.summary : null) || (typeof result?.message === "string" && result.message.length > 0 ? result.message : null) || (typeof plan?.assistantMessage === "string" && plan.assistantMessage.length > 0 ? plan.assistantMessage : null) || "Completed one agent step.",
				rawResponse: clone(response?.raw || null),
				rawContent: content
			};
		};
	}
	async function runNaturalLanguagePagePortAgentSession(port, { objective = null, completeChat, model = DEFAULT_OPENROUTER_AGENT_MODEL, temperature = .2, ...loopOptions } = {}) {
		const planner = createNaturalLanguagePlanner({
			completeChat,
			model,
			temperature
		});
		const reasoner = createNaturalLanguageReasoner({
			completeChat,
			model,
			temperature
		});
		return runPagePortAgentSession(port, {
			...loopOptions,
			objective,
			planner,
			reasoner
		});
	}
	//#endregion
	//#region extension/src/shared/officialPageAgentBridge.js
	var WIDGETVA_AGENT_BRIDGE_SOURCE_PAGE = "widgetva-official-page-agent-page";
	var WIDGETVA_AGENT_BRIDGE_REQUEST = "widgetva:official-page-agent-request";
	//#endregion
	//#region extension/src/page/officialPageAgentClient.js
	var DEFAULT_OFFICIAL_PAGE_AGENT_BRIDGE_TIMEOUT_MS = 15e3;
	var DEFAULT_OFFICIAL_PAGE_AGENT_CHAT_TIMEOUT_MS = 45e3;
	function invokeBridge(root, method, params = null, { timeoutMs = DEFAULT_OFFICIAL_PAGE_AGENT_BRIDGE_TIMEOUT_MS } = {}) {
		const requestId = `widgetva_agent_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
		return new Promise((resolve, reject) => {
			const timeout = setTimeout(() => {
				cleanup();
				reject(/* @__PURE__ */ new Error(`Timed out waiting for WidgetVA official-page agent bridge: ${method} after ${timeoutMs}ms.`));
			}, timeoutMs);
			const cleanup = () => {
				clearTimeout(timeout);
				root.removeEventListener("message", handleMessage);
			};
			const handleMessage = (event) => {
				const message = event?.data;
				if (event.source !== root || !message || message.source !== "widgetva-official-page-agent-content" || message.type !== "widgetva:official-page-agent-response" || message.id !== requestId) return;
				cleanup();
				if (message.ok === false) {
					reject(new Error(message?.error?.message || `WidgetVA official-page agent bridge failed: ${method}.`));
					return;
				}
				resolve(message.result ?? null);
			};
			root.addEventListener("message", handleMessage);
			root.postMessage({
				source: WIDGETVA_AGENT_BRIDGE_SOURCE_PAGE,
				type: WIDGETVA_AGENT_BRIDGE_REQUEST,
				id: requestId,
				method,
				params
			}, "*");
		});
	}
	function configureOfficialPageAgent(root, params = {}) {
		return invokeBridge(root, "configure", params);
	}
	function readOfficialPageAgentConfig(root) {
		return invokeBridge(root, "readConfig", null);
	}
	async function completeOfficialPageAgentChat(root, request = {}) {
		const { timeoutMs = DEFAULT_OFFICIAL_PAGE_AGENT_CHAT_TIMEOUT_MS, ...chatRequest } = request || {};
		const result = await invokeBridge(root, "chat", chatRequest, { timeoutMs });
		return {
			model: result?.model || chatRequest?.model || null,
			raw: result?.raw || null,
			content: result?.content || ""
		};
	}
	//#endregion
	//#region extension/src/page/observableD3Bootstrap.js
	var OBSERVABLE_D3_BOOTSTRAP_KEY = "__widgetVAOfficialPageBootstrap";
	var OBSERVABLE_D3_BOOTSTRAP_ENTRY = "observableD3";
	function readOfficialPageChatTimeout(normalized) {
		if (!normalized || typeof normalized !== "object") return;
		return normalized.chatTimeoutMs ?? normalized.timeoutMs;
	}
	function readOfficialPageMaxTurns(normalized) {
		if (!normalized || typeof normalized !== "object") return;
		return normalized.maxTurns;
	}
	function summarizeError(error) {
		if (error instanceof Error) return {
			name: error.name,
			message: error.message
		};
		return {
			name: "Error",
			message: String(error || "Unknown WidgetVA Observable D3 bootstrap error.")
		};
	}
	function ensureBootstrapState(root) {
		if (!root["__widgetVAOfficialPageBootstrap"] || typeof root["__widgetVAOfficialPageBootstrap"] !== "object") root[OBSERVABLE_D3_BOOTSTRAP_KEY] = {};
		const state = root[OBSERVABLE_D3_BOOTSTRAP_KEY];
		if (!state["observableD3"] || typeof state["observableD3"] !== "object") state[OBSERVABLE_D3_BOOTSTRAP_ENTRY] = {
			provider: "d3",
			pageType: "official-observable-notebook",
			status: "idle",
			pageShape: null,
			workerFrame: null,
			surface: null,
			controller: null,
			pagePort: null,
			runAgentLoop: null,
			runNaturalLanguageAgentLoop: null,
			configureAgent: null,
			readAgentConfig: null,
			error: null,
			pageUrl: null,
			startedAt: null,
			completedAt: null,
			promise: null
		};
		return state[OBSERVABLE_D3_BOOTSTRAP_ENTRY];
	}
	function disposeController(entry) {
		try {
			entry?.controller?.dispose?.();
		} catch {}
	}
	function clearEntryRuntime(entry, root) {
		disposeController(entry);
		entry.workerFrame = null;
		entry.surface = null;
		entry.controller = null;
		entry.pagePort = null;
		entry.runAgentLoop = null;
		entry.runNaturalLanguageAgentLoop = null;
		entry.configureAgent = null;
		entry.readAgentConfig = null;
		entry.promise = null;
		root.__widgetVAOfficialPageRunAgentLoop = void 0;
		root.__widgetVAOfficialPageRunNaturalLanguageAgentLoop = void 0;
		root.__widgetVAOfficialPageConfigureAgent = void 0;
		root.__widgetVAOfficialPageReadAgentConfig = void 0;
		if (root?.__widgetVA) try {
			delete root.__widgetVA;
		} catch {
			root.__widgetVA = void 0;
		}
	}
	async function ensureObservableD3PageBootstrap({ root = globalThis.window, isSupportedPage, describePage, waitForWorkerFrame, bootstrapPage, timeoutMs = 15e3 } = {}) {
		if (!root || typeof root !== "object") throw new Error("ensureObservableD3PageBootstrap requires a page-like root object.");
		if (typeof isSupportedPage !== "function") throw new Error("ensureObservableD3PageBootstrap requires isSupportedPage().");
		if (typeof describePage !== "function") throw new Error("ensureObservableD3PageBootstrap requires describePage().");
		if (typeof waitForWorkerFrame !== "function") throw new Error("ensureObservableD3PageBootstrap requires waitForWorkerFrame().");
		if (typeof bootstrapPage !== "function") throw new Error("ensureObservableD3PageBootstrap requires bootstrapPage().");
		const entry = ensureBootstrapState(root);
		const pageUrl = root?.location?.href || "";
		if (!isSupportedPage(pageUrl)) {
			if (entry.pageUrl && entry.pageUrl !== pageUrl) clearEntryRuntime(entry, root);
			entry.status = "unsupported";
			entry.error = null;
			entry.pageUrl = pageUrl;
			entry.promise = null;
			return entry;
		}
		if (entry.pageUrl && entry.pageUrl !== pageUrl) {
			clearEntryRuntime(entry, root);
			entry.status = "idle";
			entry.error = null;
		}
		if (entry.status === "ready") {
			entry.pageUrl = pageUrl;
			entry.pagePort = root.__widgetVA || entry.controller?.pagePort || null;
			entry.runAgentLoop = (options = {}) => entry.controller?.runAgentLoop?.(options);
			entry.runNaturalLanguageAgentLoop = async (options = {}) => {
				const normalized = typeof options === "string" ? { objective: options } : { ...options || {} };
				return runNaturalLanguagePagePortAgentSession(root.__widgetVA, {
					objective: normalized.objective || normalized.prompt || null,
					model: normalized.model || "deepseek/deepseek-v4-flash",
					temperature: normalized.temperature,
					maxTurns: readOfficialPageMaxTurns(normalized),
					completeChat: (request) => completeOfficialPageAgentChat(root, {
						...request,
						timeoutMs: readOfficialPageChatTimeout(normalized)
					})
				});
			};
			entry.configureAgent = (options = {}) => configureOfficialPageAgent(root, options);
			entry.readAgentConfig = () => readOfficialPageAgentConfig(root);
			root.__widgetVAOfficialPageRunAgentLoop = entry.runAgentLoop;
			root.__widgetVAOfficialPageRunNaturalLanguageAgentLoop = entry.runNaturalLanguageAgentLoop;
			root.__widgetVAOfficialPageConfigureAgent = entry.configureAgent;
			root.__widgetVAOfficialPageReadAgentConfig = entry.readAgentConfig;
			return entry;
		}
		if (entry.promise) return entry.promise;
		entry.status = "booting";
		entry.error = null;
		entry.pageUrl = pageUrl;
		entry.startedAt = Date.now();
		entry.completedAt = null;
		entry.promise = (async () => {
			try {
				const pageShape = describePage(root);
				const workerFrame = await waitForWorkerFrame({
					root,
					timeoutMs
				});
				const controller = await bootstrapPage({
					root,
					sessionId: `official-observable-d3-${pageShape?.notebook?.slug || "page"}`,
					enableExtensionBridge: true,
					timeoutMs
				});
				entry.status = "ready";
				entry.pageShape = pageShape;
				entry.workerFrame = workerFrame ? { src: workerFrame.getAttribute?.("src") || workerFrame.src || null } : null;
				entry.surface = controller?.surface || null;
				entry.controller = controller;
				entry.pagePort = root.__widgetVA || controller?.pagePort || null;
				entry.runAgentLoop = (options = {}) => entry.controller?.runAgentLoop?.(options);
				entry.runNaturalLanguageAgentLoop = async (options = {}) => {
					const normalized = typeof options === "string" ? { objective: options } : { ...options || {} };
					return runNaturalLanguagePagePortAgentSession(root.__widgetVA, {
						objective: normalized.objective || normalized.prompt || null,
						model: normalized.model || "deepseek/deepseek-v4-flash",
						temperature: normalized.temperature,
						maxTurns: readOfficialPageMaxTurns(normalized),
						completeChat: (request) => completeOfficialPageAgentChat(root, {
							...request,
							timeoutMs: readOfficialPageChatTimeout(normalized)
						})
					});
				};
				entry.configureAgent = (options = {}) => configureOfficialPageAgent(root, options);
				entry.readAgentConfig = () => readOfficialPageAgentConfig(root);
				root.__widgetVAOfficialPageRunAgentLoop = entry.runAgentLoop;
				root.__widgetVAOfficialPageRunNaturalLanguageAgentLoop = entry.runNaturalLanguageAgentLoop;
				root.__widgetVAOfficialPageConfigureAgent = entry.configureAgent;
				root.__widgetVAOfficialPageReadAgentConfig = entry.readAgentConfig;
				root.__widgetVAObservableD3Debug = () => entry.controller?.readDebugSnapshot?.();
				root.__widgetVAObservableD3PreviewBrush = () => entry.controller?.previewVisibleBrush?.();
				root.__widgetVAObservableD3Probe = () => entry.controller?.renderDebugProbe?.();
				entry.completedAt = Date.now();
				entry.promise = null;
				return entry;
			} catch (error) {
				entry.status = "error";
				entry.error = summarizeError(error);
				entry.runAgentLoop = null;
				entry.runNaturalLanguageAgentLoop = null;
				entry.configureAgent = null;
				entry.readAgentConfig = null;
				root.__widgetVAOfficialPageRunAgentLoop = void 0;
				root.__widgetVAOfficialPageRunNaturalLanguageAgentLoop = void 0;
				root.__widgetVAOfficialPageConfigureAgent = void 0;
				root.__widgetVAOfficialPageReadAgentConfig = void 0;
				entry.completedAt = Date.now();
				entry.promise = null;
				throw error;
			}
		})();
		return entry.promise;
	}
	//#endregion
	//#region extension/src/page/observableD3PageScript.js
	async function startWidgetVAOnObservableD3Pages() {
		window.__widgetVAOfficialPageBootstrap = window.__widgetVAOfficialPageBootstrap || {};
		window.__widgetVAOfficialPageBootstrap.observableD3 = {
			provider: "d3",
			pageType: "official-observable-notebook",
			status: "booting"
		};
		return ensureObservableD3PageBootstrap({
			root: window,
			isSupportedPage: isObservableD3NotebookPage,
			describePage: describeObservableD3PageShape,
			waitForWorkerFrame: waitForObservableWorkerFrame,
			bootstrapPage: bootstrapObservableD3Page,
			timeoutMs: 15e3
		});
	}
	function recordObservableD3BootstrapError(error) {
		window.__widgetVAOfficialPageBootstrap = window.__widgetVAOfficialPageBootstrap || {};
		window.__widgetVAOfficialPageBootstrap.observableD3 = {
			provider: "d3",
			pageType: "official-observable-notebook",
			status: "error",
			error: {
				name: error?.name || "Error",
				message: error?.message || String(error)
			}
		};
		console.error("[WidgetVA] Failed to bootstrap on the official Observable D3 page.", error);
	}
	function createObservableD3RouteWatcher() {
		let lastHref = window.location.href;
		let scheduled = false;
		const runBootstrap = async () => {
			try {
				await startWidgetVAOnObservableD3Pages();
			} catch (error) {
				recordObservableD3BootstrapError(error);
			}
		};
		const scheduleBootstrap = () => {
			if (scheduled) return;
			scheduled = true;
			queueMicrotask(() => {
				scheduled = false;
				const currentHref = window.location.href;
				if (currentHref === lastHref) return;
				lastHref = currentHref;
				runBootstrap();
			});
		};
		const wrapHistoryMethod = (name) => {
			const original = window.history?.[name];
			if (typeof original !== "function") return;
			window.history[name] = function wrappedHistoryMethod(...args) {
				const result = original.apply(this, args);
				scheduleBootstrap();
				return result;
			};
		};
		wrapHistoryMethod("pushState");
		wrapHistoryMethod("replaceState");
		window.addEventListener("popstate", scheduleBootstrap);
		window.addEventListener("hashchange", scheduleBootstrap);
	}
	createObservableD3RouteWatcher();
	startWidgetVAOnObservableD3Pages().catch(recordObservableD3BootstrapError);
	//#endregion
})();
