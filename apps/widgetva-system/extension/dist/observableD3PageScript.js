var WidgetVAObservableD3PageScript = (function(exports) {
	Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
	//#region ../../widgetva-kit/src/integrations/officialPages/observableD3Pages.js
	function tryParseUrl(value) {
		try {
			return new URL(value);
		} catch {
			return null;
		}
	}
	function normalizeRoot$1(root) {
		if (!root || typeof root !== "object") throw new Error("A browser-like root object is required.");
		return root;
	}
	function readDocument$1(root) {
		return root?.document || null;
	}
	function readIframes(root) {
		const doc = readDocument$1(root);
		if (!doc?.querySelectorAll) return [];
		return [...doc.querySelectorAll("iframe")];
	}
	function readFrameRect(frame) {
		try {
			if (typeof frame?.getBoundingClientRect === "function") {
				const rect = frame.getBoundingClientRect();
				return {
					width: Number(rect?.width) || 0,
					height: Number(rect?.height) || 0
				};
			}
		} catch {}
		return {
			width: 0,
			height: 0
		};
	}
	function computeFrameArea(frame) {
		const rect = readFrameRect(frame);
		return rect.width * rect.height;
	}
	function isObservableD3NotebookPage(pageUrl) {
		const parsedUrl = tryParseUrl(pageUrl);
		if (!parsedUrl) return false;
		const segments = parsedUrl.pathname.split("/").filter(Boolean);
		return parsedUrl.hostname === "observablehq.com" && segments.length >= 2 && segments[0].startsWith("@");
	}
	function isObservableWorkerFrameUrl(frameUrl) {
		const parsedUrl = tryParseUrl(frameUrl);
		if (!parsedUrl) return false;
		return parsedUrl.hostname.endsWith(".observableusercontent.com") && /\/worker-[^/]+\.html$/.test(parsedUrl.pathname);
	}
	function parseObservableNotebookIdentity(pageUrl) {
		const parsedUrl = tryParseUrl(pageUrl);
		if (!parsedUrl || parsedUrl.hostname !== "observablehq.com") return null;
		const segments = parsedUrl.pathname.split("/").filter(Boolean);
		if (segments.length < 2 || !segments[0].startsWith("@")) return null;
		return {
			owner: segments[0],
			slug: segments[1],
			version: segments[2] || null,
			path: parsedUrl.pathname,
			url: parsedUrl.href
		};
	}
	function findObservableWorkerFrame(root = globalThis.window) {
		const iframes = readIframes(normalizeRoot$1(root));
		const candidates = [];
		for (const iframe of iframes) if (isObservableWorkerFrameUrl(iframe?.getAttribute?.("src") || iframe?.src || null)) candidates.push(iframe);
		if (candidates.length === 0) return null;
		return candidates.reduce((best, frame) => {
			if (!best) return frame;
			return computeFrameArea(frame) > computeFrameArea(best) ? frame : best;
		}, null);
	}
	async function waitForObservableWorkerFrame({ root = globalThis.window, timeoutMs = 5e3, pollMs = 25 } = {}) {
		const normalizedRoot = normalizeRoot$1(root);
		const startedAt = Date.now();
		while (Date.now() - startedAt <= timeoutMs) {
			const frame = findObservableWorkerFrame(normalizedRoot);
			if (frame) return frame;
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for an Observable worker iframe after ${timeoutMs}ms.`);
	}
	function describeObservableD3PageShape(root = globalThis.window) {
		const normalizedRoot = normalizeRoot$1(root);
		const pageUrl = normalizedRoot?.location?.href || null;
		const notebook = parseObservableNotebookIdentity(pageUrl);
		const iframes = readIframes(normalizedRoot);
		const workerFrame = findObservableWorkerFrame(normalizedRoot);
		const bodyText = normalizedRoot?.document?.body?.innerText || "";
		return {
			provider: "d3",
			source: "observablehq",
			pageUrl,
			notebook,
			iframeCount: iframes.length,
			workerFrame: workerFrame ? { src: workerFrame.getAttribute?.("src") || workerFrame.src || null } : null,
			bodyTextPreview: bodyText.slice(0, 400),
			bodyTextHint: bodyText.slice(0, 4e3)
		};
	}
	function normalizeObservableFieldName(value) {
		if (typeof value !== "string") return null;
		const trimmed = value.trim();
		return trimmed.length > 0 ? trimmed : null;
	}
	function readObservableAccessorField(code = "", channel = "x") {
		if (typeof code !== "string" || code.length === 0) return null;
		const patterns = [
			new RegExp(`${channel}\\s*:\\s*d\\s*=>\\s*d\\.([A-Za-z_][A-Za-z0-9_]*)`),
			new RegExp(`${channel}\\s*:\\s*d\\s*=>\\s*d\\[(?:\"|')([^\"']+)(?:\"|')\\]`),
			new RegExp(`${channel}\\s*:\\s*(?:\"|')([^\"']+)(?:\"|')`)
		];
		for (const pattern of patterns) {
			const match = code.match(pattern);
			if (match?.[1]) return normalizeObservableFieldName(match[1]);
		}
		return null;
	}
	function readQuotedFieldList(text = "") {
		if (typeof text !== "string" || text.length === 0) return [];
		for (const pattern of [/columns\s*=\s*\[([^\]]+)\]/i, /domain\s*\(\s*\[([^\]]+)\]\s*\)/i]) {
			const match = text.match(pattern);
			if (!match?.[1]) continue;
			const fields = [...match[1].matchAll(/["']([^"']+)["']/g)].map((entry) => normalizeObservableFieldName(entry?.[1])).filter(Boolean);
			if (fields.length >= 2) return fields;
		}
		return [];
	}
	function inferObservableD3ScatterSemanticHints(pageShape = {}) {
		const text = typeof pageShape?.bodyTextHint === "string" && pageShape.bodyTextHint.length > 0 ? pageShape.bodyTextHint : typeof pageShape?.bodyTextPreview === "string" ? pageShape.bodyTextPreview : "";
		return {
			xField: readObservableAccessorField(text, "x"),
			yField: readObservableAccessorField(text, "y"),
			matrixFields: readQuotedFieldList(text)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/shared/providerStatePayloads.js
	function clone$54(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeSelections$1(state) {
		return Object.values(state?.selections || {}).filter(Boolean);
	}
	function resolveRepresentativeSelection(selections) {
		const normalizedSelections = Array.isArray(selections) ? selections.filter(Boolean) : [];
		if (normalizedSelections.length === 1) return normalizedSelections[0] || null;
		const pointSelections = normalizedSelections.filter((selection) => selection?.kind === "point");
		if (pointSelections.length === 1) return pointSelections[0] || null;
		return normalizedSelections[0] || null;
	}
	function resolvePrimaryIntervalSelection(selections) {
		return (Array.isArray(selections) ? selections : []).find((selection) => selection?.kind === "interval") || null;
	}
	function resolveFocusPayload(state) {
		const view = state?.view && typeof state.view === "object" && !Array.isArray(state.view) ? state.view : null;
		if (!view) return null;
		const focusPayload = {
			...view.focusedCarId != null ? { focusedCarId: view.focusedCarId } : {},
			...view.focusedSeries != null ? { focusedSeries: clone$54(view.focusedSeries) } : {},
			...view.focusedNode != null ? { focusedNode: view.focusedNode } : {},
			...view.focusedFlow != null ? { focusedFlow: clone$54(view.focusedFlow) } : {}
		};
		return Object.keys(focusPayload).length > 0 ? focusPayload : null;
	}
	function resolveHighlightStatePayload(state) {
		const highlight = state?.view?.highlight;
		if (!highlight || typeof highlight !== "object" || Array.isArray(highlight)) return null;
		return clone$54(highlight);
	}
	function resolveAggregateStatePayload(state) {
		const aggregate = state?.view?.aggregate;
		if (!aggregate || typeof aggregate !== "object" || Array.isArray(aggregate)) return null;
		return clone$54(aggregate);
	}
	function resolveAddRemoveStatePayload(state) {
		const addRemove = state?.view?.addRemove;
		if (!addRemove || typeof addRemove !== "object" || Array.isArray(addRemove)) return null;
		return clone$54(addRemove);
	}
	function resolveDrillDownStatePayload(state) {
		const drillDown = state?.view?.drillDown;
		if (!drillDown || typeof drillDown !== "object" || Array.isArray(drillDown)) return null;
		return clone$54(drillDown);
	}
	function resolveNavigateStatePayload(state) {
		const navigate = state?.view?.navigate;
		if (!navigate || typeof navigate !== "object" || Array.isArray(navigate)) return null;
		return clone$54(navigate);
	}
	function resolveReencodeStatePayload(state) {
		const reencode = state?.view?.reencode;
		if (!reencode || typeof reencode !== "object" || Array.isArray(reencode)) return null;
		return clone$54(reencode);
	}
	function resolveSortPayload(state) {
		const sort = state?.view?.sort;
		if (!sort || typeof sort !== "object" || Array.isArray(sort)) return null;
		const payload = {
			...typeof sort.channel === "string" ? { channel: sort.channel } : {},
			...typeof sort.field === "string" ? { field: sort.field } : {},
			...typeof sort.mode === "string" ? { mode: sort.mode } : {},
			...typeof sort.order === "string" ? { order: sort.order } : {},
			...typeof sort.aggregate === "string" ? { aggregate: sort.aggregate } : {},
			...Array.isArray(sort.values) ? { values: clone$54(sort.values) } : {}
		};
		return Object.keys(payload).length > 0 ? payload : null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/d3/d3FamilyBehavior.js
	function uniqueValues$2(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => value != null))];
	}
	function buildHumanActionCall({ name, actionTargetRef, params }) {
		return {
			callId: `human_${Date.now()}`,
			name,
			actor: "human",
			targetRef: actionTargetRef || void 0,
			params: {
				targetRef: actionTargetRef || void 0,
				...params
			}
		};
	}
	function resolveCategoryField$1({ spec, interactionConfig, selection = null }) {
		if (typeof selection?.field === "string" && selection.field.length > 0) return selection.field;
		const enc = spec?.encoding || {};
		const preferredChannel = interactionConfig?.categoryFieldChannel;
		if (preferredChannel && enc?.[preferredChannel]?.field) return enc[preferredChannel].field;
		if (typeof interactionConfig?.categoryField === "string" && interactionConfig.categoryField.length > 0) return interactionConfig.categoryField;
		return enc.color?.field || enc.shape?.field || enc.detail?.field || enc.key?.field || enc.x?.field || enc.y?.field || null;
	}
	function resolveCellFields$1({ spec, interactionConfig, selection = null }) {
		return {
			xField: selection?.xField || interactionConfig?.xField || spec?.encoding?.x?.field || null,
			yField: selection?.yField || interactionConfig?.yField || spec?.encoding?.y?.field || null
		};
	}
	function writeSurfaceState(surface, state, selection) {
		if (!surface) return;
		surface.dataset.widgetvaSelectedCount = String(state?.data?.selectedCount ?? 0);
		surface.dataset.widgetvaVisibleCount = String(state?.data?.visibleCount ?? 0);
		surface.dataset.widgetvaSelectionSummary = selection?.summary || "";
	}
	function applyD3HostState({ view, surface, state }) {
		if (!state) return;
		const selections = normalizeSelections$1(state);
		const selection = resolveRepresentativeSelection(selections);
		const intervalSelection = resolvePrimaryIntervalSelection(selections);
		const xDomain = state?.view?.xDomain || intervalSelection?.domain?.xDomain || null;
		const yDomain = state?.view?.yDomain || intervalSelection?.domain?.yDomain || null;
		const aggregateState = resolveAggregateStatePayload(state);
		const addRemoveState = resolveAddRemoveStatePayload(state);
		const drillDownState = resolveDrillDownStatePayload(state);
		const highlightState = resolveHighlightStatePayload(state);
		const focusPayload = resolveFocusPayload(state);
		const navigateState = resolveNavigateStatePayload(state);
		const reencodeState = resolveReencodeStatePayload(state);
		const sortPayload = resolveSortPayload(state);
		const highlightedKeys = Array.isArray(state?.feedback?.highlightedKeys) ? state.feedback.highlightedKeys : [];
		writeSurfaceState(surface, state, selection);
		if (typeof view?.setBrush === "function") view.setBrush(intervalSelection || null);
		if (typeof view?.setViewport === "function") view.setViewport({
			...Array.isArray(xDomain) ? { xDomain: clone$54(xDomain) } : {},
			...Array.isArray(yDomain) ? { yDomain: clone$54(yDomain) } : {}
		});
		else if ((Array.isArray(xDomain) || Array.isArray(yDomain)) && typeof view?.setDomain === "function") view.setDomain(Array.isArray(xDomain) ? xDomain : null, Array.isArray(yDomain) ? yDomain : null);
		if (typeof view?.setSelection === "function") view.setSelection(selection || null);
		if (typeof view?.setHighlights === "function") view.setHighlights([...highlightedKeys]);
		if (highlightState && typeof view?.setHighlightState === "function") view.setHighlightState(highlightState);
		if (aggregateState && typeof view?.setAggregateState === "function") view.setAggregateState(aggregateState);
		if (addRemoveState && typeof view?.setAddRemoveState === "function") view.setAddRemoveState(addRemoveState);
		if (drillDownState && typeof view?.setDrillDownState === "function") view.setDrillDownState(drillDownState);
		if (focusPayload && typeof view?.setFocus === "function") view.setFocus(focusPayload);
		if (navigateState && typeof view?.setNavigateState === "function") view.setNavigateState(navigateState);
		if (reencodeState && typeof view?.setReencodeState === "function") view.setReencodeState(reencodeState);
		if (sortPayload && typeof view?.setSort === "function") view.setSort(sortPayload);
	}
	function bindD3FamilyInteractions({ view, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
		if (!view || !interactionConfig || interactionConfig.mode === "none") return () => {};
		if (interactionConfig.mode === "brush2d" && typeof view.onBrush === "function") {
			const cleanup = view.onBrush((selection) => {
				const normalizedSelection = selection || null;
				const [xField, yField] = Array.isArray(normalizedSelection?.fields) ? normalizedSelection.fields : [];
				const xRange = xField ? normalizedSelection?.value?.[xField] : null;
				const yRange = yField ? normalizedSelection?.value?.[yField] : null;
				if (typeof onActionCall === "function" && xField && yField && Array.isArray(xRange) && Array.isArray(yRange)) {
					onActionCall(buildHumanActionCall({
						name: interactionConfig.actionName || "scatter.brushRegion",
						actionTargetRef,
						params: {
							xField,
							yField,
							xRange,
							yRange
						}
					}));
					return;
				}
				if (typeof onSelectionChange === "function") onSelectionChange(normalizedSelection ? {
					...normalizedSelection,
					source_widget_id: selectionSourceWidgetId || void 0
				} : null);
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		if (interactionConfig.mode === "categoryClick" && typeof view.onCategoryClick === "function") {
			const cleanup = view.onCategoryClick((selection) => {
				const field = resolveCategoryField$1({
					spec,
					interactionConfig,
					selection
				});
				const values = uniqueValues$2(selection?.values || [selection?.value]).filter((value) => typeof value === "string" || typeof value === "number");
				if (!field || values.length === 0 || typeof onActionCall !== "function") return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "widget.selectCategory",
					actionTargetRef,
					params: {
						field,
						values
					}
				}));
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		if (interactionConfig.mode === "cellClick" && typeof view.onCellClick === "function") {
			const cleanup = view.onCellClick((selection) => {
				const { xField, yField } = resolveCellFields$1({
					spec,
					interactionConfig,
					selection
				});
				const xValue = selection?.xValue;
				const yValue = selection?.yValue;
				if (!xField || !yField || xValue == null || yValue == null || typeof onActionCall !== "function") return;
				onActionCall(buildHumanActionCall({
					name: interactionConfig.actionName || "heatmap.filterCells",
					actionTargetRef,
					params: {
						xField,
						yField,
						xValue,
						yValue
					}
				}));
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		if (interactionConfig.mode === "multiBrush" && typeof view.onMultiBrush === "function") {
			const cleanup = view.onMultiBrush((rules) => {
				const normalizedRules = Array.isArray(rules) ? rules : [];
				if (normalizedRules.length === 0) return;
				if (typeof onActionCall === "function") {
					onActionCall(buildHumanActionCall({
						name: interactionConfig.actionName || "parallelCoordinates.brushAxes",
						actionTargetRef,
						params: { rules: clone$54(normalizedRules) }
					}));
					return;
				}
				if (typeof onSelectionChange === "function") onSelectionChange({
					selection_id: `sel_${Date.now()}`,
					source_widget_id: selectionSourceWidgetId || void 0,
					selection_type: "interval",
					predicates: normalizedRules.map((rule) => ({
						field: rule.field,
						op: "between",
						value: rule.range
					}))
				});
			});
			return typeof cleanup === "function" ? cleanup : () => {};
		}
		return () => {};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/d3/D3WidgetAdapter.js
	function clone$53(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readCachedState(view) {
		return view?.__widgetvaLastAppliedState && typeof view.__widgetvaLastAppliedState === "object" ? view.__widgetvaLastAppliedState : null;
	}
	function resolveCachedSelection(view) {
		const selections = Object.values(readCachedState(view)?.selections || {}).filter(Boolean);
		if (selections.length === 1) return clone$53(selections[0]);
		return clone$53(selections.find((selection) => selection?.kind === "interval") || selections[0] || null);
	}
	function normalizeViewportCandidate(value) {
		if (!value || typeof value !== "object" || Array.isArray(value)) return null;
		const viewport = {
			...Array.isArray(value.xDomain) ? { xDomain: clone$53(value.xDomain) } : {},
			...Array.isArray(value.yDomain) ? { yDomain: clone$53(value.yDomain) } : {},
			...value.zoom && typeof value.zoom === "object" ? { zoom: clone$53(value.zoom) } : {}
		};
		return Object.keys(viewport).length > 0 ? viewport : null;
	}
	function createD3WidgetAdapter(definition = {}) {
		return {
			provider: "d3",
			providerCapabilities: {
				supportedWidgetKinds: definition.kind ? [definition.kind] : [],
				renderStrategy: "providerView",
				stateApplyStrategy: "imperativeRender",
				interactionBindingStrategy: "providerEvents",
				supportsRendererMount: true,
				supportsRendererUpdate: true,
				supportsRendererDispose: true,
				supportsSignalPatching: false,
				supportsOptionMerging: false,
				supportsImperativeRender: true,
				supportsPointSelection: true,
				supportsIntervalSelection: true,
				supportsZoomPan: true,
				supportsSelectionReadback: true,
				supportsViewportReadback: true,
				supportsHighlightProjection: true,
				supportsInteractionEvents: true
			},
			mount({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			update({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			dispose() {},
			bindHumanInteractions(args = {}) {
				return bindD3FamilyInteractions(args);
			},
			async applyState(args) {
				const { view, state, surface } = args || {};
				if (view && state && typeof state === "object" && !Array.isArray(state)) view.__widgetvaLastAppliedState = clone$53(state);
				applyD3HostState({
					view,
					surface,
					state
				});
				return definition.renderFromState?.(args);
			},
			readSelection({ view } = {}) {
				if (typeof view?.getSelection === "function") return clone$53(view.getSelection() || null);
				if (typeof view?.getBrush === "function") return clone$53(view.getBrush() || null);
				return resolveCachedSelection(view);
			},
			readViewport({ view } = {}) {
				if (typeof view?.getViewport === "function") return clone$53(view.getViewport() || null);
				if (typeof view?.getDomain === "function") return normalizeViewportCandidate(view.getDomain());
				return normalizeViewportCandidate(readCachedState(view)?.view);
			},
			...definition
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/schemas/actors.schema.js
	var RUNTIME_ACTORS = [
		"agent",
		"human",
		"system"
	];
	var RUNTIME_ACTOR_SCHEMA = {
		type: "string",
		enum: RUNTIME_ACTORS
	};
	//#endregion
	//#region ../../widgetva-kit/src/schemas/query-scope.schema.js
	var QUERY_SCOPE_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			dataRef: { type: "string" },
			selectionRef: { type: "string" },
			focusRef: { type: "string" },
			viewportRef: { type: "string" }
		}
	};
	var ACTION_CALL_SCHEMA = {
		type: "object",
		additionalProperties: false,
		required: ["name", "params"],
		properties: {
			callId: { type: "string" },
			name: { type: "string" },
			actor: RUNTIME_ACTOR_SCHEMA,
			reason: { type: "string" },
			target: {
				type: "object",
				additionalProperties: false,
				required: ["widgetRef"],
				properties: { widgetRef: { type: "string" } }
			},
			queryScope: QUERY_SCOPE_SCHEMA,
			params: { type: "object" }
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/schemas/data-handles.schema.js
	var DATA_QUERY_TARGET_SCHEMA = {
		type: "object",
		additionalProperties: false,
		required: ["widgetRef"],
		properties: { widgetRef: { type: "string" } }
	};
	var DATA_QUERY_KINDS = [
		"schema",
		"sampleRows",
		"filter",
		"aggregate",
		"groupBy",
		"sql",
		"summary",
		"computeCorrelation",
		"findExtremes",
		"findOutliers",
		"compareGroups"
	];
	var DATA_QUERY_PREDICATE_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			field: {
				type: "string",
				minLength: 1
			},
			op: {
				type: "string",
				minLength: 1
			},
			value: {}
		},
		required: [
			"field",
			"op",
			"value"
		]
	};
	var DATA_QUERY_MEASURE_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			op: { type: "string" },
			field: { type: "string" },
			as: { type: "string" }
		},
		required: ["op"]
	};
	var DATA_QUERY_SQL_SPEC_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			sql: {
				type: "string",
				minLength: 1
			},
			text: {
				type: "string",
				minLength: 1
			}
		},
		oneOf: [{ required: ["sql"] }, { required: ["text"] }]
	};
	var DATA_QUERY_SUMMARY_SPEC_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			fields: {
				type: "array",
				items: { type: "string" }
			},
			metrics: {
				type: "array",
				items: { type: "string" }
			},
			groupBy: {
				type: "array",
				items: { type: "string" }
			},
			measures: {
				type: "array",
				items: DATA_QUERY_MEASURE_SCHEMA
			},
			sortBy: {
				type: "object",
				properties: {
					field: { type: "string" },
					order: {
						type: "string",
						enum: ["ascending", "descending"]
					}
				}
			},
			limit: {
				type: "integer",
				minimum: 1,
				maximum: 500
			}
		}
	};
	var DATA_QUERY_CORRELATION_SPEC_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			xField: {
				type: "string",
				minLength: 1
			},
			yField: {
				type: "string",
				minLength: 1
			}
		},
		required: ["xField", "yField"]
	};
	var DATA_QUERY_EXTREMES_SPEC_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			field: {
				type: "string",
				minLength: 1
			},
			direction: {
				type: "string",
				enum: [
					"min",
					"max",
					"both"
				]
			},
			limit: {
				type: "integer",
				minimum: 1,
				maximum: 200
			}
		},
		required: ["field"]
	};
	var DATA_QUERY_OUTLIERS_SPEC_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			field: {
				type: "string",
				minLength: 1
			},
			method: {
				type: "string",
				enum: ["iqr", "zscore"]
			},
			limit: {
				type: "integer",
				minimum: 1,
				maximum: 200
			}
		},
		required: ["field"]
	};
	var DATA_QUERY_COMPARE_GROUPS_SPEC_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			groupField: {
				type: "string",
				minLength: 1
			},
			valueField: {
				type: "string",
				minLength: 1
			},
			groups: {
				type: "array",
				minItems: 2,
				items: {}
			},
			leftGroup: {},
			rightGroup: {}
		},
		required: ["groupField", "valueField"]
	};
	var DATA_QUERY_AGGREGATE_SPEC_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			groupBy: {
				type: "array",
				items: { type: "string" }
			},
			measures: {
				type: "array",
				items: DATA_QUERY_MEASURE_SCHEMA
			},
			metrics: {
				type: "array",
				items: DATA_QUERY_MEASURE_SCHEMA
			}
		}
	};
	var DATA_QUERY_CALL_QUERY_SCHEMA = {
		type: "object",
		additionalProperties: false,
		required: ["kind"],
		properties: {
			kind: {
				type: "string",
				enum: DATA_QUERY_KINDS
			},
			spec: { type: "object" }
		}
	};
	var DATA_RECORD_SCHEMA = { type: "object" };
	var DATA_SUMMARY_ROW_SCHEMA = { type: "object" };
	var DATA_GROUP_COMPARISON_SCHEMA = { type: ["object", "null"] };
	var DATA_SCHEMA_RESULT_SCHEMA = {
		type: "object",
		properties: { fields: {
			type: "array",
			items: {
				type: "object",
				required: ["name", "type"],
				properties: {
					name: { type: "string" },
					type: { type: "string" },
					nullable: { type: "boolean" },
					description: { type: "string" }
				}
			}
		} }
	};
	var DATA_ROWS_RESULT_SCHEMA = {
		type: "array",
		items: DATA_RECORD_SCHEMA
	};
	var DATA_SUMMARY_TABLE_SCHEMA = {
		type: "object",
		properties: { rows: {
			type: "array",
			items: DATA_SUMMARY_ROW_SCHEMA
		} }
	};
	var DATA_QUERY_RESULT_SCHEMAS = {
		schema: DATA_SCHEMA_RESULT_SCHEMA,
		sampleRows: DATA_ROWS_RESULT_SCHEMA,
		filter: {
			type: "object",
			properties: {
				rows: DATA_ROWS_RESULT_SCHEMA,
				rowCount: { type: "integer" },
				predicates: {
					type: "array",
					items: DATA_QUERY_PREDICATE_SCHEMA
				}
			}
		},
		aggregate: DATA_SUMMARY_TABLE_SCHEMA,
		groupBy: DATA_SUMMARY_TABLE_SCHEMA,
		sql: DATA_ROWS_RESULT_SCHEMA,
		summary: DATA_SUMMARY_TABLE_SCHEMA,
		computeCorrelation: {
			type: "object",
			properties: {
				xField: { type: "string" },
				yField: { type: "string" },
				correlation: { type: ["number", "null"] },
				sampleSize: { type: "integer" }
			}
		},
		findExtremes: DATA_SUMMARY_TABLE_SCHEMA,
		findOutliers: DATA_SUMMARY_TABLE_SCHEMA,
		compareGroups: {
			type: "object",
			properties: {
				groupField: { type: ["string", "null"] },
				valueField: { type: ["string", "null"] },
				groups: {
					type: "array",
					items: DATA_SUMMARY_ROW_SCHEMA
				},
				comparison: DATA_GROUP_COMPARISON_SCHEMA
			}
		}
	};
	var DATA_QUERY_SCHEMAS = {
		schema: {
			type: "object",
			additionalProperties: false,
			properties: { queryScope: QUERY_SCOPE_SCHEMA }
		},
		sampleRows: {
			type: "object",
			additionalProperties: false,
			properties: {
				limit: {
					type: "integer",
					minimum: 1,
					maximum: 500
				},
				queryScope: QUERY_SCOPE_SCHEMA
			}
		},
		filter: {
			type: "object",
			additionalProperties: false,
			properties: {
				predicates: {
					type: "array",
					minItems: 1,
					items: DATA_QUERY_PREDICATE_SCHEMA
				},
				queryScope: QUERY_SCOPE_SCHEMA
			},
			required: ["predicates"]
		},
		aggregate: {
			...DATA_QUERY_AGGREGATE_SPEC_SCHEMA,
			properties: {
				...DATA_QUERY_AGGREGATE_SPEC_SCHEMA.properties,
				queryScope: QUERY_SCOPE_SCHEMA
			}
		},
		groupBy: {
			...DATA_QUERY_AGGREGATE_SPEC_SCHEMA,
			properties: {
				...DATA_QUERY_AGGREGATE_SPEC_SCHEMA.properties,
				queryScope: QUERY_SCOPE_SCHEMA
			}
		},
		sql: {
			...DATA_QUERY_SQL_SPEC_SCHEMA,
			properties: {
				...DATA_QUERY_SQL_SPEC_SCHEMA.properties,
				queryScope: QUERY_SCOPE_SCHEMA
			}
		},
		summary: {
			...DATA_QUERY_SUMMARY_SPEC_SCHEMA,
			properties: {
				...DATA_QUERY_SUMMARY_SPEC_SCHEMA.properties,
				queryScope: QUERY_SCOPE_SCHEMA
			}
		},
		computeCorrelation: {
			...DATA_QUERY_CORRELATION_SPEC_SCHEMA,
			properties: {
				...DATA_QUERY_CORRELATION_SPEC_SCHEMA.properties,
				queryScope: QUERY_SCOPE_SCHEMA
			}
		},
		findExtremes: {
			...DATA_QUERY_EXTREMES_SPEC_SCHEMA,
			properties: {
				...DATA_QUERY_EXTREMES_SPEC_SCHEMA.properties,
				queryScope: QUERY_SCOPE_SCHEMA
			}
		},
		findOutliers: {
			...DATA_QUERY_OUTLIERS_SPEC_SCHEMA,
			properties: {
				...DATA_QUERY_OUTLIERS_SPEC_SCHEMA.properties,
				queryScope: QUERY_SCOPE_SCHEMA
			}
		},
		compareGroups: {
			...DATA_QUERY_COMPARE_GROUPS_SPEC_SCHEMA,
			properties: {
				...DATA_QUERY_COMPARE_GROUPS_SPEC_SCHEMA.properties,
				queryScope: QUERY_SCOPE_SCHEMA
			}
		}
	};
	({ ...DATA_SCHEMA_RESULT_SCHEMA });
	var DATA_QUERY_CALL_SCHEMA = {
		type: "object",
		additionalProperties: false,
		required: ["query"],
		properties: {
			callId: { type: "string" },
			actor: RUNTIME_ACTOR_SCHEMA,
			target: DATA_QUERY_TARGET_SCHEMA,
			dataRef: { type: "string" },
			query: DATA_QUERY_CALL_QUERY_SCHEMA
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/schemas/perception.schema.js
	var PERCEPTION_TARGET_SCHEMA = {
		type: "object",
		additionalProperties: false,
		required: ["widgetRef"],
		properties: { widgetRef: { type: "string" } }
	};
	var ENCODING_TYPES = [
		"quantitative",
		"nominal",
		"ordinal",
		"temporal",
		"geo"
	];
	var TRANSFORM_KINDS = [
		"filter",
		"sort",
		"aggregate",
		"derive",
		"sample",
		"syncDomain",
		"highlight"
	];
	var SELECTION_KINDS = [
		"interval",
		"point",
		"category",
		"cell"
	];
	var SELECTION_PREDICATE_OPERATIONS = [
		"equals",
		"in",
		"between"
	];
	var FIELD_ENCODING_SCHEMA = {
		type: "object",
		required: ["field", "type"],
		properties: {
			field: { type: "string" },
			type: {
				type: "string",
				enum: ENCODING_TYPES
			},
			aggregate: { type: ["string", "null"] },
			bin: { type: ["boolean", "null"] },
			scale: { anyOf: [{
				type: "object",
				properties: {
					domain: {},
					range: {},
					clamp: { type: "boolean" },
					nice: { type: "boolean" },
					zero: { type: "boolean" },
					type: { type: "string" }
				}
			}, { type: "null" }] }
		}
	};
	var PERCEPTION_SELECTION_DOMAIN_AXIS_SCHEMA = {
		type: "array",
		minItems: 2,
		maxItems: 2,
		items: { anyOf: [
			{ type: "number" },
			{ type: "string" },
			{ type: "null" }
		] }
	};
	var PERCEPTION_INSPECT_VIEW_CONFIG_RESULT_SCHEMA = {
		type: "object",
		properties: {
			ref: { type: ["string", "null"] },
			kind: { type: ["string", "null"] },
			encodings: {
				type: "object",
				additionalProperties: FIELD_ENCODING_SCHEMA
			},
			transforms: {
				type: "array",
				items: {
					type: "object",
					required: ["kind", "spec"],
					properties: {
						kind: {
							type: "string",
							enum: TRANSFORM_KINDS
						},
						source: { type: ["string", "null"] },
						sourceWidgetId: { type: ["string", "null"] },
						sourceSelectionRef: { type: ["string", "null"] },
						linkId: { type: ["string", "null"] },
						spec: { type: "object" }
					}
				}
			},
			view: {
				type: "object",
				properties: {
					xDomain: { anyOf: [
						PERCEPTION_SELECTION_DOMAIN_AXIS_SCHEMA,
						{
							type: "array",
							items: { anyOf: [
								{ type: "number" },
								{ type: "string" },
								{ type: "null" }
							] }
						},
						{ type: "null" }
					] },
					yDomain: { anyOf: [
						PERCEPTION_SELECTION_DOMAIN_AXIS_SCHEMA,
						{
							type: "array",
							items: { anyOf: [
								{ type: "number" },
								{ type: "string" },
								{ type: "null" }
							] }
						},
						{ type: "null" }
					] },
					zoom: { anyOf: [{
						type: "object",
						properties: {
							level: { type: "number" },
							center: {
								type: "array",
								minItems: 2,
								maxItems: 2,
								items: { type: "number" }
							}
						}
					}, { type: "null" }] },
					sort: { anyOf: [{
						type: "object",
						properties: {
							field: { type: "string" },
							order: {
								type: "string",
								enum: ["ascending", "descending"]
							}
						}
					}, { type: "null" }] }
				}
			},
			selections: {
				type: "object",
				additionalProperties: {
					type: "object",
					required: ["kind"],
					properties: {
						selectionRef: { type: ["string", "null"] },
						selectionId: { type: ["string", "null"] },
						kind: {
							type: "string",
							enum: SELECTION_KINDS
						},
						sourceWidgetRef: { type: ["string", "null"] },
						sourceWidgetId: { type: ["string", "null"] },
						scope: { type: "string" },
						selectionDataRef: { type: ["string", "null"] },
						fields: {
							type: "array",
							items: { type: "string" }
						},
						value: { type: "object" },
						domain: { anyOf: [{
							type: "object",
							properties: {
								xDomain: PERCEPTION_SELECTION_DOMAIN_AXIS_SCHEMA,
								yDomain: PERCEPTION_SELECTION_DOMAIN_AXIS_SCHEMA
							}
						}, { type: "null" }] },
						predicates: {
							type: "array",
							items: {
								type: "object",
								required: ["field", "op"],
								properties: {
									field: { type: "string" },
									op: {
										type: "string",
										enum: SELECTION_PREDICATE_OPERATIONS
									},
									value: {}
								}
							}
						},
						summary: { type: "string" },
						keyField: { type: "string" },
						keys: {
							type: "array",
							items: { type: ["string", "number"] }
						},
						field: { type: "string" },
						values: {
							type: "array",
							items: { type: ["string", "number"] }
						}
					}
				}
			},
			feedback: { anyOf: [{
				type: "object",
				properties: {
					hoveredItem: { type: ["object", "null"] },
					highlightedKeys: {
						type: "array",
						items: { type: ["string", "number"] }
					},
					tooltip: { type: ["object", "null"] },
					inboundLinkIds: {
						type: "array",
						items: { type: "string" }
					},
					highlightLinkIds: {
						type: "array",
						items: { type: "string" }
					},
					linkedSourceRefs: {
						type: "array",
						items: { type: "string" }
					},
					sharedSelectionSourceWidgetId: { type: ["string", "null"] }
				}
			}, { type: "null" }] }
		}
	};
	var PERCEPTION_INSPECT_VISIBLE_ROWS_RESULT_SCHEMA = {
		type: "object",
		properties: {
			ref: { type: ["string", "null"] },
			dataRef: { type: ["string", "null"] },
			visibleCount: { type: "integer" },
			rows: DATA_QUERY_RESULT_SCHEMAS.sampleRows
		}
	};
	var PERCEPTION_SUMMARY_GROUPS_SCHEMA = {
		type: "array",
		items: DATA_QUERY_RESULT_SCHEMAS.summary.properties.rows.items
	};
	var PERCEPTION_SUMMARIZE_SELECTION_RESULT_SCHEMA = {
		type: "object",
		properties: {
			hasSelection: { type: "boolean" },
			selectionCount: { type: "integer" },
			selectionRefs: {
				type: "array",
				items: { type: "string" }
			},
			dataRef: { type: ["string", "null"] },
			selectedCount: { type: "integer" },
			summary: { type: "string" },
			selectionSummaries: {
				type: "array",
				items: { type: "string" }
			},
			groups: PERCEPTION_SUMMARY_GROUPS_SCHEMA,
			aggregates: PERCEPTION_SUMMARY_GROUPS_SCHEMA,
			predicates: {
				type: "array",
				items: { type: "object" }
			},
			selectionPredicates: {
				type: "array",
				items: {
					type: "array",
					items: { type: "object" }
				}
			}
		}
	};
	var PERCEPTION_SUMMARIZE_VISIBLE_RESULT_SCHEMA = {
		type: "object",
		properties: {
			dataRef: { type: ["string", "null"] },
			rowCount: { type: "integer" },
			groups: PERCEPTION_SUMMARY_GROUPS_SCHEMA,
			aggregates: PERCEPTION_SUMMARY_GROUPS_SCHEMA,
			summary: { type: ["string", "null"] }
		}
	};
	var PERCEPTION_VERIFY_ACTION_EFFECT_PARAMS_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			actionName: { type: "string" },
			stateId: { type: "string" },
			refs: {
				type: "array",
				items: { type: "string" }
			}
		}
	};
	var PERCEPTION_RETURN_SCHEMAS = {
		"perception.inspectViewConfig": PERCEPTION_INSPECT_VIEW_CONFIG_RESULT_SCHEMA,
		"perception.inspectVisibleRows": PERCEPTION_INSPECT_VISIBLE_ROWS_RESULT_SCHEMA,
		"perception.summarizeSelection": PERCEPTION_SUMMARIZE_SELECTION_RESULT_SCHEMA,
		"perception.summarizeVisible": PERCEPTION_SUMMARIZE_VISIBLE_RESULT_SCHEMA,
		"perception.verifyActionEffect": {
			type: "object",
			properties: {
				verified: { type: "boolean" },
				matchedStateId: { type: ["string", "null"] },
				matchedActionName: { type: ["string", "null"] },
				affectedRefs: {
					type: "array",
					items: { type: "string" }
				},
				missingRefs: {
					type: "array",
					items: { type: "string" }
				},
				expectedPostconditions: {
					type: "array",
					items: {
						type: "object",
						properties: {
							description: { type: "string" },
							checkHint: { type: "string" },
							failureMessage: { type: "string" }
						}
					}
				},
				verificationHints: {
					type: "array",
					items: { type: "string" }
				},
				traceEvidence: { anyOf: [{ type: "null" }, { type: "object" }] },
				statePatch: { type: ["object", "null"] },
				finalSnapshot: { anyOf: [{ type: "null" }, { type: "object" }] },
				linkPropagation: {
					type: "array",
					items: {
						type: "object",
						properties: {
							ok: { type: "boolean" },
							sourceRef: { type: ["string", "null"] },
							linkCount: { type: "integer" },
							passedCount: { type: "integer" },
							consistencyScore: { type: "number" },
							results: {
								type: "array",
								items: { type: "object" }
							}
						}
					}
				},
				semanticVerification: { anyOf: [{ type: "null" }, {
					type: "object",
					properties: {
						ok: { type: "boolean" },
						kind: { type: "string" },
						summary: { type: "string" },
						expected: {
							type: "object",
							additionalProperties: true
						},
						actual: { anyOf: [{ type: "null" }, {
							type: "object",
							additionalProperties: true
						}] }
					}
				}] }
			}
		},
		"perception.computeCorrelation": {
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				xField: { type: "string" },
				yField: { type: "string" },
				correlation: { type: ["number", "null"] },
				sampleSize: { type: "integer" }
			}
		},
		"perception.findExtremes": {
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				field: { type: ["string", "null"] },
				direction: { type: "string" },
				rows: DATA_QUERY_RESULT_SCHEMAS.sampleRows
			}
		},
		"perception.findOutliers": {
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				field: { type: ["string", "null"] },
				method: { type: "string" },
				rows: DATA_QUERY_RESULT_SCHEMAS.sampleRows
			}
		},
		"perception.compareGroups": {
			type: "object",
			properties: {
				dataRef: { type: ["string", "null"] },
				groupField: { type: ["string", "null"] },
				valueField: { type: ["string", "null"] },
				groups: DATA_QUERY_RESULT_SCHEMAS.compareGroups.properties.groups,
				comparison: DATA_QUERY_RESULT_SCHEMAS.compareGroups.properties.comparison
			}
		}
	};
	function readPerceptionParamsSchema(queryName) {
		if (queryName === "perception.verifyActionEffect") return PERCEPTION_VERIFY_ACTION_EFFECT_PARAMS_SCHEMA;
	}
	function readPerceptionReturnsSchema(queryName) {
		return queryName ? PERCEPTION_RETURN_SCHEMAS[queryName] : void 0;
	}
	var PERCEPTION_QUERY_CALL_SCHEMA = {
		type: "object",
		additionalProperties: false,
		required: ["name"],
		properties: {
			callId: { type: "string" },
			name: { type: "string" },
			actor: RUNTIME_ACTOR_SCHEMA,
			target: PERCEPTION_TARGET_SCHEMA,
			queryScope: QUERY_SCOPE_SCHEMA,
			params: { type: "object" }
		}
	};
	var REF_SCHEMA = {
		type: "string",
		pattern: "^wl:\\/\\/[^/]+\\/workspace\\/[^/]+\\/.+$"
	};
	//#endregion
	//#region ../../widgetva-kit/src/transports/pagePortProtocol.js
	var PAGE_PORT_METHODS = [
		"describePagePort",
		"describeWorkspace",
		"parseRef",
		"readState",
		"readView",
		"readSnapshot",
		"listStateHistory",
		"listBranches",
		"executeAction",
		"executeVerifiedAction",
		"jumpToState",
		"branchFromState",
		"queryPerception",
		"runDataQuery",
		"queryData",
		"readTrace",
		"getInteractionTrace",
		"replay",
		"getTraceGraph",
		"getLatestAgentResponse",
		"listAgentResponses",
		"evaluateLinkPropagation",
		"recordAgentResponse"
	];
	var PAGE_PORT_ALIASES = {
		page_port_describe: "describePagePort",
		workspace_describe: "describeWorkspace",
		ref_parse: "parseRef",
		state_read: "readState",
		view_read: "readView",
		read_snapshot: "readSnapshot",
		state_history_read: "listStateHistory",
		branch_list: "listBranches",
		action_run: "executeAction",
		verified_action_run: "executeVerifiedAction",
		jump_to_state: "jumpToState",
		branch_from_state: "branchFromState",
		perception_query: "queryPerception",
		data_query_run: "runDataQuery",
		data_query: "queryData",
		trace_read: "readTrace",
		interaction_trace_read: "getInteractionTrace",
		workspace_replay: "replay",
		trace_graph_read: "getTraceGraph",
		agent_response_read: "getLatestAgentResponse",
		agent_response_list: "listAgentResponses",
		link_propagation_evaluate: "evaluateLinkPropagation",
		agent_response_record: "recordAgentResponse"
	};
	var PAGE_PORT_ERROR_CODES = {
		pagePort: [{
			code: "METHOD_NOT_INSTALLED",
			description: "The requested page-port method is unavailable on the current page runtime."
		}],
		action: [
			{
				code: "UNKNOWN_OPERATION",
				description: "The requested WidgetVA action name is not registered in the current runtime."
			},
			{
				code: "UNSUPPORTED_TARGET",
				description: "The requested action exists but is not declared for the target widget or ref."
			},
			{
				code: "INVALID_PARAMS",
				description: "The action call failed params-schema validation before execution."
			},
			{
				code: "PRECONDITION_FAILED",
				description: "The action is declared, but the current runtime state does not satisfy its documented preconditions."
			},
			{
				code: "RUNTIME_ERROR",
				description: "The action handler threw during execution."
			}
		],
		perception: [
			{
				code: "UNKNOWN_QUERY",
				description: "The requested perception query name is not registered in the current runtime."
			},
			{
				code: "INVALID_PARAMS",
				description: "The perception query failed params-schema validation before execution."
			},
			{
				code: "RUNTIME_ERROR",
				description: "The perception handler threw during execution."
			}
		],
		dataQuery: [
			{
				code: "UNSUPPORTED_TARGET",
				description: "The requested data-query target ref does not resolve to a materialized widget or data handle."
			},
			{
				code: "UNKNOWN_DATA_REF",
				description: "The requested data ref is not materialized in the current runtime store."
			},
			{
				code: "UNKNOWN_QUERY_KIND",
				description: "The requested data query kind is unsupported by WidgetVA."
			},
			{
				code: "UNSUPPORTED_QUERY_KIND",
				description: "The requested data query kind is valid globally but not exposed by the current data handle."
			},
			{
				code: "INVALID_QUERY_SPEC",
				description: "The data query spec failed schema validation before execution."
			}
		]
	};
	function makePagePortProtocolMethodDescriptor(descriptor) {
		return {
			stability: "stable",
			aliases: [],
			inputSchema: {
				type: "object",
				additionalProperties: true,
				properties: {}
			},
			returns: {
				kind: "result",
				description: ""
			},
			errors: [],
			...descriptor
		};
	}
	var BOOLEAN_SCHEMA = { type: "boolean" };
	var STRING_SCHEMA = { type: "string" };
	var REF_ARRAY_SCHEMA = {
		type: "array",
		items: { type: "string" }
	};
	var STATE_READ_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			refs: REF_ARRAY_SCHEMA,
			deltaSince: STRING_SCHEMA
		}
	};
	var TRACE_READ_SCHEMA = {
		type: "object",
		additionalProperties: false,
		properties: {
			limit: {
				type: "integer",
				minimum: 1,
				maximum: 1e3
			},
			sinceStateId: STRING_SCHEMA,
			actors: {
				type: "array",
				items: RUNTIME_ACTOR_SCHEMA
			}
		}
	};
	var PAGE_PORT_METHOD_DESCRIPTORS = {
		describePagePort: makePagePortProtocolMethodDescriptor({
			description: "Describe the installed WidgetVA page port, including stable methods, aliases, schemas, and error semantics.",
			returns: {
				kind: "pagePortDescription",
				description: "A stable description of the external WidgetVA page API."
			}
		}),
		describeWorkspace: makePagePortProtocolMethodDescriptor({
			aliases: ["workspace_describe"],
			description: "Describe the current workspace, widgets, data handles, links, and externally callable action/perception surfaces.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					includeSchemas: BOOLEAN_SCHEMA,
					includeExamples: BOOLEAN_SCHEMA
				}
			},
			returns: {
				kind: "workspaceDescription",
				description: "A workspace-level description for external callers."
			}
		}),
		parseRef: makePagePortProtocolMethodDescriptor({
			aliases: ["ref_parse"],
			description: "Parse a WidgetVA ref string into structured ref parts.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { ref: REF_SCHEMA },
				required: ["ref"]
			},
			returns: {
				kind: "refParts",
				description: "Structured app/workspace/kind/id components parsed from a WidgetVA ref."
			}
		}),
		readState: makePagePortProtocolMethodDescriptor({
			aliases: ["state_read"],
			description: "Read the current workspace state or a delta restricted to selected refs.",
			inputSchema: STATE_READ_SCHEMA,
			returns: {
				kind: "workspaceState",
				description: "The current workspace state snapshot, optionally scoped by refs and/or delta base."
			}
		}),
		readView: makePagePortProtocolMethodDescriptor({
			aliases: ["view_read"],
			description: "Read the current workspace view/state projection.",
			inputSchema: STATE_READ_SCHEMA,
			returns: {
				kind: "workspaceState",
				description: "The current workspace state snapshot, optionally scoped by refs and/or delta base."
			}
		}),
		readSnapshot: makePagePortProtocolMethodDescriptor({
			aliases: ["read_snapshot"],
			description: "Read a historical workspace snapshot by stateId, optionally scoped to refs and enriched with metadata.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					stateId: STRING_SCHEMA,
					refs: REF_ARRAY_SCHEMA,
					includeMeta: BOOLEAN_SCHEMA
				},
				required: ["stateId"]
			},
			returns: {
				kind: "workspaceSnapshot",
				description: "A historical workspace snapshot."
			}
		}),
		listStateHistory: makePagePortProtocolMethodDescriptor({
			aliases: ["state_history_read"],
			description: "List recent runtime state snapshots in reverse chronological order.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					limit: {
						type: "integer",
						minimum: 1,
						maximum: 500
					},
					sinceStateId: STRING_SCHEMA,
					actors: {
						type: "array",
						items: RUNTIME_ACTOR_SCHEMA
					}
				}
			},
			returns: {
				kind: "stateSnapshotMeta[]",
				description: "Recent state-history entries with branch/transition metadata."
			}
		}),
		listBranches: makePagePortProtocolMethodDescriptor({
			aliases: ["branch_list"],
			description: "List known runtime branches and their lineage metadata.",
			returns: {
				kind: "branchSummary[]",
				description: "Branch records maintained by the runtime trace store."
			}
		}),
		executeAction: makePagePortProtocolMethodDescriptor({
			aliases: ["action_run"],
			description: "Execute a WidgetVA action against a target ref or widget.",
			inputSchema: ACTION_CALL_SCHEMA,
			returns: {
				kind: "actionResult",
				description: "An action execution envelope with state, trace, and verification hints."
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		executeVerifiedAction: makePagePortProtocolMethodDescriptor({
			aliases: ["verified_action_run"],
			description: "Execute an action and immediately return runtime verification evidence.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					call: ACTION_CALL_SCHEMA,
					options: {
						type: "object",
						additionalProperties: false,
						properties: {
							verify: BOOLEAN_SCHEMA,
							includeDeltaSince: BOOLEAN_SCHEMA,
							includeFinalSnapshotRefs: BOOLEAN_SCHEMA
						}
					}
				},
				required: ["call"]
			},
			returns: {
				kind: "verifiedActionResult",
				description: "An action execution envelope augmented with post-action verification evidence."
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		jumpToState: makePagePortProtocolMethodDescriptor({
			aliases: ["jump_to_state"],
			description: "Jump the runtime back to a historical state and replay it into the live workspace.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					callId: STRING_SCHEMA,
					actor: RUNTIME_ACTOR_SCHEMA,
					stateId: STRING_SCHEMA
				},
				required: ["stateId"]
			},
			returns: {
				kind: "actionResult",
				description: "A workspace.jumpToState action result."
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		branchFromState: makePagePortProtocolMethodDescriptor({
			aliases: ["branch_from_state"],
			description: "Create and switch to a new runtime branch seeded from a historical state.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					callId: STRING_SCHEMA,
					actor: RUNTIME_ACTOR_SCHEMA,
					stateId: STRING_SCHEMA,
					branchLabel: STRING_SCHEMA
				},
				required: ["stateId"]
			},
			returns: {
				kind: "actionResult",
				description: "A workspace.branchFromState action result."
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		queryPerception: makePagePortProtocolMethodDescriptor({
			aliases: ["perception_query"],
			description: "Execute a WidgetVA perception query against the current view or a target widget/data ref.",
			inputSchema: PERCEPTION_QUERY_CALL_SCHEMA,
			returns: {
				kind: "perceptionResult",
				description: "A perception query envelope carrying structured evidence."
			},
			errors: PAGE_PORT_ERROR_CODES.perception
		}),
		runDataQuery: makePagePortProtocolMethodDescriptor({
			aliases: ["data_query_run"],
			description: "Execute a WidgetVA data query against a materialized data handle.",
			inputSchema: DATA_QUERY_CALL_SCHEMA,
			returns: {
				kind: "dataQueryResult",
				description: "A data-query envelope with rows, aggregates, or diagnostics depending on query kind."
			},
			errors: PAGE_PORT_ERROR_CODES.dataQuery
		}),
		queryData: makePagePortProtocolMethodDescriptor({
			aliases: ["data_query"],
			description: "Execute a WidgetVA data query against a materialized data handle.",
			inputSchema: DATA_QUERY_CALL_SCHEMA,
			returns: {
				kind: "dataQueryResult",
				description: "A data-query envelope with rows, aggregates, or diagnostics depending on query kind."
			},
			errors: PAGE_PORT_ERROR_CODES.dataQuery
		}),
		readTrace: makePagePortProtocolMethodDescriptor({
			aliases: ["trace_read"],
			description: "Read the unified human+agent interaction trace from the runtime store.",
			inputSchema: TRACE_READ_SCHEMA,
			returns: {
				kind: "interactionTraceRecord[]",
				description: "Recent runtime interaction trace records."
			}
		}),
		getInteractionTrace: makePagePortProtocolMethodDescriptor({
			aliases: ["interaction_trace_read"],
			description: "Read the unified human+agent interaction trace from the runtime store.",
			inputSchema: TRACE_READ_SCHEMA,
			returns: {
				kind: "interactionTraceRecord[]",
				description: "Recent runtime interaction trace records."
			}
		}),
		replay: makePagePortProtocolMethodDescriptor({
			aliases: ["workspace_replay"],
			description: "Replay a historical state into the live workspace.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					callId: STRING_SCHEMA,
					actor: RUNTIME_ACTOR_SCHEMA,
					stateId: STRING_SCHEMA
				},
				required: ["stateId"]
			},
			returns: {
				kind: "actionResult",
				description: "A workspace.jumpToState action result."
			},
			errors: PAGE_PORT_ERROR_CODES.action
		}),
		getTraceGraph: makePagePortProtocolMethodDescriptor({
			aliases: ["trace_graph_read"],
			description: "Read the runtime trace graph with branch and transition relationships.",
			inputSchema: TRACE_READ_SCHEMA,
			returns: {
				kind: "traceGraph",
				description: "A graph-friendly projection of runtime states and transitions."
			}
		}),
		getLatestAgentResponse: makePagePortProtocolMethodDescriptor({
			aliases: ["agent_response_read"],
			description: "Read the latest recorded final response for the current or specified workspace.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { workspaceId: STRING_SCHEMA }
			},
			returns: {
				kind: "agentResponseRecord",
				description: "Latest recorded final response, if available."
			}
		}),
		listAgentResponses: makePagePortProtocolMethodDescriptor({
			aliases: ["agent_response_list"],
			description: "List recent recorded final responses for the current or specified workspace.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {
					limit: {
						type: "integer",
						minimum: 1,
						maximum: 1e3
					},
					workspaceId: STRING_SCHEMA
				}
			},
			returns: {
				kind: "agentResponseRecord[]",
				description: "Recent recorded final responses for the current or specified workspace."
			}
		}),
		evaluateLinkPropagation: makePagePortProtocolMethodDescriptor({
			aliases: ["link_propagation_evaluate"],
			description: "Evaluate current propagation consistency for outgoing links from a source ref.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: { sourceRef: STRING_SCHEMA },
				required: ["sourceRef"]
			},
			returns: {
				kind: "linkPropagationEvaluation",
				description: "Per-link consistency checks for runtime propagation semantics."
			}
		}),
		recordAgentResponse: makePagePortProtocolMethodDescriptor({
			aliases: ["agent_response_record"],
			description: "Record a final response for the current workspace for later history and inspection workflows.",
			inputSchema: {
				type: "object",
				additionalProperties: false,
				required: ["content"],
				properties: {
					responseId: STRING_SCHEMA,
					runId: STRING_SCHEMA,
					sessionId: STRING_SCHEMA,
					actor: RUNTIME_ACTOR_SCHEMA,
					mode: STRING_SCHEMA,
					query: STRING_SCHEMA,
					content: STRING_SCHEMA,
					evidenceRefs: REF_ARRAY_SCHEMA
				}
			},
			returns: {
				kind: "agentResponseRecord",
				description: "The recorded final-response record with runtime lineage metadata."
			}
		})
	};
	//#endregion
	//#region ../../widgetva-kit/src/transports/pagePortBridge.js
	var TRANSPORT_PAGE_PORT_ALIASES = Object.keys(PAGE_PORT_ALIASES);
	function resolvePagePortMethod(port, alias) {
		if (!port || typeof alias !== "string" || alias.length === 0) return null;
		const aliasedMethod = port[alias];
		if (typeof aliasedMethod === "function") return aliasedMethod;
		const stableMethodName = PAGE_PORT_ALIASES[alias];
		if (typeof stableMethodName === "string" && typeof port[stableMethodName] === "function") return port[stableMethodName];
		return null;
	}
	function getInstalledPagePort(root = window) {
		const port = root?.__widgetVA;
		if (!port) throw new Error("WidgetVA page port is not installed.");
		return port;
	}
	function invokePagePortAlias(alias, args = [], root = window) {
		const method = resolvePagePortMethod(getInstalledPagePort(root), alias);
		if (typeof method !== "function") throw new Error(`WidgetVA page port alias is unavailable: ${alias}`);
		return method(...Array.isArray(args) ? args : [args]);
	}
	var WIDGETVA_EXTENSION_RESPONSE_TYPE = "widgetva:response";
	var WIDGETVA_EXTENSION_RESPONSE_SOURCE = "widgetva-page";
	function buildErrorPayload(error) {
		if (error instanceof Error) return {
			name: error.name,
			message: error.message
		};
		return {
			name: "Error",
			message: String(error || "Unknown WidgetVA bridge error")
		};
	}
	function isWidgetVABridgeRequest(message) {
		return message && typeof message === "object" && message.source === "widgetva-extension" && message.type === "widgetva:request" && typeof message.id === "string" && typeof message.alias === "string";
	}
	function installBrowserExtensionBridge({ root = window, targetOrigin = "*", allowedAliases = [...TRANSPORT_PAGE_PORT_ALIASES] } = {}) {
		const allowedAliasSet = new Set(Array.isArray(allowedAliases) ? allowedAliases : [...TRANSPORT_PAGE_PORT_ALIASES]);
		const handleMessage = async (event) => {
			const message = event?.data;
			if (!isWidgetVABridgeRequest(message)) return;
			if (!allowedAliasSet.has(message.alias)) {
				event.source?.postMessage({
					source: WIDGETVA_EXTENSION_RESPONSE_SOURCE,
					type: WIDGETVA_EXTENSION_RESPONSE_TYPE,
					id: message.id,
					ok: false,
					error: {
						name: "UnsupportedAliasError",
						message: `Unsupported WidgetVA transport alias: ${message.alias}`
					}
				}, targetOrigin);
				return;
			}
			try {
				const result = await invokePagePortAlias(message.alias, Array.isArray(message.args) ? message.args : [], root);
				event.source?.postMessage({
					source: WIDGETVA_EXTENSION_RESPONSE_SOURCE,
					type: WIDGETVA_EXTENSION_RESPONSE_TYPE,
					id: message.id,
					ok: true,
					result
				}, targetOrigin);
			} catch (error) {
				event.source?.postMessage({
					source: WIDGETVA_EXTENSION_RESPONSE_SOURCE,
					type: WIDGETVA_EXTENSION_RESPONSE_TYPE,
					id: message.id,
					ok: false,
					error: buildErrorPayload(error)
				}, targetOrigin);
			}
		};
		root.addEventListener("message", handleMessage);
		return () => root.removeEventListener("message", handleMessage);
	}
	//#endregion
	//#region ../../widgetva-kit/src/integrations/officialPages/officialPageController.js
	function clone$52(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function isPlainObject$8(value) {
		return Boolean(value) && typeof value === "object" && !Array.isArray(value);
	}
	function attachRecoverableState(result, syncResult = null) {
		const recoverableState = isPlainObject$8(syncResult?.recoverableState) ? clone$52(syncResult.recoverableState) : null;
		if (!recoverableState || !isPlainObject$8(result)) return result;
		return {
			...result,
			recoverableState,
			...isPlainObject$8(result.actionResult) ? { actionResult: {
				...result.actionResult,
				recoverableState: clone$52(recoverableState)
			} } : {}
		};
	}
	function createPostActionSyncProxy(target, syncAfterAction) {
		if (!target || typeof target !== "object") return target;
		return new Proxy(target, { get(obj, prop, receiver) {
			if (prop === "executeAction" || prop === "executeVerifiedAction") {
				const original = Reflect.get(obj, prop, receiver);
				if (typeof original !== "function") return original;
				return async (...args) => {
					const result = await original.apply(obj, args);
					return attachRecoverableState(result, await syncAfterAction?.({
						method: prop,
						args,
						result
					}));
				};
			}
			const value = Reflect.get(obj, prop, receiver);
			return typeof value === "function" ? value.bind(obj) : value;
		} });
	}
	function readControllerRecoverableState(widget) {
		return clone$52(widget?.runtime?.readState?.() || widget?.runtime?.readRecoverableState?.() || widget?.readWorkspaceState?.() || null);
	}
	async function restoreControllerRecoverableState(widget, state, controllerLabel = "Official-page controller") {
		const stateId = state?.stateId || null;
		if (!stateId) throw new Error(`${controllerLabel} cannot restore state without a stateId.`);
		if (typeof widget?.replay === "function") {
			await widget.replay(stateId);
			return;
		}
		if (typeof widget?.runtime?.jumpToState === "function") {
			await widget.runtime.jumpToState({ stateId });
			return;
		}
		throw new Error(`${controllerLabel} does not expose a state-restore surface.`);
	}
	//#endregion
	//#region ../../widgetva-kit/src/host/hostBridge.js
	function noop$1() {}
	function emptyArray() {
		return [];
	}
	function emptyObject() {
		return {};
	}
	function cloneValue$10(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function selectValue(selectors, name, readState, fallbackSelector) {
		const selector = selectors?.[name] || fallbackSelector;
		return typeof selector === "function" ? selector(readState()) : null;
	}
	function resolveAction(actions, name, fallbackFactory) {
		const action = actions?.[name];
		if (typeof action === "function") return action;
		return fallbackFactory;
	}
	function invokeResolvedAction(actions, name, readState, fallbackKey, args = []) {
		const action = actions?.[name];
		if (typeof action === "function") return action(...args);
		const state = readState() || {};
		const fallbackAction = state?.[fallbackKey];
		if (typeof fallbackAction === "function") return fallbackAction.call(state, ...args);
	}
	function firstNonNull(...values) {
		for (const value of values) if (value !== void 0 && value !== null) return value;
		return null;
	}
	function normalizeString$7(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizeSelectionRegistry(registry) {
		if (!registry || typeof registry !== "object" || Array.isArray(registry)) return {};
		return cloneValue$10(registry);
	}
	function normalizePrimarySelectionRef(primary) {
		if (typeof primary === "string" && primary.length > 0) return primary;
		if (primary && typeof primary === "object") {
			if (typeof primary.ref === "string" && primary.ref.length > 0) return primary.ref;
			if (typeof primary.selectionRef === "string" && primary.selectionRef.length > 0) return primary.selectionRef;
		}
		return null;
	}
	function normalizeFocusState$1(focus) {
		if (!focus || typeof focus !== "object" || Array.isArray(focus)) return null;
		const widgetRef = typeof focus.widgetRef === "string" ? focus.widgetRef : typeof focus.widget_ref === "string" ? focus.widget_ref : null;
		const widgetId = typeof focus.widgetId === "string" ? focus.widgetId : typeof focus.widget_id === "string" ? focus.widget_id : null;
		const source = typeof focus.source === "string" && focus.source.length > 0 ? focus.source : "workspace";
		if (!widgetRef) return null;
		return {
			widgetRef,
			widgetId,
			source
		};
	}
	function normalizeUniqueStringArray(values) {
		return Array.isArray(values) ? values.filter((value, index) => typeof value === "string" && value.length > 0 && values.indexOf(value) === index) : [];
	}
	function normalizeUniqueScalarArray(values) {
		return Array.isArray(values) ? cloneValue$10(values.filter((value, index) => value != null && values.findIndex((candidate) => JSON.stringify(candidate) === JSON.stringify(value)) === index)) : [];
	}
	function normalizeHighlightEntry$1(entry) {
		if (!entry || typeof entry !== "object" || Array.isArray(entry)) return null;
		return {
			widgetRef: typeof entry.widgetRef === "string" ? entry.widgetRef : typeof entry.widget_ref === "string" ? entry.widget_ref : null,
			widgetId: typeof entry.widgetId === "string" ? entry.widgetId : typeof entry.widget_id === "string" ? entry.widget_id : null,
			highlightedKeys: normalizeUniqueScalarArray(entry.highlightedKeys || entry.highlighted_keys),
			inboundLinkIds: normalizeUniqueStringArray(entry.inboundLinkIds || entry.inbound_link_ids),
			highlightLinkIds: normalizeUniqueStringArray(entry.highlightLinkIds || entry.highlight_link_ids),
			linkedSourceRefs: normalizeUniqueStringArray(entry.linkedSourceRefs || entry.linked_source_refs)
		};
	}
	function normalizeHighlightState$1(highlight) {
		if (!highlight || typeof highlight !== "object" || Array.isArray(highlight)) return null;
		const entries = Array.isArray(highlight.entries) ? highlight.entries.map((entry) => normalizeHighlightEntry$1(entry)).filter(Boolean) : [];
		const explicitActiveWidgetRefs = normalizeUniqueStringArray(highlight.activeWidgetRefs || highlight.active_widget_refs);
		return {
			entries,
			activeWidgetRefs: explicitActiveWidgetRefs.length > 0 ? explicitActiveWidgetRefs : entries.map((entry) => entry.widgetRef).filter(Boolean)
		};
	}
	function deriveCurrentSelectionFromRegistry(registry, primarySelectionRef) {
		const normalizedRegistry = normalizeSelectionRegistry(registry);
		if (primarySelectionRef && normalizedRegistry[primarySelectionRef]) return cloneValue$10(normalizedRegistry[primarySelectionRef]);
		return cloneValue$10(Object.values(normalizedRegistry).find(Boolean) || null);
	}
	function defaultReadSelectionRegistry(state) {
		return firstNonNull(state?.shared?.selections?.registry, state?.selectionRegistry, state?.currentSelections, {});
	}
	function defaultReadPrimarySelectionRef(state) {
		return firstNonNull(state?.shared?.selections?.views?.primary?.ref, state?.shared?.selections?.views?.primary?.selectionRef, state?.primarySelectionRef, state?.currentSelectionRef);
	}
	function createDefaultWidgetVAHostBridge() {
		const defaultSelectionRegistry = emptyObject;
		const defaultPrimarySelectionRef = () => null;
		return {
			subscribe: () => noop$1,
			readSessionId: () => null,
			readBaselineSpec: () => null,
			readCurrentSpec: () => null,
			readWorkspaceSpec: () => null,
			readPlanningRequest: () => null,
			readRunMode: () => "goal_oriented",
			readUserIntent: () => "",
			readSelectionRegistry: defaultSelectionRegistry,
			readPrimarySelectionRef: defaultPrimarySelectionRef,
			readCurrentSelection: () => deriveCurrentSelectionFromRegistry(defaultSelectionRegistry(), defaultPrimarySelectionRef()),
			readCurrentSelections: defaultSelectionRegistry,
			readFocusedWidgetRef: () => null,
			readFocusState: () => null,
			readHighlightState: () => null,
			readSharedFilters: emptyObject,
			readViewportState: emptyObject,
			readSelectionHistory: emptyArray,
			readSelectionsHistory: emptyArray,
			readSelectionFuture: emptyArray,
			readSelectionsFuture: emptyArray,
			writeCurrentSpec: void 0,
			executeProviderAction: void 0,
			resetCurrentSpec: void 0,
			writeWorkspaceSpec: void 0,
			writePlanningRequest: void 0,
			writeRunMode: void 0,
			writeUserIntent: void 0,
			writeCurrentSelection: void 0,
			writeCurrentSelections: void 0,
			resetSelectionHistory: void 0,
			setFocusedWidgetRef: void 0,
			undoSelection: void 0,
			redoSelection: void 0
		};
	}
	function createOfficialPageHostBridge({ sessionId, baselineSpec, currentSpecRef, workspaceSpec = null, userIntent = null, emitOnWrite = false } = {}) {
		const listeners = /* @__PURE__ */ new Set();
		let currentSelectionRegistry = {};
		let currentPrimarySelectionRef = null;
		let currentFocusedWidgetRef = null;
		let currentFocusState = null;
		const emit = (event = {}) => {
			if (!emitOnWrite) return;
			for (const listener of listeners) try {
				listener(event);
			} catch {}
		};
		const cloneRegistry = () => normalizeSelectionRegistry(currentSelectionRegistry);
		const makeSelectionRef = (selection) => {
			const sourceWidgetId = typeof selection?.source_widget_id === "string" && selection.source_widget_id.length > 0 ? selection.source_widget_id : typeof selection?.sourceWidgetId === "string" && selection.sourceWidgetId.length > 0 ? selection.sourceWidgetId : null;
			const selectionId = typeof selection?.selection_id === "string" && selection.selection_id.length > 0 ? selection.selection_id : typeof selection?.selectionId === "string" && selection.selectionId.length > 0 ? selection.selectionId : null;
			if (!sourceWidgetId || !selectionId) return null;
			return `${sourceWidgetId}::${selectionId}`;
		};
		const setSelectionRegistry = (nextRegistry = {}, explicitPrimarySelectionRef = null, event = null) => {
			currentSelectionRegistry = normalizeSelectionRegistry(nextRegistry);
			currentPrimarySelectionRef = normalizePrimarySelectionRef(explicitPrimarySelectionRef) || Object.keys(currentSelectionRegistry)[0] || null;
			if (event !== false) emit(event || {});
		};
		const setFocusState = (focus = null, explicitFocusedWidgetRef = null) => {
			currentFocusState = normalizeFocusState$1(focus);
			currentFocusedWidgetRef = normalizeString$7(explicitFocusedWidgetRef) || currentFocusState?.widgetRef || null;
		};
		return {
			subscribe(listener) {
				if (!emitOnWrite || typeof listener !== "function") return noop$1;
				listeners.add(listener);
				return () => {
					listeners.delete(listener);
				};
			},
			readSessionId: () => sessionId,
			readBaselineSpec: () => cloneValue$10(baselineSpec),
			readCurrentSpec: () => cloneValue$10(currentSpecRef?.current),
			writeCurrentSpec(nextSpec) {
				if (currentSpecRef && typeof currentSpecRef === "object") currentSpecRef.current = cloneValue$10(nextSpec);
				emit();
			},
			readWorkspaceSpec: () => cloneValue$10(workspaceSpec),
			readPlanningRequest: () => null,
			readRunMode: () => "goal_oriented",
			readUserIntent: () => userIntent,
			readSelectionRegistry: () => cloneRegistry(),
			readPrimarySelectionRef: () => currentPrimarySelectionRef,
			readCurrentSelection: () => deriveCurrentSelectionFromRegistry(cloneRegistry(), currentPrimarySelectionRef),
			readCurrentSelections: () => cloneRegistry(),
			readFocusedWidgetRef: () => currentFocusedWidgetRef,
			readFocusState: () => cloneValue$10(currentFocusState),
			readComparisonTargets: () => [],
			writeCurrentSelection(selection, nextOptions = {}) {
				const event = {
					visual: nextOptions?.emit !== false,
					reason: nextOptions?.reason || "selection-write"
				};
				if (!selection || typeof selection !== "object" || Array.isArray(selection)) {
					setFocusState(nextOptions?.focusState || null, nextOptions?.focusedWidgetRef || null);
					setSelectionRegistry({}, null, event);
					return;
				}
				const selectionRef = makeSelectionRef(selection);
				if (!selectionRef) return;
				setFocusState(nextOptions?.focusState || null, nextOptions?.focusedWidgetRef || null);
				setSelectionRegistry({
					...cloneRegistry(),
					[selectionRef]: cloneValue$10(selection)
				}, nextOptions?.primarySelectionRef || selectionRef, event);
			},
			writeCurrentSelections(selections, nextOptions = {}) {
				const event = {
					visual: nextOptions?.emit !== false,
					reason: nextOptions?.reason || "selection-write"
				};
				const rawSelections = selections && typeof selections === "object" && !Array.isArray(selections) ? selections : {};
				const nextRegistry = {};
				for (const selection of Object.values(rawSelections)) {
					if (!selection || typeof selection !== "object" || Array.isArray(selection)) continue;
					const selectionRef = makeSelectionRef(selection);
					if (!selectionRef) continue;
					nextRegistry[selectionRef] = cloneValue$10(selection);
				}
				setFocusState(nextOptions?.focusState || null, nextOptions?.focusedWidgetRef || null);
				setSelectionRegistry(nextRegistry, nextOptions?.primarySelectionRef || null, event);
			},
			setFocusedWidgetRef(widgetRef) {
				setFocusState(widgetRef ? {
					widgetRef,
					widgetId: currentFocusState?.widgetId || null,
					source: currentFocusState?.source || "workspace"
				} : null, widgetRef);
				emit();
			}
		};
	}
	function createWidgetVAHostBridge(options = {}) {
		const { getState, getAppState, subscribe, subscribeAppState, selectors = {}, actions = {}, ...bridgeMethods } = options;
		const defaultBridge = createDefaultWidgetVAHostBridge();
		if (Object.keys(bridgeMethods).some((key) => typeof bridgeMethods[key] === "function")) return {
			...defaultBridge,
			subscribe: typeof subscribe === "function" ? subscribe : defaultBridge.subscribe,
			...bridgeMethods
		};
		const readState = typeof getState === "function" ? getState : typeof getAppState === "function" ? getAppState : () => ({});
		const subscribeToState = typeof subscribe === "function" ? subscribe : typeof subscribeAppState === "function" ? subscribeAppState : () => noop$1;
		const initialState = readState() || {};
		const readSelectionRegistry = () => normalizeSelectionRegistry(selectValue(selectors, "readSelectionRegistry", readState, defaultReadSelectionRegistry) || {});
		const readPrimarySelectionRef = () => normalizePrimarySelectionRef(selectValue(selectors, "readPrimarySelectionRef", readState, defaultReadPrimarySelectionRef));
		return {
			subscribe: subscribeToState,
			readSessionId: () => selectValue(selectors, "readSessionId", readState, (state) => state?.currentSessionId || null),
			readBaselineSpec: () => selectValue(selectors, "readBaselineSpec", readState, (state) => state?.baselineSpec || null),
			readCurrentSpec: () => selectValue(selectors, "readCurrentSpec", readState, (state) => state?.currentSpec || null),
			readWorkspaceSpec: () => selectValue(selectors, "readWorkspaceSpec", readState, (state) => state?.workspaceSpec || null),
			readPlanningRequest: () => selectValue(selectors, "readPlanningRequest", readState, (state) => state?.planningRequest || null),
			readRunMode: () => selectValue(selectors, "readRunMode", readState, (state) => state?.runMode || "goal_oriented") || "goal_oriented",
			readUserIntent: () => selectValue(selectors, "readUserIntent", readState, (state) => state?.userIntent || "") || "",
			readSelectionRegistry,
			readPrimarySelectionRef,
			readCurrentSelection: () => {
				const explicitSelection = selectValue(selectors, "readCurrentSelection", readState, (state) => state?.currentSelection || null);
				if (explicitSelection) return cloneValue$10(explicitSelection);
				return deriveCurrentSelectionFromRegistry(readSelectionRegistry(), readPrimarySelectionRef());
			},
			readCurrentSelections: () => {
				const explicitSelections = selectValue(selectors, "readCurrentSelections", readState, (state) => state?.currentSelections || null);
				if (explicitSelections && typeof explicitSelections === "object" && !Array.isArray(explicitSelections)) return cloneValue$10(explicitSelections);
				return readSelectionRegistry();
			},
			readFocusedWidgetRef: () => selectValue(selectors, "readFocusedWidgetRef", readState, (state) => state?.currentFocusedWidgetRef || null),
			readFocusState: () => normalizeFocusState$1(selectValue(selectors, "readFocusState", readState, (state) => state?.shared?.focus || state?.currentFocusState || null)),
			readHighlightState: () => normalizeHighlightState$1(selectValue(selectors, "readHighlightState", readState, (state) => state?.shared?.highlight || state?.currentHighlightState || null)),
			readSharedFilters: () => selectValue(selectors, "readSharedFilters", readState, (state) => state?.shared?.globalFilters || state?.sharedFilters || state?.currentSharedFilters || {}) || {},
			readViewportState: () => selectValue(selectors, "readViewportState", readState, (state) => state?.shared?.viewport || state?.viewportState || state?.currentViewportState || {}) || {},
			readSelectionHistory: () => selectValue(selectors, "readSelectionHistory", readState, (state) => state?.currentSelectionHistory || []) || [],
			readSelectionsHistory: () => selectValue(selectors, "readSelectionsHistory", readState, (state) => state?.currentSelectionsHistory || []) || [],
			readSelectionFuture: () => selectValue(selectors, "readSelectionFuture", readState, (state) => state?.currentSelectionFuture || []) || [],
			readSelectionsFuture: () => selectValue(selectors, "readSelectionsFuture", readState, (state) => state?.currentSelectionsFuture || []) || [],
			writeCurrentSpec: typeof resolveAction(actions, "writeCurrentSpec", initialState?.setCurrentSpec) === "function" ? (nextSpec, options = {}) => invokeResolvedAction(actions, "writeCurrentSpec", readState, "setCurrentSpec", [nextSpec, options]) : void 0,
			executeProviderAction: typeof resolveAction(actions, "executeProviderAction", initialState?.executeProviderAction) === "function" ? (payload = {}) => invokeResolvedAction(actions, "executeProviderAction", readState, "executeProviderAction", [payload]) : void 0,
			resetCurrentSpec: typeof resolveAction(actions, "resetCurrentSpec", initialState?.resetCurrentSpec) === "function" ? () => invokeResolvedAction(actions, "resetCurrentSpec", readState, "resetCurrentSpec") : void 0,
			writeWorkspaceSpec: typeof resolveAction(actions, "writeWorkspaceSpec", initialState?.setWorkspaceSpec) === "function" ? (workspaceSpec) => invokeResolvedAction(actions, "writeWorkspaceSpec", readState, "setWorkspaceSpec", [workspaceSpec]) : void 0,
			writePlanningRequest: typeof resolveAction(actions, "writePlanningRequest", initialState?.setPlanningRequest) === "function" ? (planningRequest) => invokeResolvedAction(actions, "writePlanningRequest", readState, "setPlanningRequest", [planningRequest]) : void 0,
			writeRunMode: typeof resolveAction(actions, "writeRunMode", initialState?.setRunMode) === "function" ? (runMode) => invokeResolvedAction(actions, "writeRunMode", readState, "setRunMode", [runMode]) : void 0,
			writeUserIntent: typeof resolveAction(actions, "writeUserIntent", initialState?.setUserIntent) === "function" ? (userIntent) => invokeResolvedAction(actions, "writeUserIntent", readState, "setUserIntent", [userIntent]) : void 0,
			writeCurrentSelection: typeof resolveAction(actions, "writeCurrentSelection", initialState?.setCurrentSelection) === "function" ? (selection, nextOptions = {}) => invokeResolvedAction(actions, "writeCurrentSelection", readState, "setCurrentSelection", [selection, nextOptions]) : void 0,
			writeCurrentSelections: typeof resolveAction(actions, "writeCurrentSelections", initialState?.setCurrentSelections) === "function" ? (selections, nextOptions = {}) => invokeResolvedAction(actions, "writeCurrentSelections", readState, "setCurrentSelections", [selections, nextOptions]) : void 0,
			resetSelectionHistory: typeof resolveAction(actions, "resetSelectionHistory", initialState?.resetSelectionHistory) === "function" ? () => invokeResolvedAction(actions, "resetSelectionHistory", readState, "resetSelectionHistory") : void 0,
			setFocusedWidgetRef: typeof resolveAction(actions, "setFocusedWidgetRef", initialState?.setCurrentFocusedWidgetRef) === "function" ? (widgetRef) => invokeResolvedAction(actions, "setFocusedWidgetRef", readState, "setCurrentFocusedWidgetRef", [widgetRef]) : void 0,
			undoSelection: typeof resolveAction(actions, "undoSelection", initialState?.undoCurrentSelection) === "function" ? () => invokeResolvedAction(actions, "undoSelection", readState, "undoCurrentSelection") : void 0,
			redoSelection: typeof resolveAction(actions, "redoSelection", initialState?.redoCurrentSelection) === "function" ? () => invokeResolvedAction(actions, "redoSelection", readState, "redoCurrentSelection") : void 0
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/contracts/refs-contracts.js
	function makeRef({ appId = "widgetva-app", workspaceId = "main", path }) {
		return `wl://${appId}/workspace/${workspaceId}/${String(path || "").replace(/^\/+/, "")}`;
	}
	function makeWidgetRef({ appId = "widgetva-app", workspaceId = "main", widgetId }) {
		return makeRef({
			appId,
			workspaceId,
			path: `widget/${widgetId}`
		});
	}
	function makeSelectionRef({ appId = "widgetva-app", workspaceId = "main", widgetId, selectionId }) {
		return makeRef({
			appId,
			workspaceId,
			path: `widget/${widgetId}/selection/${selectionId}`
		});
	}
	function makeDataRef({ appId = "widgetva-app", workspaceId = "main", dataId }) {
		return makeRef({
			appId,
			workspaceId,
			path: `data/${dataId}`
		});
	}
	function makeCurrentViewDataRef({ appId = "widgetva-app", workspaceId = "main" } = {}) {
		return makeDataRef({
			appId,
			workspaceId,
			dataId: "current_view"
		});
	}
	function makeCurrentSelectionDataRef({ appId = "widgetva-app", workspaceId = "main" } = {}) {
		return makeDataRef({
			appId,
			workspaceId,
			dataId: "current_selection"
		});
	}
	function makeWidgetSelectionDataRef({ appId = "widgetva-app", workspaceId = "main", widgetId }) {
		return makeDataRef({
			appId,
			workspaceId,
			dataId: `${widgetId}_selection`
		});
	}
	function makeSelectionScopedDataRef({ appId = "widgetva-app", workspaceId = "main", widgetId, selectionId }) {
		return makeDataRef({
			appId,
			workspaceId,
			dataId: `${widgetId}_selection_${selectionId}`
		});
	}
	function makeLinkRef({ appId = "widgetva-app", workspaceId = "main", linkId }) {
		return makeRef({
			appId,
			workspaceId,
			path: `link/${linkId}`
		});
	}
	function parseRef(ref) {
		const match = String(ref || "").match(/^wl:\/\/([^/]+)\/workspace\/([^/]+)\/(.+)$/);
		if (!match) return null;
		const [, appId, workspaceId, path] = match;
		const segments = path.split("/").filter(Boolean);
		return {
			appId,
			workspaceId,
			kind: segments.includes("selection") ? "selection" : segments[0] || null,
			path,
			localId: segments[segments.length - 1] || null,
			segments,
			widgetId: segments[0] === "widget" ? segments[1] || null : null,
			selectionId: segments.includes("selection") ? segments[segments.indexOf("selection") + 1] || null : null,
			dataId: segments[0] === "data" ? segments[1] || null : null,
			linkId: segments[0] === "link" ? segments[1] || null : null,
			actionId: segments[0] === "action" ? segments[1] || null : null
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/integrations/officialPages/observableD3OfficialPageController.js
	async function syncObservableD3ControllerState({ widget, wrapper } = {}) {
		const widgetState = typeof widget?.readState === "function" ? widget.readState() : null;
		if (widgetState && typeof wrapper?.renderFromState === "function") {
			await wrapper.renderFromState(widgetState);
			return;
		}
		await wrapper?.syncCurrentSpec?.();
	}
	async function syncObservableD3ScatterControllerState(args = {}) {
		await syncObservableD3ControllerState(args);
	}
	async function syncObservableD3BarControllerState(args = {}) {
		await syncObservableD3ControllerState(args);
	}
	async function syncObservableD3LineControllerState(args = {}) {
		await syncObservableD3ControllerState(args);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/context/history.js
	function clone$51(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function isPlainObject$7(value) {
		return Boolean(value) && typeof value === "object" && !Array.isArray(value);
	}
	function summarizeValue(value) {
		if (Array.isArray(value)) return `[${value.map((entry) => summarizeValue(entry)).join(", ")}]`;
		if (isPlainObject$7(value)) return `{${Object.entries(value).map(([key, entry]) => `${key}:${summarizeValue(entry)}`).join(", ")}}`;
		if (value == null) return "null";
		return String(value);
	}
	function summarizeParams(params = null) {
		if (!isPlainObject$7(params)) return null;
		const entries = Object.entries(params).filter(([, value]) => value !== void 0).map(([key, value]) => `${key}=${summarizeValue(value)}`);
		return entries.length > 0 ? entries.join("; ") : null;
	}
	function readOutcome(turn = {}) {
		const verifyOk = typeof turn?.verify?.ok === "boolean" ? turn.verify.ok : null;
		if (verifyOk === true) return "verified";
		if (verifyOk === false) return "needs_retry";
		const actOk = typeof turn?.act?.ok === "boolean" ? turn.act.ok : null;
		if (actOk === true) return "completed";
		if (actOk === false) return "failed";
		return "completed";
	}
	function readNonEmptyString$1(value = null) {
		return typeof value === "string" && value.trim().length > 0 ? value.trim() : null;
	}
	function summarizeTurnForSession(turn = {}, index = 0) {
		const actKind = turn?.act?.kind || null;
		const actName = turn?.act?.name || null;
		const paramsSummary = summarizeParams(turn?.act?.params);
		const resultSummary = readNonEmptyString$1(turn?.act?.outputSummary) || readNonEmptyString$1(turn?.result?.summary) || null;
		const stateSummary = readNonEmptyString$1(turn?.verify?.summary) || readNonEmptyString$1(turn?.verification?.summary) || null;
		const reasonSummary = readNonEmptyString$1(turn?.reason?.answer) || readNonEmptyString$1(turn?.plan?.assistantMessage) || null;
		return {
			turnId: `turn_${index + 1}`,
			operation: {
				kind: actKind,
				name: actName,
				...paramsSummary ? { paramsSummary } : {}
			},
			status: {
				outcome: readOutcome(turn),
				...resultSummary ? { resultSummary } : {},
				...stateSummary ? { stateSummary } : {},
				...reasonSummary ? { reasonSummary } : {}
			}
		};
	}
	function buildAgentHistory({ turns = [] } = {}) {
		return { turns: Array.isArray(turns) ? turns.map((turn, index) => summarizeTurnForSession(turn, index)) : [] };
	}
	function mergeSessionHistory(previousHistory = null, turns = []) {
		return {
			...clone$51(previousHistory) || {},
			...buildAgentHistory({ turns })
		};
	}
	function readTurnCompletionStatus(turn = null) {
		if (!turn || typeof turn !== "object") return null;
		const candidates = [
			turn?.reason?.completion?.status,
			turn?.reason?.completionStatus,
			turn?.reason?.stopReason
		];
		for (const candidate of candidates) if (typeof candidate === "string" && candidate.length > 0) return candidate;
		if (turn?.reason?.completed === true || turn?.reason?.answered === true) return "answered";
		return null;
	}
	function shouldStopAgentSession(turn = null) {
		if (!turn || typeof turn !== "object") return null;
		const completionStatus = readTurnCompletionStatus(turn);
		if (completionStatus === "answered" || completionStatus === "completed") return "answered";
		if (completionStatus === "stopped" || completionStatus === "failed") return "stopped";
		if (turn?.act?.kind === "action" && turn?.act?.ok === false) return "stopped";
		if ((turn?.act?.kind === "perception" || turn?.act?.kind === "data_query") && turn?.verify?.ok === false) return "stopped";
		return null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/bar/interactionProfile.js
	function getBarHumanInteractionConfig() {
		return {
			mode: "categoryClick",
			actionName: "bar.selectCategory",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/bar/playbook.js
	var barPlaybook = {
		analysisToAction: [
			{
				goal: "Identify the most or least important categories",
				workflow: "If the goal is to find the highest, lowest, most popular, or least popular categories, first make rank order legible, then read the leading or trailing categories instead of scanning the full chart.",
				candidateActions: [
					"bar.sortBars",
					"bar.highlightTopN",
					"bar.selectCategory"
				],
				candidatePerceptions: ["perception.findExtremes", "perception.compareGroups"]
			},
			{
				goal: "Explain category imbalance or overall distribution shape",
				workflow: "If the goal is to explain whether the distribution is concentrated, long-tailed, or relatively even, first isolate the categories driving that pattern and then compare their contribution against the rest of the chart.",
				candidateActions: [
					"bar.highlightTopN",
					"bar.filterCategories",
					"bar.filterSubcategories"
				],
				candidatePerceptions: ["perception.compareGroups", "perception.findExtremes"]
			},
			{
				goal: "Compare a small subset of categories",
				workflow: "If the goal is to compare a few categories, isolate those categories first and compare their magnitudes and gaps directly rather than interpreting the full chart.",
				candidateActions: [
					"bar.selectCategory",
					"bar.filterCategories",
					"bar.clickCategory"
				],
				candidatePerceptions: ["perception.compareGroups"]
			},
			{
				goal: "Compare grouped subsets more clearly",
				workflow: "If the goal is to compare how one variable differs across grouped subsets, reorder the bars to make cross-group contrast easier to scan before drawing conclusions.",
				candidateActions: [
					"bar.sortBars",
					"bar.filterCategories",
					"bar.toggleStackMode",
					"bar.expandStack"
				],
				candidatePerceptions: ["perception.compareGroups", "perception.findExtremes"]
			}
		],
		workflows: [
			{
				name: "filter_then_rank",
				analysisIntent: "Constrained ranking",
				scenarioExamples: ["After excluding irrelevant platforms, which Xbox product ranks first by average RPG score?", "Among selected countries, which region has the second-highest average employment?"],
				description: "Use when the task asks for the top, bottom, first, second, or ranked category after excluding or keeping a subset.",
				steps: [
					{ action: "bar.filterCategories" },
					{ action: "bar.sortBars" },
					{ perception: "perception.findExtremes" }
				]
			},
			{
				name: "filter_subcategories_then_rank",
				analysisIntent: "Segment-level ranking",
				scenarioExamples: ["For 2025 equipment only, which company ranks second by average power consumption?", "Among students with a specific parental education level, which group has the highest average math score?"],
				description: "Use when the comparison depends on one subcategory slice before ranking the remaining bars.",
				steps: [
					{ action: "bar.filterSubcategories" },
					{ action: "bar.sortBars" },
					{ perception: "perception.findExtremes" }
				]
			},
			{
				name: "filter_then_compare_stack",
				analysisIntent: "Composition comparison after filtering",
				scenarioExamples: ["After keeping West and South, which region contributes more total sales and how does the mix differ?", "Within one vehicle model series, which fuel type dominates after narrowing the comparison set?"],
				description: "Use when the task asks how filtered categories differ by stacked composition or subgroup contribution.",
				steps: [
					{ action: "bar.filterCategories" },
					{ action: "bar.toggleStackMode" },
					{ perception: "perception.compareGroups" }
				]
			},
			{
				name: "filter_subcategories_then_compare_stack",
				analysisIntent: "Grouped segment comparison",
				scenarioExamples: ["For selected chest-pain and heart-disease combinations, which group has higher average cholesterol?", "Across workout experience levels, which workout type shows the largest reduction in body fat percentage?"],
				description: "Use when the task asks for grouped subset comparison after keeping or removing specific subcategories.",
				steps: [
					{ action: "bar.filterSubcategories" },
					{ action: "bar.toggleStackMode" },
					{ perception: "perception.compareGroups" }
				]
			},
			{
				name: "rank_then_highlight_top_n",
				analysisIntent: "Top performer analysis",
				scenarioExamples: ["Which companies have the strongest and weakest LinkedIn presence, and what pattern separates them?", "Which anime titles are top performers by popularity compared with the rest of the distribution?"],
				description: "Use when the task asks to identify or discuss top performers after ranking all visible categories.",
				steps: [
					{ action: "bar.sortBars" },
					{ action: "bar.highlightTopN" },
					{ perception: "perception.findExtremes" }
				]
			}
		],
		multiTurnStrategy: [
			"First reveal the overall ranking or distribution shape of the bar chart.",
			"If the query only concerns part of the chart, isolate the relevant categories before comparing them.",
			"After the subset is stable, compare magnitudes, gaps, or relative positions within that subset.",
			"If the subset needs context, compare it back against the full chart.",
			"Stop when the category-level evidence is sufficient to answer the query directly."
		]
	};
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/bar/actionDescriptors.js
	var DESCRIPTORS$4 = [
		{
			"name": "bar.clickCategory",
			"description": "Click one or more categorical groups represented by bars and preserve the chart context, matching the page’s existing in-place category emphasis when available.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"field": { "type": "string" },
					"values": {
						"type": "array",
						"items": { "type": "string" }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["field", "values"]
			},
			"examples": [{
				"userGoal": "Click the sun bar so linked views react the same way as the existing Vega bar click.",
				"params": {
					"field": "weather",
					"values": ["sun"]
				}
			}]
		},
		{
			"name": "bar.selectCategory",
			"description": "Select one or more categorical groups represented by bars while preserving the surrounding chart context.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"field": { "type": "string" },
					"values": {
						"type": "array",
						"items": { "type": "string" }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["field", "values"]
			},
			"examples": [{
				"userGoal": "Select a subset of categories from the summary bar chart.",
				"params": {
					"field": "Origin",
					"values": ["Japan", "USA"]
				}
			}]
		},
		{
			"name": "bar.sortBars",
			"description": "Sort bar groups by the requested order and optional field.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"channel": { "type": "string" },
					"order": {
						"type": "string",
						"enum": ["ascending", "descending"]
					},
					"field": { "type": "string" },
					"aggregate": { "type": "string" },
					"bySubcategory": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["channel", "order"]
			},
			"examples": [{
				"userGoal": "Sort the bar chart descending by the aggregated measure.",
				"params": {
					"channel": "x",
					"order": "descending"
				}
			}, {
				"userGoal": "Sort grouped or stacked categories using one specific subcategory as the ranking signal.",
				"params": {
					"channel": "x",
					"order": "descending",
					"bySubcategory": "Type1"
				}
			}]
		},
		{
			"name": "bar.highlightTopN",
			"description": "Visually emphasize the top-N categories by measure while dimming the remaining bars.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"n": { "type": "number" },
					"order": {
						"type": "string",
						"enum": ["ascending", "descending"]
					},
					"categoryField": { "type": "string" },
					"measureField": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["n"]
			},
			"examples": [{
				"userGoal": "Highlight only the top 5 categories by value before comparing them.",
				"params": {
					"n": 5,
					"order": "descending",
					"categoryField": "category",
					"measureField": "value"
				}
			}]
		},
		{
			"name": "bar.filterCategories",
			"description": "Filter the bar chart to a requested set of categories while leaving the other encodings intact.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"categories": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"field": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["categories"]
			},
			"examples": [{
				"userGoal": "Keep only a few categories before comparing them in detail.",
				"params": {
					"categories": ["A", "C"],
					"field": "category"
				}
			}]
		},
		{
			"name": "bar.addBars",
			"description": "Expand the managed visible-category set of a bar chart by adding one or more category bars back into the visibility filter.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"values": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"field": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["values"]
			},
			"examples": [{
				"userGoal": "Add previously hidden categories back into the current bar comparison without resetting the rest of the view.",
				"params": {
					"values": ["East", "West"],
					"field": "category"
				}
			}]
		},
		{
			"name": "bar.removeBars",
			"description": "Shrink the managed visible-category set of a bar chart by removing one or more category bars from the visibility filter.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"values": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"field": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["values"]
			},
			"examples": [{
				"userGoal": "Temporarily remove several categories from the current bar comparison without resetting the rest of the view.",
				"params": {
					"values": ["East", "West"],
					"field": "category"
				}
			}]
		},
		{
			"name": "bar.addBarItems",
			"description": "Expand the managed visible item set of a grouped or stacked bar chart by adding one or more (category, subcategory) pairs back into the visibility filter.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"items": {
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"x": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
								"sub": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
							},
							"required": ["x", "sub"]
						}
					},
					"xField": { "type": "string" },
					"subField": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["items"]
			},
			"examples": [{
				"userGoal": "Bring a few grouped or stacked members back into the current comparison without resetting the whole chart.",
				"params": {
					"items": [{
						"x": "A",
						"sub": "Type2"
					}],
					"xField": "category",
					"subField": "type"
				}
			}]
		},
		{
			"name": "bar.removeBarItems",
			"description": "Shrink the managed visible item set of a grouped or stacked bar chart by removing one or more (category, subcategory) pairs from the visibility filter.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"items": {
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"x": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
								"sub": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
							},
							"required": ["x", "sub"]
						}
					},
					"xField": { "type": "string" },
					"subField": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["items"]
			},
			"examples": [{
				"userGoal": "Temporarily remove a few grouped or stacked members from the current comparison without resetting the whole chart.",
				"params": {
					"items": [{
						"x": "A",
						"sub": "Type2"
					}],
					"xField": "category",
					"subField": "type"
				}
			}]
		},
		{
			"name": "bar.filterSubcategories",
			"description": "Exclude one or more grouped or stacked subcategories from the current bar chart while preserving the remaining categories.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"subcategoriesToRemove": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"subField": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["subcategoriesToRemove"]
			},
			"examples": [{
				"userGoal": "Remove several grouped or stacked subcategories before comparing the remaining composition.",
				"params": {
					"subcategoriesToRemove": ["Type2"],
					"subField": "type"
				}
			}]
		},
		{
			"name": "bar.expandStack",
			"description": "Filter to one category from a stacked bar chart and expand its stacked segments into parallel bars for easier comparison.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"category": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["category"]
			},
			"examples": [{
				"userGoal": "Expand one stacked category to compare its internal composition without stacked baselines.",
				"params": { "category": "East China" }
			}]
		},
		{
			"name": "bar.toggleStackMode",
			"description": "Switch a grouped/stacked bar chart between grouped and stacked display modes.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"mode": {
						"type": "string",
						"enum": ["grouped", "stacked"]
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["mode"]
			},
			"examples": [{
				"userGoal": "Switch the current stacked bar chart into grouped mode for easier cross-category comparison.",
				"params": { "mode": "grouped" }
			}]
		}
	];
	function clone$50(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function rootEncoding$2(spec) {
		return spec?.layer?.[0]?.encoding || spec?.encoding || {};
	}
	function barCapabilities(spec) {
		const encoding = rootEncoding$2(spec);
		const xField = encoding?.x?.field || null;
		const yField = encoding?.y?.field || null;
		const xType = encoding?.x?.type || null;
		const yType = encoding?.y?.type || null;
		const xIsCategory = (xType === "nominal" || xType === "ordinal") && !encoding?.x?.bin;
		const yIsCategory = (yType === "nominal" || yType === "ordinal") && !encoding?.y?.bin;
		const categoryField = xIsCategory && xField ? xField : yIsCategory && yField ? yField : null;
		const subcategoryField = encoding?.xOffset?.field || encoding?.color?.field || null;
		const colorField = encoding?.color?.field || null;
		return {
			hasCategoryField: Boolean(categoryField),
			hasSubcategoryField: Boolean(categoryField && subcategoryField),
			hasColorField: Boolean(colorField)
		};
	}
	function filterDescriptorsBySpec$4(descriptors, widgetSpec) {
		if (!widgetSpec) return descriptors;
		const { hasCategoryField, hasSubcategoryField, hasColorField } = barCapabilities(widgetSpec);
		return descriptors.filter((descriptor) => {
			if (!descriptor?.name) return true;
			if ([
				"bar.clickCategory",
				"bar.selectCategory",
				"bar.sortBars",
				"bar.highlightTopN",
				"bar.filterCategories",
				"bar.addBars",
				"bar.removeBars"
			].includes(descriptor.name)) return hasCategoryField;
			if ([
				"bar.addBarItems",
				"bar.removeBarItems",
				"bar.filterSubcategories"
			].includes(descriptor.name)) return hasSubcategoryField;
			if (descriptor.name === "bar.expandStack" || descriptor.name === "bar.toggleStackMode") return hasCategoryField && hasColorField;
			return true;
		});
	}
	function buildBarActionDescriptors({ widgetSpec = null } = {}) {
		return filterDescriptorsBySpec$4(DESCRIPTORS$4, widgetSpec).map(clone$50);
	}
	//#endregion
	//#region ../../widgetva-kit/src/contracts/perception-contracts.js
	function cloneValue$9(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function withPerceptionQueryScope(paramsSchema) {
		if (!paramsSchema || typeof paramsSchema !== "object" || Array.isArray(paramsSchema)) return {
			type: "object",
			properties: { queryScope: QUERY_SCOPE_SCHEMA }
		};
		return {
			...cloneValue$9(paramsSchema),
			properties: {
				...cloneValue$9(paramsSchema.properties) || {},
				queryScope: QUERY_SCOPE_SCHEMA
			}
		};
	}
	function makePerceptionDescriptor(descriptor) {
		const returnsSchema = descriptor?.returnsSchema || readPerceptionReturnsSchema(descriptor?.name);
		const paramsSchema = withPerceptionQueryScope(descriptor?.paramsSchema || readPerceptionParamsSchema(descriptor?.name));
		return {
			...descriptor,
			targetRef: descriptor?.targetRef ?? null,
			paramsSchema: paramsSchema || withPerceptionQueryScope({
				type: "object",
				properties: {}
			}),
			returnsSchema: returnsSchema || void 0,
			sideEffectFree: descriptor?.sideEffectFree !== false,
			evidenceKinds: Array.isArray(descriptor?.evidenceKinds) ? descriptor.evidenceKinds : [],
			verificationTargets: Array.isArray(descriptor?.verificationTargets) ? descriptor.verificationTargets : [],
			examples: Array.isArray(descriptor?.examples) ? descriptor.examples : []
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/shared/dataPerception.js
	function runDataPerceptionQuery({ ctx, dataQueryExecutor, targetWidget, kind, spec }) {
		const { dataRef } = ctx.resolveRowsForWidget(targetWidget, spec || {});
		const queryResult = dataQueryExecutor.run({
			dataRef,
			query: {
				kind,
				spec: spec || {}
			}
		});
		return {
			dataRef,
			ok: queryResult?.ok === true,
			result: queryResult?.result || null,
			error: queryResult?.error || null
		};
	}
	function buildPerceptionDataResult({ dataQueryResult, fallbackDataRef = null }) {
		return {
			dataRef: dataQueryResult?.dataRef || fallbackDataRef || null,
			...dataQueryResult?.result || {}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/shared/perceptionScope.js
	function buildScopedPerceptionParamsSchema(extraProperties = {}) {
		return {
			type: "object",
			properties: { ...extraProperties || {} }
		};
	}
	function buildQueryScopeExample({ userGoal, params = {}, dataRef = "wl://demo/workspace/main/data/current_view", selectionRef = null } = {}) {
		return {
			userGoal,
			params: {
				...params || {},
				queryScope: {
					dataRef,
					...selectionRef ? { selectionRef } : {}
				}
			}
		};
	}
	function appendQueryScopeGuidance(description) {
		const base = typeof description === "string" ? description.trim() : "";
		const guidance = "Use target.widgetRef for widget targeting and queryScope for data/selection targeting.";
		if (!base) return guidance;
		return `${base} ${guidance}`;
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/bar/perception.js
	function buildBarPerceptionDescriptors({ dataRef }) {
		return [makePerceptionDescriptor({
			name: "perception.compareGroups",
			title: "Compare groups",
			description: appendQueryScopeGuidance("Compare summary statistics for specified groups in the visible bar data."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				groupField: { type: "string" },
				valueField: { type: "string" },
				groups: {
					type: "array",
					items: { type: "string" }
				}
			}),
			sideEffectFree: true,
			evidenceKinds: ["groupComparison", "aggregateEvidence"],
			examples: [{
				userGoal: "Compare a few selected groups in the summary bars.",
				params: {
					groupField: "Origin",
					valueField: "count",
					groups: ["Japan", "USA"]
				}
			}, buildQueryScopeExample({
				userGoal: "Compare grouped bar summaries inside one filtered selection.",
				params: {
					groupField: "Origin",
					valueField: "count",
					groups: ["Japan", "USA"]
				},
				widgetRef: "wl://demo/workspace/main/widget/bar_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/bar_a/selection/current"
			})]
		}), makePerceptionDescriptor({
			name: "perception.findExtremes",
			title: "Find extremes",
			description: appendQueryScopeGuidance("Return top-k or bottom-k visible rows by a numeric field."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				field: { type: "string" },
				direction: { type: "string" },
				limit: { type: "number" }
			}),
			sideEffectFree: true,
			evidenceKinds: ["rankEvidence", "aggregateEvidence"],
			examples: [{
				userGoal: "Find the highest or lowest categories in the current summary view.",
				params: {
					field: "count",
					direction: "max",
					limit: 5
				}
			}, buildQueryScopeExample({
				userGoal: "Find the highest bars inside one filtered bar subset.",
				params: {
					field: "count",
					direction: "max",
					limit: 5
				},
				widgetRef: "wl://demo/workspace/main/widget/bar_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/bar_a/selection/current"
			})]
		})];
	}
	function registerBarPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.compareGroups", { supportedWidgetKinds: ["bar"] })) perceptionRegistry.register({
			name: "perception.compareGroups",
			supportedWidgetKinds: ["bar"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "bar" });
			const params = ctx.readCallParams();
			const comparison = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "compareGroups",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: comparison,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["bar"] });
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["bar"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["bar"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "bar" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["bar"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/bar/index.js
	function clone$49(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var BAR_LOCAL_SELECTION_CONTRACT = Object.freeze({
		localSelectionFamily: "category",
		selectionKinds: ["category"],
		sourceActionNames: ["bar.clickCategory", "bar.selectCategory"],
		cardinality: "singleActiveSelection",
		selectionValueShape: "categoricalValues",
		observationFields: [
			"state.selections",
			"coordination.localSelectionRefs",
			"coordination.widgetSelectionRef",
			"selection.activeSelectionRef",
			"selection.activeSelectionKind",
			"selection.activeSelectionSummary"
		],
		perceptionExpectations: ["selection-scoped perception queries should accept queryScope.selectionRef when a bar selection is active", "selection summaries should describe selected categorical groups rather than raw pixel regions"]
	});
	var BAR_VERIFICATION_CONTRACT = Object.freeze({
		preferredReadMethod: "readVerificationState",
		preferredObservationFields: [
			"verification.checks.selectionApplied",
			"verification.checks.filterApplied",
			"verification.checks.sortApplied",
			"verification.checks.reencodeApplied",
			"verification.checks.addRemoveApplied",
			"verification.checks.highlightApplied",
			"verification.encodings.channels",
			"verification.view.addRemove",
			"verification.view.reencode",
			"verification.view.sort",
			"verification.view.highlight",
			"verification.transforms.kinds"
		],
		supportedEffectTypes: [
			"selection",
			"filter",
			"sort",
			"reencode",
			"addRemove",
			"highlight",
			"encoding",
			"linkedPropagation"
		],
		effectChecks: {
			selection: [
				"checks.selectionApplied",
				"selections.count",
				"selections.activeSummary"
			],
			filter: [
				"checks.filterApplied",
				"data.rowCount",
				"data.visibleCount",
				"transforms.kinds"
			],
			sort: ["checks.sortApplied", "view.sort"],
			reencode: [
				"checks.reencodeApplied",
				"view.reencode",
				"encodings.channels"
			],
			addRemove: [
				"checks.addRemoveApplied",
				"view.addRemove",
				"data.visibleCount"
			],
			highlight: [
				"checks.highlightApplied",
				"view.highlight",
				"feedback.highlightKeyCount",
				"feedback.sharedSelectionSourceWidgetId"
			],
			encoding: [
				"checks.encodingReadable",
				"encodings.channels",
				"encodings.fieldsByChannel"
			],
			linkedPropagation: [
				"checks.linkedPropagationApplied",
				"feedback.linkedSourceRefCount",
				"feedback.sharedSelectionSourceWidgetId"
			]
		}
	});
	function describeBarLocalSelectionContract() {
		return clone$49(BAR_LOCAL_SELECTION_CONTRACT);
	}
	function describeBarVerificationContract() {
		return clone$49(BAR_VERIFICATION_CONTRACT);
	}
	function describeBarWidgetContract() {
		return {
			kind: "bar",
			actionNames: [
				"bar.clickCategory",
				"bar.selectCategory",
				"bar.sortBars",
				"bar.highlightTopN",
				"bar.filterCategories",
				"bar.addBars",
				"bar.removeBars",
				"bar.addBarItems",
				"bar.removeBarItems",
				"bar.filterSubcategories",
				"bar.expandStack",
				"bar.toggleStackMode"
			],
			perceptionNames: ["perception.compareGroups", "perception.findExtremes"],
			localSelection: describeBarLocalSelectionContract(),
			verification: describeBarVerificationContract()
		};
	}
	var barFamily = Object.freeze({
		kind: "bar",
		describeContract: describeBarWidgetContract,
		actions: Object.freeze({ buildDescriptors: buildBarActionDescriptors }),
		perception: Object.freeze({
			buildDescriptors: buildBarPerceptionDescriptors,
			register: registerBarPerceptionQueries
		}),
		interactionProfile: Object.freeze({ getConfig: getBarHumanInteractionConfig }),
		playbook: barPlaybook
	});
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/heatmap/interactionProfile.js
	function getHeatmapHumanInteractionConfig() {
		return {
			mode: "cellClick",
			actionName: "heatmap.filterCells",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/heatmap/playbook.js
	var heatmapPlaybook = {
		analysisToAction: [
			{
				goal: "Find hotspots or coldspots",
				workflow: "If the goal is to identify hotspots, coldspots, or strong concentration, first locate the most intense and weakest regions, then interpret them in row and column context.",
				candidateActions: [
					"heatmap.highlightRegion",
					"heatmap.highlightRegionByValue",
					"heatmap.selectSubmatrix",
					"heatmap.selectCell"
				],
				candidatePerceptions: ["perception.findExtremes", "perception.findOutliers"]
			},
			{
				goal: "Understand interaction between two dimensions",
				workflow: "If the goal is to understand the x-y relationship, look for blocks, bands, or gradients rather than isolated cells, then inspect the most informative local structure.",
				candidateActions: [
					"heatmap.selectSubmatrix",
					"heatmap.drilldownAxis",
					"heatmap.transpose"
				],
				candidatePerceptions: ["perception.findExtremes", "perception.findOutliers"]
			},
			{
				goal: "Explain an anomalous local region",
				workflow: "If the goal is to explain an anomalous cell or local block, compare the selected region against its neighborhood to determine whether the pattern is isolated or structural.",
				candidateActions: [
					"heatmap.selectSubmatrix",
					"heatmap.selectCell",
					"heatmap.highlightRegion"
				],
				candidatePerceptions: ["perception.findOutliers", "perception.findExtremes"]
			},
			{
				goal: "Focus attention on local rows, columns, or a submatrix",
				workflow: "If the goal is to inspect local structure more clearly, highlight or isolate the relevant rows, columns, or submatrix before interpreting the pattern.",
				candidateActions: [
					"heatmap.selectSubmatrix",
					"heatmap.highlightRegion",
					"heatmap.filterCellsByRegion"
				],
				candidatePerceptions: ["perception.findExtremes"]
			},
			{
				goal: "Make the x-y relationship visually easier to read",
				workflow: "If the current encoding hides the relationship, switch to a more contrastive color scheme or add a complementary encoding that makes relative magnitude easier to compare.",
				candidateActions: [
					"heatmap.adjustColorScale",
					"heatmap.addMarginalBars",
					"heatmap.transpose",
					"heatmap.clusterRowsCols"
				],
				candidatePerceptions: ["perception.findExtremes"]
			}
		],
		workflows: [
			{
				name: "filter_region_then_find_extremes",
				analysisIntent: "Local hotspot or coldspot lookup",
				scenarioExamples: ["Within haircare products, which customer demographic has the highest mean price?", "For a selected city-month range, which cell has the most extreme temperature?"],
				description: "Use when the task asks for the hottest, coldest, highest, or lowest cell inside a local heatmap region.",
				steps: [{ action: "heatmap.filterCellsByRegion" }, { perception: "perception.findExtremes" }]
			},
			{
				name: "filter_region_then_summarize",
				analysisIntent: "Local block summary",
				scenarioExamples: ["What is the difference in mean anxiety levels between composers who are instrumentalists and those who are not?", "How does one month band compare with another across the selected product categories?"],
				description: "Use when the task asks for a comparison or summary after keeping a row, column, or submatrix region.",
				steps: [{ action: "heatmap.filterCellsByRegion" }, { perception: "perception.findExtremes" }]
			},
			{
				name: "adjust_color_then_add_marginals",
				analysisIntent: "Row-column contribution reading",
				scenarioExamples: ["Which rows and columns drive the strongest hotspot pattern after the color scale is made readable?", "Do marginal totals confirm that the apparent hotspot is a row effect or a column effect?"],
				description: "Use when the global heatmap structure is hard to read and row or column totals would clarify hotspots.",
				steps: [
					{ action: "heatmap.adjustColorScale" },
					{ action: "heatmap.addMarginalBars" },
					{ perception: "perception.findExtremes" }
				]
			},
			{
				name: "cluster_then_highlight_region",
				analysisIntent: "Clustered block interpretation",
				scenarioExamples: ["Which months form a coherent seasonal block after clustering rows and columns?", "Which disease-risk features cluster together, and which region should be highlighted for explanation?"],
				description: "Use when the task asks about block structure after row or column clustering.",
				steps: [
					{ action: "heatmap.clusterRowsCols" },
					{ action: "heatmap.highlightRegion" },
					{ perception: "perception.findOutliers" }
				]
			},
			{
				name: "adjust_color_then_cluster_then_filter_region",
				analysisIntent: "Noisy matrix simplification",
				scenarioExamples: ["After improving contrast, which clustered crime-pattern block explains the strongest district-month anomaly?", "Which clustered health-measure block remains extreme after the heatmap is recolored?"],
				description: "Use when the heatmap needs stronger contrast before clustering and selecting the resulting local block.",
				steps: [
					{ action: "heatmap.adjustColorScale" },
					{ action: "heatmap.clusterRowsCols" },
					{ action: "heatmap.filterCellsByRegion" },
					{ perception: "perception.findExtremes" }
				]
			},
			{
				name: "change_view_then_filter_region",
				analysisIntent: "Reoriented matrix comparison",
				scenarioExamples: ["After transposing the heatmap, which month has the lowest mean price for Cortado?", "When rows and columns are swapped, which category block becomes the clearest target for comparison?"],
				description: "Use when the current heatmap orientation or encoding hides the region needed for the question.",
				steps: [
					{ action: "heatmap.transpose" },
					{ action: "heatmap.filterCellsByRegion" },
					{ perception: "perception.findExtremes" }
				]
			}
		],
		multiTurnStrategy: [
			"First decide whether the query is about global structure or a local region of the matrix.",
			"If local structure matters, isolate the relevant rows, columns, or submatrix before interpreting it.",
			"If the current encoding is too weak to support the query, improve readability before drawing conclusions.",
			"Then compare the local structure against its surrounding context.",
			"Stop when the selected region and its contrast with the rest of the matrix are clear enough to answer directly."
		]
	};
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/heatmap/actionDescriptors.js
	var DESCRIPTORS$3 = [
		{
			"name": "heatmap.filterCells",
			"description": "Filter the workspace through a heatmap cell identified by its x/y category pair.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"xField": { "type": "string" },
					"yField": { "type": "string" },
					"xValue": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"yValue": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": [
					"xField",
					"yField",
					"xValue",
					"yValue"
				]
			},
			"examples": [{
				"userGoal": "Select one heatmap cell and inspect linked detail views.",
				"params": {
					"xField": "Origin",
					"yField": "Cylinders",
					"xValue": "Japan",
					"yValue": "4"
				}
			}]
		},
		{
			"name": "heatmap.selectCell",
			"description": "Select a single heatmap cell through the canonical heatmap cell contract.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"xField": { "type": "string" },
					"yField": { "type": "string" },
					"xValue": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"yValue": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": [
					"xField",
					"yField",
					"xValue",
					"yValue"
				]
			},
			"examples": [{
				"userGoal": "Select one heatmap cell and inspect linked detail views.",
				"params": {
					"xField": "Origin",
					"yField": "Cylinders",
					"xValue": "Japan",
					"yValue": "4"
				}
			}]
		},
		{
			"name": "heatmap.selectSubmatrix",
			"description": "Select a heatmap submatrix defined by one or more x-axis and/or y-axis coordinates without mutating the view spec.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"xValues": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"yValues": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Select a rectangular region of the heatmap before inspecting linked views.",
				"params": {
					"xValues": ["Q1", "Q2"],
					"yValues": ["A", "B"]
				}
			}]
		},
		{
			"name": "heatmap.drilldownAxis",
			"description": "Drill a temporal heatmap x-axis from year to month or from month to date by narrowing the visible period and refining the x-axis timeUnit.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"level": { "type": "string" },
					"value": { "anyOf": [{ "type": "number" }, { "type": "string" }] },
					"parent": { "type": "object" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["level", "value"]
			},
			"examples": [{
				"userGoal": "Drill a yearly heatmap into monthly detail for one year.",
				"params": {
					"level": "year",
					"value": 2024
				}
			}]
		},
		{
			"name": "heatmap.resetDrilldown",
			"description": "Restore the original temporal x-axis encoding and remove drill-down filters from the heatmap.",
			"paramsSchema": {
				"type": "object",
				"properties": { "queryScope": {
					"type": "object",
					"additionalProperties": false,
					"properties": {
						"dataRef": { "type": "string" },
						"selectionRef": { "type": "string" },
						"focusRef": { "type": "string" },
						"viewportRef": { "type": "string" }
					}
				} }
			},
			"examples": [{
				"userGoal": "Return a drilled heatmap back to its original yearly overview.",
				"params": {}
			}]
		},
		{
			"name": "heatmap.addMarginalBars",
			"description": "Compose a heatmap with optional top and right marginal bar charts that aggregate the heatmap value field along each axis.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"op": { "type": "string" },
					"showTop": { "type": "boolean" },
					"showRight": { "type": "boolean" },
					"barSize": { "type": "number" },
					"barColor": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Add row and column marginal summaries before comparing the overall heatmap structure.",
				"params": {
					"op": "mean",
					"showTop": true,
					"showRight": true,
					"barSize": 70,
					"barColor": "#666666"
				}
			}]
		},
		{
			"name": "heatmap.highlightRegion",
			"description": "Highlight one or more heatmap rows, columns, or their intersection without filtering away the rest of the matrix.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"xValues": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"yValues": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Highlight one row/column region before comparing extreme cells.",
				"params": {
					"xValues": ["Q1"],
					"yValues": ["A"]
				}
			}]
		},
		{
			"name": "heatmap.adjustColorScale",
			"description": "Update the heatmap color scheme and optional numeric domain without changing the underlying data.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"scheme": { "type": "string" },
					"domain": {
						"type": "array",
						"minItems": 2,
						"maxItems": 2,
						"items": { "anyOf": [{ "type": "number" }, { "type": "string" }] }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Switch to a new color scheme and tighten the value range for comparison.",
				"params": {
					"scheme": "blues",
					"domain": [0, 25]
				}
			}]
		},
		{
			"name": "heatmap.thresholdMask",
			"description": "Visually dim heatmap cells whose color values fall outside the requested inclusive threshold range.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"minValue": { "anyOf": [{ "type": "number" }, { "type": "string" }] },
					"maxValue": { "anyOf": [{ "type": "number" }, { "type": "string" }] },
					"outsideOpacity": { "type": "number" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["minValue", "maxValue"]
			},
			"examples": [{
				"userGoal": "Dim low-signal cells while keeping the full matrix visible.",
				"params": {
					"minValue": 10,
					"maxValue": 25,
					"outsideOpacity": .1
				}
			}]
		},
		{
			"name": "heatmap.filterCellsByRegion",
			"description": "Exclude one or more heatmap rows, columns, or their intersection by writing a region filter into the heatmap spec.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"xValue": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"yValue": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"xValues": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"yValues": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Remove one heatmap row-column intersection before inspecting the remaining structure.",
				"params": {
					"xValues": ["Q1"],
					"yValues": ["A"]
				}
			}]
		},
		{
			"name": "heatmap.highlightRegionByValue",
			"description": "Visually emphasize cells whose displayed values fall inside a requested range, while dimming the rest without filtering data away.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"minValue": { "anyOf": [{ "type": "number" }, { "type": "string" }] },
					"maxValue": { "anyOf": [{ "type": "number" }, { "type": "string" }] },
					"outsideOpacity": { "type": "number" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Highlight only mid-range or high-value cells before comparing hotspots.",
				"params": {
					"minValue": 10,
					"maxValue": 25,
					"outsideOpacity": .12
				}
			}]
		},
		{
			"name": "heatmap.clusterRowsCols",
			"description": "Reorder heatmap rows and/or columns by aggregated cell values so high-value bands are grouped toward the front.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"clusterRows": { "type": "boolean" },
					"clusterCols": { "type": "boolean" },
					"method": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Bring the hottest rows and columns toward the front of the matrix.",
				"params": {
					"clusterRows": true,
					"clusterCols": true,
					"method": "sum"
				}
			}]
		},
		{
			"name": "heatmap.transpose",
			"description": "Swap the heatmap x and y encodings to quickly inspect the matrix from the opposite orientation.",
			"paramsSchema": {
				"type": "object",
				"properties": { "queryScope": {
					"type": "object",
					"additionalProperties": false,
					"properties": {
						"dataRef": { "type": "string" },
						"selectionRef": { "type": "string" },
						"focusRef": { "type": "string" },
						"viewportRef": { "type": "string" }
					}
				} }
			},
			"examples": [{
				"userGoal": "Flip rows and columns to compare the matrix from the opposite orientation.",
				"params": {}
			}]
		}
	];
	function clone$48(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function rootEncoding$1(spec) {
		return spec?.layer?.[0]?.encoding || spec?.encoding || {};
	}
	function heatmapCapabilities(spec) {
		const encoding = rootEncoding$1(spec);
		const hasXAxis = typeof encoding?.x?.field === "string" && encoding.x.field.length > 0;
		const hasYAxis = typeof encoding?.y?.field === "string" && encoding.y.field.length > 0;
		return {
			hasAxes: hasXAxis && hasYAxis,
			hasColorField: typeof encoding?.color?.field === "string" && encoding.color.field.length > 0,
			hasTemporalXAxis: encoding?.x?.type === "temporal" || typeof encoding?.x?.timeUnit === "string"
		};
	}
	function filterDescriptorsBySpec$3(descriptors, widgetSpec) {
		if (!widgetSpec) return descriptors;
		const { hasAxes, hasColorField, hasTemporalXAxis } = heatmapCapabilities(widgetSpec);
		return descriptors.filter((descriptor) => {
			if (!descriptor?.name) return true;
			if ([
				"heatmap.filterCells",
				"heatmap.selectCell",
				"heatmap.selectSubmatrix",
				"heatmap.highlightRegion",
				"heatmap.filterCellsByRegion",
				"heatmap.transpose"
			].includes(descriptor.name)) return hasAxes;
			if ([
				"heatmap.addMarginalBars",
				"heatmap.adjustColorScale",
				"heatmap.thresholdMask",
				"heatmap.highlightRegionByValue",
				"heatmap.clusterRowsCols"
			].includes(descriptor.name)) return hasAxes && hasColorField;
			if (descriptor.name === "heatmap.drilldownAxis" || descriptor.name === "heatmap.resetDrilldown") return hasAxes && hasTemporalXAxis;
			return true;
		});
	}
	function buildHeatmapActionDescriptors({ widgetSpec = null } = {}) {
		return filterDescriptorsBySpec$3(DESCRIPTORS$3, widgetSpec).map(clone$48);
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/heatmap/perception.js
	function buildHeatmapPerceptionDescriptors({ dataRef }) {
		return [makePerceptionDescriptor({
			name: "perception.findExtremes",
			title: "Find extremes",
			description: appendQueryScopeGuidance("Return highest or lowest valued cells in the visible heatmap data."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				field: { type: "string" },
				direction: { type: "string" },
				limit: { type: "number" }
			}),
			sideEffectFree: true,
			evidenceKinds: ["matrixEvidence", "rankEvidence"],
			examples: [{
				userGoal: "Find the highest or lowest cells in the matrix view.",
				params: {
					field: "value",
					direction: "max",
					limit: 5
				}
			}, buildQueryScopeExample({
				userGoal: "Find extreme cells inside one selected heatmap region.",
				params: {
					field: "value",
					direction: "max",
					limit: 5
				},
				widgetRef: "wl://demo/workspace/main/widget/heatmap_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/heatmap_a/selection/region"
			})]
		}), makePerceptionDescriptor({
			name: "perception.findOutliers",
			title: "Find outliers",
			description: appendQueryScopeGuidance("Return likely outlier cells in the visible heatmap data."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				field: { type: "string" },
				zThreshold: { type: "number" },
				limit: { type: "number" }
			}),
			sideEffectFree: true,
			evidenceKinds: ["outlierEvidence", "matrixEvidence"],
			examples: [{
				userGoal: "Identify suspicious cells in the current heatmap view.",
				params: {
					field: "value",
					zThreshold: 2.5,
					limit: 10
				}
			}, buildQueryScopeExample({
				userGoal: "Identify outlier cells inside one selected heatmap region.",
				params: {
					field: "value",
					zThreshold: 2.5,
					limit: 10
				},
				widgetRef: "wl://demo/workspace/main/widget/heatmap_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/heatmap_a/selection/region"
			})]
		})];
	}
	function registerHeatmapPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["heatmap"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["heatmap"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "heatmap" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["heatmap"] });
		if (!perceptionRegistry.has("perception.findOutliers", { supportedWidgetKinds: ["heatmap"] })) perceptionRegistry.register({
			name: "perception.findOutliers",
			supportedWidgetKinds: ["heatmap"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "heatmap" });
			const params = ctx.readCallParams();
			const outliers = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findOutliers",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: outliers,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["heatmap"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/heatmap/index.js
	function clone$47(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var HEATMAP_LOCAL_SELECTION_CONTRACT = Object.freeze({
		localSelectionFamily: "cell",
		selectionKinds: ["cell", "region"],
		sourceActionNames: ["heatmap.selectCell", "heatmap.selectSubmatrix"],
		cardinality: "singleActiveSelection",
		selectionValueShape: "cellOrRegionCoordinates",
		observationFields: [
			"state.selections",
			"coordination.localSelectionRefs",
			"coordination.widgetSelectionRef",
			"selection.activeSelectionRef",
			"selection.activeSelectionKind",
			"selection.activeSelectionSummary",
			"selection.activeSelectionFields"
		],
		perceptionExpectations: ["selection-scoped perception queries should resolve either one cell or a selected submatrix subset", "selection summaries should distinguish cell selections from broader row/column region selections"]
	});
	var HEATMAP_VERIFICATION_CONTRACT = Object.freeze({
		preferredReadMethod: "readVerificationState",
		preferredObservationFields: [
			"verification.checks.selectionApplied",
			"verification.checks.filterApplied",
			"verification.checks.aggregateApplied",
			"verification.checks.drillDownApplied",
			"verification.checks.reencodeApplied",
			"verification.checks.navigateApplied",
			"verification.checks.highlightApplied",
			"verification.encodings.channels",
			"verification.view.aggregate",
			"verification.view.drillDown",
			"verification.view.reencode",
			"verification.view.navigate",
			"verification.view.highlight",
			"verification.selections.activeKinds"
		],
		supportedEffectTypes: [
			"selection",
			"filter",
			"aggregate",
			"drillDown",
			"reencode",
			"navigate",
			"highlight",
			"encoding",
			"linkedPropagation"
		],
		effectChecks: {
			selection: [
				"checks.selectionApplied",
				"selections.count",
				"selections.activeKinds",
				"selections.activeSummary"
			],
			filter: [
				"checks.filterApplied",
				"data.rowCount",
				"data.visibleCount",
				"transforms.kinds"
			],
			aggregate: [
				"checks.aggregateApplied",
				"view.aggregate",
				"view.sort"
			],
			drillDown: [
				"checks.drillDownApplied",
				"view.drillDown",
				"transforms.kinds"
			],
			reencode: [
				"checks.reencodeApplied",
				"view.reencode",
				"encodings.channels"
			],
			navigate: ["checks.navigateApplied", "view.navigate"],
			highlight: [
				"checks.highlightApplied",
				"view.highlight",
				"feedback.highlightKeyCount",
				"feedback.sharedSelectionSourceWidgetId"
			],
			encoding: [
				"checks.encodingReadable",
				"encodings.channels",
				"encodings.fieldsByChannel"
			],
			linkedPropagation: [
				"checks.linkedPropagationApplied",
				"feedback.linkedSourceRefCount",
				"feedback.sharedSelectionSourceWidgetId"
			]
		}
	});
	function describeHeatmapLocalSelectionContract() {
		return clone$47(HEATMAP_LOCAL_SELECTION_CONTRACT);
	}
	function describeHeatmapVerificationContract() {
		return clone$47(HEATMAP_VERIFICATION_CONTRACT);
	}
	function describeHeatmapWidgetContract() {
		return {
			kind: "heatmap",
			actionNames: [
				"heatmap.filterCells",
				"heatmap.selectCell",
				"heatmap.selectSubmatrix",
				"heatmap.drilldownAxis",
				"heatmap.resetDrilldown",
				"heatmap.addMarginalBars",
				"heatmap.highlightRegion",
				"heatmap.adjustColorScale",
				"heatmap.thresholdMask",
				"heatmap.filterCellsByRegion",
				"heatmap.highlightRegionByValue",
				"heatmap.clusterRowsCols",
				"heatmap.transpose"
			],
			perceptionNames: ["perception.findExtremes", "perception.findOutliers"],
			localSelection: describeHeatmapLocalSelectionContract(),
			verification: describeHeatmapVerificationContract()
		};
	}
	var heatmapFamily = Object.freeze({
		kind: "heatmap",
		describeContract: describeHeatmapWidgetContract,
		actions: Object.freeze({ buildDescriptors: buildHeatmapActionDescriptors }),
		perception: Object.freeze({
			buildDescriptors: buildHeatmapPerceptionDescriptors,
			register: registerHeatmapPerceptionQueries
		}),
		interactionProfile: Object.freeze({ getConfig: getHeatmapHumanInteractionConfig }),
		playbook: heatmapPlaybook
	});
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/line/interactionProfile.js
	function getLineHumanInteractionConfig() {
		return {
			mode: "categoryClick",
			actionName: "line.selectSeries",
			categoryFieldChannel: "color",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/line/playbook.js
	var linePlaybook = {
		analysisToAction: [
			{
				goal: "Understand overall trend shape",
				workflow: "If the goal is to understand trend shape, first identify major phases such as rise, decline, plateau, or volatility, then zoom into the phase most relevant to the query.",
				candidateActions: ["line.zoomXRegion", "line.selectXValue"],
				candidatePerceptions: [
					"perception.findExtremes",
					"perception.compareGroups",
					"perception.detectAnomalies"
				]
			},
			{
				goal: "Explain a local peak, dip, or abrupt change",
				workflow: "If the goal is to explain a peak, dip, or structural break, first locate the turning region, then narrow to that interval and inspect it at higher resolution.",
				candidateActions: [
					"line.zoomXRegion",
					"line.selectXValue",
					"line.highlightTrend"
				],
				candidatePerceptions: ["perception.findExtremes", "perception.detectAnomalies"]
			},
			{
				goal: "Compare temporal patterns at different granularities",
				workflow: "If the goal is to compare annual, quarterly, monthly, or weekly behavior, resample the series to the relevant temporal grain and compare how the pattern changes across scales.",
				candidateActions: ["line.resampleXAxis", "line.resetResampleXAxis"],
				candidatePerceptions: ["perception.compareGroups", "perception.findExtremes"]
			},
			{
				goal: "Focus on only a subset of lines in a multi-line chart",
				workflow: "If the chart contains multiple lines but the query concerns only a subset, suppress irrelevant series and analyze the selected lines more clearly.",
				candidateActions: [
					"line.filterLines",
					"line.boldLines",
					"line.focusLines",
					"line.selectSeries"
				],
				candidatePerceptions: ["perception.compareGroups", "perception.detectAnomalies"]
			},
			{
				goal: "Compare different time windows",
				workflow: "If the goal is to compare periods, define meaningful intervals first and compare their level, slope, and variability rather than relying only on endpoints.",
				candidateActions: [
					"line.zoomXRegion",
					"line.selectXValue",
					"line.drillDownXAxis"
				],
				candidatePerceptions: ["perception.compareGroups", "perception.findExtremes"]
			}
		],
		workflows: [
			{
				name: "zoom_then_filter_series",
				analysisIntent: "Time-windowed entity comparison",
				scenarioExamples: ["During the selected period, which product category had the highest revenue?", "In the recent downturn window, which country contributed the most to the observed change?"],
				description: "Use when the comparison is limited to a time interval and only a subset of series matters.",
				steps: [
					{ action: "line.zoomXRegion" },
					{ action: "line.filterLines" },
					{ perception: "perception.compareGroups" }
				]
			},
			{
				name: "filter_series_then_zoom",
				analysisIntent: "Focused series inspection",
				scenarioExamples: ["For New Zealand among the selected countries, which year has the highest contribution value?", "For two chosen stock tickers, how do their prices behave during the crash period?"],
				description: "Use when the target series are known first and the question asks about their behavior in a time window.",
				steps: [
					{ action: "line.filterLines" },
					{ action: "line.zoomXRegion" },
					{ perception: "perception.compareGroups" }
				]
			},
			{
				name: "compare_time_granularities",
				analysisIntent: "Temporal granularity comparison",
				scenarioExamples: ["Do monthly, quarterly, and annual visitor trends tell the same story?", "Does stock volatility remain visible after resampling daily prices to monthly averages?"],
				description: "Use when the task asks how monthly, quarterly, annual, or other resampled trends differ.",
				steps: [
					{ action: "line.resampleXAxis" },
					{ action: "line.resampleXAxis" },
					{ perception: "perception.compareGroups" }
				]
			},
			{
				name: "zoom_then_smooth",
				analysisIntent: "Smoothed local trend reading",
				scenarioExamples: ["In the latter half of the time series, what is the lowest point of the smoothed visitor trend?", "During the unstable usage period, does the moving average show a real decline or just noise?"],
				description: "Use when a local peak, dip, or noisy interval needs a smoothed reading.",
				steps: [
					{ action: "line.zoomXRegion" },
					{ action: "line.showMovingAverage" },
					{ perception: "perception.findExtremes" }
				]
			},
			{
				name: "smooth_then_highlight_trend",
				analysisIntent: "Noise-reduced trend direction",
				scenarioExamples: ["After smoothing electricity access over time, does the trend mainly increase or plateau?", "Once short-term volatility is reduced, which segment shows a sustained decline?"],
				description: "Use when the task asks for trend direction after reducing short-term noise.",
				steps: [
					{ action: "line.showMovingAverage" },
					{ action: "line.highlightTrend" },
					{ perception: "perception.compareGroups" }
				]
			},
			{
				name: "detect_anomalies_then_resample",
				analysisIntent: "Anomaly stability across time scales",
				scenarioExamples: ["Are unusual stock-price movements still visible after comparing daily and monthly views?", "Do visitor spikes remain meaningful when the series is aggregated from months to years?"],
				description: "Use when unusual events should be checked against a coarser or finer time grain.",
				steps: [
					{ perception: "perception.detectAnomalies" },
					{ action: "line.resampleXAxis" },
					{ perception: "perception.compareGroups" }
				]
			},
			{
				name: "zoom_then_detect_anomalies",
				analysisIntent: "Interval-specific anomaly search",
				scenarioExamples: ["Within the high AI-usage window, are there unusual jumps in bug count?", "During the selected sales campaign period, which weeks are anomalous?"],
				description: "Use when anomaly detection should be restricted to one relevant interval.",
				steps: [{ action: "line.zoomXRegion" }, { perception: "perception.detectAnomalies" }]
			}
		],
		multiTurnStrategy: [
			"First decide whether the query is about the global trend or a local interval.",
			"If the query is global, summarize the major phases before narrowing further.",
			"If the query is local, zoom or resample before reading the relevant interval in detail.",
			"If multiple lines are present, reduce clutter before comparing the specific series that matter.",
			"Stop when the selected interval or series subset is specific enough to support a direct answer."
		]
	};
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/line/actionDescriptors.js
	var DESCRIPTORS$2 = [
		{
			"name": "line.selectSeries",
			"description": "Select one or more categorical series represented in the line chart.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"field": { "type": "string" },
					"values": {
						"type": "array",
						"items": { "anyOf": [{ "type": "string" }, { "type": "number" }] }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["field", "values"]
			},
			"examples": [{
				"userGoal": "Focus one or more line series before comparing trends.",
				"params": {
					"field": "Origin",
					"values": ["Japan"]
				}
			}]
		},
		{
			"name": "line.selectXValue",
			"description": "Select all visible line rows that share one x-axis value, such as one year or one named category bucket.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"value": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"field": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["value"]
			},
			"examples": [{
				"userGoal": "Select one model year across the line view before checking linked distributions and details.",
				"params": {
					"field": "year",
					"value": 1971
				}
			}]
		},
		{
			"name": "line.zoomXRegion",
			"description": "Zoom the temporal x-axis of the line chart to a specific start/end range without discarding data.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"start": { "type": "string" },
					"end": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["start", "end"]
			},
			"examples": [{
				"userGoal": "Zoom into a particular date range to inspect the detailed trend.",
				"params": {
					"start": "2024-01-01",
					"end": "2024-02-01"
				}
			}]
		},
		{
			"name": "line.focusLines",
			"description": "Emphasize one or more line series while dimming the remaining lines.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"lines": {
						"type": "array",
						"items": { "type": "string" }
					},
					"lineField": { "type": "string" },
					"dimOpacity": { "type": "number" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["lines"]
			},
			"examples": [{
				"userGoal": "Focus a subset of line series before comparing their trends.",
				"params": {
					"lines": ["A"],
					"lineField": "series",
					"dimOpacity": .08
				}
			}]
		},
		{
			"name": "line.highlightTrend",
			"description": "Add or refresh a regression trend line layer over the existing line chart.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"trendType": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Add a regression overlay before describing the overall trend direction.",
				"params": { "trendType": "increasing" }
			}]
		},
		{
			"name": "line.showMovingAverage",
			"description": "Add or replace a moving-average overlay line computed from the current temporal/value encodings, optionally grouped by line series.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"windowSize": { "type": "number" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				}
			},
			"examples": [{
				"userGoal": "Smooth a noisy line chart with a 3-period trailing moving average.",
				"params": { "windowSize": 3 }
			}]
		},
		{
			"name": "line.drillDownXAxis",
			"description": "Drill a temporal line chart from a coarser time aggregation into a more detailed x-axis view.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"level": { "type": "string" },
					"value": { "type": "number" },
					"parent": { "type": "object" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["level", "value"]
			},
			"examples": [{
				"userGoal": "Drill a yearly trend into monthly detail for a specific year.",
				"params": {
					"level": "year",
					"value": 2024
				}
			}]
		},
		{
			"name": "line.resetDrilldownXAxis",
			"description": "Restore the original line chart encoding, transforms, and title after a temporal drill-down.",
			"paramsSchema": {
				"type": "object",
				"properties": { "queryScope": {
					"type": "object",
					"additionalProperties": false,
					"properties": {
						"dataRef": { "type": "string" },
						"selectionRef": { "type": "string" },
						"focusRef": { "type": "string" },
						"viewportRef": { "type": "string" }
					}
				} }
			},
			"examples": [{
				"userGoal": "Return from a drilled monthly view back to the original yearly chart.",
				"params": {}
			}]
		},
		{
			"name": "line.resampleXAxis",
			"description": "Change the temporal aggregation granularity of a line chart and apply an aggregate to the value axis.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"granularity": { "type": "string" },
					"agg": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["granularity"]
			},
			"examples": [{
				"userGoal": "Switch a dense daily series into monthly mean values before comparing long-term trends.",
				"params": {
					"granularity": "month",
					"agg": "mean"
				}
			}]
		},
		{
			"name": "line.resetResampleXAxis",
			"description": "Restore the original temporal encoding after a line x-axis resampling operation.",
			"paramsSchema": {
				"type": "object",
				"properties": { "queryScope": {
					"type": "object",
					"additionalProperties": false,
					"properties": {
						"dataRef": { "type": "string" },
						"selectionRef": { "type": "string" },
						"focusRef": { "type": "string" },
						"viewportRef": { "type": "string" }
					}
				} }
			},
			"examples": [{
				"userGoal": "Return a resampled monthly line chart back to its original daily granularity.",
				"params": {}
			}]
		},
		{
			"name": "line.boldLines",
			"description": "Increase the stroke width of one or more line series while keeping the remaining lines thin.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"lineNames": {
						"type": "array",
						"items": { "type": "string" }
					},
					"lineField": { "type": "string" },
					"boldWidth": { "type": "number" },
					"baseWidth": { "type": "number" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["lineNames"]
			},
			"examples": [{
				"userGoal": "Make one or more line series stand out before comparing the trends.",
				"params": {
					"lineNames": ["A"],
					"lineField": "series",
					"boldWidth": 4,
					"baseWidth": 1
				}
			}]
		},
		{
			"name": "line.filterLines",
			"description": "Exclude one or more line series from the current chart by writing a series filter into the line spec.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"linesToRemove": {
						"type": "array",
						"items": { "type": "string" }
					},
					"lineField": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["linesToRemove"]
			},
			"examples": [{
				"userGoal": "Remove noisy or irrelevant series before comparing the remaining trends.",
				"params": {
					"linesToRemove": ["B"],
					"lineField": "series"
				}
			}]
		}
	];
	function clone$46(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function rootEncoding(spec) {
		return spec?.layer?.[0]?.encoding || spec?.encoding || {};
	}
	function inferRawTimeField$2(spec, encoding, transforms) {
		const timeUnitTransform = (Array.isArray(transforms) ? transforms : []).find((transform) => transform && typeof transform === "object" && transform.timeUnit);
		if (timeUnitTransform?.field) return timeUnitTransform.field;
		const xField = encoding?.x?.field;
		return typeof xField === "string" && xField.length > 0 ? xField : null;
	}
	function inferRawValueField$2(spec, encoding, transforms) {
		const aggregateTransform = (Array.isArray(transforms) ? transforms : []).find((transform) => transform && typeof transform === "object" && Array.isArray(transform.aggregate) && transform.aggregate.length > 0);
		if (aggregateTransform?.aggregate?.[0]?.field) return aggregateTransform.aggregate[0].field;
		const yField = encoding?.y?.field;
		return typeof yField === "string" && yField.length > 0 ? yField.replace(/^total_/, "").replace(/^sum_/, "") : null;
	}
	function lineCapabilities(spec) {
		const encoding = rootEncoding(spec);
		const transforms = Array.isArray(spec?.transform) ? spec.transform : [];
		const xField = typeof encoding?.x?.field === "string" && encoding.x.field.length > 0 ? encoding.x.field : null;
		const yField = typeof encoding?.y?.field === "string" && encoding.y.field.length > 0 ? encoding.y.field : null;
		const groupingField = typeof encoding?.color?.field === "string" && encoding.color.field.length > 0 ? encoding.color.field : typeof encoding?.detail?.field === "string" && encoding.detail.field.length > 0 ? encoding.detail.field : null;
		const hasTemporalAxis = encoding?.x?.type === "temporal" || encoding?.y?.type === "temporal" || typeof encoding?.x?.timeUnit === "string" || typeof encoding?.y?.timeUnit === "string";
		return {
			hasXField: Boolean(xField),
			hasXYFields: Boolean(xField && yField),
			hasGroupingField: Boolean(groupingField),
			hasTemporalAxis,
			hasDrilldownFields: Boolean(inferRawTimeField$2(spec, encoding, transforms) && inferRawValueField$2(spec, encoding, transforms))
		};
	}
	function filterDescriptorsBySpec$2(descriptors, widgetSpec) {
		if (!widgetSpec) return descriptors;
		const { hasXField, hasXYFields, hasGroupingField, hasTemporalAxis, hasDrilldownFields } = lineCapabilities(widgetSpec);
		return descriptors.filter((descriptor) => {
			if (!descriptor?.name) return true;
			if (descriptor.name === "line.selectXValue" || descriptor.name === "line.zoomXRegion") return hasXField;
			if (descriptor.name === "line.focusLines" || descriptor.name === "line.boldLines" || descriptor.name === "line.filterLines") return hasGroupingField;
			if (descriptor.name === "line.highlightTrend" || descriptor.name === "line.showMovingAverage") return hasXYFields;
			if (descriptor.name === "line.drillDownXAxis" || descriptor.name === "line.resetDrilldownXAxis") return hasTemporalAxis && hasDrilldownFields;
			if (descriptor.name === "line.resampleXAxis" || descriptor.name === "line.resetResampleXAxis") return hasTemporalAxis;
			return true;
		});
	}
	function buildLineActionDescriptors({ widgetSpec = null } = {}) {
		return filterDescriptorsBySpec$2(DESCRIPTORS$2, widgetSpec).map(clone$46);
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/line/perception.js
	function inferFieldFromSpec(rawSpec, channel) {
		if (rawSpec?.encoding?.[channel]?.field) return rawSpec.encoding[channel].field;
		if (Array.isArray(rawSpec?.layer) && rawSpec.layer.length > 0) {
			for (const layer of rawSpec.layer) if (layer?.encoding?.[channel]?.field) return layer.encoding[channel].field;
		}
		return null;
	}
	function buildLineAnomalyResult({ rows, rawSpec, threshold = 2, yField = null, xField = null }) {
		const resolvedYField = yField || inferFieldFromSpec(rawSpec, "y");
		const resolvedXField = xField || inferFieldFromSpec(rawSpec, "x");
		if (!resolvedYField) throw new Error("perception.detectAnomalies requires a yField or a line spec with an encoded y field.");
		const numericValues = rows.map((row) => Number(row?.[resolvedYField])).filter((value) => Number.isFinite(value));
		if (numericValues.length < 3) throw new Error("perception.detectAnomalies requires at least 3 numeric values.");
		const mean = numericValues.reduce((sum, value) => sum + value, 0) / numericValues.length;
		const variance = numericValues.reduce((sum, value) => {
			const delta = value - mean;
			return sum + delta * delta;
		}, 0) / numericValues.length;
		const std = Math.sqrt(variance);
		const anomalies = rows.filter((row) => {
			const value = Number(row?.[resolvedYField]);
			return Number.isFinite(value) && Math.abs(value - mean) > threshold * std;
		}).map((row) => ({
			...resolvedXField && row?.[resolvedXField] != null ? { [resolvedXField]: row[resolvedXField] } : {},
			[resolvedYField]: row?.[resolvedYField]
		}));
		return {
			operation: "detect_anomalies",
			anomaly_count: anomalies.length,
			anomalies: anomalies.slice(0, 10),
			stats: {
				mean: Math.round(mean * 100) / 100,
				std: Math.round(std * 100) / 100,
				threshold,
				sample_size: numericValues.length,
				yField: resolvedYField,
				xField: resolvedXField
			},
			message: `Detected ${anomalies.length} anomalies (threshold=${threshold} std)`
		};
	}
	function buildLinePerceptionDescriptors({ dataRef }) {
		return [
			makePerceptionDescriptor({
				name: "perception.detectAnomalies",
				title: "Detect anomalies",
				description: appendQueryScopeGuidance("Detect likely anomalous points in the visible line data using a standard-deviation threshold on the y metric."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					threshold: { type: "number" },
					yField: { type: "string" },
					xField: { type: "string" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["outlierEvidence", "trendEvidence"],
				examples: [{
					userGoal: "Identify likely anomalous line points without changing the chart encoding.",
					params: { threshold: 2 }
				}, buildQueryScopeExample({
					userGoal: "Detect anomalies inside one selected line subset.",
					params: { threshold: 2 },
					widgetRef: "wl://demo/workspace/main/widget/line_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/line_a/selection/current"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findExtremes",
				title: "Find extremes",
				description: appendQueryScopeGuidance("Return top-k or bottom-k visible rows by a numeric field in the line view."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					field: { type: "string" },
					direction: { type: "string" },
					limit: { type: "number" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["trendEvidence", "rankEvidence"],
				examples: [{
					userGoal: "Find extrema in the visible line series data.",
					params: {
						field: "Miles_per_Gallon",
						direction: "max",
						limit: 5
					}
				}, buildQueryScopeExample({
					userGoal: "Find extremes inside one selected line subset.",
					params: {
						field: "Miles_per_Gallon",
						direction: "max",
						limit: 5
					},
					widgetRef: "wl://demo/workspace/main/widget/line_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/line_a/selection/current"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.compareGroups",
				title: "Compare groups",
				description: appendQueryScopeGuidance("Compare grouped trend values in the visible line data."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					groupField: { type: "string" },
					valueField: { type: "string" },
					groups: {
						type: "array",
						items: { type: "string" }
					}
				}),
				sideEffectFree: true,
				evidenceKinds: ["groupComparison", "trendEvidence"],
				examples: [{
					userGoal: "Compare the trends or magnitudes of a few line groups.",
					params: {
						groupField: "Origin",
						valueField: "Miles_per_Gallon",
						groups: ["Japan", "USA"]
					}
				}, buildQueryScopeExample({
					userGoal: "Compare grouped line values inside one selected subset.",
					params: {
						groupField: "Origin",
						valueField: "Miles_per_Gallon",
						groups: ["Japan", "USA"]
					},
					widgetRef: "wl://demo/workspace/main/widget/line_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/line_a/selection/current"
				})]
			})
		];
	}
	function registerLinePerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.detectAnomalies", { supportedWidgetKinds: ["line"] })) perceptionRegistry.register({
			name: "perception.detectAnomalies",
			supportedWidgetKinds: ["line"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "line" });
			const params = ctx.readCallParams();
			const { rows } = ctx.resolveRowsForWidget(targetWidget, params);
			const result = buildLineAnomalyResult({
				rows,
				rawSpec: targetWidget?.rawSpec || targetWidget?.spec || null,
				threshold: Number.isFinite(params?.threshold) ? Number(params.threshold) : 2,
				yField: params?.yField || null,
				xField: params?.xField || null
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result };
		}, { supportedWidgetKinds: ["line"] });
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["line"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["line"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "line" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["line"] });
		if (!perceptionRegistry.has("perception.compareGroups", { supportedWidgetKinds: ["line"] })) perceptionRegistry.register({
			name: "perception.compareGroups",
			supportedWidgetKinds: ["line"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "line" });
			const params = ctx.readCallParams();
			const comparison = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "compareGroups",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: comparison,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["line"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/line/index.js
	function clone$45(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var LINE_LOCAL_SELECTION_CONTRACT = Object.freeze({
		localSelectionFamily: "category",
		selectionKinds: ["category", "point"],
		sourceActionNames: ["line.selectSeries", "line.selectXValue"],
		cardinality: "singleActiveSelection",
		selectionValueShape: "categoricalValues",
		observationFields: [
			"state.selections",
			"coordination.localSelectionRefs",
			"coordination.widgetSelectionRef",
			"selection.activeSelectionRef",
			"selection.activeSelectionKind",
			"selection.activeSelectionSummary"
		],
		perceptionExpectations: ["selection-scoped perception queries should resolve the currently selected line series subset", "selection summaries should preserve the selected series identity for downstream comparison and anomaly checks"]
	});
	var LINE_VERIFICATION_CONTRACT = Object.freeze({
		preferredReadMethod: "readVerificationState",
		preferredObservationFields: [
			"verification.checks.selectionApplied",
			"verification.checks.filterApplied",
			"verification.checks.aggregateApplied",
			"verification.checks.drillDownApplied",
			"verification.checks.focusApplied",
			"verification.checks.navigateApplied",
			"verification.checks.zoomApplied",
			"verification.checks.highlightApplied",
			"verification.encodings.channels",
			"verification.view.aggregate",
			"verification.view.drillDown",
			"verification.view.focusKeys",
			"verification.view.reencode",
			"verification.view.navigate",
			"verification.view.highlight",
			"verification.transforms.kinds"
		],
		supportedEffectTypes: [
			"selection",
			"filter",
			"aggregate",
			"drillDown",
			"focus",
			"reencode",
			"navigate",
			"zoom",
			"highlight",
			"encoding",
			"linkedPropagation"
		],
		effectChecks: {
			selection: [
				"checks.selectionApplied",
				"selections.count",
				"selections.activeSummary"
			],
			filter: [
				"checks.filterApplied",
				"data.rowCount",
				"data.visibleCount",
				"transforms.kinds"
			],
			aggregate: [
				"checks.aggregateApplied",
				"view.aggregate",
				"encodings.aggregateChannels"
			],
			drillDown: [
				"checks.drillDownApplied",
				"view.drillDown",
				"transforms.kinds"
			],
			focus: ["checks.focusApplied", "view.focusKeys"],
			reencode: [
				"checks.reencodeApplied",
				"view.reencode",
				"encodings.channels"
			],
			navigate: ["checks.navigateApplied", "view.navigate"],
			zoom: [
				"checks.zoomApplied",
				"view.xDomain",
				"view.zoom"
			],
			highlight: [
				"checks.highlightApplied",
				"view.highlight",
				"feedback.highlightKeyCount",
				"feedback.sharedSelectionSourceWidgetId"
			],
			encoding: [
				"checks.encodingReadable",
				"encodings.channels",
				"encodings.fieldsByChannel"
			],
			linkedPropagation: [
				"checks.linkedPropagationApplied",
				"feedback.linkedSourceRefCount",
				"feedback.sharedSelectionSourceWidgetId"
			]
		}
	});
	function describeLineLocalSelectionContract() {
		return clone$45(LINE_LOCAL_SELECTION_CONTRACT);
	}
	function describeLineVerificationContract() {
		return clone$45(LINE_VERIFICATION_CONTRACT);
	}
	function describeLineWidgetContract() {
		return {
			kind: "line",
			actionNames: [
				"line.selectSeries",
				"line.selectXValue",
				"line.zoomXRegion",
				"line.focusLines",
				"line.highlightTrend",
				"line.showMovingAverage",
				"line.drillDownXAxis",
				"line.resetDrilldownXAxis",
				"line.resampleXAxis",
				"line.resetResampleXAxis",
				"line.boldLines",
				"line.filterLines"
			],
			perceptionNames: [
				"perception.detectAnomalies",
				"perception.findExtremes",
				"perception.compareGroups"
			],
			localSelection: describeLineLocalSelectionContract(),
			verification: describeLineVerificationContract()
		};
	}
	var lineFamily = Object.freeze({
		kind: "line",
		describeContract: describeLineWidgetContract,
		actions: Object.freeze({ buildDescriptors: buildLineActionDescriptors }),
		perception: Object.freeze({
			buildDescriptors: buildLinePerceptionDescriptors,
			register: registerLinePerceptionQueries
		}),
		interactionProfile: Object.freeze({ getConfig: getLineHumanInteractionConfig }),
		playbook: linePlaybook
	});
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/parallelCoordinates/interactionProfile.js
	function getParallelCoordinatesHumanInteractionConfig() {
		return {
			mode: "multiBrush",
			actionName: "parallelCoordinates.brushAxes",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/parallelCoordinates/playbook.js
	var parallelCoordinatesPlaybook = {
		analysisToAction: [
			{
				goal: "Identify a coherent subgroup",
				workflow: "If the goal is to identify a subgroup with a shared multi-dimensional profile, first find a subset through axis brushing or category filtering, then inspect it across dimensions.",
				candidateActions: [
					"parallelCoordinates.brushAxes",
					"parallelCoordinates.filterDimension",
					"parallelCoordinates.filterByCategory",
					"parallelCoordinates.selectRecord"
				],
				candidatePerceptions: ["perception.findOutliers"]
			},
			{
				goal: "Detect anomalous profiles",
				workflow: "If the goal is to find unusual records, isolate lines that diverge sharply from the dominant bundles and compare them against the main population.",
				candidateActions: [
					"parallelCoordinates.selectRecord",
					"parallelCoordinates.brushAxes",
					"parallelCoordinates.filterDimension"
				],
				candidatePerceptions: ["perception.findOutliers"]
			},
			{
				goal: "Analyze behavior by category",
				workflow: "If the goal is to analyze a category-defined subgroup, filter or highlight the relevant categories first, then inspect how their paths move across axes.",
				candidateActions: ["parallelCoordinates.filterByCategory", "parallelCoordinates.highlightCategory"],
				candidatePerceptions: ["perception.findOutliers"]
			},
			{
				goal: "Inspect the relationship between specific axes",
				workflow: "If the goal is to inspect the relationship between two dimensions, bring those axes next to each other and reduce clutter from unrelated axes before interpreting crossings or separation.",
				candidateActions: [
					"parallelCoordinates.reorderDimensions",
					"parallelCoordinates.hideDimensions",
					"parallelCoordinates.resetHiddenDimensions"
				],
				candidatePerceptions: ["perception.findOutliers"]
			},
			{
				goal: "Define a subgroup through multi-axis constraints",
				workflow: "If the goal is to understand a subgroup defined by several constraints, constrain one or more decisive axes first, then inspect how the surviving subset behaves on the remaining dimensions.",
				candidateActions: ["parallelCoordinates.brushAxes", "parallelCoordinates.filterDimension"],
				candidatePerceptions: ["perception.findOutliers"]
			}
		],
		workflows: [
			{
				name: "filter_category_then_reorder_dimensions",
				analysisIntent: "Subgroup profile across reordered dimensions",
				scenarioExamples: ["For one penguin species, how does beak depth relate to flipper length after placing those axes together?", "For the northwest insurance region, does BMI show a clear relationship with charges after reordering dimensions?"],
				description: "Use when the task asks how a category-defined subgroup relates across a few key dimensions.",
				steps: [
					{ action: "parallelCoordinates.filterByCategory" },
					{ action: "parallelCoordinates.reorderDimensions" },
					{ perception: "perception.findOutliers" }
				]
			},
			{
				name: "filter_category_then_filter_category",
				analysisIntent: "Multi-constraint cohort profiling",
				scenarioExamples: ["How do Series A and Series C startup periods exhibit similar funding patterns after filtering each group?", "After narrowing to two retail channels, how do discount and final price patterns compare?"],
				description: "Use when several categorical constraints define the subgroup before reading the path pattern.",
				steps: [
					{ action: "parallelCoordinates.filterByCategory" },
					{ action: "parallelCoordinates.filterByCategory" },
					{ perception: "perception.findOutliers" }
				]
			},
			{
				name: "hide_dimensions_then_filter_category",
				analysisIntent: "Low-clutter subgroup comparison",
				scenarioExamples: ["After keeping only weight and calories axes, which workout type burns more calories at similar weights?", "After hiding unrelated axes, how do selected game genres distribute during platform development?"],
				description: "Use when only a few axes are relevant and the subgroup should be isolated after reducing visual clutter.",
				steps: [
					{ action: "parallelCoordinates.hideDimensions" },
					{ action: "parallelCoordinates.filterByCategory" },
					{ perception: "perception.findOutliers" }
				]
			},
			{
				name: "filter_category_then_hide_dimensions",
				analysisIntent: "Known subgroup profile simplification",
				scenarioExamples: ["For chocolate-free candies, is there a clear relationship between sugar percent and win percent after irrelevant axes are hidden?", "For high-importance products, do discount and prior-purchase patterns remain visible after simplifying the axes?"],
				description: "Use when the subgroup is known first and then irrelevant axes should be removed to read its profile.",
				steps: [
					{ action: "parallelCoordinates.filterByCategory" },
					{ action: "parallelCoordinates.hideDimensions" },
					{ perception: "perception.findOutliers" }
				]
			},
			{
				name: "highlight_category_then_reorder_dimensions",
				analysisIntent: "Highlighted category trajectory comparison",
				scenarioExamples: ["After highlighting the AI-usage category, how does study hour impact appear across adjacent score dimensions?", "For highlighted candy categories, does sugar percent align with win percent after reordering axes?"],
				description: "Use when the task needs comparison of highlighted category paths after putting key axes next to each other.",
				steps: [
					{ action: "parallelCoordinates.highlightCategory" },
					{ action: "parallelCoordinates.reorderDimensions" },
					{ perception: "perception.findOutliers" }
				]
			},
			{
				name: "hide_dimensions_then_reorder_dimensions",
				analysisIntent: "Focused dimension relationship reading",
				scenarioExamples: ["How does AI tool usage relate to code complexity compared with bug count after hiding unrelated dimensions?", "Do selected health measures move together once only the relevant axes are visible and adjacent?"],
				description: "Use when the task asks for relationship reading between specific axes and the full chart is too cluttered.",
				steps: [
					{ action: "parallelCoordinates.hideDimensions" },
					{ action: "parallelCoordinates.reorderDimensions" },
					{ perception: "perception.findOutliers" }
				]
			}
		],
		multiTurnStrategy: [
			"First decide whether the query is about subgroup discovery, anomaly detection, category comparison, or dimension-relationship reading.",
			"If category matters, separate the relevant category before interpreting path structure.",
			"If the relationship between dimensions matters, reorder axes before reading crossings or aligned trends.",
			"If clutter is too high, collapse or suppress irrelevant dimensions before continuing.",
			"Stop when the selected subgroup or adjacent-axis relationship is clear enough to answer the query directly."
		]
	};
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/parallelCoordinates/actionDescriptors.js
	var DESCRIPTORS$1 = [
		{
			"name": "parallelCoordinates.brushAxes",
			"description": "Select rows whose values fall inside one or more axis-aligned numeric ranges.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"rules": {
						"type": "array",
						"minItems": 1,
						"items": {
							"type": "object",
							"properties": {
								"field": { "type": "string" },
								"range": {
									"type": "array",
									"items": { "type": "number" },
									"minItems": 2,
									"maxItems": 2
								}
							},
							"required": ["field", "range"]
						}
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["rules"]
			},
			"examples": [{
				"userGoal": "Restrict analysis to a multivariate value corridor.",
				"params": { "rules": [{
					"field": "Horsepower",
					"range": [80, 160]
				}, {
					"field": "Weight_in_lbs",
					"range": [1800, 3200]
				}] }
			}]
		},
		{
			"name": "parallelCoordinates.selectRecord",
			"description": "Select one visible record by id so linked views can focus or compare that record without needing a brush gesture.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"recordId": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
					"field": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["recordId"]
			},
			"examples": [{
				"userGoal": "Select one car record in the parallel view before checking the same car across other linked views.",
				"params": {
					"field": "id",
					"recordId": "toyota-corona-mark-ii"
				}
			}]
		},
		{
			"name": "parallelCoordinates.reorderDimensions",
			"description": "Reorder the visible dimension axes of a parallel coordinates view by rewriting the fold order and matching x-axis domain metadata.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"dimensionOrder": {
						"type": "array",
						"minItems": 1,
						"items": { "type": "string" }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["dimensionOrder"]
			},
			"examples": [{
				"userGoal": "Move the most important dimensions to the front before comparing multivariate trends.",
				"params": { "dimensionOrder": [
					"Weight",
					"Horsepower",
					"Miles_per_Gallon"
				] }
			}]
		},
		{
			"name": "parallelCoordinates.filterDimension",
			"description": "Filter rows by a numeric range on one named dimension, inserting the predicate before the fold stage when the view is defined in wide format.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"dimension": { "type": "string" },
					"range": {
						"type": "array",
						"minItems": 2,
						"maxItems": 2,
						"items": { "type": "number" }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["dimension", "range"]
			},
			"examples": [{
				"userGoal": "Keep only rows whose horsepower falls inside a specific corridor before comparing the remaining multivariate trajectories.",
				"params": {
					"dimension": "Horsepower",
					"range": [80, 160]
				}
			}]
		},
		{
			"name": "parallelCoordinates.filterByCategory",
			"description": "Exclude rows whose category field matches one or more requested values, inserting the predicate before the fold stage when the view is defined in wide format.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"field": { "type": "string" },
					"values": { "oneOf": [{ "type": "string" }, {
						"type": "array",
						"minItems": 1,
						"items": { "type": "string" }
					}] },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["field", "values"]
			},
			"examples": [{
				"userGoal": "Exclude one or more categories before comparing the remaining multivariate trajectories.",
				"params": {
					"field": "Origin",
					"values": ["USA", "Japan"]
				}
			}]
		},
		{
			"name": "parallelCoordinates.highlightCategory",
			"description": "Visually emphasize one or more category values by keeping matching trajectories fully opaque and dimming the rest.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"field": { "type": "string" },
					"values": { "oneOf": [{ "type": "string" }, {
						"type": "array",
						"minItems": 1,
						"items": { "type": "string" }
					}] },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["field", "values"]
			},
			"examples": [{
				"userGoal": "Highlight a few categories before comparing their multivariate trajectories against the background population.",
				"params": {
					"field": "Origin",
					"values": ["USA", "Japan"]
				}
			}]
		},
		{
			"name": "parallelCoordinates.hideDimensions",
			"description": "Temporarily hide one or more dimensions from a parallel coordinates view while preserving the original full dimension order for later restoration.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"dimensions": {
						"type": "array",
						"minItems": 1,
						"items": { "type": "string" }
					},
					"mode": {
						"type": "string",
						"enum": ["hide", "show"]
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["dimensions"]
			},
			"examples": [{
				"userGoal": "Temporarily hide weight from a crowded parallel-coordinates view.",
				"params": { "dimensions": ["Weight"] }
			}, {
				"userGoal": "Restore one previously hidden dimension without resetting the full view.",
				"params": {
					"dimensions": ["Weight"],
					"mode": "show"
				}
			}]
		},
		{
			"name": "parallelCoordinates.resetHiddenDimensions",
			"description": "Restore the full original dimension order after one or more dimensions have been hidden from a parallel coordinates view.",
			"paramsSchema": {
				"type": "object",
				"properties": { "queryScope": {
					"type": "object",
					"additionalProperties": false,
					"properties": {
						"dataRef": { "type": "string" },
						"selectionRef": { "type": "string" },
						"focusRef": { "type": "string" },
						"viewportRef": { "type": "string" }
					}
				} }
			},
			"examples": [{
				"userGoal": "Restore every temporarily hidden dimension after a focused inspection pass.",
				"params": {}
			}]
		}
	];
	function clone$44(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function parallelCoordinatesCapabilities(spec) {
		const foldTransform = (Array.isArray(spec?.transform) ? spec.transform : []).find((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
		const hiddenState = spec?._pc_hidden_state && typeof spec._pc_hidden_state === "object" ? spec._pc_hidden_state : null;
		const allDimensions = Array.isArray(hiddenState?.all_dimensions) && hiddenState.all_dimensions.length > 0 ? hiddenState.all_dimensions : Array.isArray(foldTransform?.fold) ? foldTransform.fold : [];
		return {
			hasDimensionList: Array.isArray(allDimensions) && allDimensions.length > 0,
			hasHiddenDimensionsState: Array.isArray(hiddenState?.all_dimensions) && hiddenState.all_dimensions.length > 0 && Array.isArray(hiddenState?.hidden) && hiddenState.hidden.length > 0
		};
	}
	function filterDescriptorsBySpec$1(descriptors, widgetSpec) {
		if (!widgetSpec) return descriptors;
		const { hasDimensionList, hasHiddenDimensionsState } = parallelCoordinatesCapabilities(widgetSpec);
		return descriptors.filter((descriptor) => {
			if (!descriptor?.name) return true;
			if (descriptor.name === "parallelCoordinates.reorderDimensions" || descriptor.name === "parallelCoordinates.hideDimensions") return hasDimensionList;
			if (descriptor.name === "parallelCoordinates.resetHiddenDimensions") return hasHiddenDimensionsState;
			return true;
		});
	}
	function buildParallelCoordinatesActionDescriptors({ widgetSpec = null } = {}) {
		return filterDescriptorsBySpec$1(DESCRIPTORS$1, widgetSpec).map(clone$44);
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/parallelCoordinates/perception.js
	function buildParallelCoordinatesPerceptionDescriptors({ dataRef }) {
		return [makePerceptionDescriptor({
			name: "perception.findOutliers",
			title: "Find outliers",
			description: appendQueryScopeGuidance("Return likely outlier rows in the visible parallel coordinates data."),
			category: "compute",
			targetRef: dataRef,
			paramsSchema: buildScopedPerceptionParamsSchema({
				field: { type: "string" },
				zThreshold: { type: "number" },
				limit: { type: "number" }
			}),
			sideEffectFree: true,
			evidenceKinds: ["outlierEvidence", "multivariateEvidence"],
			examples: [{
				userGoal: "Find multivariate outliers in the visible parallel coordinates subset.",
				params: {
					field: "Horsepower",
					zThreshold: 2.5,
					limit: 10
				}
			}, buildQueryScopeExample({
				userGoal: "Find multivariate outliers inside one brushed parallel-coordinates subset.",
				params: {
					field: "Horsepower",
					zThreshold: 2.5,
					limit: 10
				},
				widgetRef: "wl://demo/workspace/main/widget/parallel_a",
				dataRef: "wl://demo/workspace/main/data/current_selection",
				selectionRef: "wl://demo/workspace/main/widget/parallel_a/selection/brush"
			})]
		})];
	}
	function registerParallelCoordinatesPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.findOutliers", { supportedWidgetKinds: ["parallelCoordinates"] })) perceptionRegistry.register({
			name: "perception.findOutliers",
			supportedWidgetKinds: ["parallelCoordinates"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "parallelCoordinates" });
			const params = ctx.readCallParams();
			const outliers = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findOutliers",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: outliers,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["parallelCoordinates"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/parallelCoordinates/index.js
	function clone$43(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var PARALLEL_COORDINATES_LOCAL_SELECTION_CONTRACT = Object.freeze({
		localSelectionFamily: "interval",
		selectionKinds: ["interval", "record"],
		sourceActionNames: ["parallelCoordinates.brushAxes", "parallelCoordinates.selectRecord"],
		cardinality: "singleActiveSelection",
		selectionValueShape: "multivariateAxisIntervalsOrRecordId",
		observationFields: [
			"state.selections",
			"coordination.localSelectionRefs",
			"coordination.widgetSelectionRef",
			"selection.activeSelectionRef",
			"selection.activeSelectionKind",
			"selection.activeSelectionSummary",
			"selection.activeSelectionFields"
		],
		perceptionExpectations: [
			"selection-scoped perception queries should resolve rows that satisfy all brushed axis rules",
			"selection summaries should expose the brushed dimension list and interval predicates",
			"record-level selections should preserve the selected record identifier for downstream focus and comparison"
		]
	});
	var PARALLEL_COORDINATES_VERIFICATION_CONTRACT = Object.freeze({
		preferredReadMethod: "readVerificationState",
		preferredObservationFields: [
			"verification.checks.selectionApplied",
			"verification.checks.filterApplied",
			"verification.checks.highlightApplied",
			"verification.checks.reencodeApplied",
			"verification.checks.focusApplied",
			"verification.selections.activeKinds",
			"verification.view.highlight",
			"verification.view.reencode",
			"verification.transforms.kinds"
		],
		supportedEffectTypes: [
			"selection",
			"filter",
			"highlight",
			"focus",
			"reencode",
			"encoding",
			"linkedPropagation"
		],
		effectChecks: {
			selection: [
				"checks.selectionApplied",
				"selections.count",
				"selections.activeKinds",
				"selections.activeSummary"
			],
			filter: [
				"checks.filterApplied",
				"data.rowCount",
				"data.visibleCount",
				"transforms.kinds"
			],
			highlight: [
				"checks.highlightApplied",
				"view.highlight",
				"feedback.highlightKeyCount",
				"feedback.sharedSelectionSourceWidgetId"
			],
			focus: ["checks.focusApplied", "view.focusKeys"],
			reencode: [
				"checks.reencodeApplied",
				"view.reencode",
				"encodings.channels"
			],
			encoding: [
				"checks.encodingReadable",
				"encodings.channels",
				"encodings.fieldsByChannel"
			],
			linkedPropagation: [
				"checks.linkedPropagationApplied",
				"feedback.linkedSourceRefCount",
				"feedback.sharedSelectionSourceWidgetId"
			]
		}
	});
	function describeParallelCoordinatesLocalSelectionContract() {
		return clone$43(PARALLEL_COORDINATES_LOCAL_SELECTION_CONTRACT);
	}
	function describeParallelCoordinatesVerificationContract() {
		return clone$43(PARALLEL_COORDINATES_VERIFICATION_CONTRACT);
	}
	function describeParallelCoordinatesWidgetContract() {
		return {
			kind: "parallelCoordinates",
			actionNames: [
				"parallelCoordinates.brushAxes",
				"parallelCoordinates.selectRecord",
				"parallelCoordinates.reorderDimensions",
				"parallelCoordinates.filterDimension",
				"parallelCoordinates.filterByCategory",
				"parallelCoordinates.highlightCategory",
				"parallelCoordinates.hideDimensions",
				"parallelCoordinates.resetHiddenDimensions"
			],
			perceptionNames: ["perception.findOutliers"],
			localSelection: describeParallelCoordinatesLocalSelectionContract(),
			verification: describeParallelCoordinatesVerificationContract()
		};
	}
	var parallelCoordinatesFamily = Object.freeze({
		kind: "parallelCoordinates",
		describeContract: describeParallelCoordinatesWidgetContract,
		actions: Object.freeze({ buildDescriptors: buildParallelCoordinatesActionDescriptors }),
		perception: Object.freeze({
			buildDescriptors: buildParallelCoordinatesPerceptionDescriptors,
			register: registerParallelCoordinatesPerceptionQueries
		}),
		interactionProfile: Object.freeze({ getConfig: getParallelCoordinatesHumanInteractionConfig }),
		playbook: parallelCoordinatesPlaybook
	});
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/sankey/interactionProfile.js
	function getSankeyHumanInteractionConfig() {
		return {
			mode: "categoryClick",
			actionName: "sankey.focusFlow",
			categoryFieldChannel: "color",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/sankey/playbook.js
	var sankeyPlaybook = {
		analysisToAction: [
			{
				goal: "Understand the main flow structure",
				workflow: "If the goal is to understand the major structure of the flow graph, first identify dominant paths and bottleneck nodes, then focus on the branches carrying the most informative volume.",
				candidateActions: [
					"sankey.focusFlow",
					"sankey.highlightPath",
					"sankey.traceNode"
				],
				candidatePerceptions: [
					"perception.findBottleneck",
					"perception.findExtremes",
					"perception.compareGroups"
				]
			},
			{
				goal: "Explain where one source ends up",
				workflow: "If the goal is to trace one source downstream, isolate that source first and follow how its flow splits across later stages.",
				candidateActions: [
					"sankey.selectAggregateNode",
					"sankey.focusFlow",
					"sankey.traceNode"
				],
				candidatePerceptions: ["perception.compareGroups", "perception.findExtremes"]
			},
			{
				goal: "Compare two branches or destinations",
				workflow: "If the goal is to compare two flow branches, compare their path width, intermediates, and concentration patterns rather than relying only on endpoints.",
				candidateActions: [
					"sankey.highlightPath",
					"sankey.filterFlow",
					"sankey.reorderNodesInLayer"
				],
				candidatePerceptions: ["perception.compareGroups", "perception.findExtremes"]
			},
			{
				goal: "Inspect one node and its surrounding flows",
				workflow: "If the goal is to understand one node, focus that node and trace all incoming and outgoing flows attached to it before comparing which directions dominate.",
				candidateActions: [
					"sankey.selectAggregateNode",
					"sankey.traceNode",
					"sankey.focusFlow"
				],
				candidatePerceptions: [
					"perception.getNodeOptions",
					"perception.findExtremes",
					"perception.compareGroups"
				]
			},
			{
				goal: "Analyze conversion or drop-off",
				workflow: "If the goal is to understand conversion efficiency or loss, trace the relevant path and quantify how much flow survives across stages.",
				candidateActions: [
					"sankey.highlightPath",
					"sankey.filterFlow",
					"sankey.focusFlow"
				],
				candidatePerceptions: ["perception.calculateConversionRate", "perception.findBottleneck"]
			},
			{
				goal: "Reduce clutter or reveal hidden structure",
				workflow: "If the layout is too cluttered or too coarse for the query, simplify or expand the node structure first, then re-read the main branches after the structure becomes more interpretable.",
				candidateActions: [
					"sankey.collapseNodes",
					"sankey.expandNode",
					"sankey.autoCollapseByRank",
					"sankey.filterFlow"
				],
				candidatePerceptions: ["perception.findBottleneck", "perception.findExtremes"]
			}
		],
		workflows: [
			{
				name: "highlight_path_then_calculate_conversion",
				analysisIntent: "Path conversion analysis",
				scenarioExamples: ["What is the conversion rate from medication to cured and discharged?", "Along the paid ads to purchase path, where does the funnel lose the most users?"],
				description: "Use when the task asks for conversion, survival, or drop-off along a specific flow path.",
				steps: [{ action: "sankey.highlightPath" }, { perception: "perception.calculateConversionRate" }]
			},
			{
				name: "filter_flow_then_trace_node",
				analysisIntent: "High-volume path tracing",
				scenarioExamples: ["After removing small flows, what is the largest downstream outcome for Engineering students?", "Which destinations dominate once low-volume supply-chain flows are filtered out?"],
				description: "Use when weak flows should be removed before tracing one node downstream or upstream.",
				steps: [
					{ action: "sankey.filterFlow" },
					{ action: "sankey.traceNode" },
					{ perception: "perception.compareGroups" }
				]
			},
			{
				name: "calculate_conversion_then_highlight_path",
				analysisIntent: "Metric-guided path explanation",
				scenarioExamples: ["Which treatment path deserves inspection after comparing conversion rates across discharge outcomes?", "After calculating conversion for each funnel node, which path should be highlighted to explain performance?"],
				description: "Use when the conversion metric identifies which path should be visually inspected.",
				steps: [{ perception: "perception.calculateConversionRate" }, { action: "sankey.highlightPath" }]
			},
			{
				name: "highlight_path_then_find_extremes",
				analysisIntent: "Extreme flow lookup in path context",
				scenarioExamples: ["After highlighting the AAA loan path, how many loans transition from AAA Y2 to AAA Y3?", "Inside the selected funding path, which link carries the largest amount?"],
				description: "Use when the task asks for counts, largest links, or smallest links inside a selected path context.",
				steps: [{ action: "sankey.highlightPath" }, { perception: "perception.findExtremes" }]
			},
			{
				name: "collapse_nodes_then_calculate_conversion",
				analysisIntent: "Aggregated conversion analysis",
				scenarioExamples: ["After collapsing non-recycled product nodes, what is the conversion rate for recycled products?", "When low-level campaign channels are grouped, what percentage of users still purchase?"],
				description: "Use when node-level noise should be aggregated before reading conversion or loss.",
				steps: [{ action: "sankey.collapseNodes" }, { perception: "perception.calculateConversionRate" }]
			},
			{
				name: "highlight_path_then_collapse_nodes",
				analysisIntent: "Simplified path explanation",
				scenarioExamples: ["After tracing renewables, collapse non-renewable nodes to explain the main energy-flow contrast.", "Highlight the operating-fund path, then group surrounding expense nodes to clarify where money flows."],
				description: "Use when a key path is known but surrounding non-key nodes need aggregation to reveal structure.",
				steps: [
					{ action: "sankey.highlightPath" },
					{ action: "sankey.collapseNodes" },
					{ perception: "perception.compareGroups" }
				]
			}
		],
		multiTurnStrategy: [
			"First decide whether the query is about path tracing, node analysis, branch comparison, or conversion analysis.",
			"If the graph is too cluttered, simplify or expand structure before interpreting it.",
			"Then isolate the branch, node, or source-to-target path that matters to the query.",
			"After the target flow structure is stable, trace it stage by stage and read loss, concentration, or branching behavior.",
			"Stop when the selected path or node-level flow evidence is specific enough to support a direct answer."
		]
	};
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/sankey/actionDescriptors.js
	var DESCRIPTORS = [
		{
			"name": "sankey.focusFlow",
			"description": "Focus one or more flow categories or nodes in the current Sankey view.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"field": { "type": "string" },
					"values": {
						"type": "array",
						"items": {},
						"minItems": 1
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["field", "values"]
			},
			"examples": [{
				"userGoal": "Focus a subset of flows before investigating bottlenecks.",
				"params": {
					"field": "source",
					"values": ["A"]
				}
			}]
		},
		{
			"name": "sankey.selectAggregateNode",
			"description": "Select one collapsed aggregate node by aggregate name so downstream context can focus that temporary group even when it does not map to row-level predicates.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"aggregateName": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["aggregateName"]
			},
			"examples": [{
				"userGoal": "Hold one collapsed aggregate group as the current focus before deciding whether to re-expand it.",
				"params": { "aggregateName": "collapsed:1:other" }
			}]
		},
		{
			"name": "sankey.filterFlow",
			"description": "Keep only links at or above a minimum flow value, preferably by updating the Sankey threshold signal when one exists.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"minValue": { "type": "number" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["minValue"]
			},
			"examples": [{
				"userGoal": "Hide tiny flows so the main pathways stand out more clearly.",
				"params": { "minValue": 20 }
			}]
		},
		{
			"name": "sankey.collapseNodes",
			"description": "Collapse multiple nodes into one aggregate node by rewriting the raw link list and node configuration in the current Sankey view.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"nodes": {
						"type": "array",
						"minItems": 1,
						"items": { "type": "string" }
					},
					"aggregateName": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["nodes"]
			},
			"examples": [{
				"userGoal": "Merge several low-signal source nodes into one aggregate before comparing downstream flow structure.",
				"params": {
					"nodes": ["A", "B"],
					"aggregateName": "Other Sources"
				}
			}]
		},
		{
			"name": "sankey.expandNode",
			"description": "Restore the original nodes and links for one previously collapsed aggregate node using the saved Sankey structural state.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"aggregateName": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["aggregateName"]
			},
			"examples": [{
				"userGoal": "Re-expand one aggregate group after an earlier structural simplification pass.",
				"params": { "aggregateName": "Other Sources" }
			}]
		},
		{
			"name": "sankey.highlightPath",
			"description": "Visually emphasize a multi-step path by increasing opacity for edges and nodes on the path and dimming unrelated structure.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"path": { "oneOf": [{ "type": "string" }, {
						"type": "array",
						"minItems": 2,
						"items": { "type": "string" }
					}] },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["path"]
			},
			"examples": [{
				"userGoal": "Trace one conversion route through the Sankey graph while dimming everything else.",
				"params": { "path": [
					"A",
					"B",
					"C"
				] }
			}]
		},
		{
			"name": "sankey.traceNode",
			"description": "Highlight all edges directly connected to one node and visually emphasize that node while dimming unrelated structure.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"nodeName": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["nodeName"]
			},
			"examples": [{
				"userGoal": "Trace all direct inflows and outflows for one node before deciding whether to collapse or reorder the layer.",
				"params": { "nodeName": "Checkout" }
			}]
		},
		{
			"name": "sankey.colorFlows",
			"description": "Recolor all edges directly connected to one or more nodes while leaving the unrelated edge color encoding as a fallback.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"nodes": {
						"type": "array",
						"minItems": 1,
						"items": { "type": "string" }
					},
					"color": { "type": "string" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["nodes"]
			},
			"examples": [{
				"userGoal": "Color all flows touching one or more nodes before presenting a focused Sankey story.",
				"params": {
					"nodes": ["Checkout"],
					"color": "#e74c3c"
				}
			}]
		},
		{
			"name": "sankey.reorderNodesInLayer",
			"description": "Rewrite node order values for one Sankey depth layer using an explicit top-to-bottom node order.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"depth": { "type": "number" },
					"order": {
						"type": "array",
						"minItems": 1,
						"items": { "type": "string" }
					},
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["depth", "order"]
			},
			"examples": [{
				"userGoal": "Reorder one Sankey layer to make important nodes appear first from top to bottom.",
				"params": {
					"depth": 0,
					"order": [
						"C",
						"A",
						"B"
					]
				}
			}]
		},
		{
			"name": "sankey.autoCollapseByRank",
			"description": "Keep only the top-N nodes per Sankey layer by flow volume and collapse the remainder into layer-specific aggregate nodes.",
			"paramsSchema": {
				"type": "object",
				"properties": {
					"topN": { "type": "number" },
					"queryScope": {
						"type": "object",
						"additionalProperties": false,
						"properties": {
							"dataRef": { "type": "string" },
							"selectionRef": { "type": "string" },
							"focusRef": { "type": "string" },
							"viewportRef": { "type": "string" }
						}
					}
				},
				"required": ["topN"]
			},
			"examples": [{
				"userGoal": "Simplify a large Sankey by keeping only the most important nodes in each layer.",
				"params": { "topN": 2 }
			}]
		}
	];
	function clone$42(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function findNamedDataSource$2(spec, name) {
		const data = Array.isArray(spec?.data) ? spec.data : [];
		const index = data.findIndex((entry) => entry && typeof entry === "object" && entry.name === name);
		return {
			index,
			values: index >= 0 && Array.isArray(data[index]?.values) ? data[index].values : null
		};
	}
	function findNamedMark$1(spec, name) {
		const marks = Array.isArray(spec?.marks) ? spec.marks : [];
		for (const mark of marks) {
			if (mark?.name === name) return mark;
			if (mark?.type === "group" && Array.isArray(mark.marks)) {
				const nested = mark.marks.find((entry) => entry?.name === name);
				if (nested) return nested;
			}
		}
		return null;
	}
	function sankeyCapabilities(spec) {
		const rawLinksSource = findNamedDataSource$2(spec, "rawLinks");
		const linksSource = findNamedDataSource$2(spec, "links");
		const nodesSource = findNamedDataSource$2(spec, "nodes");
		const nodeConfigSource = findNamedDataSource$2(spec, "nodeConfig");
		const collapsedGroups = spec?._sankey_state?.collapsed_groups;
		return {
			hasVegaShape: Array.isArray(spec?.data) || Array.isArray(spec?.marks),
			hasRawLinks: rawLinksSource.index >= 0 && Array.isArray(rawLinksSource.values),
			hasLinks: linksSource.index >= 0 && Array.isArray(linksSource.values),
			hasNodes: nodesSource.index >= 0 && Array.isArray(nodesSource.values),
			hasNodeConfig: nodeConfigSource.index >= 0 && Array.isArray(nodeConfigSource.values),
			hasCollapsedGroups: collapsedGroups != null && typeof collapsedGroups === "object" && !Array.isArray(collapsedGroups) && Object.keys(collapsedGroups).length > 0,
			hasEdgeMark: Boolean(findNamedMark$1(spec, "edgeMark"))
		};
	}
	function filterDescriptorsBySpec(descriptors, widgetSpec) {
		if (!widgetSpec) return descriptors;
		const { hasVegaShape, hasRawLinks, hasLinks, hasNodes, hasNodeConfig, hasCollapsedGroups, hasEdgeMark } = sankeyCapabilities(widgetSpec);
		if (!hasVegaShape) return descriptors;
		return descriptors.filter((descriptor) => {
			if (!descriptor?.name) return true;
			if (descriptor.name === "sankey.expandNode") return hasCollapsedGroups;
			if (descriptor.name === "sankey.colorFlows") return hasEdgeMark || hasLinks || hasRawLinks;
			if (descriptor.name === "sankey.reorderNodesInLayer" || descriptor.name === "sankey.autoCollapseByRank") return (hasRawLinks || hasLinks) && (hasNodeConfig || hasNodes);
			if (descriptor.name === "sankey.filterFlow" || descriptor.name === "sankey.collapseNodes" || descriptor.name === "sankey.traceNode") return hasRawLinks || hasLinks;
			return true;
		});
	}
	function buildSankeyActionDescriptors({ widgetSpec = null } = {}) {
		return filterDescriptorsBySpec(DESCRIPTORS, widgetSpec).map(clone$42);
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/sankey/perception.js
	function buildNodeFlows(links) {
		const flows = /* @__PURE__ */ new Map();
		const ensure = (name) => {
			if (!flows.has(name)) flows.set(name, {
				inflow: 0,
				outflow: 0,
				total: 0
			});
			return flows.get(name);
		};
		for (const link of Array.isArray(links) ? links : []) {
			if (!link || typeof link !== "object") continue;
			const source = link.source;
			const target = link.target;
			const value = Number(link.value || 0);
			const sourceEntry = ensure(source);
			const targetEntry = ensure(target);
			sourceEntry.outflow += value;
			targetEntry.inflow += value;
		}
		for (const entry of flows.values()) entry.total = Math.max(entry.inflow, entry.outflow);
		return flows;
	}
	function findNamedDataSource$1(spec, name) {
		const entry = (Array.isArray(spec?.data) ? spec.data : []).find((item) => item && typeof item === "object" && item.name === name) || null;
		return Array.isArray(entry?.values) ? entry.values : null;
	}
	function buildSankeyNodeOptions(rawSpec) {
		const nodeConfig = findNamedDataSource$1(rawSpec, "nodeConfig");
		const rawLinks = findNamedDataSource$1(rawSpec, "rawLinks");
		const depthLabelsData = findNamedDataSource$1(rawSpec, "depthLabelsData") || [];
		if (!nodeConfig || !rawLinks) throw new Error("perception.getNodeOptions requires rawLinks and nodeConfig data sources.");
		const nodeFlows = buildNodeFlows(rawLinks);
		const depthLabelMap = /* @__PURE__ */ new Map();
		for (const entry of depthLabelsData) {
			if (!entry || typeof entry !== "object") continue;
			depthLabelMap.set(String(entry.depth), entry.label || `Layer ${entry.depth}`);
		}
		const nodesByDepth = {};
		const allNodes = [];
		for (const node of [...nodeConfig].sort((left, right) => {
			const leftDepth = Number(left?.depth ?? 0);
			const rightDepth = Number(right?.depth ?? 0);
			if (leftDepth !== rightDepth) return leftDepth - rightDepth;
			return Number(left?.order ?? 0) - Number(right?.order ?? 0);
		})) {
			const name = node?.name;
			if (typeof name !== "string" || name.length === 0) continue;
			const depthKey = String(Number(node?.depth ?? 0));
			const flow = nodeFlows.get(name) || { total: 0 };
			allNodes.push(name);
			if (!nodesByDepth[depthKey]) nodesByDepth[depthKey] = {
				label: depthLabelMap.get(depthKey) || `Layer ${depthKey}`,
				nodes: []
			};
			const entry = {
				name,
				order: Number(node?.order ?? 0),
				total: Math.round(Number(flow.total || 0) * 100) / 100
			};
			if (node?._is_aggregate) entry.is_aggregate = true;
			if (Array.isArray(node?._collapsed_nodes) && node._collapsed_nodes.length > 0) entry.collapsed_nodes = [...node._collapsed_nodes];
			nodesByDepth[depthKey].nodes.push(entry);
		}
		const adjacency = Object.fromEntries(allNodes.map((name) => [name, {
			upstream: [],
			downstream: []
		}]));
		const edges = [];
		const values = [];
		for (const link of rawLinks) {
			if (!link || typeof link !== "object") continue;
			const source = link.source;
			const target = link.target;
			const value = Number(link.value || 0);
			edges.push({
				source,
				target,
				value
			});
			values.push(value);
			if (adjacency[source] && !adjacency[source].downstream.includes(target)) adjacency[source].downstream.push(target);
			if (adjacency[target] && !adjacency[target].upstream.includes(source)) adjacency[target].upstream.push(source);
		}
		return {
			all_nodes: allNodes,
			nodes_by_depth: nodesByDepth,
			depth_count: Object.keys(nodesByDepth).length,
			depth_labels: Object.fromEntries(depthLabelMap.entries()),
			edges,
			adjacency,
			collapsed_groups: rawSpec?._sankey_state?.collapsed_groups || {},
			value_range: values.length > 0 ? {
				min: Math.round(Math.min(...values) * 100) / 100,
				max: Math.round(Math.max(...values) * 100) / 100
			} : {
				min: 0,
				max: 0
			}
		};
	}
	function buildSankeyConversionRate(rawSpec, nodeName = null) {
		const rawLinks = findNamedDataSource$1(rawSpec, "rawLinks");
		if (!rawLinks) throw new Error("perception.calculateConversionRate requires a rawLinks data source.");
		const nodeFlows = buildNodeFlows(rawLinks);
		const nodeOptions = buildSankeyNodeOptions(rawSpec);
		const conversions = [];
		for (const name of [...nodeFlows.keys()].sort()) {
			const info = nodeFlows.get(name) || {
				inflow: 0,
				outflow: 0
			};
			const inflow = Number(info.inflow || 0);
			const outflow = Number(info.outflow || 0);
			let type = "intermediate";
			let rate = inflow > 0 ? Math.round(outflow / inflow * 1e4) / 1e4 : 0;
			if (inflow === 0 && outflow > 0) {
				type = "source";
				rate = "source";
			} else if (outflow === 0 && inflow > 0) {
				type = "sink";
				rate = 0;
			}
			const conversion = {
				node: name,
				inflow: Math.round(inflow * 100) / 100,
				outflow: Math.round(outflow * 100) / 100,
				rate,
				type
			};
			if (type === "intermediate" && inflow > 0) {
				const loss = inflow - outflow;
				conversion.loss = Math.round(loss * 100) / 100;
				conversion.loss_rate = Math.round(loss / inflow * 1e4) / 1e4;
			}
			conversions.push(conversion);
		}
		if (typeof nodeName === "string" && nodeName.length > 0) {
			const target = conversions.find((entry) => entry.node === nodeName);
			if (!target) throw new Error(`perception.calculateConversionRate cannot find node "${nodeName}".`);
			const upstream = rawLinks.filter((link) => link?.target === nodeName).map((link) => ({
				from: link.source,
				value: Number(link.value || 0)
			}));
			const downstream = rawLinks.filter((link) => link?.source === nodeName).map((link) => ({
				to: link.target,
				value: Number(link.value || 0)
			}));
			return {
				operation: "calculate_conversion_rate",
				message: `Conversion analysis for ${nodeName}`,
				node: nodeName,
				conversion: target,
				upstream,
				downstream,
				ui_hints: nodeOptions
			};
		}
		const sources = conversions.filter((entry) => entry.type === "source");
		const sinks = conversions.filter((entry) => entry.type === "sink");
		const intermediates = conversions.filter((entry) => entry.type === "intermediate");
		const highLossNodes = [...intermediates].filter((entry) => Number(entry.loss_rate || 0) > 0).sort((left, right) => Number(right.loss_rate || 0) - Number(left.loss_rate || 0)).slice(0, 5);
		return {
			operation: "calculate_conversion_rate",
			message: `Calculated conversion rates for ${conversions.length} nodes`,
			summary: {
				total_nodes: conversions.length,
				source_nodes: sources.length,
				sink_nodes: sinks.length,
				intermediate_nodes: intermediates.length
			},
			conversions,
			high_loss_nodes: highLossNodes,
			ui_hints: nodeOptions
		};
	}
	function buildSankeyBottlenecks(rawSpec, topN = 3) {
		const rawLinks = findNamedDataSource$1(rawSpec, "rawLinks");
		if (!rawLinks) throw new Error("perception.findBottleneck requires a rawLinks data source.");
		const nodeFlows = buildNodeFlows(rawLinks);
		const nodeOptions = buildSankeyNodeOptions(rawSpec);
		const bottlenecks = [];
		for (const [name, info] of nodeFlows.entries()) {
			const inflow = Number(info.inflow || 0);
			const outflow = Number(info.outflow || 0);
			if (!(inflow > 0 && outflow > 0 && inflow > outflow)) continue;
			const loss = inflow - outflow;
			bottlenecks.push({
				node: name,
				inflow: Math.round(inflow * 100) / 100,
				outflow: Math.round(outflow * 100) / 100,
				loss: Math.round(loss * 100) / 100,
				loss_rate: Math.round(loss / inflow * 1e4) / 1e4
			});
		}
		bottlenecks.sort((left, right) => {
			if (right.loss_rate !== left.loss_rate) return right.loss_rate - left.loss_rate;
			if (right.loss !== left.loss) return right.loss - left.loss;
			return String(left.node).localeCompare(String(right.node));
		});
		const limit = Number.isFinite(topN) && topN > 0 ? Math.floor(topN) : 3;
		const top = bottlenecks.slice(0, limit);
		return {
			operation: "find_bottleneck",
			message: top.length > 0 ? `Found top ${top.length} bottleneck nodes with highest loss rates` : "No bottlenecks found (no intermediate nodes with loss)",
			bottlenecks: top,
			total_bottleneck_nodes: bottlenecks.length,
			ui_hints: nodeOptions
		};
	}
	function buildSankeyPerceptionDescriptors({ dataRef }) {
		return [
			makePerceptionDescriptor({
				name: "perception.getNodeOptions",
				title: "Get Sankey node options",
				description: appendQueryScopeGuidance("Return structured Sankey node, layer, adjacency, and collapsed-group metadata for UI controls and agent-side exploration."),
				category: "inspect",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({ includeEdges: { type: "boolean" } }),
				sideEffectFree: true,
				evidenceKinds: ["topologyEvidence", "uiMetadata"],
				examples: [{
					userGoal: "Inspect all current Sankey node and layer options before choosing a path, collapse target, or filter threshold.",
					params: {}
				}, buildQueryScopeExample({
					userGoal: "Inspect Sankey node options for one focused flow view.",
					params: {},
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_view"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.calculateConversionRate",
				title: "Calculate Sankey conversion rate",
				description: appendQueryScopeGuidance("Compute inflow, outflow, conversion rate, and loss information for all Sankey nodes or one target node."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({ nodeName: { type: "string" } }),
				sideEffectFree: true,
				evidenceKinds: [
					"flowEvidence",
					"conversionEvidence",
					"topologyEvidence"
				],
				examples: [{
					userGoal: "Inspect conversion and loss rates across the whole Sankey, or drill into one node.",
					params: {}
				}, buildQueryScopeExample({
					userGoal: "Inspect conversion rates inside one focused Sankey view.",
					params: {},
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_view"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findBottleneck",
				title: "Find Sankey bottlenecks",
				description: appendQueryScopeGuidance("Identify the intermediate Sankey nodes with the highest loss rates by comparing inflow and outflow."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({ topN: { type: "number" } }),
				sideEffectFree: true,
				evidenceKinds: [
					"flowEvidence",
					"conversionEvidence",
					"rankEvidence"
				],
				examples: [{
					userGoal: "Find the worst flow drop-off points in the current Sankey.",
					params: { topN: 3 }
				}, buildQueryScopeExample({
					userGoal: "Find bottlenecks inside one focused Sankey view.",
					params: { topN: 3 },
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_view"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findExtremes",
				title: "Find extremes",
				description: appendQueryScopeGuidance("Return the highest- or lowest-valued flows in the visible Sankey data."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					field: { type: "string" },
					direction: { type: "string" },
					limit: { type: "number" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["flowEvidence", "rankEvidence"],
				examples: [{
					userGoal: "Find the largest or smallest visible flows.",
					params: {
						field: "value",
						direction: "max",
						limit: 5
					}
				}, buildQueryScopeExample({
					userGoal: "Find extreme flows inside one focused Sankey selection.",
					params: {
						field: "value",
						direction: "max",
						limit: 5
					},
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/sankey_a/selection/current"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.compareGroups",
				title: "Compare flow groups",
				description: appendQueryScopeGuidance("Compare grouped flow magnitudes in the visible Sankey data."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					groupField: { type: "string" },
					valueField: { type: "string" },
					groups: {
						type: "array",
						items: { type: "string" }
					}
				}),
				sideEffectFree: true,
				evidenceKinds: ["groupComparison", "flowEvidence"],
				examples: [{
					userGoal: "Compare grouped flow magnitudes across categories.",
					params: {
						groupField: "source",
						valueField: "value",
						groups: ["A", "B"]
					}
				}, buildQueryScopeExample({
					userGoal: "Compare grouped flows inside one focused Sankey selection.",
					params: {
						groupField: "source",
						valueField: "value",
						groups: ["A", "B"]
					},
					widgetRef: "wl://demo/workspace/main/widget/sankey_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/sankey_a/selection/current"
				})]
			})
		];
	}
	function registerSankeyPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.getNodeOptions", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.getNodeOptions",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const options = buildSankeyNodeOptions(targetWidget?.rawSpec || targetWidget?.spec || null);
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: {
				operation: "get_node_options",
				message: `Extracted ${options.all_nodes.length} nodes across ${options.depth_count} layers`,
				...options
			} };
		}, { supportedWidgetKinds: ["sankey"] });
		if (!perceptionRegistry.has("perception.calculateConversionRate", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.calculateConversionRate",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const result = buildSankeyConversionRate(targetWidget?.rawSpec || targetWidget?.spec || null, call?.params?.nodeName || null);
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result };
		}, { supportedWidgetKinds: ["sankey"] });
		if (!perceptionRegistry.has("perception.findBottleneck", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.findBottleneck",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const result = buildSankeyBottlenecks(targetWidget?.rawSpec || targetWidget?.spec || null, call?.params?.topN);
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result };
		}, { supportedWidgetKinds: ["sankey"] });
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["sankey"] });
		if (!perceptionRegistry.has("perception.compareGroups", { supportedWidgetKinds: ["sankey"] })) perceptionRegistry.register({
			name: "perception.compareGroups",
			supportedWidgetKinds: ["sankey"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "sankey" });
			const params = ctx.readCallParams();
			const comparison = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "compareGroups",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: comparison,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["sankey"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/sankey/index.js
	function clone$41(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var SANKEY_LOCAL_SELECTION_CONTRACT = Object.freeze({
		localSelectionFamily: "category",
		selectionKinds: ["category", "aggregate"],
		sourceActionNames: ["sankey.focusFlow", "sankey.selectAggregateNode"],
		cardinality: "singleActiveSelection",
		selectionValueShape: "nodeOrFlowCategoriesOrAggregateName",
		observationFields: [
			"state.selections",
			"coordination.localSelectionRefs",
			"coordination.widgetSelectionRef",
			"selection.activeSelectionRef",
			"selection.activeSelectionKind",
			"selection.activeSelectionSummary"
		],
		perceptionExpectations: [
			"selection-scoped perception queries should resolve rows or flows matching the selected node/link categories",
			"selection summaries should preserve whether the active focus refers to flow or node categories",
			"aggregate selections should preserve aggregateName even when they do not map to row-level predicates"
		]
	});
	var SANKEY_VERIFICATION_CONTRACT = Object.freeze({
		preferredReadMethod: "readVerificationState",
		preferredObservationFields: [
			"verification.checks.selectionApplied",
			"verification.checks.filterApplied",
			"verification.checks.aggregateApplied",
			"verification.checks.reencodeApplied",
			"verification.checks.navigateApplied",
			"verification.checks.highlightApplied",
			"verification.checks.focusApplied",
			"verification.view.aggregate",
			"verification.view.reencode",
			"verification.view.navigate",
			"verification.view.highlight",
			"verification.transforms.kinds"
		],
		supportedEffectTypes: [
			"selection",
			"filter",
			"aggregate",
			"reencode",
			"navigate",
			"highlight",
			"focus",
			"encoding",
			"linkedPropagation"
		],
		effectChecks: {
			selection: [
				"checks.selectionApplied",
				"selections.count",
				"selections.activeKinds",
				"selections.activeSummary"
			],
			filter: [
				"checks.filterApplied",
				"data.rowCount",
				"data.visibleCount",
				"transforms.kinds"
			],
			aggregate: ["checks.aggregateApplied", "view.aggregate"],
			reencode: [
				"checks.reencodeApplied",
				"view.reencode",
				"encodings.channels"
			],
			navigate: ["checks.navigateApplied", "view.navigate"],
			highlight: [
				"checks.highlightApplied",
				"view.highlight",
				"feedback.highlightKeyCount",
				"feedback.sharedSelectionSourceWidgetId"
			],
			focus: ["checks.focusApplied", "view.focusKeys"],
			encoding: [
				"checks.encodingReadable",
				"encodings.channels",
				"encodings.fieldsByChannel"
			],
			linkedPropagation: [
				"checks.linkedPropagationApplied",
				"feedback.linkedSourceRefCount",
				"feedback.sharedSelectionSourceWidgetId"
			]
		}
	});
	function describeSankeyLocalSelectionContract() {
		return clone$41(SANKEY_LOCAL_SELECTION_CONTRACT);
	}
	function describeSankeyVerificationContract() {
		return clone$41(SANKEY_VERIFICATION_CONTRACT);
	}
	function describeSankeyWidgetContract() {
		return {
			kind: "sankey",
			actionNames: [
				"sankey.focusFlow",
				"sankey.selectAggregateNode",
				"sankey.filterFlow",
				"sankey.collapseNodes",
				"sankey.expandNode",
				"sankey.highlightPath",
				"sankey.traceNode",
				"sankey.colorFlows",
				"sankey.reorderNodesInLayer",
				"sankey.autoCollapseByRank"
			],
			perceptionNames: [
				"perception.getNodeOptions",
				"perception.calculateConversionRate",
				"perception.findBottleneck",
				"perception.findExtremes",
				"perception.compareGroups"
			],
			localSelection: describeSankeyLocalSelectionContract(),
			verification: describeSankeyVerificationContract()
		};
	}
	var sankeyFamily = Object.freeze({
		kind: "sankey",
		describeContract: describeSankeyWidgetContract,
		actions: Object.freeze({ buildDescriptors: buildSankeyActionDescriptors }),
		perception: Object.freeze({
			buildDescriptors: buildSankeyPerceptionDescriptors,
			register: registerSankeyPerceptionQueries
		}),
		interactionProfile: Object.freeze({ getConfig: getSankeyHumanInteractionConfig }),
		playbook: sankeyPlaybook
	});
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/scatter/interactionProfile.js
	function getScatterHumanInteractionConfig() {
		return {
			mode: "brush2d",
			actionName: "scatter.brushRegion",
			supportsDirectManipulation: true
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/scatter/playbook.js
	var scatterPlaybook = {
		analysisToAction: [
			{
				goal: "Judge the relationship between two variables",
				workflow: "If the goal is to understand how x and y relate, first assess direction, spread, and rough linearity visually, then use correlation or regression only if the visible pattern supports it.",
				candidateActions: ["scatter.showRegression"],
				candidatePerceptions: ["perception.computeCorrelation", "perception.findExtremes"]
			},
			{
				goal: "Investigate a local cluster or neighborhood",
				workflow: "If the goal is to understand a local cluster, region, or neighborhood, first brush or zoom into that area, then analyze local spread, density, or structure.",
				candidateActions: [
					"scatter.brushRegion",
					"scatter.zoomDomain",
					"scatter.identifyClusters",
					"scatter.showRegression"
				],
				candidatePerceptions: [
					"perception.computeCorrelation",
					"perception.findOutliers",
					"perception.findExtremes"
				]
			},
			{
				goal: "Find outliers or rare cases",
				workflow: "If the goal is to detect outliers or unusual regions, isolate sparse or boundary points first, then compare them against the main cloud.",
				candidateActions: ["scatter.brushRegion", "scatter.zoomDomain"],
				candidatePerceptions: ["perception.findOutliers", "perception.findExtremes"]
			},
			{
				goal: "Explain how a selected region affects another linked view",
				workflow: "If the goal is to interpret another view through the scatterplot, first define a meaningful region in the scatterplot, then inspect how the linked view changes for that subset.",
				candidateActions: ["scatter.brushRegion", "scatter.zoomDomain"],
				candidatePerceptions: ["perception.computeCorrelation"]
			}
		],
		workflows: [
			{
				name: "filter_then_compute_correlation",
				analysisIntent: "Subgroup relationship measurement",
				scenarioExamples: ["Among non-US cars, is horsepower negatively correlated with miles per gallon?", "For one customer segment, does marketing spend still track visitor count?"],
				description: "Use when the relationship should be measured only within one category-defined subset.",
				steps: [{ action: "scatter.filterCategorical" }, { perception: "perception.computeCorrelation" }]
			},
			{
				name: "zoom_then_compute_local_correlation",
				analysisIntent: "Local relationship measurement",
				scenarioExamples: ["Inside the dense museum-visitor region, what is the Pearson correlation between the two visitor series?", "Within a high-score student region, how strongly do math and reading scores move together?"],
				description: "Use when the answer depends on correlation inside a dense or bounded local region.",
				steps: [{ action: "scatter.zoomDomain" }, { perception: "perception.computeCorrelation" }]
			},
			{
				name: "correlation_then_cluster",
				analysisIntent: "Relationship versus cluster structure",
				scenarioExamples: ["Do the visible car clusters correspond to origin labels after checking the horsepower-mileage relationship?", "Do gym members separate into clusters after measuring the relation between session duration and calories?"],
				description: "Use when the task asks whether a numeric relationship also separates into visible groups.",
				steps: [{ perception: "perception.computeCorrelation" }, { action: "scatter.identifyClusters" }]
			},
			{
				name: "regression_then_correlation",
				analysisIntent: "Regression-backed relationship explanation",
				scenarioExamples: ["How strong is the relationship between price percent and win percent after showing a linear trend?", "Does distance from home meaningfully predict tenure after fitting the visible relationship?"],
				description: "Use when the task asks for relationship strength after adding a visible trend model.",
				steps: [{ action: "scatter.showRegression" }, { perception: "perception.computeCorrelation" }]
			},
			{
				name: "filter_then_zoom_then_inspect_extremes",
				analysisIntent: "Extreme case lookup in a local subgroup",
				scenarioExamples: ["Among coffee drinks in the low-sugar low-calorie region, what is the minimum calories value?", "After keeping a workout type and zooming to a target duration range, which point has the highest calories burned?"],
				description: "Use when the target row or extreme value must be found inside a filtered local region.",
				steps: [
					{ action: "scatter.filterCategorical" },
					{ action: "scatter.zoomDomain" },
					{ perception: "perception.findExtremes" }
				]
			},
			{
				name: "filter_then_brush_region",
				analysisIntent: "Cohort definition for follow-up comparison",
				scenarioExamples: ["After removing European cars, compare Japanese and American cars inside the brushed horsepower window.", "Filter to a target customer group, then brush the high-spend high-visitor region for linked-view analysis."],
				description: "Use when a category-filtered scatter subset should define a local selection for closer comparison or linked views.",
				steps: [{ action: "scatter.filterCategorical" }, { action: "scatter.brushRegion" }]
			}
		],
		multiTurnStrategy: [
			"First inspect the overall scatter pattern to decide whether the query is about the global relationship or a local region.",
			"If the query is local, isolate a meaningful region through brushing or zooming.",
			"After the region is stable, run the most relevant local analysis such as correlation, regression, clustering, or outlier reading.",
			"If the local finding may be misleading without context, compare it back against the global scatter pattern.",
			"Stop when the selected region is specific enough to support a direct answer to the query."
		]
	};
	//#endregion
	//#region ../../widgetva-kit/src/contracts/action-contracts.js
	function cloneValue$8(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function withActionQueryScope(paramsSchema) {
		if (!paramsSchema || typeof paramsSchema !== "object" || Array.isArray(paramsSchema)) return {
			type: "object",
			properties: { queryScope: QUERY_SCOPE_SCHEMA }
		};
		return {
			...cloneValue$8(paramsSchema),
			properties: {
				...cloneValue$8(paramsSchema.properties) || {},
				queryScope: QUERY_SCOPE_SCHEMA
			}
		};
	}
	function makeActionDescriptor(descriptor) {
		const paramsSchema = withActionQueryScope(descriptor?.paramsSchema);
		return {
			scope: "local",
			supportedWidgetKinds: null,
			affectedRefs: [],
			affectedStatePaths: [],
			effects: [],
			reversible: false,
			preconditions: [],
			postconditions: [],
			examples: [],
			...descriptor,
			paramsSchema
		};
	}
	function makeSelectionEffect(ref, description) {
		return {
			kind: "updatesSelection",
			ref,
			description
		};
	}
	function makeFilterEffect(ref, description) {
		return {
			kind: "filtersWidget",
			ref,
			description
		};
	}
	function makeDomainEffect(ref, description) {
		return {
			kind: "updatesViewDomain",
			ref,
			description
		};
	}
	function makeEncodingEffect(ref, description) {
		return {
			kind: "changesEncoding",
			ref,
			description
		};
	}
	function makeHighlightEffect(ref, description) {
		return {
			kind: "highlightsItems",
			ref,
			description
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/scatter/actionDescriptors.js
	function makeScatterActionDescriptor(descriptor) {
		return makeActionDescriptor(descriptor);
	}
	function readMarkType$8(mark) {
		return typeof mark === "string" ? mark : mark?.type || null;
	}
	function isScatterFamilyMark$1(mark) {
		const markType = readMarkType$8(mark);
		return markType === "point" || markType === "circle" || markType === "square";
	}
	function collectNestedScatterSpecs$1(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const nested = [];
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) nested.push(...spec[key].filter((entry) => entry && typeof entry === "object"));
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) nested.push(spec.spec);
		return nested;
	}
	function findRepresentativeScatterSpec$1(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		if (isScatterFamilyMark$1(spec?.mark) && spec?.encoding && typeof spec.encoding === "object") return spec;
		if (Array.isArray(spec?.layer)) {
			const layeredMatch = spec.layer.find((entry) => entry && typeof entry === "object" && isScatterFamilyMark$1(entry?.mark) && entry?.encoding && typeof entry.encoding === "object");
			if (layeredMatch) return layeredMatch;
		}
		for (const child of collectNestedScatterSpecs$1(spec)) {
			const match = findRepresentativeScatterSpec$1(child);
			if (match) return match;
		}
		return null;
	}
	function resolveScatterRootEncoding(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return {};
		if (spec.layer?.[0]?.encoding && typeof spec.layer[0].encoding === "object" && !Array.isArray(spec.layer[0].encoding)) return spec.layer[0].encoding;
		if (spec.encoding && typeof spec.encoding === "object" && !Array.isArray(spec.encoding)) return spec.encoding;
		return {};
	}
	function buildScatterActionDescriptors({ widgetSpec = null } = {}) {
		const descriptors = [
			makeScatterActionDescriptor({
				name: "scatter.brushRegion",
				title: "Brush region",
				category: "selection",
				description: "Select points inside a data-space rectangle on the scatterplot.",
				effects: [makeSelectionEffect(null, "Updates the active scatter selection.")],
				paramsSchema: {
					type: "object",
					properties: {
						xField: { type: "string" },
						yField: { type: "string" },
						xRange: {
							type: "array",
							items: { type: "number" },
							minItems: 2,
							maxItems: 2
						},
						yRange: {
							type: "array",
							items: { type: "number" },
							minItems: 2,
							maxItems: 2
						}
					},
					required: [
						"xField",
						"yField",
						"xRange",
						"yRange"
					]
				},
				examples: [{
					userGoal: "Brush a region of interest in the scatterplot.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon",
						xRange: [80, 160],
						yRange: [20, 35]
					}
				}]
			}),
			makeScatterActionDescriptor({
				name: "scatter.selectRegion",
				title: "Select region",
				category: "selection",
				description: "Select points inside a data-space rectangle on the scatterplot and keep the region as the active interval selection.",
				effects: [makeSelectionEffect(null, "Keeps the brushed region as the active scatter selection.")],
				paramsSchema: {
					type: "object",
					properties: {
						xField: { type: "string" },
						yField: { type: "string" },
						xRange: {
							type: "array",
							items: { type: "number" },
							minItems: 2,
							maxItems: 2
						},
						yRange: {
							type: "array",
							items: { type: "number" },
							minItems: 2,
							maxItems: 2
						}
					},
					required: [
						"xField",
						"yField",
						"xRange",
						"yRange"
					]
				},
				examples: [{
					userGoal: "Select a dense rectangle in the scatterplot for downstream inspection.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon",
						xRange: [80, 160],
						yRange: [20, 35]
					}
				}]
			}),
			makeScatterActionDescriptor({
				name: "scatter.zoomDomain",
				title: "Zoom domain",
				category: "viewTransform",
				description: "Zoom the scatterplot to a specific x and/or y domain. Use null for an open lower or upper bound when only one side of the domain is known.",
				effects: [makeDomainEffect(null, "Updates the scatterplot view domain.")],
				paramsSchema: {
					type: "object",
					properties: {
						xDomain: {
							type: "array",
							minItems: 2,
							maxItems: 2,
							items: { anyOf: [
								{ type: "number" },
								{ type: "string" },
								{ type: "null" }
							] }
						},
						yDomain: {
							type: "array",
							minItems: 2,
							maxItems: 2,
							items: { anyOf: [
								{ type: "number" },
								{ type: "string" },
								{ type: "null" }
							] }
						}
					}
				},
				examples: [{
					userGoal: "Zoom into the high-risk cluster region of the scatterplot.",
					params: {
						xDomain: [80, 160],
						yDomain: [20, 35]
					}
				}, {
					userGoal: "Zoom to all points with marketing spend above 3000 while leaving the upper x bound open.",
					params: { xDomain: [3e3, null] }
				}]
			}),
			makeScatterActionDescriptor({
				name: "scatter.filterCategorical",
				title: "Filter categories",
				category: "dataTransform",
				description: "Remove one or more categories from the current scatterplot by inserting a categorical exclusion transform.",
				effects: [makeFilterEffect(null, "Filters categories from the scatterplot data view.")],
				paramsSchema: {
					type: "object",
					properties: {
						categoriesToRemove: {
							type: "array",
							items: { anyOf: [{ type: "string" }, { type: "number" }] }
						},
						field: { type: "string" }
					},
					required: ["categoriesToRemove"]
				},
				examples: [{
					userGoal: "Exclude one origin category from the scatterplot.",
					params: {
						field: "Origin",
						categoriesToRemove: ["USA"]
					}
				}]
			}),
			makeScatterActionDescriptor({
				name: "scatter.identifyClusters",
				title: "Identify clusters",
				category: "visualMapping",
				description: "Cluster visible scatter points in the frontend and recolor the scatterplot by the derived cluster labels.",
				effects: [makeHighlightEffect(null, "Highlights or recolors visible clusters in the scatterplot.")],
				paramsSchema: {
					type: "object",
					properties: {
						nClusters: { type: "number" },
						method: { type: "string" }
					}
				},
				examples: [{
					userGoal: "Color the visible scatter points by their inferred clusters.",
					params: {
						nClusters: 3,
						method: "kmeans"
					}
				}]
			}),
			makeScatterActionDescriptor({
				name: "scatter.showRegression",
				title: "Show regression",
				category: "visualMapping",
				description: "Add or replace a regression-line overlay on the current scatterplot using the active x/y encodings.",
				effects: [makeHighlightEffect(null, "Adds or replaces a regression overlay on the scatterplot.")],
				paramsSchema: {
					type: "object",
					properties: { method: { type: "string" } }
				},
				examples: [{
					userGoal: "Overlay a regression line on the scatterplot to inspect the overall trend.",
					params: { method: "linear" }
				}]
			})
		];
		if (!widgetSpec) return descriptors;
		const rootEncoding = resolveScatterRootEncoding(findRepresentativeScatterSpec$1(widgetSpec));
		const xType = rootEncoding?.x?.type || null;
		const yType = rootEncoding?.y?.type || null;
		const hasQuantitativeXY = xType === "quantitative" && yType === "quantitative";
		return descriptors.filter((descriptor) => {
			if (!descriptor?.name) return true;
			if (descriptor.name === "scatter.brushRegion" || descriptor.name === "scatter.selectRegion" || descriptor.name === "scatter.identifyClusters" || descriptor.name === "scatter.showRegression") return hasQuantitativeXY;
			return true;
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/scatter/perception.js
	function buildScatterPerceptionDescriptors({ dataRef }) {
		return [
			makePerceptionDescriptor({
				name: "perception.computeCorrelation",
				title: "Compute correlation",
				description: appendQueryScopeGuidance("Compute a correlation coefficient over the visible rows of the target scatter widget."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					xField: { type: "string" },
					yField: { type: "string" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["statisticalEvidence", "correlationEvidence"],
				examples: [{
					userGoal: "Estimate the relationship between two numeric fields in the scatterplot.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon"
					}
				}, buildQueryScopeExample({
					userGoal: "Estimate the correlation inside one brushed scatter subset.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon"
					},
					widgetRef: "wl://demo/workspace/main/widget/scatter_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/scatter_a/selection/brush"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findOutliers",
				title: "Find outliers",
				description: appendQueryScopeGuidance("Return likely outlier rows in the visible scatter data by numeric fields."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					field: { type: "string" },
					xField: { type: "string" },
					yField: { type: "string" },
					zThreshold: { type: "number" },
					limit: { type: "number" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["outlierEvidence", "rowEvidence"],
				examples: [{
					userGoal: "Identify candidate outliers in the visible scatter subset.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon",
						limit: 5
					}
				}, buildQueryScopeExample({
					userGoal: "Identify outliers inside one brushed scatter subset.",
					params: {
						xField: "Horsepower",
						yField: "Miles_per_Gallon",
						limit: 5
					},
					widgetRef: "wl://demo/workspace/main/widget/scatter_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/scatter_a/selection/brush"
				})]
			}),
			makePerceptionDescriptor({
				name: "perception.findExtremes",
				title: "Find extremes",
				description: appendQueryScopeGuidance("Return top-k or bottom-k visible rows by a numeric field."),
				category: "compute",
				targetRef: dataRef,
				paramsSchema: buildScopedPerceptionParamsSchema({
					field: { type: "string" },
					direction: { type: "string" },
					limit: { type: "number" }
				}),
				sideEffectFree: true,
				evidenceKinds: ["rankEvidence", "rowEvidence"],
				examples: [{
					userGoal: "Retrieve the highest or lowest visible rows by a metric.",
					params: {
						field: "Horsepower",
						direction: "max",
						limit: 5
					}
				}, buildQueryScopeExample({
					userGoal: "Retrieve extremes inside one brushed scatter subset.",
					params: {
						field: "Horsepower",
						direction: "max",
						limit: 5
					},
					widgetRef: "wl://demo/workspace/main/widget/scatter_a",
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/scatter_a/selection/brush"
				})]
			})
		];
	}
	function registerScatterPerceptionQueries(perceptionRegistry) {
		if (!perceptionRegistry.has("perception.computeCorrelation", { supportedWidgetKinds: ["scatter"] })) perceptionRegistry.register({
			name: "perception.computeCorrelation",
			supportedWidgetKinds: ["scatter"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "scatter" });
			const params = ctx.readCallParams();
			const correlation = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "computeCorrelation",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: correlation,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["scatter"] });
		if (!perceptionRegistry.has("perception.findOutliers", { supportedWidgetKinds: ["scatter"] })) perceptionRegistry.register({
			name: "perception.findOutliers",
			supportedWidgetKinds: ["scatter"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "scatter" });
			const params = ctx.readCallParams();
			const outliers = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findOutliers",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: outliers,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["scatter"] });
		if (!perceptionRegistry.has("perception.findExtremes", { supportedWidgetKinds: ["scatter"] })) perceptionRegistry.register({
			name: "perception.findExtremes",
			supportedWidgetKinds: ["scatter"]
		}, async (call, ctx) => {
			const targetWidget = ctx.requireTargetWidget({ kind: "scatter" });
			const params = ctx.readCallParams();
			const extremes = runDataPerceptionQuery({
				ctx,
				dataQueryExecutor: perceptionRegistry.dataQueryExecutor,
				targetWidget,
				kind: "findExtremes",
				spec: params
			});
			ctx.recordQuery({ affectedRefs: [targetWidget.ref] });
			return { result: buildPerceptionDataResult({
				dataQueryResult: extremes,
				fallbackDataRef: ctx.resolveRowsForWidget(targetWidget, params).dataRef
			}) };
		}, { supportedWidgetKinds: ["scatter"] });
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/scatter/index.js
	function clone$40(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var SCATTER_LOCAL_SELECTION_CONTRACT = Object.freeze({
		localSelectionFamily: "interval",
		selectionKinds: ["interval"],
		sourceActionNames: ["scatter.brushRegion", "scatter.selectRegion"],
		cardinality: "singleActiveSelection",
		selectionValueShape: "xAndYIntervals",
		observationFields: [
			"state.selections",
			"coordination.localSelectionRefs",
			"coordination.widgetSelectionRef",
			"selection.activeSelectionRef",
			"selection.activeSelectionKind",
			"selection.activeSelectionSummary",
			"selection.activeSelectionFields"
		],
		perceptionExpectations: ["selection-scoped perception queries should resolve rows inside the brushed x/y interval", "selection summaries should expose interval predicates rather than only selected row ids"]
	});
	var SCATTER_VERIFICATION_CONTRACT = Object.freeze({
		preferredReadMethod: "readVerificationState",
		preferredObservationFields: [
			"verification.checks.selectionApplied",
			"verification.checks.filterApplied",
			"verification.checks.reencodeApplied",
			"verification.checks.zoomApplied",
			"verification.encodings.channels",
			"verification.view.reencode",
			"verification.selections.activeKinds"
		],
		supportedEffectTypes: [
			"selection",
			"filter",
			"zoom",
			"highlight",
			"reencode",
			"encoding",
			"linkedPropagation"
		],
		effectChecks: {
			selection: [
				"checks.selectionApplied",
				"selections.count",
				"selections.activeKinds",
				"selections.activeSummary"
			],
			filter: [
				"checks.filterApplied",
				"data.rowCount",
				"data.visibleCount",
				"transforms.kinds"
			],
			zoom: [
				"checks.zoomApplied",
				"view.xDomain",
				"view.yDomain",
				"view.zoom"
			],
			reencode: [
				"checks.reencodeApplied",
				"view.reencode",
				"encodings.channels"
			],
			highlight: [
				"checks.highlightApplied",
				"feedback.highlightKeyCount",
				"feedback.sharedSelectionSourceWidgetId"
			],
			encoding: [
				"checks.encodingReadable",
				"encodings.channels",
				"encodings.fieldsByChannel"
			],
			linkedPropagation: [
				"checks.linkedPropagationApplied",
				"feedback.linkedSourceRefCount",
				"feedback.sharedSelectionSourceWidgetId"
			]
		}
	});
	function describeScatterLocalSelectionContract() {
		return clone$40(SCATTER_LOCAL_SELECTION_CONTRACT);
	}
	function describeScatterVerificationContract() {
		return clone$40(SCATTER_VERIFICATION_CONTRACT);
	}
	function describeScatterWidgetContract() {
		return {
			kind: "scatter",
			actionNames: [
				"scatter.brushRegion",
				"scatter.selectRegion",
				"scatter.zoomDomain",
				"scatter.filterCategorical",
				"scatter.identifyClusters",
				"scatter.showRegression"
			],
			perceptionNames: [
				"perception.computeCorrelation",
				"perception.findOutliers",
				"perception.findExtremes"
			],
			localSelection: describeScatterLocalSelectionContract(),
			verification: describeScatterVerificationContract()
		};
	}
	var scatterFamily = Object.freeze({
		kind: "scatter",
		describeContract: describeScatterWidgetContract,
		actions: Object.freeze({ buildDescriptors: buildScatterActionDescriptors }),
		perception: Object.freeze({
			buildDescriptors: buildScatterPerceptionDescriptors,
			register: registerScatterPerceptionQueries
		}),
		interactionProfile: Object.freeze({ getConfig: getScatterHumanInteractionConfig }),
		playbook: scatterPlaybook
	});
	//#endregion
	//#region ../../widgetva-kit/src/contracts/state-contracts.js
	function clone$39(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function isPlainObject$6(value) {
		return Boolean(value) && typeof value === "object" && !Array.isArray(value);
	}
	function normalizeString$6(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizeArray(value) {
		return Array.isArray(value) ? value.filter((item) => item !== void 0 && item !== null) : [];
	}
	function normalizePredicate(predicate) {
		if (!isPlainObject$6(predicate)) return null;
		const field = normalizeString$6(predicate.field);
		const op = normalizeString$6(predicate.op) || normalizeString$6(predicate.operator);
		if (!field || !op) return null;
		return {
			field,
			op,
			value: predicate.value
		};
	}
	function normalizePredicates$1(value) {
		return normalizeArray(value).map(normalizePredicate).filter(Boolean);
	}
	function normalizeIntervalChannels(selection = {}) {
		if (isPlainObject$6(selection.channels)) return clone$39(selection.channels);
		const domain = isPlainObject$6(selection.domain) ? selection.domain : {};
		const value = isPlainObject$6(selection.value) ? selection.value : {};
		const xDomain = normalizeArray(domain.xDomain || domain.x || value.x || selection.xDomain);
		const yDomain = normalizeArray(domain.yDomain || domain.y || value.y || selection.yDomain);
		const xField = normalizeString$6(selection.xField) || normalizeString$6(domain.xField) || normalizeArray(selection.fields)[0] || normalizeString$6(selection.field);
		const yField = normalizeString$6(selection.yField) || normalizeString$6(domain.yField) || normalizeArray(selection.fields)[1] || null;
		return {
			...xDomain.length > 0 ? { x: {
				field: xField || "x",
				domain: xDomain
			} } : {},
			...yDomain.length > 0 ? { y: {
				field: yField || "y",
				domain: yDomain
			} } : {}
		};
	}
	function normalizeSelectionKind$1(selection = {}) {
		const explicitKind = normalizeString$6(selection.kind || selection.selection_type);
		if (explicitKind === "point" || explicitKind === "interval" || explicitKind === "predicate") return explicitKind;
		if (isPlainObject$6(selection.channels) || isPlainObject$6(selection.domain)) return "interval";
		if (normalizePredicates$1(selection.predicates).length > 0) return "predicate";
		return explicitKind || "point";
	}
	function normalizeSelectionValues(selection = {}) {
		if (Array.isArray(selection.values)) return normalizeArray(selection.values);
		if (selection.value !== void 0 && !isPlainObject$6(selection.value)) return [selection.value];
		if (isPlainObject$6(selection.value)) {
			const field = normalizeString$6(selection.field) || normalizeArray(selection.fields)[0] || null;
			if (field && selection.value[field] !== void 0) return [selection.value[field]];
		}
		if (Array.isArray(selection.keys)) return normalizeArray(selection.keys);
		return [];
	}
	function normalizeTransformSource(transform = {}) {
		const sourceType = isPlainObject$6(transform.source) ? normalizeString$6(transform.source.type) : normalizeString$6(transform.source);
		const ref = normalizeString$6(transform.sourceRef) || (isPlainObject$6(transform.source) ? normalizeString$6(transform.source.ref) : null) || normalizeString$6(transform.sourceSelectionRef) || normalizeString$6(transform.linkId) || normalizeString$6(transform.spec?.actionName) || normalizeString$6(transform.actionName);
		return {
			type: sourceType || "action",
			...ref ? { ref } : {}
		};
	}
	function normalizeTransformPredicate(transform = {}) {
		const directPredicates = normalizePredicates$1(transform.predicates);
		if (directPredicates.length > 0) return directPredicates.length === 1 ? directPredicates[0] : directPredicates;
		const specPredicates = normalizePredicates$1(transform.spec?.predicates);
		if (specPredicates.length > 0) return specPredicates.length === 1 ? specPredicates[0] : specPredicates;
		const field = normalizeString$6(transform.field || transform.spec?.field);
		if (!field) return void 0;
		const values = transform.values !== void 0 ? transform.values : transform.spec?.values;
		if (values !== void 0) return {
			field,
			op: Array.isArray(values) ? "in" : "eq",
			value: values
		};
		const value = transform.value !== void 0 ? transform.value : transform.spec?.value;
		if (value !== void 0) return {
			field,
			op: "eq",
			value
		};
	}
	function makeTransformState(transform = {}) {
		const kind = normalizeString$6(transform?.kind) || "derive";
		const source = normalizeTransformSource(transform);
		const predicate = normalizeTransformPredicate(transform);
		const params = isPlainObject$6(transform.params) ? clone$39(transform.params) : isPlainObject$6(transform.spec) ? clone$39(transform.spec) : {};
		return {
			ref: normalizeString$6(transform.ref || transform.transformRef) || null,
			kind,
			source: source.type,
			sourceRef: source.ref || null,
			...predicate !== void 0 ? { predicate } : {},
			params,
			sourceWidgetId: void 0,
			sourceSelectionRef: void 0,
			linkId: void 0,
			spec: {},
			...transform,
			kind,
			source: source.type,
			sourceRef: source.ref || null
		};
	}
	function makeViewTransformState$1(view) {
		return {
			xDomain: void 0,
			yDomain: void 0,
			zoom: void 0,
			sort: void 0,
			highlight: void 0,
			drillDown: void 0,
			aggregate: void 0,
			reencode: void 0,
			addRemove: void 0,
			navigate: void 0,
			...view
		};
	}
	function makeSelectionState(selection = {}) {
		const ref = normalizeString$6(selection.ref || selection.selectionRef || selection.selection_ref);
		const id = normalizeString$6(selection.id || selection.selectionId || selection.selection_id) || (ref ? ref.split("/").pop() || null : null);
		const kind = normalizeSelectionKind$1(selection);
		const sourceWidgetRef = normalizeString$6(selection.sourceWidgetRef || selection.source_widget_ref);
		const values = normalizeSelectionValues(selection);
		const predicates = normalizePredicates$1(selection.predicates);
		const field = normalizeString$6(selection.field) || normalizeArray(selection.fields)[0] || null;
		const channels = kind === "interval" ? normalizeIntervalChannels(selection) : void 0;
		return {
			ref,
			id,
			kind,
			sourceWidgetRef,
			...kind === "point" && field ? { field } : {},
			...kind === "point" ? { values } : {},
			...kind === "interval" ? { channels: channels || {} } : {},
			...kind === "predicate" ? { predicates } : {},
			selectionRef: ref,
			selectionId: id,
			sourceWidgetId: null,
			scope: "local",
			selectionDataRef: null,
			fields: [],
			value: {},
			domain: null,
			predicates: [],
			summary: "",
			aggregateName: null,
			...selection,
			ref,
			id,
			kind,
			sourceWidgetRef,
			selectionRef: ref,
			selectionId: id,
			...kind === "interval" ? { channels: channels || {} } : {},
			...kind === "predicate" ? { predicates } : {},
			...kind === "point" && field ? { field } : {},
			...kind === "point" ? { values } : {}
		};
	}
	function makeInteractionFeedbackState(feedback) {
		return {
			hoveredItem: void 0,
			highlightedKeys: [],
			tooltip: void 0,
			inboundLinkIds: [],
			highlightLinkIds: [],
			linkedSourceRefs: [],
			sharedSelectionSourceWidgetId: void 0,
			...feedback
		};
	}
	function makeWidgetState(state) {
		const data = state?.data || {};
		const materializedDataRef = data.materializedDataRef || data.currentDataRef || null;
		return {
			version: 1,
			updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			...state,
			data: {
				sourceDataRef: null,
				materializedDataRef,
				currentDataRef: materializedDataRef,
				rowCount: 0,
				visibleCount: 0,
				selectedCount: 0,
				...data,
				materializedDataRef
			},
			encodings: { ...state?.encodings || {} },
			transforms: Array.isArray(state?.transforms) ? state.transforms.map((item) => makeTransformState(item)) : [],
			view: makeViewTransformState$1(state?.view),
			selections: Object.fromEntries(Object.entries(state?.selections || {}).map(([ref, selection]) => [ref, makeSelectionState(selection)])),
			feedback: makeInteractionFeedbackState(state?.feedback),
			humanInteraction: state?.humanInteraction || void 0
		};
	}
	function makeWorkspaceState(state) {
		return {
			stateId: "main:empty",
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			widgets: {},
			shared: {
				selections: {
					registry: {},
					views: {
						primary: null,
						byWidget: {}
					}
				},
				focus: null,
				highlight: null,
				viewport: null,
				globalFilters: {},
				focusedWidget: void 0,
				links: {
					definitions: [],
					topology: {}
				},
				...state?.shared || {}
			},
			taskContext: state?.taskContext || void 0,
			replayContext: state?.replayContext || void 0,
			delta: state?.delta || void 0,
			...state
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/shared/filterPatch.js
	function buildWidgetFilterPatch({ targetWidget, currentState, actionName, field, fields, predicates, values, value, range, visibleCount = null }) {
		const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget || {};
		const nextTransform = makeTransformState({
			kind: "filter",
			source: "action",
			sourceWidgetId: targetWidget.widgetId || null,
			ref: `${targetWidget.ref}/transform/${actionName}`,
			spec: {
				actionName,
				...field ? { field } : {},
				...Array.isArray(fields) && fields.length > 0 ? { fields } : {},
				...values !== void 0 ? { values } : {},
				...value !== void 0 ? { value } : {},
				...range !== void 0 ? { range } : {},
				predicates: Array.isArray(predicates) ? predicates : []
			}
		});
		const nextTransforms = [...(Array.isArray(currentWidgetState?.transforms) ? currentWidgetState.transforms : []).filter((transform) => !(transform?.kind === "filter" && transform?.source === "action" && transform?.spec?.actionName === actionName)), nextTransform];
		return { [targetWidget.ref]: {
			transforms: nextTransforms,
			data: {
				...currentWidgetState?.data || {},
				...Number.isFinite(visibleCount) ? { visibleCount } : {}
			}
		} };
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/shared/viewPatch.js
	function buildWidgetViewPatch({ targetWidget, view }) {
		return { [targetWidget.ref]: { view: {
			...targetWidget?.view || {},
			...view || {}
		} } };
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/selectionStateShape.js
	function cloneValue$7(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeScalarArray$1(values) {
		if (!Array.isArray(values)) return void 0;
		const normalized = values.filter((value) => typeof value === "string" || typeof value === "number");
		return normalized.length > 0 ? normalized : void 0;
	}
	function normalizeFieldArray(fields) {
		if (!Array.isArray(fields)) return void 0;
		const normalized = fields.filter((field) => typeof field === "string" && field.length > 0);
		return normalized.length > 0 ? normalized : void 0;
	}
	function normalizeString$5(value) {
		return typeof value === "string" && value.length > 0 ? value : void 0;
	}
	function buildSelectionStateInput(selection) {
		if (!selection || typeof selection !== "object") return null;
		const nextState = {
			selectionRef: normalizeString$5(selection.selection_ref) || normalizeString$5(selection.selectionRef),
			selectionId: normalizeString$5(selection.selection_id) || normalizeString$5(selection.selectionId),
			kind: selection.selection_type || selection.kind || "interval",
			sourceWidgetRef: normalizeString$5(selection.source_widget_ref) || normalizeString$5(selection.sourceWidgetRef),
			sourceWidgetId: normalizeString$5(selection.source_widget_id) || normalizeString$5(selection.sourceWidgetId),
			scope: normalizeString$5(selection.scope) || "local",
			selectionDataRef: normalizeString$5(selection.selection_data_ref) || normalizeString$5(selection.selectionDataRef),
			domain: selection.domain || null,
			predicates: Array.isArray(selection.predicates) ? cloneValue$7(selection.predicates) : [],
			summary: typeof selection.summary === "string" ? selection.summary : ""
		};
		if (typeof selection.aggregateName === "string" && selection.aggregateName.length > 0) nextState.aggregateName = selection.aggregateName;
		const fields = normalizeFieldArray(selection.fields);
		if (fields) nextState.fields = fields;
		if (selection.value && typeof selection.value === "object" && !Array.isArray(selection.value)) nextState.value = cloneValue$7(selection.value);
		if (typeof selection.keyField === "string" && selection.keyField.length > 0) nextState.keyField = selection.keyField;
		const keys = normalizeScalarArray$1(selection.keys);
		if (keys) nextState.keys = keys;
		if (typeof selection.field === "string" && selection.field.length > 0) nextState.field = selection.field;
		const values = normalizeScalarArray$1(selection.values);
		if (values) nextState.values = values;
		return nextState;
	}
	function buildSelectionPayloadFromState({ selectionRef, selectionState, sourceWidgetId, selectedCount = 0 }) {
		if (!selectionState) return null;
		const normalizedSelectionRef = typeof selectionRef === "string" ? selectionRef : selectionState.selectionRef || null;
		const normalizedSelectionId = selectionState.selectionId || (typeof normalizedSelectionRef === "string" ? normalizedSelectionRef.split("/").pop() || null : null) || `sel_${Date.now()}`;
		const normalizedSourceWidgetId = sourceWidgetId || selectionState.sourceWidgetId || void 0;
		const payload = {
			selection_ref: normalizedSelectionRef || void 0,
			selection_id: normalizedSelectionId,
			source_widget_ref: selectionState.sourceWidgetRef || void 0,
			source_widget_id: normalizedSourceWidgetId,
			selection_type: selectionState.kind || "interval",
			scope: selectionState.scope || "local",
			selection_data_ref: selectionState.selectionDataRef || void 0,
			domain: selectionState.domain || null,
			predicates: Array.isArray(selectionState.predicates) ? cloneValue$7(selectionState.predicates) : [],
			count: selectedCount,
			summary: selectionState.summary || ""
		};
		if (typeof selectionState.aggregateName === "string" && selectionState.aggregateName.length > 0) payload.aggregateName = selectionState.aggregateName;
		const fields = normalizeFieldArray(selectionState.fields);
		if (fields) payload.fields = fields;
		if (selectionState.value && typeof selectionState.value === "object" && !Array.isArray(selectionState.value) && Object.keys(selectionState.value).length > 0) payload.value = cloneValue$7(selectionState.value);
		if (typeof selectionState.keyField === "string" && selectionState.keyField.length > 0) payload.keyField = selectionState.keyField;
		const keys = normalizeScalarArray$1(selectionState.keys);
		if (keys) payload.keys = keys;
		if (typeof selectionState.field === "string" && selectionState.field.length > 0) payload.field = selectionState.field;
		const values = normalizeScalarArray$1(selectionState.values);
		if (values) payload.values = values;
		return payload;
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/selectionStateModel.js
	function clone$38(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function cloneObject$2(value) {
		return value && typeof value === "object" && !Array.isArray(value) ? clone$38(value) : {};
	}
	function normalizeString$4(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizePredicates(value) {
		return Array.isArray(value) ? clone$38(value) : [];
	}
	function normalizePrimarySelectionPayload(value) {
		if (!value || typeof value !== "object" || Array.isArray(value)) return null;
		return clone$38(value);
	}
	function normalizeWidgetGroupingKey(value) {
		return normalizeString$4(value);
	}
	function normalizePrimarySelectionView(primary = null, registry = {}) {
		const normalizedPrimary = normalizePrimarySelectionPayload(primary);
		if (!normalizedPrimary) return null;
		const normalizedRegistry = cloneObject$2(registry);
		const selectionRef = normalizeString$4(normalizedPrimary.selectionRef || normalizedPrimary.selection_ref);
		const registryEntry = selectionRef ? normalizedRegistry[selectionRef] || null : null;
		const merged = {
			...registryEntry && typeof registryEntry === "object" ? clone$38(registryEntry) : {},
			...normalizedPrimary
		};
		return {
			...merged,
			selectionRef: selectionRef || normalizeString$4(registryEntry?.selectionRef || registryEntry?.selection_ref),
			selectionId: normalizeString$4(merged.selectionId || merged.selection_id),
			sourceWidgetRef: normalizeString$4(merged.sourceWidgetRef || merged.source_widget_ref),
			sourceWidgetId: normalizeString$4(merged.sourceWidgetId || merged.source_widget_id),
			summary: typeof merged.summary === "string" ? merged.summary : "",
			predicates: normalizePredicates(merged.predicates),
			selectionDataRef: normalizeString$4(merged.selectionDataRef || merged.selection_data_ref),
			scope: normalizeString$4(merged.scope) || "local"
		};
	}
	function deriveByWidgetViewFromRegistry(registry = {}) {
		const normalizedRegistry = cloneObject$2(registry);
		const nextByWidget = {};
		for (const [selectionRef, selectionState] of Object.entries(normalizedRegistry)) {
			if (!selectionState || typeof selectionState !== "object") continue;
			const normalizedView = normalizePrimarySelectionView({
				...selectionState,
				selectionRef
			}, normalizedRegistry);
			const widgetKey = normalizeWidgetGroupingKey(normalizedView?.sourceWidgetId || normalizedView?.source_widget_id || normalizedView?.sourceWidgetRef || normalizedView?.source_widget_ref);
			if (!widgetKey || !normalizedView) continue;
			nextByWidget[widgetKey] = normalizedView;
		}
		return nextByWidget;
	}
	function readSelectionRegistry(shared = {}) {
		const canonicalRegistry = cloneObject$2(shared?.selections?.registry);
		if (Object.keys(canonicalRegistry).length > 0) return canonicalRegistry;
		return cloneObject$2(shared?.activeSelections);
	}
	function readSelectionPrimaryView(shared = {}) {
		const registry = readSelectionRegistry(shared);
		const explicitPrimary = shared?.selections?.views?.primary;
		if (explicitPrimary != null) return normalizePrimarySelectionView(explicitPrimary, registry);
		const legacyPrimarySelectionRef = normalizeString$4(shared?.primarySelectionRef);
		return legacyPrimarySelectionRef ? normalizePrimarySelectionView({ selectionRef: legacyPrimarySelectionRef }, registry) : null;
	}
	function readSelectionByWidgetView(shared = {}) {
		const registry = readSelectionRegistry(shared);
		const explicitByWidget = shared?.selections?.views?.byWidget;
		if (!explicitByWidget || typeof explicitByWidget !== "object" || Array.isArray(explicitByWidget)) return deriveByWidgetViewFromRegistry(registry);
		const normalizedByWidget = {};
		for (const [widgetKey, value] of Object.entries(explicitByWidget)) {
			const normalizedWidgetKey = normalizeWidgetGroupingKey(widgetKey);
			const normalizedView = normalizePrimarySelectionView(value, registry);
			if (!normalizedWidgetKey || !normalizedView) continue;
			normalizedByWidget[normalizedWidgetKey] = normalizedView;
		}
		return Object.keys(normalizedByWidget).length > 0 ? normalizedByWidget : deriveByWidgetViewFromRegistry(registry);
	}
	function resolvePrimarySelectionEntry(shared = {}) {
		const registry = readSelectionRegistry(shared);
		const primaryView = readSelectionPrimaryView(shared);
		const primarySelectionRef = primaryView?.selectionRef || null;
		if (primarySelectionRef && registry[primarySelectionRef]) return [primarySelectionRef, normalizePrimarySelectionView(primaryView, registry)];
		const selectionEntries = Object.entries(registry);
		if (selectionEntries.length === 0) return null;
		const [selectionRef, selectionState] = selectionEntries[0];
		return [selectionRef, normalizePrimarySelectionView({
			...selectionState && typeof selectionState === "object" ? selectionState : {},
			selectionRef
		}, registry)];
	}
	function withSelectionSubmodel(shared = {}, { registry, primary, byWidget } = {}) {
		const previousSelections = shared?.selections || {};
		const previousViews = previousSelections?.views || {};
		const nextRegistry = registry === void 0 ? readSelectionRegistry(shared) : cloneObject$2(registry);
		const nextPrimary = primary === void 0 ? normalizePrimarySelectionView(previousViews?.primary, nextRegistry) || (normalizeString$4(shared?.primarySelectionRef) ? normalizePrimarySelectionView({ selectionRef: shared.primarySelectionRef }, nextRegistry) : null) : normalizePrimarySelectionView(primary, nextRegistry);
		const nextByWidget = byWidget === void 0 ? readSelectionByWidgetView({
			...shared || {},
			selections: {
				...previousSelections,
				registry: nextRegistry,
				views: {
					...previousViews,
					byWidget: previousViews?.byWidget
				}
			}
		}) : (() => {
			const normalizedByWidget = {};
			const rawByWidget = cloneObject$2(byWidget);
			for (const [widgetKey, value] of Object.entries(rawByWidget)) {
				const normalizedWidgetKey = normalizeWidgetGroupingKey(widgetKey);
				const normalizedView = normalizePrimarySelectionView(value, nextRegistry);
				if (!normalizedWidgetKey || !normalizedView) continue;
				normalizedByWidget[normalizedWidgetKey] = normalizedView;
			}
			return normalizedByWidget;
		})();
		return {
			...shared || {},
			activeSelections: nextRegistry,
			primarySelectionRef: nextPrimary?.selectionRef || null,
			selections: {
				...previousSelections,
				registry: nextRegistry,
				views: {
					...previousViews,
					primary: nextPrimary,
					byWidget: nextByWidget
				}
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/shared/selectionPatch.js
	var MERGE_SHARED_PATCH_MODE$1 = "widgetva.mergeSharedPatch";
	function buildWidgetSelectionPatch({ targetWidget, selection }) {
		const selectionId = selection.selection_id || selection.selectionId || selection.id || "selection";
		const selectionRef = `${targetWidget.ref}/selection/${selectionId}`;
		const selectionState = makeSelectionState(buildSelectionStateInput({
			...selection,
			selectionRef,
			selectionId,
			sourceWidgetRef: targetWidget.ref,
			sourceWidgetId: targetWidget.widgetId || null
		}));
		const nextRegistry = { [selectionRef]: selectionState };
		const primarySelectionView = {
			...selectionState,
			selectionRef
		};
		const widgetSelectionViews = { [targetWidget.widgetId || targetWidget.ref]: primarySelectionView };
		return {
			[targetWidget.ref]: {
				selections: { [selectionRef]: selectionState },
				...Number.isFinite(selection.count) ? { data: { selectedCount: selection.count } } : {}
			},
			shared: {
				__widgetvaPatchMode: MERGE_SHARED_PATCH_MODE$1,
				...withSelectionSubmodel({}, {
					registry: nextRegistry,
					primary: primarySelectionView,
					byWidget: widgetSelectionViews
				}),
				focusedWidget: targetWidget.ref
			}
		};
	}
	function buildRuntimeSelectionPatch({ state, targetWidget, selection }) {
		const selectionId = selection.selection_id || selection.selectionId || selection.id || "selection";
		const selectionRef = `${targetWidget.ref}/selection/${selectionId}`;
		const selectionState = makeSelectionState(buildSelectionStateInput({
			...selection,
			selectionRef,
			selectionId,
			sourceWidgetRef: targetWidget.ref,
			sourceWidgetId: targetWidget.widgetId || null
		}));
		const currentWidgetState = state?.widgets?.[targetWidget.ref] || targetWidget || {};
		const currentShared = state?.shared || {};
		const nextRegistry = {
			...readSelectionRegistry(currentShared),
			[selectionRef]: selectionState
		};
		const primaryView = normalizePrimarySelectionView({
			...selectionState,
			selectionRef
		}, nextRegistry);
		const nextByWidget = {
			...readSelectionByWidgetView(currentShared),
			[targetWidget.widgetId || targetWidget.ref]: primaryView
		};
		return {
			[targetWidget.ref]: {
				selections: {
					...currentWidgetState.selections || {},
					[selectionRef]: selectionState
				},
				data: {
					...currentWidgetState.data || {},
					...Number.isFinite(selection.count) ? { selectedCount: selection.count } : {}
				}
			},
			shared: {
				...withSelectionSubmodel(currentShared, {
					registry: nextRegistry,
					primary: primaryView,
					byWidget: nextByWidget
				}),
				focusedWidget: targetWidget.ref
			}
		};
	}
	function buildClearSelectionPatch({ state, targetWidget = null }) {
		const shared = state?.shared || {};
		const widgets = state?.widgets || {};
		const targetRef = targetWidget?.ref || null;
		const targetWidgetState = targetRef ? widgets?.[targetRef] || targetWidget : null;
		const selectionRefsToClear = targetRef ? Object.keys(targetWidgetState?.selections || {}) : Object.keys(readSelectionRegistry(shared));
		const refsToClear = new Set(selectionRefsToClear);
		const nextRegistry = { ...readSelectionRegistry(shared) };
		for (const selectionRef of refsToClear) delete nextRegistry[selectionRef];
		const currentByWidget = readSelectionByWidgetView(shared);
		const nextByWidget = {};
		for (const [widgetKey, selectionView] of Object.entries(currentByWidget || {})) if (!selectionView?.selectionRef || !refsToClear.has(selectionView.selectionRef)) nextByWidget[widgetKey] = selectionView;
		const patch = { shared: withSelectionSubmodel(shared, {
			registry: nextRegistry,
			primary: null,
			byWidget: nextByWidget
		}) };
		if (targetRef) patch[targetRef] = {
			selections: {},
			data: {
				...targetWidgetState?.data || {},
				selectedCount: 0
			}
		};
		else for (const [widgetRef, widgetState] of Object.entries(widgets)) {
			if (Object.keys(widgetState?.selections || {}).length === 0) continue;
			patch[widgetRef] = {
				selections: {},
				data: {
					...widgetState?.data || {},
					selectedCount: 0
				}
			};
		}
		return patch;
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/bar/semanticPatches.js
	function replaceActionTransform$6(transforms, actionName, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !(transform?.source === "action" && transform?.spec?.actionName === actionName));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function readCurrentWidgetState$4(currentState, targetWidget) {
		return currentState?.widgets?.[targetWidget.ref] || targetWidget || {};
	}
	function buildBarCategorySelectionPatch({ targetWidget, currentState, actionName, field, values, selectedCount }) {
		return buildWidgetSelectionPatch({
			targetWidget,
			currentState,
			selection: {
				selection_id: field,
				source_widget_id: targetWidget.widgetId || void 0,
				selection_type: "category",
				field,
				values,
				predicates: [{
					field,
					op: "in",
					value: values
				}],
				count: selectedCount,
				summary: `${field}: ${values.join(", ")}`
			}
		});
	}
	function buildBarVisibilityPatch({ targetWidget, currentState, actionName, field, visibleValues, operation, changedValues, rows }) {
		const currentWidgetState = readCurrentWidgetState$4(currentState, targetWidget);
		const visibleCount = rows.filter((row) => visibleValues.includes(row?.[field])).length;
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$6(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "filter",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					field,
					values: visibleValues,
					predicates: [{
						field,
						op: "in",
						value: visibleValues
					}],
					operation,
					changedValues
				}
			})),
			data: {
				...currentWidgetState.data || {},
				visibleCount,
				analysis: {
					...currentWidgetState.data?.analysis || {},
					visibility: {
						sourceAction: actionName,
						mode: "category",
						field,
						visibleValues,
						operation,
						changedValues
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				addRemove: {
					mode: "categoryVisibility",
					sourceAction: actionName,
					field,
					visibleValues,
					operation,
					changedValues
				}
			}
		} };
	}
	function buildBarItemVisibilityPatch({ targetWidget, currentState, actionName, xField, subField, visibleItems, operation, changedItems, rows }) {
		const currentWidgetState = readCurrentWidgetState$4(currentState, targetWidget);
		const visibleKeys = new Set(visibleItems.map((item) => JSON.stringify([item.x, item.sub])));
		const visibleCount = rows.filter((row) => visibleKeys.has(JSON.stringify([row?.[xField], row?.[subField]]))).length;
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$6(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "filter",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					fields: [xField, subField],
					values: visibleItems,
					predicates: [{
						fields: [xField, subField],
						op: "tupleIn",
						value: visibleItems
					}],
					operation,
					changedItems
				}
			})),
			data: {
				...currentWidgetState.data || {},
				visibleCount,
				analysis: {
					...currentWidgetState.data?.analysis || {},
					visibility: {
						sourceAction: actionName,
						mode: "item",
						xField,
						subField,
						visibleItems,
						operation,
						changedItems
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				addRemove: {
					mode: "itemVisibility",
					sourceAction: actionName,
					xField,
					subField,
					visibleItems,
					operation,
					changedItems
				}
			}
		} };
	}
	function buildBarExpandStackPatch({ targetWidget, currentState, category, categoryField, subField }) {
		const currentWidgetState = readCurrentWidgetState$4(currentState, targetWidget);
		const actionName = "bar.expandStack";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$6(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "filter",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					field: categoryField,
					values: [category],
					predicates: [{
						field: categoryField,
						op: "in",
						value: [category]
					}]
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					expandStack: {
						sourceAction: actionName,
						category,
						categoryField,
						subField
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				reencode: {
					mode: "expandStack",
					sourceAction: actionName,
					category,
					categoryField,
					subField
				}
			}
		} };
	}
	function buildBarStackModePatch({ targetWidget, currentState, mode, subField }) {
		const currentWidgetState = readCurrentWidgetState$4(currentState, targetWidget);
		const actionName = "bar.toggleStackMode";
		return { [targetWidget.ref]: {
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					stackMode: {
						sourceAction: actionName,
						mode,
						subField
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				reencode: {
					mode: "stackMode",
					sourceAction: actionName,
					layout: mode,
					subField
				}
			}
		} };
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/bar/runtimeActions.js
	function targetWidgetState$5(targetWidget) {
		return { widgets: { [targetWidget.ref]: targetWidget } };
	}
	function detectSubcategoryField$1(spec, explicitField) {
		const encoding = spec?.encoding || {};
		const inferredField = encoding?.xOffset?.field || encoding?.color?.field || null;
		if (typeof explicitField === "string" && explicitField.trim().length > 0) return explicitField === inferredField ? explicitField : inferredField;
		return inferredField;
	}
	function detectCategoryField(spec, explicitField) {
		if (typeof explicitField === "string" && explicitField.trim().length > 0) return explicitField;
		const encoding = spec?.encoding || {};
		const xField = encoding?.x?.field || null;
		const yField = encoding?.y?.field || null;
		const xType = encoding?.x?.type || null;
		const yType = encoding?.y?.type || null;
		if ((xType === "nominal" || xType === "ordinal") && xField) return xField;
		if ((yType === "nominal" || yType === "ordinal") && yField) return yField;
		return xField || yField || null;
	}
	function readMarkType$7(mark) {
		return typeof mark === "string" ? mark : mark?.type || null;
	}
	function isBarFamilyMark(mark) {
		return readMarkType$7(mark) === "bar";
	}
	function collectNestedBarSpecs(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const nested = [];
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) nested.push(...spec[key].filter((entry) => entry && typeof entry === "object"));
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) nested.push(spec.spec);
		return nested;
	}
	function findRepresentativeBarSpec(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		if (isBarFamilyMark(spec?.mark) && spec?.encoding && typeof spec.encoding === "object") return spec;
		if (Array.isArray(spec?.layer)) {
			const layeredMatch = spec.layer.find((entry) => entry && typeof entry === "object" && isBarFamilyMark(entry?.mark) && entry?.encoding && typeof entry.encoding === "object");
			if (layeredMatch) return layeredMatch;
		}
		for (const child of collectNestedBarSpecs(spec)) {
			const match = findRepresentativeBarSpec(child);
			if (match) return match;
		}
		return null;
	}
	function uniqueSortedValues(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => value != null))].sort((left, right) => String(left).localeCompare(String(right)));
	}
	function readBarRowsFromContext(ctx, targetWidget, rawSpec) {
		const rows = ctx.readRows(targetWidget.ref);
		return Array.isArray(rows) && rows.length > 0 ? rows : Array.isArray(rawSpec?.data?.values) ? rawSpec.data.values : [];
	}
	function resolveBarFields({ currentWidgetState, targetWidget, explicitField, explicitXField, explicitSubField }) {
		const rawSpec = currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || targetWidget?.currentSpec || targetWidget?.rawSpec || null;
		const representativeSpec = findRepresentativeBarSpec(rawSpec) || rawSpec;
		return {
			rawSpec,
			representativeSpec,
			categoryField: detectCategoryField(representativeSpec, explicitField || explicitXField),
			subcategoryField: detectSubcategoryField$1(representativeSpec, explicitSubField),
			colorField: representativeSpec?.encoding?.color?.field || null
		};
	}
	function registerBarActions(actionExecutor) {
		const registerCategorySelectionAction = (actionName) => {
			if (actionExecutor.has(actionName)) return;
			actionExecutor.register({ name: actionName }, async (params, ctx) => {
				const targetWidget = ctx.targetWidget();
				const field = typeof params.field === "string" ? params.field : null;
				const values = Array.isArray(params.values) ? params.values.filter((value) => typeof value === "string") : [];
				if (!targetWidget || !field || values.length === 0) throw new Error(`${actionName} requires a bar target, field, and one or more values.`);
				const matchedCount = ctx.readRows(targetWidget.ref).filter((row) => values.includes(row?.[field])).length;
				return {
					patch: buildBarCategorySelectionPatch({
						targetWidget,
						actionName,
						field,
						values,
						selectedCount: matchedCount
					}),
					affectedRefs: [targetWidget.ref],
					result: { selectedCount: matchedCount },
					selectedCount: matchedCount,
					verificationHints: ["Read the updated bar selection state.", "Read the linked widget feedback or visible rows to confirm highlight propagation."],
					propagateFromSelection: true
				};
			});
		};
		registerCategorySelectionAction("bar.clickCategory");
		registerCategorySelectionAction("bar.selectCategory");
		if (!actionExecutor.has("bar.sortBars")) actionExecutor.register({ name: "bar.sortBars" }, async (params, ctx) => {
			const channel = typeof params.channel === "string" ? params.channel : null;
			const order = params.order === "ascending" || params.order === "descending" ? params.order : null;
			const field = typeof params.field === "string" ? params.field : null;
			const aggregate = typeof params.aggregate === "string" ? params.aggregate : null;
			const bySubcategory = params.bySubcategory;
			const targetWidget = ctx.targetWidget();
			const targetWidgetId = targetWidget?.widgetId || null;
			if (!channel || !order || !targetWidgetId) throw new Error("bar.sortBars requires a bar target, channel, and order.");
			return {
				patch: buildWidgetViewPatch({
					targetWidget,
					view: { sort: {
						sourceAction: "bar.sortBars",
						channel,
						order,
						mode: "direction",
						...field ? { field } : {},
						...aggregate ? { aggregate } : {},
						...bySubcategory != null ? { bySubcategory } : {}
					} }
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidgetId,
					channel,
					order,
					...field ? { field } : {},
					...aggregate ? { aggregate } : {},
					...bySubcategory != null ? { bySubcategory } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the bar chart encoding now uses an explicit sorted category array.", "Read the target widget state to confirm the requested category ranking propagated to the view."]
			};
		});
		if (!actionExecutor.has("bar.highlightTopN")) actionExecutor.register({ name: "bar.highlightTopN" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const n = Number.isFinite(params.n) ? Math.max(1, Math.floor(params.n)) : null;
			const order = params.order === "ascending" || params.order === "descending" ? params.order : "descending";
			if (!targetWidget || !n) throw new Error("bar.highlightTopN requires a bar target and a positive integer n.");
			return {
				patch: buildWidgetViewPatch({
					targetWidget,
					view: { highlight: {
						sourceAction: "bar.highlightTopN",
						mode: "topN",
						n,
						order,
						...typeof params.categoryField === "string" ? { categoryField: params.categoryField } : {},
						...typeof params.measureField === "string" ? { measureField: params.measureField } : {}
					} }
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					n,
					order,
					...typeof params.categoryField === "string" ? { categoryField: params.categoryField } : {},
					...typeof params.measureField === "string" ? { measureField: params.measureField } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the bar chart opacity condition now highlights the top-N categories.", "Read the target widget view state to confirm the requested top categories are emphasized."]
			};
		});
		if (!actionExecutor.has("bar.filterCategories")) actionExecutor.register({ name: "bar.filterCategories" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const categories = Array.isArray(params.categories) ? params.categories.filter((value) => value != null) : [];
			if (!targetWidget || categories.length === 0) throw new Error("bar.filterCategories requires a bar target and one or more categories.");
			const currentState = targetWidgetState$5(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const rawSpec = currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || targetWidget?.currentSpec || targetWidget?.rawSpec || null;
			const field = typeof params.field === "string" ? params.field : detectCategoryField(rawSpec);
			if (!field) throw new Error("bar.filterCategories requires a categorical field on the active bar spec.");
			const rows = ctx.readRows(targetWidget.ref);
			const nextVisibleRows = (Array.isArray(rows) && rows.length > 0 ? rows : Array.isArray(rawSpec?.data?.values) ? rawSpec.data.values : []).filter((row) => categories.includes(row?.[field]));
			return {
				patch: buildWidgetFilterPatch({
					targetWidget,
					currentState,
					actionName: "bar.filterCategories",
					field,
					values: categories,
					predicates: [{
						field,
						op: "in",
						value: categories
					}],
					visibleCount: nextVisibleRows.length
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					field,
					categories
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the bar chart transform now filters to the requested categories.", "Read the target widget rows to confirm only the requested categories remain visible."]
			};
		});
		if (!actionExecutor.has("bar.addBars")) actionExecutor.register({ name: "bar.addBars" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
			if (!targetWidget || values.length === 0) throw new Error("bar.addBars requires a bar target and one or more category values.");
			const currentState = targetWidgetState$5(targetWidget);
			const currentWidgetState = readCurrentWidgetState$4(currentState, targetWidget);
			const { rawSpec, categoryField } = resolveBarFields({
				currentWidgetState,
				targetWidget,
				explicitField: params.field
			});
			if (!categoryField) throw new Error("bar.addBars requires a categorical field on the active bar spec.");
			const rows = readBarRowsFromContext(ctx, targetWidget, rawSpec);
			const existingValues = uniqueSortedValues(rows.map((row) => row?.[categoryField]));
			const previousVisibility = currentWidgetState.data?.analysis?.visibility;
			const currentVisible = previousVisibility?.mode === "category" && previousVisibility?.field === categoryField ? new Set(previousVisibility.visibleValues || []) : new Set(existingValues);
			for (const value of values) if (existingValues.length === 0 || existingValues.includes(value)) currentVisible.add(value);
			const visibleValues = uniqueSortedValues([...currentVisible]);
			return {
				patch: buildBarVisibilityPatch({
					targetWidget,
					currentState,
					actionName: "bar.addBars",
					field: categoryField,
					visibleValues,
					operation: "add",
					changedValues: values,
					rows
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					values,
					field: categoryField,
					visibleValues
				},
				verificationHints: ["Read the target widget state and confirm data.analysis.visibility includes the requested categories.", "Read the target widget transforms and confirm bar.addBars is recorded as a semantic filter."]
			};
		});
		if (!actionExecutor.has("bar.removeBars")) actionExecutor.register({ name: "bar.removeBars" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
			if (!targetWidget || values.length === 0) throw new Error("bar.removeBars requires a bar target and one or more category values.");
			const currentState = targetWidgetState$5(targetWidget);
			const currentWidgetState = readCurrentWidgetState$4(currentState, targetWidget);
			const { rawSpec, categoryField } = resolveBarFields({
				currentWidgetState,
				targetWidget,
				explicitField: params.field
			});
			if (!categoryField) throw new Error("bar.removeBars requires a categorical field on the active bar spec.");
			const rows = readBarRowsFromContext(ctx, targetWidget, rawSpec);
			const existingValues = uniqueSortedValues(rows.map((row) => row?.[categoryField]));
			const previousVisibility = currentWidgetState.data?.analysis?.visibility;
			const currentVisible = previousVisibility?.mode === "category" && previousVisibility?.field === categoryField ? new Set(previousVisibility.visibleValues || []) : new Set(existingValues);
			for (const value of values) currentVisible.delete(value);
			const visibleValues = uniqueSortedValues([...currentVisible]);
			return {
				patch: buildBarVisibilityPatch({
					targetWidget,
					currentState,
					actionName: "bar.removeBars",
					field: categoryField,
					visibleValues,
					operation: "remove",
					changedValues: values,
					rows
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					values,
					field: categoryField,
					visibleValues
				},
				verificationHints: ["Read the target widget state and confirm data.analysis.visibility excludes the requested categories.", "Read the target widget transforms and confirm bar.removeBars is recorded as a semantic filter."]
			};
		});
		if (!actionExecutor.has("bar.addBarItems")) actionExecutor.register({ name: "bar.addBarItems" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const items = Array.isArray(params.items) ? params.items.filter((item) => item && typeof item === "object" && item.x != null && item.sub != null) : [];
			if (!targetWidget || items.length === 0) throw new Error("bar.addBarItems requires a bar target and one or more { x, sub } items.");
			const currentState = targetWidgetState$5(targetWidget);
			const currentWidgetState = readCurrentWidgetState$4(currentState, targetWidget);
			const { rawSpec, categoryField, subcategoryField } = resolveBarFields({
				currentWidgetState,
				targetWidget,
				explicitXField: params.xField,
				explicitSubField: params.subField
			});
			if (!categoryField || !subcategoryField) throw new Error("bar.addBarItems requires category and subcategory fields on the active bar spec.");
			const rows = readBarRowsFromContext(ctx, targetWidget, rawSpec);
			const existingItemKeys = new Set(rows.map((row) => row?.[categoryField] != null && row?.[subcategoryField] != null ? JSON.stringify([row[categoryField], row[subcategoryField]]) : null).filter(Boolean));
			const previousVisibility = currentWidgetState.data?.analysis?.visibility;
			const currentVisibleKeys = previousVisibility?.mode === "item" && previousVisibility?.xField === categoryField && previousVisibility?.subField === subcategoryField ? new Set((previousVisibility.visibleItems || []).map((item) => JSON.stringify([item.x, item.sub]))) : new Set(existingItemKeys);
			for (const item of items) {
				const itemKey = JSON.stringify([item.x, item.sub]);
				if (existingItemKeys.size === 0 || existingItemKeys.has(itemKey)) currentVisibleKeys.add(itemKey);
			}
			const visibleItems = [...currentVisibleKeys].map((itemKey) => {
				const [x, sub] = JSON.parse(itemKey);
				return {
					x,
					sub
				};
			}).sort((left, right) => `${left.x}:${left.sub}`.localeCompare(`${right.x}:${right.sub}`));
			return {
				patch: buildBarItemVisibilityPatch({
					targetWidget,
					currentState,
					actionName: "bar.addBarItems",
					xField: categoryField,
					subField: subcategoryField,
					visibleItems,
					operation: "add",
					changedItems: items,
					rows
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					items,
					xField: categoryField,
					subField: subcategoryField,
					visibleItems
				},
				verificationHints: ["Read the target widget state and confirm data.analysis.visibility includes the requested grouped members.", "Read the target widget transforms and confirm bar.addBarItems is recorded as a semantic tuple filter."]
			};
		});
		if (!actionExecutor.has("bar.removeBarItems")) actionExecutor.register({ name: "bar.removeBarItems" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const items = Array.isArray(params.items) ? params.items.filter((item) => item && typeof item === "object" && item.x != null && item.sub != null) : [];
			if (!targetWidget || items.length === 0) throw new Error("bar.removeBarItems requires a bar target and one or more { x, sub } items.");
			const currentState = targetWidgetState$5(targetWidget);
			const currentWidgetState = readCurrentWidgetState$4(currentState, targetWidget);
			const { rawSpec, categoryField, subcategoryField } = resolveBarFields({
				currentWidgetState,
				targetWidget,
				explicitXField: params.xField,
				explicitSubField: params.subField
			});
			if (!categoryField || !subcategoryField) throw new Error("bar.removeBarItems requires category and subcategory fields on the active bar spec.");
			const rows = readBarRowsFromContext(ctx, targetWidget, rawSpec);
			const existingItemKeys = new Set(rows.map((row) => row?.[categoryField] != null && row?.[subcategoryField] != null ? JSON.stringify([row[categoryField], row[subcategoryField]]) : null).filter(Boolean));
			const previousVisibility = currentWidgetState.data?.analysis?.visibility;
			const currentVisibleKeys = previousVisibility?.mode === "item" && previousVisibility?.xField === categoryField && previousVisibility?.subField === subcategoryField ? new Set((previousVisibility.visibleItems || []).map((item) => JSON.stringify([item.x, item.sub]))) : new Set(existingItemKeys);
			for (const item of items) currentVisibleKeys.delete(JSON.stringify([item.x, item.sub]));
			const visibleItems = [...currentVisibleKeys].map((itemKey) => {
				const [x, sub] = JSON.parse(itemKey);
				return {
					x,
					sub
				};
			}).sort((left, right) => `${left.x}:${left.sub}`.localeCompare(`${right.x}:${right.sub}`));
			return {
				patch: buildBarItemVisibilityPatch({
					targetWidget,
					currentState,
					actionName: "bar.removeBarItems",
					xField: categoryField,
					subField: subcategoryField,
					visibleItems,
					operation: "remove",
					changedItems: items,
					rows
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					items,
					xField: categoryField,
					subField: subcategoryField,
					visibleItems
				},
				verificationHints: ["Read the target widget state and confirm data.analysis.visibility excludes the requested grouped members.", "Read the target widget transforms and confirm bar.removeBarItems is recorded as a semantic tuple filter."]
			};
		});
		if (!actionExecutor.has("bar.filterSubcategories")) actionExecutor.register({ name: "bar.filterSubcategories" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const subcategoriesToRemove = Array.isArray(params.subcategoriesToRemove) ? params.subcategoriesToRemove.filter((value) => value != null) : [];
			if (!targetWidget || subcategoriesToRemove.length === 0) throw new Error("bar.filterSubcategories requires a bar target and one or more subcategories to remove.");
			const currentState = targetWidgetState$5(targetWidget);
			const { rawSpec, subcategoryField } = resolveBarFields({
				currentWidgetState: readCurrentWidgetState$4(currentState, targetWidget),
				targetWidget,
				explicitSubField: params.subField
			});
			if (!subcategoryField) throw new Error("bar.filterSubcategories requires a subcategory field on the active bar spec.");
			const visibleCount = readBarRowsFromContext(ctx, targetWidget, rawSpec).filter((row) => !subcategoriesToRemove.includes(row?.[subcategoryField])).length;
			return {
				patch: buildWidgetFilterPatch({
					targetWidget,
					currentState,
					actionName: "bar.filterSubcategories",
					field: subcategoryField,
					values: subcategoriesToRemove,
					predicates: [{
						field: subcategoryField,
						op: "notIn",
						value: subcategoriesToRemove
					}],
					visibleCount
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					subcategoriesToRemove,
					subField: subcategoryField,
					visibleCount
				},
				verificationHints: ["Read the target widget transforms and confirm bar.filterSubcategories is recorded as a semantic filter.", "Read the target widget data.visibleCount to confirm the filtered row count."]
			};
		});
		if (!actionExecutor.has("bar.expandStack")) actionExecutor.register({ name: "bar.expandStack" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const category = params.category;
			if (!targetWidget || category == null) throw new Error("bar.expandStack requires a bar target and a category value.");
			const currentState = targetWidgetState$5(targetWidget);
			const { categoryField, subcategoryField } = resolveBarFields({
				currentWidgetState: readCurrentWidgetState$4(currentState, targetWidget),
				targetWidget
			});
			if (!categoryField || !subcategoryField) throw new Error("bar.expandStack requires category and subcategory fields on the active bar spec.");
			return {
				patch: buildBarExpandStackPatch({
					targetWidget,
					currentState,
					category,
					categoryField,
					subField: subcategoryField
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					category,
					categoryField,
					subField: subcategoryField
				},
				verificationHints: ["Read the target widget state and confirm view.reencode.mode is expandStack.", "Read the target widget transforms and confirm bar.expandStack filters to the requested category."]
			};
		});
		if (!actionExecutor.has("bar.toggleStackMode")) actionExecutor.register({ name: "bar.toggleStackMode" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const mode = params.mode === "grouped" || params.mode === "stacked" ? params.mode : null;
			if (!targetWidget || !mode) throw new Error("bar.toggleStackMode requires a bar target and mode of either \"grouped\" or \"stacked\".");
			const currentState = targetWidgetState$5(targetWidget);
			const { subcategoryField, colorField } = resolveBarFields({
				currentWidgetState: readCurrentWidgetState$4(currentState, targetWidget),
				targetWidget
			});
			const subField = subcategoryField || colorField;
			if (!subField) throw new Error("bar.toggleStackMode requires a color or subcategory field on the active bar spec.");
			return {
				patch: buildBarStackModePatch({
					targetWidget,
					currentState,
					mode,
					subField
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					mode,
					subField
				},
				verificationHints: ["Read the target widget state and confirm view.reencode.mode is stackMode.", "Read data.analysis.stackMode to confirm the selected grouped/stacked layout."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/heatmap/semanticPatches.js
	function replaceActionTransform$5(transforms, actionName, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !(transform?.source === "action" && transform?.spec?.actionName === actionName));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function readCurrentWidgetState$3(currentState, targetWidget) {
		return currentState?.widgets?.[targetWidget.ref] || targetWidget || {};
	}
	function buildHeatmapSemanticPatch({ targetWidget, currentState, actionName, transformKind = "derive", analysisKey, viewKey, viewState, spec, clearActionName = null }) {
		const currentWidgetState = readCurrentWidgetState$3(currentState, targetWidget);
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$5(currentWidgetState.transforms, clearActionName || actionName, clearActionName ? null : makeTransformState({
				kind: transformKind,
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					...spec || {}
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					[analysisKey]: {
						sourceAction: actionName,
						...spec || {}
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				[viewKey]: viewState === null ? null : {
					sourceAction: actionName,
					...viewState || spec || {}
				}
			}
		} };
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/heatmap/runtimeActions.js
	function targetWidgetState$4(targetWidget) {
		return { widgets: { [targetWidget.ref]: targetWidget } };
	}
	function isPrimitiveSelectionValue(value) {
		return typeof value === "string" || typeof value === "number";
	}
	function resolveHeatmapAxisFieldAlias$1(spec, requestedField, axis) {
		if (typeof requestedField !== "string" || requestedField.length === 0) return null;
		const encodingField = typeof spec?.encoding?.[axis]?.field === "string" ? spec.encoding[axis].field : null;
		if (!encodingField) return requestedField;
		if (requestedField === axis || requestedField === encodingField) return encodingField;
		return requestedField;
	}
	function readMarkType$6(mark) {
		return typeof mark === "string" ? mark : mark?.type || null;
	}
	function isHeatmapFamilyMark(mark) {
		return readMarkType$6(mark) === "rect";
	}
	function collectNestedHeatmapSpecs(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const nested = [];
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) nested.push(...spec[key].filter((entry) => entry && typeof entry === "object"));
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) nested.push(spec.spec);
		return nested;
	}
	function findRepresentativeHeatmapSpec(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		if (isHeatmapFamilyMark(spec?.mark) && spec?.encoding && typeof spec.encoding === "object") return spec;
		if (Array.isArray(spec?.layer)) {
			const layeredMatch = spec.layer.find((entry) => entry && typeof entry === "object" && isHeatmapFamilyMark(entry?.mark) && entry?.encoding && typeof entry.encoding === "object");
			if (layeredMatch) return layeredMatch;
		}
		for (const child of collectNestedHeatmapSpecs(spec)) {
			const match = findRepresentativeHeatmapSpec(child);
			if (match) return match;
		}
		return null;
	}
	function registerHeatmapActions(actionExecutor) {
		const registerHeatmapCellAction = (name, invalidTargetMessage, invalidParamsMessage) => {
			if (actionExecutor.has(name)) return;
			actionExecutor.register({ name }, async (params, ctx) => {
				const targetWidget = ctx.targetWidget();
				const currentSpec = targetWidget?.currentSpec || targetWidget?.rawSpec || null;
				const representativeSpec = findRepresentativeHeatmapSpec(currentSpec) || currentSpec;
				const xField = resolveHeatmapAxisFieldAlias$1(representativeSpec, typeof params.xField === "string" ? params.xField : null, "x");
				const yField = resolveHeatmapAxisFieldAlias$1(representativeSpec, typeof params.yField === "string" ? params.yField : null, "y");
				const xValue = isPrimitiveSelectionValue(params.xValue) ? params.xValue : null;
				const yValue = isPrimitiveSelectionValue(params.yValue) ? params.yValue : null;
				if (!targetWidget || !xField || !yField || xValue == null || yValue == null) throw new Error(invalidParamsMessage);
				const matchedCount = ctx.readRows(targetWidget.ref).filter((row) => row?.[xField] === xValue && row?.[yField] === yValue).length;
				const selection = {
					selection_id: `sel_${Date.now()}`,
					source_widget_id: targetWidget.widgetId || void 0,
					selection_type: "cell",
					fields: [xField, yField],
					value: {
						[xField]: xValue,
						[yField]: yValue
					},
					predicates: [{
						field: xField,
						op: "equals",
						value: xValue
					}, {
						field: yField,
						op: "equals",
						value: yValue
					}],
					count: matchedCount,
					summary: `${xField}: ${xValue}; ${yField}: ${yValue}`
				};
				const currentState = targetWidgetState$4(targetWidget);
				const selectionPatch = buildWidgetSelectionPatch({
					targetWidget,
					currentState,
					selection
				});
				const filterPatch = name === "heatmap.filterCells" ? buildWidgetFilterPatch({
					targetWidget,
					currentState,
					actionName: "heatmap.filterCells",
					fields: [xField, yField],
					value: {
						[xField]: xValue,
						[yField]: yValue
					},
					predicates: selection.predicates,
					visibleCount: matchedCount
				}) : null;
				return {
					patch: filterPatch ? {
						...selectionPatch,
						[targetWidget.ref]: {
							...selectionPatch[targetWidget.ref],
							transforms: filterPatch[targetWidget.ref]?.transforms || [],
							data: {
								...selectionPatch[targetWidget.ref]?.data || {},
								...filterPatch[targetWidget.ref]?.data || {}
							}
						}
					} : selectionPatch,
					affectedRefs: [targetWidget.ref],
					result: { selectedCount: matchedCount },
					selectedCount: matchedCount,
					verificationHints: [...name === "heatmap.filterCells" ? ["Call perception.inspectVisibleRows to verify only the requested heatmap cell remains visible.", "Call perception.inspectViewConfig to verify the active heatmap state includes x/y equality filters for the requested cell."] : ["Read the updated heatmap selection state.", "Read linked widgets to confirm cell-level filter propagation."]],
					propagateFromSelection: name !== "heatmap.filterCells"
				};
			});
		};
		registerHeatmapCellAction("heatmap.filterCells", "heatmap.filterCells requires a valid heatmap target widget.", "heatmap.filterCells requires a heatmap target, xField, yField, xValue, and yValue.");
		registerHeatmapCellAction("heatmap.selectCell", "heatmap.selectCell requires a valid heatmap target widget.", "heatmap.selectCell requires a heatmap target, xField, yField, xValue, and yValue.");
		if (!actionExecutor.has("heatmap.selectSubmatrix")) actionExecutor.register({ name: "heatmap.selectSubmatrix" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const xValues = Array.isArray(params.xValues) ? params.xValues.filter((value) => value != null) : [];
			const yValues = Array.isArray(params.yValues) ? params.yValues.filter((value) => value != null) : [];
			if (!targetWidget || xValues.length === 0 && yValues.length === 0) throw new Error("heatmap.selectSubmatrix requires a heatmap target and at least one of xValues or yValues.");
			const currentWidgetState = targetWidgetState$4(targetWidget)?.widgets?.[targetWidget.ref] || targetWidget;
			const spec = currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null;
			const xField = spec?.encoding?.x?.field || null;
			const yField = spec?.encoding?.y?.field || null;
			if (!xField || !yField) throw new Error("heatmap.selectSubmatrix requires x and y encodings on the active heatmap spec.");
			const matchedCount = ctx.readRows(targetWidget.ref).filter((row) => {
				const xMatch = xValues.length === 0 || xValues.includes(row?.[xField]);
				const yMatch = yValues.length === 0 || yValues.includes(row?.[yField]);
				return xMatch && yMatch;
			}).length;
			const predicates = [];
			if (xValues.length > 0) predicates.push({
				field: xField,
				op: "in",
				value: xValues
			});
			if (yValues.length > 0) predicates.push({
				field: yField,
				op: "in",
				value: yValues
			});
			const selection = {
				selection_id: `sel_${Date.now()}`,
				source_widget_id: targetWidget.widgetId || void 0,
				selection_type: "cell",
				fields: [xField, yField],
				value: {
					...xValues.length > 0 ? { [xField]: xValues } : {},
					...yValues.length > 0 ? { [yField]: yValues } : {}
				},
				predicates,
				count: matchedCount,
				summary: `${xField}: ${xValues.length > 0 ? xValues.join(", ") : "all"}; ${yField}: ${yValues.length > 0 ? yValues.join(", ") : "all"}`
			};
			return {
				patch: buildWidgetSelectionPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					selection
				}),
				affectedRefs: [targetWidget.ref],
				result: { selectedCount: matchedCount },
				selectedCount: matchedCount,
				verificationHints: ["Read the updated heatmap selection state.", "Read linked widgets to confirm submatrix-level filter propagation."],
				propagateFromSelection: true
			};
		});
		if (!actionExecutor.has("heatmap.drilldownAxis")) actionExecutor.register({ name: "heatmap.drilldownAxis" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const level = typeof params.level === "string" ? params.level.toLowerCase().trim() : null;
			const value = params.value;
			const parent = params.parent && typeof params.parent === "object" && !Array.isArray(params.parent) ? params.parent : {};
			if (!targetWidget || !level || value == null || value === "") throw new Error("heatmap.drilldownAxis requires a heatmap target plus level and value.");
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					actionName: "heatmap.drilldownAxis",
					transformKind: "filter",
					analysisKey: "drillDown",
					viewKey: "drillDown",
					viewState: {
						level,
						value,
						parent
					},
					spec: {
						level,
						value,
						parent
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					level,
					value,
					parent
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap x-axis timeUnit now reflects the finer drill-down level.", "Read the target widget view state to confirm the tagged drill-down filter now narrows the visible period."]
			};
		});
		if (!actionExecutor.has("heatmap.resetDrilldown")) actionExecutor.register({ name: "heatmap.resetDrilldown" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			if (!targetWidget) throw new Error("heatmap.resetDrilldown requires a valid heatmap target widget.");
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					actionName: "heatmap.resetDrilldown",
					analysisKey: "drillDown",
					viewKey: "drillDown",
					viewState: null,
					spec: { reset: true },
					clearActionName: "heatmap.drilldownAxis"
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					reset: true
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the original heatmap x-axis encoding was restored.", "Read the target widget view state to confirm the drill-down filter and state marker were removed."]
			};
		});
		if (!actionExecutor.has("heatmap.addMarginalBars")) actionExecutor.register({ name: "heatmap.addMarginalBars" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const op = typeof params.op === "string" ? params.op.toLowerCase().trim() : "mean";
			const showTop = params.showTop !== false;
			const showRight = params.showRight !== false;
			const barSize = Number.isFinite(params.barSize) ? Number(params.barSize) : 70;
			const barColor = typeof params.barColor === "string" && params.barColor.length > 0 ? params.barColor : "#666666";
			const allowedAgg = new Set([
				"mean",
				"sum",
				"median",
				"max",
				"min",
				"count"
			]);
			if (!targetWidget) throw new Error("heatmap.addMarginalBars requires a valid heatmap target widget.");
			if (!showTop && !showRight) throw new Error("heatmap.addMarginalBars requires at least one of showTop/showRight to be true.");
			if (!allowedAgg.has(op)) throw new Error(`heatmap.addMarginalBars does not support op "${params.op}".`);
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					actionName: "heatmap.addMarginalBars",
					transformKind: "aggregate",
					analysisKey: "marginalBars",
					viewKey: "addRemove",
					viewState: {
						mode: "marginalBars",
						op,
						showTop,
						showRight,
						barSize,
						barColor
					},
					spec: {
						op,
						showTop,
						showRight,
						barSize,
						barColor
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					op,
					showTop,
					showRight
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap was composed with top and/or right marginal bar charts.", "Read the target widget view state to confirm the marginal bars aggregate the original heatmap value field."]
			};
		});
		if (!actionExecutor.has("heatmap.highlightRegion")) actionExecutor.register({ name: "heatmap.highlightRegion" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const xValues = Array.isArray(params.xValues) ? params.xValues.filter((value) => value != null) : [];
			const yValues = Array.isArray(params.yValues) ? params.yValues.filter((value) => value != null) : [];
			if (!targetWidget || xValues.length === 0 && yValues.length === 0) throw new Error("heatmap.highlightRegion requires a heatmap target and at least one of xValues or yValues.");
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					actionName: "heatmap.highlightRegion",
					transformKind: "derive",
					analysisKey: "regionHighlight",
					viewKey: "highlight",
					viewState: {
						mode: "regionHighlight",
						xValues,
						yValues
					},
					spec: {
						xValues,
						yValues
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					xValues,
					yValues
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap opacity condition was updated.", "Read the target widget view state to confirm the requested region is emphasized."]
			};
		});
		if (!actionExecutor.has("heatmap.adjustColorScale")) actionExecutor.register({ name: "heatmap.adjustColorScale" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const scheme = typeof params.scheme === "string" ? params.scheme.trim() : "";
			const domain = Array.isArray(params.domain) ? params.domain : null;
			if (!targetWidget || !scheme) throw new Error("heatmap.adjustColorScale requires a heatmap target and a non-empty scheme.");
			if (domain && domain.length !== 2) throw new Error("heatmap.adjustColorScale domain must contain exactly two values when provided.");
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					actionName: "heatmap.adjustColorScale",
					transformKind: "reencode",
					analysisKey: "colorScale",
					viewKey: "reencode",
					viewState: {
						mode: "colorScale",
						scheme,
						...domain ? { domain } : {}
					},
					spec: {
						scheme,
						...domain ? { domain } : {}
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					scheme,
					...domain ? { domain } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap color scale scheme was updated.", "Read the target widget view state to confirm the requested color domain values."]
			};
		});
		if (!actionExecutor.has("heatmap.thresholdMask")) actionExecutor.register({ name: "heatmap.thresholdMask" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const minValue = Number(params.minValue);
			const maxValue = Number(params.maxValue);
			const outsideOpacity = Number.isFinite(params.outsideOpacity) ? Math.max(0, Math.min(1, params.outsideOpacity)) : .1;
			if (!targetWidget || !Number.isFinite(minValue) || !Number.isFinite(maxValue)) throw new Error("heatmap.thresholdMask requires a heatmap target plus finite minValue and maxValue.");
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					actionName: "heatmap.thresholdMask",
					transformKind: "filter",
					analysisKey: "thresholdMask",
					viewKey: "highlight",
					viewState: {
						mode: "thresholdMask",
						minValue,
						maxValue,
						outsideOpacity
					},
					spec: {
						minValue,
						maxValue,
						outsideOpacity,
						predicates: [{
							field: "value",
							op: "between",
							value: [minValue, maxValue]
						}]
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					minValue,
					maxValue,
					outsideOpacity
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap opacity condition now masks values outside the requested threshold range.", "Read the target widget view state to confirm the requested threshold mask was applied."]
			};
		});
		if (!actionExecutor.has("heatmap.filterCellsByRegion")) actionExecutor.register({ name: "heatmap.filterCellsByRegion" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const xValues = Array.isArray(params.xValues) ? params.xValues.filter((value) => value != null) : params.xValue != null ? [params.xValue] : [];
			const yValues = Array.isArray(params.yValues) ? params.yValues.filter((value) => value != null) : params.yValue != null ? [params.yValue] : [];
			if (!targetWidget || xValues.length === 0 && yValues.length === 0) throw new Error("heatmap.filterCellsByRegion requires a heatmap target and at least one of xValues/yValues or xValue/yValue.");
			const currentState = targetWidgetState$4(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const spec = findRepresentativeHeatmapSpec(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null) || currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null;
			const xField = spec?.encoding?.x?.field || null;
			const yField = spec?.encoding?.y?.field || null;
			const predicates = [...xField && xValues.length > 0 ? [{
				field: xField,
				op: "in",
				value: xValues
			}] : [], ...yField && yValues.length > 0 ? [{
				field: yField,
				op: "in",
				value: yValues
			}] : []];
			return {
				patch: buildWidgetFilterPatch({
					targetWidget,
					currentState,
					actionName: "heatmap.filterCellsByRegion",
					fields: [xField, yField].filter(Boolean),
					value: {
						...xField && xValues.length > 0 ? { [xField]: xValues } : {},
						...yField && yValues.length > 0 ? { [yField]: yValues } : {}
					},
					predicates
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					xValues,
					yValues
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap transform now excludes the requested region.", "Read the target widget rows to confirm the requested region no longer appears in the visible matrix."]
			};
		});
		if (!actionExecutor.has("heatmap.highlightRegionByValue")) actionExecutor.register({ name: "heatmap.highlightRegionByValue" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const hasMin = params.minValue != null && params.minValue !== "";
			const hasMax = params.maxValue != null && params.maxValue !== "";
			const minValue = hasMin ? Number(params.minValue) : null;
			const maxValue = hasMax ? Number(params.maxValue) : null;
			const outsideOpacity = Number.isFinite(params.outsideOpacity) ? Math.max(0, Math.min(1, params.outsideOpacity)) : .12;
			if (!targetWidget || !hasMin && !hasMax || hasMin && !Number.isFinite(minValue) || hasMax && !Number.isFinite(maxValue)) throw new Error("heatmap.highlightRegionByValue requires a heatmap target and at least one finite minValue or maxValue.");
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					actionName: "heatmap.highlightRegionByValue",
					transformKind: "derive",
					analysisKey: "valueHighlight",
					viewKey: "highlight",
					viewState: {
						mode: "valueHighlight",
						...hasMin ? { minValue } : {},
						...hasMax ? { maxValue } : {},
						outsideOpacity
					},
					spec: {
						...hasMin ? { minValue } : {},
						...hasMax ? { maxValue } : {},
						outsideOpacity
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					...hasMin ? { minValue } : {},
					...hasMax ? { maxValue } : {},
					outsideOpacity
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap opacity condition now highlights values inside the requested range.", "Read the target widget view state to confirm the value-range highlight was applied without filtering data away."]
			};
		});
		if (!actionExecutor.has("heatmap.clusterRowsCols")) actionExecutor.register({ name: "heatmap.clusterRowsCols" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const clusterRows = params.clusterRows !== false;
			const clusterCols = params.clusterCols !== false;
			const requestedMethod = typeof params.method === "string" ? params.method.toLowerCase().trim() : "sum";
			const method = [
				"sum",
				"mean",
				"max"
			].includes(requestedMethod) ? requestedMethod : "sum";
			if (!targetWidget || !clusterRows && !clusterCols) throw new Error("heatmap.clusterRowsCols requires a heatmap target and at least one of clusterRows or clusterCols to be true.");
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState: targetWidgetState$4(targetWidget),
					actionName: "heatmap.clusterRowsCols",
					transformKind: "aggregate",
					analysisKey: "cluster",
					viewKey: "reencode",
					viewState: {
						mode: "clusterRowsCols",
						clusterRows,
						clusterCols,
						method
					},
					spec: {
						clusterRows,
						clusterCols,
						method
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					clusterRows,
					clusterCols,
					method
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap x/y encodings now carry aggregate sort metadata.", "Read the target widget view state to confirm the requested rows and/or columns are reordered by aggregated value."]
			};
		});
		if (!actionExecutor.has("heatmap.transpose")) actionExecutor.register({ name: "heatmap.transpose" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			if (!targetWidget) throw new Error("heatmap.transpose requires a valid heatmap target widget.");
			const currentState = targetWidgetState$4(targetWidget);
			const transposed = (currentState?.widgets?.[targetWidget.ref] || targetWidget)?.data?.analysis?.transpose?.transposed !== true;
			return {
				patch: buildHeatmapSemanticPatch({
					targetWidget,
					currentState,
					actionName: "heatmap.transpose",
					transformKind: "reencode",
					analysisKey: "transpose",
					viewKey: "reencode",
					viewState: {
						mode: "transpose",
						transposed
					},
					spec: { transposed }
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					transposed
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the heatmap x and y encodings were swapped.", "Read the target widget view state to confirm the transpose marker and any width/height swap were applied."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/viewStateMetadata.js
	function isNumericDomain(domain) {
		return Array.isArray(domain) && domain.length >= 2 && typeof domain[0] === "number" && typeof domain[1] === "number";
	}
	function midpoint(domain) {
		if (!isNumericDomain(domain)) return null;
		return (domain[0] + domain[1]) / 2;
	}
	function span(domain) {
		if (!isNumericDomain(domain)) return null;
		return Math.abs(domain[1] - domain[0]);
	}
	function getDataExtent(rows, field) {
		if (!Array.isArray(rows) || typeof field !== "string" || field.length === 0) return null;
		const values = rows.map((row) => row?.[field]).filter((value) => typeof value === "number" && Number.isFinite(value));
		if (values.length === 0) return null;
		return [Math.min(...values), Math.max(...values)];
	}
	function inferZoomLevel({ currentDomain, baselineDomain }) {
		const currentSpan = span(currentDomain);
		const baselineSpan = span(baselineDomain);
		if (!currentSpan || !baselineSpan || currentSpan <= 0 || baselineSpan <= 0) return null;
		return Number((baselineSpan / currentSpan).toFixed(4));
	}
	function stableSerialize$2(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	function appendHighlightEntry(entries, nextEntry) {
		const signature = stableSerialize$2(nextEntry);
		if (entries.some((entry) => stableSerialize$2(entry) === signature)) return;
		entries.push(nextEntry);
	}
	function buildViewZoomState({ widgetSpec, xDomain, yDomain }) {
		const nextXDomain = Array.isArray(xDomain) ? xDomain : null;
		const nextYDomain = Array.isArray(yDomain) ? yDomain : null;
		if (!nextXDomain && !nextYDomain) return null;
		const rows = Array.isArray(widgetSpec?.data?.values) ? widgetSpec.data.values : [];
		const xField = widgetSpec?.encoding?.x?.field;
		const yField = widgetSpec?.encoding?.y?.field;
		const baselineXDomain = getDataExtent(rows, xField);
		const baselineYDomain = getDataExtent(rows, yField);
		const centerX = midpoint(nextXDomain);
		const centerY = midpoint(nextYDomain);
		const levelCandidates = [inferZoomLevel({
			currentDomain: nextXDomain,
			baselineDomain: baselineXDomain
		}), inferZoomLevel({
			currentDomain: nextYDomain,
			baselineDomain: baselineYDomain
		})].filter((value) => typeof value === "number" && Number.isFinite(value));
		const zoom = {};
		if (centerX != null && centerY != null) zoom.center = [centerX, centerY];
		if (levelCandidates.length > 0) zoom.level = Number((levelCandidates.reduce((sum, value) => sum + value, 0) / levelCandidates.length).toFixed(4));
		return Object.keys(zoom).length > 0 ? zoom : null;
	}
	function buildViewHighlightState({ widgetSpec }) {
		const entries = [];
		const inspectEncoding = (encoding, scope = "root", layerIndex = null) => {
			if (!encoding || typeof encoding !== "object" || Array.isArray(encoding)) return;
			for (const [channel, channelSpec] of Object.entries(encoding)) {
				if (!channelSpec || typeof channelSpec !== "object" || Array.isArray(channelSpec)) continue;
				if (!Object.prototype.hasOwnProperty.call(channelSpec, "condition")) continue;
				appendHighlightEntry(entries, {
					source: "encoding",
					channel,
					scope,
					...layerIndex != null ? { layerIndex } : {}
				});
			}
		};
		inspectEncoding(widgetSpec?.encoding, "root", null);
		if (Array.isArray(widgetSpec?.layer)) widgetSpec.layer.forEach((layer, layerIndex) => {
			inspectEncoding(layer?.encoding, "layer", layerIndex);
		});
		for (const mark of Array.isArray(widgetSpec?.marks) ? widgetSpec.marks : []) {
			const updateEncoding = mark?.encode?.update;
			if (!updateEncoding || typeof updateEncoding !== "object" || Array.isArray(updateEncoding)) continue;
			for (const [channel, channelSpec] of Object.entries(updateEncoding)) {
				if (!channelSpec || typeof channelSpec !== "object" || Array.isArray(channelSpec)) continue;
				if (!Object.prototype.hasOwnProperty.call(channelSpec, "signal")) continue;
				appendHighlightEntry(entries, {
					source: "mark",
					channel,
					markName: typeof mark?.name === "string" ? mark.name : null
				});
			}
		}
		if (entries.length === 0) return null;
		return {
			channels: [...new Set(entries.map((entry) => entry.channel))],
			entries
		};
	}
	function normalizeDrillParent(parent) {
		if (!parent || typeof parent !== "object" || Array.isArray(parent)) return null;
		const nextParent = {};
		if (Number.isFinite(parent.year)) nextParent.year = Number(parent.year);
		if (Number.isFinite(parent.month)) nextParent.month = Number(parent.month);
		if (Number.isFinite(parent.date)) nextParent.date = Number(parent.date);
		return Object.keys(nextParent).length > 0 ? nextParent : null;
	}
	function inferDrillLevel(parent) {
		if (!parent || typeof parent !== "object") return null;
		if (Number.isFinite(parent.date)) return "date";
		if (Number.isFinite(parent.month)) return "month";
		if (Number.isFinite(parent.year)) return "year";
		return null;
	}
	function buildViewDrillDownState({ widgetSpec }) {
		const lineDrillState = widgetSpec?._line_drilldown_state;
		if (lineDrillState && typeof lineDrillState === "object") {
			const parent = normalizeDrillParent(lineDrillState.parent);
			const level = inferDrillLevel(parent);
			const targetField = typeof widgetSpec?.encoding?.x?.field === "string" ? widgetSpec.encoding.x.field : null;
			if (!level || !targetField) return null;
			return {
				axis: "x",
				active: true,
				level,
				parent,
				targetField
			};
		}
		const heatmapState = widgetSpec?._heatmap_state;
		if (heatmapState && typeof heatmapState === "object") {
			const parent = normalizeDrillParent(heatmapState.parent);
			const level = inferDrillLevel(parent);
			const targetField = typeof widgetSpec?.encoding?.x?.field === "string" ? widgetSpec.encoding.x.field : null;
			if (!level || !targetField) return null;
			return {
				axis: "x",
				active: true,
				level,
				parent,
				targetField
			};
		}
		return null;
	}
	function buildViewAggregateState({ widgetSpec }) {
		const resampleState = widgetSpec?._resample_state;
		if (resampleState && typeof resampleState === "object") {
			const granularity = typeof resampleState.current_granularity === "string" ? resampleState.current_granularity : null;
			const agg = typeof resampleState.current_agg === "string" ? resampleState.current_agg : null;
			const timeField = typeof widgetSpec?.encoding?.x?.field === "string" ? widgetSpec.encoding.x.field : null;
			const valueField = typeof widgetSpec?.encoding?.y?.field === "string" ? widgetSpec.encoding.y.field : null;
			if (!granularity || !agg || !timeField || !valueField) return null;
			return {
				mode: "resample",
				axis: "x",
				granularity,
				agg,
				timeField,
				valueField
			};
		}
		const clusterState = widgetSpec?._cluster_rows_cols_state;
		if (clusterState && typeof clusterState === "object") {
			const method = typeof clusterState.method === "string" ? clusterState.method : null;
			const colorField = typeof clusterState.color_field === "string" ? clusterState.color_field : null;
			if (!method || !colorField) return null;
			return {
				mode: "clusterRowsCols",
				clusterRows: clusterState.cluster_rows !== false,
				clusterCols: clusterState.cluster_cols !== false,
				method,
				colorField
			};
		}
		const sankeyAggregateState = widgetSpec?._sankey_aggregate_state;
		if (sankeyAggregateState && typeof sankeyAggregateState === "object") {
			const mode = typeof sankeyAggregateState.mode === "string" ? sankeyAggregateState.mode : null;
			if (!mode) return null;
			if (mode === "collapseNodes") return {
				mode,
				aggregateName: sankeyAggregateState.aggregate_name || null,
				collapsedNodes: Array.isArray(sankeyAggregateState.collapsed_nodes) ? [...sankeyAggregateState.collapsed_nodes] : []
			};
			if (mode === "autoCollapseByRank") return {
				mode,
				topN: Number.isFinite(sankeyAggregateState.top_n) ? sankeyAggregateState.top_n : null,
				collapsedGroups: Array.isArray(sankeyAggregateState.collapsed_groups) ? sankeyAggregateState.collapsed_groups.map((group) => ({
					depth: group?.depth,
					aggregateName: group?.aggregate_name || null,
					collapsedNodes: Array.isArray(group?.collapsed_nodes) ? [...group.collapsed_nodes] : []
				})) : []
			};
		}
		const movingAverageState = widgetSpec?._line_moving_average_state;
		if (movingAverageState && typeof movingAverageState === "object") return {
			mode: "movingAverageOverlay",
			sourceAction: movingAverageState.sourceAction || "line.showMovingAverage",
			windowSize: Number.isFinite(movingAverageState.windowSize) ? movingAverageState.windowSize : null
		};
		const marginalBarsState = widgetSpec?._marginal_bars_state;
		if (marginalBarsState && typeof marginalBarsState === "object" && marginalBarsState.enabled === true) return {
			mode: "marginalBars",
			op: marginalBarsState.op || "sum",
			showTop: marginalBarsState.show_top !== false,
			showRight: marginalBarsState.show_right !== false,
			valueField: marginalBarsState.value_field || null
		};
		return null;
	}
	function buildViewReencodeState({ widgetSpec }) {
		const lineTrendState = widgetSpec?._line_trend_state;
		if (lineTrendState && typeof lineTrendState === "object") return {
			mode: "regressionOverlay",
			sourceAction: "line.highlightTrend",
			method: lineTrendState.trendType || null
		};
		const scatterRegressionState = widgetSpec?._scatter_regression_state;
		if (scatterRegressionState && typeof scatterRegressionState === "object") return {
			mode: "regressionOverlay",
			sourceAction: "scatter.showRegression",
			method: scatterRegressionState.method || null
		};
		const scatterClusterState = widgetSpec?._scatter_cluster_state;
		if (scatterClusterState && typeof scatterClusterState === "object") return {
			mode: "clusterEncoding",
			method: scatterClusterState.method || null,
			clusterField: scatterClusterState.cluster_field || null,
			nClusters: Number.isFinite(scatterClusterState.n_clusters) ? scatterClusterState.n_clusters : null
		};
		const stackMode = typeof widgetSpec?._stack_mode === "string" ? widgetSpec._stack_mode : null;
		if (stackMode) return {
			mode: "stackMode",
			layout: stackMode,
			colorField: typeof widgetSpec?.encoding?.color?.field === "string" ? widgetSpec.encoding.color.field : null
		};
		const colorScaleState = widgetSpec?._color_scale_state;
		if (colorScaleState && typeof colorScaleState === "object") {
			const scheme = typeof colorScaleState.scheme === "string" ? colorScaleState.scheme : null;
			const channel = typeof colorScaleState.channel === "string" ? colorScaleState.channel : "color";
			const domain = Array.isArray(colorScaleState.domain) ? [...colorScaleState.domain] : null;
			if (!scheme) return null;
			return {
				mode: "colorScale",
				channel,
				scheme,
				...domain ? { domain } : {}
			};
		}
		const transposeState = widgetSpec?._transpose_state;
		if (transposeState && typeof transposeState === "object" && transposeState.transposed === true) return {
			mode: "transpose",
			transposed: true
		};
		const barExpandStackState = widgetSpec?._bar_expand_stack_state;
		if (barExpandStackState && typeof barExpandStackState === "object") return {
			mode: "expandStack",
			category: barExpandStackState.category ?? null,
			categoryField: barExpandStackState.category_field || null,
			groupField: barExpandStackState.group_field || null
		};
		const parallelReencodeState = widgetSpec?._pc_reencode_state;
		if (parallelReencodeState && typeof parallelReencodeState === "object") return {
			mode: parallelReencodeState.mode || "dimensionVisibility",
			sourceAction: parallelReencodeState.sourceAction || null,
			operation: parallelReencodeState.operation || null,
			hiddenDimensions: Array.isArray(parallelReencodeState.hidden_dimensions) ? [...parallelReencodeState.hidden_dimensions] : [],
			visibleDimensions: Array.isArray(parallelReencodeState.visible_dimensions) ? [...parallelReencodeState.visible_dimensions] : []
		};
		const sankeyReencodeState = widgetSpec?._sankey_reencode_state;
		if (sankeyReencodeState && typeof sankeyReencodeState === "object") {
			const mode = typeof sankeyReencodeState.mode === "string" ? sankeyReencodeState.mode : null;
			if (!mode) return null;
			if (mode === "colorFlows") return {
				mode,
				nodes: Array.isArray(sankeyReencodeState.nodes) ? [...sankeyReencodeState.nodes] : [],
				color: sankeyReencodeState.color || null
			};
			if (mode === "reorderNodesInLayer") return {
				mode,
				depth: Number.isFinite(sankeyReencodeState.depth) ? sankeyReencodeState.depth : null,
				order: Array.isArray(sankeyReencodeState.order) ? [...sankeyReencodeState.order] : []
			};
		}
		return null;
	}
	function buildViewAddRemoveState({ widgetSpec }) {
		const visibilityState = widgetSpec?._bar_visibility_state;
		if (!visibilityState || typeof visibilityState !== "object") return null;
		if (visibilityState.mode === "x" && typeof visibilityState.x_field === "string" && Array.isArray(visibilityState.visible_x)) return {
			mode: "categoryVisibility",
			operation: visibilityState.last_operation === "add" ? "add" : "remove",
			field: visibilityState.x_field,
			visibleValues: [...visibilityState.visible_x]
		};
		if (visibilityState.mode === "item" && typeof visibilityState.x_field === "string" && typeof visibilityState.sub_field === "string" && Array.isArray(visibilityState.visible_items)) return {
			mode: "itemVisibility",
			operation: visibilityState.last_operation === "add" ? "add" : "remove",
			xField: visibilityState.x_field,
			subField: visibilityState.sub_field,
			visibleItems: visibilityState.visible_items.map((pair) => Array.isArray(pair) ? [...pair] : pair)
		};
		return null;
	}
	function buildViewFocusState({ widgetSpec }) {
		const lineFocusState = widgetSpec?._line_focus_state;
		if (lineFocusState && typeof lineFocusState === "object" && Array.isArray(lineFocusState.lines) && lineFocusState.lines.length > 0) {
			const focusedSeries = [...lineFocusState.lines];
			return {
				focusedSeries,
				focusKeys: { focusedSeries }
			};
		}
		const sankeyFocusState = widgetSpec?._sankey_focus_state;
		if (sankeyFocusState && typeof sankeyFocusState === "object" && typeof sankeyFocusState.node_name === "string") {
			const focusedNode = sankeyFocusState.node_name;
			return {
				focusedNode,
				focusKeys: { focusedNode }
			};
		}
		return {};
	}
	function buildViewNavigateState({ widgetSpec }) {
		const navigationState = widgetSpec?._navigation_state;
		if (!navigationState || typeof navigationState !== "object") return null;
		if (typeof navigationState.sourceAction !== "string" || typeof navigationState.mode !== "string") return null;
		return {
			mode: navigationState.mode,
			sourceAction: navigationState.sourceAction
		};
	}
	function buildViewSortState({ widgetSpec }) {
		const encoding = widgetSpec?.encoding;
		if (!encoding || typeof encoding !== "object") return null;
		for (const [channel, channelSpec] of Object.entries(encoding)) {
			if (!channelSpec || typeof channelSpec !== "object") continue;
			const sort = channelSpec.sort;
			const field = typeof channelSpec.field === "string" && channelSpec.field.length > 0 ? channelSpec.field : null;
			if (Array.isArray(sort) && sort.length > 0) return {
				channel,
				field,
				mode: "explicitOrder",
				values: [...sort]
			};
			if (typeof sort === "string") {
				if (sort !== "ascending" && sort !== "descending") continue;
				if (!field) continue;
				return {
					channel,
					field,
					mode: "direction",
					order: sort
				};
			}
			if (sort && typeof sort === "object" && !Array.isArray(sort)) {
				const sortField = typeof sort.field === "string" && sort.field.length > 0 ? sort.field : field;
				const order = typeof sort.order === "string" ? sort.order : null;
				if (!sortField || order !== "ascending" && order !== "descending") continue;
				return {
					channel,
					field: sortField,
					mode: "direction",
					order,
					...typeof sort.op === "string" && sort.op.length > 0 ? { aggregate: sort.op } : {}
				};
			}
		}
		return null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/line/semanticPatches.js
	function replaceActionTransform$4(transforms, actionName, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !(transform?.source === "action" && transform?.spec?.actionName === actionName));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function readCurrentWidgetState$2(currentState, targetWidget) {
		return currentState?.widgets?.[targetWidget.ref] || targetWidget || {};
	}
	function buildLineSeriesSelectionPatch({ targetWidget, currentState, field, values, selectedCount, selectionId = `sel_${Date.now()}` }) {
		return buildWidgetSelectionPatch({
			targetWidget,
			currentState,
			selection: {
				selection_id: selectionId,
				source_widget_id: targetWidget.widgetId || void 0,
				selection_type: "category",
				field,
				values,
				predicates: [{
					field,
					op: "in",
					value: values
				}],
				count: selectedCount,
				summary: `${field}: ${values.join(", ")}`
			}
		});
	}
	function buildLineZoomXRegionPatch({ targetWidget, start, end }) {
		return buildWidgetViewPatch({
			targetWidget,
			view: {
				xDomain: [start, end],
				zoom: {
					...buildViewZoomState({
						widgetSpec: targetWidget.rawSpec || targetWidget.currentSpec,
						xDomain: [start, end]
					}) || {},
					sourceAction: "line.zoomXRegion",
					start,
					end
				}
			}
		});
	}
	function buildLineTrendPatch({ targetWidget, currentState, trendType, xField, yField }) {
		const currentWidgetState = readCurrentWidgetState$2(currentState, targetWidget);
		const actionName = "line.highlightTrend";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$4(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "derive",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					trendType,
					fields: [xField, yField],
					overlay: "regression"
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					trend: {
						sourceAction: actionName,
						trendType,
						xField,
						yField,
						overlay: "regression"
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				reencode: {
					mode: "regressionOverlay",
					sourceAction: actionName,
					method: trendType,
					xField,
					yField
				}
			}
		} };
	}
	function buildLineMovingAveragePatch({ targetWidget, currentState, windowSize, xField, yField, groupBy }) {
		const currentWidgetState = readCurrentWidgetState$2(currentState, targetWidget);
		const actionName = "line.showMovingAverage";
		const outputField = "__widgetva_moving_average";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$4(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "derive",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					windowSize,
					fields: [xField, yField],
					outputField,
					...groupBy ? { groupBy: [groupBy] } : {}
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					movingAverage: {
						sourceAction: actionName,
						windowSize,
						xField,
						yField,
						outputField,
						...groupBy ? { groupBy: [groupBy] } : {}
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				addRemove: {
					mode: "movingAverageOverlay",
					sourceAction: actionName,
					windowSize,
					xField,
					yField,
					outputField,
					...groupBy ? { groupBy: [groupBy] } : {}
				}
			}
		} };
	}
	function buildLineDrillDownPatch({ targetWidget, currentState, level, value, parent, rawTimeField, rawValueField }) {
		const currentWidgetState = readCurrentWidgetState$2(currentState, targetWidget);
		const actionName = "line.drillDownXAxis";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$4(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "filter",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					level,
					value,
					parent,
					field: rawTimeField,
					predicates: [{
						field: rawTimeField,
						op: "timeUnitEquals",
						value,
						level
					}]
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					drillDown: {
						sourceAction: actionName,
						level,
						value,
						parent,
						rawTimeField,
						rawValueField
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				drillDown: {
					sourceAction: actionName,
					level,
					value,
					parent,
					rawTimeField,
					rawValueField
				}
			}
		} };
	}
	function buildLineResetDrillDownPatch({ targetWidget, currentState }) {
		const currentWidgetState = readCurrentWidgetState$2(currentState, targetWidget);
		const actionName = "line.drillDownXAxis";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$4(currentWidgetState.transforms, actionName, null),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					drillDown: null
				}
			},
			view: {
				...currentWidgetState.view || {},
				drillDown: null
			}
		} };
	}
	function buildLineResamplePatch({ targetWidget, currentState, granularity, timeUnit, agg, rawTimeField, rawValueField, groupBy }) {
		const currentWidgetState = readCurrentWidgetState$2(currentState, targetWidget);
		const actionName = "line.resampleXAxis";
		const timeField = `${granularity}_${rawTimeField}`;
		const valueField = `${agg}_${rawValueField}`;
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$4(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "aggregate",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					granularity,
					timeUnit,
					agg,
					rawTimeField,
					rawValueField,
					timeField,
					valueField,
					...groupBy ? { groupBy: [groupBy] } : {}
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					resample: {
						sourceAction: actionName,
						granularity,
						timeUnit,
						agg,
						rawTimeField,
						rawValueField,
						timeField,
						valueField,
						...groupBy ? { groupBy: [groupBy] } : {}
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				reencode: {
					mode: "resample",
					sourceAction: actionName,
					granularity,
					timeUnit,
					agg,
					rawTimeField,
					rawValueField,
					timeField,
					valueField,
					...groupBy ? { groupBy: [groupBy] } : {}
				}
			}
		} };
	}
	function buildLineResetResamplePatch({ targetWidget, currentState }) {
		const currentWidgetState = readCurrentWidgetState$2(currentState, targetWidget);
		const actionName = "line.resampleXAxis";
		const currentReencode = currentWidgetState.view?.reencode || null;
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$4(currentWidgetState.transforms, actionName, null),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					resample: null
				}
			},
			view: {
				...currentWidgetState.view || {},
				reencode: currentReencode?.sourceAction === actionName ? null : currentReencode
			}
		} };
	}
	function buildLineBoldPatch({ targetWidget, currentState, lineField, lineNames, boldWidth, baseWidth }) {
		const currentWidgetState = readCurrentWidgetState$2(currentState, targetWidget);
		const actionName = "line.boldLines";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$4(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "derive",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					field: lineField,
					values: lineNames,
					boldWidth,
					baseWidth
				}
			})),
			view: {
				...currentWidgetState.view || {},
				highlight: {
					sourceAction: actionName,
					mode: "seriesEmphasis",
					lineField,
					lineNames,
					boldWidth,
					baseWidth
				}
			}
		} };
	}
	function buildLineFilterPatch({ targetWidget, currentState, lineField, linesToRemove, visibleCount }) {
		const currentWidgetState = readCurrentWidgetState$2(currentState, targetWidget);
		const actionName = "line.filterLines";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$4(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "filter",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					field: lineField,
					values: linesToRemove,
					predicates: [{
						field: lineField,
						op: "notIn",
						value: linesToRemove
					}]
				}
			})),
			data: {
				...currentWidgetState.data || {},
				...Number.isFinite(visibleCount) ? { visibleCount } : {}
			}
		} };
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/line/runtimeActions.js
	function targetWidgetState$3(targetWidget) {
		return { widgets: { [targetWidget.ref]: targetWidget } };
	}
	function inferLineRootEncoding$1(spec) {
		return spec?.layer?.[0]?.encoding || spec?.encoding || {};
	}
	function readMarkType$5(mark) {
		return typeof mark === "string" ? mark : mark?.type || null;
	}
	function isLineFamilyMark(mark) {
		const markType = readMarkType$5(mark);
		return markType === "line" || markType === "area";
	}
	function collectNestedLineSpecs(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const nested = [];
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) nested.push(...spec[key].filter((entry) => entry && typeof entry === "object"));
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) nested.push(spec.spec);
		return nested;
	}
	function findRepresentativeLineSpec(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return spec;
		if (isLineFamilyMark(spec?.mark) && spec?.encoding && typeof spec.encoding === "object") return spec;
		if (Array.isArray(spec?.layer)) {
			const layeredMatch = spec.layer.find((entry) => entry && typeof entry === "object" && isLineFamilyMark(entry?.mark) && entry?.encoding && typeof entry.encoding === "object");
			if (layeredMatch) return layeredMatch;
		}
		for (const child of collectNestedLineSpecs(spec)) {
			const match = findRepresentativeLineSpec(child);
			if (match && match !== spec) return match;
		}
		return spec;
	}
	function inferRawTimeField$1(spec, rootEncoding, originalTransforms) {
		const taggedTimeUnit = (Array.isArray(originalTransforms) ? originalTransforms : []).find((transform) => transform && typeof transform === "object" && "timeUnit" in transform && typeof transform.field === "string");
		if (taggedTimeUnit?.field) return taggedTimeUnit.field;
		const xField = rootEncoding?.x?.field;
		if (typeof xField !== "string" || xField.length === 0) return null;
		if (!xField.includes("_")) return xField;
		const candidate = xField.split("_").at(-1);
		const rows = spec?.data?.values;
		if (Array.isArray(rows) && rows.length > 0 && candidate && Object.prototype.hasOwnProperty.call(rows[0], candidate)) return candidate;
		return xField;
	}
	function inferRawValueField$1(spec, rootEncoding, originalTransforms) {
		const aggregateTransform = (Array.isArray(originalTransforms) ? originalTransforms : []).find((transform) => transform && typeof transform === "object" && Array.isArray(transform.aggregate) && transform.aggregate.length > 0);
		if (aggregateTransform?.aggregate?.[0]?.field) return aggregateTransform.aggregate[0].field;
		const yField = rootEncoding?.y?.field;
		if (typeof yField !== "string" || yField.length === 0) return null;
		if (yField.startsWith("total_")) return yField.slice(6);
		if (yField.startsWith("sum_")) return yField.slice(4);
		return yField;
	}
	function registerLineActions(actionExecutor) {
		if (!actionExecutor.has("line.selectSeries")) actionExecutor.register({ name: "line.selectSeries" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const field = typeof params.field === "string" ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => typeof value === "string" || typeof value === "number") : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("line.selectSeries requires a line target, field, and one or more values.");
			const matchedCount = ctx.readRows(targetWidget.ref).filter((row) => values.includes(row?.[field])).length;
			return {
				patch: buildLineSeriesSelectionPatch({
					targetWidget,
					field,
					values,
					selectedCount: matchedCount,
					selectionId: `${field}-series`
				}),
				affectedRefs: [targetWidget.ref],
				result: { selectedCount: matchedCount },
				selectedCount: matchedCount,
				verificationHints: ["Read the updated line selection state.", "Read linked widgets to confirm series-level filter propagation."],
				propagateFromSelection: true
			};
		});
		if (!actionExecutor.has("line.zoomXRegion")) actionExecutor.register({ name: "line.zoomXRegion" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const start = typeof params.start === "string" ? params.start : null;
			const end = typeof params.end === "string" ? params.end : null;
			if (!targetWidget || !start || !end) throw new Error("line.zoomXRegion requires a line target plus start and end values.");
			return {
				patch: buildLineZoomXRegionPatch({
					targetWidget,
					start,
					end
				}),
				affectedRefs: [targetWidget.ref],
				propagateFromRef: `${targetWidget.ref}/view/zoom`,
				result: {
					widgetId: targetWidget.widgetId,
					xDomain: [start, end]
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line chart x-domain was updated.", "Read the target widget view state to confirm the new temporal x-domain values."]
			};
		});
		if (!actionExecutor.has("line.selectXValue")) actionExecutor.register({ name: "line.selectXValue" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const rawValue = params.value;
			const explicitField = typeof params.field === "string" && params.field.length > 0 ? params.field : null;
			if (!targetWidget || rawValue == null) throw new Error("line.selectXValue requires a line target and a non-null x-axis value.");
			const visibleRows = ctx.readRows(targetWidget.ref);
			const currentSpec = targetWidget?.currentSpec || targetWidget?.rawSpec || targetWidget.readState?.().currentSpec || targetWidget.readState?.().rawSpec || null;
			const rootEncoding = currentSpec?.layer?.[0]?.encoding || currentSpec?.encoding || {};
			const field = explicitField || rootEncoding?.x?.field || null;
			if (!field) throw new Error("line.selectXValue requires a resolvable x-axis field on the active line spec.");
			const matchedCount = visibleRows.filter((row) => row?.[field] === rawValue).length;
			return {
				patch: buildLineSeriesSelectionPatch({
					targetWidget,
					field,
					values: [rawValue],
					selectedCount: matchedCount,
					selectionId: `${field}-value`
				}),
				affectedRefs: [targetWidget.ref],
				result: { selectedCount: matchedCount },
				selectedCount: matchedCount,
				verificationHints: ["Read the updated line selection state.", "Read linked widgets to confirm the selected x-axis slice propagated beyond the line chart."],
				propagateFromSelection: true
			};
		});
		if (!actionExecutor.has("line.focusLines")) actionExecutor.register({ name: "line.focusLines" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const lines = Array.isArray(params.lines) ? params.lines.filter((value) => typeof value === "string" && value.length > 0) : [];
			const dimOpacity = typeof params.dimOpacity === "number" ? params.dimOpacity : .08;
			if (!targetWidget || lines.length === 0) throw new Error("line.focusLines requires a line target and at least one line identifier.");
			return {
				patch: buildWidgetViewPatch({
					targetWidget,
					view: {
						focus: {
							sourceAction: "line.focusLines",
							focusedSeries: lines,
							focusKeys: { focusedSeries: lines }
						},
						highlight: {
							sourceAction: "line.focusLines",
							mode: "seriesFocus",
							lines,
							dimOpacity,
							lineField: params.lineField || null
						}
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					lines,
					lineField: params.lineField || null,
					dimOpacity
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line chart opacity condition now focuses the requested series.", "Read the target widget view state to confirm non-focused lines are dimmed."]
			};
		});
		if (!actionExecutor.has("line.highlightTrend")) actionExecutor.register({ name: "line.highlightTrend" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const trendType = typeof params.trendType === "string" && params.trendType.length > 0 ? params.trendType : "increasing";
			if (!targetWidget) throw new Error("line.highlightTrend requires a valid line target widget.");
			const currentState = targetWidgetState$3(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const rootEncoding = inferLineRootEncoding$1(findRepresentativeLineSpec(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null));
			const xField = rootEncoding?.x?.field || null;
			const yField = rootEncoding?.y?.field || null;
			if (!xField || !yField) throw new Error("line.highlightTrend requires x and y encodings.");
			return {
				patch: buildLineTrendPatch({
					targetWidget,
					currentState,
					trendType,
					xField,
					yField
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					trendType,
					overlay: "regression",
					xField,
					yField
				},
				verificationHints: ["Read the target widget state and confirm view.reencode.mode is regressionOverlay.", "Read data.analysis.trend to confirm the semantic trend overlay metadata."]
			};
		});
		if (!actionExecutor.has("line.showMovingAverage")) actionExecutor.register({ name: "line.showMovingAverage" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const windowSize = Number.isFinite(params.windowSize) ? Math.floor(Number(params.windowSize)) : 3;
			if (!targetWidget || windowSize < 1) throw new Error("line.showMovingAverage requires a valid line target widget and a windowSize >= 1.");
			const currentState = targetWidgetState$3(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const rootEncoding = inferLineRootEncoding$1(findRepresentativeLineSpec(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null));
			const xField = rootEncoding?.x?.field || null;
			const yField = rootEncoding?.y?.field || null;
			const groupBy = rootEncoding?.color?.field || rootEncoding?.detail?.field || null;
			if (!xField || !yField) throw new Error("line.showMovingAverage requires x and y encodings.");
			return {
				patch: buildLineMovingAveragePatch({
					targetWidget,
					currentState,
					windowSize,
					xField,
					yField,
					groupBy
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					windowSize,
					overlay: "movingAverage",
					xField,
					yField,
					...groupBy ? { groupBy } : {}
				},
				verificationHints: ["Read the target widget state and confirm view.addRemove.mode is movingAverageOverlay.", "Read data.analysis.movingAverage to confirm the semantic moving-average metadata."]
			};
		});
		if (!actionExecutor.has("line.drillDownXAxis")) actionExecutor.register({ name: "line.drillDownXAxis" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const level = typeof params.level === "string" ? params.level.toLowerCase().trim() : null;
			const numericValue = Number.isFinite(params.value) ? Number(params.value) : null;
			const parent = params.parent && typeof params.parent === "object" && !Array.isArray(params.parent) ? params.parent : {};
			if (!targetWidget || !level || numericValue === null) throw new Error("line.drillDownXAxis requires a line target plus level and numeric value.");
			const currentState = targetWidgetState$3(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const representativeSpec = findRepresentativeLineSpec(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null);
			const rootEncoding = inferLineRootEncoding$1(representativeSpec);
			const originalTransforms = Array.isArray(representativeSpec?.transform) ? representativeSpec.transform : [];
			const rawTimeField = inferRawTimeField$1(representativeSpec, rootEncoding, originalTransforms);
			const rawValueField = inferRawValueField$1(representativeSpec, rootEncoding, originalTransforms);
			if (!rawTimeField || !rawValueField) throw new Error("line.drillDownXAxis requires resolvable temporal and value fields.");
			return {
				patch: buildLineDrillDownPatch({
					targetWidget,
					currentState,
					level,
					value: numericValue,
					parent,
					rawTimeField,
					rawValueField
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					level,
					value: numericValue,
					parent,
					rawTimeField,
					rawValueField
				},
				verificationHints: ["Read the target widget state and confirm view.drillDown describes the finer temporal drill-down.", "Read the target widget transforms and confirm a line.drillDownXAxis filter transform was recorded."]
			};
		});
		if (!actionExecutor.has("line.resetDrilldownXAxis")) actionExecutor.register({ name: "line.resetDrilldownXAxis" }, (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			if (!targetWidget) throw new Error("line.resetDrilldownXAxis requires a valid line target widget.");
			return {
				patch: buildLineResetDrillDownPatch({
					targetWidget,
					currentState: targetWidgetState$3(targetWidget)
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					reset: true
				},
				verificationHints: ["Read the target widget view state and confirm view.drillDown is cleared.", "Read the target widget transforms and confirm line.drillDownXAxis is removed."]
			};
		});
		if (!actionExecutor.has("line.resampleXAxis")) actionExecutor.register({ name: "line.resampleXAxis" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const granularity = typeof params.granularity === "string" ? params.granularity.toLowerCase().trim() : null;
			const agg = typeof params.agg === "string" ? params.agg.toLowerCase().trim() : "mean";
			const granularityMap = {
				day: "yearmonthdate",
				week: "yearweek",
				month: "yearmonth",
				quarter: "yearquarter",
				year: "year"
			};
			const allowedAgg = new Set([
				"mean",
				"sum",
				"max",
				"min",
				"median",
				"count"
			]);
			if (!targetWidget || !granularity || !granularityMap[granularity]) throw new Error("line.resampleXAxis requires a line target and a supported granularity: day, week, month, quarter, or year.");
			if (!allowedAgg.has(agg)) throw new Error("line.resampleXAxis requires a supported aggregation: mean, sum, max, min, median, or count.");
			const currentState = targetWidgetState$3(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const representativeSpec = findRepresentativeLineSpec(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null);
			const rootEncoding = inferLineRootEncoding$1(representativeSpec);
			const originalTransforms = Array.isArray(representativeSpec?.transform) ? representativeSpec.transform : [];
			const rawTimeField = inferRawTimeField$1(representativeSpec, rootEncoding, originalTransforms);
			const rawValueField = inferRawValueField$1(representativeSpec, rootEncoding, originalTransforms);
			const groupBy = rootEncoding?.color?.field || rootEncoding?.detail?.field || null;
			if (!rawTimeField || !rawValueField) throw new Error("line.resampleXAxis requires resolvable temporal and value fields.");
			return {
				patch: buildLineResamplePatch({
					targetWidget,
					currentState,
					granularity,
					timeUnit: granularityMap[granularity],
					agg,
					rawTimeField,
					rawValueField,
					groupBy
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					granularity,
					agg,
					rawTimeField,
					rawValueField,
					...groupBy ? { groupBy } : {}
				},
				verificationHints: ["Read the target widget state and confirm view.reencode.mode is resample.", "Read data.analysis.resample to confirm the temporal aggregation metadata."]
			};
		});
		if (!actionExecutor.has("line.resetResampleXAxis")) actionExecutor.register({ name: "line.resetResampleXAxis" }, (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			if (!targetWidget) throw new Error("line.resetResampleXAxis requires a valid line target widget.");
			return {
				patch: buildLineResetResamplePatch({
					targetWidget,
					currentState: targetWidgetState$3(targetWidget)
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					reset: true
				},
				verificationHints: ["Read the target widget view state and confirm resample reencoding is cleared.", "Read the target widget transforms and confirm line.resampleXAxis is removed."]
			};
		});
		if (!actionExecutor.has("line.boldLines")) actionExecutor.register({ name: "line.boldLines" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const lineNames = Array.isArray(params.lineNames) ? params.lineNames.filter((value) => typeof value === "string" && value.length > 0) : [];
			const boldWidth = typeof params.boldWidth === "number" ? params.boldWidth : 4;
			const baseWidth = typeof params.baseWidth === "number" ? params.baseWidth : 1;
			if (!targetWidget || lineNames.length === 0) throw new Error("line.boldLines requires a line target and at least one line identifier.");
			const currentState = targetWidgetState$3(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const rootEncoding = inferLineRootEncoding$1(findRepresentativeLineSpec(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null));
			const lineField = typeof params.lineField === "string" && params.lineField.length > 0 ? params.lineField : rootEncoding?.color?.field || rootEncoding?.detail?.field || null;
			if (!lineField) throw new Error("line.boldLines requires a lineField or color/detail encoding field.");
			return {
				patch: buildLineBoldPatch({
					targetWidget,
					currentState,
					lineField,
					lineNames,
					boldWidth,
					baseWidth
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					lineNames,
					lineField,
					boldWidth,
					baseWidth
				},
				verificationHints: ["Read the target widget state and confirm view.highlight.mode is seriesEmphasis.", "Read the target widget transforms and confirm line.boldLines metadata was recorded."]
			};
		});
		if (!actionExecutor.has("line.filterLines")) actionExecutor.register({ name: "line.filterLines" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const linesToRemove = Array.isArray(params.linesToRemove) ? params.linesToRemove.filter((value) => typeof value === "string" && value.length > 0) : [];
			if (!targetWidget || linesToRemove.length === 0) throw new Error("line.filterLines requires a line target and one or more line names to remove.");
			const currentState = targetWidgetState$3(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const rootEncoding = inferLineRootEncoding$1(findRepresentativeLineSpec(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null));
			const lineField = typeof params.lineField === "string" && params.lineField.length > 0 ? params.lineField : rootEncoding?.color?.field || rootEncoding?.detail?.field || null;
			if (!lineField) throw new Error("line.filterLines requires a lineField or color/detail encoding field.");
			const visibleCount = ctx.readRows(targetWidget.ref).filter((row) => !linesToRemove.includes(row?.[lineField])).length;
			return {
				patch: buildLineFilterPatch({
					targetWidget,
					currentState,
					lineField,
					linesToRemove,
					visibleCount
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					linesToRemove,
					lineField,
					visibleCount
				},
				verificationHints: ["Read the target widget transforms and confirm line.filterLines is recorded as a semantic filter.", "Read the target widget data.visibleCount to confirm the filtered row count."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/parallelCoordinates/semanticPatches.js
	function readMarkType$4(mark) {
		return typeof mark === "string" ? mark : mark?.type || null;
	}
	function isParallelCoordinatesMark$1(mark) {
		return readMarkType$4(mark) === "line";
	}
	function isParallelCoordinatesSpec$2(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return false;
		const xField = (spec?.encoding)?.x?.field;
		const hasFoldTransform = (Array.isArray(spec?.transform) ? spec.transform : []).some((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
		return spec?.kind === "parallelCoordinates" || (isParallelCoordinatesMark$1(spec?.mark) || hasFoldTransform) && typeof xField === "string" && [
			"dimension",
			"key",
			"variable"
		].includes(xField);
	}
	function collectNestedParallelCoordinatesSpecs$1(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const nested = [];
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) nested.push(...spec[key].filter((entry) => entry && typeof entry === "object"));
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) nested.push(spec.spec);
		return nested;
	}
	function findRepresentativeParallelCoordinatesSpec$1(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		if (isParallelCoordinatesSpec$2(spec)) return spec;
		if (Array.isArray(spec?.layer)) {
			const layeredMatch = spec.layer.find((entry) => entry && typeof entry === "object" && isParallelCoordinatesSpec$2(entry));
			if (layeredMatch) return layeredMatch;
		}
		for (const child of collectNestedParallelCoordinatesSpecs$1(spec)) {
			const match = findRepresentativeParallelCoordinatesSpec$1(child);
			if (match) return match;
		}
		return null;
	}
	function replaceActionTransform$3(transforms, actionName, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !(transform?.source === "action" && transform?.spec?.actionName === actionName));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function readCurrentWidgetState$1(currentState, targetWidget) {
		return currentState?.widgets?.[targetWidget.ref] || targetWidget || {};
	}
	function findFoldTransform$1(transforms = []) {
		return (Array.isArray(transforms) ? transforms : []).find((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold)) || null;
	}
	function readDimensionOrderFromSpec(spec) {
		const representativeSpec = findRepresentativeParallelCoordinatesSpec$1(spec) || spec;
		const sort = representativeSpec?.encoding?.x?.sort;
		if (Array.isArray(sort) && sort.length > 0) return sort.filter((dimension) => typeof dimension === "string");
		const domain = representativeSpec?.encoding?.x?.scale?.domain;
		if (Array.isArray(domain) && domain.length > 0) return domain.filter((dimension) => typeof dimension === "string");
		const fold = findFoldTransform$1(representativeSpec?.transform);
		if (Array.isArray(fold?.fold) && fold.fold.length > 0) return fold.fold.filter((dimension) => typeof dimension === "string");
		return [];
	}
	function readParallelDimensionOrder(currentWidgetState, targetWidget) {
		const viewOrder = currentWidgetState?.view?.reencode?.dimensionOrder || currentWidgetState?.view?.reencode?.dimensions || currentWidgetState?.view?.dimensionVisibility?.visibleDimensions;
		if (Array.isArray(viewOrder) && viewOrder.length > 0) return viewOrder.filter((dimension) => typeof dimension === "string");
		return readDimensionOrderFromSpec(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || targetWidget?.currentSpec || targetWidget?.rawSpec || null);
	}
	function buildParallelReorderPatch({ targetWidget, currentState, dimensionOrder }) {
		const currentWidgetState = readCurrentWidgetState$1(currentState, targetWidget);
		const actionName = "parallelCoordinates.reorderDimensions";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$3(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "reencode",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					dimensionOrder
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					dimensionOrder: {
						sourceAction: actionName,
						dimensionOrder
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				reencode: {
					mode: "dimensionOrder",
					sourceAction: actionName,
					dimensionOrder
				}
			}
		} };
	}
	function buildParallelHighlightPatch({ targetWidget, currentState, field, values }) {
		const currentWidgetState = readCurrentWidgetState$1(currentState, targetWidget);
		const actionName = "parallelCoordinates.highlightCategory";
		return { [targetWidget.ref]: {
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					highlight: {
						sourceAction: actionName,
						field,
						values,
						predicates: [{
							field,
							op: "in",
							value: values
						}]
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				highlight: {
					mode: "categoryEmphasis",
					sourceAction: actionName,
					field,
					values
				}
			}
		} };
	}
	function buildParallelDimensionVisibilityPatch({ targetWidget, currentState, dimensions, mode }) {
		const currentWidgetState = readCurrentWidgetState$1(currentState, targetWidget);
		const actionName = "parallelCoordinates.hideDimensions";
		const previousVisibility = currentWidgetState?.view?.dimensionVisibility || currentWidgetState?.data?.analysis?.dimensionVisibility || {};
		const allDimensions = Array.isArray(previousVisibility.allDimensions) && previousVisibility.allDimensions.length > 0 ? previousVisibility.allDimensions : readParallelDimensionOrder(currentWidgetState, targetWidget);
		const previousHidden = new Set(Array.isArray(previousVisibility.hiddenDimensions) ? previousVisibility.hiddenDimensions : []);
		for (const dimension of dimensions) if (mode === "show") previousHidden.delete(dimension);
		else previousHidden.add(dimension);
		const hiddenDimensions = allDimensions.filter((dimension) => previousHidden.has(dimension));
		const visibleDimensions = allDimensions.filter((dimension) => !previousHidden.has(dimension));
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$3(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "filter",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					operation: mode,
					dimensions,
					hiddenDimensions,
					visibleDimensions,
					allDimensions
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					dimensionVisibility: {
						sourceAction: actionName,
						operation: mode,
						dimensions,
						hiddenDimensions,
						visibleDimensions,
						allDimensions
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				dimensionVisibility: {
					mode: "dimensionVisibility",
					sourceAction: actionName,
					operation: mode,
					dimensions,
					hiddenDimensions,
					visibleDimensions,
					allDimensions
				},
				reencode: {
					mode: "dimensionVisibility",
					sourceAction: actionName,
					operation: mode,
					hiddenDimensions,
					visibleDimensions
				}
			}
		} };
	}
	function buildParallelResetHiddenDimensionsPatch({ targetWidget, currentState }) {
		const currentWidgetState = readCurrentWidgetState$1(currentState, targetWidget);
		const actionName = "parallelCoordinates.resetHiddenDimensions";
		const previousVisibility = currentWidgetState?.view?.dimensionVisibility || currentWidgetState?.data?.analysis?.dimensionVisibility || {};
		const allDimensions = Array.isArray(previousVisibility.allDimensions) && previousVisibility.allDimensions.length > 0 ? previousVisibility.allDimensions : readParallelDimensionOrder(currentWidgetState, targetWidget);
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$3(currentWidgetState.transforms, "parallelCoordinates.hideDimensions", null),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					dimensionVisibility: {
						sourceAction: actionName,
						operation: "reset",
						dimensions: [],
						hiddenDimensions: [],
						visibleDimensions: allDimensions,
						allDimensions
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				dimensionVisibility: {
					mode: "dimensionVisibility",
					sourceAction: actionName,
					operation: "reset",
					dimensions: [],
					hiddenDimensions: [],
					visibleDimensions: allDimensions,
					allDimensions
				},
				reencode: {
					mode: "dimensionVisibility",
					sourceAction: actionName,
					operation: "reset",
					hiddenDimensions: [],
					visibleDimensions: allDimensions
				}
			}
		} };
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/parallelCoordinates/runtimeActions.js
	function targetWidgetState$2(targetWidget) {
		return { widgets: { [targetWidget.ref]: targetWidget } };
	}
	function readMarkType$3(mark) {
		return typeof mark === "string" ? mark : mark?.type || null;
	}
	function isParallelCoordinatesMark(mark) {
		return readMarkType$3(mark) === "line";
	}
	function isParallelCoordinatesSpec$1(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return false;
		const xField = (spec?.encoding)?.x?.field;
		const hasFoldTransform = (Array.isArray(spec?.transform) ? spec.transform : []).some((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
		return spec?.kind === "parallelCoordinates" || (isParallelCoordinatesMark(spec?.mark) || hasFoldTransform) && typeof xField === "string" && [
			"dimension",
			"key",
			"variable"
		].includes(xField);
	}
	function collectNestedParallelCoordinatesSpecs(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const nested = [];
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) nested.push(...spec[key].filter((entry) => entry && typeof entry === "object"));
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) nested.push(spec.spec);
		return nested;
	}
	function findRepresentativeParallelCoordinatesSpec(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		if (isParallelCoordinatesSpec$1(spec)) return spec;
		if (Array.isArray(spec?.layer)) {
			const layeredMatch = spec.layer.find((entry) => entry && typeof entry === "object" && isParallelCoordinatesSpec$1(entry));
			if (layeredMatch) return layeredMatch;
		}
		for (const child of collectNestedParallelCoordinatesSpecs(spec)) {
			const match = findRepresentativeParallelCoordinatesSpec(child);
			if (match) return match;
		}
		return null;
	}
	function readParallelRecordField(spec) {
		const representativeSpec = findRepresentativeParallelCoordinatesSpec(spec) || spec;
		const rootEncoding = Array.isArray(representativeSpec?.layer) && representativeSpec.layer.length > 0 ? representativeSpec.layer[0]?.encoding || representativeSpec?.encoding || {} : representativeSpec?.encoding || {};
		return typeof rootEncoding?.detail?.field === "string" && rootEncoding.detail.field.length > 0 ? rootEncoding.detail.field : "id";
	}
	function countParallelSemanticRows(rows = [], spec = null, fallbackField = "id") {
		const safeRows = Array.isArray(rows) ? rows : [];
		const recordField = readParallelRecordField(spec) || fallbackField;
		return new Set(safeRows.map((row) => row?.[recordField]).filter((value) => value != null).map((value) => JSON.stringify(value))).size;
	}
	function registerParallelCoordinatesActions(actionExecutor) {
		if (!actionExecutor.has("parallelCoordinates.brushAxes")) actionExecutor.register({ name: "parallelCoordinates.brushAxes" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const rules = Array.isArray(params.rules) ? params.rules.filter((rule) => typeof rule?.field === "string" && Array.isArray(rule?.range) && rule.range.length === 2) : [];
			if (!targetWidget || rules.length === 0) throw new Error("parallelCoordinates.brushAxes requires a parallel coordinates target and at least one valid rule.");
			const rows = ctx.readRows(targetWidget.ref);
			const currentSpec = targetWidget?.currentSpec || targetWidget?.rawSpec || null;
			const normalizedRules = rules.map((rule) => ({
				field: rule.field,
				range: [Math.min(...rule.range), Math.max(...rule.range)]
			}));
			const selectedCount = countParallelSemanticRows(rows.filter((row) => normalizedRules.every((rule) => {
				const value = row?.[rule.field];
				return typeof value === "number" && value >= rule.range[0] && value <= rule.range[1];
			})), currentSpec);
			return {
				patch: buildWidgetSelectionPatch({
					targetWidget,
					selection: {
						selection_id: `sel_${Date.now()}`,
						source_widget_id: targetWidget.widgetId || void 0,
						selection_type: "interval",
						fields: normalizedRules.map((rule) => rule.field),
						value: Object.fromEntries(normalizedRules.map((rule) => [rule.field, rule.range])),
						predicates: normalizedRules.map((rule) => ({
							field: rule.field,
							op: "between",
							value: rule.range
						})),
						count: selectedCount,
						summary: normalizedRules.map((rule) => `${rule.field} ${rule.range[0]}~${rule.range[1]}`).join("; ")
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: { selectedCount },
				selectedCount,
				verificationHints: ["Read the updated parallel coordinates selection state.", "Read linked widgets to confirm multivariate range propagation."],
				propagateFromSelection: true
			};
		});
		if (!actionExecutor.has("parallelCoordinates.reorderDimensions")) actionExecutor.register({ name: "parallelCoordinates.reorderDimensions" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const dimensionOrder = Array.isArray(params.dimensionOrder) ? params.dimensionOrder.filter((dimension) => typeof dimension === "string" && dimension.trim().length > 0) : [];
			if (!targetWidget || dimensionOrder.length === 0) throw new Error("parallelCoordinates.reorderDimensions requires a parallel coordinates target and a non-empty dimensionOrder.");
			return {
				patch: buildParallelReorderPatch({
					targetWidget,
					currentState: targetWidgetState$2(targetWidget),
					dimensionOrder
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					dimensionOrder
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the fold order and x-axis domain now match the requested dimension ordering.", "Read the target widget view state to confirm the visible parallel-axis order was updated without changing the underlying data rows."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.selectRecord")) actionExecutor.register({ name: "parallelCoordinates.selectRecord" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const recordId = params.recordId;
			const field = typeof params.field === "string" && params.field.length > 0 ? params.field : "id";
			if (!targetWidget || recordId == null) throw new Error("parallelCoordinates.selectRecord requires a parallel coordinates target and a recordId.");
			const rows = ctx.readRows(targetWidget.ref);
			const currentSpec = targetWidget?.currentSpec || targetWidget?.rawSpec || null;
			const matchedRows = rows.filter((row) => row?.[field] === recordId);
			const selectedCount = countParallelSemanticRows(matchedRows, currentSpec, field);
			const summary = matchedRows[0]?.name ? `Record: ${matchedRows[0].name}` : `${field}: ${recordId}`;
			return {
				patch: buildWidgetSelectionPatch({
					targetWidget,
					selection: {
						selection_id: `sel_${Date.now()}`,
						source_widget_id: targetWidget.widgetId || void 0,
						selection_type: "category",
						field,
						values: [recordId],
						predicates: [{
							field,
							op: "equals",
							value: recordId
						}],
						count: selectedCount,
						summary
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: { selectedCount },
				selectedCount,
				verificationHints: ["Read the updated parallel-coordinates selection state.", "Read linked widgets to confirm the selected record became the current focus subset."],
				propagateFromSelection: true
			};
		});
		if (!actionExecutor.has("parallelCoordinates.filterDimension")) actionExecutor.register({ name: "parallelCoordinates.filterDimension" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const dimension = typeof params.dimension === "string" && params.dimension.trim().length > 0 ? params.dimension : null;
			const range = Array.isArray(params.range) && params.range.length === 2 ? params.range : null;
			if (!targetWidget || !dimension || !range || !range.every((value) => typeof value === "number" && Number.isFinite(value))) throw new Error("parallelCoordinates.filterDimension requires a parallel coordinates target, a dimension, and a two-number range.");
			const normalizedRange = [Math.min(...range), Math.max(...range)];
			const visibleCount = ctx.readRows(targetWidget.ref).filter((row) => {
				const value = row?.[dimension];
				return typeof value === "number" && value >= normalizedRange[0] && value <= normalizedRange[1];
			}).length;
			return {
				patch: buildWidgetFilterPatch({
					targetWidget,
					currentState: targetWidgetState$2(targetWidget),
					actionName: "parallelCoordinates.filterDimension",
					field: dimension,
					value: normalizedRange,
					predicates: [{
						field: dimension,
						op: "between",
						value: normalizedRange
					}],
					visibleCount
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					dimension,
					range: normalizedRange
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the transform list now includes the requested numeric range filter ahead of the fold stage.", "Read the target widget state to confirm the filter transform is present."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.filterByCategory")) actionExecutor.register({ name: "parallelCoordinates.filterByCategory" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const field = typeof params.field === "string" && params.field.trim().length > 0 ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => typeof value === "string" && value.length > 0) : typeof params.values === "string" && params.values.length > 0 ? [params.values] : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("parallelCoordinates.filterByCategory requires a parallel coordinates target, a field, and one or more category values.");
			const visibleCount = ctx.readRows(targetWidget.ref).filter((row) => !values.includes(row?.[field])).length;
			return {
				patch: buildWidgetFilterPatch({
					targetWidget,
					currentState: targetWidgetState$2(targetWidget),
					actionName: "parallelCoordinates.filterByCategory",
					field,
					values,
					predicates: [{
						field,
						op: "notIn",
						value: values
					}],
					visibleCount
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					field,
					values
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the transform list now includes the requested categorical exclusion filter ahead of the fold stage.", "Read the target widget state to confirm the filter transform is present."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.highlightCategory")) actionExecutor.register({ name: "parallelCoordinates.highlightCategory" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const field = typeof params.field === "string" && params.field.trim().length > 0 ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => typeof value === "string" && value.length > 0) : typeof params.values === "string" && params.values.length > 0 ? [params.values] : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("parallelCoordinates.highlightCategory requires a parallel coordinates target, a field, and one or more category values.");
			return {
				patch: buildParallelHighlightPatch({
					targetWidget,
					currentState: targetWidgetState$2(targetWidget),
					field,
					values
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					field,
					values
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the line encoding now contains an opacity condition for the requested category values.", "Read the target widget view state to confirm matching categories remain visually prominent while other trajectories are dimmed."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.hideDimensions")) actionExecutor.register({ name: "parallelCoordinates.hideDimensions" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const dimensions = Array.isArray(params.dimensions) ? params.dimensions.filter((dimension) => typeof dimension === "string" && dimension.trim().length > 0) : [];
			const mode = params.mode === "show" ? "show" : "hide";
			if (!targetWidget || dimensions.length === 0) throw new Error("parallelCoordinates.hideDimensions requires a parallel coordinates target and at least one dimension.");
			return {
				patch: buildParallelDimensionVisibilityPatch({
					targetWidget,
					currentState: targetWidgetState$2(targetWidget),
					dimensions,
					mode
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					dimensions,
					mode
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the visible fold order and x-axis domain no longer include the hidden dimensions.", "Read the target widget view state to confirm the original full dimension list remains stored for later restoration."]
			};
		});
		if (!actionExecutor.has("parallelCoordinates.resetHiddenDimensions")) actionExecutor.register({ name: "parallelCoordinates.resetHiddenDimensions" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			if (!targetWidget) throw new Error("parallelCoordinates.resetHiddenDimensions requires a valid parallel coordinates target widget.");
			return {
				patch: buildParallelResetHiddenDimensionsPatch({
					targetWidget,
					currentState: targetWidgetState$2(targetWidget)
				}),
				affectedRefs: [targetWidget.ref],
				result: { widgetId: targetWidget.widgetId },
				verificationHints: ["Call perception.inspectViewConfig to verify the full original dimension order has been restored on the fold transform and x-axis domain metadata.", "Read the target widget view state to confirm the hidden-dimension bookkeeping state has been cleared."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/sankey/semanticPatches.js
	function replaceActionTransform$2(transforms, actionName, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !(transform?.source === "action" && transform?.spec?.actionName === actionName));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function readCurrentWidgetState(currentState, targetWidget) {
		return currentState?.widgets?.[targetWidget.ref] || targetWidget || {};
	}
	function buildSankeySemanticPatch({ targetWidget, currentState, actionName, transformKind = "derive", analysisKey, viewKey, viewState, spec, clearActionName = null }) {
		const currentWidgetState = readCurrentWidgetState(currentState, targetWidget);
		const nextTransforms = replaceActionTransform$2(currentWidgetState.transforms, clearActionName || actionName, clearActionName ? null : makeTransformState({
			kind: transformKind,
			source: "action",
			sourceWidgetId: targetWidget.widgetId || null,
			spec: {
				actionName,
				...spec || {}
			}
		}));
		return { [targetWidget.ref]: {
			transforms: nextTransforms,
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					[analysisKey]: {
						sourceAction: actionName,
						...spec || {}
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				[viewKey]: {
					sourceAction: actionName,
					...viewState || spec || {}
				}
			}
		} };
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/sankey/runtimeActions.js
	function targetWidgetState$1(targetWidget) {
		return { widgets: { [targetWidget.ref]: targetWidget } };
	}
	function registerSankeyActions(actionExecutor) {
		if (!actionExecutor.has("sankey.focusFlow")) actionExecutor.register({ name: "sankey.focusFlow" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const field = typeof params.field === "string" ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
			if (!targetWidget || !field || values.length === 0) throw new Error("sankey.focusFlow requires a sankey target, field, and one or more values.");
			const matchedCount = ctx.readRows(targetWidget.ref).filter((row) => values.includes(row?.[field])).length;
			return {
				patch: buildWidgetSelectionPatch({
					targetWidget,
					selection: {
						selection_id: `sel_${Date.now()}`,
						source_widget_id: targetWidget.widgetId || void 0,
						selection_type: "category",
						field,
						values,
						predicates: [{
							field,
							op: "in",
							value: values
						}],
						count: matchedCount,
						summary: `${field}: ${values.join(", ")}`
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: { selectedCount: matchedCount },
				selectedCount: matchedCount,
				verificationHints: ["Read the updated Sankey selection state.", "Read linked widgets or feedback to confirm focused flow propagation."],
				propagateFromSelection: true
			};
		});
		if (!actionExecutor.has("sankey.selectAggregateNode")) actionExecutor.register({ name: "sankey.selectAggregateNode" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const aggregateName = typeof params.aggregateName === "string" && params.aggregateName.trim().length > 0 ? params.aggregateName.trim() : null;
			if (!targetWidget || !aggregateName) throw new Error("sankey.selectAggregateNode requires a sankey target and an aggregateName.");
			return {
				patch: buildWidgetSelectionPatch({
					targetWidget,
					selection: {
						selection_id: `sel_${Date.now()}`,
						source_widget_id: targetWidget.widgetId || void 0,
						selection_type: "category",
						field: "aggregateName",
						values: [aggregateName],
						predicates: [],
						count: 1,
						summary: `aggregateName: ${aggregateName}`,
						aggregateName
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: { selectedCount: 1 },
				selectedCount: 1,
				verificationHints: ["Read the updated Sankey selection state and confirm the aggregateName is preserved on the active selection.", "Read the coordination state to verify the aggregate selection became the current focused Sankey context."],
				propagateFromSelection: true
			};
		});
		if (!actionExecutor.has("sankey.filterFlow")) actionExecutor.register({ name: "sankey.filterFlow" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const minValue = Number(params.minValue);
			if (!targetWidget || !Number.isFinite(minValue)) throw new Error("sankey.filterFlow requires a sankey target and a finite minValue.");
			return {
				patch: buildSankeySemanticPatch({
					targetWidget,
					currentState: targetWidgetState$1(targetWidget),
					actionName: "sankey.filterFlow",
					transformKind: "filter",
					analysisKey: "flowFilter",
					viewKey: "reencode",
					viewState: {
						mode: "flowFilter",
						minValue
					},
					spec: {
						minValue,
						predicates: [{
							field: "value",
							op: "gte",
							value: minValue
						}]
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					minValue
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the threshold signal or rawLinks data now reflect the requested minimum flow.", "Read the target widget view state to confirm low-value links no longer participate in the visible Sankey structure."]
			};
		});
		if (!actionExecutor.has("sankey.collapseNodes")) actionExecutor.register({ name: "sankey.collapseNodes" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const nodes = Array.isArray(params.nodes) ? params.nodes.filter((value) => typeof value === "string" && value.trim().length > 0) : [];
			const aggregateName = typeof params.aggregateName === "string" && params.aggregateName.trim().length > 0 ? params.aggregateName : "Other";
			if (!targetWidget || nodes.length === 0) throw new Error("sankey.collapseNodes requires a sankey target and one or more node names.");
			return {
				patch: buildSankeySemanticPatch({
					targetWidget,
					currentState: targetWidgetState$1(targetWidget),
					actionName: "sankey.collapseNodes",
					transformKind: "aggregate",
					analysisKey: "nodeCollapse",
					viewKey: "addRemove",
					viewState: {
						mode: "nodeCollapse",
						nodes,
						aggregateName
					},
					spec: {
						nodes,
						aggregateName
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					nodes,
					aggregateName
				},
				verificationHints: ["Call perception.inspectViewConfig to verify rawLinks and nodeConfig now include the aggregate node and collapsed topology.", "Read the target widget view state to confirm the original Sankey nodes and links were preserved in _sankey_state for later expansion."]
			};
		});
		if (!actionExecutor.has("sankey.expandNode")) actionExecutor.register({ name: "sankey.expandNode" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const aggregateName = typeof params.aggregateName === "string" && params.aggregateName.trim().length > 0 ? params.aggregateName : null;
			if (!targetWidget || !aggregateName) throw new Error("sankey.expandNode requires a sankey target and an aggregateName.");
			return {
				patch: buildSankeySemanticPatch({
					targetWidget,
					currentState: targetWidgetState$1(targetWidget),
					actionName: "sankey.expandNode",
					transformKind: "derive",
					analysisKey: "nodeExpansion",
					viewKey: "addRemove",
					viewState: {
						mode: "nodeExpansion",
						aggregateName
					},
					spec: { aggregateName },
					clearActionName: "sankey.collapseNodes"
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					aggregateName
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the aggregate node has been replaced by its original nodes and links.", "Read the target widget view state to confirm the corresponding collapsed-group entry was removed from _sankey_state."]
			};
		});
		if (!actionExecutor.has("sankey.highlightPath")) actionExecutor.register({ name: "sankey.highlightPath" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const path = Array.isArray(params.path) ? params.path.filter((value) => typeof value === "string" && value.trim().length > 0) : typeof params.path === "string" && params.path.trim().length > 0 ? params.path.split(",").map((value) => value.trim()).filter((value) => value.length > 0) : [];
			if (!targetWidget || path.length < 2) throw new Error("sankey.highlightPath requires a sankey target and a path with at least two nodes.");
			return {
				patch: buildSankeySemanticPatch({
					targetWidget,
					currentState: targetWidgetState$1(targetWidget),
					actionName: "sankey.highlightPath",
					transformKind: "derive",
					analysisKey: "pathHighlight",
					viewKey: "highlight",
					viewState: {
						mode: "pathHighlight",
						path
					},
					spec: {
						path,
						nodes: path
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					path
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the Sankey edge and node marks now contain path-sensitive opacity and stroke updates.", "Read the target widget view state to confirm the requested path remains prominent while unrelated edges and nodes are dimmed."]
			};
		});
		if (!actionExecutor.has("sankey.traceNode")) actionExecutor.register({ name: "sankey.traceNode" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const nodeName = typeof params.nodeName === "string" && params.nodeName.trim().length > 0 ? params.nodeName.trim() : null;
			if (!targetWidget || !nodeName) throw new Error("sankey.traceNode requires a sankey target and a nodeName.");
			return {
				patch: buildSankeySemanticPatch({
					targetWidget,
					currentState: targetWidgetState$1(targetWidget),
					actionName: "sankey.traceNode",
					transformKind: "derive",
					analysisKey: "nodeTrace",
					viewKey: "highlight",
					viewState: {
						mode: "nodeTrace",
						nodeName
					},
					spec: { nodeName }
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					nodeName
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the selectedNode signal or mark encodings now target the requested node and its directly connected flows.", "Read the target widget view state to confirm unrelated Sankey edges and nodes are dimmed relative to the traced node."]
			};
		});
		if (!actionExecutor.has("sankey.colorFlows")) actionExecutor.register({ name: "sankey.colorFlows" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const nodes = Array.isArray(params.nodes) ? params.nodes.filter((value) => typeof value === "string" && value.trim().length > 0) : [];
			const color = typeof params.color === "string" && params.color.trim().length > 0 ? params.color.trim() : "#e74c3c";
			if (!targetWidget || nodes.length === 0) throw new Error("sankey.colorFlows requires a sankey target and one or more node names.");
			return {
				patch: buildSankeySemanticPatch({
					targetWidget,
					currentState: targetWidgetState$1(targetWidget),
					actionName: "sankey.colorFlows",
					transformKind: "reencode",
					analysisKey: "flowColor",
					viewKey: "reencode",
					viewState: {
						mode: "flowColor",
						nodes,
						color
					},
					spec: {
						nodes,
						color
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					nodes,
					color
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the Sankey edgeMark fill encoding now recolors flows connected to the requested nodes.", "Read the target widget view state to confirm unrelated edge colors still resolve through the prior fill encoding fallback."]
			};
		});
		if (!actionExecutor.has("sankey.reorderNodesInLayer")) actionExecutor.register({ name: "sankey.reorderNodesInLayer" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const depth = typeof params.depth === "number" && Number.isFinite(params.depth) ? params.depth : null;
			const order = Array.isArray(params.order) ? params.order.filter((value) => typeof value === "string" && value.trim().length > 0) : [];
			if (!targetWidget || depth == null || order.length === 0) throw new Error("sankey.reorderNodesInLayer requires a sankey target, a numeric depth, and a non-empty order list.");
			return {
				patch: buildSankeySemanticPatch({
					targetWidget,
					currentState: targetWidgetState$1(targetWidget),
					actionName: "sankey.reorderNodesInLayer",
					transformKind: "reencode",
					analysisKey: "nodeLayerOrder",
					viewKey: "reencode",
					viewState: {
						mode: "nodeLayerOrder",
						depth,
						order
					},
					spec: {
						depth,
						order
					}
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					depth,
					order
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the nodeConfig entries at the requested depth now use the requested order values.", "Read the target widget view state to confirm the Sankey layer ordering changed without altering the underlying raw link topology."]
			};
		});
		if (!actionExecutor.has("sankey.autoCollapseByRank")) actionExecutor.register({ name: "sankey.autoCollapseByRank" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const topN = typeof params.topN === "number" && Number.isFinite(params.topN) ? params.topN : null;
			if (!targetWidget || topN == null || topN < 0) throw new Error("sankey.autoCollapseByRank requires a sankey target and a non-negative numeric topN.");
			return {
				patch: buildSankeySemanticPatch({
					targetWidget,
					currentState: targetWidgetState$1(targetWidget),
					actionName: "sankey.autoCollapseByRank",
					transformKind: "aggregate",
					analysisKey: "autoCollapse",
					viewKey: "addRemove",
					viewState: {
						mode: "autoCollapseByRank",
						topN
					},
					spec: { topN }
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					topN
				},
				verificationHints: ["Call perception.inspectViewConfig to verify lower-ranked nodes were replaced by per-layer aggregate nodes in rawLinks/nodeConfig.", "Read the target widget view state to confirm the original Sankey topology remains preserved in _sankey_state for later restoration."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/scatter/semanticPatches.js
	function replaceActionTransform$1(transforms, actionName, nextTransform) {
		return [...(Array.isArray(transforms) ? transforms : []).filter((transform) => !(transform?.source === "action" && transform?.spec?.actionName === actionName)), nextTransform];
	}
	function buildScatterRegionSelectionPatch({ targetWidget, currentState, selectionId, xField, yField, xRange, yRange, selectedCount, tag }) {
		const [xMin, xMax] = [Math.min(...xRange), Math.max(...xRange)];
		const [yMin, yMax] = [Math.min(...yRange), Math.max(...yRange)];
		return buildWidgetSelectionPatch({
			targetWidget,
			currentState,
			selection: {
				selection_id: selectionId,
				source_widget_id: targetWidget.widgetId || void 0,
				selection_type: "interval",
				fields: [xField, yField],
				value: {
					[xField]: [xMin, xMax],
					[yField]: [yMin, yMax]
				},
				domain: {
					xDomain: [xMin, xMax],
					yDomain: [yMin, yMax]
				},
				predicates: [{
					field: xField,
					op: "between",
					value: [xMin, xMax]
				}, {
					field: yField,
					op: "between",
					value: [yMin, yMax]
				}],
				count: selectedCount,
				summary: `${xField} ${xMin}~${xMax}; ${yField} ${yMin}~${yMax}`,
				...tag ? { tag } : {}
			}
		});
	}
	function buildScatterZoomDomainPatch({ targetWidget, xDomain, yDomain }) {
		return { [targetWidget.ref]: { view: {
			...targetWidget.view || {},
			...xDomain ? { xDomain } : {},
			...yDomain ? { yDomain } : {},
			zoom: buildViewZoomState({
				widgetSpec: targetWidget.rawSpec || targetWidget.currentSpec,
				xDomain,
				yDomain
			})
		} } };
	}
	function buildScatterCategoricalFilterPatch({ targetWidget, currentState, field, categoriesToRemove, visibleCount }) {
		return buildWidgetFilterPatch({
			targetWidget,
			currentState,
			actionName: "scatter.filterCategorical",
			field,
			values: categoriesToRemove,
			predicates: [{
				field,
				op: "notIn",
				value: categoriesToRemove
			}],
			visibleCount
		});
	}
	function buildScatterClusterAnalysisPatch({ targetWidget, currentState, method, nClusters, clusterField, clusterStatistics, labels, annotatedRows, xField, yField }) {
		const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget || {};
		const actionName = "scatter.identifyClusters";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$1(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "derive",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					method,
					nClusters,
					clusterField,
					fields: [xField, yField],
					clusterStatistics,
					labels
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					clusters: {
						sourceAction: actionName,
						method,
						nClusters,
						clusterField,
						clusterStatistics
					}
				},
				derivedRows: annotatedRows
			},
			encodings: {
				...currentWidgetState.encodings || {},
				color: {
					...currentWidgetState.encodings?.color || {},
					field: clusterField,
					type: "nominal",
					sourceAction: actionName
				}
			},
			view: {
				...currentWidgetState.view || {},
				reencode: {
					mode: "clusterEncoding",
					sourceAction: actionName,
					method,
					clusterField,
					nClusters
				}
			}
		} };
	}
	function buildScatterRegressionOverlayPatch({ targetWidget, currentState, method, xField, yField, regression }) {
		const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget || {};
		const actionName = "scatter.showRegression";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform$1(currentWidgetState.transforms, actionName, makeTransformState({
				kind: "derive",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					method,
					fields: [xField, yField],
					slope: regression.slope,
					intercept: regression.intercept,
					domain: regression.domain
				}
			})),
			data: {
				...currentWidgetState.data || {},
				analysis: {
					...currentWidgetState.data?.analysis || {},
					regression: {
						sourceAction: actionName,
						method,
						xField,
						yField,
						slope: regression.slope,
						intercept: regression.intercept,
						domain: regression.domain,
						points: regression.points
					}
				}
			},
			view: {
				...currentWidgetState.view || {},
				reencode: {
					mode: "regressionOverlay",
					sourceAction: actionName,
					method,
					xField,
					yField,
					slope: regression.slope,
					intercept: regression.intercept,
					domain: regression.domain,
					points: regression.points
				}
			}
		} };
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/scatter/runtimeActions.js
	function targetWidgetState(targetWidget) {
		return { widgets: { [targetWidget.ref]: targetWidget } };
	}
	var SCATTER_SELECT_TAG = "scatter.selectRegion";
	function countRowsInRegion(rows, { xField, yField, xRange, yRange }) {
		const [xMin, xMax] = [Math.min(...xRange), Math.max(...xRange)];
		const [yMin, yMax] = [Math.min(...yRange), Math.max(...yRange)];
		return {
			selectedCount: (Array.isArray(rows) ? rows : []).filter((row) => {
				const x = row?.[xField];
				const y = row?.[yField];
				return typeof x === "number" && typeof y === "number" && x >= xMin && x <= xMax && y >= yMin && y <= yMax;
			}).length,
			xRange: [xMin, xMax],
			yRange: [yMin, yMax]
		};
	}
	function buildScatterRegionActionOutput({ params, ctx, actionName, selectionId, tag, selectionCountHint }) {
		const targetWidget = ctx.targetWidget();
		const xRange = Array.isArray(params.xRange) ? params.xRange : null;
		const yRange = Array.isArray(params.yRange) ? params.yRange : null;
		if (!targetWidget || !params.xField || !params.yField || !xRange || !yRange) throw new Error(`${actionName} requires a scatter target, xField, yField, xRange, and yRange.`);
		const selected = countRowsInRegion(ctx.readRows(targetWidget.ref), {
			xField: params.xField,
			yField: params.yField,
			xRange,
			yRange
		});
		return {
			patch: buildScatterRegionSelectionPatch({
				targetWidget,
				selectionId,
				xField: params.xField,
				yField: params.yField,
				xRange: selected.xRange,
				yRange: selected.yRange,
				selectedCount: selected.selectedCount,
				...tag ? { tag } : {}
			}),
			affectedRefs: [targetWidget.ref],
			result: { selectedCount: selected.selectedCount },
			selectedCount: selected.selectedCount,
			verificationHints: ["Read the updated widget selection state.", selectionCountHint],
			propagateFromSelection: true
		};
	}
	function euclideanDistanceSquared$1(a, b) {
		const dx = a[0] - b[0];
		const dy = a[1] - b[1];
		return dx * dx + dy * dy;
	}
	function assignPointsToCenters$1(points, centers) {
		return points.map((point) => {
			let bestIndex = 0;
			let bestDistance = Number.POSITIVE_INFINITY;
			centers.forEach((center, index) => {
				const distance = euclideanDistanceSquared$1(point, center);
				if (distance < bestDistance) {
					bestDistance = distance;
					bestIndex = index;
				}
			});
			return bestIndex;
		});
	}
	function recomputeCenters$1(points, labels, clusterCount, previousCenters) {
		return Array.from({ length: clusterCount }, (_, clusterIndex) => {
			const clusterPoints = points.filter((_, pointIndex) => labels[pointIndex] === clusterIndex);
			if (!clusterPoints.length) return previousCenters[clusterIndex];
			const sums = clusterPoints.reduce((acc, point) => [acc[0] + point[0], acc[1] + point[1]], [0, 0]);
			return [sums[0] / clusterPoints.length, sums[1] / clusterPoints.length];
		});
	}
	function runKMeans$1(points, clusterCount, maxIterations = 25) {
		const safeClusterCount = Math.max(1, Math.min(clusterCount, points.length));
		let centers = points.slice(0, safeClusterCount).map((point) => [...point]);
		let labels = assignPointsToCenters$1(points, centers);
		for (let iteration = 0; iteration < maxIterations; iteration += 1) {
			const nextCenters = recomputeCenters$1(points, labels, safeClusterCount, centers);
			const nextLabels = assignPointsToCenters$1(points, nextCenters);
			const unchanged = nextLabels.every((label, index) => label === labels[index]);
			centers = nextCenters;
			labels = nextLabels;
			if (unchanged) break;
		}
		return {
			labels,
			centers
		};
	}
	function readMarkType$2(mark) {
		return typeof mark === "string" ? mark : mark?.type || null;
	}
	function isScatterFamilyMark(mark) {
		const markType = readMarkType$2(mark);
		return markType === "point" || markType === "circle" || markType === "square";
	}
	function collectNestedScatterSpecs(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const nested = [];
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) nested.push(...spec[key].filter((entry) => entry && typeof entry === "object"));
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) nested.push(spec.spec);
		return nested;
	}
	function findRepresentativeScatterSpec(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		if (isScatterFamilyMark(spec?.mark) && spec?.encoding && typeof spec.encoding === "object") return spec;
		if (Array.isArray(spec?.layer)) {
			const layeredMatch = spec.layer.find((entry) => entry && typeof entry === "object" && isScatterFamilyMark(entry?.mark) && entry?.encoding && typeof entry.encoding === "object");
			if (layeredMatch) return layeredMatch;
		}
		for (const child of collectNestedScatterSpecs(spec)) {
			const match = findRepresentativeScatterSpec(child);
			if (match) return match;
		}
		return null;
	}
	function detectScatterCategoricalField(spec, explicitField) {
		if (typeof explicitField === "string" && explicitField.trim().length > 0) return explicitField;
		return (findRepresentativeScatterSpec(spec) || spec)?.encoding?.color?.field || null;
	}
	function computeLinearRegression(points) {
		const mean = (values) => values.reduce((sum, value) => sum + value, 0) / values.length;
		const xs = points.map((point) => point.x);
		const ys = points.map((point) => point.y);
		const mx = mean(xs);
		const my = mean(ys);
		const denominator = xs.reduce((sum, x) => sum + (x - mx) ** 2, 0);
		const slope = denominator === 0 ? 0 : points.reduce((sum, point) => sum + (point.x - mx) * (point.y - my), 0) / denominator;
		const intercept = my - slope * mx;
		const minX = Math.min(...xs);
		const maxX = Math.max(...xs);
		return {
			slope,
			intercept,
			domain: [minX, maxX],
			points: [{
				x: minX,
				y: intercept + slope * minX
			}, {
				x: maxX,
				y: intercept + slope * maxX
			}]
		};
	}
	function registerScatterActions(actionExecutor) {
		if (!actionExecutor.has("scatter.brushRegion")) actionExecutor.register({ name: "scatter.brushRegion" }, async (params, ctx) => {
			return buildScatterRegionActionOutput({
				params,
				ctx,
				actionName: "scatter.brushRegion",
				selectionId: "brush",
				selectionCountHint: "Call perception.summarizeSelection to confirm the selected count."
			});
		});
		if (!actionExecutor.has("scatter.selectRegion")) actionExecutor.register({ name: "scatter.selectRegion" }, async (params, ctx) => {
			return buildScatterRegionActionOutput({
				params,
				ctx,
				actionName: "scatter.selectRegion",
				selectionId: "region",
				tag: SCATTER_SELECT_TAG,
				selectionCountHint: "Call perception.summarizeSelection to confirm the selected region count."
			});
		});
		if (!actionExecutor.has("scatter.zoomDomain")) actionExecutor.register({ name: "scatter.zoomDomain" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const xDomain = Array.isArray(params.xDomain) ? params.xDomain : null;
			const yDomain = Array.isArray(params.yDomain) ? params.yDomain : null;
			if (!targetWidget || !xDomain && !yDomain) throw new Error("scatter.zoomDomain requires a scatter target and at least one domain range.");
			return {
				patch: buildScatterZoomDomainPatch({
					targetWidget,
					xDomain,
					yDomain
				}),
				affectedRefs: [targetWidget.ref],
				propagateFromRef: `${targetWidget.ref}/view/zoom`,
				result: {
					widgetId: targetWidget.widgetId,
					...xDomain ? { xDomain } : {},
					...yDomain ? { yDomain } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the scatterplot domain was updated.", "Read the target widget view state to confirm the new x/y domain values."]
			};
		});
		if (!actionExecutor.has("scatter.filterCategorical")) actionExecutor.register({ name: "scatter.filterCategorical" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const categoriesToRemove = Array.isArray(params.categoriesToRemove) ? params.categoriesToRemove.filter((value) => value != null) : [];
			if (!targetWidget || categoriesToRemove.length === 0) throw new Error("scatter.filterCategorical requires a scatter target and one or more categories to remove.");
			const currentState = targetWidgetState(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const field = detectScatterCategoricalField(currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null, params.field);
			if (!field) throw new Error("scatter.filterCategorical requires a categorical field or color encoding on the active scatter spec.");
			const visibleCount = ctx.readRows(targetWidget.ref).filter((row) => !categoriesToRemove.includes(row?.[field])).length;
			return {
				patch: buildScatterCategoricalFilterPatch({
					targetWidget,
					currentState,
					field,
					categoriesToRemove,
					visibleCount
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					field,
					categoriesToRemove
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the scatter transform now excludes the requested categories.", "Read the target widget rows to confirm the removed categories no longer appear in the visible scatter data."]
			};
		});
		if (!actionExecutor.has("scatter.identifyClusters")) actionExecutor.register({ name: "scatter.identifyClusters" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const method = typeof params.method === "string" && params.method.trim().length > 0 ? params.method.trim().toLowerCase() : "kmeans";
			const requestedClusters = Number.isFinite(params.nClusters) ? Math.floor(params.nClusters) : 3;
			const nClusters = Math.max(1, requestedClusters);
			if (method !== "kmeans") throw new Error(`scatter.identifyClusters currently only supports the kmeans method. Received: ${method}.`);
			const currentSpec = targetWidget?.currentSpec || targetWidget?.rawSpec || null;
			const visibleRows = ctx.readRows(targetWidget.ref);
			const sourceRows = Array.isArray(visibleRows) && visibleRows.length > 0 ? visibleRows : Array.isArray(currentSpec?.data?.values) ? currentSpec.data.values : [];
			const representativeSpec = findRepresentativeScatterSpec(currentSpec);
			const rootEncoding = representativeSpec?.layer?.[0]?.encoding || representativeSpec?.encoding || {};
			const xField = rootEncoding?.x?.field || null;
			const yField = rootEncoding?.y?.field || null;
			if (!xField || !yField) throw new Error("scatter.identifyClusters requires x and y encodings on the active scatter spec.");
			const points = [];
			const validSourceIndices = [];
			sourceRows.forEach((row, rowIndex) => {
				const x = row?.[xField];
				const y = row?.[yField];
				if (typeof x === "number" && Number.isFinite(x) && typeof y === "number" && Number.isFinite(y)) {
					points.push([x, y]);
					validSourceIndices.push(rowIndex);
				}
			});
			if (points.length < nClusters) throw new Error(`scatter.identifyClusters requires at least ${nClusters} visible numeric points.`);
			const { labels, centers } = runKMeans$1(points, nClusters);
			const clusterField = `cluster_${nClusters}`;
			const clusterStatistics = centers.map((center, clusterId) => ({
				cluster_id: clusterId,
				size: labels.filter((label) => label === clusterId).length,
				center
			}));
			const sourceIndexToCluster = /* @__PURE__ */ new Map();
			validSourceIndices.forEach((rowIndex, index) => {
				sourceIndexToCluster.set(rowIndex, labels[index]);
			});
			const annotatedRows = sourceRows.map((row, rowIndex) => {
				const nextRow = { ...row };
				if (sourceIndexToCluster.has(rowIndex)) nextRow[clusterField] = sourceIndexToCluster.get(rowIndex);
				return nextRow;
			});
			return {
				patch: buildScatterClusterAnalysisPatch({
					targetWidget,
					currentState: targetWidgetState(targetWidget),
					method,
					nClusters,
					clusterField,
					clusterStatistics,
					labels,
					annotatedRows,
					xField,
					yField
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					method,
					nClusters,
					clusterField,
					clusterStatistics,
					centers,
					message: `Identified ${nClusters} clusters`
				},
				verificationHints: ["Read the target widget state and confirm view.reencode.mode is clusterEncoding.", "Read the target widget data.analysis.clusters to confirm the derived cluster summary."]
			};
		});
		if (!actionExecutor.has("scatter.showRegression")) actionExecutor.register({ name: "scatter.showRegression" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const method = typeof params.method === "string" && params.method.trim().length > 0 ? params.method.trim().toLowerCase() : "linear";
			if (!targetWidget) throw new Error("scatter.showRegression requires a valid scatter target widget.");
			if (method !== "linear") throw new Error(`scatter.showRegression currently only supports the linear method. Received: ${method}.`);
			const currentState = targetWidgetState(targetWidget);
			const currentWidgetState = currentState?.widgets?.[targetWidget.ref] || targetWidget;
			const currentSpec = currentWidgetState?.currentSpec || currentWidgetState?.rawSpec || null;
			const representativeSpec = findRepresentativeScatterSpec(currentSpec);
			const rootEncoding = representativeSpec?.layer?.[0]?.encoding || representativeSpec?.encoding || {};
			const xField = rootEncoding?.x?.field || null;
			const yField = rootEncoding?.y?.field || null;
			if (!xField || !yField) throw new Error("scatter.showRegression requires x and y encodings.");
			const rows = ctx.readRows(targetWidget.ref);
			const points = (Array.isArray(rows) && rows.length > 0 ? rows : Array.isArray(currentSpec?.data?.values) ? currentSpec.data.values : []).map((row) => ({
				x: Number(row?.[xField]),
				y: Number(row?.[yField])
			})).filter((point) => Number.isFinite(point.x) && Number.isFinite(point.y));
			if (points.length < 2) throw new Error("scatter.showRegression requires at least two finite x/y points.");
			const regression = computeLinearRegression(points);
			return {
				patch: buildScatterRegressionOverlayPatch({
					targetWidget,
					currentState,
					method,
					xField,
					yField,
					regression
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					method,
					overlay: "regression",
					xField,
					yField,
					slope: regression.slope,
					intercept: regression.intercept
				},
				verificationHints: ["Read the target widget state and confirm view.reencode.mode is regressionOverlay.", "Read data.analysis.regression to confirm the computed slope and intercept."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/index.js
	var defaultWidgetFamily = Object.freeze({
		kind: "custom",
		actions: Object.freeze({}),
		perception: Object.freeze({}),
		interactionProfile: Object.freeze({})
	});
	var widgetFamiliesByKind = new Map([
		["scatter", scatterFamily],
		["bar", barFamily],
		["line", lineFamily],
		["heatmap", heatmapFamily],
		["parallelCoordinates", parallelCoordinatesFamily],
		["sankey", sankeyFamily],
		["custom", defaultWidgetFamily]
	]);
	var runtimeActionRegistrarsByKind = new Map([
		["scatter", registerScatterActions],
		["bar", registerBarActions],
		["line", registerLineActions],
		["heatmap", registerHeatmapActions],
		["parallelCoordinates", registerParallelCoordinatesActions],
		["sankey", registerSankeyActions]
	]);
	function normalizeSupportedWidgetKinds$1(descriptor = {}, familyKind = null) {
		if (Array.isArray(descriptor.supportedWidgetKinds) && descriptor.supportedWidgetKinds.length > 0) return descriptor.supportedWidgetKinds;
		if (descriptor.targetKind) return [descriptor.targetKind];
		if (familyKind) return [familyKind];
	}
	function normalizeExtensionDescriptor(descriptor = {}, familyKind = null) {
		return {
			...descriptor,
			supportedWidgetKinds: normalizeSupportedWidgetKinds$1(descriptor, familyKind)
		};
	}
	function normalizeExtensionEntries(entries) {
		if (!entries) return [];
		if (Array.isArray(entries)) return entries;
		if (typeof entries === "object") return Object.values(entries);
		return [];
	}
	function hasExtensionEntries(entries) {
		return normalizeExtensionEntries(entries).some((entry) => typeof entry?.handler === "function");
	}
	function registerExtensionActions(family, { actionExecutor, store } = {}) {
		for (const entry of normalizeExtensionEntries(family?.actions)) {
			const descriptor = normalizeExtensionDescriptor(entry?.descriptor || entry, family?.kind);
			if (!descriptor?.name || typeof entry?.handler !== "function") continue;
			store?.upsertActionDescriptor?.(descriptor.name, descriptor);
			actionExecutor?.register?.(descriptor, entry.handler);
		}
	}
	function registerExtensionPerceptions(family, { perceptionExecutor, store } = {}) {
		for (const entry of normalizeExtensionEntries(family?.perceptions || family?.perceptionQueries)) {
			const descriptor = normalizeExtensionDescriptor(entry?.descriptor || entry, family?.kind);
			if (!descriptor?.name || typeof entry?.handler !== "function") continue;
			store?.upsertPerceptionDescriptor?.(descriptor.name, descriptor);
			perceptionExecutor?.register?.(descriptor, async (_call, ctx) => entry.handler(ctx.readCallParams(), ctx), { supportedWidgetKinds: descriptor.supportedWidgetKinds });
		}
	}
	function getWidgetFamily(kind) {
		return widgetFamiliesByKind.get(kind) || defaultWidgetFamily;
	}
	function listWidgetFamilies() {
		return Array.from(widgetFamiliesByKind.values());
	}
	function buildWidgetFamilyActionDescriptors(kind, args = {}) {
		return getWidgetFamily(kind)?.actions?.buildDescriptors?.(args) || [];
	}
	function buildWidgetFamilyPerceptionDescriptors(kind, args = {}) {
		return getWidgetFamily(kind)?.perception?.buildDescriptors?.(args) || [];
	}
	function getWidgetFamilyHumanInteractionConfig(kind, args = {}) {
		return getWidgetFamily(kind)?.interactionProfile?.getConfig?.(args) || null;
	}
	function registerWidgetFamilyRuntime(kind, { actionExecutor, perceptionExecutor } = {}) {
		const family = getWidgetFamily(kind);
		runtimeActionRegistrarsByKind.get(family.kind)?.(actionExecutor);
		family.perception?.register?.(perceptionExecutor);
	}
	function registerRuntimeWidgetFamily(family, { actionExecutor, perceptionExecutor, store } = {}) {
		if (!family || typeof family !== "object") throw new Error("registerWidgetFamily requires a widget family object.");
		if (!family.kind) throw new Error("registerWidgetFamily requires family.kind.");
		const builtinFamily = getWidgetFamily(family.kind);
		const hasExtensions = hasExtensionEntries(family.actions) || hasExtensionEntries(family.perceptions) || hasExtensionEntries(family.perceptionQueries);
		if (builtinFamily === family || !hasExtensions) {
			registerWidgetFamilyRuntime(family.kind, {
				actionExecutor,
				perceptionExecutor
			});
			return family;
		}
		registerExtensionActions(family, {
			actionExecutor,
			store
		});
		registerExtensionPerceptions(family, {
			perceptionExecutor,
			store
		});
		return family;
	}
	function registerDefaultWidgetFamiliesRuntime({ actionExecutor, perceptionExecutor } = {}) {
		for (const family of listWidgetFamilies()) registerWidgetFamilyRuntime(family.kind, {
			actionExecutor,
			perceptionExecutor
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/context/knowledge.js
	function clone$37(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var FIRST_CLASS_WIDGET_FAMILIES = Object.freeze([
		barFamily,
		heatmapFamily,
		lineFamily,
		parallelCoordinatesFamily,
		sankeyFamily,
		scatterFamily
	]);
	var COMMON_AGENT_TOOLS = Object.freeze({
		actions: Object.freeze([]),
		perceptions: Object.freeze([
			Object.freeze({
				name: "perception.inspectViewConfig",
				description: "Read the current widget encodings, transforms, domains, selections, and feedback state.",
				paramsSchema: Object.freeze({
					type: "object",
					properties: Object.freeze({})
				})
			}),
			Object.freeze({
				name: "perception.inspectVisibleRows",
				description: "Read a bounded sample of rows currently visible in the target widget.",
				paramsSchema: Object.freeze({
					type: "object",
					properties: Object.freeze({ limit: Object.freeze({ type: "number" }) })
				})
			}),
			Object.freeze({
				name: "perception.summarizeVisible",
				description: "Read a grouped or aggregate summary over rows currently visible in the target widget.",
				paramsSchema: Object.freeze({
					type: "object",
					properties: Object.freeze({
						groupBy: Object.freeze({
							type: "array",
							items: Object.freeze({ type: "string" })
						}),
						fields: Object.freeze({
							type: "array",
							items: Object.freeze({ type: "string" })
						}),
						metrics: Object.freeze({
							type: "array",
							items: Object.freeze({ type: "string" })
						}),
						limit: Object.freeze({ type: "integer" })
					})
				})
			})
		])
	});
	function uniqueStrings$4(values = []) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => typeof value === "string" && value.length > 0))];
	}
	function filterDescriptorsByName(descriptors = [], allowedNames = null) {
		if (!(allowedNames instanceof Set)) return clone$37(descriptors);
		return (Array.isArray(descriptors) ? descriptors : []).filter((descriptor) => allowedNames.has(descriptor?.name)).map(clone$37);
	}
	function buildFamilyKnowledge(family, { actionNamesByKind = null, perceptionNamesByKind = null } = {}) {
		const allowedActionNames = actionNamesByKind instanceof Map ? actionNamesByKind.get(family.kind) || null : null;
		const allowedPerceptionNames = perceptionNamesByKind instanceof Map ? perceptionNamesByKind.get(family.kind) || null : null;
		const actionDescriptors = typeof family.actions?.buildDescriptors === "function" ? family.actions.buildDescriptors() : [];
		const perceptionDescriptors = typeof family.perception?.buildDescriptors === "function" ? family.perception.buildDescriptors({ dataRef: null }) : [];
		return {
			kind: family.kind,
			actions: filterDescriptorsByName(actionDescriptors, allowedActionNames),
			perceptions: filterDescriptorsByName(perceptionDescriptors, allowedPerceptionNames),
			playbook: clone$37(family.playbook || null)
		};
	}
	function buildAgentKnowledge({ task = null, widgetKinds = null, widgetActionNamesByKind = null, widgetPerceptionNamesByKind = null } = {}) {
		const selectedKinds = Array.isArray(widgetKinds) && widgetKinds.length > 0 ? uniqueStrings$4(widgetKinds) : [];
		const selectedKindSet = new Set(selectedKinds);
		const families = selectedKinds.length > 0 ? FIRST_CLASS_WIDGET_FAMILIES.filter((family) => selectedKindSet.has(family.kind)) : FIRST_CLASS_WIDGET_FAMILIES;
		return {
			...task ? { task: clone$37(task) } : {},
			commonTools: clone$37(COMMON_AGENT_TOOLS),
			widgetFamilies: families.map((family) => buildFamilyKnowledge(family, {
				actionNamesByKind: widgetActionNamesByKind,
				perceptionNamesByKind: widgetPerceptionNamesByKind
			}))
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/context/observation.js
	function clone$36(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function collectInlineRowsFromSpec(spec = null, rows = []) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return rows;
		if (Array.isArray(spec?.data?.values)) rows.push(...spec.data.values.filter((row) => row && typeof row === "object" && !Array.isArray(row)));
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) for (const child of spec[key]) collectInlineRowsFromSpec(child, rows);
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) collectInlineRowsFromSpec(spec.spec, rows);
		return rows;
	}
	function shouldSummarizeField(field = {}, rows = []) {
		const type = typeof field?.type === "string" ? field.type.toLowerCase() : "";
		if (type === "nominal" || type === "ordinal") return true;
		return false;
	}
	function buildFieldValueSummary({ fields = [], widgetState = null, maxValuesPerField = 12 } = {}) {
		const rows = collectInlineRowsFromSpec(widgetState?.rawSpec || widgetState?.currentSpec || null);
		if (rows.length === 0) return {};
		const summary = {};
		for (const field of Array.isArray(fields) ? fields : []) {
			const name = field?.name;
			if (typeof name !== "string" || name.length === 0) continue;
			if (!shouldSummarizeField(field, rows)) continue;
			const values = [];
			const seen = /* @__PURE__ */ new Set();
			for (const row of rows) {
				const value = row?.[name];
				if (value == null || typeof value === "object") continue;
				const key = JSON.stringify(value);
				if (seen.has(key)) continue;
				seen.add(key);
				values.push(value);
				if (values.length >= maxValuesPerField) break;
			}
			if (values.length > 0) summary[name] = values;
		}
		return summary;
	}
	function normalizeObservationWidgetState(widget = {}) {
		const normalized = {
			ref: widget?.ref || null,
			kind: widget?.kind || null,
			recognizedKinds: Array.isArray(widget?.recognizedKinds) ? [...widget.recognizedKinds] : [],
			focused: Boolean(widget?.focused)
		};
		if (typeof widget?.widgetId === "string" && widget.widgetId.length > 0) normalized.widgetId = widget.widgetId;
		if (typeof widget?.title === "string" && widget.title.length > 0) normalized.title = widget.title;
		if (Array.isArray(widget?.actionNames) && widget.actionNames.length > 0) normalized.actionNames = [...new Set(widget.actionNames.filter((name) => typeof name === "string" && name.length > 0))];
		if (Array.isArray(widget?.perceptionNames) && widget.perceptionNames.length > 0) normalized.perceptionNames = [...new Set(widget.perceptionNames.filter((name) => typeof name === "string" && name.length > 0))];
		if (widget?.data && typeof widget.data === "object" && !Array.isArray(widget.data)) {
			const data = clone$36(widget.data);
			if (data.currentDataRef || data.primaryDataRef || Number.isFinite(data.visibleCount) || Number.isFinite(data.selectedCount) || Array.isArray(data.fields) && data.fields.length > 0) {
				delete data.rowCount;
				normalized.data = data;
			}
		}
		if (widget?.encodings && typeof widget.encodings === "object" && !Array.isArray(widget.encodings) && Object.keys(widget.encodings).length > 0) normalized.encodings = clone$36(widget.encodings);
		return normalized;
	}
	function readActiveWidgetRefs(...candidates) {
		for (const candidate of candidates) if (Array.isArray(candidate)) return candidate.filter((ref) => typeof ref === "string" && ref.length > 0);
		return [];
	}
	function countLinks(shared = {}) {
		const explicitCount = shared?.sharedStructuralContext?.linkCount ?? shared?.structure?.linkCount ?? null;
		if (Number.isFinite(explicitCount)) return explicitCount;
		const definitions = shared?.links?.definitions || shared?.sharedStructuralContext?.links?.definitions || [];
		return Array.isArray(definitions) ? definitions.length : 0;
	}
	function buildAgentSharedAnalyticalState(shared = {}) {
		const sharedFilterContext = shared?.sharedFilterContext || {};
		const sharedViewportContext = shared?.sharedViewportContext || {};
		const sharedSemanticFocus = shared?.sharedSemanticFocus || {};
		const sharedViewContext = shared?.sharedViewContext || {};
		const sharedTransformationContext = shared?.sharedTransformationContext || {};
		const activeAnalyticalContext = shared?.activeAnalyticalContext || {};
		const highlight = shared?.highlight || {};
		return {
			filters: clone$36(sharedFilterContext?.globalFilters || shared?.filters || {}),
			viewport: clone$36(sharedViewportContext?.viewport || shared?.viewport || null),
			focus: {
				selectionSummary: sharedSemanticFocus?.selectionSummary || shared?.focus?.selectionSummary || null,
				highlightedWidgetRefs: readActiveWidgetRefs(sharedSemanticFocus?.highlightedWidgetRefs, highlight?.activeWidgetRefs, shared?.focus?.highlightedWidgetRefs)
			},
			activeContextKinds: readActiveWidgetRefs(activeAnalyticalContext?.activeContextKinds, shared?.activeContextKinds),
			comparisonTargets: readActiveWidgetRefs(sharedViewportContext?.comparisonTargets, shared?.comparisonTargets),
			structure: { linkCount: countLinks(shared) },
			sharedView: { activeWidgetRefs: readActiveWidgetRefs(sharedViewContext?.activeWidgetRefs, shared?.sharedView?.activeWidgetRefs) },
			transformation: {
				activeWidgetRefs: readActiveWidgetRefs(sharedTransformationContext?.activeWidgetRefs, shared?.transformation?.activeWidgetRefs),
				widgets: clone$36(sharedTransformationContext?.widgets || shared?.transformation?.widgets || {})
			}
		};
	}
	function buildAgentObservationState({ stateId = null, summary = null, widgets = [], sharedAnalyticalState = {} } = {}) {
		return {
			stateId,
			summary,
			widgets: (Array.isArray(widgets) ? widgets : []).map(normalizeObservationWidgetState).filter((widget) => widget.ref),
			sharedAnalyticalState: buildAgentSharedAnalyticalState(sharedAnalyticalState || {})
		};
	}
	function buildAgentObservationView({ stateId = null, snapshot = void 0, image = null, summary = null } = {}) {
		return {
			snapshot: snapshot !== void 0 ? clone$36(snapshot) : stateId ? {
				ref: `widgetva-view:${stateId}`,
				mimeType: "application/widgetva-view+json"
			} : null,
			image: clone$36(image),
			summary
		};
	}
	function buildViewObservation({ state = null, view = null } = {}) {
		if (view && typeof view === "object") return buildAgentObservationView({
			stateId: state?.stateId || null,
			snapshot: view.snapshot,
			image: view.image || null,
			summary: view.summary || null
		});
		return buildAgentObservationView({ stateId: state?.stateId || null });
	}
	function buildAgentObservation({ query = null, state = null, view = null } = {}) {
		return {
			query,
			state: buildAgentObservationState(state || {}),
			view: buildViewObservation({
				state,
				view
			})
		};
	}
	function buildAgentObservationFromWorkspaceState({ query = null, workspace = null, state = null, sharedAnalyticalState = {}, view = null } = {}) {
		const focusedWidgetRef = sharedAnalyticalState?.focusedWidgetRef || sharedAnalyticalState?.sharedSemanticFocus?.focusedWidgetRef || state?.shared?.focusedWidget || null;
		const widgetStatesByRef = state?.widgets && typeof state.widgets === "object" && !Array.isArray(state.widgets) ? state.widgets : {};
		const dataHandlesByRef = new Map((Array.isArray(workspace?.dataHandles) ? workspace.dataHandles : []).filter((handle) => typeof handle?.ref === "string" && handle.ref.length > 0).map((handle) => [handle.ref, handle]));
		const widgets = (Array.isArray(workspace?.widgets) ? workspace.widgets : []).map((widget) => {
			const widgetState = widget?.ref ? widgetStatesByRef[widget.ref] || null : null;
			const currentDataRef = widgetState?.data?.currentDataRef || widget?.primaryDataRef || null;
			const currentDataHandle = currentDataRef ? dataHandlesByRef.get(currentDataRef) || null : null;
			const data = {
				primaryDataRef: widget?.primaryDataRef || widgetState?.data?.sourceDataRef || null,
				currentDataRef,
				visibleCount: widgetState?.data?.visibleCount ?? currentDataHandle?.stats?.visibleCount ?? null,
				selectedCount: widgetState?.data?.selectedCount ?? currentDataHandle?.stats?.selectedCount ?? null,
				fields: Array.isArray(currentDataHandle?.schema?.fields) ? clone$36(currentDataHandle.schema.fields) : []
			};
			const fieldValues = buildFieldValueSummary({
				fields: data.fields,
				widgetState
			});
			if (Object.keys(fieldValues).length > 0) data.fieldValues = fieldValues;
			return {
				ref: widget?.ref || null,
				widgetId: widget?.widgetId || widgetState?.widgetId || null,
				title: widget?.title || null,
				kind: widget?.kind || widgetState?.kind || null,
				recognizedKinds: Array.isArray(widget?.recognizedKinds) ? [...widget.recognizedKinds] : [],
				actionNames: Array.isArray(widget?.actionNames) ? [...widget.actionNames] : [],
				perceptionNames: Array.isArray(widget?.perceptionNames) ? [...widget.perceptionNames] : [],
				data,
				encodings: widgetState?.encodings && typeof widgetState.encodings === "object" ? clone$36(widgetState.encodings) : null,
				focused: Boolean(widget?.ref && widget.ref === focusedWidgetRef)
			};
		}).filter((widget) => widget.ref);
		return buildAgentObservation({
			query,
			state: {
				stateId: state?.stateId || null,
				summary: state?.summary || null,
				widgets,
				sharedAnalyticalState
			},
			view: view || {
				stateId: state?.stateId || null,
				summary: state?.summary || null
			}
		});
	}
	function readObservation$1({ query = null, workspace = null, state = null, sharedAnalyticalState = null, view = null, describeWorkspace = null, readState = null, readSharedAnalyticalState = null, options = {} } = {}) {
		const resolvedWorkspace = workspace || (typeof describeWorkspace === "function" ? describeWorkspace() : null);
		const resolvedState = state || (typeof readState === "function" ? readState(options?.readStateOptions || options) : null);
		return buildAgentObservationFromWorkspaceState({
			query,
			workspace: resolvedWorkspace,
			state: resolvedState,
			sharedAnalyticalState: sharedAnalyticalState || (typeof readSharedAnalyticalState === "function" ? readSharedAnalyticalState({
				state: resolvedState,
				workspace: resolvedWorkspace
			}) : {}),
			view
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/verification/turnVerification.js
	function firstString(values = []) {
		for (const value of Array.isArray(values) ? values : []) if (typeof value === "string" && value.length > 0) return value;
		return null;
	}
	function normalizeRequiredParams(requiredParams = []) {
		return (Array.isArray(requiredParams) ? requiredParams : []).filter((name, index, names) => typeof name === "string" && name.length > 0 && names.indexOf(name) === index);
	}
	function buildTurnVerificationFeedback({ operationKind = null, act = null, beforeStateId = null, requiredParams = [], providedParams = {}, usageConfirmed = null, verificationOk = null, verificationSummary = null } = {}) {
		const normalizedRequiredParams = normalizeRequiredParams(requiredParams);
		const safeProvidedParams = providedParams && typeof providedParams === "object" && !Array.isArray(providedParams) ? providedParams : {};
		const actionErrorMessage = typeof act?.error?.message === "string" && act.error.message.length > 0 ? act.error.message : null;
		const stepChoice = (() => {
			if (!act?.ok) return {
				ok: false,
				summary: actionErrorMessage || "The selected step did not execute successfully."
			};
			if (act?.kind === "action" && usageConfirmed === false) return {
				ok: false,
				summary: "The requested action is not confirmed on the target widget."
			};
			if (act?.kind === "action") return {
				ok: true,
				summary: "The runtime accepted and executed the requested action surface."
			};
			return {
				ok: true,
				summary: "The turn executed a read-oriented step and produced an output."
			};
		})();
		const params = (() => {
			if (!act?.ok) {
				const missingParams = normalizedRequiredParams.filter((paramName) => !(paramName in safeProvidedParams));
				return {
					ok: false,
					summary: missingParams.length > 0 ? `Missing required parameters: ${missingParams.join(", ")}.` : "The runtime rejected this step before parameters could be trusted."
				};
			}
			if (normalizedRequiredParams.length === 0) return {
				ok: true,
				summary: "No required-parameter gaps were detected for this step."
			};
			const missingParams = normalizedRequiredParams.filter((paramName) => !(paramName in safeProvidedParams));
			if (missingParams.length > 0) return {
				ok: false,
				summary: `Missing required parameters: ${missingParams.join(", ")}.`
			};
			return {
				ok: true,
				summary: "Required parameters were present for this step."
			};
		})();
		const stateChange = (() => {
			if (act?.kind !== "action") return {
				ok: null,
				summary: "This turn did not execute a state-mutating action."
			};
			if (act?.stateId && beforeStateId && act.stateId !== beforeStateId) return {
				ok: true,
				summary: `State changed from ${beforeStateId} to ${act.stateId}.`
			};
			if (!act?.ok) return {
				ok: false,
				summary: "The action did not complete successfully, so no state change can be confirmed."
			};
			return {
				ok: null,
				summary: "The action returned success, but a distinct state transition was not confirmed."
			};
		})();
		const visualChange = (() => {
			if (act?.kind !== "action") return {
				ok: null,
				summary: "This turn did not request a visual state change."
			};
			if (!act?.ok) return {
				ok: null,
				summary: "Visual verification was skipped because the action did not execute successfully."
			};
			if (verificationOk === true) return {
				ok: true,
				summary: verificationSummary || "Verification confirmed the expected visible effect."
			};
			if (verificationOk === false) return {
				ok: false,
				summary: verificationSummary || "Verification reported that the visible effect was not achieved."
			};
			return {
				ok: null,
				summary: "No explicit visual verification result was available for this action."
			};
		})();
		const ok = Boolean(act?.ok && stepChoice.ok !== false && params.ok !== false && stateChange.ok !== false && visualChange.ok !== false);
		const guidance = (() => {
			if (stepChoice.ok === false) return "Re-check whether the target widget actually exposes this step before retrying.";
			if (params.ok === false) return "Re-check required parameters before retrying this step.";
			if (stateChange.ok === false) return "Choose a simpler state-changing action or inspect the current state before retrying.";
			if (visualChange.ok === false) return "Try a narrower or better-scoped action, then verify the visible effect again.";
			if (act?.kind === "action" && (stateChange.ok === null || visualChange.ok === null)) return "Inspect the current view or verification surface before deciding the next step.";
			if (ok) return "This turn verified cleanly enough to answer unless the query explicitly requires another step.";
			return `Choose a simpler, better-scoped next ${operationKind === "action" ? "action" : "step"} based on the current state.`;
		})();
		return {
			ok,
			summary: ok ? visualChange.ok === true ? visualChange.summary : act?.outputSummary || "The step executed successfully." : firstString([
				stepChoice.ok === false ? stepChoice.summary : null,
				params.ok === false ? params.summary : null,
				stateChange.ok === false ? stateChange.summary : null,
				visualChange.ok === false ? visualChange.summary : null,
				params.summary,
				stateChange.summary,
				visualChange.summary,
				stepChoice.summary,
				"The step did not verify cleanly."
			]),
			checks: {
				stepChoice,
				params,
				stateChange,
				visualChange
			},
			guidance
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/contracts/turnPayloads.js
	function clone$35(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function deriveFormalActOk(result = {}) {
		if (typeof result?.actionResult?.ok === "boolean") return result.actionResult.ok;
		if (typeof result?.ok === "boolean") return result.ok;
		if (typeof result?.success === "boolean") return result.success;
		return true;
	}
	function deriveFormalStateId(result = {}) {
		return result?.actionResult?.stateId || result?.stateId || null;
	}
	function deriveFormalUpdatedRefs(result = {}) {
		if (Array.isArray(result?.actionResult?.updatedRefs)) return result.actionResult.updatedRefs;
		if (Array.isArray(result?.updatedRefs)) return result.updatedRefs;
		return [];
	}
	function deriveFormalRecoverableState(result = {}) {
		return result?.actionResult?.recoverableState || result?.recoverableState || result?.afterView || null;
	}
	function deriveFormalError(result = {}) {
		const error = result?.actionResult?.error || result?.error || null;
		return error && typeof error === "object" && !Array.isArray(error) ? clone$35(error) : null;
	}
	function deriveFormalRecoveryHints(result = {}) {
		if (Array.isArray(result?.actionResult?.recoveryHints)) return clone$35(result.actionResult.recoveryHints);
		if (Array.isArray(result?.recoveryHints)) return clone$35(result.recoveryHints);
		return [];
	}
	function deriveFormalVerificationOk(verification = null) {
		if (!verification || typeof verification !== "object") return null;
		if (typeof verification?.result?.verified === "boolean") return verification.result.verified;
		if (typeof verification?.result?.passed === "boolean") return verification.result.passed;
		if (typeof verification?.passed === "boolean") return verification.passed;
		if (typeof verification.ok === "boolean") return verification.ok;
		return null;
	}
	function summarizeFormalRuntimePayload(payload = null) {
		if (!payload || typeof payload !== "object") return null;
		if (typeof payload.summary === "string" && payload.summary.length > 0) return payload.summary;
		if (typeof payload.message === "string" && payload.message.length > 0) return payload.message;
		if (typeof payload?.actionResult?.error?.message === "string" && payload.actionResult.error.message.length > 0) return payload.actionResult.error.message;
		if (typeof payload?.error?.message === "string" && payload.error.message.length > 0) return payload.error.message;
		if (typeof payload.result === "string" && payload.result.length > 0) return payload.result;
		if (payload?.result && typeof payload.result === "object") return JSON.stringify(payload.result).slice(0, 220);
		return null;
	}
	function buildFormalPlanPayload(plan = {}) {
		const operation = plan?.operation || {};
		return {
			objective: plan?.objective || null,
			step: {
				kind: operation.kind,
				...operation.name ? { name: operation.name } : {},
				...operation.target ? { target: clone$35(operation.target) } : {},
				...operation.params && Object.keys(operation.params).length > 0 ? { params: clone$35(operation.params) } : {},
				...operation.dataRef ? { dataRef: operation.dataRef } : {},
				...operation.query ? { query: clone$35(operation.query) } : {}
			},
			rationale: plan?.rationale || ""
		};
	}
	function buildFormalActPayload(plan = {}, result = {}) {
		const operation = plan?.operation || {};
		const error = deriveFormalError(result);
		const recoveryHints = deriveFormalRecoveryHints(result);
		const recoverableState = deriveFormalRecoverableState(result);
		return {
			kind: operation.kind,
			name: operation.name || operation.query?.kind || "data_query",
			...operation.params && Object.keys(operation.params).length > 0 ? { params: clone$35(operation.params) } : {},
			...operation.target ? { target: clone$35(operation.target) } : {},
			ok: deriveFormalActOk(result),
			outputSummary: summarizeFormalRuntimePayload(result),
			stateId: deriveFormalStateId(result),
			updatedRefs: deriveFormalUpdatedRefs(result),
			...recoverableState ? { recoverableState: clone$35(recoverableState) } : {},
			...error ? { error } : {},
			...recoveryHints.length > 0 ? { recoveryHints } : {}
		};
	}
	function buildFormalVerifyPayload({ plan = {}, act = null, verification = null, observe = null } = {}) {
		const operation = plan?.operation || {};
		const beforeStateId = observe?.state?.stateId || null;
		const verificationOk = deriveFormalVerificationOk(verification);
		const verificationSummary = summarizeFormalRuntimePayload(verification);
		const providedParams = plan?.operation?.params && typeof plan.operation.params === "object" && !Array.isArray(plan.operation.params) ? plan.operation.params : {};
		const requiredParams = Array.isArray(act?.error?.details?.requiredParams) ? act.error.details.requiredParams : [];
		return buildTurnVerificationFeedback({
			operationKind: operation?.kind || null,
			act,
			beforeStateId,
			requiredParams,
			providedParams,
			usageConfirmed: null,
			verificationOk,
			verificationSummary
		});
	}
	function buildFormalReasonPayload(baseReason = {}, { act = null, verify = null, plan = null } = {}) {
		const prefersRuntimeSummary = act?.kind === "perception" || act?.kind === "data_query";
		const completionStatus = typeof baseReason?.completion?.status === "string" && baseReason.completion.status.length > 0 ? baseReason.completion.status : typeof baseReason?.completionStatus === "string" && baseReason.completionStatus.length > 0 ? baseReason.completionStatus : null;
		if (verify?.ok) {
			const assistantText = baseReason?.answer || plan?.assistantMessage || null;
			const runtimeText = act?.outputSummary || null;
			return {
				answer: prefersRuntimeSummary ? [assistantText, runtimeText].filter((part, index, array) => typeof part === "string" && part.length > 0 && array.indexOf(part) === index).join(" ") || "Completed one agent loop step." : assistantText || runtimeText || "Completed one agent loop step.",
				...completionStatus ? { completion: { status: completionStatus } } : {}
			};
		}
		return {
			answer: verify?.summary || baseReason?.answer || plan?.assistantMessage || "The last step did not verify cleanly.",
			...completionStatus ? { completion: { status: completionStatus } } : {}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/loop/runAgentLoop.js
	function clone$34(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var COMPACT_OBSERVATION_DROP_KEYS = new Set([
		"bodyText",
		"bodyTextHint",
		"bodyTextPreview",
		"fullRows",
		"marks",
		"pageText",
		"rawCode",
		"rawRows",
		"rawSpec",
		"row",
		"rowCount",
		"rows",
		"snapshot",
		"source",
		"sourceCode",
		"spec"
	]);
	function compactArrayForTurn(value = [], key = null) {
		const limit = key === "cells" ? 64 : key === "widgets" ? 24 : key === "fields" ? 32 : 16;
		return value.slice(0, limit).map((entry) => compactValueForTurn(entry));
	}
	function compactValueForTurn(value, key = null) {
		if (Array.isArray(value)) return compactArrayForTurn(value, key);
		if (!value || typeof value !== "object") return value;
		const next = {};
		for (const [entryKey, entryValue] of Object.entries(value)) {
			if (COMPACT_OBSERVATION_DROP_KEYS.has(entryKey)) continue;
			next[entryKey] = compactValueForTurn(entryValue, entryKey);
		}
		return next;
	}
	function compactMatrixForTurn(matrix = null) {
		if (!matrix || typeof matrix !== "object" || Array.isArray(matrix)) return matrix;
		return {
			provider: matrix.provider || null,
			kind: matrix.kind || null,
			cellCount: Number.isFinite(matrix.cellCount) ? matrix.cellCount : null,
			cells: (Array.isArray(matrix.cells) ? matrix.cells : []).slice(0, 64).map((cell) => ({
				ref: cell?.ref || null,
				xField: cell?.xField || null,
				yField: cell?.yField || null,
				xDomain: Array.isArray(cell?.xDomain) ? [...cell.xDomain] : null,
				yDomain: Array.isArray(cell?.yDomain) ? [...cell.yDomain] : null
			})).filter((cell) => cell.ref),
			currentBrush: matrix.currentBrush ? compactValueForTurn(matrix.currentBrush) : null,
			summary: matrix.summary || null
		};
	}
	function compactObservationForTurn(observe = null) {
		const snapshotRef = typeof observe?.view?.snapshot?.ref === "string" && observe.view.snapshot.ref.length > 0 ? observe.view.snapshot.ref : null;
		const snapshotMimeType = typeof observe?.view?.snapshot?.mimeType === "string" && observe.view.snapshot.mimeType.length > 0 ? observe.view.snapshot.mimeType : null;
		const compact = compactValueForTurn(clone$34(observe));
		if (compact?.state?.matrix) compact.state.matrix = compactMatrixForTurn(compact.state.matrix);
		if (compact?.matrix) compact.matrix = compactMatrixForTurn(compact.matrix);
		if (compact?.view && typeof compact.view === "object") if (snapshotRef) compact.view.snapshot = {
			ref: snapshotRef,
			...snapshotMimeType ? { mimeType: snapshotMimeType } : {}
		};
		else delete compact.view.snapshot;
		return compact;
	}
	function isPlainObject$5(value) {
		return Boolean(value) && typeof value === "object" && !Array.isArray(value);
	}
	function uniqueStrings$3(values = []) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => typeof value === "string" && value.length > 0))];
	}
	function readWidgetKindsFromObservation(observation = {}) {
		return uniqueStrings$3((Array.isArray(observation?.state?.widgets) ? observation.state.widgets : []).flatMap((widget) => {
			const recognizedKinds = Array.isArray(widget?.recognizedKinds) ? widget.recognizedKinds : [];
			return recognizedKinds.length > 0 ? recognizedKinds : [widget?.kind];
		}));
	}
	function addNamesForKind(target, kind = null, names = []) {
		if (typeof kind !== "string" || kind.length === 0) return;
		const filteredNames = uniqueStrings$3(names);
		if (filteredNames.length === 0) return;
		const existing = target.get(kind) || /* @__PURE__ */ new Set();
		for (const name of filteredNames) existing.add(name);
		target.set(kind, existing);
	}
	function readWidgetNameMapFromObservation(observation = {}, fieldName = "") {
		const widgets = Array.isArray(observation?.state?.widgets) ? observation.state.widgets : [];
		const result = /* @__PURE__ */ new Map();
		for (const widget of widgets) {
			const names = Array.isArray(widget?.[fieldName]) ? widget[fieldName] : [];
			if (names.length === 0) continue;
			const recognizedKinds = Array.isArray(widget?.recognizedKinds) ? widget.recognizedKinds : [];
			const kinds = uniqueStrings$3(recognizedKinds.length > 0 ? recognizedKinds : [widget?.kind]);
			for (const kind of kinds) addNamesForKind(result, kind, names);
		}
		return result.size > 0 ? result : null;
	}
	function normalizeQueryScope(call = {}, fallbackWidgetRef = null) {
		const { widgetRef: _removedWidgetRef, widget_ref: _removedWidgetRefSnake, ...queryScope } = isPlainObject$5(call?.queryScope) ? call.queryScope : {};
		return Object.keys(queryScope).length > 0 ? queryScope : null;
	}
	function normalizeTarget(call = {}, fallbackWidgetRef = null) {
		const target = isPlainObject$5(call?.target) ? { ...call.target } : {};
		const widgetRef = target.widgetRef || call?.targetRef || fallbackWidgetRef || null;
		if (widgetRef && !target.widgetRef) target.widgetRef = widgetRef;
		return Object.keys(target).length > 0 ? target : null;
	}
	function normalizeOperation$1(operation = {}, { fallbackWidgetRef = null } = {}) {
		if (!isPlainObject$5(operation)) return null;
		const queryScope = normalizeQueryScope(operation, fallbackWidgetRef);
		const target = normalizeTarget(operation, fallbackWidgetRef || null);
		const normalized = { ...operation };
		if (target) normalized.target = target;
		if (queryScope) normalized.queryScope = queryScope;
		return normalized;
	}
	function buildActionCall$1(operation = {}, { callId = "agent_step_action", actor = "agent" } = {}) {
		const queryScope = normalizeQueryScope(operation);
		const target = normalizeTarget(operation);
		return {
			callId,
			name: operation.name,
			actor,
			...target ? { target } : {},
			...queryScope ? { queryScope } : {},
			params: clone$34(operation.params || {})
		};
	}
	function buildDataQueryCall(operation = {}, { callId = "agent_step_action", actor = "agent" } = {}) {
		const queryScope = normalizeQueryScope(operation);
		const target = normalizeTarget(operation);
		const query = isPlainObject$5(operation?.query) ? clone$34(operation.query) : {};
		const querySpec = isPlainObject$5(query?.spec) ? clone$34(query.spec) : {};
		const querySpecScope = normalizeQueryScope({ queryScope: querySpec.queryScope });
		if (queryScope && !querySpecScope) query.spec = {
			...querySpec,
			queryScope
		};
		else if (queryScope && querySpecScope) query.spec = {
			...querySpec,
			queryScope: {
				...queryScope,
				...querySpecScope
			}
		};
		else if (querySpecScope) query.spec = {
			...querySpec,
			queryScope: querySpecScope
		};
		else if (Object.keys(querySpec).length > 0) {
			delete querySpec.queryScope;
			query.spec = querySpec;
		}
		return {
			callId,
			actor,
			...target ? { target } : {},
			...operation?.dataRef ? { dataRef: operation.dataRef } : {},
			query,
			...queryScope ? { queryScope } : {}
		};
	}
	function buildVerificationCall(actionCall = {}, result = {}) {
		const queryScope = normalizeQueryScope(actionCall);
		const target = normalizeTarget(actionCall);
		const stateId = result?.actionResult?.stateId || result?.stateId || null;
		const refs = Array.isArray(result?.actionResult?.updatedRefs) ? result.actionResult.updatedRefs : Array.isArray(result?.updatedRefs) ? result.updatedRefs : [];
		return {
			callId: `${actionCall.callId || "agent_step_action"}_verify`,
			name: "perception.verifyActionEffect",
			actor: actionCall.actor || "agent",
			...target ? { target } : {},
			...queryScope ? { queryScope } : {},
			params: {
				actionName: actionCall.name || null,
				stateId,
				refs
			}
		};
	}
	function resolveMethod(target, methodNames = []) {
		for (const methodName of methodNames) if (typeof target?.[methodName] === "function") return target[methodName].bind(target);
		return null;
	}
	async function readObserveStage(target, { loopOptions = {}, observationOptions = {} } = {}) {
		const readObservation = resolveMethod(target, ["readObservation"]);
		if (typeof readObservation !== "function") throw new Error("runAgentLoop requires target.readObservation().");
		return buildAgentObservation(await readObservation({
			...observationOptions,
			query: loopOptions?.objective || observationOptions?.query || null
		}));
	}
	async function readWorkspacePlan(target, planningRequest = null) {
		const planWorkspace = resolveMethod(target, ["planWorkspace"]);
		if (!planningRequest || typeof planWorkspace !== "function") return null;
		return planWorkspace(planningRequest);
	}
	function defaultReasoner({ plan, result, verification, latestCoordinationResult }) {
		const actionApplied = Boolean(result?.actionResult?.ok ?? result?.ok ?? result?.success ?? false);
		const verificationPassed = Boolean(verification?.ok ?? verification?.result?.passed ?? result?.verification?.ok ?? false);
		const operationKind = plan?.operation?.kind || null;
		const runtimeSummary = summarizeFormalRuntimePayload(result);
		return {
			answer: operationKind === "perception" || operationKind === "data_query" ? runtimeSummary || plan?.assistantMessage || "Completed one agent loop step." : plan?.assistantMessage || runtimeSummary || "Completed one agent loop step.",
			rationale: plan?.rationale || "",
			success: actionApplied,
			verificationPassed,
			latestCoordinationResult: clone$34(latestCoordinationResult)
		};
	}
	function buildPlanStage({ objective = null, workspacePlan = null, planningResult = null, operation = null } = {}) {
		return {
			objective,
			workspacePlan: clone$34(workspacePlan),
			assistantMessage: planningResult?.assistantMessage || "",
			rationale: planningResult?.rationale || "",
			operation: clone$34(operation),
			rawPlan: clone$34(planningResult)
		};
	}
	async function resolvePlanningResult({ planner, observe, knowledge = null, history = null, workspacePlan, objective = null, operation = null, actor = "agent" } = {}) {
		if (operation) return {
			assistantMessage: "",
			rationale: "",
			operation: normalizeOperation$1(operation),
			actor,
			source: "provided_operation"
		};
		if (typeof planner !== "function") throw new Error("runAgentLoop requires either operation or planner.");
		const planningResult = await planner({
			objective,
			observe: clone$34(observe),
			knowledge: clone$34(knowledge),
			history: clone$34(history),
			workspacePlan: clone$34(workspacePlan),
			actor
		});
		if (!isPlainObject$5(planningResult)) throw new Error("planner must return an object containing operation.");
		const normalizedOperation = normalizeOperation$1(planningResult.operation);
		if (!normalizedOperation) throw new Error("planner must return a valid operation object.");
		return {
			...planningResult,
			operation: normalizedOperation,
			actor: planningResult.actor || actor,
			source: planningResult.source || "planner"
		};
	}
	async function executeOperation(target, operation, { actor = "agent", callId = "agent_step_action" } = {}) {
		if (!operation || typeof operation.kind !== "string") throw new Error("executeOperation requires an operation with kind.");
		if (operation.kind === "action") {
			const actionCall = buildActionCall$1(operation, {
				callId,
				actor
			});
			const runVerifiedAction = resolveMethod(target, ["executeVerifiedAction", "runVerifiedAction"]);
			if (typeof runVerifiedAction === "function") return {
				executionKind: "verified_action",
				actionCall,
				result: await runVerifiedAction(actionCall, {})
			};
			const executeAction = resolveMethod(target, ["executeAction", "runAction"]);
			if (typeof executeAction !== "function") throw new Error("Target does not expose executeAction() or executeVerifiedAction().");
			return {
				executionKind: "action",
				actionCall,
				result: await executeAction(actionCall)
			};
		}
		if (operation.kind === "perception") {
			const queryPerception = resolveMethod(target, ["queryPerception"]);
			if (typeof queryPerception !== "function") throw new Error("Target does not expose queryPerception().");
			const call = {
				callId,
				name: operation.name,
				actor,
				...operation.target ? { target: clone$34(operation.target) } : {},
				...operation.queryScope ? { queryScope: clone$34(operation.queryScope) } : {},
				params: clone$34(operation.params || {})
			};
			return {
				executionKind: "perception",
				actionCall: call,
				result: await queryPerception(call)
			};
		}
		if (operation.kind === "data_query") {
			const runDataQuery = resolveMethod(target, ["runDataQuery", "queryData"]);
			if (typeof runDataQuery !== "function") throw new Error("Target does not expose runDataQuery().");
			const call = buildDataQueryCall(operation, {
				callId,
				actor
			});
			return {
				executionKind: "data_query",
				actionCall: call,
				result: await runDataQuery(call)
			};
		}
		throw new Error(`Unsupported operation kind: ${operation.kind}`);
	}
	async function verifyOperation(target, execution = null, { verify = true } = {}) {
		if (!verify || !execution || execution.executionKind !== "action" && execution.executionKind !== "verified_action") return null;
		const resultVerification = execution?.result?.verification;
		if (resultVerification) return resultVerification;
		const queryPerception = resolveMethod(target, ["queryPerception"]);
		if (typeof queryPerception !== "function") return null;
		return queryPerception(buildVerificationCall(execution.actionCall, execution.result));
	}
	async function runAgentLoop(target, options = {}) {
		if (!target || typeof target !== "object") throw new Error("runAgentLoop requires a target object.");
		if (typeof target.readObservation !== "function") throw new Error("runAgentLoop requires target.readObservation().");
		const { objective = null, planner = null, reasoner = defaultReasoner, operation = null, planningRequest = null, verify = true, loopOptions = {}, observationOptions = {}, callId = "agent_step_action", actor = "agent" } = options;
		const observe = await readObserveStage(target, {
			loopOptions: {
				...loopOptions,
				objective
			},
			observationOptions
		});
		const workspacePlan = await readWorkspacePlan(target, planningRequest);
		const baseKnowledge = buildAgentKnowledge({
			widgetKinds: readWidgetKindsFromObservation(observe),
			widgetActionNamesByKind: readWidgetNameMapFromObservation(observe, "actionNames"),
			widgetPerceptionNamesByKind: readWidgetNameMapFromObservation(observe, "perceptionNames")
		});
		const knowledge = options?.sessionKnowledge || baseKnowledge;
		const history = mergeSessionHistory(options?.sessionHistory || null, options?.sessionTurns || []);
		const planningResult = await resolvePlanningResult({
			planner,
			observe,
			knowledge,
			history,
			workspacePlan,
			objective,
			operation,
			actor
		});
		const plan = buildPlanStage({
			objective,
			workspacePlan,
			planningResult,
			operation: planningResult.operation
		});
		const execution = await executeOperation(target, planningResult.operation, {
			actor: planningResult.actor || actor,
			callId
		});
		const verification = await verifyOperation(target, execution, { verify });
		const latestCoordinationResult = typeof target?.readLatestCoordinationResult === "function" ? await target.readLatestCoordinationResult() : null;
		const reason = await reasoner({
			objective,
			observe: clone$34(compactObservationForTurn(observe)),
			history: clone$34(history),
			plan: clone$34(plan),
			result: clone$34(execution.result),
			verification: clone$34(verification),
			latestCoordinationResult: clone$34(latestCoordinationResult)
		});
		return {
			observe,
			plan,
			result: execution.result,
			verification,
			reason,
			latestCoordinationResult
		};
	}
	async function runAgentTurn(target, options = {}) {
		const turnIndex = Number.isFinite(options?.turnIndex) && options.turnIndex >= 0 ? Math.floor(options.turnIndex) : 0;
		const base = await runAgentLoop(target, options);
		const observe = compactObservationForTurn(base.observe);
		const plan = buildFormalPlanPayload(base.plan);
		const act = buildFormalActPayload(base.plan, base.result);
		const verify = buildFormalVerifyPayload({
			plan: base.plan,
			act,
			verification: base.verification,
			observe: base.observe
		});
		return {
			index: turnIndex,
			observe,
			plan,
			act,
			verify,
			reason: buildFormalReasonPayload(base.reason, {
				act,
				verify,
				plan: base.plan
			})
		};
	}
	async function runAgentSession(target, options = {}) {
		const { objective = null, maxTurns = 3, finalSynthesizer = null, onTurn = null } = options;
		const safeMaxTurns = Number.isFinite(maxTurns) && maxTurns > 0 ? Math.max(1, Math.floor(maxTurns)) : 3;
		const turns = [];
		let knowledge = null;
		let history = mergeSessionHistory(null, []);
		let stopReason = "turn_budget_reached";
		for (let index = 0; index < safeMaxTurns; index += 1) {
			const turn = await runAgentTurn(target, {
				...options,
				objective,
				sessionKnowledge: knowledge,
				sessionHistory: history,
				sessionTurns: turns,
				turnIndex: index,
				callId: `agent_step_${index + 1}`
			});
			turns.push(turn);
			history = mergeSessionHistory(history, turns);
			if (!knowledge) knowledge = buildAgentKnowledge({
				widgetKinds: readWidgetKindsFromObservation(turn.observe),
				widgetActionNamesByKind: readWidgetNameMapFromObservation(turn.observe, "actionNames"),
				widgetPerceptionNamesByKind: readWidgetNameMapFromObservation(turn.observe, "perceptionNames")
			});
			if (typeof onTurn === "function") await onTurn({
				index,
				turn: clone$34(turn),
				turns: clone$34(turns),
				history: clone$34(history),
				objective: objective || null
			});
			const resolvedStopReason = shouldStopAgentSession(turn);
			if (resolvedStopReason) {
				stopReason = resolvedStopReason;
				break;
			}
			if (index === safeMaxTurns - 1) stopReason = "turn_budget_reached";
		}
		const lastTurn = turns.at(-1) || null;
		let finalAnswer = lastTurn?.reason?.answer || "";
		if (typeof finalSynthesizer === "function") {
			const synthesis = await finalSynthesizer({
				objective: objective || null,
				turns: clone$34(turns),
				history: clone$34(history),
				stopReason,
				knowledge: clone$34(knowledge || buildAgentKnowledge({})),
				lastTurn: clone$34(lastTurn)
			});
			if (typeof synthesis === "string") finalAnswer = synthesis;
			else if (synthesis && typeof synthesis === "object") finalAnswer = synthesis.finalAnswer || synthesis.answer || finalAnswer;
		}
		const status = lastTurn?.act?.ok === false ? "failed" : stopReason === "answered" ? "completed" : stopReason === "stopped" || stopReason === "turn_budget_reached" ? "stopped" : "completed";
		return {
			objective: objective || null,
			status,
			ok: Boolean(lastTurn?.verify?.ok),
			answer: finalAnswer,
			finalAnswer,
			stopReason,
			knowledge: knowledge || buildAgentKnowledge({}),
			history,
			turns
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/adapters/agentTargetPort.js
	function bindOptionalMethod(target, methodName) {
		return typeof target?.[methodName] === "function" ? target[methodName].bind(target) : void 0;
	}
	function isAgentTargetSurface(value) {
		return Boolean(value) && typeof value === "object" && typeof value.readObservation === "function";
	}
	function createAgentTargetPort(target) {
		if (!isAgentTargetSurface(target)) return null;
		return {
			readObservation: bindOptionalMethod(target, "readObservation"),
			describeWorkspace: bindOptionalMethod(target, "describeWorkspace"),
			readLatestCoordinationResult: bindOptionalMethod(target, "readLatestCoordinationResult"),
			listAvailableDataQueries: bindOptionalMethod(target, "listAvailableDataQueries"),
			executeVerifiedAction: bindOptionalMethod(target, "executeVerifiedAction"),
			executeAction: bindOptionalMethod(target, "executeAction"),
			queryPerception: bindOptionalMethod(target, "queryPerception"),
			runDataQuery: bindOptionalMethod(target, "runDataQuery"),
			queryData: bindOptionalMethod(target, "queryData") || bindOptionalMethod(target, "runDataQuery"),
			planWorkspace: bindOptionalMethod(target, "planWorkspace")
		};
	}
	function resolveAgentTargetPort(target, sessionOptions = {}) {
		const candidates = [
			target,
			target?.pagePort,
			target?.runtime?.pagePort,
			target?.widget?.runtime?.pagePort,
			target?.workspace,
			target?.widget
		];
		for (const candidate of candidates) {
			const port = createAgentTargetPort(candidate, sessionOptions);
			if (port) return port;
		}
		throw new Error("WidgetVA agent execution requires a target that exposes readObservation() through a page port, runtime page port, widget, or workspace surface.");
	}
	async function runAgentLoopOnTarget(target, options = {}) {
		return runAgentLoop(resolveAgentTargetPort(target, options), options);
	}
	//#endregion
	//#region ../../widgetva-kit/src/contracts/data-contracts.js
	function makeDataHandle(handle) {
		return {
			sourceKind: "inline",
			schema: { fields: [] },
			stats: {},
			kind: "dataView",
			scope: "workspace",
			widgetRef: void 0,
			sourceSelectionRef: void 0,
			supportedQueries: ["sampleRows", "summary"],
			supportedQueryDescriptors: [],
			...handle
		};
	}
	function makeDataQueryDescriptor(descriptor) {
		return {
			inputSchema: {
				type: "object",
				additionalProperties: false,
				properties: {}
			},
			resultSchema: descriptor?.resultSchema || (descriptor?.name ? DATA_QUERY_RESULT_SCHEMAS[descriptor.name] : void 0),
			examples: [],
			...descriptor
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/data/dataQueryDescriptorTemplates.js
	var DATA_QUERY_DESCRIPTOR_TEMPLATES = {
		schema: {
			title: "Inspect data schema",
			description: "Read the exposed field schema for the materialized data view.",
			resultKind: "schema",
			examples: [
				{ spec: {} },
				{ spec: { queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" } } },
				{ spec: { queryScope: {
					dataRef: "wl://demo/workspace/main/data/current_selection",
					selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
				} } }
			]
		},
		sampleRows: {
			title: "Sample visible rows",
			description: "Read a bounded sample of rows from the shared current view or another materialized data view, optionally scoped to an active selection.",
			resultKind: "rowSample",
			examples: [
				{ spec: { limit: 10 } },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					limit: 10
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					limit: 10
				} }
			]
		},
		filter: {
			title: "Filter current data view",
			description: "Run a predicate filter over the shared current view or another materialized data view, optionally after scoping to an active selection, without mutating workspace state.",
			resultKind: "filteredRows",
			examples: [
				{ spec: { predicates: [{
					field: "Region",
					op: "in",
					value: ["North", "West"]
				}] } },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					predicates: [{
						field: "Region",
						op: "in",
						value: ["North", "West"]
					}]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					predicates: [{
						field: "Region",
						op: "in",
						value: ["North", "West"]
					}]
				} }
			]
		},
		aggregate: {
			title: "Aggregate current data view",
			description: "Aggregate the shared current view or another materialized data view, optionally scoped to an active selection, using group-by keys and measures.",
			resultKind: "aggregateTable",
			examples: [
				{ spec: {
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} }
			]
		},
		groupBy: {
			title: "Group current data view",
			description: "Group rows and compute summary measures over the shared current view or another materialized data view, optionally scoped to an active selection.",
			resultKind: "groupedTable",
			examples: [
				{ spec: {
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					groupBy: ["Region"],
					measures: [{
						op: "count",
						as: "recordCount"
					}]
				} }
			]
		},
		sql: {
			title: "Execute data-view SQL",
			description: "Run a SQL-like query supported by the underlying data engine over the current view or a scoped active selection.",
			resultKind: "queryTable",
			examples: [
				{ spec: { sql: "select Region, count(*) as recordCount from data group by Region" } },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					sql: "select Region, count(*) as recordCount from data group by Region"
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					sql: "select Region, count(*) as recordCount from data group by Region"
				} }
			]
		},
		summary: {
			title: "Summarize current data view",
			description: "Compute a compact summary over the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "summaryTable",
			examples: [
				{ spec: {
					fields: ["LatencyMs"],
					metrics: [
						"min",
						"max",
						"mean"
					]
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					fields: ["LatencyMs"],
					metrics: ["mean"]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					fields: ["LatencyMs"],
					metrics: ["mean"]
				} }
			]
		},
		computeCorrelation: {
			title: "Compute correlation",
			description: "Compute correlation statistics between two quantitative fields over the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "correlationStats",
			examples: [
				{ spec: {
					xField: "LatencyMs",
					yField: "ErrorRate"
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					xField: "LatencyMs",
					yField: "ErrorRate"
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					xField: "LatencyMs",
					yField: "ErrorRate"
				} }
			]
		},
		findExtremes: {
			title: "Find extremes",
			description: "Find records with the minimum, maximum, or both extremes for a field over the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "extremeRows",
			examples: [
				{ spec: {
					field: "Traffic",
					direction: "max",
					limit: 5
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					field: "Traffic",
					direction: "max",
					limit: 5
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					field: "Traffic",
					direction: "max",
					limit: 5
				} }
			]
		},
		findOutliers: {
			title: "Find outliers",
			description: "Find records that are statistically outlying for a field over the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "outlierRows",
			examples: [
				{ spec: {
					field: "ErrorRate",
					method: "iqr",
					limit: 10
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					field: "ErrorRate",
					method: "iqr",
					limit: 10
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					field: "ErrorRate",
					method: "iqr",
					limit: 10
				} }
			]
		},
		compareGroups: {
			title: "Compare groups",
			description: "Compare two groups over a quantitative value field in the shared current view, another materialized data view, or a scoped active selection.",
			resultKind: "groupComparison",
			examples: [
				{ spec: {
					groupField: "Region",
					valueField: "LatencyMs",
					groups: ["North", "South"]
				} },
				{ spec: {
					queryScope: { dataRef: "wl://demo/workspace/main/data/current_view" },
					groupField: "Region",
					valueField: "LatencyMs",
					groups: ["North", "South"]
				} },
				{ spec: {
					queryScope: {
						dataRef: "wl://demo/workspace/main/data/current_selection",
						selectionRef: "wl://demo/workspace/main/widget/scatter/selection/brush"
					},
					groupField: "Region",
					valueField: "LatencyMs",
					groups: ["North", "South"]
				} }
			]
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/DataQueryEngine.js
	function makeDataQueryEngineEngineSummary(summary = {}) {
		return {
			kind: summary?.kind ?? null,
			className: summary?.className ?? null
		};
	}
	function makeDataQueryEngineCounts(counts = {}) {
		return {
			supportedQueryKindCount: 0,
			supportedQueryDescriptorCount: 0,
			...counts
		};
	}
	function makeDataQueryEngineCapabilities(capabilities = {}) {
		return {
			localExecution: false,
			remoteExecution: false,
			sqlSupport: false,
			fallbackEngine: false,
			...capabilities
		};
	}
	function makeDataQueryEngineSummary(summary = {}) {
		return {
			engine: makeDataQueryEngineEngineSummary(summary?.engine),
			counts: makeDataQueryEngineCounts(summary?.counts),
			supportedQueryKinds: Array.isArray(summary?.supportedQueryKinds) ? [...summary.supportedQueryKinds] : [],
			supportedQueryDescriptors: Array.isArray(summary?.supportedQueryDescriptors) ? [...summary.supportedQueryDescriptors] : [],
			capabilities: makeDataQueryEngineCapabilities(summary?.capabilities)
		};
	}
	function buildSupportedQueryDescriptors$2(kinds = []) {
		return kinds.map((kind) => makeDataQueryDescriptor({
			name: kind,
			...DATA_QUERY_DESCRIPTOR_TEMPLATES[kind],
			inputSchema: DATA_QUERY_SCHEMAS[kind] || {
				type: "object",
				additionalProperties: true,
				properties: {}
			}
		}));
	}
	function buildSummaryMeasures$1({ fields = [], metrics = [] } = {}) {
		const normalizedFields = Array.isArray(fields) ? fields.filter((field) => typeof field === "string" && field.length > 0) : [];
		const normalizedMetrics = Array.isArray(metrics) ? metrics.filter((metric) => typeof metric === "string" && metric.length > 0) : [];
		const measures = [];
		for (const metric of normalizedMetrics) {
			if (metric === "count") {
				measures.push({
					op: "count",
					as: "count"
				});
				continue;
			}
			for (const field of normalizedFields) measures.push({
				op: metric,
				field,
				as: `${field}_${metric}`
			});
		}
		return measures;
	}
	function normalizeAggregateSpec$1(spec = {}) {
		const groupBy = Array.isArray(spec.groupBy) ? spec.groupBy : [];
		const measures = Array.isArray(spec.measures) ? spec.measures : Array.isArray(spec.metrics) ? spec.metrics : [];
		return {
			...spec,
			groupBy,
			measures
		};
	}
	function normalizeSummarySpec$1(spec = {}) {
		const measures = Array.isArray(spec.measures) && spec.measures.length > 0 ? spec.measures : buildSummaryMeasures$1({
			fields: spec.fields,
			metrics: spec.metrics
		});
		return {
			...spec,
			measures
		};
	}
	function normalizeExtremesSpec$1(spec = {}) {
		const direction = typeof spec.direction === "string" ? spec.direction : null;
		return {
			...spec,
			order: direction === "min" ? "ascending" : "descending"
		};
	}
	function normalizeCompareGroupsSpec$1(spec = {}) {
		if (Array.isArray(spec.groups) && spec.groups.length >= 2) return spec;
		return {
			...spec,
			groups: [spec.leftGroup, spec.rightGroup].filter((value) => value != null)
		};
	}
	var DataQueryEngine = class {
		resolveQuerySource(source) {
			if (typeof source !== "string" || source.length === 0) return source;
			const resolver = typeof this.resolveSource === "function" ? this.resolveSource.bind(this) : typeof this.options?.resolveSource === "function" ? this.options.resolveSource.bind(this.options) : null;
			if (typeof resolver !== "function") return source;
			const resolvedSource = resolver(source);
			return resolvedSource == null ? source : resolvedSource;
		}
		query(source, query = {}) {
			const resolvedSource = this.resolveQuerySource(source);
			const kind = query?.kind || null;
			const spec = query?.spec || {};
			if (kind === "schema") return this.getSchema(resolvedSource);
			if (kind === "filter") return this.filter(resolvedSource, Array.isArray(spec?.predicates) ? spec.predicates : []);
			if (kind === "sampleRows") return this.sampleRows(resolvedSource, spec?.limit || 20);
			if (kind === "aggregate" || kind === "groupBy") return this.aggregate(resolvedSource, normalizeAggregateSpec$1(spec));
			if (kind === "summary") return this.summarize(resolvedSource, normalizeSummarySpec$1(spec));
			if (kind === "sql") return this.executeSql(resolvedSource, spec);
			if (kind === "computeCorrelation") return this.computeCorrelation(resolvedSource, spec);
			if (kind === "findExtremes") return this.findExtremes(resolvedSource, normalizeExtremesSpec$1(spec));
			if (kind === "findOutliers") return this.findOutliers(resolvedSource, spec);
			if (kind === "compareGroups") return this.compareGroups(resolvedSource, normalizeCompareGroupsSpec$1(spec));
			throw new Error(`Unsupported data query kind: ${kind || "missing"}.`);
		}
		describeEngine() {
			const supportedQueryKinds = typeof this.listSupportedQueryKinds === "function" ? this.listSupportedQueryKinds() : [];
			const supportedQueryDescriptors = buildSupportedQueryDescriptors$2(supportedQueryKinds);
			return makeDataQueryEngineSummary({
				engine: makeDataQueryEngineEngineSummary({
					kind: this.kind || null,
					className: this.constructor?.name || null
				}),
				counts: makeDataQueryEngineCounts({
					supportedQueryKindCount: supportedQueryKinds.length,
					supportedQueryDescriptorCount: supportedQueryDescriptors.length
				}),
				supportedQueryKinds,
				supportedQueryDescriptors,
				capabilities: makeDataQueryEngineCapabilities({
					localExecution: true,
					remoteExecution: false,
					sqlSupport: supportedQueryKinds.includes("sql"),
					fallbackEngine: false
				})
			});
		}
		listSupportedQueryKinds() {
			return [
				"schema",
				"sampleRows",
				"filter",
				"aggregate",
				"groupBy",
				"summary"
			];
		}
		getSchema() {
			throw new Error("DataQueryEngine.getSchema() must be implemented by subclasses.");
		}
		filter() {
			throw new Error("DataQueryEngine.filter() must be implemented by subclasses.");
		}
		sampleRows() {
			throw new Error("DataQueryEngine.sampleRows() must be implemented by subclasses.");
		}
		executeSql() {
			throw new Error("DataQueryEngine.executeSql() must be implemented by subclasses.");
		}
		aggregate() {
			throw new Error("DataQueryEngine.aggregate() must be implemented by subclasses.");
		}
		summarize() {
			throw new Error("DataQueryEngine.summarize() must be implemented by subclasses.");
		}
		computeCorrelation() {
			throw new Error("DataQueryEngine.computeCorrelation() must be implemented by subclasses.");
		}
		findExtremes() {
			throw new Error("DataQueryEngine.findExtremes() must be implemented by subclasses.");
		}
		findOutliers() {
			throw new Error("DataQueryEngine.findOutliers() must be implemented by subclasses.");
		}
		compareGroups() {
			throw new Error("DataQueryEngine.compareGroups() must be implemented by subclasses.");
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/JsArrayDataQueryEngine.js
	function inferFieldType(value) {
		if (typeof value === "number") return "quantitative";
		if (typeof value === "boolean") return "boolean";
		if (typeof value === "string") return "nominal";
		return "nominal";
	}
	function applyPredicate$2(row, predicate) {
		const value = row?.[predicate.field];
		if (predicate.op === "between" && Array.isArray(predicate.value)) return value >= predicate.value[0] && value <= predicate.value[1];
		if (predicate.op === "gte" || predicate.op === "greaterThanOrEqual") return value >= predicate.value;
		if (predicate.op === "lte" || predicate.op === "lessThanOrEqual") return value <= predicate.value;
		if (predicate.op === "gt" || predicate.op === "greaterThan") return value > predicate.value;
		if (predicate.op === "lt" || predicate.op === "lessThan") return value < predicate.value;
		if (predicate.op === "equals" || predicate.op === "eq") return value === predicate.value;
		if (predicate.op === "in" && Array.isArray(predicate.value)) return predicate.value.includes(value);
		if (predicate.op === "notIn" && Array.isArray(predicate.value)) return !predicate.value.includes(value);
		return true;
	}
	function filterRows(rows, predicates = []) {
		if (!Array.isArray(predicates) || predicates.length === 0) return rows;
		return rows.filter((row) => predicates.every((predicate) => applyPredicate$2(row, predicate)));
	}
	function numericValuesForMeasure(rows, field) {
		if (!field) return [];
		return rows.map((row) => row?.[field]).filter((value) => typeof value === "number");
	}
	function median(values) {
		const sorted = [...values].sort((a, b) => a - b);
		if (sorted.length === 0) return null;
		const middle = Math.floor(sorted.length / 2);
		if (sorted.length % 2 === 1) return sorted[middle];
		return (sorted[middle - 1] + sorted[middle]) / 2;
	}
	function computeMeasureValue(rows, measure) {
		if (!measure?.op || !measure?.as) return void 0;
		if (measure.op === "count") return rows.length;
		const values = numericValuesForMeasure(rows, measure.field);
		if (measure.op === "sum") return values.reduce((sum, value) => sum + value, 0);
		if (measure.op === "mean") return values.length > 0 ? values.reduce((sum, value) => sum + value, 0) / values.length : null;
		if (measure.op === "min") return values.length > 0 ? Math.min(...values) : null;
		if (measure.op === "max") return values.length > 0 ? Math.max(...values) : null;
		if (measure.op === "median") return values.length > 0 ? median(values) : null;
	}
	function aggregateRows$1(rows, spec = {}) {
		const groupBy = Array.isArray(spec.groupBy) ? spec.groupBy.filter(Boolean) : [];
		const measures = Array.isArray(spec.measures) ? spec.measures : [];
		if (measures.length === 0) return {
			rows,
			rowCount: rows.length
		};
		if (groupBy.length === 0) {
			const summaryRow = {};
			measures.forEach((measure) => {
				if (!measure?.as) return;
				summaryRow[measure.as] = computeMeasureValue(rows, measure);
			});
			return {
				rows: [summaryRow],
				rowCount: 1
			};
		}
		const buckets = /* @__PURE__ */ new Map();
		for (const row of rows) {
			const key = JSON.stringify(groupBy.map((field) => row?.[field]));
			if (!buckets.has(key)) {
				const seed = { __widgetva_rows: [] };
				groupBy.forEach((field) => {
					seed[field] = row?.[field] ?? null;
				});
				buckets.set(key, seed);
			}
			buckets.get(key).__widgetva_rows.push(row);
		}
		let result = Array.from(buckets.values()).map((bucket) => {
			const nextBucket = { ...bucket };
			const bucketRows = Array.isArray(bucket.__widgetva_rows) ? bucket.__widgetva_rows : [];
			measures.forEach((measure) => {
				if (!measure?.as) return;
				nextBucket[measure.as] = computeMeasureValue(bucketRows, measure);
			});
			delete nextBucket.__widgetva_rows;
			return nextBucket;
		});
		if (spec.sortBy?.field) {
			const dir = spec.sortBy.order === "ascending" ? 1 : -1;
			const field = spec.sortBy.field;
			result = result.sort((a, b) => {
				const left = a?.[field];
				const right = b?.[field];
				if (left === right) return 0;
				return left > right ? dir : -dir;
			});
		}
		if (Number.isFinite(spec.limit) && spec.limit > 0) result = result.slice(0, spec.limit);
		return {
			rows: result,
			rowCount: result.length
		};
	}
	var JsArrayDataQueryEngine = class extends DataQueryEngine {
		constructor(options = {}) {
			super();
			this.options = options;
			this.kind = options.kind || "js_array";
		}
		listSupportedQueryKinds() {
			return [
				"schema",
				"sampleRows",
				"filter",
				"aggregate",
				"groupBy",
				"summary",
				"computeCorrelation",
				"findExtremes",
				"findOutliers",
				"compareGroups"
			];
		}
		getSchema(rows) {
			const resolvedRows = this.resolveQuerySource(rows);
			const sample = Array.isArray(resolvedRows) && resolvedRows.length > 0 ? resolvedRows[0] : {};
			return { fields: Object.keys(sample || {}).map((name) => ({
				name,
				type: inferFieldType(sample[name])
			})) };
		}
		filter(rows, predicates = []) {
			return filterRows(Array.isArray(rows) ? rows : [], predicates);
		}
		sampleRows(rows, limit = 20) {
			return (Array.isArray(rows) ? rows : []).slice(0, Math.max(0, limit));
		}
		executeSql() {
			return {
				ok: false,
				error: {
					code: "SQL_QUERY_UNSUPPORTED",
					message: "SQL queries are not supported by the in-memory JS array query engine."
				}
			};
		}
		aggregate(rows, spec = {}) {
			return aggregateRows$1(Array.isArray(rows) ? rows : [], spec);
		}
		summarize(rows, options = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			const groupBy = Array.isArray(options.groupBy) ? options.groupBy.filter(Boolean) : [];
			const measures = Array.isArray(options.measures) ? options.measures : [{
				op: "count",
				as: "count"
			}];
			return this.aggregate(safeRows, {
				groupBy,
				measures,
				sortBy: options.sortBy,
				limit: options.limit
			});
		}
		computeCorrelation(rows, { xField, yField } = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			if (!xField || !yField) return {
				ok: false,
				reason: "xField and yField are required."
			};
			const pairs = safeRows.map((row) => [row?.[xField], row?.[yField]]).filter(([x, y]) => typeof x === "number" && typeof y === "number");
			if (pairs.length < 2) return {
				ok: false,
				reason: "Not enough numeric rows to compute correlation."
			};
			const n = pairs.length;
			const sumX = pairs.reduce((acc, [x]) => acc + x, 0);
			const sumY = pairs.reduce((acc, [, y]) => acc + y, 0);
			const meanX = sumX / n;
			const meanY = sumY / n;
			let num = 0;
			let denX = 0;
			let denY = 0;
			for (const [x, y] of pairs) {
				const dx = x - meanX;
				const dy = y - meanY;
				num += dx * dy;
				denX += dx * dx;
				denY += dy * dy;
			}
			const denominator = Math.sqrt(denX * denY);
			return {
				ok: denominator > 0,
				coefficient: denominator > 0 ? num / denominator : null,
				sampleSize: n,
				fields: [xField, yField]
			};
		}
		findExtremes(rows, { field, order = "descending", limit = 5 } = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			if (!field) return { rows: [] };
			return { rows: safeRows.filter((row) => typeof row?.[field] === "number").sort((a, b) => {
				const diff = (a?.[field] || 0) - (b?.[field] || 0);
				return order === "ascending" ? diff : -diff;
			}).slice(0, Math.max(0, limit)) };
		}
		findOutliers(rows, { field, xField, yField, zThreshold = 2, limit = 10 } = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			const fields = [
				field,
				xField,
				yField
			].filter(Boolean);
			if (fields.length === 0) return {
				rows: [],
				summary: { reason: "No numeric field specified." }
			};
			const scored = [];
			for (const currentField of fields) {
				const numericValues = safeRows.map((row) => row?.[currentField]).filter((value) => typeof value === "number");
				if (numericValues.length < 2) continue;
				const mean = numericValues.reduce((acc, value) => acc + value, 0) / numericValues.length;
				const variance = numericValues.reduce((acc, value) => acc + (value - mean) ** 2, 0) / numericValues.length;
				const std = Math.sqrt(variance);
				if (!std) continue;
				for (const row of safeRows) {
					const value = row?.[currentField];
					if (typeof value !== "number") continue;
					const zScore = Math.abs((value - mean) / std);
					if (zScore >= zThreshold) scored.push({
						row,
						field: currentField,
						value,
						zScore
					});
				}
			}
			const deduped = [];
			const seen = /* @__PURE__ */ new Set();
			for (const item of scored.sort((a, b) => b.zScore - a.zScore)) {
				const key = JSON.stringify(item.row);
				if (seen.has(key)) continue;
				seen.add(key);
				deduped.push(item);
				if (deduped.length >= Math.max(0, limit)) break;
			}
			return {
				rows: deduped.map((item) => ({
					...item.row,
					__widgetva_outlier_field: item.field,
					__widgetva_outlier_score: item.zScore
				})),
				summary: {
					inspectedFields: fields,
					zThreshold,
					count: deduped.length
				}
			};
		}
		compareGroups(rows, { groupField, valueField, groups = [] } = {}) {
			const safeRows = Array.isArray(rows) ? rows : [];
			if (!groupField || !valueField || groups.length < 2) return {
				ok: false,
				reason: "groupField, valueField, and at least two groups are required."
			};
			return {
				ok: true,
				groups: groups.map((group) => {
					const numericValues = safeRows.filter((row) => row?.[groupField] === group).map((row) => row?.[valueField]).filter((value) => typeof value === "number");
					const count = numericValues.length;
					const sum = numericValues.reduce((acc, value) => acc + value, 0);
					return {
						group,
						count,
						mean: count > 0 ? sum / count : null,
						min: count > 0 ? Math.min(...numericValues) : null,
						max: count > 0 ? Math.max(...numericValues) : null
					};
				})
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/DuckDbDataQueryEngine.js
	var DuckDbDataQueryEngine = class extends DataQueryEngine {
		constructor(options = {}) {
			super();
			this.options = options;
			this.kind = options.kind || "duckdb";
			this.execute = options.request || options.execute || null;
			this.fallbackEngine = new JsArrayDataQueryEngine(options);
		}
		unsupported() {
			return {
				ok: false,
				error: {
					code: "DUCKDB_ENGINE_UNAVAILABLE",
					message: "DuckDB-backed data queries are not configured in this frontend runtime build."
				}
			};
		}
		listSupportedQueryKinds() {
			return [
				"schema",
				"sampleRows",
				"filter",
				"aggregate",
				"groupBy",
				"sql",
				"summary",
				"computeCorrelation",
				"findExtremes",
				"findOutliers",
				"compareGroups"
			];
		}
		getSchema() {
			return this.fallbackEngine.getSchema(...arguments);
		}
		filter() {
			return this.fallbackEngine.filter(...arguments);
		}
		sampleRows() {
			return this.fallbackEngine.sampleRows(...arguments);
		}
		executeSql() {
			if (typeof this.execute === "function") return this.execute({
				kind: "sql",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.unsupported();
		}
		aggregate() {
			if (typeof this.execute === "function") return this.execute({
				kind: "aggregate",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.aggregate(...arguments);
		}
		summarize() {
			if (typeof this.execute === "function") return this.execute({
				kind: "summary",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.summarize(...arguments);
		}
		computeCorrelation() {
			if (typeof this.execute === "function") return this.execute({
				kind: "computeCorrelation",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.computeCorrelation(...arguments);
		}
		findExtremes() {
			if (typeof this.execute === "function") return this.execute({
				kind: "findExtremes",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.findExtremes(...arguments);
		}
		findOutliers() {
			if (typeof this.execute === "function") return this.execute({
				kind: "findOutliers",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.findOutliers(...arguments);
		}
		compareGroups() {
			if (typeof this.execute === "function") return this.execute({
				kind: "compareGroups",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.compareGroups(...arguments);
		}
		describeEngine() {
			const summary = super.describeEngine();
			return {
				...summary,
				capabilities: {
					...summary.capabilities,
					localExecution: true,
					remoteExecution: false,
					sqlSupport: typeof this.execute === "function",
					fallbackEngine: true
				}
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/RemoteDataQueryEngine.js
	var RemoteDataQueryEngine = class extends DataQueryEngine {
		constructor(options = {}) {
			super();
			const { request, execute } = options;
			this.request = request || execute || null;
			this.kind = "remote";
			this.fallbackEngine = new JsArrayDataQueryEngine(options);
		}
		unsupported() {
			return {
				ok: false,
				error: {
					code: "REMOTE_ENGINE_UNAVAILABLE",
					message: "Remote data query execution is not configured in this frontend runtime build."
				}
			};
		}
		listSupportedQueryKinds() {
			return [
				"schema",
				"sampleRows",
				"filter",
				"aggregate",
				"groupBy",
				"sql",
				"summary",
				"computeCorrelation",
				"findExtremes",
				"findOutliers",
				"compareGroups"
			];
		}
		getSchema() {
			return this.fallbackEngine.getSchema(...arguments);
		}
		filter() {
			return this.fallbackEngine.filter(...arguments);
		}
		sampleRows() {
			return this.fallbackEngine.sampleRows(...arguments);
		}
		executeSql() {
			if (typeof this.request === "function") return this.request({
				kind: "sql",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.unsupported();
		}
		aggregate() {
			if (typeof this.request === "function") return this.request({
				kind: "aggregate",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.aggregate(...arguments);
		}
		summarize() {
			if (typeof this.request === "function") return this.request({
				kind: "summary",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.summarize(...arguments);
		}
		computeCorrelation() {
			if (typeof this.request === "function") return this.request({
				kind: "computeCorrelation",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.computeCorrelation(...arguments);
		}
		findExtremes() {
			if (typeof this.request === "function") return this.request({
				kind: "findExtremes",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.findExtremes(...arguments);
		}
		findOutliers() {
			if (typeof this.request === "function") return this.request({
				kind: "findOutliers",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.findOutliers(...arguments);
		}
		compareGroups() {
			if (typeof this.request === "function") return this.request({
				kind: "compareGroups",
				rows: arguments[0],
				spec: arguments[1] || {}
			});
			return this.fallbackEngine.compareGroups(...arguments);
		}
		describeEngine() {
			const summary = super.describeEngine();
			return {
				...summary,
				capabilities: {
					...summary.capabilities,
					localExecution: typeof this.request !== "function",
					remoteExecution: typeof this.request === "function",
					sqlSupport: typeof this.request === "function",
					fallbackEngine: true
				}
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/data/index.js
	function createDataQueryEngine({ kind = "js_array", ...options } = {}) {
		if (kind === "duckdb") return new DuckDbDataQueryEngine({
			kind,
			...options
		});
		if (kind === "remote") return new RemoteDataQueryEngine({
			kind,
			...options
		});
		return new JsArrayDataQueryEngine({
			kind,
			...options
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/schemas/widget-links.schema.js
	var WIDGET_LINK_KINDS = [
		"filter",
		"highlight",
		"syncDomain",
		"sharesSelection",
		"drillDown",
		"reencode",
		"aggregate",
		"structure",
		"contains",
		"usesData",
		"filters",
		"highlights",
		"syncsDomain",
		"comparesWith",
		"derivesFrom"
	];
	var WIDGET_LINK_ACTIVATION_POLICIES = ["automatic", "manual"];
	var WIDGET_LINK_EFFECT_CONSTRAINTS = ["highlightOnly", "focusOnly"];
	var WIDGET_LINK_ADVANCED_RESPONSE_KINDS = [
		"drillDown",
		"reencode",
		"aggregate",
		"expand",
		"collapse"
	];
	//#endregion
	//#region ../../widgetva-kit/src/contracts/widget-links-contracts.js
	function cloneValue$6(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var WIDGET_LINK_KIND_ALIASES = {
		filters: "filter",
		highlights: "highlight",
		syncsDomain: "syncDomain"
	};
	function readCompatibilityActivationPolicy(link) {
		const explicitActivationPolicy = typeof link?.activationPolicy === "string" ? link.activationPolicy : null;
		const explicitPropagationPolicy = typeof link?.propagationPolicy === "string" ? link.propagationPolicy : null;
		const explicitAutomatic = typeof link?.automatic === "boolean" ? link.automatic : null;
		if (WIDGET_LINK_ACTIVATION_POLICIES.includes(explicitActivationPolicy)) return explicitActivationPolicy;
		if (explicitPropagationPolicy === "manual") return "manual";
		if (explicitAutomatic === false) return "manual";
		return "automatic";
	}
	function readCompatibilityEffectConstraint(link) {
		const explicitEffectConstraint = typeof link?.effectConstraint === "string" ? link.effectConstraint : null;
		const explicitPropagationPolicy = typeof link?.propagationPolicy === "string" ? link.propagationPolicy : null;
		if (WIDGET_LINK_EFFECT_CONSTRAINTS.includes(explicitEffectConstraint)) return explicitEffectConstraint;
		if (explicitPropagationPolicy === "highlightOnly" || explicitPropagationPolicy === "focusOnly") return explicitPropagationPolicy;
		return null;
	}
	function makeWidgetLink(link) {
		const { propagationPolicy: _propagationPolicy, automatic: _automatic, trigger: _trigger, ...publicInput } = link || {};
		const normalizedKind = WIDGET_LINK_KIND_ALIASES[link?.kind] || link?.kind || null;
		const normalizedActivationPolicy = readCompatibilityActivationPolicy(link);
		const normalizedEffectConstraint = readCompatibilityEffectConstraint(link);
		const explicitEffect = typeof link?.effect === "string" ? link.effect : null;
		const inferredEffect = normalizedKind === "filter" ? "applyFilter" : normalizedKind === "highlight" ? "applyHighlight" : normalizedKind === "syncDomain" ? "syncDomain" : normalizedKind === "sharesSelection" ? "shareSelection" : normalizedKind === "drillDown" || normalizedKind === "reencode" ? "transformView" : normalizedKind === "aggregate" ? "transformDataView" : normalizedKind === "structure" ? "transformStructure" : null;
		return {
			effect: inferredEffect,
			fieldMapping: [],
			activationPolicy: "automatic",
			effectConstraint: null,
			responseSpec: null,
			...publicInput,
			kind: normalizedKind,
			effect: explicitEffect || inferredEffect,
			activationPolicy: normalizedActivationPolicy,
			effectConstraint: normalizedEffectConstraint,
			responseSpec: link?.responseSpec && typeof link.responseSpec === "object" && !Array.isArray(link.responseSpec) ? cloneValue$6(link.responseSpec) : null
		};
	}
	function stripWidgetLinkCompatibilityFields(link) {
		if (!link || typeof link !== "object" || Array.isArray(link)) return link;
		const { propagationPolicy: _propagationPolicy, trigger: _trigger, automatic: _automatic, ...rest } = link;
		return rest;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/planning/workspacePlanningShapes.js
	function makeWorkspacePlanningDataField(field = {}) {
		return {
			name: "",
			type: "",
			...field
		};
	}
	function makeWorkspacePlanningDatasetSchema(schema = {}) {
		return {
			fields: Array.isArray(schema?.fields) ? schema.fields.map((field) => makeWorkspacePlanningDataField(field)) : [],
			...schema,
			fields: Array.isArray(schema?.fields) ? schema.fields.map((field) => makeWorkspacePlanningDataField(field)) : []
		};
	}
	function makeWorkspacePlanningTask(task = {}) {
		return {
			taskId: null,
			userQuery: null,
			taskMode: null,
			taskFamily: null,
			answerType: null,
			expectedAnswerType: null,
			interactionHorizon: null,
			coordinationScope: null,
			evidenceType: null,
			targetWidgetRefs: [],
			...task
		};
	}
	function makeWorkspaceWidgetPlanSource(source = {}) {
		return {
			kind: "baseSpec",
			spec: null,
			title: null,
			...source
		};
	}
	function makeWorkspaceWidgetPlanMetric(metric = {}) {
		return {
			field: "",
			op: "",
			as: "",
			...metric
		};
	}
	function makeWorkspaceWidgetPlanSort(sort = {}) {
		return {
			field: "",
			order: "descending",
			...sort
		};
	}
	function makeWorkspaceWidgetPlanTransform(transform = {}) {
		return {
			kind: "",
			groupBy: [],
			metrics: [],
			sortBy: null,
			...transform,
			metrics: Array.isArray(transform?.metrics) ? transform.metrics.map((metric) => makeWorkspaceWidgetPlanMetric(metric)) : [],
			sortBy: transform?.sortBy ? makeWorkspaceWidgetPlanSort(transform.sortBy) : null
		};
	}
	function makeWorkspaceWidgetDataBinding(binding = {}) {
		return {
			sourceWidgetId: null,
			transforms: [],
			...binding,
			transforms: Array.isArray(binding?.transforms) ? binding.transforms.map((transform) => makeWorkspaceWidgetPlanTransform(transform)) : []
		};
	}
	function makeWorkspaceLinkFieldMapping(mapping = {}) {
		return {
			sourceField: "",
			targetField: "",
			...mapping
		};
	}
	function makeWorkspaceLinkResponseSpec(responseSpec = {}) {
		return { ...responseSpec };
	}
	function makeWorkspaceWidgetPlan(plan = {}) {
		return {
			widgetId: "",
			role: "",
			kind: null,
			title: "",
			description: "",
			source: makeWorkspaceWidgetPlanSource(),
			dataBinding: null,
			analyticRoles: [],
			...plan,
			source: makeWorkspaceWidgetPlanSource(plan?.source),
			dataBinding: plan?.dataBinding ? makeWorkspaceWidgetDataBinding(plan.dataBinding) : null,
			analyticRoles: Array.isArray(plan?.analyticRoles) ? [...plan.analyticRoles] : []
		};
	}
	function makeWorkspaceLinkPlan(plan = {}) {
		const compatibilityInput = plan || {};
		const publicPlan = stripWidgetLinkCompatibilityFields(compatibilityInput);
		const normalizedLink = makeWidgetLink({
			...compatibilityInput,
			fieldMapping: Array.isArray(plan?.fieldMapping) ? plan.fieldMapping.map((mapping) => makeWorkspaceLinkFieldMapping(mapping)) : []
		});
		return {
			linkId: "",
			sourceWidgetId: "",
			targetWidgetId: "",
			kind: null,
			effect: null,
			activationPolicy: "automatic",
			effectConstraint: null,
			responseSpec: null,
			description: "",
			fieldMapping: [],
			...publicPlan,
			kind: normalizedLink.kind,
			effect: normalizedLink.effect,
			activationPolicy: normalizedLink.activationPolicy,
			effectConstraint: normalizedLink.effectConstraint,
			responseSpec: normalizedLink.responseSpec ? makeWorkspaceLinkResponseSpec(normalizedLink.responseSpec) : null,
			description: normalizedLink.description,
			fieldMapping: normalizedLink.fieldMapping
		};
	}
	function makeWorkspacePlanningRequest(request = {}) {
		return {
			task: null,
			datasetSchema: null,
			userIntent: null,
			runMode: "goal_oriented",
			complexityBudget: "standard",
			preferredTopology: null,
			...request,
			task: request?.task ? makeWorkspacePlanningTask(request.task) : null,
			datasetSchema: request?.datasetSchema ? makeWorkspacePlanningDatasetSchema(request.datasetSchema) : null
		};
	}
	function makeWorkspacePlanningResult(result = {}) {
		return {
			topology: "T1",
			widgets: [],
			links: [],
			rationale: [],
			source: "planner",
			planningMode: "topology_driven",
			primaryWidgetId: null,
			title: "Workspace",
			...result,
			widgets: Array.isArray(result?.widgets) ? result.widgets.map((widget) => makeWorkspaceWidgetPlan(widget)) : [],
			links: Array.isArray(result?.links) ? result.links.map((link) => makeWorkspaceLinkPlan(link)) : []
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/schemas/interaction-trace.schema.js
	var INTERACTION_TRACE_QUERY_EVENT_KINDS = ["perceptionQuery", "dataQuery"];
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/shapes/interactionTraceShapes.js
	function getInteractionTraceEventFamily(recordOrEventKind) {
		const eventKind = typeof recordOrEventKind === "string" ? recordOrEventKind : recordOrEventKind?.eventKind || null;
		if (INTERACTION_TRACE_QUERY_EVENT_KINDS.includes(eventKind)) return "query";
		if (eventKind === "systemTransition") return "systemTransition";
		return "action";
	}
	function getInteractionTraceQuerySurface(recordOrEventKind) {
		const eventKind = typeof recordOrEventKind === "string" ? recordOrEventKind : recordOrEventKind?.eventKind || null;
		if (eventKind === "perceptionQuery") return "perception";
		if (eventKind === "dataQuery") return "data";
		return null;
	}
	function makeInteractionTraceRecord(record = {}) {
		const hasAction = !!record?.action;
		const hasQuery = !!record?.query;
		const eventKind = record?.eventKind || (hasAction ? "action" : hasQuery ? "perceptionQuery" : "systemTransition");
		const eventFamily = record?.eventFamily || getInteractionTraceEventFamily(eventKind);
		const querySurface = record?.querySurface || getInteractionTraceQuerySurface(eventKind);
		return {
			timestamp: (/* @__PURE__ */ new Date()).toISOString(),
			actor: RUNTIME_ACTORS[0],
			eventKind,
			eventFamily,
			querySurface,
			affectedRefs: [],
			parentStateId: null,
			branchId: null,
			primitive: null,
			statePatch: {},
			notes: {},
			...record
		};
	}
	function getInteractionTraceQueryName(query) {
		if (typeof query?.name === "string" && query.name.length > 0) return query.name;
		if (typeof query?.query?.kind === "string" && query.query.kind.length > 0) return `data.${query.query.kind}`;
		return null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/shapes/historyShapes.js
	function makeStateSnapshotMeta(meta) {
		return {
			stateId: "",
			createdAt: null,
			baseStateId: null,
			branchId: null,
			transitionType: "continue",
			branchLabel: null,
			actor: "system",
			changedRefs: [],
			removedRefs: [],
			...meta
		};
	}
	function makeBranchSummary(summary) {
		return {
			branchId: "",
			label: "",
			originStateId: null,
			parentBranchId: null,
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			...summary
		};
	}
	function makeTraceGraphNode(node) {
		return {
			id: "",
			stateId: "",
			parentStateId: null,
			branchId: null,
			branchLabel: null,
			timestamp: null,
			actor: "system",
			eventFamily: "systemTransition",
			querySurface: null,
			actionName: null,
			queryName: null,
			responseId: null,
			responseActor: null,
			responsePreview: null,
			label: "",
			transitionType: "continue",
			current: false,
			...node
		};
	}
	function makeTraceGraphEdge(edge) {
		return {
			id: "",
			from_id: "",
			to_id: "",
			edge_type: "continue",
			branchId: null,
			label: "continue",
			timestamp: null,
			...edge
		};
	}
	function makeTraceGraph(graph) {
		return {
			current_state_id: null,
			current_branch_id: null,
			sinceStateId: null,
			actors: [],
			...graph,
			branches: Array.isArray(graph?.branches) ? graph.branches.map((branch) => makeBranchSummary(branch)) : [],
			nodes: Array.isArray(graph?.nodes) ? graph.nodes.map((node) => makeTraceGraphNode(node)) : [],
			edges: Array.isArray(graph?.edges) ? graph.edges.map((edge) => makeTraceGraphEdge(edge)) : []
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/coordination/deriveWorkspaceTopology.js
	var TOPOLOGY_LABELS = {
		T1: "Single View",
		T2: "Coordinated Pair",
		T3: "Overview + Detail",
		T4: "Triple Dashboard",
		T5: "Drill-down Chain",
		T6: "Global Control + Multiple Targets"
	};
	var TOPOLOGY_LINK_KINDS = new Set([
		"filter",
		"filters",
		"highlight",
		"highlights",
		"syncDomain",
		"syncsDomain",
		"sharesSelection",
		"comparesWith"
	]);
	function unique$1(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter(Boolean))];
	}
	function resolveWidgetId(ref) {
		return parseRef(ref)?.widgetId || null;
	}
	function buildDegreeMaps(widgetIds, links) {
		const outDegree = new Map(widgetIds.map((widgetId) => [widgetId, 0]));
		const inDegree = new Map(widgetIds.map((widgetId) => [widgetId, 0]));
		const adjacency = new Map(widgetIds.map((widgetId) => [widgetId, /* @__PURE__ */ new Set()]));
		for (const link of links) {
			const sourceWidgetId = link?.sourceWidgetId || resolveWidgetId(link?.from);
			const targetWidgetId = link?.targetWidgetId || resolveWidgetId(link?.to);
			if (!sourceWidgetId || !targetWidgetId || sourceWidgetId === targetWidgetId) continue;
			if (!adjacency.has(sourceWidgetId)) adjacency.set(sourceWidgetId, /* @__PURE__ */ new Set());
			if (!outDegree.has(sourceWidgetId)) outDegree.set(sourceWidgetId, 0);
			if (!inDegree.has(targetWidgetId)) inDegree.set(targetWidgetId, 0);
			const targets = adjacency.get(sourceWidgetId);
			if (targets.has(targetWidgetId)) continue;
			targets.add(targetWidgetId);
			outDegree.set(sourceWidgetId, (outDegree.get(sourceWidgetId) || 0) + 1);
			inDegree.set(targetWidgetId, (inDegree.get(targetWidgetId) || 0) + 1);
		}
		return {
			outDegree,
			inDegree,
			adjacency
		};
	}
	function isLinearChain(widgetIds, edgeCount, outDegree, inDegree) {
		if (widgetIds.length < 3) return false;
		if (edgeCount !== widgetIds.length - 1) return false;
		const starts = widgetIds.filter((widgetId) => (inDegree.get(widgetId) || 0) === 0 && (outDegree.get(widgetId) || 0) === 1);
		const ends = widgetIds.filter((widgetId) => (inDegree.get(widgetId) || 0) === 1 && (outDegree.get(widgetId) || 0) === 0);
		const middles = widgetIds.filter((widgetId) => (inDegree.get(widgetId) || 0) === 1 && (outDegree.get(widgetId) || 0) === 1);
		return starts.length === 1 && ends.length === 1 && middles.length === widgetIds.length - 2;
	}
	function hasDetailRole(widgets) {
		return (Array.isArray(widgets) ? widgets : []).some((widget) => widget?.role === "detail");
	}
	function hasContextRole(widgets) {
		return (Array.isArray(widgets) ? widgets : []).some((widget) => widget?.role === "context");
	}
	function makeWorkspaceTopologySummary(summary = {}) {
		return {
			topology: summary?.topology || "",
			topologyLabel: summary?.topologyLabel || "",
			widgetCount: summary?.widgetCount || 0,
			edgeCount: summary?.edgeCount || 0,
			linkDensity: summary?.linkDensity || 0,
			sourceWidgetCount: summary?.sourceWidgetCount || 0,
			targetWidgetCount: summary?.targetWidgetCount || 0,
			maxOutDegree: summary?.maxOutDegree || 0,
			maxInDegree: summary?.maxInDegree || 0,
			rationale: Array.isArray(summary?.rationale) ? [...summary.rationale] : []
		};
	}
	function isTopologyLink(link) {
		const normalizedLink = makeWidgetLink(link);
		return TOPOLOGY_LINK_KINDS.has(normalizedLink?.kind);
	}
	function deriveWorkspaceTopology({ widgets = [], links = [] } = {}) {
		const widgetIds = unique$1((Array.isArray(widgets) ? widgets : []).map((widget) => widget?.widgetId || resolveWidgetId(widget?.ref)).filter(Boolean));
		const { outDegree, inDegree } = buildDegreeMaps(widgetIds, (Array.isArray(links) ? links : []).map((link) => makeWidgetLink(link)).filter(isTopologyLink));
		const edgeCount = Array.from(outDegree.values()).reduce((sum, value) => sum + value, 0);
		const maxOutDegree = Math.max(0, ...Array.from(outDegree.values()));
		const maxInDegree = Math.max(0, ...Array.from(inDegree.values()));
		const sourceWidgetCount = Array.from(outDegree.values()).filter((value) => value > 0).length;
		const targetWidgetCount = Array.from(inDegree.values()).filter((value) => value > 0).length;
		const possibleDirectedEdges = widgetIds.length > 1 ? widgetIds.length * (widgetIds.length - 1) : 1;
		const linkDensity = Number((edgeCount / possibleDirectedEdges).toFixed(3));
		let topology = "T1";
		const rationale = [];
		if (widgetIds.length <= 1) {
			topology = "T1";
			rationale.push("A single widget is active in the current workspace.");
		} else if (isLinearChain(widgetIds, edgeCount, outDegree, inDegree)) {
			topology = "T5";
			rationale.push("The link graph forms a linear multi-step chain.");
		} else if (widgetIds.length >= 3 && hasDetailRole(widgets) && hasContextRole(widgets) && sourceWidgetCount > 1) {
			topology = "T4";
			rationale.push("The workspace includes coordinated overview, context, and detail roles across multiple propagation stages.");
		} else if (maxOutDegree >= 2) {
			topology = "T6";
			rationale.push("One widget currently propagates to multiple targets.");
		} else if (hasDetailRole(widgets)) {
			topology = "T3";
			rationale.push("A detail-role widget is present in the current workspace plan.");
		} else if (widgetIds.length === 2) {
			topology = "T2";
			rationale.push("Two widgets are active, forming a coordinated pair.");
		} else {
			topology = "T4";
			rationale.push("Three or more widgets are active without a single fan-out controller.");
		}
		return makeWorkspaceTopologySummary({
			topology,
			topologyLabel: TOPOLOGY_LABELS[topology] || topology,
			widgetCount: widgetIds.length,
			edgeCount,
			linkDensity,
			sourceWidgetCount,
			targetWidgetCount,
			maxOutDegree,
			maxInDegree,
			rationale
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/StateManager.js
	function clone$33(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeStateManagerCapabilities(capabilities = {}) {
		return {
			stateIdGeneration: true,
			deltaCreation: true,
			statePatchCreation: true,
			refScopedReads: true,
			...capabilities
		};
	}
	function makeStateManagerCounters(counters = {}) {
		return {
			generatedStateCount: 0,
			lastGeneratedStateId: null,
			...counters
		};
	}
	function makeStateManagerSummary(summary = {}) {
		return {
			capabilities: makeStateManagerCapabilities(summary?.capabilities),
			reservedRefs: [],
			counters: makeStateManagerCounters(summary?.counters),
			...summary
		};
	}
	function stableSerialize$1(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	var SHARED_STATE_REF = "shared";
	var TASK_CONTEXT_REF = "taskContext";
	var REPLAY_CONTEXT_REF = "replayContext";
	var StateManager = class {
		constructor() {
			this.generatedStateCount = 0;
			this.lastGeneratedStateId = null;
		}
		createStateMeta({ workspaceId = "main", previousState = null, nextWidgets = {}, nextShared = {}, nextTaskContext = void 0, nextReplayContext = void 0, branchId = "main" } = {}) {
			const nextStateSignature = stableSerialize$1({
				widgets: nextWidgets,
				shared: nextShared,
				taskContext: nextTaskContext,
				replayContext: nextReplayContext,
				branchId
			});
			const previousStateSignature = previousState ? stableSerialize$1({
				widgets: previousState.widgets || {},
				shared: previousState.shared || {},
				taskContext: previousState.taskContext,
				replayContext: previousState.replayContext,
				branchId
			}) : null;
			const createdAt = (/* @__PURE__ */ new Date()).toISOString();
			if (previousState?.stateId && previousStateSignature === nextStateSignature) return {
				stateId: previousState.stateId,
				createdAt: previousState.createdAt || createdAt,
				reused: true
			};
			this.generatedStateCount += 1;
			this.lastGeneratedStateId = `${workspaceId}:s${this.generatedStateCount}`;
			return {
				stateId: this.lastGeneratedStateId,
				createdAt,
				reused: false
			};
		}
		createDelta({ previousState, nextState, previousReplayContext = void 0, nextReplayContext = void 0 }) {
			const previousWidgets = previousState?.widgets || {};
			const nextWidgets = nextState?.widgets || {};
			const changedRefs = [];
			const removedRefs = [];
			const allRefs = new Set([...Object.keys(previousWidgets), ...Object.keys(nextWidgets)]);
			for (const ref of allRefs) {
				if (!(ref in nextWidgets)) {
					removedRefs.push(ref);
					continue;
				}
				if (!(ref in previousWidgets)) {
					changedRefs.push(ref);
					continue;
				}
				if (stableSerialize$1(previousWidgets[ref]) !== stableSerialize$1(nextWidgets[ref])) changedRefs.push(ref);
			}
			if (stableSerialize$1(previousState?.shared || null) !== stableSerialize$1(nextState?.shared || null)) changedRefs.push(SHARED_STATE_REF);
			const previousTaskContextExists = previousState?.taskContext !== void 0;
			const nextTaskContextExists = nextState?.taskContext !== void 0;
			if (previousTaskContextExists || nextTaskContextExists) {
				if (!nextTaskContextExists) removedRefs.push(TASK_CONTEXT_REF);
				else if (!previousTaskContextExists || stableSerialize$1(previousState?.taskContext) !== stableSerialize$1(nextState?.taskContext)) changedRefs.push(TASK_CONTEXT_REF);
			}
			const previousReplayContextExists = previousReplayContext !== void 0;
			const nextReplayContextExists = nextReplayContext !== void 0;
			if (previousReplayContextExists || nextReplayContextExists) {
				if (!nextReplayContextExists) removedRefs.push(REPLAY_CONTEXT_REF);
				else if (!previousReplayContextExists || stableSerialize$1(previousReplayContext) !== stableSerialize$1(nextReplayContext)) changedRefs.push(REPLAY_CONTEXT_REF);
			}
			return {
				baseStateId: previousState?.stateId || null,
				changedRefs,
				removedRefs
			};
		}
		buildStatePatch({ state, refs, replayContext = void 0 }) {
			const widgets = state?.widgets || {};
			const patch = {};
			for (const ref of refs || []) {
				if (ref === "shared") {
					patch[ref] = clone$33(state?.shared);
					continue;
				}
				if (ref === "taskContext") {
					patch[ref] = clone$33(state?.taskContext);
					continue;
				}
				if (ref === "replayContext") {
					patch[ref] = clone$33(replayContext);
					continue;
				}
				if (widgets[ref]) patch[ref] = clone$33(widgets[ref]);
			}
			return patch;
		}
		pickRefs({ state, refs, replayContext = void 0 }) {
			if (!Array.isArray(refs) || refs.length === 0) return clone$33(state);
			const widgets = {};
			const includeShared = refs.includes(SHARED_STATE_REF);
			const includeTaskContext = refs.includes(TASK_CONTEXT_REF);
			const includeReplayContext = refs.includes(REPLAY_CONTEXT_REF);
			for (const ref of refs) if (state?.widgets?.[ref]) widgets[ref] = clone$33(state.widgets[ref]);
			const nextState = {
				...clone$33(state),
				widgets
			};
			if (!includeShared) delete nextState.shared;
			if (!includeTaskContext) delete nextState.taskContext;
			if (includeReplayContext) nextState.replayContext = clone$33(replayContext);
			else delete nextState.replayContext;
			return nextState;
		}
		describeManager() {
			return makeStateManagerSummary({
				capabilities: makeStateManagerCapabilities({
					stateIdGeneration: true,
					deltaCreation: true,
					statePatchCreation: true,
					refScopedReads: true
				}),
				reservedRefs: [
					SHARED_STATE_REF,
					TASK_CONTEXT_REF,
					REPLAY_CONTEXT_REF
				],
				counters: makeStateManagerCounters({
					generatedStateCount: this.generatedStateCount,
					lastGeneratedStateId: this.lastGeneratedStateId
				})
			});
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/workspace/store/workspaceStoreReaders.js
	var WORKSPACE_TRANSPORT_TOOL_NAMES = [
		"workspace_describe",
		"view_read",
		"action_run",
		"perception_query",
		"interaction_trace_read"
	];
	function makeWorkspaceSpecSummary(summary = {}) {
		return {
			topology: typeof summary?.topology === "string" ? summary.topology : null,
			widgetCount: Number.isInteger(summary?.widgetCount) ? summary.widgetCount : 0,
			linkCount: Number.isInteger(summary?.linkCount) ? summary.linkCount : 0
		};
	}
	function makeWidgetDescription(description = {}) {
		return {
			title: "",
			description: "",
			analyticRoles: [],
			recognizedKinds: [],
			widgetId: null,
			role: "primary",
			sourceKind: null,
			supportsSpecMutation: false,
			primaryDataRef: null,
			actionNames: [],
			perceptionQueryNames: [],
			usageNotes: [],
			humanInteraction: {
				mode: "none",
				actionName: null,
				supportsDirectManipulation: false
			},
			...description
		};
	}
	function makeWorkspaceTransportHints(transportHints = {}) {
		const { optionalTools: _optionalTools, ...safeTransportHints } = transportHints || {};
		return {
			recommendedTools: [],
			note: "",
			...safeTransportHints
		};
	}
	function makeWorkspaceDescriptionPlanning(planning = {}) {
		return {
			supportedTopologies: [],
			supportedRunModes: [],
			supportedComplexityBudgets: [],
			supportedPlanSources: [],
			supportedPlanningModes: [],
			requestedWorkspaceSpec: null,
			planningRequest: null,
			workspaceSpecStatus: null,
			workspaceSpecIssues: [],
			materializedFromSpec: false,
			materializedFromPlanner: false,
			planner: null,
			...planning,
			...planning?.requestedWorkspaceSpec ? { requestedWorkspaceSpec: makeWorkspaceSpecSummary(planning.requestedWorkspaceSpec) } : {},
			...planning?.planningRequest ? { planningRequest: makeWorkspacePlanningRequest(planning.planningRequest) } : {},
			...planning?.planner ? { planner: makeWorkspacePlanningResult(planning.planner) } : {}
		};
	}
	function makeWorkspaceDescription(description = {}) {
		return {
			appId: "widgetva-app",
			workspaceId: "main",
			generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			workspaceCapabilities: [],
			transportHints: null,
			runtimeTopology: null,
			taskContext: null,
			widgetAdapters: [],
			widgets: [],
			dataHandles: [],
			links: [],
			actions: [],
			perceptionQueries: [],
			planning: null,
			...description,
			...description?.transportHints ? { transportHints: makeWorkspaceTransportHints(description.transportHints) } : {},
			...description?.planning ? { planning: makeWorkspaceDescriptionPlanning(description.planning) } : {}
		};
	}
	function clone$32(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readWidgetRefFromStateRef(stateRef) {
		if (typeof stateRef !== "string" || stateRef.length === 0) return null;
		const index = stateRef.lastIndexOf("/widget/");
		if (index < 0) return null;
		const prefix = stateRef.slice(0, index + 8);
		const widgetId = stateRef.slice(index + 8).split("/")[0];
		return widgetId ? `${prefix}${widgetId}` : null;
	}
	function readStateByRef(state = {}, stateRef = null) {
		if (typeof stateRef !== "string" || stateRef.length === 0) return null;
		const widgetRef = readWidgetRefFromStateRef(stateRef);
		if (!widgetRef) return state?.widgets?.[stateRef] || null;
		const widgetState = state?.widgets?.[widgetRef] || null;
		if (!widgetState) return null;
		if (stateRef === widgetRef) return widgetState;
		const [kind, key] = stateRef.slice(widgetRef.length).replace(/^\/+/, "").split("/");
		if (kind === "selection") return widgetState.selections?.[stateRef] || widgetState.selections?.[key] || null;
		if (kind === "transform") return (Array.isArray(widgetState.transforms) ? widgetState.transforms : []).find((transform) => transform?.ref === stateRef || transform?.id === key || transform?.params?.id === key) || null;
		if (kind === "view") {
			if (!key) return widgetState.view || null;
			if (key === "zoom") return {
				...widgetState.view?.zoom || {},
				...widgetState.view?.xDomain ? { xDomain: widgetState.view.xDomain } : {},
				...widgetState.view?.yDomain ? { yDomain: widgetState.view.yDomain } : {}
			};
			return widgetState.view?.[key] ?? null;
		}
		if (kind === "encoding" || kind === "encodings") {
			if (!key) return widgetState.encodings || null;
			return widgetState.encodings?.[key] ?? null;
		}
		return null;
	}
	function normalizeActorFilter$1(actors) {
		return Array.isArray(actors) ? [...new Set(actors.filter((actor) => typeof actor === "string" && actor.length > 0))] : [];
	}
	function getTraceRecordPriority$1(record) {
		const eventKind = record?.eventKind || null;
		if (eventKind === "action") return 3;
		if (eventKind === "perceptionQuery" || eventKind === "dataQuery") return 2;
		if (eventKind === "systemTransition") return 1;
		return 0;
	}
	function pickRepresentativeTraceRecord$1(currentRecord, nextRecord) {
		if (!currentRecord) return nextRecord || null;
		if (!nextRecord) return currentRecord;
		return getTraceRecordPriority$1(nextRecord) >= getTraceRecordPriority$1(currentRecord) ? nextRecord : currentRecord;
	}
	function buildResponsePreview$1(content, maxLength = 160) {
		const text = String(content || "").trim();
		if (!text) return null;
		if (!Number.isFinite(maxLength) || maxLength <= 0 || text.length <= maxLength) return text;
		return `${text.slice(0, maxLength - 3)}...`;
	}
	function stableSerialize(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	function actionDescriptorKey(descriptor) {
		return [descriptor?.name || "", descriptor?.targetRef || ""].join("::");
	}
	function mergeActionDescriptorLists(primaryActions = [], supplementalActions = []) {
		const merged = [];
		const seen = /* @__PURE__ */ new Set();
		for (const descriptor of [...primaryActions, ...supplementalActions]) {
			if (!descriptor?.name) continue;
			const key = actionDescriptorKey(descriptor);
			if (seen.has(key)) continue;
			seen.add(key);
			merged.push(descriptor);
		}
		return merged;
	}
	function createDeltaFallback({ previousState, nextState, previousReplayContext = void 0, nextReplayContext = void 0 }) {
		const previousWidgets = previousState?.widgets || {};
		const nextWidgets = nextState?.widgets || {};
		const changedRefs = [];
		const removedRefs = [];
		const allRefs = new Set([...Object.keys(previousWidgets), ...Object.keys(nextWidgets)]);
		for (const ref of allRefs) {
			if (!(ref in nextWidgets)) {
				removedRefs.push(ref);
				continue;
			}
			if (!(ref in previousWidgets)) {
				changedRefs.push(ref);
				continue;
			}
			if (stableSerialize(previousWidgets[ref]) !== stableSerialize(nextWidgets[ref])) changedRefs.push(ref);
		}
		if (stableSerialize(previousState?.shared || null) !== stableSerialize(nextState?.shared || null)) changedRefs.push(SHARED_STATE_REF);
		const previousTaskContextExists = previousState?.taskContext !== void 0;
		const nextTaskContextExists = nextState?.taskContext !== void 0;
		if (previousTaskContextExists || nextTaskContextExists) {
			if (!nextTaskContextExists) removedRefs.push(TASK_CONTEXT_REF);
			else if (!previousTaskContextExists || stableSerialize(previousState?.taskContext) !== stableSerialize(nextState?.taskContext)) changedRefs.push(TASK_CONTEXT_REF);
		}
		const previousReplayContextExists = previousReplayContext !== void 0;
		const nextReplayContextExists = nextReplayContext !== void 0;
		if (previousReplayContextExists || nextReplayContextExists) {
			if (!nextReplayContextExists) removedRefs.push(REPLAY_CONTEXT_REF);
			else if (!previousReplayContextExists || stableSerialize(previousReplayContext) !== stableSerialize(nextReplayContext)) changedRefs.push(REPLAY_CONTEXT_REF);
		}
		return {
			baseStateId: previousState?.stateId || null,
			changedRefs,
			removedRefs
		};
	}
	function pickRefsFromStateFallback({ state, refs, replayContext = void 0 }) {
		if (!Array.isArray(refs) || refs.length === 0) return clone$32(state);
		const widgets = {};
		const includeShared = refs.includes(SHARED_STATE_REF);
		const includeTaskContext = refs.includes(TASK_CONTEXT_REF);
		const includeReplayContext = refs.includes(REPLAY_CONTEXT_REF);
		for (const ref of refs) if (state?.widgets?.[ref]) widgets[ref] = clone$32(state.widgets[ref]);
		const nextState = {
			...clone$32(state),
			widgets
		};
		if (!includeShared) delete nextState.shared;
		if (!includeTaskContext) delete nextState.taskContext;
		if (includeReplayContext) nextState.replayContext = clone$32(replayContext);
		else delete nextState.replayContext;
		return nextState;
	}
	function deriveWorkspaceCapabilities({ widgets, links, store }) {
		const capabilities = /* @__PURE__ */ new Set();
		if (Array.isArray(widgets) && widgets.length > 0) capabilities.add("singleWidgetAnalysis");
		if (Array.isArray(widgets) && widgets.length > 1) capabilities.add("multiWidgetCoordination");
		if (Array.isArray(links)) {
			if (links.some((link) => link?.kind === "filter" || link?.kind === "filters")) capabilities.add("crossFilter");
			if (links.some((link) => link?.kind === "sharesSelection")) capabilities.add("sharedSelection");
			if (links.some((link) => link?.kind === "syncDomain" || link?.kind === "syncsDomain")) capabilities.add("domainSync");
		}
		if (Array.isArray(store?.interactionTrace) && store.interactionTrace.length > 0) capabilities.add("traceReplay");
		if (Array.isArray(store?.stateSnapshots) && store.stateSnapshots.length > 0) capabilities.add("traceReplay");
		return Array.from(capabilities);
	}
	function valuesFromRecord(record) {
		return record && typeof record === "object" && !Array.isArray(record) ? Object.values(record).filter(Boolean) : [];
	}
	function listResponsesFromStore(store, limit = 20, options = {}) {
		if (typeof store?.listResponses === "function") return store.listResponses(limit, options);
		const workspaceId = options.workspaceId || store?.workspaceId || null;
		const fullResponses = Array.isArray(store?.responseHistory) ? store.responseHistory : [];
		return (workspaceId ? fullResponses.filter((item) => item?.workspaceId === workspaceId) : fullResponses).slice(-(Number.isFinite(limit) ? limit : 20)).map((item) => clone$32(item));
	}
	function readLatestResponseFromStore(store, options = {}) {
		if (typeof store?.readLatestResponse === "function") return store.readLatestResponse(options);
		return listResponsesFromStore(store, 1, options)[0] || null;
	}
	function listStateSnapshotsFromStore(store, options = 50) {
		if (typeof store?.listStateSnapshots === "function") return store.listStateSnapshots(options);
		const normalizedOptions = typeof options === "number" ? { limit: options } : options || {};
		const limit = Number.isFinite(normalizedOptions.limit) ? normalizedOptions.limit : 50;
		const sinceStateId = normalizedOptions.sinceStateId || null;
		const allowedActors = normalizeActorFilter$1(normalizedOptions.actors);
		const fullSnapshots = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots : [];
		const traceByStateId = new Map((Array.isArray(store?.interactionTrace) ? store.interactionTrace : []).filter((record) => typeof record?.stateId === "string" && record.stateId.length > 0).map((record) => [record.stateId, record]));
		const scopedSnapshots = sinceStateId ? (() => {
			const idx = fullSnapshots.findIndex((snapshot) => snapshot?.stateId === sinceStateId);
			return idx >= 0 ? fullSnapshots.slice(idx + 1) : fullSnapshots;
		})() : fullSnapshots;
		return (allowedActors.length > 0 ? scopedSnapshots.filter((snapshot) => {
			const actor = traceByStateId.get(snapshot?.stateId)?.actor || "system";
			return allowedActors.includes(actor);
		}) : scopedSnapshots).slice(-limit).map((item) => {
			const representativeTrace = traceByStateId.get(item?.stateId);
			return makeStateSnapshotMeta({
				stateId: item?.stateId || "",
				createdAt: item?.createdAt || item?.state?.createdAt || null,
				baseStateId: (item?.parentStateId ?? item?.state?.delta?.baseStateId) || null,
				branchId: item?.branchId || null,
				transitionType: item?.transitionType || "continue",
				branchLabel: item?.branchLabel || null,
				actor: representativeTrace?.actor || "system",
				changedRefs: Array.isArray(item?.state?.delta?.changedRefs) ? [...item.state.delta.changedRefs] : [],
				removedRefs: Array.isArray(item?.state?.delta?.removedRefs) ? [...item.state.delta.removedRefs] : []
			});
		});
	}
	function listBranchesFromStore(store) {
		if (typeof store?.listBranches === "function") return store.listBranches();
		return valuesFromRecord(store?.branchRegistry).map((entry) => makeBranchSummary(clone$32(entry)));
	}
	function readInteractionTraceFromStore(store, options = {}) {
		if (typeof store?.readTraceWindow === "function") return store.readTraceWindow({
			limit: options.limit || 50,
			sinceStateId: options.sinceStateId || null,
			actors: options.actors || []
		});
		if (typeof store?.readTrace === "function") {
			const fullTrace = store.readTrace(options.limit || 50);
			const sinceStateId = options.sinceStateId || null;
			const allowedActors = normalizeActorFilter$1(options.actors);
			const scopedTrace = sinceStateId ? (() => {
				const idx = fullTrace.findIndex((record) => record?.stateId === sinceStateId);
				return idx >= 0 ? fullTrace.slice(idx + 1) : fullTrace;
			})() : fullTrace;
			return allowedActors.length > 0 ? scopedTrace.filter((record) => allowedActors.includes(record?.actor || "system")) : scopedTrace;
		}
		const fullTrace = Array.isArray(store?.interactionTrace) ? [...store.interactionTrace] : [];
		const sinceStateId = options.sinceStateId || null;
		const allowedActors = normalizeActorFilter$1(options.actors);
		const scopedTrace = sinceStateId ? (() => {
			const idx = fullTrace.findIndex((record) => record?.stateId === sinceStateId);
			return idx >= 0 ? fullTrace.slice(idx + 1) : fullTrace;
		})() : fullTrace;
		const actorScopedTrace = allowedActors.length > 0 ? scopedTrace.filter((record) => allowedActors.includes(record?.actor || "system")) : scopedTrace;
		const limit = options.limit || 50;
		if (!Number.isFinite(limit) || limit <= 0) return actorScopedTrace;
		return actorScopedTrace.slice(-limit);
	}
	function readSnapshotEntryFromStore(store, stateId) {
		if (!stateId || typeof stateId !== "string") return null;
		if (typeof store?.readSnapshotEntry === "function") return store.readSnapshotEntry(stateId);
		const snapshot = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots.find((item) => item?.stateId === stateId) || null : null;
		return snapshot ? clone$32(snapshot) : null;
	}
	function readCurrentSnapshotMetaFromStore(store) {
		if (typeof store?.readCurrentSnapshotMeta === "function") return store.readCurrentSnapshotMeta();
		const fullSnapshots = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots : [];
		if (fullSnapshots.length === 0) return null;
		const currentStateId = store?.state?.stateId || store?.stateId || null;
		return clone$32((currentStateId ? fullSnapshots.find((entry) => entry?.stateId === currentStateId) || null : null) || fullSnapshots[fullSnapshots.length - 1] || null);
	}
	function readSnapshotFromStore(store, stateId, options = {}) {
		const entry = readSnapshotEntryFromStore(store, stateId);
		const snapshot = entry?.state || null;
		if (!snapshot) return null;
		const scopedSnapshot = Array.isArray(options.refs) && options.refs.length > 0 ? typeof store?.stateManager?.pickRefs === "function" ? store.stateManager.pickRefs({
			state: snapshot,
			refs: options.refs,
			replayContext: entry?.replayContext || null
		}) : (() => {
			const refs = options.refs.filter(Boolean);
			const widgets = {};
			for (const ref of refs) if (snapshot?.widgets?.[ref]) widgets[ref] = clone$32(snapshot.widgets[ref]);
			const nextSnapshot = {
				...clone$32(snapshot),
				widgets
			};
			if (!refs.includes("shared")) delete nextSnapshot.shared;
			if (!refs.includes("taskContext")) delete nextSnapshot.taskContext;
			if (refs.includes("replayContext")) nextSnapshot.replayContext = clone$32(entry?.replayContext || null);
			else delete nextSnapshot.replayContext;
			return nextSnapshot;
		})() : {
			...clone$32(snapshot),
			replayContext: clone$32(entry?.replayContext || null)
		};
		return options.includeMeta ? {
			...scopedSnapshot,
			__meta: {
				stateId: entry?.stateId || null,
				parentStateId: entry?.parentStateId || null,
				branchId: entry?.branchId || null,
				transitionType: entry?.transitionType || "continue",
				branchLabel: entry?.branchLabel || null
			}
		} : scopedSnapshot;
	}
	function buildTraceGraphFromStore(store, options = 50) {
		if (typeof store?.buildTraceGraph === "function") return store.buildTraceGraph(options);
		const normalizedOptions = typeof options === "number" ? { limit: options } : options || {};
		const limit = Number.isFinite(normalizedOptions.limit) ? normalizedOptions.limit : 50;
		const sinceStateId = normalizedOptions.sinceStateId || null;
		const allowedActors = normalizeActorFilter$1(normalizedOptions.actors);
		const fullSnapshots = Array.isArray(store?.stateSnapshots) ? store.stateSnapshots : [];
		const scopedSnapshots = sinceStateId ? (() => {
			const idx = fullSnapshots.findIndex((snapshot) => snapshot?.stateId === sinceStateId);
			return idx >= 0 ? fullSnapshots.slice(idx + 1) : fullSnapshots;
		})() : fullSnapshots;
		const traceByStateId = /* @__PURE__ */ new Map();
		for (const record of Array.isArray(store?.interactionTrace) ? store.interactionTrace : []) {
			if (!record?.stateId) continue;
			traceByStateId.set(record.stateId, pickRepresentativeTraceRecord$1(traceByStateId.get(record.stateId), record));
		}
		const snapshots = (allowedActors.length > 0 ? scopedSnapshots.filter((snapshot) => {
			const actor = traceByStateId.get(snapshot?.stateId)?.actor || "system";
			return allowedActors.includes(actor);
		}) : scopedSnapshots).slice(-limit);
		const latestResponseByStateId = /* @__PURE__ */ new Map();
		for (const response of Array.isArray(store?.responseHistory) ? store.responseHistory : []) {
			if (!response?.stateId) continue;
			latestResponseByStateId.set(response.stateId, response);
		}
		const nodes = snapshots.map((snapshot) => {
			const record = traceByStateId.get(snapshot?.stateId);
			const response = latestResponseByStateId.get(snapshot?.stateId);
			const queryName = getInteractionTraceQueryName(record?.query);
			return makeTraceGraphNode({
				id: snapshot?.stateId || "",
				stateId: snapshot?.stateId || "",
				parentStateId: snapshot?.parentStateId || null,
				branchId: snapshot?.branchId || "main",
				branchLabel: snapshot?.branchLabel || null,
				timestamp: snapshot?.createdAt || snapshot?.state?.createdAt || null,
				actor: record?.actor || "system",
				eventFamily: record?.eventFamily || getInteractionTraceEventFamily(record),
				querySurface: record?.querySurface || getInteractionTraceQuerySurface(record),
				actionName: record?.action?.name || null,
				queryName,
				responseId: response?.responseId || null,
				responseActor: response?.actor || null,
				responsePreview: buildResponsePreview$1(response?.content),
				label: record?.action?.name || queryName || snapshot?.transitionType || "state",
				transitionType: snapshot?.transitionType || "continue",
				current: snapshot?.stateId === (store?.state?.stateId || store?.stateId || null)
			});
		});
		const nodeIds = new Set(nodes.map((node) => node.id));
		const edges = snapshots.filter((snapshot) => snapshot?.parentStateId).filter((snapshot) => nodeIds.has(snapshot.stateId) && nodeIds.has(snapshot.parentStateId)).map((snapshot) => makeTraceGraphEdge({
			id: `${snapshot.parentStateId}->${snapshot.stateId}`,
			from_id: snapshot.parentStateId,
			to_id: snapshot.stateId,
			edge_type: snapshot.transitionType || "continue",
			branchId: snapshot.branchId || "main",
			label: snapshot.transitionType || "continue",
			timestamp: snapshot.createdAt || snapshot.state?.createdAt || null
		}));
		return makeTraceGraph({
			current_state_id: store?.state?.stateId || store?.stateId || null,
			current_branch_id: store?.currentBranchId || null,
			sinceStateId,
			actors: allowedActors,
			branches: listBranchesFromStore(store),
			nodes,
			edges
		});
	}
	function listStoreWidgets(store) {
		if (typeof store?.listWidgetDescriptions === "function") return store.listWidgetDescriptions();
		return valuesFromRecord(store?.descriptions);
	}
	function listStoreDataHandles(store) {
		if (typeof store?.listDataHandles === "function") return store.listDataHandles();
		return valuesFromRecord(store?.dataHandles);
	}
	function readDataHandleFromStore(store, ref) {
		if (!ref || typeof ref !== "string") return null;
		if (typeof store?.getDataHandle === "function") return store.getDataHandle(ref);
		return store?.dataHandles?.[ref] || null;
	}
	function listStoreLinks(store) {
		if (typeof store?.listLinks === "function") return (store.listLinks() || []).map((link) => stripWidgetLinkCompatibilityFields(makeWidgetLink(link)));
		return valuesFromRecord(store?.links).map((link) => stripWidgetLinkCompatibilityFields(makeWidgetLink(link)));
	}
	function listStoreActions(store, actionExecutor = null) {
		if (typeof store?.listActions === "function") {
			const listedActions = store.listActions();
			if (Array.isArray(listedActions) && listedActions.length > 0) return mergeActionDescriptorLists(listedActions, typeof actionExecutor?.list === "function" ? actionExecutor.list() : []);
		}
		if (typeof actionExecutor?.list === "function") {
			const listedActions = actionExecutor.list();
			if (Array.isArray(listedActions) && listedActions.length > 0) return listedActions;
		}
		return valuesFromRecord(store?.actions);
	}
	function listStorePerceptionQueries(store, perceptionExecutor = null) {
		if (typeof store?.listPerceptionQueries === "function") {
			const listedQueries = store.listPerceptionQueries();
			if (Array.isArray(listedQueries) && listedQueries.length > 0) return listedQueries;
		}
		if (typeof perceptionExecutor?.list === "function") {
			const listedQueries = perceptionExecutor.list();
			if (Array.isArray(listedQueries) && listedQueries.length > 0) return listedQueries;
		}
		return valuesFromRecord(store?.perceptionQueries);
	}
	function readRuntimeDataFromStore(store, dataRef) {
		if (!dataRef || typeof dataRef !== "string") return null;
		if (typeof store?.readRuntimeData === "function") return store.readRuntimeData(dataRef);
		return store?.runtimeData?.[dataRef] || null;
	}
	function resolveWidgetRecordFromStore(store, ref) {
		if (!ref || typeof ref !== "string") return null;
		if (typeof store?.getResolvedWidgetForTarget === "function") {
			const resolvedTarget = store.getResolvedWidgetForTarget(ref);
			if (resolvedTarget) return resolvedTarget;
		}
		if (typeof store?.getResolvedWidget === "function") {
			const resolvedWidget = store.getResolvedWidget(ref);
			if (resolvedWidget) return resolvedWidget;
		}
		if (typeof store?.getWidgetDescription === "function") {
			const widgetDescription = store.getWidgetDescription(ref);
			if (widgetDescription) return widgetDescription;
		}
		const widgetDescription = store?.descriptions?.[ref] || null;
		const widgetState = store?.widgets?.[ref] || null;
		if (widgetDescription || widgetState) return {
			...widgetDescription || {},
			...widgetState || {},
			ref,
			data: {
				...widgetDescription?.data || {},
				...widgetState?.data || {}
			}
		};
		const widgetByDataRef = listStoreWidgets(store).find((widget) => {
			const currentDataRef = widget?.data?.currentDataRef || null;
			const sourceDataRef = widget?.data?.sourceDataRef || null;
			const primaryDataRef = widget?.primaryDataRef || null;
			return currentDataRef === ref || sourceDataRef === ref || primaryDataRef === ref;
		}) || null;
		if (!widgetByDataRef?.ref) return null;
		const widgetStateByDataRef = store?.widgets?.[widgetByDataRef.ref] || null;
		return {
			...widgetByDataRef,
			...widgetStateByDataRef || {},
			ref: widgetByDataRef.ref,
			data: {
				...widgetByDataRef?.data || {},
				...widgetStateByDataRef?.data || {}
			}
		};
	}
	function resolveSelectionDataRefFromStore(store, selectionRef) {
		if (!selectionRef || typeof selectionRef !== "string") return null;
		if (typeof store?.resolveSelectionDataRef === "function") {
			const resolvedDataRef = store.resolveSelectionDataRef(selectionRef);
			if (resolvedDataRef) return resolvedDataRef;
		}
		const parts = parseRef(selectionRef);
		if (!parts?.widgetId || !parts?.selectionId) return null;
		return makeSelectionScopedDataRef({
			appId: parts.appId || store?.appId || "widgetva-app",
			workspaceId: parts.workspaceId || store?.workspaceId || "main",
			widgetId: parts.widgetId,
			selectionId: parts.selectionId
		});
	}
	function readWorkspaceDescriptionFromStore(store, { actionExecutor = null, perceptionExecutor = null } = {}) {
		if (typeof store?.readDescription === "function") {
			const description = store.readDescription() || {};
			const describedActions = Array.isArray(description?.actions) ? description.actions : [];
			const executorActions = typeof actionExecutor?.list === "function" ? actionExecutor.list() : [];
			return makeWorkspaceDescription({
				...description,
				actions: mergeActionDescriptorLists(describedActions, executorActions)
			});
		}
		const widgets = listStoreWidgets(store);
		const links = listStoreLinks(store);
		const workspaceCapabilities = deriveWorkspaceCapabilities({
			widgets,
			links,
			store
		});
		return makeWorkspaceDescription({
			appId: store?.appId || null,
			workspaceId: store?.workspaceId || null,
			generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			workspaceCapabilities,
			transportHints: makeWorkspaceTransportHints({
				recommendedTools: [...WORKSPACE_TRANSPORT_TOOL_NAMES],
				note: "External transports should prefer the stable workspace/view/action/perception/trace surface."
			}),
			runtimeTopology: deriveWorkspaceTopology({
				widgets,
				links
			}),
			widgets,
			dataHandles: listStoreDataHandles(store),
			links,
			actions: listStoreActions(store, actionExecutor),
			perceptionQueries: listStorePerceptionQueries(store, perceptionExecutor)
		});
	}
	function readWorkspaceStateFromStore(store, options = {}) {
		if (typeof store?.readState === "function") return store.readState(options);
		const normalizedRefs = Array.isArray(options?.refs) ? options.refs.filter(Boolean) : null;
		const allWidgets = store?.widgets && typeof store.widgets === "object" && !Array.isArray(store.widgets) ? store.widgets : {};
		const widgetEntries = normalizedRefs ? normalizedRefs.map((ref) => [ref, allWidgets?.[ref] || null]).filter(([, widgetState]) => Boolean(widgetState)) : Object.entries(allWidgets).filter(([, widgetState]) => Boolean(widgetState));
		const fallbackState = {
			stateId: store?.stateId || null,
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			widgets: Object.fromEntries(widgetEntries.map(([ref, widgetState]) => [ref, clone$32(widgetState)])),
			shared: {
				selections: {
					registry: {},
					views: {
						primary: null,
						byWidget: {}
					}
				},
				focus: null,
				highlight: null,
				viewport: null,
				globalFilters: {},
				focusedWidget: void 0,
				links: {
					definitions: [],
					topology: {}
				},
				...clone$32(store?.shared) || {}
			}
		};
		if (options?.deltaSince) {
			const baseEntry = readSnapshotEntryFromStore(store, options.deltaSince);
			if (baseEntry?.state) {
				const currentSnapshotEntry = readCurrentSnapshotMetaFromStore(store);
				const currentState = clone$32(currentSnapshotEntry?.state || {
					...fallbackState,
					...store?.taskContext !== void 0 ? { taskContext: clone$32(store.taskContext) } : {}
				});
				const delta = typeof store?.stateManager?.createDelta === "function" ? store.stateManager.createDelta({
					previousState: baseEntry.state,
					nextState: currentState,
					previousReplayContext: baseEntry.replayContext,
					nextReplayContext: currentSnapshotEntry?.replayContext ?? store?.replayContext
				}) : createDeltaFallback({
					previousState: baseEntry.state,
					nextState: currentState,
					previousReplayContext: baseEntry.replayContext,
					nextReplayContext: currentSnapshotEntry?.replayContext ?? store?.replayContext
				});
				const scopedRefs = normalizedRefs && normalizedRefs.length > 0 ? (delta.changedRefs || []).filter((ref) => normalizedRefs.includes(ref)) : null;
				const nextState = {
					...currentState,
					delta
				};
				return typeof store?.stateManager?.pickRefs === "function" ? store.stateManager.pickRefs({
					state: nextState,
					replayContext: currentSnapshotEntry?.replayContext ?? store?.replayContext,
					refs: scopedRefs || delta.changedRefs || []
				}) : pickRefsFromStateFallback({
					state: nextState,
					replayContext: currentSnapshotEntry?.replayContext ?? store?.replayContext,
					refs: scopedRefs || delta.changedRefs || []
				});
			}
		}
		return fallbackState;
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/viewportStateModel.js
	function clone$31(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeString$3(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizeDomain$1(value) {
		return Array.isArray(value) ? clone$31(value) : null;
	}
	function normalizeZoom(value) {
		if (!value || typeof value !== "object" || Array.isArray(value)) return null;
		return clone$31(value);
	}
	function normalizeViewportState(viewport = null) {
		if (!viewport || typeof viewport !== "object" || Array.isArray(viewport)) return null;
		const normalized = clone$31(viewport);
		return {
			...normalized,
			sourceWidgetRef: normalizeString$3(normalized.sourceWidgetRef || normalized.source_widget_ref),
			xDomain: normalizeDomain$1(normalized.xDomain || normalized.x_domain),
			yDomain: normalizeDomain$1(normalized.yDomain || normalized.y_domain),
			zoom: normalizeZoom(normalized.zoom)
		};
	}
	function readViewportState(shared = {}) {
		return normalizeViewportState(shared?.viewport || null);
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/focusStateModel.js
	function normalizeString$2(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizeFocusState(focus = null) {
		if (!focus || typeof focus !== "object" || Array.isArray(focus)) return null;
		return {
			widgetRef: normalizeString$2(focus.widgetRef || focus.widget_ref),
			widgetId: normalizeString$2(focus.widgetId || focus.widget_id),
			source: normalizeString$2(focus.source) || "workspace"
		};
	}
	function readFocusState(shared = {}, widgets = {}) {
		const explicitFocus = normalizeFocusState(shared?.focus || null);
		if (explicitFocus) return explicitFocus;
		const focusedWidgetRef = normalizeString$2(shared?.focusedWidget);
		if (!focusedWidgetRef) return null;
		return {
			widgetRef: focusedWidgetRef,
			widgetId: normalizeString$2((widgets?.[focusedWidgetRef] || null)?.widgetId),
			source: "workspace"
		};
	}
	function withFocusSubmodel(shared = {}, focus = void 0, widgets = {}) {
		const nextFocus = focus === void 0 ? readFocusState(shared, widgets) : normalizeFocusState(focus);
		return {
			...shared || {},
			focusedWidget: nextFocus?.widgetRef || null,
			focus: nextFocus
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/highlightStateModel.js
	function clone$30(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeString$1(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function normalizeStringArray$1(values) {
		return Array.isArray(values) ? values.filter((value, index) => typeof value === "string" && value.length > 0 && values.indexOf(value) === index) : [];
	}
	function normalizeScalarArray(values) {
		return Array.isArray(values) ? clone$30(values.filter((value, index) => value != null && values.findIndex((candidate) => JSON.stringify(candidate) === JSON.stringify(value)) === index)) : [];
	}
	function normalizeHighlightEntry(entry = {}) {
		if (!entry || typeof entry !== "object" || Array.isArray(entry)) return null;
		return {
			widgetRef: normalizeString$1(entry.widgetRef || entry.widget_ref),
			widgetId: normalizeString$1(entry.widgetId || entry.widget_id),
			sourceWidgetRef: normalizeString$1(entry.sourceWidgetRef || entry.source_widget_ref),
			sourceWidgetId: normalizeString$1(entry.sourceWidgetId || entry.source_widget_id),
			selectionRef: normalizeString$1(entry.selectionRef || entry.selection_ref),
			summary: normalizeString$1(entry.summary),
			predicates: Array.isArray(entry.predicates) ? clone$30(entry.predicates) : [],
			highlightedKeys: normalizeScalarArray(entry.highlightedKeys || entry.highlighted_keys),
			inboundLinkIds: normalizeStringArray$1(entry.inboundLinkIds || entry.inbound_link_ids),
			highlightLinkIds: normalizeStringArray$1(entry.highlightLinkIds || entry.highlight_link_ids),
			linkedSourceRefs: normalizeStringArray$1(entry.linkedSourceRefs || entry.linked_source_refs)
		};
	}
	function deriveHighlightState(state = {}) {
		const explicitHighlight = normalizeHighlightState(state?.shared?.highlight || null);
		if (explicitHighlight) return explicitHighlight;
		const widgets = state?.widgets && typeof state.widgets === "object" ? state.widgets : {};
		const entries = Object.values(widgets).map((widgetState) => normalizeHighlightEntry({
			widgetRef: widgetState?.ref || null,
			widgetId: widgetState?.widgetId || null,
			highlightedKeys: widgetState?.feedback?.highlightedKeys || [],
			inboundLinkIds: widgetState?.feedback?.inboundLinkIds || [],
			highlightLinkIds: widgetState?.feedback?.highlightLinkIds || [],
			linkedSourceRefs: widgetState?.feedback?.linkedSourceRefs || []
		})).filter((entry) => entry && (entry.predicates.length > 0 || Boolean(entry.summary) || entry.highlightedKeys.length > 0 || entry.inboundLinkIds.length > 0 || entry.highlightLinkIds.length > 0 || entry.linkedSourceRefs.length > 0));
		return {
			entries,
			activeWidgetRefs: entries.map((entry) => entry.widgetRef).filter(Boolean)
		};
	}
	function normalizeHighlightState(highlight = null) {
		if (!highlight || typeof highlight !== "object" || Array.isArray(highlight)) return null;
		const entries = Array.isArray(highlight.entries) ? highlight.entries.map((entry) => normalizeHighlightEntry(entry)).filter(Boolean) : [];
		return {
			entries,
			activeWidgetRefs: Array.isArray(highlight.activeWidgetRefs) ? highlight.activeWidgetRefs.filter((value, index) => typeof value === "string" && value.length > 0 && highlight.activeWidgetRefs.indexOf(value) === index) : entries.map((entry) => entry.widgetRef).filter(Boolean)
		};
	}
	function withHighlightSubmodel(shared = {}, highlight = void 0) {
		return {
			...shared || {},
			highlight: highlight === void 0 ? normalizeHighlightState(shared?.highlight || null) : normalizeHighlightState(highlight)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/humanInteractionBindings.js
	function buildActionCall({ name, actionTargetRef, params }) {
		return {
			callId: `human_${Date.now()}`,
			name,
			actor: "human",
			targetRef: actionTargetRef || void 0,
			params: {
				targetRef: actionTargetRef || void 0,
				...params
			}
		};
	}
	function resolveCategoryField({ enc, interactionConfig }) {
		const preferredChannel = interactionConfig?.categoryFieldChannel;
		if (preferredChannel && enc?.[preferredChannel]?.field) return enc[preferredChannel].field;
		if (interactionConfig?.categoryField && typeof interactionConfig.categoryField === "string") return interactionConfig.categoryField;
		return enc.color?.field || enc.shape?.field || enc.detail?.field || enc.key?.field || enc.x?.field || enc.y?.field;
	}
	function resolveCellFields({ enc, interactionConfig }) {
		return {
			xField: interactionConfig?.xField || enc.x?.field || null,
			yField: interactionConfig?.yField || enc.y?.field || null
		};
	}
	function normalizeMultiBrushRules(rawRules) {
		if (!Array.isArray(rawRules)) return [];
		return rawRules.filter((rule) => typeof rule?.field === "string" && Array.isArray(rule?.range) && rule.range.length === 2).map((rule) => ({
			field: rule.field,
			range: [Math.min(...rule.range), Math.max(...rule.range)]
		}));
	}
	function buildFallbackSelection({ selectionSourceWidgetId, selectionType, domain, predicates, count, summary, fields, value, keyField, keys, field, values }) {
		return {
			selection_id: `sel_${Date.now()}`,
			source_widget_id: selectionSourceWidgetId || void 0,
			selection_type: selectionType,
			...domain ? { domain } : {},
			...Array.isArray(fields) ? { fields } : {},
			...value && typeof value === "object" && !Array.isArray(value) ? { value } : {},
			...typeof keyField === "string" ? { keyField } : {},
			...Array.isArray(keys) ? { keys } : {},
			...typeof field === "string" ? { field } : {},
			...Array.isArray(values) ? { values } : {},
			predicates,
			count,
			summary
		};
	}
	function bindWidgetHumanInteractions({ view, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
		const data = Array.isArray(spec?.data?.values) ? spec.data.values : [];
		const enc = spec?.encoding || {};
		const cleanups = [];
		if (!interactionConfig || interactionConfig.mode === "none") return () => {};
		function emitBrushSelection() {
			try {
				const xField = enc.x?.field ?? "x";
				const yField = enc.y?.field ?? "y";
				let xMin;
				let xMax;
				let yMin;
				let yMax;
				const brush = view.signal("brush");
				if (brush && (Array.isArray(brush) || brush.x && brush.y)) {
					const xr = Array.isArray(brush) ? brush : brush.x;
					const yr = Array.isArray(brush) ? brush : brush.y;
					if (xr && yr && xr.length >= 2 && yr.length >= 2) {
						xMin = Math.min(xr[0], xr[1]);
						xMax = Math.max(xr[0], xr[1]);
						yMin = Math.min(yr[0], yr[1]);
						yMax = Math.max(yr[0], yr[1]);
					}
				}
				if (xMin == null) {
					const x1 = view.signal("brush_x_1");
					const x2 = view.signal("brush_x_2");
					const y1 = view.signal("brush_y_1");
					const y2 = view.signal("brush_y_2");
					if (x1 != null && x2 != null && y1 != null && y2 != null) {
						xMin = Math.min(x1, x2);
						xMax = Math.max(x1, x2);
						yMin = Math.min(y1, y2);
						yMax = Math.max(y1, y2);
					}
				}
				if (xMin == null) {
					const tupleStore = view.data?.("brush_store");
					const tuple = Array.isArray(tupleStore) && tupleStore.length > 0 ? tupleStore[0] : null;
					const fields = tuple?.fields;
					const values = tuple?.values;
					if (Array.isArray(fields) && Array.isArray(values) && fields.length >= 2 && values.length >= 2) {
						const xVal = values[0];
						const yVal = values[1];
						if (Array.isArray(xVal) && xVal.length >= 2 && Array.isArray(yVal) && yVal.length >= 2) {
							xMin = Math.min(xVal[0], xVal[1]);
							xMax = Math.max(xVal[0], xVal[1]);
							yMin = Math.min(yVal[0], yVal[1]);
							yMax = Math.max(yVal[0], yVal[1]);
						}
					}
				}
				if (xMin == null || xMax == null || yMin == null || yMax == null) {
					onSelectionChange?.(null);
					return;
				}
				const filtered = data.filter((row) => {
					const x = row?.[xField];
					const y = row?.[yField];
					return x != null && y != null && x >= xMin && x <= xMax && y >= yMin && y <= yMax;
				});
				if (onActionCall) {
					onActionCall(buildActionCall({
						name: interactionConfig.actionName || "scatter.brushRegion",
						actionTargetRef,
						params: {
							xField,
							yField,
							xRange: [xMin, xMax],
							yRange: [yMin, yMax]
						}
					}));
					return;
				}
				onSelectionChange?.(buildFallbackSelection({
					selectionSourceWidgetId,
					selectionType: "interval",
					fields: [xField, yField],
					value: {
						[xField]: [xMin, xMax],
						[yField]: [yMin, yMax]
					},
					domain: {
						xDomain: [xMin, xMax],
						yDomain: [yMin, yMax]
					},
					predicates: [{
						field: xField,
						op: "between",
						value: [xMin, xMax]
					}, {
						field: yField,
						op: "between",
						value: [yMin, yMax]
					}],
					count: filtered.length,
					summary: `${xField} ${xMin.toFixed(1)}–${xMax.toFixed(1)}, ${yField} ${yMin.toFixed(1)}–${yMax.toFixed(1)}`
				}));
			} catch {
				onSelectionChange?.(null);
			}
		}
		function emitCategorySelection(_event, item) {
			try {
				const categoryField = resolveCategoryField({
					enc,
					interactionConfig
				});
				const categoryValue = categoryField ? item?.datum?.[categoryField] : void 0;
				if (!categoryField || categoryValue == null) return;
				const matched = data.filter((row) => row?.[categoryField] === categoryValue);
				if (onActionCall) {
					onActionCall(buildActionCall({
						name: interactionConfig.actionName || "bar.selectCategory",
						actionTargetRef,
						params: {
							field: categoryField,
							values: [categoryValue]
						}
					}));
					return;
				}
				onSelectionChange?.(buildFallbackSelection({
					selectionSourceWidgetId,
					selectionType: "category",
					field: categoryField,
					values: [categoryValue],
					predicates: [{
						field: categoryField,
						op: "in",
						value: [categoryValue]
					}],
					count: matched.length,
					summary: `${categoryField}: ${String(categoryValue)}`
				}));
			} catch {
				onSelectionChange?.(null);
			}
		}
		function emitCellSelection(_event, item) {
			try {
				const { xField, yField } = resolveCellFields({
					enc,
					interactionConfig
				});
				const xValue = xField ? item?.datum?.[xField] : void 0;
				const yValue = yField ? item?.datum?.[yField] : void 0;
				if (!xField || !yField || xValue == null || yValue == null) return;
				const matched = data.filter((row) => row?.[xField] === xValue && row?.[yField] === yValue);
				if (onActionCall) {
					onActionCall(buildActionCall({
						name: interactionConfig.actionName || "heatmap.selectCell",
						actionTargetRef,
						params: {
							xField,
							yField,
							xValue,
							yValue
						}
					}));
					return;
				}
				onSelectionChange?.(buildFallbackSelection({
					selectionSourceWidgetId,
					selectionType: "cell",
					fields: [xField, yField],
					value: {
						[xField]: xValue,
						[yField]: yValue
					},
					predicates: [{
						field: xField,
						op: "equals",
						value: xValue
					}, {
						field: yField,
						op: "equals",
						value: yValue
					}],
					count: matched.length,
					summary: `${xField}: ${String(xValue)}; ${yField}: ${String(yValue)}`
				}));
			} catch {
				onSelectionChange?.(null);
			}
		}
		function emitMultiBrushSelection(...args) {
			try {
				const rules = normalizeMultiBrushRules(args.find((value) => Array.isArray(value)) || interactionConfig?.rules);
				if (rules.length === 0) return;
				const matched = data.filter((row) => rules.every((rule) => {
					const value = row?.[rule.field];
					return typeof value === "number" && value >= rule.range[0] && value <= rule.range[1];
				}));
				if (onActionCall) {
					onActionCall(buildActionCall({
						name: interactionConfig.actionName || "parallelCoordinates.brushAxes",
						actionTargetRef,
						params: { rules }
					}));
					return;
				}
				onSelectionChange?.(buildFallbackSelection({
					selectionSourceWidgetId,
					selectionType: "interval",
					predicates: rules.map((rule) => ({
						field: rule.field,
						op: "between",
						value: rule.range
					})),
					count: matched.length,
					summary: rules.map((rule) => `${rule.field} ${rule.range[0]}–${rule.range[1]}`).join("; ")
				}));
			} catch {
				onSelectionChange?.(null);
			}
		}
		if (interactionConfig.mode === "brush2d") {
			try {
				view.addSignalListener("brush", emitBrushSelection);
				cleanups.push(() => view.removeSignalListener?.("brush", emitBrushSelection));
			} catch {}
			for (const signalName of [
				"brush_x_1",
				"brush_x_2",
				"brush_y_1",
				"brush_y_2"
			]) try {
				view.addSignalListener(signalName, emitBrushSelection);
				cleanups.push(() => view.removeSignalListener?.(signalName, emitBrushSelection));
			} catch {}
			try {
				view.addEventListener("mouseup", emitBrushSelection);
				cleanups.push(() => view.removeEventListener?.("mouseup", emitBrushSelection));
			} catch {}
		}
		if (interactionConfig.mode === "categoryClick") try {
			view.addEventListener("click", emitCategorySelection);
			cleanups.push(() => view.removeEventListener?.("click", emitCategorySelection));
		} catch {}
		if (interactionConfig.mode === "cellClick") try {
			view.addEventListener("click", emitCellSelection);
			cleanups.push(() => view.removeEventListener?.("click", emitCellSelection));
		} catch {}
		if (interactionConfig.mode === "multiBrush") for (const signalName of ["widgetva_multiBrush", "multiBrush"]) try {
			view.addSignalListener(signalName, emitMultiBrushSelection);
			cleanups.push(() => view.removeSignalListener?.(signalName, emitMultiBrushSelection));
		} catch {}
		return () => {
			cleanups.forEach((cleanup) => {
				try {
					cleanup();
				} catch {}
			});
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/VegaLiteWidgetAdapter.js
	function createVegaLiteWidgetAdapter(definition = {}) {
		return {
			provider: "vega-lite",
			providerCapabilities: {
				supportedWidgetKinds: definition.kind ? [definition.kind] : [],
				renderStrategy: "vegaEmbed",
				stateApplyStrategy: "signalPatch",
				interactionBindingStrategy: "vegaViewListeners",
				supportsRendererMount: true,
				supportsRendererUpdate: true,
				supportsRendererDispose: true,
				supportsSignalPatching: true,
				supportsOptionMerging: false,
				supportsImperativeRender: false,
				supportsPointSelection: true,
				supportsIntervalSelection: true,
				supportsZoomPan: true,
				supportsSelectionReadback: true,
				supportsViewportReadback: true,
				supportsHighlightProjection: true,
				supportsInteractionEvents: true
			},
			mount({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			update({ view = null, surface = null } = {}) {
				return {
					view,
					surface
				};
			},
			dispose({ view = null } = {}) {
				return view?.finalize?.();
			},
			bindHumanInteractions({ view, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange }) {
				return bindWidgetHumanInteractions({
					view,
					spec,
					interactionConfig,
					selectionSourceWidgetId,
					actionTargetRef,
					onActionCall,
					onSelectionChange
				});
			},
			async applyState({ view }) {
				try {
					await view.runAsync?.();
				} catch {}
			},
			readSelection() {
				return null;
			},
			readViewport() {
				return null;
			},
			...definition
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/workspaceDescriptorBuilders.js
	var dataQueryEngine$1 = createDataQueryEngine({ kind: "js_array" });
	var supportedQueryKinds = dataQueryEngine$1.listSupportedQueryKinds();
	function buildSupportedQueryDescriptors$1(kinds) {
		return kinds.map((kind) => makeDataQueryDescriptor({
			name: kind,
			...DATA_QUERY_DESCRIPTOR_TEMPLATES[kind],
			inputSchema: DATA_QUERY_SCHEMAS[kind] || {
				type: "object",
				additionalProperties: true,
				properties: {}
			}
		}));
	}
	function buildSelectionAffectedStatePaths(affectedRefs, widgetRef) {
		return affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms");
	}
	function buildHighlightAffectedStatePaths(affectedRefs) {
		return affectedRefs.map(() => "feedback");
	}
	function buildDataTransformAffectedStatePaths(affectedRefs) {
		return affectedRefs.flatMap(() => [
			"transforms",
			"data.currentDataRef",
			"data.visibleCount",
			"data.selectedCount"
		]);
	}
	function buildSortAffectedStatePaths(affectedRefs) {
		return affectedRefs.flatMap(() => ["encodings", "view.sort"]);
	}
	function buildEncodingAffectedStatePaths(affectedRefs) {
		return affectedRefs.map(() => "encodings");
	}
	function buildZoomAffectedStatePaths(affectedRefs) {
		return affectedRefs.flatMap(() => [
			"view.xDomain",
			"view.yDomain",
			"view.zoom"
		]);
	}
	function buildWorkspaceAffectedStatePaths(actionName, affectedRefs, widgetRef) {
		if (actionName === "workspace.focusWidget") return ["shared.focusedWidget"];
		if (actionName === "workspace.jumpToState" || actionName === "workspace.branchFromState") return [
			"widgets",
			"shared",
			"replayContext"
		];
		if (actionName === "workspace.resetWorkspace") return [
			"widgets",
			"shared.selections.registry",
			"shared.globalFilters"
		];
		return affectedRefs.map((ref) => ref === widgetRef ? "selections" : "transforms");
	}
	function uniqueKinds(kinds = []) {
		return [...new Set((Array.isArray(kinds) ? kinds : []).filter((kind) => typeof kind === "string" && kind.length > 0))];
	}
	function mergeDescriptorsByName(descriptors = []) {
		const merged = [];
		const seen = /* @__PURE__ */ new Set();
		for (const descriptor of descriptors) {
			const name = descriptor?.name;
			if (typeof name !== "string" || name.length === 0 || seen.has(name)) continue;
			seen.add(name);
			merged.push(descriptor);
		}
		return merged;
	}
	function readAllowedDescriptorNames(widgetDef = null, keys = []) {
		if (!widgetDef || typeof widgetDef !== "object" || Array.isArray(widgetDef)) return null;
		for (const key of keys) {
			const names = widgetDef?.[key];
			if (Array.isArray(names)) {
				const filteredNames = names.filter((name) => typeof name === "string" && name.length > 0);
				if (filteredNames.length > 0) return new Set(filteredNames);
			}
		}
		return null;
	}
	function filterDescriptorsByAllowedNames(descriptors = [], allowedNames = null) {
		if (!(allowedNames instanceof Set)) return descriptors;
		return descriptors.filter((descriptor) => allowedNames.has(descriptor?.name));
	}
	function readCapabilityKinds(widgetKind, widgetSpec = null, widgetDef = null) {
		const specKinds = uniqueKinds(widgetSpec?.__widgetvaRecognizedKinds || widgetSpec?.recognizedKinds || widgetSpec?.__widgetvaCandidateKinds || widgetSpec?.candidateKinds || widgetDef?.recognizedKinds || widgetDef?.candidateKinds);
		if (widgetKind !== "custom" || specKinds.length === 0) return typeof widgetKind === "string" && widgetKind.length > 0 ? [widgetKind] : [];
		return specKinds.filter((kind) => kind !== "custom");
	}
	function withSupportedWidgetKinds(descriptor, supportedWidgetKinds) {
		if (!descriptor || !Array.isArray(supportedWidgetKinds) || supportedWidgetKinds.length === 0) return descriptor;
		return Array.isArray(descriptor.supportedWidgetKinds) && descriptor.supportedWidgetKinds.length > 0 ? descriptor : {
			...descriptor,
			supportedWidgetKinds
		};
	}
	function buildDataHandle({ appId, workspaceId, spec }) {
		const rows = Array.isArray(spec?.data?.values) ? spec.data.values : [];
		const supportedQueries = supportedQueryKinds;
		return makeDataHandle({
			ref: `wl://${appId}/workspace/${workspaceId}/data/primary`,
			title: "Primary Data",
			description: "Current inline dataset bound to the active widget.",
			sourceKind: "inline",
			schema: dataQueryEngine$1.getSchema(rows),
			stats: { rowCount: rows.length },
			supportedQueries,
			supportedQueryDescriptors: buildSupportedQueryDescriptors$1(supportedQueries)
		});
	}
	function buildDerivedDataHandle({ ref, title, description, rows, selectedCount = 0, kind = "dataView", scope = "workspace", widgetRef = void 0, sourceSelectionRef = void 0 }) {
		const supportedQueries = supportedQueryKinds;
		return makeDataHandle({
			ref,
			title,
			description,
			sourceKind: "inline",
			kind,
			scope,
			widgetRef,
			sourceSelectionRef,
			schema: dataQueryEngine$1.getSchema(rows),
			stats: {
				rowCount: rows.length,
				visibleCount: rows.length,
				selectedCount
			},
			supportedQueries,
			supportedQueryDescriptors: buildSupportedQueryDescriptors$1(supportedQueries)
		});
	}
	function buildFamilyActionDescriptors(kind, args) {
		return buildWidgetFamilyActionDescriptors(kind, args);
	}
	function buildFamilyPerceptionDescriptors(kind, args) {
		return buildWidgetFamilyPerceptionDescriptors(kind, args);
	}
	function buildActionDescriptors({ widgetRef, selectionRef, widgetKind, widgetSourceKind, widgetSpec = null, widgetDef = null, scope = "local", affectedRefs = [widgetRef] }) {
		const supportedWidgetKinds = typeof widgetKind === "string" ? [widgetKind] : null;
		const descriptors = [
			makeActionDescriptor({
				name: "widget.updateSelection",
				title: "Update widget selection",
				description: "Apply a runtime selection payload to the current widget through the shared action pipeline.",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildSelectionAffectedStatePaths(affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {
						selection_id: { type: "string" },
						source_widget_id: { type: "string" },
						selection_type: { type: "string" },
						domain: { type: "object" },
						fields: {
							type: "array",
							items: { type: "string" }
						},
						value: { type: "object" },
						keyField: { type: "string" },
						keys: {
							type: "array",
							items: {}
						},
						field: { type: "string" },
						values: {
							type: "array",
							items: {}
						},
						predicates: {
							type: "array",
							items: { type: "object" }
						},
						count: { type: "integer" },
						summary: { type: "string" }
					},
					required: ["selection_type"]
				},
				postconditions: [{ description: "The widget selection state should match the supplied runtime selection payload." }],
				effects: [makeSelectionEffect(widgetRef, "Creates or updates the active selection through the shared runtime action path.")],
				examples: [{
					userGoal: "Apply a directly-manipulated runtime selection through the shared action pipeline.",
					params: {
						selection_type: "category",
						field: "Origin",
						values: ["USA"],
						predicates: [{
							field: "Origin",
							op: "in",
							value: ["USA"]
						}],
						count: 1,
						summary: "Origin: USA"
					}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "widget.clearSelection",
				title: "Clear current selection",
				description: "Clear the active selection on the current widget.",
				category: "selection",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildSelectionAffectedStatePaths(affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The widget should have no active selection." }],
				effects: [makeSelectionEffect(widgetRef, "Clears the active selection on the target widget.")],
				examples: [{
					userGoal: "Clear the current focus region before starting a new analysis step.",
					params: {}
				}],
				reversible: false
			}),
			makeActionDescriptor({
				name: "widget.resetView",
				title: "Reset widget view",
				description: "Reset the target widget view state to its baseline, clearing local view transforms, selections, and visual emphasis.",
				category: "view",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.flatMap(() => [
					"view",
					"transforms",
					"selections",
					"feedback"
				]),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The target widget should render without runtime view projections." }],
				effects: [makeEncodingEffect(widgetRef, "Restores the target widget view state to baseline.")],
				examples: [{
					userGoal: "Reset this chart after zooming or filtering the visible view.",
					params: {}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "widget.undoView",
				title: "Undo widget view",
				description: "Restore the target widget view from the previous runtime state snapshot.",
				category: "view",
				scope,
				supportedWidgetKinds,
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: affectedRefs.flatMap(() => [
					"view",
					"transforms",
					"selections",
					"feedback"
				]),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The target widget should render using the previous runtime view state." }],
				preconditions: [{
					description: "A previous runtime state snapshot exists.",
					failureMessage: "No prior runtime state is available to restore."
				}],
				effects: [makeEncodingEffect(widgetRef, "Restores the target widget view state from runtime history.")],
				examples: [{
					userGoal: "Undo the last zoom or view change on this widget.",
					params: {}
				}],
				reversible: false
			}),
			makeActionDescriptor({
				name: "workspace.jumpToState",
				title: "Jump to previous state",
				description: "Restore the workspace interaction state from a previously recorded state snapshot.",
				category: "coordination",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.jumpToState", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: { stateId: { type: "string" } },
					required: ["stateId"]
				},
				postconditions: [{ description: "The workspace interaction state should match the target snapshot as closely as possible." }],
				preconditions: [{
					description: "The referenced state snapshot exists in runtime history.",
					failureMessage: "The requested stateId is not available in runtime history."
				}],
				examples: [{
					userGoal: "Return to the state before a failed exploratory branch.",
					params: { stateId: "main:previous-state" }
				}],
				reversible: false
			}),
			makeActionDescriptor({
				name: "workspace.focusWidget",
				title: "Focus workspace widget",
				description: "Move the workspace analytical focus to a target widget without changing data selection.",
				category: "navigation",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.focusWidget", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {}
				},
				postconditions: [{ description: "The workspace focused widget should match the requested target." }],
				preconditions: [{
					description: "The requested targetRef resolves to a valid widget in the current workspace.",
					failureMessage: "workspace.focusWidget requires a valid target widget."
				}],
				examples: [{
					userGoal: "Move analytical attention from the overview chart to the focused detail view.",
					params: {}
				}],
				reversible: true
			}),
			makeActionDescriptor({
				name: "workspace.branchFromState",
				title: "Branch from previous state",
				description: "Create a new branch from a previously recorded state snapshot and restore that interaction state.",
				category: "coordination",
				scope: "workspace",
				targetRef: widgetRef,
				affectedRefs,
				affectedStatePaths: buildWorkspaceAffectedStatePaths("workspace.branchFromState", affectedRefs, widgetRef),
				paramsSchema: {
					type: "object",
					properties: {
						stateId: { type: "string" },
						branchLabel: { type: "string" }
					},
					required: ["stateId"]
				},
				postconditions: [{ description: "A new branch should be created and the workspace interaction state should match the branch origin snapshot." }],
				preconditions: [{
					description: "The referenced state snapshot exists in runtime history.",
					failureMessage: "The requested branch origin stateId is not available."
				}],
				reversible: false
			})
		];
		if (widgetSourceKind === "baseSpec") descriptors.push(makeActionDescriptor({
			name: "widget.highlightValues",
			title: "Highlight matching values",
			description: "Highlight rows/items in the target widget that match a categorical field-value set without changing the active selection.",
			category: "coordination",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildHighlightAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					field: { type: "string" },
					values: {
						type: "array",
						items: {},
						minItems: 1
					}
				},
				required: ["field", "values"]
			},
			postconditions: [{ description: "The target widget should expose highlight feedback for the requested values." }, { description: "Matching rows/items should be marked as highlighted in runtime-visible state." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.highlightValues requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept highlight updates.",
				failureMessage: "No active base spec is available for highlight updates."
			}],
			effects: [makeHighlightEffect(widgetRef, "Applies direct runtime highlighting to matching values on the target widget.")],
			examples: [{
				userGoal: "Temporarily highlight records from one category before deciding whether to filter.",
				params: {
					field: "Origin",
					values: ["USA"]
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.aggregateData",
			title: "Aggregate current data view",
			description: "Apply or replace an aggregate transform on the current base specification to produce grouped summary rows.",
			category: "dataTransform",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildDataTransformAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					groupBy: {
						type: "array",
						items: { type: "string" },
						minItems: 1
					},
					measures: {
						type: "array",
						minItems: 1,
						items: {
							type: "object",
							properties: {
								op: { type: "string" },
								field: { type: "string" },
								as: { type: "string" }
							},
							required: ["op", "as"]
						}
					}
				},
				required: ["groupBy", "measures"]
			},
			postconditions: [{ description: "The target widget spec should include an aggregate transform with the requested groupBy and measure definitions." }, { description: "The target widget visible rows should represent grouped summary output rather than the raw record set." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.aggregateData requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept aggregate updates.",
				failureMessage: "No active base spec is available for aggregate updates."
			}],
			examples: [{
				userGoal: "Roll the current detail view up to counts by category before comparing groups.",
				params: {
					groupBy: ["Origin"],
					measures: [{
						op: "count",
						as: "count"
					}]
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.filterByValues",
			title: "Filter by categorical values",
			description: "Apply or replace a categorical filter transform on the current base specification.",
			category: "dataTransform",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildDataTransformAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					field: { type: "string" },
					values: {
						type: "array",
						items: {},
						minItems: 1
					}
				},
				required: ["field", "values"]
			},
			postconditions: [{ description: "The target widget spec should include a categorical filter transform for the requested field." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.filterByValues requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept transform updates.",
				failureMessage: "No active base spec is available for filter updates."
			}],
			effects: [makeFilterEffect(widgetRef, "Applies a categorical filter transform to the target widget specification.")],
			examples: [{
				userGoal: "Restrict the chart to records from a small set of categories.",
				params: {
					field: "Origin",
					values: ["Japan", "USA"]
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.filterByRange",
			title: "Filter by numeric range",
			description: "Apply or replace a numeric range filter transform on the current base specification.",
			category: "dataTransform",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildDataTransformAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					field: { type: "string" },
					range: {
						type: "array",
						items: { type: "number" },
						minItems: 2,
						maxItems: 2
					}
				},
				required: ["field", "range"]
			},
			postconditions: [{ description: "The target widget spec should include a range filter transform for the requested field." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.filterByRange requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept transform updates.",
				failureMessage: "No active base spec is available for filter updates."
			}],
			effects: [makeFilterEffect(widgetRef, "Applies a numeric range filter transform to the target widget specification.")],
			examples: [{
				userGoal: "Restrict the chart to a bounded numeric interval.",
				params: {
					field: "Horsepower",
					range: [80, 160]
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.sortEncoding",
			title: "Sort encoded values",
			description: "Apply a declarative sort rule to a target encoding channel in the current base specification.",
			category: "visualMapping",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildSortAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					channel: { type: "string" },
					field: { type: "string" },
					order: { type: "string" },
					aggregate: { type: "string" }
				},
				required: ["channel", "order"]
			},
			postconditions: [{ description: "The target widget encoding should expose the requested sort rule." }],
			preconditions: [
				{
					description: "The requested targetRef resolves to a valid widget in the current workspace.",
					failureMessage: "widget.sortEncoding requires a valid target widget."
				},
				{
					description: "The target widget is backed by an active base specification that can accept sort updates.",
					failureMessage: "No active base spec is available for sort updates."
				},
				{
					description: "The requested encoding channel already exists on the active spec.",
					failureMessage: "The active spec does not define the requested encoding channel."
				}
			],
			effects: [makeEncodingEffect(widgetRef, "Updates the target encoding channel sort rule in the base specification.")],
			examples: [{
				userGoal: "Sort the category axis by descending count.",
				params: {
					channel: "x",
					order: "descending",
					aggregate: "count"
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.changeEncoding",
			title: "Change visual encoding",
			description: "Rebind a widget encoding channel to a different field in the current base specification.",
			category: "visualMapping",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildEncodingAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					channel: { type: "string" },
					field: { type: "string" },
					type: { type: "string" },
					aggregate: { type: "string" }
				},
				required: ["channel", "field"]
			},
			postconditions: [{ description: "The target widget encoding should point to the specified field." }],
			preconditions: [{
				description: "The requested targetRef resolves to a valid widget in the current workspace.",
				failureMessage: "widget.changeEncoding requires a valid target widget."
			}, {
				description: "The target widget is backed by an active base specification that can accept encoding updates.",
				failureMessage: "No active base spec is available for encoding updates."
			}],
			effects: [makeEncodingEffect(widgetRef, "Rebinds a visual encoding channel to a new field in the base specification.")],
			examples: [{
				userGoal: "Switch the y channel to a different measure.",
				params: {
					channel: "y",
					field: "Miles_per_Gallon",
					type: "quantitative"
				}
			}],
			reversible: true
		}), makeActionDescriptor({
			name: "widget.zoomDomain",
			title: "Zoom widget domain",
			description: "Update the data-space x/y domain of a widget without using pixel coordinates. Use null for an open lower or upper bound when only one side of the domain is known.",
			category: "viewTransform",
			scope,
			supportedWidgetKinds,
			targetRef: widgetRef,
			affectedRefs,
			affectedStatePaths: buildZoomAffectedStatePaths(affectedRefs),
			paramsSchema: {
				type: "object",
				properties: {
					xDomain: {
						type: "array",
						items: { anyOf: [
							{ type: "number" },
							{ type: "string" },
							{ type: "null" }
						] },
						minItems: 2,
						maxItems: 2
					},
					yDomain: {
						type: "array",
						items: { anyOf: [
							{ type: "number" },
							{ type: "string" },
							{ type: "null" }
						] },
						minItems: 2,
						maxItems: 2
					}
				}
			},
			postconditions: [{ description: "The target widget view domain should match the requested x/y ranges." }],
			preconditions: [
				{
					description: "The requested targetRef resolves to a valid widget in the current workspace.",
					failureMessage: "widget.zoomDomain requires a valid target widget."
				},
				{
					description: "The target widget is backed by an active base specification that can accept domain updates.",
					failureMessage: "No active base spec is available for domain updates."
				},
				{
					description: "Any requested x/y domain corresponds to an encoding channel present on the active spec.",
					failureMessage: "The active spec does not define the requested zoom domain channel."
				}
			],
			effects: [makeDomainEffect(widgetRef, "Updates the x/y data-space domain of the target widget.")],
			examples: [{
				userGoal: "Zoom to a narrower value range without using pixel coordinates.",
				params: {
					xDomain: [0, 50],
					yDomain: [10, 100]
				}
			}, {
				userGoal: "Zoom the x domain from 3000 upward while leaving the upper bound open.",
				params: { xDomain: [3e3, null] }
			}],
			reversible: true
		}));
		const capabilityKinds = readCapabilityKinds(widgetKind, widgetSpec, widgetDef);
		const familyDescriptors = capabilityKinds.length > 1 ? mergeDescriptorsByName(capabilityKinds.flatMap((kind) => {
			return buildFamilyActionDescriptors(kind, {
				widgetRef,
				selectionRef,
				widgetSpec,
				widgetKind: kind,
				scope,
				affectedRefs
			});
		})) : buildFamilyActionDescriptors(capabilityKinds[0] || widgetKind, {
			widgetRef,
			selectionRef,
			widgetSpec,
			widgetKind: capabilityKinds[0] || widgetKind,
			scope,
			affectedRefs
		});
		descriptors.unshift(...familyDescriptors.map((descriptor) => withSupportedWidgetKinds(descriptor, supportedWidgetKinds)));
		return filterDescriptorsByAllowedNames(descriptors, readAllowedDescriptorNames(widgetDef, ["exposedActionNames"]));
	}
	function buildPerceptionDescriptors({ appId = "widgetva-app", workspaceId = "main", widgetRef, dataRef, widgetKind, widgetDef, widgetSpec }) {
		const currentViewDataRef = makeCurrentViewDataRef({
			appId,
			workspaceId
		});
		const currentSelectionDataRef = makeCurrentSelectionDataRef({
			appId,
			workspaceId
		});
		const sampleSelectionRef = `${widgetRef}/selection/current`;
		const descriptors = [
			makePerceptionDescriptor({
				name: "perception.inspectViewConfig",
				title: "Inspect current view config",
				description: "Return encodings, transforms, domains, and selection state for the active widget.",
				category: "inspect",
				targetRef: widgetRef,
				paramsSchema: {
					type: "object",
					properties: {}
				},
				sideEffectFree: true,
				evidenceKinds: [
					"viewState",
					"encodingState",
					"selectionState"
				],
				examples: [{
					userGoal: "Check the current encoding and domain before deciding the next action.",
					params: {}
				}]
			}),
			makePerceptionDescriptor({
				name: "perception.inspectVisibleRows",
				title: "Inspect visible rows",
				description: "Return a sampled subset of the rows currently visible in the target widget.",
				category: "inspect",
				targetRef: currentViewDataRef,
				paramsSchema: {
					type: "object",
					properties: { limit: { type: "number" } }
				},
				sideEffectFree: true,
				evidenceKinds: ["rowSample", "visibleData"],
				examples: [
					{
						userGoal: "Look at representative visible records before aggregating.",
						params: { limit: 10 }
					},
					{
						userGoal: "Inspect rows from the shared current_view data view.",
						params: {
							queryScope: { dataRef: currentViewDataRef },
							limit: 10
						}
					},
					{
						userGoal: "Inspect one specific selection-scoped data view.",
						params: {
							queryScope: {
								dataRef: currentSelectionDataRef,
								selectionRef: sampleSelectionRef
							},
							limit: 10
						}
					}
				]
			}),
			makePerceptionDescriptor({
				name: "perception.inspectSelection",
				title: "Inspect current selection",
				description: "Return the active selection payload, predicates, and selected-row count for the current widget or shared current_selection view.",
				category: "inspect",
				targetRef: currentSelectionDataRef,
				paramsSchema: {
					type: "object",
					properties: {}
				},
				sideEffectFree: true,
				evidenceKinds: [
					"selectionState",
					"selectionPredicates",
					"selectionRows"
				],
				verificationTargets: ["shared.selections.registry"],
				examples: [{
					userGoal: "Inspect which selection is currently active before deciding the next action.",
					params: {}
				}, {
					userGoal: "Inspect one specific selection when multiple active selections are present.",
					params: { queryScope: {
						dataRef: currentSelectionDataRef,
						selectionRef: sampleSelectionRef
					} }
				}]
			}),
			makePerceptionDescriptor({
				name: "perception.summarizeSelection",
				title: "Summarize current selection",
				description: "Return a lightweight summary of the current selection, including the shared current_selection data view when available.",
				category: "summarize",
				targetRef: currentSelectionDataRef,
				paramsSchema: {
					type: "object",
					properties: {
						groupBy: {
							type: "array",
							items: { type: "string" }
						},
						measures: {
							type: "array",
							items: { type: "object" }
						},
						fields: {
							type: "array",
							items: { type: "string" }
						},
						metrics: {
							type: "array",
							items: { type: "string" }
						},
						sortBy: {
							type: "object",
							properties: {
								field: { type: "string" },
								order: {
									type: "string",
									enum: ["ascending", "descending"]
								}
							}
						},
						limit: { type: "integer" }
					}
				},
				sideEffectFree: true,
				evidenceKinds: ["selectionSummary", "aggregateEvidence"],
				verificationTargets: ["shared.selections.registry"],
				examples: [{
					userGoal: "Quantify the currently selected subset before answering.",
					params: {
						fields: ["Horsepower"],
						metrics: ["mean", "max"],
						groupBy: ["Origin"]
					}
				}, {
					userGoal: "Summarize one specific selection when multiple active selections are present.",
					params: {
						queryScope: {
							dataRef: currentSelectionDataRef,
							selectionRef: sampleSelectionRef
						},
						fields: ["Horsepower"],
						metrics: ["mean"]
					}
				}]
			}),
			makePerceptionDescriptor({
				name: "perception.summarizeVisible",
				title: "Summarize visible rows",
				description: "Return grouped or aggregated summaries over the rows currently visible in the target widget.",
				category: "summarize",
				targetRef: currentViewDataRef,
				paramsSchema: {
					type: "object",
					properties: {
						groupBy: {
							type: "array",
							items: { type: "string" }
						},
						measures: {
							type: "array",
							items: { type: "object" }
						},
						fields: {
							type: "array",
							items: { type: "string" }
						},
						metrics: {
							type: "array",
							items: { type: "string" }
						},
						sortBy: {
							type: "object",
							properties: {
								field: { type: "string" },
								order: {
									type: "string",
									enum: ["ascending", "descending"]
								}
							}
						},
						limit: { type: "integer" }
					}
				},
				sideEffectFree: true,
				evidenceKinds: ["aggregateEvidence", "visibleDataSummary"],
				examples: [
					{
						userGoal: "Get a grouped summary over the currently visible subset.",
						params: {
							fields: ["Horsepower"],
							metrics: ["mean"],
							groupBy: ["Origin"]
						}
					},
					{
						userGoal: "Summarize rows from the shared current_view data view without changing workspace state.",
						params: {
							queryScope: { dataRef: currentViewDataRef },
							fields: ["Horsepower"],
							metrics: ["mean"]
						}
					},
					{
						userGoal: "Summarize rows from the shared current_selection data view without changing workspace state.",
						params: {
							queryScope: {
								dataRef: currentSelectionDataRef,
								selectionRef: sampleSelectionRef
							},
							fields: ["Horsepower"],
							metrics: ["mean"]
						}
					}
				]
			}),
			makePerceptionDescriptor({
				name: "perception.verifyActionEffect",
				title: "Verify action effect",
				description: "Check whether a recent action produced the expected state change on the specified refs.",
				category: "verify",
				targetRef: widgetRef,
				paramsSchema: {
					type: "object",
					properties: {
						actionName: { type: "string" },
						stateId: { type: "string" },
						refs: {
							type: "array",
							items: { type: "string" }
						}
					}
				},
				sideEffectFree: true,
				evidenceKinds: ["verificationEvidence", "stateDelta"],
				verificationTargets: ["statePatch", "workspaceState"],
				examples: [{
					userGoal: "Verify that a selection or filter changed the intended widgets.",
					params: { actionName: "scatter.brushRegion" }
				}]
			})
		];
		const capabilityKinds = readCapabilityKinds(widgetKind, widgetSpec, widgetDef);
		const familyPerceptions = capabilityKinds.length > 1 ? mergeDescriptorsByName(capabilityKinds.flatMap((kind) => {
			return buildFamilyPerceptionDescriptors(kind, {
				widgetRef,
				dataRef,
				widgetKind: kind,
				widgetDef,
				widgetSpec
			});
		})) : buildFamilyPerceptionDescriptors(capabilityKinds[0] || widgetKind, {
			widgetRef,
			dataRef,
			widgetKind: capabilityKinds[0] || widgetKind,
			widgetDef,
			widgetSpec
		});
		descriptors.push(...familyPerceptions);
		return filterDescriptorsByAllowedNames(descriptors, readAllowedDescriptorNames(widgetDef, ["exposedPerceptionNames"]));
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/planning/WorkspacePlanner.js
	function clone$29(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function getMarkType(spec) {
		return typeof spec?.mark === "string" ? spec.mark : spec?.mark?.type;
	}
	function inferWidgetKind$1(spec) {
		if (spec?.kind === "map") return "map";
		if (spec?.kind === "parallelCoordinates") return "parallelCoordinates";
		if (spec?.kind === "sankey") return "sankey";
		const mark = getMarkType(spec);
		if (mark === "point" || mark === "circle") return "scatter";
		if (mark === "bar") return "bar";
		if (mark === "line") return "line";
		if (mark === "rect") return "heatmap";
		return "custom";
	}
	function isNumber(value) {
		return typeof value === "number" && Number.isFinite(value);
	}
	function normalizeText(value) {
		return typeof value === "string" ? value.trim().toLowerCase() : "";
	}
	function includesAny(text, patterns) {
		return patterns.some((pattern) => text.includes(pattern));
	}
	function derivePlanningSignals({ task, userIntent, runMode, complexityBudget }) {
		const taskFamily = task?.taskFamily || null;
		const coordinationScope = task?.coordinationScope || task?.taskMode || null;
		const evidenceType = task?.evidenceType || null;
		const interactionHorizon = task?.interactionHorizon || null;
		const answerType = task?.answerType || task?.expectedAnswerType || null;
		const queryText = normalizeText(task?.userQuery || userIntent);
		const expansiveMode = runMode === "autonomous" || runMode === "open_ended" || complexityBudget === "extended";
		const asksForDetail = includesAny(queryText, [
			"detail",
			"record",
			"row",
			"exact",
			"which",
			"list",
			"show"
		]);
		const asksForComparison = includesAny(queryText, [
			"compare",
			"distribution",
			"breakdown",
			"summary",
			"group"
		]);
		const asksForCoordination = includesAny(queryText, [
			"linked",
			"across views",
			"cross-widget",
			"shared selection",
			"filter the other"
		]);
		return {
			taskFamily,
			coordinationScope,
			evidenceType,
			interactionHorizon,
			answerType,
			wantsMultiWidget: coordinationScope === "multi_widget" || coordinationScope === "workspace" || evidenceType === "cross_widget" || taskFamily === "multiViewCoordination" || asksForCoordination,
			wantsDetailEvidence: answerType === "exact" || evidenceType === "interaction_revealed" || evidenceType === "cross_widget" || interactionHorizon === "multi_step" || asksForDetail,
			wantsSummaryContext: answerType === "exploratory" || taskFamily === "compare" || taskFamily === "distribution" || taskFamily === "rank" || taskFamily === "trend" || asksForComparison,
			expansiveMode
		};
	}
	function appendPlanningSignals(plan, signals, extras = []) {
		const rationale = Array.isArray(plan?.rationale) ? [...plan.rationale] : [];
		if (signals.taskFamily) rationale.push(`Planner considered task family "${signals.taskFamily}" when choosing topology ${plan.topology}.`);
		if (signals.coordinationScope) rationale.push(`Requested coordination scope "${signals.coordinationScope}" influenced the workspace layout.`);
		if (signals.evidenceType) rationale.push(`Evidence target "${signals.evidenceType}" guided the choice of summary/detail coordination surfaces.`);
		if (signals.interactionHorizon) rationale.push(`Interaction horizon "${signals.interactionHorizon}" informed the workspace complexity level.`);
		return {
			...plan,
			rationale: [...rationale, ...extras]
		};
	}
	function normalizeSchemaFieldKind(type) {
		const kind = normalizeText(type);
		if (kind === "quantitative" || kind === "number" || kind === "integer" || kind === "float" || kind === "double" || kind === "numeric") return "quantitative";
		return "nominal";
	}
	function inferFieldKinds({ rows, datasetSchema }) {
		const fields = {};
		const sample = Array.isArray(rows) ? rows.slice(0, 50) : [];
		for (const row of sample) {
			if (!row || typeof row !== "object") continue;
			for (const [field, value] of Object.entries(row)) {
				if (!(field in fields)) fields[field] = {
					numeric: 0,
					string: 0,
					other: 0
				};
				if (isNumber(value)) fields[field].numeric += 1;
				else if (typeof value === "string") fields[field].string += 1;
				else fields[field].other += 1;
			}
		}
		if (sample.length === 0 && Array.isArray(datasetSchema?.fields)) return datasetSchema.fields.filter((field) => typeof field?.name === "string" && field.name).map((field) => ({
			field: field.name,
			kind: normalizeSchemaFieldKind(field.type)
		}));
		return Object.entries(fields).map(([field, counts]) => ({
			field,
			kind: counts.numeric >= counts.string ? "quantitative" : "nominal"
		}));
	}
	function findCategoricalField({ spec, rows, datasetSchema }) {
		const colorField = spec?.encoding?.color?.field;
		if (typeof colorField === "string") return colorField;
		const xField = spec?.encoding?.x?.field;
		const yField = spec?.encoding?.y?.field;
		return inferFieldKinds({
			rows,
			datasetSchema
		}).find((entry) => entry.kind === "nominal" && entry.field !== xField && entry.field !== yField)?.field || null;
	}
	function findSeriesField({ spec, rows, datasetSchema }) {
		const colorField = spec?.encoding?.color?.field;
		if (typeof colorField === "string") return colorField;
		const detailField = spec?.encoding?.detail?.field;
		if (typeof detailField === "string") return detailField;
		return inferFieldKinds({
			rows,
			datasetSchema
		}).find((entry) => entry.kind === "nominal")?.field || null;
	}
	function findValueField(spec) {
		const yField = spec?.encoding?.y?.field;
		if (typeof yField === "string") return yField;
		const colorField = spec?.encoding?.color?.field;
		if (typeof colorField === "string") return colorField;
		return null;
	}
	function hasCategoricalAxes(spec) {
		return !!spec?.encoding?.x?.field && !!spec?.encoding?.y?.field;
	}
	function hasQuantitativeXY(spec) {
		return spec?.encoding?.x?.type === "quantitative" && spec?.encoding?.y?.type === "quantitative";
	}
	function createCategorySummarySpec(field) {
		return {
			$schema: "https://vega.github.io/schema/vega-lite/v5.json",
			width: 280,
			height: 220,
			mark: {
				type: "bar",
				cornerRadiusTopLeft: 3,
				cornerRadiusTopRight: 3,
				color: "#4d7ea8"
			},
			encoding: {
				x: {
					field,
					type: "nominal",
					sort: "-y",
					axis: {
						title: field,
						labelAngle: -18
					}
				},
				y: {
					field: "recordCount",
					type: "quantitative",
					axis: { title: "Visible Records" }
				},
				tooltip: [{
					field,
					type: "nominal"
				}, {
					field: "recordCount",
					type: "quantitative"
				}]
			},
			config: {
				view: { stroke: "#d6dbe6" },
				axis: {
					labelColor: "#2d3a4e",
					titleColor: "#2d3a4e"
				}
			}
		};
	}
	function createLineSeriesSummarySpec(field, valueField) {
		return {
			$schema: "https://vega.github.io/schema/vega-lite/v5.json",
			width: 280,
			height: 220,
			mark: {
				type: "bar",
				cornerRadiusTopLeft: 3,
				cornerRadiusTopRight: 3,
				color: "#4d7ea8"
			},
			encoding: {
				x: {
					field,
					type: "nominal",
					sort: "-y",
					axis: {
						title: field,
						labelAngle: -18
					}
				},
				y: {
					field: valueField ? `total_${valueField}` : "recordCount",
					type: "quantitative",
					axis: { title: valueField ? `${valueField} Total` : "Visible Records" }
				},
				tooltip: [{
					field,
					type: "nominal"
				}, {
					field: valueField ? `total_${valueField}` : "recordCount",
					type: "quantitative"
				}]
			},
			config: {
				view: { stroke: "#d6dbe6" },
				axis: {
					labelColor: "#2d3a4e",
					titleColor: "#2d3a4e"
				}
			}
		};
	}
	function createFocusedDetailSpec(spec) {
		const next = clone$29(spec);
		next.width = 280;
		next.height = 220;
		next.title = typeof spec?.title === "string" ? `${spec.title} Detail` : {
			text: "Focused Detail View",
			anchor: "start"
		};
		return next;
	}
	function createDetailViewSource(spec) {
		return {
			kind: "templateSpec",
			spec: createFocusedDetailSpec(spec)
		};
	}
	function buildMapDetailPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: [
					"geoPattern",
					"compare",
					"distribution"
				]
			}, {
				widgetId: `${primaryWidgetId}_detail`,
				role: "detail",
				title: "Selected Region Focus",
				description: "Focused map view for the currently selected regions.",
				source: createDetailViewSource(spec),
				analyticRoles: [
					"geoPattern",
					"detail",
					"compare"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Selected map regions filter the focused detail view.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}, {
				linkId: `${primaryWidgetId}_share_selection_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The focused detail view mirrors the active map-region selection context.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}],
			rationale: ["Map views benefit from a coordinated detail view for inspecting selected regions in context.", "This topology supports geographic overview plus focused visual evidence."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Map Detail Workspace"
		});
	}
	function buildParallelCoordinatesDetailPlan({ sessionId, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: [
					"compare",
					"outlier",
					"distribution"
				]
			}, {
				widgetId: `${primaryWidgetId}_detail`,
				role: "detail",
				title: "Brushed Focus",
				description: "Focused parallel coordinates view for the currently brushed multivariate range.",
				source: createDetailViewSource(spec),
				analyticRoles: [
					"compare",
					"detail",
					"outlier"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Parallel coordinates brushing filters the focused detail view.",
				fieldMapping: []
			}, {
				linkId: `${primaryWidgetId}_share_selection_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The focused detail view mirrors the active multivariate brush context.",
				fieldMapping: []
			}],
			rationale: ["Parallel coordinates often require a focused follow-up view to interpret multivariate brushes.", "This topology supports multivariate filtering plus visual detail inspection."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Parallel Coordinates Detail Workspace"
		});
	}
	function buildSankeyDetailPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: ["flow", "compare"]
			}, {
				widgetId: `${primaryWidgetId}_detail`,
				role: "detail",
				title: "Focused Flow View",
				description: "Focused Sankey view for the currently selected flow categories.",
				source: createDetailViewSource(spec),
				analyticRoles: [
					"flow",
					"detail",
					"compare"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Focused Sankey flows filter the focused detail view.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}, {
				linkId: `${primaryWidgetId}_share_selection_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The focused detail view mirrors the active Sankey flow selection context.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}],
			rationale: ["Sankey views benefit from a focused follow-up view for selected flows.", "This topology supports flow overview plus focused visual evidence."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Sankey Detail Workspace"
		});
	}
	function buildLineCoordinationPlan({ sessionId, field, valueField, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: ["trend", "compare"]
			}, {
				widgetId: `${primaryWidgetId}_summary`,
				role: "context",
				title: `${field} Trend Summary`,
				description: `Aggregated summary for visible series grouped by ${field}.`,
				source: {
					kind: "templateSpec",
					spec: createLineSeriesSummarySpec(field, valueField)
				},
				dataBinding: {
					sourceDataId: "primary",
					transforms: [{
						kind: "aggregate",
						groupBy: [field],
						metrics: valueField ? [{
							op: "sum",
							field: valueField,
							as: `total_${valueField}`
						}] : [{
							op: "count",
							as: "recordCount"
						}],
						sortBy: {
							field: valueField ? `total_${valueField}` : "recordCount",
							order: "descending"
						}
					}]
				},
				analyticRoles: [
					"compare",
					"rank",
					"trend"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_summary`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_summary`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Selected line series filter the summary view.",
				fieldMapping: []
			}],
			rationale: ["Line views benefit from a grouped summary for the selected series.", "This topology supports trend comparison with a coordinated aggregate context."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Coordination` : "Line Coordination Workspace"
		});
	}
	function buildHeatmapDetailPlan({ sessionId, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: [
					"distribution",
					"outlier",
					"compare"
				]
			}, {
				widgetId: `${primaryWidgetId}_detail`,
				role: "detail",
				title: "Selected Cell Focus",
				description: "Focused heatmap view for the currently selected cell.",
				source: createDetailViewSource(spec),
				analyticRoles: [
					"distribution",
					"detail",
					"compare"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Selected heatmap cell filters the focused detail view.",
				fieldMapping: []
			}, {
				linkId: `${primaryWidgetId}_share_selection_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The focused detail view mirrors the active heatmap cell selection context.",
				fieldMapping: []
			}],
			rationale: ["Heatmaps benefit from a focused follow-up view for a selected cell.", "This topology supports matrix overview plus focused visual evidence."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Heatmap Detail Workspace"
		});
	}
	function buildSingleViewPlan({ sessionId, spec }) {
		const widgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T1",
			widgets: [{
				widgetId,
				role: "primary",
				source: { kind: "baseSpec" }
			}],
			links: [],
			rationale: ["Single-view topology is sufficient for the current data and task context."],
			planningMode: "minimal_default",
			primaryWidgetId: widgetId,
			title: typeof spec?.title === "string" ? spec.title : "Active Widget"
		});
	}
	function buildCoordinatedPairPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T2",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: ["lookup", "compare"]
			}, {
				widgetId: `${primaryWidgetId}_summary`,
				role: "context",
				title: `${field} Distribution`,
				description: `Category summary for ${field} over the current visible rows.`,
				source: {
					kind: "templateSpec",
					spec: createCategorySummarySpec(field)
				},
				dataBinding: {
					sourceDataId: "primary",
					transforms: [{
						kind: "aggregate",
						groupBy: [field],
						metrics: [{
							op: "count",
							as: "recordCount"
						}],
						sortBy: {
							field: "recordCount",
							order: "descending"
						}
					}]
				},
				analyticRoles: [
					"compare",
					"rank",
					"distribution"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_summary`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_summary`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary selection filters the summary view.",
				fieldMapping: []
			}],
			rationale: ["A categorical summary view adds context to the primary analysis view.", "Filter propagation supports coordinated overview and summary inspection."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? spec.title : "Coordinated Workspace"
		});
	}
	function buildScatterDetailPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T3",
			widgets: [{
				widgetId: primaryWidgetId,
				role: "primary",
				source: { kind: "baseSpec" },
				analyticRoles: [
					"correlate",
					"outlier",
					"cluster"
				]
			}, {
				widgetId: `${primaryWidgetId}_detail`,
				role: "detail",
				title: "Filtered Records",
				description: "Focused scatter view showing records currently revealed by the active selection.",
				source: createDetailViewSource(spec),
				analyticRoles: [
					"correlate",
					"detail",
					"outlier"
				]
			}],
			links: [{
				linkId: `${primaryWidgetId}_filter_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary scatter selection filters the focused detail view.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}, {
				linkId: `${primaryWidgetId}_share_selection_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "sharesSelection",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "The focused detail view mirrors the active scatter selection context.",
				fieldMapping: [{
					sourceField: field,
					targetField: field
				}]
			}],
			rationale: [
				"Scatter data with quantitative x/y benefits from a linked focused detail view.",
				"The overview scatter directly controls a dedicated visual detail surface for drill-down evidence.",
				"This workspace topology keeps overview and detail tightly coordinated without adding a third dashboard role."
			],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Detail` : "Scatter Detail Workspace"
		});
	}
	function buildTripleDashboardPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T4",
			widgets: [
				{
					widgetId: primaryWidgetId,
					role: "primary",
					source: { kind: "baseSpec" },
					analyticRoles: [
						"correlate",
						"outlier",
						"cluster"
					]
				},
				{
					widgetId: `${primaryWidgetId}_summary`,
					role: "context",
					title: `${field} Distribution`,
					description: `Category summary for ${field} under the current scatter selection.`,
					source: {
						kind: "templateSpec",
						spec: createCategorySummarySpec(field)
					},
					dataBinding: {
						sourceDataId: "primary",
						transforms: [{
							kind: "aggregate",
							groupBy: [field],
							metrics: [{
								op: "count",
								as: "recordCount"
							}],
							sortBy: {
								field: "recordCount",
								order: "descending"
							}
						}]
					},
					analyticRoles: [
						"compare",
						"rank",
						"distribution"
					]
				},
				{
					widgetId: `${primaryWidgetId}_detail`,
					role: "detail",
					title: "Filtered Records",
					description: "Focused scatter view showing the records currently revealed by the active selection.",
					source: createDetailViewSource(spec),
					analyticRoles: [
						"correlate",
						"detail",
						"outlier"
					]
				}
			],
			links: [
				{
					linkId: `${primaryWidgetId}_filter_summary`,
					sourceWidgetId: primaryWidgetId,
					targetWidgetId: `${primaryWidgetId}_summary`,
					kind: "filter",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "Primary scatter selection filters the summary view.",
					fieldMapping: []
				},
				{
					linkId: `${primaryWidgetId}_filter_detail`,
					sourceWidgetId: primaryWidgetId,
					targetWidgetId: `${primaryWidgetId}_detail`,
					kind: "filter",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "Primary scatter selection filters the focused detail view.",
					fieldMapping: []
				},
				{
					linkId: `${primaryWidgetId}_summary_highlight_detail`,
					sourceWidgetId: `${primaryWidgetId}_summary`,
					targetWidgetId: `${primaryWidgetId}_detail`,
					kind: "highlight",
					activationPolicy: "automatic",
					effectConstraint: null,
					description: "Category selection in the summary view highlights matching records in the focused detail view.",
					fieldMapping: [{
						sourceField: field,
						targetField: field
					}]
				}
			],
			rationale: [
				"A triple dashboard supports overview, grouped comparison, and focused visual inspection in parallel.",
				"Scatter brush drives both grouped summary and a focused detail view.",
				"This topology is suitable when the task requires aggregate comparison plus visual evidence."
			],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Dashboard` : "Triple Dashboard Workspace"
		});
	}
	function buildDrillDownChainPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T5",
			widgets: [
				{
					widgetId: primaryWidgetId,
					role: "primary",
					source: { kind: "baseSpec" },
					analyticRoles: [
						"correlate",
						"outlier",
						"cluster"
					]
				},
				{
					widgetId: `${primaryWidgetId}_summary`,
					role: "context",
					title: `${field} Distribution`,
					description: `Category summary for ${field} under the current scatter selection.`,
					source: {
						kind: "templateSpec",
						spec: createCategorySummarySpec(field)
					},
					dataBinding: {
						sourceDataId: "primary",
						transforms: [{
							kind: "aggregate",
							groupBy: [field],
							metrics: [{
								op: "count",
								as: "recordCount"
							}],
							sortBy: {
								field: "recordCount",
								order: "descending"
							}
						}]
					},
					analyticRoles: [
						"compare",
						"rank",
						"distribution"
					]
				},
				{
					widgetId: `${primaryWidgetId}_detail`,
					role: "detail",
					title: "Drill-down Records",
					description: "Focused scatter view reached through the staged summary-to-detail drill-down path.",
					source: createDetailViewSource(spec),
					analyticRoles: [
						"correlate",
						"detail",
						"outlier"
					]
				}
			],
			links: [{
				linkId: `${primaryWidgetId}_filter_summary`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_summary`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary scatter selection filters the summary view.",
				fieldMapping: []
			}, {
				linkId: `${primaryWidgetId}_summary_filter_detail`,
				sourceWidgetId: `${primaryWidgetId}_summary`,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Summary-category selections progressively narrow the focused detail view.",
				fieldMapping: [{
					sourceField: field,
					targetField: field
				}]
			}],
			rationale: ["A drill-down chain should narrow the analysis scope in stages rather than fan out from a single controller.", "The summary view sits between overview and focused detail so each interaction progressively refines the evidence surface."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Drill-down` : "Drill-down Chain Workspace"
		});
	}
	function buildGlobalControlPlan({ sessionId, field, spec }) {
		const primaryWidgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T6",
			widgets: [
				{
					widgetId: primaryWidgetId,
					role: "primary",
					source: { kind: "baseSpec" },
					analyticRoles: [
						"correlate",
						"outlier",
						"cluster"
					]
				},
				{
					widgetId: `${primaryWidgetId}_summary`,
					role: "context",
					title: `${field} Distribution`,
					description: `Category summary for ${field} under the current global control selection.`,
					source: {
						kind: "templateSpec",
						spec: createCategorySummarySpec(field)
					},
					dataBinding: {
						sourceDataId: "primary",
						transforms: [{
							kind: "aggregate",
							groupBy: [field],
							metrics: [{
								op: "count",
								as: "recordCount"
							}],
							sortBy: {
								field: "recordCount",
								order: "descending"
							}
						}]
					},
					analyticRoles: [
						"compare",
						"rank",
						"distribution"
					]
				},
				{
					widgetId: `${primaryWidgetId}_detail`,
					role: "detail",
					title: "Filtered Records",
					description: "Focused scatter view directly controlled by the global selection surface.",
					source: createDetailViewSource(spec),
					analyticRoles: [
						"correlate",
						"detail",
						"outlier"
					]
				}
			],
			links: [{
				linkId: `${primaryWidgetId}_filter_summary`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_summary`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary selection filters the grouped summary view.",
				fieldMapping: []
			}, {
				linkId: `${primaryWidgetId}_filter_detail`,
				sourceWidgetId: primaryWidgetId,
				targetWidgetId: `${primaryWidgetId}_detail`,
				kind: "filter",
				activationPolicy: "automatic",
				effectConstraint: null,
				description: "Primary selection simultaneously filters the focused detail view.",
				fieldMapping: field ? [{
					sourceField: field,
					targetField: field
				}] : []
			}],
			rationale: ["A global-control topology fans one interaction surface out to multiple dependent targets.", "Grouped context and focused detail update in parallel from the same primary control widget."],
			planningMode: "topology_driven",
			primaryWidgetId,
			title: typeof spec?.title === "string" ? `${spec.title} Global Control` : "Global Control Workspace"
		});
	}
	function planWorkspace({ sessionId, spec, task = null, datasetSchema = null, userIntent = null, runMode = "goal_oriented", complexityBudget = "standard", preferredTopology = null }) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		const rows = Array.isArray(spec?.data?.values) ? spec.data.values : [];
		const planningSignals = derivePlanningSignals({
			task,
			userIntent,
			runMode,
			complexityBudget
		});
		const widgetKind = inferWidgetKind$1(spec);
		const categoryField = findCategoricalField({
			spec,
			rows,
			datasetSchema
		});
		const seriesField = findSeriesField({
			spec,
			rows,
			datasetSchema
		});
		const valueField = findValueField(spec);
		const requestedTopology = typeof preferredTopology === "string" ? preferredTopology : null;
		const canUseScatterWorkspace = widgetKind === "scatter" && hasQuantitativeXY(spec) && categoryField;
		const canUseCoordinatedPair = !!categoryField;
		const canUseLineWorkspace = widgetKind === "line" && !!seriesField;
		const canUseHeatmapWorkspace = widgetKind === "heatmap" && hasCategoricalAxes(spec);
		const canUseMapWorkspace = widgetKind === "map" && !!categoryField;
		const canUseParallelWorkspace = widgetKind === "parallelCoordinates";
		const canUseSankeyWorkspace = widgetKind === "sankey" && !!categoryField;
		const expansiveMode = planningSignals.expansiveMode;
		const canUseTripleDashboard = canUseScatterWorkspace;
		const taskDrivenMultiWidget = planningSignals.wantsMultiWidget;
		const taskDrivenDetail = planningSignals.wantsDetailEvidence;
		const taskDrivenSummary = planningSignals.wantsSummaryContext;
		if (requestedTopology === "T2" && canUseLineWorkspace) return appendPlanningSignals(buildLineCoordinationPlan({
			sessionId,
			field: seriesField,
			valueField,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseHeatmapWorkspace) return appendPlanningSignals(buildHeatmapDetailPlan({
			sessionId,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseMapWorkspace) return appendPlanningSignals(buildMapDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseParallelWorkspace) return appendPlanningSignals(buildParallelCoordinatesDetailPlan({
			sessionId,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseSankeyWorkspace) return appendPlanningSignals(buildSankeyDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T4" && canUseTripleDashboard) return appendPlanningSignals(buildTripleDashboardPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T3" && canUseScatterWorkspace) return appendPlanningSignals(buildScatterDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T5" && canUseScatterWorkspace) return appendPlanningSignals(buildDrillDownChainPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T6" && canUseScatterWorkspace) return appendPlanningSignals(buildGlobalControlPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T2" && canUseCoordinatedPair) return appendPlanningSignals(buildCoordinatedPairPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (requestedTopology === "T1") return appendPlanningSignals(buildSingleViewPlan({
			sessionId,
			spec
		}), planningSignals);
		if (canUseTripleDashboard && (complexityBudget === "extended" || taskDrivenMultiWidget && taskDrivenSummary && taskDrivenDetail)) return appendPlanningSignals(buildTripleDashboardPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals, ["Planner selected a triple-dashboard topology because the task requires cross-widget evidence, grouped context, and focused visual detail together."]);
		if (canUseScatterWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildScatterDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals, ["Planner selected a scatter-detail topology because the current task requires interaction-driven evidence or coordinated follow-up views."]);
		if (canUseLineWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenSummary || taskDrivenMultiWidget)) return appendPlanningSignals(buildLineCoordinationPlan({
			sessionId,
			field: seriesField,
			valueField,
			spec
		}), planningSignals);
		if (canUseHeatmapWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildHeatmapDetailPlan({
			sessionId,
			spec
		}), planningSignals);
		if (canUseMapWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildMapDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (canUseParallelWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildParallelCoordinatesDetailPlan({
			sessionId,
			spec
		}), planningSignals);
		if (canUseSankeyWorkspace && (expansiveMode || taskDrivenDetail || taskDrivenMultiWidget)) return appendPlanningSignals(buildSankeyDetailPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals);
		if (canUseCoordinatedPair && (expansiveMode || taskDrivenSummary || taskDrivenMultiWidget)) return appendPlanningSignals(buildCoordinatedPairPlan({
			sessionId,
			field: categoryField,
			spec
		}), planningSignals, ["Planner selected a coordinated-pair topology to provide an overview view plus grouped context without adding extra detail-view complexity."]);
		return appendPlanningSignals(buildSingleViewPlan({
			sessionId,
			spec
		}), planningSignals, ["Planner kept a single-view topology because the current task signals do not require additional coordination surfaces."]);
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/workspacePlanBuilder.js
	var WORKSPACE_TOPOLOGIES = [
		"T1",
		"T2",
		"T3",
		"T4",
		"T5",
		"T6"
	];
	function isPlainObject$4(value) {
		return !!value && typeof value === "object" && !Array.isArray(value);
	}
	function summarizeRequestedWorkspaceSpec(spec) {
		if (!isPlainObject$4(spec)) return null;
		return {
			topology: typeof spec.topology === "string" ? spec.topology : null,
			widgetCount: Array.isArray(spec.widgets) ? spec.widgets.length : 0,
			linkCount: Array.isArray(spec.links) ? spec.links.length : 0
		};
	}
	function validateRequestedWorkspaceSpec(spec) {
		if (spec == null) return {
			ok: true,
			issues: []
		};
		if (!isPlainObject$4(spec)) return {
			ok: false,
			issues: ["workspaceSpec must be a plain object."]
		};
		const issues = [];
		if (spec.topology != null && !WORKSPACE_TOPOLOGIES.includes(spec.topology)) issues.push(`workspaceSpec.topology must be one of: ${WORKSPACE_TOPOLOGIES.join(", ")}.`);
		if (spec.widgets != null && !Array.isArray(spec.widgets)) issues.push("workspaceSpec.widgets must be an array when provided.");
		if (spec.links != null && !Array.isArray(spec.links)) issues.push("workspaceSpec.links must be an array when provided.");
		const widgetIds = /* @__PURE__ */ new Set();
		if (Array.isArray(spec.widgets)) spec.widgets.forEach((widget, index) => {
			if (!isPlainObject$4(widget)) {
				issues.push(`workspaceSpec.widgets[${index}] must be a plain object.`);
				return;
			}
			if (!widget.widgetId || typeof widget.widgetId !== "string") {
				issues.push(`workspaceSpec.widgets[${index}].widgetId must be a non-empty string.`);
				return;
			}
			if (widgetIds.has(widget.widgetId)) {
				issues.push(`workspaceSpec.widgets[${index}].widgetId must be unique.`);
				return;
			}
			widgetIds.add(widget.widgetId);
		});
		if (Array.isArray(spec.links)) spec.links.forEach((link, index) => {
			if (!isPlainObject$4(link)) {
				issues.push(`workspaceSpec.links[${index}] must be a plain object.`);
				return;
			}
			if (typeof link.sourceStateRef === "string" && typeof link.targetStateRef === "string") {
				const linkRef = link.ref || link.linkId || link.id;
				if (!linkRef || typeof linkRef !== "string") issues.push(`workspaceSpec.links[${index}].ref or linkId must be a non-empty string for state-to-state coordination relations.`);
				if (!link.sourceStateRef || typeof link.sourceStateRef !== "string") issues.push(`workspaceSpec.links[${index}].sourceStateRef must be a non-empty string.`);
				if (!link.targetStateRef || typeof link.targetStateRef !== "string") issues.push(`workspaceSpec.links[${index}].targetStateRef must be a non-empty string.`);
				if (link.relation != null && link.relation !== "controls" && link.relation !== "derives") issues.push(`workspaceSpec.links[${index}].relation must be "controls" or "derives" when provided.`);
				if (link.transform != null) {
					if (!isPlainObject$4(link.transform)) issues.push(`workspaceSpec.links[${index}].transform must be a plain object when provided.`);
					else if (!link.transform.kind || typeof link.transform.kind !== "string") issues.push(`workspaceSpec.links[${index}].transform.kind must be a non-empty string when transform is provided.`);
				}
				return;
			}
			if (!link.linkId || typeof link.linkId !== "string") issues.push(`workspaceSpec.links[${index}].linkId must be a non-empty string.`);
			if (!link.sourceWidgetId || typeof link.sourceWidgetId !== "string") issues.push(`workspaceSpec.links[${index}].sourceWidgetId must be a non-empty string.`);
			else if (widgetIds.size > 0 && !widgetIds.has(link.sourceWidgetId)) issues.push(`workspaceSpec.links[${index}].sourceWidgetId must reference an existing widgetId.`);
			if (!link.targetWidgetId || typeof link.targetWidgetId !== "string") issues.push(`workspaceSpec.links[${index}].targetWidgetId must be a non-empty string.`);
			else if (widgetIds.size > 0 && !widgetIds.has(link.targetWidgetId)) issues.push(`workspaceSpec.links[${index}].targetWidgetId must reference an existing widgetId.`);
			if (!link.kind || typeof link.kind !== "string") issues.push(`workspaceSpec.links[${index}].kind must be a non-empty string.`);
			else if (!WIDGET_LINK_KINDS.includes(link.kind)) issues.push(`workspaceSpec.links[${index}].kind must be one of: ${WIDGET_LINK_KINDS.join(", ")}.`);
			if (link.activationPolicy != null && !WIDGET_LINK_ACTIVATION_POLICIES.includes(link.activationPolicy)) issues.push(`workspaceSpec.links[${index}].activationPolicy must be one of: ${WIDGET_LINK_ACTIVATION_POLICIES.join(", ")}.`);
			if (link.effectConstraint != null && !WIDGET_LINK_EFFECT_CONSTRAINTS.includes(link.effectConstraint)) issues.push(`workspaceSpec.links[${index}].effectConstraint must be one of: ${WIDGET_LINK_EFFECT_CONSTRAINTS.join(", ")}.`);
			if (link.responseSpec != null) if (!isPlainObject$4(link.responseSpec)) issues.push(`workspaceSpec.links[${index}].responseSpec must be a plain object when provided.`);
			else {
				if (!link.responseSpec.kind || typeof link.responseSpec.kind !== "string") issues.push(`workspaceSpec.links[${index}].responseSpec.kind must be a non-empty string when responseSpec is provided.`);
				else if (!WIDGET_LINK_ADVANCED_RESPONSE_KINDS.includes(link.responseSpec.kind)) issues.push(`workspaceSpec.links[${index}].responseSpec.kind must be one of: ${WIDGET_LINK_ADVANCED_RESPONSE_KINDS.join(", ")}.`);
				if (link.responseSpec.params != null && !isPlainObject$4(link.responseSpec.params)) issues.push(`workspaceSpec.links[${index}].responseSpec.params must be a plain object when provided.`);
			}
		});
		return {
			ok: issues.length === 0,
			issues
		};
	}
	function getTitleText$1(title) {
		if (typeof title === "string") return title;
		if (title && typeof title === "object" && typeof title.text === "string") return title.text;
		return null;
	}
	function buildBaseWidgetPlan({ sessionId, spec }) {
		const widgetId = sessionId ? `session_${sessionId}` : "active_widget";
		return makeWorkspacePlanningResult({
			topology: "T1",
			widgets: [{
				widgetId,
				role: "primary",
				source: { kind: "baseSpec" }
			}],
			links: [],
			source: "runtime_default",
			planningMode: "minimal_default",
			primaryWidgetId: widgetId,
			title: getTitleText$1(spec?.title) || "Active Widget"
		});
	}
	function normalizeWorkspacePlan({ sessionId, spec, workspaceSpec, planningRequest = null }) {
		const validation = validateRequestedWorkspaceSpec(workspaceSpec);
		if (!workspaceSpec || !validation.ok || !Array.isArray(workspaceSpec.widgets) || workspaceSpec.widgets.length === 0) {
			const planned = planWorkspace({
				sessionId,
				spec,
				...makeWorkspacePlanningRequest(planningRequest || {})
			});
			return {
				plan: planned || buildBaseWidgetPlan({
					sessionId,
					spec
				}),
				validation,
				summary: summarizeRequestedWorkspaceSpec(workspaceSpec),
				planningSummary: planned || null,
				materializedFromSpec: false,
				materializedFromPlanner: !!planned
			};
		}
		const primaryWidgetId = workspaceSpec.widgets[0]?.widgetId;
		return {
			plan: makeWorkspacePlanningResult({
				topology: workspaceSpec.topology || "T1",
				widgets: workspaceSpec.widgets,
				links: Array.isArray(workspaceSpec.links) ? workspaceSpec.links : [],
				source: "workspace_spec",
				planningMode: "explicit_spec",
				primaryWidgetId,
				title: getTitleText$1(spec?.title) || "Workspace"
			}),
			validation,
			summary: summarizeRequestedWorkspaceSpec(workspaceSpec),
			planningSummary: null,
			materializedFromSpec: true,
			materializedFromPlanner: false
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/selectionHelpers.js
	function normalizePredicateValues(predicate) {
		if (!predicate) return [];
		return Array.isArray(predicate.value) ? predicate.value : [predicate.value];
	}
	function normalizeComparableValue$1(value) {
		if (value instanceof Date && Number.isFinite(value.getTime())) return {
			kind: "temporal",
			value: value.getTime()
		};
		if (typeof value === "number" && Number.isFinite(value)) return {
			kind: "numeric",
			value
		};
		if (typeof value === "string") {
			const parsedDate = Date.parse(value);
			if (Number.isFinite(parsedDate)) return {
				kind: "temporal",
				value: parsedDate
			};
			const parsedNumber = Number(value);
			if (Number.isFinite(parsedNumber)) return {
				kind: "numeric",
				value: parsedNumber
			};
		}
		return {
			kind: "raw",
			value
		};
	}
	function normalizeComparableValueForKind(value, expectedKind = "raw") {
		if (expectedKind === "temporal") {
			if (value instanceof Date && Number.isFinite(value.getTime())) return {
				kind: "temporal",
				value: value.getTime()
			};
			if (typeof value === "number" && Number.isFinite(value)) return {
				kind: "temporal",
				value
			};
			if (typeof value === "string") {
				const parsedDate = Date.parse(value);
				if (Number.isFinite(parsedDate)) return {
					kind: "temporal",
					value: parsedDate
				};
			}
			return {
				kind: "raw",
				value
			};
		}
		if (expectedKind === "numeric") {
			if (typeof value === "number" && Number.isFinite(value)) return {
				kind: "numeric",
				value
			};
			if (typeof value === "string") {
				const parsedNumber = Number(value);
				if (Number.isFinite(parsedNumber)) return {
					kind: "numeric",
					value: parsedNumber
				};
			}
			return {
				kind: "raw",
				value
			};
		}
		return normalizeComparableValue$1(value);
	}
	function comparableEquals(left, right) {
		const normalizedLeft = normalizeComparableValue$1(left);
		const normalizedRight = normalizeComparableValue$1(right);
		if (normalizedLeft.kind === normalizedRight.kind && normalizedLeft.kind !== "raw") return normalizedLeft.value === normalizedRight.value;
		return left === right;
	}
	function rowMatchesPredicate(row, predicate) {
		if (!predicate || !row || typeof row !== "object") return false;
		const field = predicate.field;
		if (typeof field !== "string" || field.length === 0) return false;
		const rowValue = row?.[field];
		if (predicate.op === "equals" || predicate.op === "eq") return comparableEquals(rowValue, predicate.value);
		if (predicate.op === "in") return normalizePredicateValues(predicate).some((value) => comparableEquals(rowValue, value));
		if (predicate.op === "notIn") return !normalizePredicateValues(predicate).some((value) => comparableEquals(rowValue, value));
		if (predicate.op === "between") {
			const range = normalizePredicateValues(predicate);
			if (range.length < 2) return false;
			const normalizedStart = normalizeComparableValue$1(range[0]);
			const normalizedEnd = normalizeComparableValue$1(range[1]);
			if (normalizedStart.kind === "raw" || normalizedEnd.kind === "raw" || normalizedStart.kind !== normalizedEnd.kind) return false;
			const normalizedRow = normalizeComparableValueForKind(rowValue, normalizedStart.kind);
			if (normalizedRow.kind === "raw" || normalizedRow.kind !== normalizedStart.kind) return false;
			const minValue = Math.min(normalizedStart.value, normalizedEnd.value);
			const maxValue = Math.max(normalizedStart.value, normalizedEnd.value);
			return normalizedRow.value >= minValue && normalizedRow.value <= maxValue;
		}
		if (predicate.op === "gte" || predicate.op === "greaterThanOrEqual" || predicate.op === "lte" || predicate.op === "lessThanOrEqual" || predicate.op === "gt" || predicate.op === "greaterThan" || predicate.op === "lt" || predicate.op === "lessThan") {
			const normalizedBoundary = normalizeComparableValue$1(predicate.value);
			const normalizedRow = normalizeComparableValueForKind(rowValue, normalizedBoundary.kind);
			if (normalizedBoundary.kind === "raw" || normalizedRow.kind === "raw" || normalizedRow.kind !== normalizedBoundary.kind) return false;
			if (predicate.op === "gte" || predicate.op === "greaterThanOrEqual") return normalizedRow.value >= normalizedBoundary.value;
			if (predicate.op === "lte" || predicate.op === "lessThanOrEqual") return normalizedRow.value <= normalizedBoundary.value;
			if (predicate.op === "gt" || predicate.op === "greaterThan") return normalizedRow.value > normalizedBoundary.value;
			return normalizedRow.value < normalizedBoundary.value;
		}
		return false;
	}
	function rowMatchesSelection(row, activeSelection) {
		const predicates = Array.isArray(activeSelection?.predicates) ? activeSelection.predicates : [];
		if (predicates.length === 0) return false;
		return predicates.every((predicate) => rowMatchesPredicate(row, predicate));
	}
	function countSelectedRows(rows, activeSelection) {
		if (!Array.isArray(rows) || rows.length === 0 || !activeSelection) return 0;
		return rows.filter((row) => rowMatchesSelection(row, activeSelection)).length;
	}
	function rowMatchesAnySelection(row, activeSelections) {
		const selections = Array.isArray(activeSelections) ? activeSelections.filter(Boolean) : [];
		if (selections.length === 0) return false;
		return selections.some((selection) => rowMatchesSelection(row, selection));
	}
	function countSelectedRowsForSelections(rows, activeSelections) {
		if (!Array.isArray(rows) || rows.length === 0) return 0;
		return rows.filter((row) => rowMatchesAnySelection(row, activeSelections)).length;
	}
	function mapSelectionToTargetSelection(activeSelection, fieldMapping = []) {
		if (!activeSelection) return null;
		const mappingIndex = new Map((Array.isArray(fieldMapping) ? fieldMapping : []).filter((entry) => typeof entry?.sourceField === "string" && typeof entry?.targetField === "string").map((entry) => [entry.sourceField, entry.targetField]));
		if (mappingIndex.size === 0) return activeSelection;
		const predicates = Array.isArray(activeSelection.predicates) ? activeSelection.predicates.map((predicate) => ({
			...predicate,
			field: mappingIndex.get(predicate.field) || predicate.field
		})) : [];
		const fields = Array.isArray(activeSelection.fields) ? activeSelection.fields.map((field) => mappingIndex.get(field) || field) : activeSelection.fields;
		const value = activeSelection.value && typeof activeSelection.value === "object" && !Array.isArray(activeSelection.value) ? Object.fromEntries(Object.entries(activeSelection.value).map(([field, fieldValue]) => [mappingIndex.get(field) || field, fieldValue])) : activeSelection.value;
		const field = typeof activeSelection.field === "string" ? mappingIndex.get(activeSelection.field) || activeSelection.field : activeSelection.field;
		const keyField = typeof activeSelection.keyField === "string" ? mappingIndex.get(activeSelection.keyField) || activeSelection.keyField : activeSelection.keyField;
		return {
			...activeSelection,
			...fields ? { fields } : {},
			...value ? { value } : {},
			...field ? { field } : {},
			...keyField ? { keyField } : {},
			predicates
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/transformHelpers.js
	function isPlainObject$3(value) {
		return !!value && typeof value === "object" && !Array.isArray(value);
	}
	function parseLiteralValue(rawValue) {
		if (typeof rawValue !== "string") return rawValue;
		const trimmed = rawValue.trim();
		if (trimmed.startsWith("'") && trimmed.endsWith("'") || trimmed.startsWith("\"") && trimmed.endsWith("\"")) return trimmed.slice(1, -1);
		if (trimmed === "true") return true;
		if (trimmed === "false") return false;
		if (trimmed === "null") return null;
		if (/^-?\d+(?:\.\d+)?(?:[eE][+-]?\d+)?$/.test(trimmed)) {
			const numeric = Number(trimmed);
			if (Number.isFinite(numeric)) return numeric;
		}
		return trimmed;
	}
	function extractPredicatesFromStringFilter(filter) {
		if (typeof filter !== "string" || filter.trim().length === 0) return [];
		const clauses = filter.split(/\s*&&\s*/u).map((entry) => entry.trim()).filter(Boolean);
		if (clauses.length === 0) return [];
		const predicates = [];
		for (const clause of clauses) {
			const match = clause.match(/^datum\.([A-Za-z_$][\w$]*)\s*(===|==)\s*(.+)$/u);
			if (!match) return [];
			const [, field, operator, rawValue] = match;
			if (operator !== "===" && operator !== "==" || typeof field !== "string" || field.length === 0) return [];
			predicates.push({
				field,
				op: "equals",
				value: parseLiteralValue(rawValue)
			});
		}
		return predicates;
	}
	function applyFoldTransform(rows, transform) {
		const fields = Array.isArray(transform?.fold) ? transform.fold.filter((field) => typeof field === "string" && field.length > 0) : [];
		if (fields.length === 0) return rows;
		const [keyField, valueField] = Array.isArray(transform?.as) && transform.as.length >= 2 ? transform.as : ["key", "value"];
		return rows.flatMap((row) => fields.map((field) => ({
			...row,
			[keyField]: field,
			[valueField]: row?.[field]
		})));
	}
	function extractPredicatesFromFilterTransform(transform) {
		const filter = transform?.filter;
		if (typeof filter === "string") return extractPredicatesFromStringFilter(filter);
		if (!isPlainObject$3(filter)) return [];
		if (typeof filter.field === "string" && Array.isArray(filter.oneOf) && filter.oneOf.length > 0) return [{
			field: filter.field,
			op: "in",
			value: [...filter.oneOf]
		}];
		if (typeof filter.field === "string" && Array.isArray(filter.notOneOf) && filter.notOneOf.length > 0) return [{
			field: filter.field,
			op: "notIn",
			value: [...filter.notOneOf]
		}];
		const nestedNot = filter?.not;
		if (isPlainObject$3(nestedNot) && typeof nestedNot.field === "string" && Array.isArray(nestedNot.oneOf) && nestedNot.oneOf.length > 0) return [{
			field: nestedNot.field,
			op: "notIn",
			value: [...nestedNot.oneOf]
		}];
		if (typeof filter.field === "string" && Array.isArray(filter.range) && filter.range.length === 2) return [{
			field: filter.field,
			op: "between",
			value: [...filter.range]
		}];
		if (typeof filter.field === "string" && Object.prototype.hasOwnProperty.call(filter, "equal")) return [{
			field: filter.field,
			op: "equals",
			value: filter.equal
		}];
		return [];
	}
	function extractPredicatesFromTransforms(transforms = []) {
		if (!Array.isArray(transforms) || transforms.length === 0) return [];
		return transforms.flatMap((transform) => extractPredicatesFromFilterTransform(transform));
	}
	function applySupportedSpecTransforms({ rows, transforms = [], dataQueryEngine }) {
		let nextRows = Array.isArray(rows) ? rows : [];
		for (const transform of Array.isArray(transforms) ? transforms : []) {
			const predicates = extractPredicatesFromFilterTransform(transform);
			if (predicates.length > 0) {
				nextRows = dataQueryEngine.filter(nextRows, predicates);
				continue;
			}
			if (Array.isArray(transform?.fold) && transform.fold.length > 0) {
				nextRows = applyFoldTransform(nextRows, transform);
				continue;
			}
			if (Array.isArray(transform?.aggregate) && transform.aggregate.length > 0) {
				const result = dataQueryEngine.aggregate(nextRows, {
					groupBy: Array.isArray(transform?.groupby) ? transform.groupby.filter(Boolean) : [],
					measures: transform.aggregate.map((measure) => ({
						op: measure?.op,
						field: measure?.field,
						as: measure?.as
					})).filter((measure) => typeof measure.op === "string" && typeof measure.as === "string")
				});
				nextRows = Array.isArray(result?.rows) ? result.rows : nextRows;
			}
		}
		return nextRows;
	}
	function inferTransformKind(transform) {
		if (extractPredicatesFromFilterTransform(transform).length > 0) return "filter";
		if (Array.isArray(transform?.aggregate) || transform?.joinaggregate) return "aggregate";
		if (Array.isArray(transform?.fold) && transform.fold.length > 0) return "derive";
		if (transform?.calculate) return "derive";
		if (transform?.sample) return "sample";
		return "derive";
	}
	function normalizeSpecTransforms(transforms = []) {
		if (!Array.isArray(transforms)) return [];
		return transforms.map((transform) => makeTransformState({
			kind: inferTransformKind(transform),
			source: "widgetSpec",
			spec: {
				...transform,
				predicates: extractPredicatesFromFilterTransform(transform)
			}
		}));
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/widgetStateBuilders.js
	function cloneValue$5(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeFieldEncoding(encoding) {
		return {
			field: "",
			type: "nominal",
			aggregate: void 0,
			bin: void 0,
			scale: void 0,
			...encoding
		};
	}
	function makeViewTransformState(view) {
		return {
			xDomain: void 0,
			yDomain: void 0,
			zoom: void 0,
			sort: void 0,
			highlight: void 0,
			drillDown: void 0,
			aggregate: void 0,
			reencode: void 0,
			addRemove: void 0,
			navigate: void 0,
			...view
		};
	}
	function readMarkType$1(widgetSpec) {
		if (typeof widgetSpec?.mark === "string") return widgetSpec.mark;
		if (widgetSpec?.mark && typeof widgetSpec.mark === "object" && typeof widgetSpec.mark.type === "string") return widgetSpec.mark.type;
		return null;
	}
	function normalizeSelections(activeSelectionOrSelections) {
		if (Array.isArray(activeSelectionOrSelections)) return activeSelectionOrSelections.filter(Boolean);
		return activeSelectionOrSelections ? [activeSelectionOrSelections] : [];
	}
	function selectionTargetsField(selections, field) {
		if (typeof field !== "string" || field.length === 0) return false;
		return selections.some((selection) => selection?.field === field || Array.isArray(selection?.predicates) && selection.predicates.some((predicate) => predicate?.field === field));
	}
	function readLineGroupingField(spec = {}) {
		return spec?.encoding?.color?.field || spec?.encoding?.detail?.field || null;
	}
	function escapeDatumField$1(field) {
		return String(field).replace(/\\/g, "\\\\").replace(/'/g, "\\'");
	}
	function isTemporalLikeValue$1(value) {
		if (value instanceof Date && Number.isFinite(value.getTime())) return true;
		if (typeof value === "string") {
			const parsed = Date.parse(value);
			return Number.isFinite(parsed);
		}
		return false;
	}
	function buildComparableExpression$1(value) {
		if (value instanceof Date && Number.isFinite(value.getTime())) return `toDate(${JSON.stringify(value.toISOString())})`;
		if (isTemporalLikeValue$1(value)) return `toDate(${JSON.stringify(value)})`;
		return JSON.stringify(value);
	}
	function buildDatumFieldExpression$1(field, predicate = null) {
		const datumRef = `datum['${escapeDatumField$1(field)}']`;
		const value = predicate?.value;
		return isTemporalLikeValue$1(Array.isArray(value) ? value[0] : value) ? `toDate(${datumRef})` : datumRef;
	}
	function buildPredicateExpression$1(predicate) {
		const field = typeof predicate?.field === "string" ? predicate.field : null;
		if (!field) return null;
		const datumRef = buildDatumFieldExpression$1(field, predicate);
		if (predicate.op === "equals" || predicate.op === "eq") return `${datumRef} === ${buildComparableExpression$1(predicate.value)}`;
		if (predicate.op === "in" && Array.isArray(predicate.value) && predicate.value.length > 0) return `indexof([${predicate.value.map((value) => buildComparableExpression$1(value)).join(", ")}], ${datumRef}) >= 0`;
		if (predicate.op === "between" && Array.isArray(predicate.value) && predicate.value.length >= 2) return `${datumRef} >= ${buildComparableExpression$1(predicate.value[0])} && ${datumRef} <= ${buildComparableExpression$1(predicate.value[1])}`;
		return null;
	}
	function buildSelectionConditionExpression(selections) {
		const clauses = normalizeSelections(selections).map((selection) => {
			const predicateClauses = (Array.isArray(selection?.predicates) ? selection.predicates : []).map((predicate) => buildPredicateExpression$1(predicate)).filter(Boolean);
			return predicateClauses.length > 0 ? `(${predicateClauses.join(" && ")})` : null;
		}).filter(Boolean);
		return clauses.length > 0 ? clauses.join(" || ") : null;
	}
	function removeRuntimeSelectionTransforms(transforms = []) {
		return (Array.isArray(transforms) ? transforms : []).filter((entry) => entry?._widgetvaRuntimeSelection !== true);
	}
	function applyRuntimeSelectionFilterToSpec(spec, selectionTest) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec) || !selectionTest) return spec;
		const nextSpec = cloneValue$5(spec);
		function attachFilter(node) {
			if (!node || typeof node !== "object" || Array.isArray(node)) return false;
			const hasInlineValues = Array.isArray(node?.data?.values);
			if (Boolean(node?.data) && typeof node.data === "object" && !Array.isArray(node.data) && !hasInlineValues) {
				node.transform = [{
					_widgetvaRuntimeSelection: true,
					filter: selectionTest
				}, ...removeRuntimeSelectionTransforms(node.transform)];
				return true;
			}
			if (node?.spec && typeof node.spec === "object" && !Array.isArray(node.spec) && attachFilter(node.spec)) return true;
			for (const key of [
				"layer",
				"concat",
				"vconcat",
				"hconcat"
			]) {
				if (!Array.isArray(node?.[key])) continue;
				for (const child of node[key]) if (attachFilter(child)) return true;
			}
			return false;
		}
		if (!attachFilter(nextSpec)) nextSpec.transform = [{
			_widgetvaRuntimeSelection: true,
			filter: selectionTest
		}, ...removeRuntimeSelectionTransforms(nextSpec.transform)];
		return nextSpec;
	}
	function buildLineXSelectionSpec(nextSpec) {
		const baseMark = typeof nextSpec?.mark === "string" ? {
			type: nextSpec.mark,
			point: false
		} : {
			...nextSpec?.mark || {},
			point: false
		};
		const rootEncoding = cloneValue$5(nextSpec?.encoding || {});
		const layeredSpec = {
			...nextSpec,
			encoding: rootEncoding,
			layer: [{
				mark: baseMark,
				encoding: { opacity: { value: .22 } }
			}, {
				transform: [{ filter: "datum.__widgetva_selected === true" }],
				mark: {
					type: "point",
					filled: true,
					size: 72,
					stroke: "#ffffff",
					strokeWidth: 1.25
				},
				encoding: { opacity: { value: 1 } }
			}]
		};
		delete layeredSpec.mark;
		return layeredSpec;
	}
	function buildEncodings(spec) {
		const enc = spec?.encoding || {};
		const normalized = {};
		for (const [channel, value] of Object.entries(enc)) {
			if (!value || typeof value !== "object") continue;
			normalized[channel] = makeFieldEncoding({
				field: value.field,
				type: value.type,
				aggregate: value.aggregate,
				bin: value.bin,
				scale: value.scale
			});
		}
		return normalized;
	}
	function buildHighlightFeedback({ link, activeSelections, existingFeedback }) {
		const values = normalizeSelections(activeSelections).flatMap((selection) => Array.isArray(selection?.predicates) ? selection.predicates : []).filter((predicate) => predicate.op === "equals" || predicate.op === "eq" || predicate.op === "in").flatMap((predicate) => Array.isArray(predicate.value) ? predicate.value : [predicate.value]);
		return {
			...makeInteractionFeedbackState(existingFeedback),
			highlightLinkIds: [...new Set([...existingFeedback?.highlightLinkIds || [], link.linkId])],
			highlightedKeys: [...new Set(values)]
		};
	}
	function getHighlightInfo({ link, activeSelections }) {
		const categoricalPredicates = normalizeSelections(activeSelections).flatMap((selection) => Array.isArray(selection?.predicates) ? selection.predicates : []).filter((predicate) => predicate.op === "equals" || predicate.op === "eq" || predicate.op === "in");
		if (categoricalPredicates.length === 0) return null;
		const mapping = Array.isArray(link?.fieldMapping) && link.fieldMapping.length > 0 ? link.fieldMapping[0] : null;
		const sourceField = mapping?.sourceField || categoricalPredicates[0].field;
		const targetField = mapping?.targetField || sourceField;
		const matchedPredicate = categoricalPredicates.find((predicate) => predicate.field === sourceField) || categoricalPredicates[0];
		return {
			sourceField,
			targetField,
			values: Array.isArray(matchedPredicate.value) ? matchedPredicate.value : [matchedPredicate.value]
		};
	}
	function buildLinkedViewState({ widgetSpec, inboundLinks, activeSelections }) {
		const viewState = {
			xDomain: widgetSpec?.encoding?.x?.scale?.domain,
			yDomain: widgetSpec?.encoding?.y?.scale?.domain
		};
		const selections = normalizeSelections(activeSelections);
		for (const link of inboundLinks) if (link.kind === "syncDomain") for (const activeSelection of selections) {
			if (!activeSelection?.domain) continue;
			if (Array.isArray(activeSelection.domain.xDomain)) viewState.xDomain = activeSelection.domain.xDomain;
			if (Array.isArray(activeSelection.domain.yDomain)) viewState.yDomain = activeSelection.domain.yDomain;
		}
		viewState.zoom = buildViewZoomState({
			widgetSpec,
			xDomain: viewState.xDomain,
			yDomain: viewState.yDomain
		});
		viewState.sort = buildViewSortState({ widgetSpec });
		viewState.highlight = buildViewHighlightState({ widgetSpec });
		viewState.drillDown = buildViewDrillDownState({ widgetSpec });
		viewState.aggregate = buildViewAggregateState({ widgetSpec });
		viewState.reencode = buildViewReencodeState({ widgetSpec });
		viewState.addRemove = buildViewAddRemoveState({ widgetSpec });
		viewState.navigate = buildViewNavigateState({ widgetSpec });
		Object.assign(viewState, buildViewFocusState({ widgetSpec }));
		return viewState;
	}
	function applyLinkedViewStateToSpec({ widgetSpec, inboundLinks, activeSelections }) {
		if (!widgetSpec?.encoding) return widgetSpec;
		const hasDomainSync = inboundLinks.some((link) => link.kind === "syncDomain");
		const selections = normalizeSelections(activeSelections).filter((selection) => selection?.domain);
		if (!hasDomainSync || selections.length === 0) return widgetSpec;
		const nextSpec = cloneValue$5(widgetSpec);
		const resolvedXDomain = [...selections].reverse().find((selection) => Array.isArray(selection?.domain?.xDomain))?.domain?.xDomain || null;
		const resolvedYDomain = [...selections].reverse().find((selection) => Array.isArray(selection?.domain?.yDomain))?.domain?.yDomain || null;
		if (Array.isArray(resolvedXDomain) && nextSpec.encoding?.x) nextSpec.encoding.x = {
			...nextSpec.encoding.x,
			scale: {
				...nextSpec.encoding.x.scale || {},
				domain: resolvedXDomain
			}
		};
		if (Array.isArray(resolvedYDomain) && nextSpec.encoding?.y) nextSpec.encoding.y = {
			...nextSpec.encoding.y,
			scale: {
				...nextSpec.encoding.y.scale || {},
				domain: resolvedYDomain
			}
		};
		return nextSpec;
	}
	function applyHighlightToSpec({ widgetSpec, inboundLinks, activeSelections }) {
		const highlightLink = inboundLinks.find((link) => link.kind === "highlight");
		if (!highlightLink || !Array.isArray(widgetSpec?.data?.values)) return widgetSpec;
		const highlightInfo = getHighlightInfo({
			link: highlightLink,
			activeSelections
		});
		if (!highlightInfo || highlightInfo.values.length === 0) return widgetSpec;
		const nextSpec = cloneValue$5(widgetSpec);
		nextSpec.data = { values: nextSpec.data.values.map((row) => ({
			...row,
			__widgetva_highlight: highlightInfo.values.includes(row?.[highlightInfo.targetField])
		})) };
		nextSpec.encoding = {
			...nextSpec.encoding || {},
			opacity: {
				condition: {
					test: "datum.__widgetva_highlight === true",
					value: .95
				},
				value: .18
			}
		};
		return nextSpec;
	}
	function applySelectionToSpec({ widgetSpec, activeSelections, selectionEnabled = false }) {
		const selections = normalizeSelections(activeSelections);
		if (!selectionEnabled || selections.length === 0) return widgetSpec;
		const nextSpec = cloneValue$5(widgetSpec);
		const markType = readMarkType$1(nextSpec);
		if (markType === "rect") {
			const selectionTest = buildSelectionConditionExpression(selections);
			if (!selectionTest) return nextSpec;
			nextSpec.encoding = {
				...nextSpec.encoding || {},
				opacity: {
					condition: {
						test: selectionTest,
						value: 1
					},
					value: .22
				}
			};
			return nextSpec;
		}
		if (!Array.isArray(nextSpec?.data?.values)) {
			const selectionTest = buildSelectionConditionExpression(selections);
			if (!selectionTest) return widgetSpec;
			return applyRuntimeSelectionFilterToSpec(nextSpec, selectionTest);
		}
		const selectedRows = nextSpec.data.values.map((row) => ({
			...row,
			__widgetva_selected: rowMatchesAnySelection(row, selections)
		}));
		nextSpec.data = {
			...nextSpec.data || {},
			values: selectedRows
		};
		const xField = nextSpec?.encoding?.x?.field || null;
		if (markType === "line" && selectionTargetsField(selections, xField)) return buildLineXSelectionSpec(nextSpec);
		const lineGroupingField = readLineGroupingField(nextSpec);
		if (markType === "line" && selectionTargetsField(selections, lineGroupingField)) {
			const selectionTest = buildSelectionConditionExpression(selections);
			if (!selectionTest) return nextSpec;
			nextSpec.encoding = {
				...nextSpec.encoding || {},
				opacity: {
					condition: {
						test: selectionTest,
						value: .98
					},
					value: .18
				},
				strokeWidth: {
					condition: {
						test: selectionTest,
						value: 3.5
					},
					value: 1.2
				}
			};
			return nextSpec;
		}
		const nextEncoding = {
			...nextSpec.encoding || {},
			opacity: {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: .98
				},
				value: .22
			}
		};
		if (markType === "bar") {
			const barSelectionTest = buildSelectionConditionExpression(selections) || "datum.__widgetva_selected === true";
			nextEncoding.stroke = {
				condition: {
					test: barSelectionTest,
					value: "#1f4f82"
				},
				value: null
			};
			nextEncoding.strokeWidth = {
				condition: {
					test: barSelectionTest,
					value: 2
				},
				value: 0
			};
			nextEncoding.opacity = {
				condition: {
					test: barSelectionTest,
					value: .98
				},
				value: .22
			};
		}
		if (markType === "rect") nextEncoding.opacity = {
			condition: {
				test: "datum.__widgetva_selected === true",
				value: 1
			},
			value: .22
		};
		if (markType === "point" || markType === "circle") {
			nextEncoding.opacity = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: .98
				},
				value: .38
			};
			nextEncoding.strokeOpacity = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: 1
				},
				value: .24
			};
			nextEncoding.strokeWidth = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: 1.8
				},
				value: .6
			};
			nextEncoding.fill = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: "#4c78a8"
				},
				value: "#4c78a8"
			};
			nextEncoding.fillOpacity = {
				condition: {
					test: "datum.__widgetva_selected === true",
					value: .9
				},
				value: .12
			};
		}
		nextSpec.encoding = nextEncoding;
		return nextSpec;
	}
	function buildTransforms({ widgetSpec, inboundLinks }) {
		const specTransforms = normalizeSpecTransforms(widgetSpec?.transform);
		const sankeyFilterState = widgetSpec?._sankey_filter_state;
		if (sankeyFilterState && typeof sankeyFilterState === "object") specTransforms.push(makeTransformState({
			kind: "filter",
			source: "widgetSpec",
			spec: {
				mode: sankeyFilterState.mode || "threshold",
				minValue: Number.isFinite(sankeyFilterState.min_value) ? sankeyFilterState.min_value : null,
				sourceAction: sankeyFilterState.source_action || null
			}
		}));
		return [...specTransforms, ...inboundLinks.filter((link) => link.kind === "filter" || link.kind === "syncDomain").map((link) => makeTransformState({
			kind: link.kind,
			sourceWidgetId: link.sourceWidgetId,
			linkId: link.linkId,
			spec: {
				linkRef: link.ref || null,
				sourceWidgetId: link.sourceWidgetId,
				targetWidgetId: link.targetWidgetId
			}
		}))];
	}
	function buildWidgetState({ widgetRef, widgetId, role, kind, updatedAt, sourceDataRef, currentDataRef, rowCount, visibleCount, selectedCount, widgetSpec, inboundLinks, activeSelections, widgetSelectionRefs, mirroredSelectionEntries, humanInteraction, linkedRefs, existingFeedback }) {
		const inboundHighlightLink = inboundLinks.find((link) => link.kind === "highlight");
		const inboundSharedSelectionLink = inboundLinks.find((link) => link.kind === "sharesSelection");
		const normalizedSelections = normalizeSelections(activeSelections);
		const feedback = inboundHighlightLink ? buildHighlightFeedback({
			link: inboundHighlightLink,
			activeSelections: normalizedSelections,
			existingFeedback
		}) : {
			...makeInteractionFeedbackState(existingFeedback),
			inboundLinkIds: inboundLinks.length > 0 ? inboundLinks.map((link) => link.linkId) : makeInteractionFeedbackState(existingFeedback).inboundLinkIds,
			linkedSourceRefs: linkedRefs.length > 0 ? linkedRefs : makeInteractionFeedbackState(existingFeedback).linkedSourceRefs,
			...inboundSharedSelectionLink ? { sharedSelectionSourceWidgetId: inboundSharedSelectionLink.sourceWidgetId } : {}
		};
		const selectionStateMap = {};
		for (const entry of Array.isArray(widgetSelectionRefs) ? widgetSelectionRefs : []) {
			if (!entry?.ref || !entry?.selection) continue;
			selectionStateMap[entry.ref] = makeSelectionState(buildSelectionStateInput(entry.selection));
		}
		for (const entry of Array.isArray(mirroredSelectionEntries) ? mirroredSelectionEntries : []) {
			if (!entry?.ref || !entry?.selection) continue;
			selectionStateMap[entry.ref] = makeSelectionState(buildSelectionStateInput(entry.selection));
		}
		const state = makeWidgetState({
			ref: widgetRef,
			widgetId,
			role,
			kind,
			version: 1,
			updatedAt,
			data: {
				sourceDataRef,
				currentDataRef,
				rowCount,
				visibleCount,
				selectedCount: countSelectedRowsForSelections(widgetSpec?.data?.values || [], [...Array.isArray(widgetSelectionRefs) ? widgetSelectionRefs.map((entry) => entry?.selection) : [], ...Array.isArray(mirroredSelectionEntries) ? mirroredSelectionEntries.map((entry) => entry?.selection) : []]) || selectedCount
			},
			encodings: buildEncodings(widgetSpec),
			transforms: buildTransforms({
				widgetSpec,
				inboundLinks
			}),
			view: makeViewTransformState(buildLinkedViewState({
				widgetSpec,
				inboundLinks,
				activeSelections: normalizedSelections
			})),
			selections: selectionStateMap,
			feedback: makeInteractionFeedbackState(feedback),
			rawSpec: widgetSpec,
			humanInteraction
		});
		if (!state.feedback.inboundLinkIds) state.feedback = {
			...state.feedback || {},
			inboundLinkIds: inboundLinks.map((link) => link.linkId),
			linkedSourceRefs: linkedRefs
		};
		return state;
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/sharedStateDerivation.js
	function uniquePredicates(predicates = []) {
		const seen = /* @__PURE__ */ new Set();
		const nextPredicates = [];
		for (const predicate of Array.isArray(predicates) ? predicates : []) {
			const key = JSON.stringify(predicate);
			if (seen.has(key)) continue;
			seen.add(key);
			nextPredicates.push(predicate);
		}
		return nextPredicates;
	}
	function uniqueValues$1(values = []) {
		return (Array.isArray(values) ? values : []).filter((value, index, list) => {
			const key = JSON.stringify(value);
			return list.findIndex((candidate) => JSON.stringify(candidate) === key) === index;
		});
	}
	function resolveLinkedSelectionPredicates(transform, activeSelections = {}) {
		const sourceSelectionRef = transform?.source || transform?.sourceSelectionRef || transform?.spec?.sourceSelectionRef || null;
		if (sourceSelectionRef && Array.isArray(activeSelections?.[sourceSelectionRef]?.predicates)) return activeSelections[sourceSelectionRef].predicates;
		const sourceWidgetId = transform?.sourceWidgetId || transform?.spec?.sourceWidgetId || null;
		if (!sourceWidgetId) return [];
		return Object.entries(activeSelections || {}).filter(([selectionRef, selectionState]) => typeof selectionRef === "string" && selectionRef.includes(`/widget/${sourceWidgetId}/selection/`) && Array.isArray(selectionState?.predicates)).flatMap(([, selectionState]) => selectionState.predicates);
	}
	function deriveHighlightStateFromSelection(primarySelection = null, widgets = []) {
		if (!primarySelection?.sourceWidgetId) return {
			entries: [],
			activeWidgetRefs: []
		};
		const predicates = uniquePredicates(Array.isArray(primarySelection?.predicates) ? primarySelection.predicates.map((predicate) => ({ ...predicate })) : []);
		const highlightedKeys = uniqueValues$1(predicates.filter((predicate) => (predicate?.op === "equals" || predicate?.op === "eq") && predicate?.value != null).map((predicate) => predicate.value));
		const activeWidgetRefs = (Array.isArray(widgets) ? widgets : []).map((widget) => widget?.resolveWidgetRef?.() || widget?.describe?.()?.ref || null).filter(Boolean);
		const sourceWidget = (Array.isArray(widgets) ? widgets : []).find((widget) => (widget?.resolveWidgetId?.() || widget?.describe?.()?.widgetId || null) === primarySelection.sourceWidgetId);
		const sourceWidgetRef = sourceWidget?.resolveWidgetRef?.() || sourceWidget?.describe?.()?.ref || primarySelection?.sourceWidgetRef || null;
		return {
			entries: (Array.isArray(widgets) ? widgets : []).map((widget) => ({
				widgetRef: widget?.resolveWidgetRef?.() || widget?.describe?.()?.ref || null,
				widgetId: widget?.resolveWidgetId?.() || widget?.describe?.()?.widgetId || null,
				sourceWidgetRef,
				sourceWidgetId: primarySelection.sourceWidgetId,
				selectionRef: primarySelection.selectionRef || null,
				summary: primarySelection.summary || null,
				predicates,
				highlightedKeys,
				linkedSourceRefs: primarySelection.selectionRef ? [primarySelection.selectionRef] : []
			})),
			activeWidgetRefs
		};
	}
	function clampNumericRange(range = [], domain = null) {
		if (!Array.isArray(range) || range.length !== 2) return null;
		const start = Number(range[0]);
		const end = Number(range[1]);
		if (!Number.isFinite(start) || !Number.isFinite(end)) return null;
		let min = Math.min(start, end);
		let max = Math.max(start, end);
		if (Array.isArray(domain) && domain.length === 2) {
			const domainStart = Number(domain[0]);
			const domainEnd = Number(domain[1]);
			if (Number.isFinite(domainStart) && Number.isFinite(domainEnd)) {
				const domainMin = Math.min(domainStart, domainEnd);
				const domainMax = Math.max(domainStart, domainEnd);
				min = Math.max(min, domainMin);
				max = Math.min(max, domainMax);
			}
		}
		return min <= max ? [min, max] : null;
	}
	function deriveGlobalFiltersFromSelection(primarySelection = null, { rangeDomains = {} } = {}) {
		const predicates = Array.isArray(primarySelection?.predicates) ? primarySelection.predicates : [];
		if (predicates.length === 0) return null;
		const globalFilters = {};
		for (const predicate of predicates) {
			if (!predicate || typeof predicate !== "object") return null;
			if (predicate.field === "origin" && (predicate.op === "equals" || predicate.op === "eq") && typeof predicate.value === "string") {
				globalFilters.origin = predicate.value;
				continue;
			}
			if (predicate.field === "year" && (predicate.op === "equals" || predicate.op === "eq") && Number.isFinite(predicate.value)) {
				globalFilters.year = Number(predicate.value);
				continue;
			}
			if (predicate.field === "cylinders" && (predicate.op === "equals" || predicate.op === "eq") && Number.isFinite(predicate.value)) {
				globalFilters.cylinders = [Number(predicate.value)];
				continue;
			}
			if (predicate.field === "horsepower" && predicate.op === "between") {
				const range = clampNumericRange(predicate.value, rangeDomains?.horsepower || null);
				if (!range) return null;
				globalFilters.horsepowerRange = range;
				continue;
			}
			return null;
		}
		return Object.keys(globalFilters).length > 0 ? globalFilters : null;
	}
	function deriveGlobalFiltersFromState(state) {
		const widgets = state?.widgets || {};
		const activeSelections = readSelectionRegistry(state?.shared || {});
		const globalFilters = {};
		for (const [widgetRef, widgetState] of Object.entries(widgets)) {
			const transforms = Array.isArray(widgetState?.transforms) ? widgetState.transforms : [];
			const transformPredicates = extractPredicatesFromTransforms(transforms.map((transform) => transform?.spec || transform));
			const linkedPredicates = transforms.filter((transform) => transform?.kind === "filter").flatMap((transform) => resolveLinkedSelectionPredicates(transform, activeSelections));
			const predicates = uniquePredicates([...transformPredicates, ...linkedPredicates]);
			if (predicates.length > 0) globalFilters[widgetRef] = predicates;
		}
		return globalFilters;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/summaries/summarizeWidgetAdapter.js
	function clone$28(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeWidgetAdapterProviderCapabilities(capabilities = {}) {
		return {
			supportedWidgetKinds: [],
			renderStrategy: "custom",
			stateApplyStrategy: "custom",
			interactionBindingStrategy: "custom",
			supportsRendererMount: false,
			supportsRendererUpdate: false,
			supportsRendererDispose: false,
			supportsSignalPatching: false,
			supportsOptionMerging: false,
			supportsImperativeRender: false,
			supportsPointSelection: false,
			supportsIntervalSelection: false,
			supportsZoomPan: false,
			supportsFocusReadback: false,
			supportsSelectionReadback: false,
			supportsViewportReadback: false,
			supportsHighlightProjection: false,
			supportsInteractionEvents: false,
			...capabilities
		};
	}
	function makeWidgetAdapterHumanInteraction(interaction = {}) {
		return {
			mode: "none",
			actionName: null,
			supportsDirectManipulation: false,
			...interaction
		};
	}
	function makeWidgetAdapterCapabilities(capabilities = {}) {
		return {
			canDescribe: false,
			canReadState: false,
			canApplyState: false,
			canMount: false,
			canUpdate: false,
			canDisposeRenderer: false,
			canBindHumanInteractions: false,
			canReadSelection: false,
			canReadViewport: false,
			canRegisterActions: false,
			canRegisterPerceptionQueries: false,
			...capabilities
		};
	}
	function makeWidgetAdapterSummary(summary = {}) {
		return {
			widgetRef: null,
			dataRef: null,
			kind: null,
			title: "",
			description: "",
			analyticRoles: [],
			primaryDataRef: null,
			provider: "custom",
			providerCapabilities: makeWidgetAdapterProviderCapabilities(),
			role: null,
			sourceKind: null,
			supportsSpecMutation: false,
			metadata: {},
			usageNotes: [],
			actionNames: [],
			perceptionQueryNames: [],
			humanInteraction: makeWidgetAdapterHumanInteraction(),
			capabilities: makeWidgetAdapterCapabilities(),
			...summary,
			providerCapabilities: makeWidgetAdapterProviderCapabilities(summary?.providerCapabilities),
			humanInteraction: makeWidgetAdapterHumanInteraction(summary?.humanInteraction),
			capabilities: makeWidgetAdapterCapabilities(summary?.capabilities)
		};
	}
	function summarizeWidgetAdapter(adapter) {
		if (!adapter) return null;
		const humanInteraction = adapter.getHumanInteractionConfig?.() || null;
		const description = adapter.getDescription?.() || null;
		return makeWidgetAdapterSummary({
			widgetRef: adapter.widgetRef,
			dataRef: adapter.dataRef || null,
			kind: adapter.kind || description?.kind || null,
			title: description?.title || adapter.title || "",
			description: description?.description || adapter.description || "",
			analyticRoles: Array.isArray(description?.analyticRoles) ? [...description.analyticRoles] : Array.isArray(adapter.analyticRoles) ? [...adapter.analyticRoles] : [],
			primaryDataRef: description?.primaryDataRef || adapter.primaryDataRef || null,
			provider: adapter.provider || "custom",
			providerCapabilities: makeWidgetAdapterProviderCapabilities(clone$28(adapter.providerCapabilities || {})),
			role: description?.role || adapter.role || null,
			sourceKind: description?.sourceKind || adapter.sourceKind || null,
			supportsSpecMutation: description?.supportsSpecMutation === true || adapter.supportsSpecMutation === true,
			metadata: clone$28(adapter.metadata || {}),
			usageNotes: Array.isArray(description?.usageNotes) ? [...description.usageNotes] : Array.isArray(adapter.usageNotes) ? [...adapter.usageNotes] : [],
			actionNames: Array.isArray(description?.actionNames) ? [...description.actionNames] : Array.isArray(adapter.actionNames) ? [...adapter.actionNames] : [],
			perceptionQueryNames: Array.isArray(description?.perceptionQueryNames) ? [...description.perceptionQueryNames] : Array.isArray(adapter.perceptionQueryNames) ? [...adapter.perceptionQueryNames] : [],
			humanInteraction: makeWidgetAdapterHumanInteraction(clone$28(humanInteraction || adapter.humanInteraction || null)),
			capabilities: makeWidgetAdapterCapabilities({
				canDescribe: typeof adapter.getDescription === "function",
				canReadState: typeof adapter.getState === "function",
				canApplyState: typeof adapter.applyState === "function",
				canMount: typeof adapter.mount === "function",
				canUpdate: typeof adapter.update === "function",
				canDisposeRenderer: typeof adapter.dispose === "function",
				canBindHumanInteractions: typeof adapter.bindHumanInteractions === "function",
				canReadSelection: typeof adapter.readSelection === "function",
				canReadViewport: typeof adapter.readViewport === "function",
				canRegisterActions: typeof adapter.registerActions === "function",
				canRegisterPerceptionQueries: typeof adapter.registerPerceptionQueries === "function"
			})
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/schemas/planning-constants.schema.js
	var WORKSPACE_PLANNING_TOPOLOGIES = [
		"T1",
		"T2",
		"T3",
		"T4",
		"T5",
		"T6"
	];
	var WORKSPACE_PLANNING_BUDGETS = [
		"minimal",
		"standard",
		"extended"
	];
	var WORKSPACE_PLANNING_RUN_MODES = [
		"goal_oriented",
		"open_ended",
		"autonomous"
	];
	var WORKSPACE_PLAN_SOURCES = [
		"planner",
		"runtime_default",
		"workspace_spec"
	];
	var WORKSPACE_PLAN_MODES = [
		"topology_driven",
		"minimal_default",
		"explicit_spec"
	];
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/WorkspaceMaterializer.js
	var dataQueryEngine = createDataQueryEngine({ kind: "js_array" });
	function cloneValue$4(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function getInlineRows(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const rows = spec?.data?.values;
		if (Array.isArray(rows)) return rows;
		if (Array.isArray(spec?.data)) {
			const namedValues = spec.data.map((entry) => Array.isArray(entry?.values) ? entry.values : null).find((entry) => Array.isArray(entry) && entry.length > 0);
			if (Array.isArray(namedValues)) return namedValues;
		}
		if (spec.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) {
			const nestedRows = getInlineRows(spec.spec);
			if (nestedRows.length > 0) return nestedRows;
		}
		const compositeChildren = [
			...Array.isArray(spec?.vconcat) ? spec.vconcat : [],
			...Array.isArray(spec?.hconcat) ? spec.hconcat : [],
			...Array.isArray(spec?.concat) ? spec.concat : [],
			...Array.isArray(spec?.layer) ? spec.layer : []
		];
		for (const child of compositeChildren) {
			const childRows = getInlineRows(child);
			if (childRows.length > 0) return childRows;
		}
		return [];
	}
	function getTitleText(title) {
		if (typeof title === "string") return title;
		if (title && typeof title === "object" && typeof title.text === "string") return title.text;
		return null;
	}
	function isSelectionPayload(selection) {
		return selection != null && typeof selection === "object" && !Array.isArray(selection);
	}
	function normalizeSelectionPayloadEntries({ selections, fallbackSelection }) {
		const entries = [];
		const pushSelection = (selection, fallbackKey = null) => {
			if (!isSelectionPayload(selection)) return;
			const sourceWidgetId = selection.source_widget_id || null;
			const selectionId = selection.selection_id || selection.id || null;
			if (!sourceWidgetId || !selectionId) return;
			entries.push([
				fallbackKey || `${sourceWidgetId}::${selectionId}`,
				selection,
				{
					sourceWidgetId,
					selectionId
				}
			]);
		};
		if (selections && typeof selections === "object" && !Array.isArray(selections)) for (const [key, value] of Object.entries(selections)) pushSelection(value, key || null);
		if (entries.length === 0 && isSelectionPayload(fallbackSelection)) pushSelection(fallbackSelection);
		return entries;
	}
	function buildActiveSelectionMap({ appId, workspaceId, selections, fallbackSelection }) {
		return Object.fromEntries(normalizeSelectionPayloadEntries({
			selections,
			fallbackSelection
		}).map(([, selection, selectionMeta]) => [makeSelectionRef({
			appId,
			workspaceId,
			widgetId: selectionMeta.sourceWidgetId,
			selectionId: selectionMeta.selectionId
		}), makeSelectionState(buildSelectionStateInput(selection))]));
	}
	function buildActiveSelectionEntries({ appId, workspaceId, selections, fallbackSelection }) {
		return normalizeSelectionPayloadEntries({
			selections,
			fallbackSelection
		}).map(([, selection, selectionMeta]) => ({
			ref: makeSelectionRef({
				appId,
				workspaceId,
				widgetId: selectionMeta.sourceWidgetId,
				selectionId: selectionMeta.selectionId
			}),
			widgetId: selectionMeta.sourceWidgetId,
			selectionId: selectionMeta.selectionId,
			selection
		}));
	}
	function resolvePrimarySelection({ selection, selections, preferredWidgetId = null }) {
		if (isSelectionPayload(selection)) return selection;
		const entries = normalizeSelectionPayloadEntries({
			selections,
			fallbackSelection: null
		});
		if (entries.length === 0) return null;
		if (preferredWidgetId) {
			const preferredEntry = entries.find(([, , selectionMeta]) => selectionMeta?.sourceWidgetId === preferredWidgetId);
			if (preferredEntry?.[1]) return preferredEntry[1];
		}
		if (entries.length === 1) return entries[0]?.[1] || null;
		return entries[entries.length - 1]?.[1] || null;
	}
	function buildPlanningTaskContext({ planningRequest, coordinationScope, targetWidgetRefs }) {
		const task = planningRequest?.task || null;
		const runMode = planningRequest?.runMode || null;
		const complexityBudget = planningRequest?.complexityBudget || null;
		const userIntent = planningRequest?.userIntent || null;
		const userQuery = task?.userQuery || userIntent || null;
		const expectedAnswerType = task?.answerType || task?.expectedAnswerType || null;
		const taskMode = task?.taskMode || runMode || "goal_oriented";
		return {
			taskId: task?.taskId || null,
			userQuery,
			taskMode,
			runMode,
			coordinationScope: task?.coordinationScope || coordinationScope,
			expectedAnswerType,
			interactionHorizon: task?.interactionHorizon || null,
			evidenceType: task?.evidenceType || null,
			complexityBudget,
			targetWidgetRefs
		};
	}
	function inferWidgetKind(spec) {
		if (spec?.kind === "parallelCoordinates") return "parallelCoordinates";
		if (spec?.kind === "sankey") return "sankey";
		if (spec?.kind === "map") return "map";
		const mark = typeof spec?.mark === "string" ? spec.mark : spec?.mark?.type;
		if (mark === "bar") return "bar";
		if (mark === "line") return "line";
		if (mark === "point" || mark === "circle") return "scatter";
		if (mark === "rect") return "heatmap";
		return "custom";
	}
	function inferAnalyticRoles(kind) {
		switch (kind) {
			case "scatter": return [
				"correlate",
				"cluster",
				"outlier",
				"distribution"
			];
			case "bar": return [
				"compare",
				"rank",
				"distribution"
			];
			case "line": return ["trend", "compare"];
			case "heatmap": return [
				"distribution",
				"outlier",
				"compare"
			];
			case "parallelCoordinates": return [
				"compare",
				"outlier",
				"distribution"
			];
			case "sankey": return ["flow", "compare"];
			case "map": return [
				"geoPattern",
				"compare",
				"distribution"
			];
			default: return ["lookup"];
		}
	}
	function aggregateRows(rows, transform) {
		return dataQueryEngine.aggregate(rows, {
			groupBy: Array.isArray(transform?.groupBy) ? transform.groupBy.filter(Boolean) : [],
			measures: Array.isArray(transform?.metrics) ? transform.metrics : [],
			sortBy: transform?.sortBy,
			limit: transform?.limit
		}).rows;
	}
	function buildLinkRef({ appId, workspaceId, linkId }) {
		return `wl://${appId}/workspace/${workspaceId}/link/${linkId}`;
	}
	function uniqueRecognizedKinds(kinds = []) {
		return [...new Set((Array.isArray(kinds) ? kinds : []).filter((kind) => typeof kind === "string" && kind.length > 0))];
	}
	function buildSpecFromSource({ source, baseSpec, rows }) {
		if (source?.kind === "baseSpec") {
			const nextSpec = JSON.parse(JSON.stringify(baseSpec));
			nextSpec.data = { values: rows };
			return nextSpec;
		}
		if (source?.kind === "nativeArtifact" && source?.providerSpec?.spec) {
			const nextSpec = JSON.parse(JSON.stringify(source.providerSpec.spec));
			if (Array.isArray(rows) && rows.length > 0) nextSpec.data = {
				...nextSpec.data || {},
				values: rows
			};
			return nextSpec;
		}
		if (source?.kind === "templateSpec" && source.spec) {
			const nextSpec = JSON.parse(JSON.stringify(source.spec));
			if (Array.isArray(rows) && rows.length > 0) nextSpec.data = {
				...nextSpec.data || {},
				values: rows
			};
			return nextSpec;
		}
		return null;
	}
	function getSourceTransforms({ widgetDef, baseSpec }) {
		if (widgetDef?.source?.kind === "baseSpec") return Array.isArray(baseSpec?.transform) ? baseSpec.transform : [];
		if (widgetDef?.source?.kind === "templateSpec") return Array.isArray(widgetDef?.source?.spec?.transform) ? widgetDef.source.spec.transform : [];
		if (widgetDef?.source?.kind === "nativeArtifact") return Array.isArray(widgetDef?.source?.providerSpec?.spec?.transform) ? widgetDef.source.providerSpec.spec.transform : [];
		return [];
	}
	function readSpecFromWidgetSource(source = null) {
		if (source?.kind === "nativeArtifact" && source?.providerSpec?.spec) return source.providerSpec.spec;
		if (source?.kind === "templateSpec" && source?.spec) return source.spec;
		return null;
	}
	function readMaterializationRootSpec({ spec, workspaceSpec }) {
		if (spec && typeof spec === "object" && !Array.isArray(spec)) return spec;
		return (Array.isArray(workspaceSpec?.widgets) ? workspaceSpec.widgets : []).map((widgetDef) => readSpecFromWidgetSource(widgetDef?.source)).find((candidate) => candidate && typeof candidate === "object" && !Array.isArray(candidate)) || null;
	}
	function materializeRows({ baseRows, widgetDef, inboundLinks, activeSelections, sourceTransforms = [] }) {
		let rows = applySupportedSpecTransforms({
			rows: baseRows,
			transforms: sourceTransforms,
			dataQueryEngine
		});
		const filteringLinks = inboundLinks.filter((link) => link.kind === "filter");
		if (filteringLinks.length > 0 && Array.isArray(activeSelections) && activeSelections.length > 0) for (const link of filteringLinks) {
			const relevantSelections = activeSelections.filter((entry) => entry?.widgetId === link.sourceWidgetId);
			for (const selectionEntry of relevantSelections) {
				const mappedSelection = mapSelectionToTargetSelection(selectionEntry.selection, link.fieldMapping);
				rows = dataQueryEngine.filter(rows, mappedSelection?.predicates || []);
			}
		}
		const transforms = Array.isArray(widgetDef?.dataBinding?.transforms) ? widgetDef.dataBinding.transforms : [];
		for (const transform of transforms) if (transform?.kind === "aggregate") rows = aggregateRows(rows, transform);
		return rows;
	}
	function materializeWorkspace({ appId = "widgetva-app", workspaceId = "main", sessionId, spec, workspaceSpec = null, planningRequest = null, replayContext = null, selection, selections = null, focusedWidgetRef = null, focusState = null, highlightState = null, viewportState = null, previousState = null, currentBranchId = "main", stateManager = null }) {
		const materializationSpec = readMaterializationRootSpec({
			spec,
			workspaceSpec
		});
		const { plan, validation: workspaceSpecValidation, summary: workspaceSpecSummary, planningSummary, materializedFromSpec, materializedFromPlanner } = normalizeWorkspacePlan({
			sessionId,
			spec: materializationSpec,
			workspaceSpec,
			planningRequest
		});
		const workspaceSpecStatus = !workspaceSpecSummary ? materializedFromPlanner ? "planned" : "idle" : workspaceSpecValidation.ok ? materializedFromSpec ? "materialized" : "deferred" : "invalid";
		if (!materializationSpec || typeof materializationSpec !== "object" || Array.isArray(materializationSpec)) return {
			description: {
				appId,
				workspaceId,
				generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
				workspaceCapabilities: ["singleWidgetAnalysis"],
				taskContext: null,
				planning: {
					requestedWorkspaceSpec: workspaceSpecSummary,
					planningRequest: planningRequest || null,
					workspaceSpecStatus,
					workspaceSpecIssues: workspaceSpecValidation.issues,
					materializedFromSpec,
					materializedFromPlanner,
					planner: planningSummary
				},
				widgets: [],
				dataHandles: [],
				links: [],
				actions: [],
				perceptionQueries: []
			},
			state: {
				stateId: `${workspaceId}:empty`,
				createdAt: (/* @__PURE__ */ new Date()).toISOString(),
				widgets: {},
				shared: {
					selections: {
						registry: {},
						views: {
							primary: null,
							byWidget: {}
						}
					},
					globalFilters: {}
				}
			},
			replayContext
		};
		const dataHandle = buildDataHandle({
			appId,
			workspaceId,
			spec: materializationSpec
		});
		const baseRows = getInlineRows(materializationSpec);
		const runtimeData = { [dataHandle.ref]: {
			ref: dataHandle.ref,
			rows: baseRows,
			handle: dataHandle
		} };
		const primarySelection = resolvePrimarySelection({
			selection,
			selections,
			preferredWidgetId: plan.primaryWidgetId || null
		});
		const activeSelections = buildActiveSelectionMap({
			appId,
			workspaceId,
			selections,
			fallbackSelection: primarySelection
		});
		const activeSelectionEntries = buildActiveSelectionEntries({
			appId,
			workspaceId,
			selections,
			fallbackSelection: primarySelection
		});
		const normalizedViewportState = normalizeViewportState(viewportState);
		const selectionSourceWidgetId = primarySelection?.source_widget_id || plan.primaryWidgetId;
		const selectionRef = primarySelection?.selection_id && selectionSourceWidgetId ? makeSelectionRef({
			appId,
			workspaceId,
			widgetId: selectionSourceWidgetId,
			selectionId: primarySelection.selection_id
		}) : null;
		const widgets = {};
		const widgetDescriptions = [];
		const widgetAdapters = [];
		const actionDescriptors = [];
		const perceptionDescriptors = [];
		const dataHandles = [dataHandle];
		const structuralLinks = [];
		const widgetRefsById = /* @__PURE__ */ new Map();
		for (const widgetDef of plan.widgets) {
			const widgetRef = makeWidgetRef({
				appId,
				workspaceId,
				widgetId: widgetDef.widgetId
			});
			widgetRefsById.set(widgetDef.widgetId, widgetRef);
		}
		for (const widgetDef of plan.widgets) {
			const widgetRef = widgetRefsById.get(widgetDef.widgetId);
			const inboundLinks = (plan.links || []).filter((link) => link.targetWidgetId === widgetDef.widgetId);
			const localSelectionEntries = activeSelectionEntries.filter((entry) => entry.widgetId === widgetDef.widgetId);
			const mirroredSelectionEntries = inboundLinks.filter((link) => link.kind === "sharesSelection").flatMap((link) => activeSelectionEntries.filter((entry) => entry.widgetId === link.sourceWidgetId).map((entry) => ({
				ref: makeSelectionRef({
					appId,
					workspaceId,
					widgetId: widgetDef.widgetId,
					selectionId: `shared_from_${link.sourceWidgetId}_${entry.selectionId}`
				}),
				selection: mapSelectionToTargetSelection(entry.selection, link.fieldMapping),
				sourceWidgetId: link.sourceWidgetId
			})).filter((entry) => entry.selection));
			const widgetSelectionEntries = localSelectionEntries.map((entry) => ({
				ref: entry.ref,
				selection: entry.selection
			}));
			const widgetRuntimeSelections = [...widgetSelectionEntries.map((entry) => entry.selection), ...mirroredSelectionEntries.map((entry) => entry.selection)];
			const materializedRows = materializeRows({
				baseRows,
				widgetDef,
				inboundLinks,
				activeSelections: activeSelectionEntries,
				sourceTransforms: getSourceTransforms({
					widgetDef,
					baseSpec: spec
				})
			});
			const visibleDataRef = makeDataRef({
				appId,
				workspaceId,
				dataId: `${widgetDef.widgetId}_visible`
			});
			const widgetSpec = buildSpecFromSource({
				source: widgetDef.source,
				baseSpec: materializationSpec,
				rows: materializedRows
			});
			if (!widgetSpec) continue;
			const runtimeSpec = applyLinkedViewStateToSpec({
				widgetSpec,
				inboundLinks,
				activeSelections: activeSelectionEntries.filter((entry) => inboundLinks.some((link) => link.kind === "syncDomain" && link.sourceWidgetId === entry.widgetId)).map((entry) => entry.selection)
			});
			const selectionVisibleOnWidget = widgetRuntimeSelections.length > 0;
			const materializedSelectedCount = selectionVisibleOnWidget ? countSelectedRowsForSelections(materializedRows, widgetRuntimeSelections) : 0;
			const selectedRows = selectionVisibleOnWidget ? materializedRows.filter((row) => rowMatchesAnySelection(row, widgetRuntimeSelections)) : [];
			const highlightedSpec = applyHighlightToSpec({
				widgetSpec: applySelectionToSpec({
					widgetSpec: runtimeSpec,
					activeSelections: widgetRuntimeSelections,
					selectionEnabled: selectionVisibleOnWidget
				}),
				inboundLinks,
				activeSelections: activeSelectionEntries.filter((entry) => inboundLinks.some((link) => link.kind === "highlight" && link.sourceWidgetId === entry.widgetId)).map((entry) => mapSelectionToTargetSelection(entry.selection, inboundLinks.find((link) => link.kind === "highlight" && link.sourceWidgetId === entry.widgetId)?.fieldMapping))
			});
			const visibleDataHandle = buildDerivedDataHandle({
				ref: visibleDataRef,
				title: `${widgetDef.title || widgetDef.widgetId} Visible Data`,
				description: `Current visible rows for widget ${widgetDef.widgetId}.`,
				rows: materializedRows,
				selectedCount: materializedSelectedCount,
				kind: "dataView",
				scope: "visible",
				widgetRef
			});
			runtimeData[visibleDataRef] = {
				ref: visibleDataRef,
				rows: materializedRows,
				baseRows: materializedRows,
				handle: visibleDataHandle,
				widgetRef
			};
			dataHandles.push(visibleDataHandle);
			structuralLinks.push(makeWidgetLink({
				ref: makeLinkRef({
					appId,
					workspaceId,
					linkId: `${widgetDef.widgetId}_uses_visible_data`
				}),
				kind: "usesData",
				from: widgetRef,
				to: visibleDataRef,
				sourceRef: widgetRef,
				targetRef: visibleDataRef,
				sourceDataRef: dataHandle.ref,
				targetDataRef: visibleDataRef,
				description: `${widgetDef.widgetId} reads from its current visible data view.`,
				activationPolicy: "manual"
			}));
			structuralLinks.push(makeWidgetLink({
				ref: makeLinkRef({
					appId,
					workspaceId,
					linkId: `${widgetDef.widgetId}_visible_derives_from_source`
				}),
				kind: "derivesFrom",
				from: visibleDataRef,
				to: dataHandle.ref,
				sourceRef: visibleDataRef,
				targetRef: dataHandle.ref,
				sourceDataRef: visibleDataRef,
				targetDataRef: dataHandle.ref,
				description: `${widgetDef.widgetId} visible data is derived from the primary workspace data handle.`,
				activationPolicy: "manual"
			}));
			if (selectionVisibleOnWidget) {
				const selectionDataRef = makeWidgetSelectionDataRef({
					appId,
					workspaceId,
					widgetId: widgetDef.widgetId
				});
				const selectionDataHandle = buildDerivedDataHandle({
					ref: selectionDataRef,
					title: `${widgetDef.title || widgetDef.widgetId} Selection Data`,
					description: `Current rows selected on widget ${widgetDef.widgetId}.`,
					rows: selectedRows,
					selectedCount: selectedRows.length,
					kind: "selectionData",
					scope: "combined",
					widgetRef
				});
				runtimeData[selectionDataRef] = {
					ref: selectionDataRef,
					rows: selectedRows,
					baseRows: materializedRows,
					handle: selectionDataHandle,
					widgetRef,
					kind: "selectionData",
					scope: "combined"
				};
				dataHandles.push(selectionDataHandle);
				structuralLinks.push(makeWidgetLink({
					ref: makeLinkRef({
						appId,
						workspaceId,
						linkId: `${widgetDef.widgetId}_selection_data_derives_from_visible`
					}),
					kind: "derivesFrom",
					from: selectionDataRef,
					to: visibleDataRef,
					sourceRef: selectionDataRef,
					targetRef: visibleDataRef,
					sourceDataRef: selectionDataRef,
					targetDataRef: visibleDataRef,
					description: `${widgetDef.widgetId} combined selection data is derived from the widget visible data view.`,
					activationPolicy: "manual"
				}));
				for (const entry of widgetSelectionEntries) {
					const selectionId = entry?.ref?.split("/").pop() || null;
					if (!selectionId || !entry?.selection) continue;
					const selectionRows = materializedRows.filter((row) => rowMatchesSelection(row, entry.selection));
					const selectionScopedDataRef = makeSelectionScopedDataRef({
						appId,
						workspaceId,
						widgetId: widgetDef.widgetId,
						selectionId
					});
					const selectionScopedDataHandle = buildDerivedDataHandle({
						ref: selectionScopedDataRef,
						title: `${widgetDef.title || widgetDef.widgetId} Selection ${selectionId} Data`,
						description: `Current rows for selection ${selectionId} on widget ${widgetDef.widgetId}.`,
						rows: selectionRows,
						selectedCount: selectionRows.length,
						kind: "selectionData",
						scope: "selection",
						widgetRef,
						sourceSelectionRef: entry.ref
					});
					runtimeData[selectionScopedDataRef] = {
						ref: selectionScopedDataRef,
						rows: selectionRows,
						baseRows: materializedRows,
						handle: selectionScopedDataHandle,
						widgetRef,
						sourceSelectionRef: entry.ref,
						kind: "selectionData",
						scope: "selection"
					};
					dataHandles.push(selectionScopedDataHandle);
					structuralLinks.push(makeWidgetLink({
						ref: makeLinkRef({
							appId,
							workspaceId,
							linkId: `${widgetDef.widgetId}_${selectionId}_contains_selection`
						}),
						kind: "contains",
						from: widgetRef,
						to: entry.ref,
						sourceRef: widgetRef,
						targetRef: entry.ref,
						description: `${widgetDef.widgetId} contains the local selection ${selectionId}.`,
						activationPolicy: "manual"
					}));
					structuralLinks.push(makeWidgetLink({
						ref: makeLinkRef({
							appId,
							workspaceId,
							linkId: `${widgetDef.widgetId}_${selectionId}_selection_data_derives_from_selection`
						}),
						kind: "derivesFrom",
						from: selectionScopedDataRef,
						to: entry.ref,
						sourceRef: selectionScopedDataRef,
						targetRef: entry.ref,
						sourceDataRef: visibleDataRef,
						description: `${widgetDef.widgetId} selection-scoped data is derived from selection ${selectionId}.`,
						activationPolicy: "manual"
					}));
				}
			}
			const widgetKind = widgetDef.kind || inferWidgetKind(highlightedSpec);
			const linkedRefs = inboundLinks.map((link) => widgetRefsById.get(link.sourceWidgetId)).filter(Boolean);
			const outboundRefs = (plan.links || []).filter((link) => link.sourceWidgetId === widgetDef.widgetId).map((link) => widgetRefsById.get(link.targetWidgetId)).filter(Boolean);
			const affectedRefs = [widgetRef, ...new Set(outboundRefs)];
			const widgetActions = buildActionDescriptors({
				widgetRef,
				selectionRef: widgetSelectionEntries[0]?.ref || null,
				widgetKind,
				widgetSourceKind: widgetDef.source?.kind,
				widgetSpec: highlightedSpec,
				widgetDef,
				scope: outboundRefs.length > 0 ? "workspace" : "local",
				affectedRefs
			});
			const widgetPerceptions = buildPerceptionDescriptors({
				appId,
				workspaceId,
				widgetRef,
				dataRef: visibleDataRef,
				widgetKind,
				widgetDef,
				widgetSpec: highlightedSpec
			});
			const humanInteraction = getWidgetFamilyHumanInteractionConfig(widgetKind, {
				widgetRef,
				widgetDef,
				widgetSpec: highlightedSpec
			});
			const widgetDescription = makeWidgetDescription({
				ref: widgetRef,
				widgetId: widgetDef.widgetId,
				role: widgetDef.role,
				kind: widgetKind,
				recognizedKinds: uniqueRecognizedKinds(highlightedSpec?.__widgetvaRecognizedKinds || highlightedSpec?.recognizedKinds || highlightedSpec?.__widgetvaCandidateKinds || highlightedSpec?.candidateKinds || widgetDef?.recognizedKinds || widgetDef?.candidateKinds),
				sourceKind: widgetDef.source?.kind || null,
				supportsSpecMutation: widgetDef.source?.kind === "baseSpec",
				title: widgetDef.title || getTitleText(highlightedSpec?.title) || widgetDef.widgetId,
				description: widgetDef.description || "Workspace visualization widget.",
				analyticRoles: Array.isArray(widgetDef.analyticRoles) && widgetDef.analyticRoles.length > 0 ? widgetDef.analyticRoles : inferAnalyticRoles(widgetKind),
				primaryDataRef: dataHandle.ref,
				actionNames: widgetActions.map((d) => d.name),
				perceptionQueryNames: widgetPerceptions.map((d) => d.name),
				usageNotes: widgetDef.usageNotes || [],
				humanInteraction
			});
			widgetDescriptions.push(widgetDescription);
			widgets[widgetRef] = buildWidgetState({
				widgetRef,
				widgetId: widgetDef.widgetId,
				role: widgetDef.role,
				kind: widgetKind,
				updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
				sourceDataRef: dataHandle.ref,
				currentDataRef: visibleDataRef,
				rowCount: dataHandle.stats?.rowCount || 0,
				visibleCount: materializedRows.length,
				selectedCount: materializedSelectedCount,
				widgetSpec: highlightedSpec,
				inboundLinks,
				activeSelections: widgetRuntimeSelections,
				widgetSelectionRefs: widgetSelectionEntries,
				mirroredSelectionEntries,
				humanInteraction,
				linkedRefs,
				existingFeedback: previousState?.widgets?.[widgetRef]?.feedback
			});
			widgetAdapters.push({
				...createVegaLiteWidgetAdapter({ kind: widgetKind }),
				provider: widgetDef.provider || "vega-lite",
				widgetRef,
				dataRef: visibleDataRef,
				kind: widgetKind,
				title: widgetDescription.title,
				description: widgetDescription.description,
				analyticRoles: widgetDescription.analyticRoles,
				primaryDataRef: dataHandle.ref,
				role: widgetDef.role,
				sourceKind: widgetDef.source?.kind || null,
				supportsSpecMutation: widgetDef.source?.kind === "baseSpec",
				actionNames: widgetActions.map((d) => d.name),
				perceptionQueryNames: widgetPerceptions.map((d) => d.name),
				usageNotes: widgetDef.usageNotes || [],
				humanInteraction,
				metadata: {
					widgetId: widgetDef.widgetId,
					role: widgetDef.role,
					primaryDataRef: dataHandle.ref,
					currentDataRef: visibleDataRef
				}
			});
			actionDescriptors.push(...widgetActions);
			perceptionDescriptors.push(...widgetPerceptions);
		}
		const links = [...(plan.links || []).map((link) => {
			if (typeof link?.sourceStateRef === "string" && typeof link?.targetStateRef === "string") return {
				...cloneValue$4(link),
				ref: link.ref || link.linkId || link.id,
				linkId: link.linkId || link.id || link.ref,
				relation: link.relation || "controls",
				activation: link.activation || link.activationPolicy || "automatic"
			};
			return makeWidgetLink({
				ref: buildLinkRef({
					appId,
					workspaceId,
					linkId: link.linkId
				}),
				kind: link.kind,
				from: widgetRefsById.get(link.sourceWidgetId) || null,
				to: widgetRefsById.get(link.targetWidgetId) || null,
				description: link.description || `${link.sourceWidgetId} -> ${link.targetWidgetId}`,
				effect: link.effect || (link.kind === "filter" ? "applyFilter" : link.kind === "highlight" ? "applyHighlight" : link.kind === "syncDomain" ? "syncDomain" : link.kind === "sharesSelection" ? "shareSelection" : null),
				activationPolicy: link.activationPolicy,
				effectConstraint: link.effectConstraint,
				fieldMapping: Array.isArray(link.fieldMapping) ? link.fieldMapping : []
			});
		}), ...structuralLinks];
		const defaultFocusedWidgetRef = widgetRefsById.get(plan.primaryWidgetId) || widgetDescriptions[0]?.ref || null;
		const runtimeTopology = deriveWorkspaceTopology({
			widgets: widgetDescriptions,
			links
		});
		const resolvedFocusedWidgetRef = focusedWidgetRef && widgets[focusedWidgetRef] ? focusedWidgetRef : defaultFocusedWidgetRef;
		const targetWidgetRefs = widgetDescriptions.map((widget) => widget.ref);
		const taskContext = buildPlanningTaskContext({
			planningRequest,
			coordinationScope: targetWidgetRefs.length > 1 ? "multi_widget" : "single_widget",
			targetWidgetRefs
		});
		const capabilities = new Set(["singleWidgetAnalysis", "traceReplay"]);
		if (targetWidgetRefs.length > 1) capabilities.add("multiWidgetCoordination");
		if (links.some((link) => link.kind === "filter")) capabilities.add("crossFilter");
		if (links.some((link) => link.kind === "sharesSelection")) capabilities.add("sharedSelection");
		if (links.some((link) => link.kind === "syncDomain")) capabilities.add("domainSync");
		const sharedStateWithFocus = withFocusSubmodel(withSelectionSubmodel({
			activeSelections,
			viewport: normalizedViewportState,
			globalFilters: deriveGlobalFiltersFromState({
				widgets,
				shared: { activeSelections }
			}),
			focusedWidget: resolvedFocusedWidgetRef
		}, {
			registry: activeSelections,
			primary: selectionRef ? { selectionRef } : null
		}), focusState == null ? void 0 : focusState, widgets);
		const sharedState = withHighlightSubmodel(sharedStateWithFocus, highlightState ?? deriveHighlightState({
			widgets,
			shared: sharedStateWithFocus
		}));
		const stateMeta = stateManager?.createStateMeta({
			workspaceId,
			previousState,
			nextWidgets: widgets,
			nextShared: sharedState,
			nextTaskContext: taskContext,
			nextReplayContext: replayContext,
			branchId: currentBranchId
		}) || {
			stateId: `${workspaceId}:${Date.now()}`,
			createdAt: (/* @__PURE__ */ new Date()).toISOString()
		};
		const currentSelectionDataRef = makeCurrentSelectionDataRef({
			appId,
			workspaceId
		});
		const currentViewDataRef = makeCurrentViewDataRef({
			appId,
			workspaceId
		});
		const focusedWidgetSelectionRef = resolvedFocusedWidgetRef ? Object.keys(widgets[resolvedFocusedWidgetRef]?.selections || {})[0] || null : null;
		const focusedWidgetVisibleDataRef = resolvedFocusedWidgetRef ? widgets[resolvedFocusedWidgetRef]?.data?.currentDataRef || null : null;
		const focusedWidgetVisibleDataEntry = focusedWidgetVisibleDataRef ? runtimeData[focusedWidgetVisibleDataRef] : null;
		const focusedWidgetSelectionDataRef = resolvedFocusedWidgetRef ? runtimeData[makeWidgetSelectionDataRef({
			appId,
			workspaceId,
			widgetId: widgets[resolvedFocusedWidgetRef]?.widgetId
		})] : null;
		if (focusedWidgetSelectionDataRef?.widgetRef && Array.isArray(focusedWidgetSelectionDataRef?.rows)) {
			const currentSelectionHandle = buildDerivedDataHandle({
				ref: currentSelectionDataRef,
				title: "Current Selection Data",
				description: "Current rows for the focused widget selection.",
				rows: focusedWidgetSelectionDataRef.rows,
				selectedCount: focusedWidgetSelectionDataRef.rows.length,
				kind: "selectionData",
				scope: "workspaceCurrent",
				widgetRef: focusedWidgetSelectionDataRef.widgetRef,
				sourceSelectionRef: focusedWidgetSelectionRef
			});
			runtimeData[currentSelectionDataRef] = {
				ref: currentSelectionDataRef,
				rows: focusedWidgetSelectionDataRef.rows,
				baseRows: focusedWidgetSelectionDataRef.baseRows,
				handle: currentSelectionHandle,
				widgetRef: focusedWidgetSelectionDataRef.widgetRef,
				sourceSelectionRef: focusedWidgetSelectionRef,
				kind: "selectionData",
				scope: "workspaceCurrent"
			};
			dataHandles.push(currentSelectionHandle);
		}
		if (focusedWidgetVisibleDataEntry?.widgetRef && Array.isArray(focusedWidgetVisibleDataEntry?.rows)) {
			const currentViewHandle = buildDerivedDataHandle({
				ref: currentViewDataRef,
				title: "Current View Data",
				description: "Current visible rows for the focused widget.",
				rows: focusedWidgetVisibleDataEntry.rows,
				selectedCount: focusedWidgetVisibleDataEntry.handle?.stats?.selectedCount || 0,
				kind: "dataView",
				scope: "workspaceCurrentView",
				widgetRef: focusedWidgetVisibleDataEntry.widgetRef
			});
			runtimeData[currentViewDataRef] = {
				ref: currentViewDataRef,
				rows: focusedWidgetVisibleDataEntry.rows,
				baseRows: focusedWidgetVisibleDataEntry.baseRows,
				handle: currentViewHandle,
				widgetRef: focusedWidgetVisibleDataEntry.widgetRef,
				kind: "dataView",
				scope: "workspaceCurrentView"
			};
			dataHandles.push(currentViewHandle);
		}
		const widgetAdapterSummaries = widgetAdapters.map((adapter) => summarizeWidgetAdapter(adapter)).filter(Boolean);
		return {
			description: makeWorkspaceDescription({
				appId,
				workspaceId,
				generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
				workspaceCapabilities: Array.from(capabilities),
				transportHints: {
					recommendedTools: [
						"workspace_describe",
						"view_read",
						"action_run",
						"perception_query",
						"interaction_trace_read"
					],
					note: "External transports should prefer the stable workspace/view/action/perception/trace surface."
				},
				runtimeTopology,
				taskContext,
				planning: {
					supportedTopologies: [...WORKSPACE_PLANNING_TOPOLOGIES],
					supportedRunModes: [...WORKSPACE_PLANNING_RUN_MODES],
					supportedComplexityBudgets: [...WORKSPACE_PLANNING_BUDGETS],
					supportedPlanSources: [...WORKSPACE_PLAN_SOURCES],
					supportedPlanningModes: [...WORKSPACE_PLAN_MODES],
					requestedWorkspaceSpec: workspaceSpecSummary,
					planningRequest: planningRequest || null,
					workspaceSpecStatus,
					workspaceSpecIssues: workspaceSpecValidation.issues,
					materializedFromSpec,
					materializedFromPlanner,
					planner: planningSummary
				},
				widgets: widgetDescriptions,
				widgetAdapters: widgetAdapterSummaries,
				dataHandles,
				links,
				actions: actionDescriptors,
				perceptionQueries: perceptionDescriptors
			}),
			state: makeWorkspaceState({
				stateId: stateMeta.stateId,
				createdAt: stateMeta.createdAt,
				widgets,
				shared: sharedState,
				taskContext
			}),
			runtimeData,
			widgetAdapters,
			replayContext
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/support/actionErrors.js
	var ActionPreconditionError = class extends Error {
		constructor(message, details, recoveryHints) {
			super(message);
			this.name = "ActionPreconditionError";
			this.details = details;
			this.recoveryHints = Array.isArray(recoveryHints) ? recoveryHints : [];
		}
	};
	function throwActionPrecondition(message, details, recoveryHints) {
		throw new ActionPreconditionError(message, details, recoveryHints);
	}
	function isActionPreconditionError(error) {
		return error instanceof ActionPreconditionError;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/support/schemaValidation.js
	function describeExpectedTypes(type) {
		if (Array.isArray(type)) return type.join(" or ");
		return typeof type === "string" ? type : "valid value";
	}
	function isTypeMatch(value, type) {
		if (type === "string") return typeof value === "string";
		if (type === "number") return typeof value === "number" && Number.isFinite(value);
		if (type === "integer") return Number.isInteger(value);
		if (type === "boolean") return typeof value === "boolean";
		if (type === "array") return Array.isArray(value);
		if (type === "object") return !!value && typeof value === "object" && !Array.isArray(value);
		if (type === "null") return value === null;
		return true;
	}
	function validateCompositeSchema(value, schema, path) {
		if (Array.isArray(schema.anyOf) && schema.anyOf.length > 0) {
			const errors = [];
			for (const candidate of schema.anyOf) try {
				validateAgainstSchema(value, candidate, path);
				return;
			} catch (error) {
				errors.push(error instanceof Error ? error.message : String(error));
			}
			throw new Error(`${path} must satisfy at least one allowed schema. ${errors[0] || ""}`.trim());
		}
		if (Array.isArray(schema.oneOf) && schema.oneOf.length > 0) {
			let passedCount = 0;
			let lastError = null;
			for (const candidate of schema.oneOf) try {
				validateAgainstSchema(value, candidate, path);
				passedCount += 1;
			} catch (error) {
				lastError = error;
			}
			if (passedCount !== 1) throw new Error(passedCount === 0 ? `${path} must satisfy exactly one allowed schema. ${lastError instanceof Error ? lastError.message : ""}`.trim() : `${path} matches multiple incompatible schemas.`);
		}
	}
	function validateType(value, schema, path) {
		if (schema.type == null) return;
		if (!(Array.isArray(schema.type) ? schema.type : [schema.type]).some((type) => isTypeMatch(value, type))) throw new Error(`${path} must be ${describeExpectedTypes(schema.type)}.`);
	}
	function validateScalarConstraints(value, schema, path) {
		if (schema.const !== void 0 && value !== schema.const) throw new Error(`${path} must equal ${JSON.stringify(schema.const)}.`);
		if (Array.isArray(schema.enum) && schema.enum.length > 0 && !schema.enum.includes(value)) throw new Error(`${path} must be one of ${schema.enum.map((item) => JSON.stringify(item)).join(", ")}.`);
		if (typeof value === "number") {
			if (Number.isFinite(schema.minimum) && value < schema.minimum) throw new Error(`${path} must be >= ${schema.minimum}.`);
			if (Number.isFinite(schema.maximum) && value > schema.maximum) throw new Error(`${path} must be <= ${schema.maximum}.`);
		}
		if (typeof value === "string") {
			if (Number.isFinite(schema.minLength) && value.length < schema.minLength) throw new Error(`${path} must contain at least ${schema.minLength} characters.`);
			if (Number.isFinite(schema.maxLength) && value.length > schema.maxLength) throw new Error(`${path} must contain at most ${schema.maxLength} characters.`);
		}
	}
	function validateArray(value, schema, path) {
		if (!Array.isArray(value)) return;
		if (Number.isFinite(schema.minItems) && value.length < schema.minItems) throw new Error(`${path} must contain at least ${schema.minItems} items.`);
		if (Number.isFinite(schema.maxItems) && value.length > schema.maxItems) throw new Error(`${path} must contain at most ${schema.maxItems} items.`);
		if (schema.items && typeof schema.items === "object") value.forEach((item, index) => validateAgainstSchema(item, schema.items, `${path}[${index}]`));
	}
	function validateObject(value, schema, path) {
		if (!value || typeof value !== "object" || Array.isArray(value)) return;
		const required = Array.isArray(schema.required) ? schema.required : [];
		for (const key of required) if (!(key in value) || value[key] == null) throw new Error(`${path}.${key} is required.`);
		const properties = schema.properties && typeof schema.properties === "object" ? schema.properties : {};
		for (const [key, childSchema] of Object.entries(properties)) {
			if (!(key in value) || value[key] == null) continue;
			validateAgainstSchema(value[key], childSchema, `${path}.${key}`);
		}
		if (schema.additionalProperties === false) {
			const allowedKeys = new Set(Object.keys(properties));
			for (const key of Object.keys(value)) if (!allowedKeys.has(key)) throw new Error(`${path}.${key} is not allowed.`);
		}
	}
	function validateAgainstSchema(value, schema, path = "value") {
		if (!schema || typeof schema !== "object") return;
		if (value == null) {
			if (schema.nullable === true) return;
			if (schema.type === "null" || Array.isArray(schema.type) && schema.type.includes("null")) return;
		}
		validateCompositeSchema(value, schema, path);
		validateType(value, schema, path);
		validateScalarConstraints(value, schema, path);
		validateArray(value, schema, path);
		validateObject(value, schema, path);
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/workspaceSharedStateMutators.js
	function clone$27(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function ensureSelectionTimeline(store) {
		if (!store.__selectionTimeline || typeof store.__selectionTimeline !== "object") store.__selectionTimeline = {
			past: [],
			future: []
		};
		return store.__selectionTimeline;
	}
	function selectionSnapshot(store) {
		const state = readWorkspaceStateFromStore(store);
		return {
			widgets: clone$27(state?.widgets || {}),
			shared: clone$27(state?.shared || {})
		};
	}
	function nextStateId(currentStateId = "main:s0") {
		const prefix = typeof currentStateId === "string" && currentStateId.includes(":") ? currentStateId.split(":")[0] : "main";
		if (typeof globalThis.crypto?.randomUUID === "function") return `${prefix}:${globalThis.crypto.randomUUID()}`;
		return `${prefix}:${Date.now()}`;
	}
	function commitStateUpdate(store, { widgets, shared, transitionType = "continue" } = {}) {
		const currentState = readWorkspaceStateFromStore(store);
		const nextState = {
			...clone$27(currentState || {}),
			stateId: nextStateId(currentState?.stateId),
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			widgets: clone$27(widgets || currentState?.widgets || {}),
			shared: clone$27(shared || currentState?.shared || {})
		};
		return store.commitState(nextState, { transitionType });
	}
	function updateSharedStateInStore(store, updater) {
		if (!store || typeof updater !== "function") return readWorkspaceStateFromStore(store);
		const currentState = readWorkspaceStateFromStore(store);
		const nextShared = updater(clone$27(currentState?.shared || {}));
		if (!nextShared || typeof nextShared !== "object") return currentState;
		if (typeof store?.commitState === "function") return commitStateUpdate(store, {
			widgets: currentState?.widgets || {},
			shared: nextShared
		});
		if (store.state?.shared && typeof store.state === "object") store.state.shared = nextShared;
		store.shared = nextShared;
		store.version = (store.version || 0) + 1;
		store.stateId = nextStateId(store.stateId);
		return readWorkspaceStateFromStore(store);
	}
	function resetWorkspaceInteractionsInStore(store) {
		const currentState = readWorkspaceStateFromStore(store);
		const nextWidgets = {};
		for (const [ref, widgetState] of Object.entries(currentState?.widgets || {})) nextWidgets[ref] = {
			...clone$27(widgetState),
			version: (widgetState?.version || 0) + 1,
			updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			selections: {},
			data: {
				...widgetState?.data || {},
				selectedCount: 0
			}
		};
		store.widgets = nextWidgets;
		if (store.state?.widgets && typeof store.state === "object") store.state.widgets = nextWidgets;
		const nextShared = withHighlightSubmodel(withFocusSubmodel({ ...withSelectionSubmodel(currentState?.shared || {}, {
			registry: {},
			primary: null,
			byWidget: {}
		}) }, null), null);
		if (typeof store?.commitState === "function") return commitStateUpdate(store, {
			widgets: nextWidgets,
			shared: nextShared,
			transitionType: "reset"
		});
		if (store.state?.shared && typeof store.state === "object") store.state.shared = nextShared;
		store.shared = nextShared;
		store.version = (store.version || 0) + 1;
		store.stateId = nextStateId(store.stateId);
		return readWorkspaceStateFromStore(store);
	}
	function restoreSnapshotStateInStore(store, snapshot) {
		const snapshotState = snapshot?.state || snapshot || {};
		const nextWidgets = clone$27(snapshotState?.widgets || {});
		const nextShared = withHighlightSubmodel(withFocusSubmodel(withSelectionSubmodel({
			globalFilters: {},
			focusedWidget: null,
			...clone$27(snapshotState?.shared) || {}
		}, {
			registry: void 0,
			primary: void 0,
			byWidget: void 0
		}), void 0, nextWidgets), void 0);
		if (typeof store?.commitState === "function") return commitStateUpdate(store, {
			widgets: nextWidgets,
			shared: nextShared,
			transitionType: "continue"
		});
		if (store.state?.widgets && typeof store.state === "object") store.state.widgets = nextWidgets;
		if (store.state?.shared && typeof store.state === "object") store.state.shared = nextShared;
		store.widgets = nextWidgets;
		store.shared = nextShared;
		store.version = (store.version || 0) + 1;
		store.stateId = nextStateId(store.stateId);
		return readWorkspaceStateFromStore(store);
	}
	function hasUndoSelectionHistoryInStore(store) {
		return Array.isArray(store?.__selectionTimeline?.past) && store.__selectionTimeline.past.length > 0;
	}
	function hasRedoSelectionHistoryInStore(store) {
		return Array.isArray(store?.__selectionTimeline?.future) && store.__selectionTimeline.future.length > 0;
	}
	function undoSelectionInStore(store) {
		const timeline = ensureSelectionTimeline(store);
		if (!timeline.past.length) return readWorkspaceStateFromStore(store);
		const previous = timeline.past.pop();
		timeline.future = [...timeline.future || [], selectionSnapshot(store)].slice(-50);
		return restoreSnapshotStateInStore(store, previous);
	}
	function redoSelectionInStore(store) {
		const timeline = ensureSelectionTimeline(store);
		if (!timeline.future.length) return readWorkspaceStateFromStore(store);
		const next = timeline.future.pop();
		timeline.past = [...timeline.past || [], selectionSnapshot(store)].slice(-50);
		return restoreSnapshotStateInStore(store, next);
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/store/workspaceStoreMutators.js
	function clone$26(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readStateRefSuffix(stateRef, widgetRef) {
		if (!stateRef || !widgetRef || !stateRef.startsWith(widgetRef)) return {
			kind: null,
			key: null
		};
		const [kind, key] = stateRef.slice(widgetRef.length).replace(/^\/+/, "").split("/");
		return {
			kind: kind || null,
			key: key || null
		};
	}
	function mergePatch(base, patch) {
		if (!patch || typeof patch !== "object" || Array.isArray(patch)) return clone$26(patch);
		const output = Array.isArray(base) ? [...base] : { ...base || {} };
		for (const [key, value] of Object.entries(patch)) if (Array.isArray(value)) output[key] = clone$26(value);
		else if (value && typeof value === "object") output[key] = mergePatch(output[key], value);
		else output[key] = value;
		return output;
	}
	function patchTransformByRef(transforms, stateRef, key, patch) {
		const safeTransforms = Array.isArray(transforms) ? transforms : [];
		const index = safeTransforms.findIndex((transform) => transform?.ref === stateRef || transform?.id === key || transform?.params?.id === key);
		const nextTransform = mergePatch(index >= 0 ? safeTransforms[index] : { ref: stateRef }, {
			ref: stateRef,
			...patch
		});
		if (index < 0) return [...safeTransforms, nextTransform];
		return safeTransforms.map((transform, transformIndex) => transformIndex === index ? nextTransform : transform);
	}
	function patchStateByRef(state = {}, stateRef = null, patch = {}) {
		if (typeof stateRef !== "string" || stateRef.length === 0) return makeWorkspaceState(state);
		const widgetRef = readWidgetRefFromStateRef(stateRef);
		if (!widgetRef) {
			if (state?.widgets?.[stateRef]) return makeWorkspaceState({
				...clone$26(state),
				widgets: {
					...state.widgets || {},
					[stateRef]: mergePatch(state.widgets[stateRef], patch)
				}
			});
			return makeWorkspaceState(state);
		}
		const currentWidget = state?.widgets?.[widgetRef];
		if (!currentWidget) return makeWorkspaceState(state);
		const { kind, key } = readStateRefSuffix(stateRef, widgetRef);
		let nextWidget = clone$26(currentWidget);
		if (stateRef === widgetRef || !kind) nextWidget = mergePatch(nextWidget, patch);
		else if (kind === "selection") {
			const selectionRef = stateRef;
			nextWidget.selections = {
				...nextWidget.selections || {},
				[selectionRef]: mergePatch(nextWidget.selections?.[selectionRef] || nextWidget.selections?.[key] || { ref: selectionRef }, {
					ref: selectionRef,
					...patch
				})
			};
		} else if (kind === "transform") nextWidget.transforms = patchTransformByRef(nextWidget.transforms, stateRef, key, patch);
		else if (kind === "view") if (!key) nextWidget.view = mergePatch(nextWidget.view || {}, patch);
		else nextWidget.view = {
			...nextWidget.view || {},
			[key]: mergePatch(nextWidget.view?.[key], patch)
		};
		else if (kind === "encoding" || kind === "encodings") if (!key) nextWidget.encodings = mergePatch(nextWidget.encodings || {}, patch);
		else nextWidget.encodings = {
			...nextWidget.encodings || {},
			[key]: mergePatch(nextWidget.encodings?.[key], patch)
		};
		else return makeWorkspaceState(state);
		return makeWorkspaceState({
			...clone$26(state),
			widgets: {
				...state.widgets || {},
				[widgetRef]: nextWidget
			}
		});
	}
	function beginBranchFromStateInStore(store, { stateId, label } = {}) {
		if (typeof store?.beginBranchFromState === "function") return store.beginBranchFromState({
			stateId,
			label
		});
		if (!stateId) throw new Error("beginBranchFromState requires a stateId.");
		const snapshotEntry = (typeof store?.readSnapshotEntry === "function" ? store.readSnapshotEntry(stateId) : null) || (Array.isArray(store?.stateSnapshots) ? store.stateSnapshots.find((entry) => entry?.stateId === stateId) || null : null);
		if (!snapshotEntry) throw new Error(`Unknown branch origin state: ${stateId}`);
		const branchId = `branch_${Date.now()}`;
		const branchLabel = label || `Branch from ${stateId}`;
		const parentBranchId = snapshotEntry?.branchId || store?.currentBranchId || "main";
		store.branchRegistry = {
			...store.branchRegistry || {},
			[branchId]: {
				branchId,
				label: branchLabel,
				originStateId: stateId,
				parentBranchId,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			}
		};
		store.currentBranchId = branchId;
		return store.branchRegistry[branchId];
	}
	function appendTraceRecordToStore(store, record) {
		if (typeof store?.appendTrace === "function") return store.appendTrace(record);
		if (!store || !record || typeof record !== "object") return null;
		const nextTrace = [...Array.isArray(store.interactionTrace) ? [...store.interactionTrace] : [], clone$26(record)];
		const retention = Number.isFinite(store.maxTraceRetention) ? store.maxTraceRetention : null;
		store.interactionTrace = retention && retention > 0 ? nextTrace.slice(-retention) : nextTrace;
		if (typeof store.emitChange === "function") store.emitChange();
		return record;
	}
	//#endregion
	//#region ../../widgetva-kit/src/contracts/result-contracts.js
	function compactObject$1(value) {
		return Object.fromEntries(Object.entries(value).filter(([, entryValue]) => entryValue !== void 0));
	}
	function makeResultError(error) {
		return {
			code: "RUNTIME_ERROR",
			message: "",
			...error
		};
	}
	function makeActionResult(result) {
		return compactObject$1({
			ok: false,
			callId: "",
			actionName: "unknown",
			updatedRefs: [],
			stateId: null,
			statePatch: {},
			expectedPostconditions: [],
			verificationHints: [],
			...result
		});
	}
	function makePerceptionResult(result) {
		return compactObject$1({
			ok: false,
			callId: "",
			queryName: "unknown",
			result: null,
			...result
		});
	}
	function makeActionVerificationResult(result) {
		return {
			verified: false,
			matchedStateId: null,
			matchedActionName: null,
			affectedRefs: [],
			missingRefs: [],
			expectedPostconditions: [],
			verificationHints: [],
			traceEvidence: null,
			statePatch: null,
			finalSnapshot: null,
			linkPropagation: [],
			semanticVerification: null,
			...result
		};
	}
	function makeDataQueryResult(result) {
		return compactObject$1({
			ok: false,
			dataRef: null,
			result: null,
			...result
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/support/queryScope.js
	function normalizeString(value) {
		return typeof value === "string" && value.length > 0 ? value : null;
	}
	function makeQueryScope(scope = {}) {
		const normalizedScope = scope && typeof scope === "object" && !Array.isArray(scope) ? scope : {};
		return {
			dataRef: normalizeString(normalizedScope.dataRef || normalizedScope.data_ref),
			selectionRef: normalizeString(normalizedScope.selectionRef || normalizedScope.selection_ref),
			focusRef: normalizeString(normalizedScope.focusRef || normalizedScope.focus_ref),
			viewportRef: normalizeString(normalizedScope.viewportRef || normalizedScope.viewport_ref)
		};
	}
	function readNormalizedQueryScope({ call = null, descriptor = null, querySpec = null } = {}) {
		const scope = makeQueryScope(querySpec?.queryScope || call?.params?.queryScope || call?.queryScope || call?.target || descriptor?.queryScope || null);
		return {
			widgetRef: (typeof call?.target?.widgetRef === "string" && call.target.widgetRef.length > 0 ? call.target.widgetRef : null) || (typeof call?.targetRef === "string" && call.targetRef.length > 0 ? call.targetRef : null) || (typeof descriptor?.targetRef === "string" && descriptor.targetRef.length > 0 ? descriptor.targetRef : null),
			dataRef: scope.dataRef || (typeof call?.dataRef === "string" && call.dataRef.length > 0 ? call.dataRef : null),
			selectionRef: scope.selectionRef || (typeof call?.selectionRef === "string" && call.selectionRef.length > 0 ? call.selectionRef : null),
			focusRef: scope.focusRef,
			viewportRef: scope.viewportRef
		};
	}
	function readScopedQueryScope(options = {}) {
		const scope = readNormalizedQueryScope(options);
		return {
			dataRef: scope.dataRef,
			selectionRef: scope.selectionRef,
			focusRef: scope.focusRef,
			viewportRef: scope.viewportRef
		};
	}
	function hasScopedQueryScopeValues(scope = {}) {
		return Object.values(scope || {}).some((value) => typeof value === "string" && value.length > 0);
	}
	function buildScopedParams({ params = null, call = null, descriptor = null, querySpec = null, includeTopLevelLegacyTargeting = true } = {}) {
		const rawParams = params ?? call?.params ?? querySpec ?? null;
		const normalizedParams = rawParams && typeof rawParams === "object" && !Array.isArray(rawParams) ? { ...rawParams } : {};
		call && {
			...call,
			...includeTopLevelLegacyTargeting ? {} : {
				targetRef: null,
				dataRef: null
			}
		};
		if (normalizedParams.queryScope) normalizedParams.queryScope = makeQueryScope(normalizedParams.queryScope);
		delete normalizedParams.targetRef;
		delete normalizedParams.targetDataRef;
		delete normalizedParams.selectionRef;
		delete normalizedParams.dataRef;
		return normalizedParams;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/verification/actionVerification.js
	function cloneValue$3(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function uniqueStrings$2(values) {
		return [...new Set(values.filter((value) => typeof value === "string" && value.trim().length > 0))];
	}
	function formatVerificationRef(ref) {
		if (ref === "shared") return "shared state";
		if (ref === "taskContext") return "task context";
		return ref;
	}
	function normalizeActionConditions(conditions) {
		if (!Array.isArray(conditions)) return [];
		return conditions.filter((condition) => condition && typeof condition === "object").map((condition) => ({
			description: typeof condition.description === "string" ? condition.description : "",
			...typeof condition.checkHint === "string" ? { checkHint: condition.checkHint } : {},
			...typeof condition.failureMessage === "string" ? { failureMessage: condition.failureMessage } : {}
		})).filter((condition) => condition.description || condition.checkHint || condition.failureMessage);
	}
	function buildPostconditionVerificationHints({ descriptor, updatedRefs = [] } = {}) {
		const refs = Array.isArray(updatedRefs) ? updatedRefs.filter((ref) => typeof ref === "string").map((ref) => formatVerificationRef(ref)) : [];
		const refSummary = refs.length > 0 ? refs.join(", ") : null;
		return normalizeActionConditions(descriptor?.postconditions).map((condition) => {
			if (condition.checkHint) return condition.checkHint;
			if (refSummary) return `Check ${refSummary} and confirm: ${condition.description}`;
			return `Confirm: ${condition.description}`;
		});
	}
	function buildActionVerificationPayload({ descriptor, updatedRefs = [], verificationHints = [] } = {}) {
		const expectedPostconditions = normalizeActionConditions(descriptor?.postconditions);
		const mergedHints = uniqueStrings$2([...buildPostconditionVerificationHints({
			descriptor,
			updatedRefs
		}), ...Array.isArray(verificationHints) ? verificationHints : []]);
		const verificationNote = expectedPostconditions.length === 0 ? null : expectedPostconditions.length === 1 ? `Expected postcondition: ${expectedPostconditions[0].description}` : `Expected postconditions: ${expectedPostconditions.map((condition) => condition.description).join("; ")}`;
		return {
			expectedPostconditions: cloneValue$3(expectedPostconditions),
			verificationHints: mergedHints,
			verificationNote
		};
	}
	function resolveActionDescriptorForVerification({ store, actionName, targetRef, affectedRefs = [] } = {}) {
		if (!store || !actionName) return null;
		const candidateRefs = [
			targetRef,
			...Array.isArray(affectedRefs) ? affectedRefs : [],
			null
		];
		for (const ref of candidateRefs) {
			const descriptor = store.getActionDescriptor(actionName, ref || void 0);
			if (descriptor) return descriptor;
		}
		return store.listActions().find((descriptor) => descriptor?.name === actionName) || null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/StateCommitter.js
	var MERGE_SHARED_PATCH_MODE = "widgetva.mergeSharedPatch";
	function cloneValue$2(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function mergeRuntimePatch$1(base, patch) {
		if (!patch || typeof patch !== "object" || Array.isArray(patch)) return cloneValue$2(base) || {};
		const output = Array.isArray(base) ? [...base] : { ...cloneValue$2(base) || {} };
		for (const [key, value] of Object.entries(patch)) {
			if (key === "__widgetvaPatchMode") continue;
			if (Array.isArray(value)) {
				output[key] = cloneValue$2(value);
				continue;
			}
			if (value && typeof value === "object") {
				output[key] = mergeRuntimePatch$1(output[key], value);
				continue;
			}
			output[key] = value;
		}
		return output;
	}
	function mergeSharedStatePatch(currentShared = {}, sharedPatch = {}) {
		const patch = cloneValue$2(sharedPatch) || {};
		const nextSharedBase = mergeRuntimePatch$1(currentShared, patch);
		const patchRegistry = readSelectionRegistry(patch);
		if (!(Object.keys(patchRegistry).length > 0 || Object.prototype.hasOwnProperty.call(patch, "activeSelections") || Object.prototype.hasOwnProperty.call(patch, "selections"))) return nextSharedBase;
		const nextRegistry = {
			...readSelectionRegistry(currentShared),
			...patchRegistry
		};
		const patchPrimary = readSelectionPrimaryView(patch);
		const nextByWidget = {
			...readSelectionByWidgetView(currentShared),
			...readSelectionByWidgetView(patch)
		};
		return withSelectionSubmodel(nextSharedBase, {
			registry: nextRegistry,
			primary: patchPrimary === null ? void 0 : patchPrimary,
			byWidget: nextByWidget
		});
	}
	function createCommittedState({ store, currentState, nextWidgets, nextShared, transition = {} } = {}) {
		const stateMeta = typeof store?.stateManager?.createStateMeta === "function" ? store.stateManager.createStateMeta({
			workspaceId: store.workspaceId || "main",
			previousState: currentState,
			nextWidgets,
			nextShared,
			nextTaskContext: currentState?.taskContext,
			nextReplayContext: store?.replayContext ?? currentState?.replayContext,
			branchId: store?.currentBranchId || "main"
		}) : null;
		const nextState = {
			...cloneValue$2(currentState) || {},
			stateId: stateMeta?.stateId || currentState?.stateId || null,
			createdAt: stateMeta?.createdAt || (/* @__PURE__ */ new Date()).toISOString(),
			widgets: nextWidgets,
			shared: {
				...nextShared || {},
				globalFilters: deriveGlobalFiltersFromState({
					...currentState || {},
					widgets: nextWidgets,
					shared: nextShared || {}
				})
			}
		};
		if (typeof store?.commitState === "function") return store.commitState(nextState, {
			transitionType: transition?.type || transition?.transitionType || "continue",
			parentStateId: transition?.parentStateId,
			branchId: transition?.branchId,
			branchLabel: transition?.branchLabel
		});
		if (store?.state && typeof store.state === "object") store.state = nextState;
		store.widgets = nextWidgets;
		store.shared = nextState.shared;
		store.version = (store?.version || 0) + 1;
		store.stateId = nextState.stateId;
		return nextState;
	}
	function rememberWidgetPatches(store, widgetPatchEntries) {
		if (!store || !Object.prototype.hasOwnProperty.call(store, "widgetStatePatches")) return;
		const nextPatches = { ...store.widgetStatePatches || {} };
		for (const [ref, patch] of widgetPatchEntries) nextPatches[ref] = mergeRuntimePatch$1(nextPatches[ref] || {}, patch);
		store.widgetStatePatches = nextPatches;
	}
	function buildActionStatePatch(store, refs = []) {
		const normalizedRefs = Array.isArray(refs) ? refs.filter(Boolean) : [];
		if (typeof store?.buildStatePatch === "function") return store.buildStatePatch(normalizedRefs);
		const state = readWorkspaceStateFromStore(store, { refs: normalizedRefs }) || {};
		const patch = {};
		for (const ref of normalizedRefs) {
			if (state?.widgets?.[ref]) {
				patch[ref] = cloneValue$2(state.widgets[ref]);
				continue;
			}
			if (ref === "shared" && state?.shared !== void 0) {
				patch.shared = cloneValue$2(state.shared);
				continue;
			}
			if (ref === "taskContext" && state?.taskContext !== void 0) {
				patch.taskContext = cloneValue$2(state.taskContext);
				continue;
			}
			if (ref === "replayContext" && state?.replayContext !== void 0) patch.replayContext = cloneValue$2(state.replayContext);
		}
		return patch;
	}
	function commitActionPatch(output, ctx) {
		if (!output || typeof output !== "object" || !output.patch || typeof output.patch !== "object" || Array.isArray(output.patch)) return output;
		const widgetPatchEntries = Object.entries(output.patch).filter(([ref]) => ref !== "shared");
		const affectedRefs = Array.isArray(output.affectedRefs) && output.affectedRefs.length > 0 ? output.affectedRefs : widgetPatchEntries.map(([ref]) => ref);
		const currentState = ctx.readCurrentState();
		const currentWidgets = currentState?.widgets || {};
		const nextWidgets = { ...currentWidgets };
		const updatedAt = (/* @__PURE__ */ new Date()).toISOString();
		for (const [ref, patch] of widgetPatchEntries) {
			const currentWidget = currentWidgets?.[ref];
			if (!currentWidget) throw new Error(`Unknown widget: ${ref}`);
			nextWidgets[ref] = mergeRuntimePatch$1({
				...cloneValue$2(currentWidget),
				version: (currentWidget.version || 0) + 1,
				updatedAt
			}, patch);
		}
		let nextShared = cloneValue$2(currentState?.shared || {});
		if (Object.prototype.hasOwnProperty.call(output.patch, "shared")) nextShared = output.patch.shared?.__widgetvaPatchMode === MERGE_SHARED_PATCH_MODE ? mergeSharedStatePatch(nextShared, output.patch.shared) : cloneValue$2(output.patch.shared);
		rememberWidgetPatches(ctx.store, widgetPatchEntries);
		const nextState = createCommittedState({
			store: ctx.store,
			currentState,
			nextWidgets,
			nextShared,
			transition: output.transition
		});
		return {
			...output,
			updatedRefs: Array.isArray(output.updatedRefs) ? output.updatedRefs : affectedRefs,
			nextState: output.nextState || nextState
		};
	}
	function defaultCollectUpdatedRefs(nextState, extraRefs = []) {
		const changedRefs = nextState?.delta?.changedRefs?.length ? nextState.delta.changedRefs : Object.keys(nextState?.widgets || {});
		return Array.from(new Set([...changedRefs || [], ...extraRefs || []]));
	}
	function mergeUniqueRefs$1(...groups) {
		return Array.from(new Set(groups.flatMap((group) => Array.isArray(group) ? group : []).filter((ref) => typeof ref === "string" && ref.length > 0)));
	}
	function resolveActionTargetRef$1(call, descriptor = null) {
		return readNormalizedQueryScope({
			call,
			descriptor
		}).widgetRef || null;
	}
	function resolveSelectionRefsForAction(ctx, state, options = {}) {
		const activeSelections = readSelectionRegistry(state?.shared || {});
		const allRefs = Object.keys(activeSelections).filter(Boolean);
		if (allRefs.length === 0) return [];
		const explicitTargetRef = options?.targetRef || null;
		const targetWidgetRef = (explicitTargetRef ? resolveWidgetRecordFromStore(ctx.store, explicitTargetRef) : ctx.resolveTargetWidget({
			kind: options?.kind,
			targetRef: options?.targetRef
		}))?.ref || explicitTargetRef || null;
		if (!targetWidgetRef) return allRefs;
		const targetSelectionRefs = allRefs.filter((selectionRef) => state?.widgets?.[targetWidgetRef]?.selections?.[selectionRef]);
		return targetSelectionRefs.length > 0 ? targetSelectionRefs : allRefs;
	}
	function resolveSelectionRefForAction(ctx, state, options = {}) {
		return resolveSelectionRefsForAction(ctx, state, options)[0] || null;
	}
	function descriptorUpdatesSelection(descriptor) {
		return descriptor?.category === "selection" || Array.isArray(descriptor?.effects) && descriptor.effects.some((effect) => effect?.kind === "updatesSelection");
	}
	function actionMayClearSelection(call) {
		const actionName = call?.name || "";
		return actionName === "widget.clearSelection" || actionName === "workspace.resetWorkspace" || actionName === "workspace.jumpToState" || actionName === "workspace.branchFromState" || actionName.endsWith(".reset") || actionName.toLowerCase().includes("reset") || actionName.toLowerCase().includes("undo");
	}
	function callRemovesSelection(output, descriptor) {
		if (output?.clearsSelection === true) return true;
		const actionName = descriptor?.name || null;
		return actionName === "widget.clearSelection" || actionName === "workspace.resetWorkspace" || actionName === "workspace.jumpToState" || actionName === "workspace.branchFromState";
	}
	function collectPropagation(sourceRef, options = {}) {
		const coordinationEngine = options.coordinationEngine || null;
		if (!sourceRef || typeof coordinationEngine?.propagate !== "function") return {
			refs: [],
			links: [],
			effects: []
		};
		const fallbackState = typeof options.readCurrentState === "function" ? options.readCurrentState() : null;
		const propagation = coordinationEngine.propagate({
			sourceRef,
			state: options.state || fallbackState
		});
		return {
			nextState: propagation?.nextState || options.state || fallbackState,
			refs: Array.isArray(propagation?.affectedRefs) ? propagation.affectedRefs : [],
			links: Array.isArray(propagation?.links) ? propagation.links : [],
			effects: Array.isArray(propagation?.effects) ? propagation.effects : []
		};
	}
	function enrichActionOutputWithPropagation({ output, descriptor, call, ctx, coordinationEngine = null, previousState, nextState, collectUpdatedRefs = defaultCollectUpdatedRefs } = {}) {
		if (!output || typeof output !== "object") return output;
		const selectionResolveOptions = {
			targetRef: resolveActionTargetRef$1(call, descriptor),
			kind: descriptor?.supportedWidgetKinds?.[0] || null
		};
		const previousSelectionRef = resolveSelectionRefForAction(ctx, previousState, selectionResolveOptions);
		const nextSelectionRef = resolveSelectionRefForAction(ctx, nextState, selectionResolveOptions);
		const shouldPropagateSelection = Boolean(output.propagateFromSelection) || descriptorUpdatesSelection(descriptor) && !Array.isArray(output.updatedRefs);
		const propagateFromRef = output.propagateFromRef || (shouldPropagateSelection ? nextSelectionRef : null) || (previousSelectionRef && !nextSelectionRef && (descriptorUpdatesSelection(descriptor) || actionMayClearSelection(call) || callRemovesSelection(output, descriptor)) ? previousSelectionRef : null);
		if (!propagateFromRef) return output;
		const propagation = collectPropagation(propagateFromRef, {
			state: nextState,
			coordinationEngine,
			readCurrentState: () => ctx.readCurrentState()
		});
		return {
			...output,
			nextState: propagation.nextState || nextState,
			updatedRefs: mergeUniqueRefs$1(Array.isArray(output.updatedRefs) ? output.updatedRefs : collectUpdatedRefs(nextState, output?.extraRefs || []), propagation.refs),
			result: {
				...output.result || {},
				...output.result?.propagationSourceRef ? {} : { propagationSourceRef: propagateFromRef },
				...output.result?.propagated ? {} : { propagated: propagation.links },
				...output.result?.propagationEffects ? {} : { propagationEffects: propagation.effects }
			}
		};
	}
	function finalizeActionCommit({ store, traceRecorder = null, call, descriptor, output, nextState, collectUpdatedRefs = defaultCollectUpdatedRefs } = {}) {
		const resolvedNextState = nextState || output?.nextState || {};
		const updatedRefs = Array.isArray(output?.updatedRefs) ? output.updatedRefs : Array.isArray(output?.affectedRefs) ? output.affectedRefs : collectUpdatedRefs(resolvedNextState, output?.extraRefs || []);
		const statePatch = output?.statePatch || buildActionStatePatch(store, updatedRefs);
		const verificationPayload = buildActionVerificationPayload({
			descriptor,
			updatedRefs,
			verificationHints: output?.verificationHints
		});
		traceRecorder?.recordAction?.({
			call,
			updatedRefs,
			stateId: resolvedNextState.stateId,
			statePatch,
			notes: {
				...output?.notes || {},
				...typeof verificationPayload.verificationNote === "string" && verificationPayload.verificationNote.length > 0 ? { verification: verificationPayload.verificationNote } : {},
				...verificationPayload.verificationHints.length > 0 ? { verificationHints: verificationPayload.verificationHints } : {}
			}
		});
		if (output?.transition && typeof traceRecorder?.recordSystemTransition === "function") traceRecorder.recordSystemTransition({
			actor: call?.actor || "agent",
			transitionType: output.transition.type || "continue",
			stateId: resolvedNextState.stateId,
			affectedRefs: updatedRefs,
			statePatch,
			notes: output.transition.notes
		});
		return makeActionResult({
			ok: true,
			callId: call?.callId || `call_${Date.now()}`,
			actionName: call?.name || "unknown",
			updatedRefs,
			stateId: resolvedNextState.stateId,
			statePatch,
			result: output?.result,
			expectedPostconditions: verificationPayload.expectedPostconditions,
			verificationHints: verificationPayload.verificationHints.length > 0 ? verificationPayload.verificationHints : ["Call readState({ refs: updatedRefs }) to verify the state update.", "Call a perception query if the task requires numerical or aggregate evidence."]
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/ActionExecutor.js
	function widgetMatchesKind$1(widget, kind) {
		if (!widget || !kind) return Boolean(widget);
		if (widget.kind === kind) return true;
		return Array.isArray(widget.recognizedKinds) && widget.recognizedKinds.includes(kind);
	}
	function makeActionHandlerContextSummary() {
		return {
			methods: ["targetWidget", "readRows"],
			capabilities: {
				workspaceRead: true,
				workspaceWrite: false,
				specMutation: false,
				snapshotReplay: false,
				runtimeDataRead: true,
				runtimeDataWrite: false,
				widgetTargetResolution: true,
				selectionMutation: false,
				linkPropagation: false
			},
			integrations: {
				store: false,
				appState: false,
				coordinationEngine: false,
				traceRecorder: false
			}
		};
	}
	function createActionRuntimeContext({ store, descriptor, call, hostBridge, getState, getAppState, subscribe, targetWidget = null }) {
		return {
			store,
			descriptor,
			call,
			hostBridge: hostBridge || createWidgetVAHostBridge({
				getState,
				getAppState,
				subscribe
			}) || createDefaultWidgetVAHostBridge(),
			resolvedTargetWidget: targetWidget || null,
			readCurrentState(options = {}) {
				return readWorkspaceStateFromStore(this.store, options);
			},
			readQueryScope() {
				return readNormalizedQueryScope({
					call: this.call,
					descriptor: this.descriptor
				});
			},
			resolveTargetWidget({ kind, targetRef } = {}) {
				const resolvedRef = targetRef || this.readQueryScope()?.widgetRef || null;
				if (resolvedRef) {
					const explicitTarget = resolveWidgetRecordFromStore(this.store, resolvedRef);
					if (!explicitTarget || !widgetMatchesKind$1(explicitTarget, kind)) return null;
					return explicitTarget;
				}
				const widgets = listStoreWidgets(this.store);
				if (kind) {
					const matches = widgets.filter((widget) => widgetMatchesKind$1(widget, kind)).map((widget) => resolveWidgetRecordFromStore(this.store, widget.ref) || widget).filter(Boolean);
					if (matches.length === 1) return matches[0];
					return null;
				}
				const focusedWidgetRef = this.readCurrentState()?.shared?.focusedWidget || this.hostBridge.readFocusedWidgetRef?.() || null;
				if (focusedWidgetRef) {
					const focusedWidget = resolveWidgetRecordFromStore(this.store, focusedWidgetRef);
					if (focusedWidget) return focusedWidget;
				}
				const firstWidget = widgets[0] || null;
				return firstWidget ? resolveWidgetRecordFromStore(this.store, firstWidget.ref) || firstWidget : null;
			},
			targetWidget(options = {}) {
				const kind = options?.kind || this.descriptor?.supportedWidgetKinds?.[0] || null;
				const targetRef = options?.targetRef || this.readQueryScope()?.widgetRef || null;
				if (this.resolvedTargetWidget && (!targetRef || this.resolvedTargetWidget.ref === targetRef || this.resolvedTargetWidget.widgetId === targetRef) && widgetMatchesKind$1(this.resolvedTargetWidget, kind)) return this.resolvedTargetWidget;
				const resolvedTargetWidget = this.resolveTargetWidget({
					kind,
					targetRef
				});
				if (resolvedTargetWidget) return resolvedTargetWidget;
				if (kind) throw new Error(`No ${kind}-compatible target widget is available for this action.`);
				throw new Error("No target widget is available for this action.");
			},
			readRows(widgetRef = null) {
				const resolvedWidgetRef = widgetRef || this.targetWidget()?.ref || null;
				const widgetState = this.store.getWidgetState?.(resolvedWidgetRef) || this.readCurrentState({ refs: [resolvedWidgetRef] })?.widgets?.[resolvedWidgetRef];
				const currentDataRef = widgetState?.data?.materializedDataRef || widgetState?.data?.currentDataRef || widgetState?.data?.sourceDataRef;
				const runtimeData = readRuntimeDataFromStore(this.store, currentDataRef);
				return Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
			}
		};
	}
	function createActionHandlerContext(runtimeContext) {
		const context = {
			call: runtimeContext.call,
			targetWidget(options = {}) {
				return runtimeContext.targetWidget(options);
			},
			readRows(widgetRef = null) {
				return runtimeContext.readRows(widgetRef);
			}
		};
		Object.defineProperty(context, "__widgetvaActionHandlerContext", {
			value: true,
			enumerable: false,
			configurable: false
		});
		return context;
	}
	function cloneArray$1(value) {
		return Array.isArray(value) ? [...value] : [];
	}
	function makeActionExecutorCounts(counts = {}) {
		return {
			descriptorCount: 0,
			handlerCount: 0,
			preconditionCount: 0,
			...counts
		};
	}
	function makeActionExecutorCapabilities(capabilities = {}) {
		return {
			paramsValidation: false,
			preconditionValidation: false,
			stateSync: false,
			traceRecording: false,
			linkPropagation: false,
			...capabilities
		};
	}
	function makeActionExecutorActionEntry(action = {}) {
		return {
			name: action?.name || "",
			category: action?.category ?? null,
			targetRef: action?.targetRef ?? null,
			affectedRefs: cloneArray$1(action?.affectedRefs),
			affectedStatePaths: cloneArray$1(action?.affectedStatePaths),
			supportedWidgetKinds: Array.isArray(action?.supportedWidgetKinds) ? [...action.supportedWidgetKinds] : null,
			hasPreconditions: action?.hasPreconditions === true,
			preconditionDescriptorCount: action?.preconditionDescriptorCount || 0,
			preconditionHandlerRegistered: action?.preconditionHandlerRegistered === true,
			postconditionCount: action?.postconditionCount || 0,
			reversible: action?.reversible === true,
			effectCount: action?.effectCount || 0,
			effectKinds: cloneArray$1(action?.effectKinds)
		};
	}
	function makeActionExecutorSummary(summary = {}) {
		return {
			counts: makeActionExecutorCounts(summary?.counts),
			capabilities: makeActionExecutorCapabilities(summary?.capabilities),
			actions: Array.isArray(summary?.actions) ? summary.actions.map((action) => makeActionExecutorActionEntry(action)) : []
		};
	}
	function buildActionError({ call, code, message, recoveryHints, details }) {
		return makeActionResult({
			ok: false,
			callId: call?.callId || `call_${Date.now()}`,
			actionName: call?.name || "unknown",
			error: makeResultError({
				code,
				message,
				...details !== void 0 ? { details } : {}
			}),
			...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {}
		});
	}
	function resolveRepresentativeActiveSelection(snapshot) {
		const activeSelections = readSelectionRegistry(snapshot?.shared || {});
		const activeSelectionEntries = Object.entries(activeSelections).filter(([, selectionState]) => Boolean(selectionState));
		if (activeSelectionEntries.length === 0) return [null, null];
		const focusedWidgetRef = snapshot?.shared?.focusedWidget || null;
		if (focusedWidgetRef) {
			const focusedSelectionRef = Object.keys(snapshot?.widgets?.[focusedWidgetRef]?.selections || {}).find((selectionRef) => activeSelections[selectionRef]);
			if (focusedSelectionRef) return [focusedSelectionRef, activeSelections[focusedSelectionRef]];
		}
		const primaryWidgetEntry = Object.entries(snapshot?.widgets || {}).find(([, widgetState]) => widgetState?.role === "primary");
		if (primaryWidgetEntry) {
			const [primaryWidgetRef] = primaryWidgetEntry;
			const primarySelectionRef = Object.keys(snapshot?.widgets?.[primaryWidgetRef]?.selections || {}).find((selectionRef) => activeSelections[selectionRef]);
			if (primarySelectionRef) return [primarySelectionRef, activeSelections[primarySelectionRef]];
		}
		if (activeSelectionEntries.length === 1) return activeSelectionEntries[0];
		return activeSelectionEntries[activeSelectionEntries.length - 1];
	}
	function selectionPayloadFromSnapshot(snapshot) {
		const [selectionRef, selectionState] = resolveRepresentativeActiveSelection(snapshot);
		if (!selectionRef || !selectionState) return null;
		const sourceWidget = Object.values(snapshot?.widgets || {}).find((widget) => widget?.selections?.[selectionRef]);
		return buildSelectionPayloadFromState({
			selectionRef,
			selectionState,
			sourceWidgetId: sourceWidget?.widgetId || void 0,
			selectedCount: sourceWidget?.data?.selectedCount || 0
		});
	}
	function selectionPayloadsFromSnapshot(snapshot) {
		const activeSelections = readSelectionRegistry(snapshot?.shared || {});
		const widgets = Object.values(snapshot?.widgets || {});
		return Object.fromEntries(Object.entries(activeSelections).map(([selectionRef, selectionState]) => {
			if (!selectionRef || !selectionState) return null;
			const sourceWidget = widgets.find((widget) => widget?.selections?.[selectionRef]) || null;
			return [selectionRef, buildSelectionPayloadFromState({
				selectionRef,
				selectionState,
				sourceWidgetId: sourceWidget?.widgetId || void 0,
				selectedCount: sourceWidget?.data?.selectedCount || 0
			})];
		}).filter(Boolean));
	}
	function primaryWidgetSpecFromSnapshot(snapshot) {
		const widgetEntries = Object.entries(snapshot?.widgets || {});
		if (widgetEntries.length === 0) return null;
		const focusedWidgetRef = snapshot?.shared?.focusedWidget;
		const focusedWidget = focusedWidgetRef ? snapshot.widgets?.[focusedWidgetRef] : null;
		if (focusedWidget?.rawSpec) return focusedWidget.rawSpec;
		const primaryWidget = widgetEntries.map(([, widgetState]) => widgetState).find((widgetState) => widgetState?.role === "primary" && widgetState?.rawSpec);
		if (primaryWidget?.rawSpec) return primaryWidget.rawSpec;
		return (widgetEntries[0]?.[1])?.rawSpec || null;
	}
	function resolvedWidgetRefForTarget(store, targetRef) {
		if (!targetRef) return null;
		return store?.getResolvedWidgetForTarget?.(targetRef)?.ref || store?.getResolvedWidget?.(targetRef)?.ref || null;
	}
	function readWidgetKindForTarget(store, targetRef) {
		if (!targetRef) return null;
		const widget = store?.getResolvedWidgetForTarget?.(targetRef) || store?.getResolvedWidget?.(targetRef) || store?.getWidgetDescription?.(targetRef) || store?.getWidgetState?.(targetRef) || null;
		return typeof widget?.kind === "string" && widget.kind.length > 0 ? widget.kind : null;
	}
	function actionFamilyName(actionName) {
		if (typeof actionName !== "string" || actionName.length === 0) return null;
		const [family] = actionName.split(".");
		return family || null;
	}
	function descriptorCanTargetWidget(descriptor, actionName, targetKind) {
		if (!descriptor?.name || descriptor.name !== actionName || !targetKind) return false;
		if (Array.isArray(descriptor.supportedWidgetKinds) && descriptor.supportedWidgetKinds.length > 0) return descriptor.supportedWidgetKinds.includes(targetKind);
		return actionFamilyName(actionName) === targetKind;
	}
	function mergeUniqueRefs(...groups) {
		return Array.from(new Set(groups.flatMap((group) => Array.isArray(group) ? group : []).filter((ref) => typeof ref === "string" && ref.length > 0)));
	}
	function buildHandlerInput$1(call) {
		const params = buildScopedParams({
			params: call?.params || {},
			call
		});
		const targetRef = readNormalizedQueryScope({ call }).widgetRef || null;
		const scopedQueryScope = readScopedQueryScope({ call });
		const { targetRef: _legacyTargetRef, dataRef: _legacyDataRef, params: _legacyParams, queryScope: _legacyQueryScope, ...rawCall } = call && typeof call === "object" ? call : {};
		return {
			...params,
			...rawCall,
			...targetRef ? { targetRef } : {},
			params,
			queryScope: hasScopedQueryScopeValues(scopedQueryScope) ? scopedQueryScope : null
		};
	}
	function buildHandlerParams(call) {
		return buildScopedParams({
			params: call?.params || {},
			call
		});
	}
	function buildActionCallSchemaInput(call) {
		const { targetRef: _legacyTargetRef, dataRef: _legacyDataRef, queryScope: _legacyQueryScope, ...rawCall } = call && typeof call === "object" ? call : {};
		const queryScope = readScopedQueryScope({ call });
		return {
			...rawCall,
			...hasScopedQueryScopeValues(queryScope) ? { queryScope } : {}
		};
	}
	function resolveActionTargetRef(call, descriptor = null) {
		return readNormalizedQueryScope({
			call,
			descriptor
		}).widgetRef || null;
	}
	function hasOwnParam(params, name) {
		return Boolean(params) && Object.prototype.hasOwnProperty.call(params, name);
	}
	function buildResolvedQueryScope(normalizedCall) {
		return normalizedCall?.queryScope && typeof normalizedCall.queryScope === "object" ? { ...normalizedCall.queryScope } : {};
	}
	function buildExecutableCall(call) {
		const normalizedCall = buildHandlerInput$1(call);
		const resolvedQueryScope = buildResolvedQueryScope(normalizedCall);
		return {
			...normalizedCall,
			...normalizedCall?.targetRef ? { targetRef: normalizedCall.targetRef } : {},
			params: { ...normalizedCall.params || {} },
			queryScope: resolvedQueryScope
		};
	}
	function readRequiredParamsFromSchema(schema) {
		return Array.isArray(schema?.required) ? schema.required.filter((param) => typeof param === "string" && param.length > 0) : [];
	}
	function buildInvalidParamsRecoveryHints({ call, requiredParams, executableCall }) {
		const hints = [];
		const params = executableCall?.params || {};
		const missingParams = (requiredParams || []).filter((param) => !hasOwnParam(params, param));
		if (missingParams.length > 0) hints.push(`Provide the required params for ${call?.name || "this action"}: ${missingParams.join(", ")}.`);
		if (call?.name) hints.push(`Call describeWorkspace() to inspect the action descriptor and params schema for ${call.name}.`);
		return hints;
	}
	var ActionExecutor = class {
		constructor({ store, sync, hostBridge, getState, getAppState, subscribe, coordinationEngine, traceRecorder } = {}) {
			this.store = store || { listActions() {
				return [];
			} };
			this.sync = typeof sync === "function" ? sync : () => {};
			this.hostBridge = hostBridge || createWidgetVAHostBridge({
				getState,
				getAppState,
				subscribe
			}) || createDefaultWidgetVAHostBridge();
			this.coordinationEngine = coordinationEngine || null;
			this.traceRecorder = traceRecorder || null;
			this.builtinDescriptors = /* @__PURE__ */ new Map();
			this.handlers = /* @__PURE__ */ new Map();
			this.preconditionHandlers = /* @__PURE__ */ new Map();
			this.runtimeHandlerNames = /* @__PURE__ */ new Set();
			this.isRegisteringRuntimeHandlers = false;
			this.registerBuiltinPreconditions();
			this.isRegisteringRuntimeHandlers = true;
			this.registerBuiltinHandlers();
			this.isRegisteringRuntimeHandlers = false;
		}
		register(descriptor, handler, options = {}) {
			if (!descriptor?.name || typeof handler !== "function") throw new Error("ActionExecutor.register requires a descriptor.name and handler.");
			if (this.handlers.has(descriptor.name)) throw new Error(`Duplicate action handler registration: ${descriptor.name}`);
			this.builtinDescriptors.set(descriptor.name, descriptor);
			this.handlers.set(descriptor.name, handler);
			if (this.isRegisteringRuntimeHandlers || options.runtimeHandler === true) this.runtimeHandlerNames.add(descriptor.name);
		}
		list() {
			const descriptors = this.store?.listActions?.() || [];
			if (descriptors.length > 0) {
				const listedDescriptors = descriptors.filter((descriptor) => this.handlers.has(descriptor.name));
				const listedNames = new Set(listedDescriptors.map((descriptor) => descriptor?.name).filter(Boolean));
				const exposedBuiltinDescriptors = Array.from(this.builtinDescriptors.values()).filter((descriptor) => descriptor?.exposeInWorkspace === true && !listedNames.has(descriptor.name));
				return [...listedDescriptors, ...exposedBuiltinDescriptors];
			}
			return Array.from(this.builtinDescriptors.values());
		}
		has(name) {
			return this.handlers.has(name);
		}
		describeExecutor() {
			const actions = this.list().map((descriptor) => makeActionExecutorActionEntry({
				name: descriptor.name,
				category: descriptor.category || null,
				targetRef: descriptor.targetRef || null,
				affectedRefs: Array.isArray(descriptor.affectedRefs) ? [...descriptor.affectedRefs] : [],
				affectedStatePaths: Array.isArray(descriptor.affectedStatePaths) ? [...descriptor.affectedStatePaths] : [],
				supportedWidgetKinds: Array.isArray(descriptor.supportedWidgetKinds) ? [...descriptor.supportedWidgetKinds] : null,
				hasPreconditions: Array.isArray(descriptor.preconditions) && descriptor.preconditions.length > 0,
				preconditionDescriptorCount: Array.isArray(descriptor.preconditions) ? descriptor.preconditions.length : 0,
				preconditionHandlerRegistered: this.preconditionHandlers.has(descriptor.name),
				postconditionCount: Array.isArray(descriptor.postconditions) ? descriptor.postconditions.length : 0,
				reversible: descriptor.reversible === true,
				effectCount: Array.isArray(descriptor.effects) ? descriptor.effects.length : 0,
				effectKinds: Array.isArray(descriptor.effects) ? [...new Set(descriptor.effects.map((effect) => effect?.kind).filter((kind) => typeof kind === "string" && kind.length > 0))] : []
			}));
			return makeActionExecutorSummary({
				counts: makeActionExecutorCounts({
					descriptorCount: actions.length,
					handlerCount: this.handlers.size,
					preconditionCount: this.preconditionHandlers.size
				}),
				capabilities: makeActionExecutorCapabilities({
					paramsValidation: true,
					preconditionValidation: true,
					stateSync: typeof this.sync === "function",
					traceRecording: typeof this.traceRecorder?.recordAction === "function",
					linkPropagation: Boolean(this.coordinationEngine)
				}),
				actions
			});
		}
		describeContext() {
			return makeActionHandlerContextSummary();
		}
		registerPrecondition(name, handler) {
			if (!name || typeof handler !== "function") throw new Error("ActionExecutor.registerPrecondition requires an action name and handler.");
			this.preconditionHandlers.set(name, handler);
		}
		findCompatibleDescriptor(actionName, targetRef, storeOverride = null) {
			const store = storeOverride || this.store;
			if (!actionName || !targetRef) return null;
			const exactDescriptor = store?.getActionDescriptor?.(actionName, targetRef);
			if (exactDescriptor) return exactDescriptor;
			const targetWidgetRef = resolvedWidgetRefForTarget(store, targetRef);
			if (!targetWidgetRef) return null;
			const targetWidgetKind = readWidgetKindForTarget(store, targetRef);
			return (store?.listActions?.() || []).filter((descriptor) => descriptor?.name === actionName).find((descriptor) => {
				const descriptorTargetRef = descriptor?.targetRef || null;
				if (!descriptorTargetRef) return descriptorCanTargetWidget(descriptor, actionName, targetWidgetKind);
				return resolvedWidgetRefForTarget(store, descriptorTargetRef) === targetWidgetRef;
			}) || null;
		}
		resolveDescriptor(call, storeOverride = null) {
			return this.findCompatibleDescriptor(call?.name, resolveActionTargetRef(call), storeOverride) || this.builtinDescriptors.get(call?.name) || null;
		}
		createRuntimeContext(call, descriptor, contextOverride = null) {
			const normalizedCall = buildHandlerInput$1(call);
			if (contextOverride?.__widgetvaActionRuntimeContext === true) {
				contextOverride.descriptor = descriptor;
				contextOverride.call = normalizedCall;
				return contextOverride;
			}
			const store = contextOverride?.store || this.store;
			const targetRef = resolveActionTargetRef(normalizedCall, descriptor);
			const hasStateAccessOverride = typeof contextOverride?.getState === "function" || typeof contextOverride?.getAppState === "function" || typeof contextOverride?.subscribe === "function" || typeof contextOverride?.subscribeAppState === "function";
			const ctx = createActionRuntimeContext({
				store,
				descriptor,
				call: normalizedCall,
				hostBridge: contextOverride?.hostBridge || (hasStateAccessOverride ? {
					...this.hostBridge,
					...createWidgetVAHostBridge({
						getState: contextOverride?.getState,
						getAppState: contextOverride?.getAppState,
						subscribe: contextOverride?.subscribe,
						subscribeAppState: contextOverride?.subscribeAppState
					})
				} : null) || this.hostBridge,
				getAppState: contextOverride?.getAppState,
				targetWidget: contextOverride?.targetWidget || null
			});
			if (!ctx.resolvedTargetWidget && (targetRef || descriptor?.supportedWidgetKinds?.length > 0)) ctx.resolvedTargetWidget = ctx.resolveTargetWidget({
				targetRef,
				kind: descriptor?.supportedWidgetKinds?.[0] || null
			});
			if (typeof contextOverride?.readCurrentState === "function") ctx.readCurrentState = contextOverride.readCurrentState.bind(contextOverride);
			Object.defineProperty(ctx, "__widgetvaActionRuntimeContext", {
				value: true,
				enumerable: false,
				configurable: false
			});
			return ctx;
		}
		createContext(call, descriptor, contextOverride = null) {
			return createActionHandlerContext(this.createRuntimeContext(call, descriptor, contextOverride));
		}
		async checkPreconditions(call, descriptor, ctx) {
			const handler = this.preconditionHandlers.get(call?.name);
			if (!handler) return;
			const outcome = await handler(buildHandlerInput$1(call), ctx, descriptor);
			if (outcome == null || outcome === true) return;
			if (outcome === false) throwActionPrecondition(descriptor?.preconditions?.[0]?.failureMessage || `Preconditions failed for action ${call?.name || "unknown"}.`);
			if (typeof outcome === "string") throwActionPrecondition(outcome);
			if (typeof outcome === "object" && outcome.ok === false) throwActionPrecondition(outcome.message || descriptor?.preconditions?.[0]?.failureMessage || `Preconditions failed for action ${call?.name || "unknown"}.`, outcome.details, outcome.recoveryHints);
		}
		async run(call, contextOverride = null) {
			const effectiveStore = contextOverride?.store || this.store || null;
			const traceRecorder = contextOverride?.traceRecorder || this.traceRecorder || null;
			try {
				validateAgainstSchema(buildActionCallSchemaInput(call), ACTION_CALL_SCHEMA, "action");
			} catch (error) {
				const result = buildActionError({
					call,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the action call schema in describeWorkspace() before retrying."]
				});
				traceRecorder?.recordActionFailure?.({
					call,
					code: result.error.code,
					message: result.error.message,
					recoveryHints: result.recoveryHints
				});
				return result;
			}
			const resolvedDescriptor = this.resolveDescriptor(call, effectiveStore);
			const targetRef = resolveActionTargetRef(call, resolvedDescriptor);
			const storeDescriptor = this.findCompatibleDescriptor(call?.name, targetRef, effectiveStore);
			const descriptor = storeDescriptor || resolvedDescriptor;
			const storeHasActionSurface = (effectiveStore?.listActions?.() || []).length > 0;
			const handler = this.handlers.get(call?.name);
			if (!handler) {
				const result = buildActionError({
					call,
					code: "UNKNOWN_OPERATION",
					message: `Unsupported WidgetVA action: ${call?.name || "unknown"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect currently supported actions."]
				});
				traceRecorder?.recordActionFailure?.({
					call,
					code: result.error.code,
					message: result.error.message,
					recoveryHints: result.recoveryHints
				});
				return result;
			}
			if (storeHasActionSurface && targetRef && !storeDescriptor && !this.builtinDescriptors.has(call?.name)) {
				const result = buildActionError({
					call,
					code: "UNSUPPORTED_TARGET",
					message: `Action ${call?.name || "unknown"} is not declared for target ${targetRef}.`,
					recoveryHints: ["Call describeWorkspace() to inspect which actions are declared on the target widget."]
				});
				traceRecorder?.recordActionFailure?.({
					call,
					code: result.error.code,
					message: result.error.message,
					recoveryHints: result.recoveryHints
				});
				return result;
			}
			const executableCall = buildExecutableCall(call);
			if (descriptor?.paramsSchema) try {
				validateAgainstSchema(executableCall.params || {}, descriptor.paramsSchema, "params");
			} catch (error) {
				const requiredParams = readRequiredParamsFromSchema(descriptor.paramsSchema);
				const result = buildActionError({
					call: executableCall,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: buildInvalidParamsRecoveryHints({
						call,
						requiredParams,
						executableCall
					}),
					details: {
						targetRef: targetRef || null,
						requiredParams
					}
				});
				traceRecorder?.recordActionFailure?.({
					call: executableCall,
					code: result.error.code,
					message: result.error.message,
					recoveryHints: result.recoveryHints
				});
				return result;
			}
			try {
				const runtimeCtx = this.createRuntimeContext(executableCall, descriptor, contextOverride);
				const handlerCtx = this.runtimeHandlerNames.has(executableCall.name) ? runtimeCtx : createActionHandlerContext(runtimeCtx);
				const runtimeCoordinationEngine = contextOverride?.coordinationEngine || this.coordinationEngine || null;
				const previousState = runtimeCtx.readCurrentState();
				await this.checkPreconditions(executableCall, descriptor, runtimeCtx);
				const rawOutput = commitActionPatch(await handler(buildHandlerParams(executableCall), handlerCtx), runtimeCtx);
				const baseNextState = rawOutput?.nextState || runtimeCtx.readCurrentState();
				const output = enrichActionOutputWithPropagation({
					output: rawOutput,
					descriptor,
					call: executableCall,
					ctx: runtimeCtx,
					coordinationEngine: runtimeCoordinationEngine,
					previousState,
					nextState: baseNextState,
					collectUpdatedRefs: (state, extraRefs) => this.collectUpdatedRefs(state, extraRefs)
				});
				return finalizeActionCommit({
					store: effectiveStore,
					traceRecorder,
					call: executableCall,
					descriptor,
					output,
					nextState: output?.nextState || baseNextState,
					collectUpdatedRefs: (state, extraRefs) => this.collectUpdatedRefs(state, extraRefs)
				});
			} catch (error) {
				if (isActionPreconditionError(error)) {
					const result = buildActionError({
						call,
						code: "PRECONDITION_FAILED",
						message: error.message,
						details: error.details,
						recoveryHints: error.recoveryHints
					});
					traceRecorder?.recordActionFailure?.({
						call,
						code: result.error.code,
						message: result.error.message,
						details: result.error.details,
						recoveryHints: result.recoveryHints
					});
					return result;
				}
				const result = buildActionError({
					call,
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error)
				});
				traceRecorder?.recordActionFailure?.({
					call,
					code: result.error.code,
					message: result.error.message
				});
				return result;
			}
		}
		collectUpdatedRefs(nextState, extraRefs = []) {
			const changedRefs = nextState?.delta?.changedRefs?.length ? nextState.delta.changedRefs : Object.keys(nextState?.widgets || {});
			return Array.from(new Set([...changedRefs || [], ...extraRefs || []]));
		}
		syncWorkspace() {
			this.sync();
			return readWorkspaceStateFromStore(this.store);
		}
		readSnapshot(stateId) {
			return readSnapshotFromStore(this.store, stateId) || this.store.readSnapshot?.(stateId) || null;
		}
		readSnapshotEntry(stateId) {
			return readSnapshotEntryFromStore(this.store, stateId);
		}
		hasUndoSelectionHistory() {
			const history = this.hostBridge.readSelectionHistory();
			const selectionsHistory = this.hostBridge.readSelectionsHistory();
			return Array.isArray(history) && history.length > 0 || Array.isArray(selectionsHistory) && selectionsHistory.length > 0 || hasUndoSelectionHistoryInStore(this.store);
		}
		hasRedoSelectionHistory() {
			const future = this.hostBridge.readSelectionFuture();
			const selectionsFuture = this.hostBridge.readSelectionsFuture();
			return Array.isArray(future) && future.length > 0 || Array.isArray(selectionsFuture) && selectionsFuture.length > 0 || hasRedoSelectionHistoryInStore(this.store);
		}
		undoSelection() {
			if (typeof this.store?.beginTransition !== "function" || typeof this.hostBridge.undoSelection !== "function") return undoSelectionInStore(this.store);
			this.store.beginTransition({ transitionType: "undo" });
			this.hostBridge.undoSelection();
			return this.syncWorkspace();
		}
		redoSelection() {
			if (typeof this.store?.beginTransition !== "function" || typeof this.hostBridge.redoSelection !== "function") return redoSelectionInStore(this.store);
			this.store.beginTransition({ transitionType: "redo" });
			this.hostBridge.redoSelection();
			return this.syncWorkspace();
		}
		resetWorkspace() {
			if (typeof this.store?.beginTransition !== "function" || typeof this.hostBridge.resetCurrentSpec !== "function" || typeof this.hostBridge.writeCurrentSelection !== "function" || typeof this.hostBridge.resetSelectionHistory !== "function" || typeof this.hostBridge.setFocusedWidgetRef !== "function") return resetWorkspaceInteractionsInStore(this.store);
			this.store.beginTransition({ transitionType: "reset" });
			this.store.resetWidgetPatches?.();
			this.hostBridge.resetCurrentSpec();
			this.hostBridge.writeCurrentSelection(null, { trackHistory: false });
			this.hostBridge.resetSelectionHistory();
			this.hostBridge.setFocusedWidgetRef(null);
			return this.syncWorkspace();
		}
		restoreSnapshotState(snapshot) {
			const snapshotState = snapshot?.state || snapshot;
			const replayContext = snapshot?.replayContext || null;
			const restoredSpec = primaryWidgetSpecFromSnapshot(snapshotState);
			if (typeof this.hostBridge.resetSelectionHistory !== "function" || typeof this.hostBridge.writeCurrentSelections !== "function" || typeof this.hostBridge.setFocusedWidgetRef !== "function" || typeof this.hostBridge.writeCurrentSpec !== "function") return restoreSnapshotStateInStore(this.store, snapshot);
			this.store.resetWidgetPatches?.();
			if (Object.prototype.hasOwnProperty.call(replayContext || {}, "baselineSpec")) this.hostBridge.writeCurrentSpec(replayContext?.baselineSpec ?? null, {
				replaceBaseline: true,
				trackHistory: false
			});
			this.hostBridge.writeWorkspaceSpec?.(replayContext?.workspaceSpec ?? null);
			this.hostBridge.writePlanningRequest?.(replayContext?.planningRequest ?? null);
			if (replayContext?.runMode) this.hostBridge.writeRunMode?.(replayContext.runMode);
			this.hostBridge.writeUserIntent?.(replayContext?.userIntent ?? "");
			if (restoredSpec) this.hostBridge.writeCurrentSpec(restoredSpec, { trackHistory: false });
			else if (Object.prototype.hasOwnProperty.call(replayContext || {}, "currentSpec")) this.hostBridge.writeCurrentSpec(replayContext?.currentSpec ?? null, { trackHistory: false });
			const selection = selectionPayloadFromSnapshot(snapshotState);
			const selections = selectionPayloadsFromSnapshot(snapshotState);
			this.hostBridge.resetSelectionHistory();
			this.hostBridge.writeCurrentSelections(selections, {
				fallbackSelection: selection,
				trackHistory: false
			});
			this.hostBridge.setFocusedWidgetRef(snapshotState?.shared?.focusedWidget || null);
			return this.syncWorkspace();
		}
		registerBuiltinPreconditions() {
			this.registerPrecondition("widget.undoSelection", (call, ctx) => {
				if (!this.hasUndoSelectionHistory()) return {
					ok: false,
					message: "No prior selection state is available to restore.",
					recoveryHints: ["Run a selection action first so there is history to restore.", "Call readState() or readTrace() to confirm whether any prior selection exists."]
				};
				return { ok: true };
			});
			this.registerPrecondition("widget.redoSelection", (call, ctx) => {
				if (!this.hasRedoSelectionHistory()) return {
					ok: false,
					message: "No redo selection state is available to restore.",
					recoveryHints: ["Run widget.undoSelection first so there is a redo selection state to restore.", "Avoid making a new selection before redo, because new selections clear redo history."]
				};
				return { ok: true };
			});
			this.registerPrecondition("workspace.jumpToState", (call, ctx) => {
				const targetStateId = call?.params?.stateId;
				if (!(targetStateId ? this.readSnapshot(targetStateId) : null)) return {
					ok: false,
					message: "The requested stateId is not available in runtime history.",
					details: { stateId: targetStateId || null },
					recoveryHints: ["Call listStateHistory() to inspect available stateIds.", "Choose a valid historical state before retrying jumpToState."]
				};
				return { ok: true };
			});
			this.registerPrecondition("workspace.branchFromState", (call, ctx) => {
				const targetStateId = call?.params?.stateId;
				if (!(targetStateId ? this.readSnapshot(targetStateId) : null)) return {
					ok: false,
					message: "The requested branch origin stateId is not available.",
					details: { stateId: targetStateId || null },
					recoveryHints: ["Call listStateHistory() to inspect available branch origin stateIds.", "Retry branchFromState with a valid historical state."]
				};
				return { ok: true };
			});
			this.registerPrecondition("workspace.focusWidget", (call, ctx) => {
				return this.readTargetWidgetPrecondition({
					call,
					ctx,
					message: "workspace.focusWidget requires a valid target widget."
				});
			});
		}
		readTargetWidgetPrecondition({ call, ctx, kind = null, allowMissing = false, message = null, recoveryHints = null } = {}) {
			const targetRef = resolveActionTargetRef(call);
			if (!targetRef && allowMissing) return { ok: true };
			if (!(ctx?.resolveTargetWidget?.({
				targetRef,
				...kind ? { kind } : {}
			}) || null)?.ref) return {
				ok: false,
				message: message || "The requested target widget is not available in the current workspace.",
				details: {
					targetRef: targetRef || null,
					kind
				},
				recoveryHints: Array.isArray(recoveryHints) && recoveryHints.length > 0 ? recoveryHints : ["Call describeWorkspace() to inspect valid widget refs before retrying.", "Retry the action with a targetRef that resolves to a materialized widget."]
			};
			return { ok: true };
		}
		resolveTargetWidgetRefs(requestedRefs, ctx) {
			const seen = /* @__PURE__ */ new Set();
			return requestedRefs.map((ref) => ctx?.resolveTargetWidget?.({ targetRef: ref })?.ref || null).filter((ref) => {
				if (!ref || seen.has(ref)) return false;
				seen.add(ref);
				return true;
			});
		}
		registerBuiltinHandlers() {
			this.register({
				name: "coordination.applyLink",
				category: "coordination",
				exposeInWorkspace: true,
				affectedStatePaths: [
					"shared.links",
					"shared.selections",
					"widgets"
				],
				paramsSchema: {
					type: "object",
					properties: {
						linkId: { type: "string" },
						linkRef: { type: "string" },
						sourceSelectionRef: { type: "string" },
						sourceRef: { type: "string" }
					}
				},
				examples: [{
					label: "Apply an existing coordination link",
					spec: {
						linkId: "scatter_filters_bar",
						sourceSelectionRef: "wl://widgetva-app/workspace/main/widget/scatter/selection/brush"
					}
				}]
			}, async (params, ctx) => {
				const linkRef = params.linkRef || params.linkId || params.ref || null;
				if (!linkRef) throwActionPrecondition("coordination.applyLink requires linkId or linkRef.", { requiredParams: ["linkId"] }, ["Call describeWorkspace() or coordination.describeLinks to inspect available links."]);
				const coordinationEngine = this.coordinationEngine;
				if (typeof coordinationEngine?.applyLink !== "function") throwActionPrecondition("No link engine is available for coordination.applyLink.", { coordinationEngine: false }, ["Use this action in a multi-widget runtime that provides CoordinationEngine."]);
				const sourceRef = params.sourceSelectionRef || params.sourceRef || null;
				const currentState = ctx.readCurrentState();
				const coordinationResult = coordinationEngine.applyLink({
					linkRef,
					sourceRef,
					state: currentState
				});
				if (coordinationResult?.ok !== true) throwActionPrecondition(`coordination.applyLink could not apply link ${linkRef}.`, {
					linkRef,
					sourceRef,
					reason: coordinationResult?.reason || null,
					skippedTargets: coordinationResult?.skippedTargets || []
				}, ["Confirm the link exists and has an active source selection.", "If no source selection exists, create one first or pass sourceSelectionRef."]);
				return {
					patch: coordinationResult.patch,
					nextState: coordinationResult.nextState || ctx.readCurrentState(),
					updatedRefs: Array.isArray(coordinationResult.affectedRefs) ? coordinationResult.affectedRefs : [],
					result: { coordination: coordinationResult },
					verificationHints: ["Inspect the affected target widgets and confirm the visible view changed according to the link summary.", "Read trace and coordination state to confirm this was an agent-invoked coordination link."],
					notes: { userVisibleSummary: `Applied coordination link ${linkRef}.` }
				};
			});
			this.register({ name: "widget.undoSelection" }, async (params, ctx) => ({
				nextState: this.undoSelection(),
				result: { restored: true },
				verificationHints: ["Read the widget state and confirm the previous selection was restored."],
				transition: {
					type: "undo",
					notes: { userVisibleSummary: "Selection history was rolled back to the prior state." }
				}
			}));
			this.register({ name: "widget.redoSelection" }, async (params, ctx) => ({
				nextState: this.redoSelection(),
				result: { restored: true },
				verificationHints: ["Read the widget state and confirm the selection matches the next recorded selection state."],
				notes: { userVisibleSummary: "Selection history was replayed to the next state." },
				transition: {
					type: "redo",
					notes: { userVisibleSummary: "Selection history was replayed to the next state." }
				}
			}));
			this.register({ name: "workspace.resetWorkspace" }, async (params, ctx) => ({
				nextState: this.resetWorkspace(),
				result: { reset: true },
				verificationHints: ["Read the workspace state and confirm selections and derived interaction state are cleared."],
				transition: {
					type: "reset",
					notes: { userVisibleSummary: "Workspace interaction state was reset to its baseline." }
				}
			}));
			this.register({ name: "workspace.jumpToState" }, async (params, ctx) => {
				const targetStateId = params.stateId;
				const snapshot = targetStateId ? this.readSnapshotEntry(targetStateId) : null;
				if (typeof this.store?.beginTransition === "function") this.store.beginTransition({
					transitionType: "jump_back",
					parentStateId: targetStateId
				});
				return {
					nextState: this.restoreSnapshotState(snapshot),
					result: { restoredStateId: targetStateId },
					verificationHints: ["Read the workspace state and confirm the restored interaction state matches the target snapshot."],
					transition: {
						type: "jump_back",
						notes: { userVisibleSummary: `Jumped workspace state back to ${targetStateId}.` }
					}
				};
			});
			this.register({ name: "workspace.branchFromState" }, async (params, ctx) => {
				const targetStateId = params.stateId;
				const snapshot = targetStateId ? this.readSnapshotEntry(targetStateId) : null;
				const branch = beginBranchFromStateInStore(this.store, {
					stateId: targetStateId,
					label: params.branchLabel
				});
				return {
					nextState: this.restoreSnapshotState(snapshot),
					result: {
						restoredStateId: targetStateId,
						branchId: branch.branchId,
						branchLabel: branch.label
					},
					verificationHints: ["Read the workspace state and confirm the restored interaction state matches the branch origin snapshot.", "Read trace graph or state history to confirm a new branch was created."],
					transition: {
						type: "branch",
						notes: { userVisibleSummary: `Created branch ${branch.label} from ${targetStateId}.` }
					}
				};
			});
			this.register({ name: "workspace.focusWidget" }, async (params, ctx) => {
				const targetWidget = ctx.targetWidget();
				const currentState = ctx.readCurrentState();
				const previousFocusedWidgetRef = currentState?.shared?.focusedWidget || null;
				return {
					patch: { shared: {
						...withFocusSubmodel(currentState?.shared || {}, {
							widgetRef: targetWidget.ref,
							widgetId: targetWidget.widgetId || null,
							source: "workspace"
						}, currentState?.widgets || {}),
						focusedWidget: targetWidget.ref
					} },
					affectedRefs: mergeUniqueRefs([previousFocusedWidgetRef], [targetWidget.ref, "shared"]),
					result: {
						focusedWidgetRef: targetWidget.ref,
						widgetId: targetWidget.widgetId
					},
					verificationHints: ["Read the workspace state and confirm shared.focusedWidget matches the requested target."],
					notes: { userVisibleSummary: `Focused widget set to ${targetWidget.widgetId}.` }
				};
			});
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/support/implicitQueryScope.js
	function selectionRefBelongsToWidget(selectionRef, targetWidget = null) {
		if (typeof selectionRef !== "string" || selectionRef.length === 0 || !targetWidget) return false;
		if (targetWidget?.selections?.[selectionRef]) return true;
		return selectionRef.startsWith(`${targetWidget.ref}/selection/`);
	}
	function resolveImplicitSelectionRefForWidget({ targetWidget = null, explicitSelectionRef = null, selectionRegistry = {}, primarySelectionRef = null } = {}) {
		if (typeof explicitSelectionRef === "string" && explicitSelectionRef.length > 0) return explicitSelectionRef;
		if (!targetWidget || !selectionRegistry || typeof selectionRegistry !== "object") return null;
		if (primarySelectionRef && selectionRegistry?.[primarySelectionRef] && selectionRefBelongsToWidget(primarySelectionRef, targetWidget)) return primarySelectionRef;
		const widgetSelectionRefs = Object.keys(selectionRegistry).filter((selectionRef) => selectionRefBelongsToWidget(selectionRef, targetWidget));
		if (widgetSelectionRefs.length === 1) return widgetSelectionRefs[0];
		return null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/DataQueryExecutor.js
	function makeDataQueryHandlerContextCapabilities(capabilities = {}) {
		return {
			workspaceRead: false,
			runtimeDataRead: false,
			widgetTargetResolution: false,
			dataHandleResolution: false,
			selectionTargetResolution: false,
			explicitTargetValidation: false,
			selectionScopedQueries: false,
			traceRecording: false,
			...capabilities
		};
	}
	function makeDataQueryHandlerContextIntegrations(integrations = {}) {
		return {
			store: false,
			traceRecorder: false,
			...integrations
		};
	}
	function makeDataQueryHandlerContextSummary(summary = {}) {
		return {
			methods: Array.isArray(summary?.methods) ? [...summary.methods] : [],
			capabilities: makeDataQueryHandlerContextCapabilities(summary?.capabilities),
			integrations: makeDataQueryHandlerContextIntegrations(summary?.integrations)
		};
	}
	var DataQueryHandlerContext = class {
		constructor({ store, call, traceRecorder, hostBridge }) {
			this.store = store;
			this.call = call;
			this.traceRecorder = traceRecorder;
			this.hostBridge = hostBridge || createDefaultWidgetVAHostBridge();
			this.pendingDataQueryTrace = null;
		}
		static describeContract() {
			return makeDataQueryHandlerContextSummary({
				methods: [
					"readDescription",
					"readCurrentState",
					"readSelectionRegistry",
					"readFocusState",
					"readHighlightState",
					"readQueryScope",
					"readNormalizedQuerySpec",
					"readFocusedWidgetRef",
					"readPrimarySelectionRef",
					"readRuntimeData",
					"resolveTargetWidget",
					"resolveExplicitTarget",
					"resolveSelectionDataRef",
					"resolveImplicitSelectionRef",
					"resolveDataRef",
					"resolveDataHandle",
					"resolveSelectionState",
					"resolveRows",
					"recordDataQuery"
				],
				capabilities: makeDataQueryHandlerContextCapabilities({
					workspaceRead: true,
					runtimeDataRead: true,
					widgetTargetResolution: true,
					dataHandleResolution: true,
					selectionTargetResolution: true,
					explicitTargetValidation: true,
					selectionScopedQueries: true,
					traceRecording: true
				}),
				integrations: makeDataQueryHandlerContextIntegrations({
					store: true,
					traceRecorder: true
				})
			});
		}
		readDescription() {
			return readWorkspaceDescriptionFromStore(this.store);
		}
		readCurrentState(options = {}) {
			return readWorkspaceStateFromStore(this.store, options);
		}
		readSelectionRegistry() {
			const hostRegistry = this.hostBridge.readSelectionRegistry?.();
			if (hostRegistry && typeof hostRegistry === "object" && !Array.isArray(hostRegistry) && Object.keys(hostRegistry).length > 0) return hostRegistry;
			return readSelectionRegistry(this.readCurrentState()?.shared || {});
		}
		readFocusState() {
			const hostFocusState = this.hostBridge.readFocusState?.();
			if (hostFocusState && typeof hostFocusState === "object" && !Array.isArray(hostFocusState)) return hostFocusState;
			const currentState = this.readCurrentState();
			return readFocusState(currentState?.shared || {}, currentState?.widgets || {});
		}
		readHighlightState() {
			const hostHighlightState = this.hostBridge.readHighlightState?.();
			if (hostHighlightState && typeof hostHighlightState === "object" && !Array.isArray(hostHighlightState)) return hostHighlightState;
			return deriveHighlightState(this.readCurrentState() || {});
		}
		readQueryScope() {
			return readNormalizedQueryScope({
				call: this.call,
				querySpec: this.readQuerySpec()
			});
		}
		readTargetWidgetRef() {
			return readNormalizedQueryScope({
				call: this.call,
				querySpec: this.readQuerySpec()
			}).widgetRef;
		}
		readNormalizedQuerySpec() {
			return this.normalizeScopedParams(this.readQuerySpec(), {
				querySpec: this.readQuerySpec(),
				includeTopLevelLegacyTargeting: false
			});
		}
		readFocusedWidgetRef() {
			const hostFocusedWidgetRef = this.hostBridge.readFocusedWidgetRef?.();
			if (typeof hostFocusedWidgetRef === "string" && hostFocusedWidgetRef.length > 0) return hostFocusedWidgetRef;
			const hostFocusWidgetRef = this.readFocusState()?.widgetRef;
			if (typeof hostFocusWidgetRef === "string" && hostFocusWidgetRef.length > 0) return hostFocusWidgetRef;
			return this.readCurrentState()?.shared?.focusedWidget || null;
		}
		readPrimarySelectionRef() {
			const hostPrimarySelectionRef = this.hostBridge.readPrimarySelectionRef?.();
			if (typeof hostPrimarySelectionRef === "string" && hostPrimarySelectionRef.length > 0) return hostPrimarySelectionRef;
			return this.readCurrentState()?.shared?.selections?.views?.primary?.selectionRef || null;
		}
		readRuntimeData(dataRef) {
			return readRuntimeDataFromStore(this.store, dataRef);
		}
		normalizeScopedParams(params = {}, options = {}) {
			return buildScopedParams({
				params,
				call: options?.call || this.call,
				querySpec: options?.querySpec || this.readQuerySpec(),
				includeTopLevelLegacyTargeting: options?.includeTopLevelLegacyTargeting ?? true
			});
		}
		resolveTargetWidget(targetRef = this.readQueryScope()?.widgetRef || null) {
			const resolvedTargetRef = targetRef || this.readQueryScope()?.widgetRef || null;
			if (!resolvedTargetRef) return null;
			return resolveWidgetRecordFromStore(this.store, resolvedTargetRef);
		}
		readQuerySpec() {
			return this.call?.query?.spec || {};
		}
		resolveExplicitTarget(options = {}) {
			const normalizedOptions = this.normalizeScopedParams(options, { querySpec: options });
			const callTargetRef = this.readQueryScope()?.widgetRef || null;
			const dataRef = normalizedOptions?.queryScope?.dataRef || this.call?.dataRef || this.readQueryScope()?.dataRef;
			const targetRef = callTargetRef;
			if (dataRef) {
				const selectionDataRef = this.resolveSelectionDataRef(dataRef);
				if (selectionDataRef) return {
					kind: "data",
					ref: selectionDataRef,
					sourceRef: dataRef
				};
				if (this.store.getDataHandle?.(dataRef) || this.readRuntimeData(dataRef)) return {
					kind: "data",
					ref: dataRef
				};
				return null;
			}
			if (!targetRef) return null;
			const selectionDataRef = this.resolveSelectionDataRef(targetRef);
			if (selectionDataRef) return {
				kind: "data",
				ref: selectionDataRef,
				sourceRef: targetRef
			};
			if (this.store.getDataHandle?.(targetRef) || this.readRuntimeData(targetRef)) return {
				kind: "data",
				ref: targetRef
			};
			const targetWidget = this.resolveTargetWidget(targetRef);
			if (targetWidget?.ref) return {
				kind: "widget",
				ref: targetWidget.ref,
				widget: targetWidget
			};
			return null;
		}
		resolveSelectionDataRef(selectionRef) {
			if (!selectionRef) return null;
			const resolvedDataRef = resolveSelectionDataRefFromStore(this.store, selectionRef);
			if (!resolvedDataRef) return null;
			if (readDataHandleFromStore(this.store, resolvedDataRef) || this.readRuntimeData(resolvedDataRef)) return resolvedDataRef;
			return null;
		}
		resolveImplicitSelectionRef({ queryScope = null, dataRef = null, targetWidget = null } = {}) {
			const explicitSelectionRef = queryScope?.selectionRef || null;
			if (typeof explicitSelectionRef === "string" && explicitSelectionRef.length > 0) return explicitSelectionRef;
			const scopedRuntimeData = dataRef ? this.readRuntimeData(dataRef) : null;
			const runtimeSelectionRef = scopedRuntimeData?.sourceSelectionRef || null;
			if (typeof runtimeSelectionRef === "string" && runtimeSelectionRef.length > 0) return runtimeSelectionRef;
			return resolveImplicitSelectionRefForWidget({
				targetWidget: targetWidget || (scopedRuntimeData?.widgetRef ? this.resolveTargetWidget(scopedRuntimeData.widgetRef) : null) || this.resolveTargetWidget(this.readTargetWidgetRef() || this.readFocusedWidgetRef()) || null,
				explicitSelectionRef,
				selectionRegistry: this.readSelectionRegistry() || {},
				primarySelectionRef: this.readPrimarySelectionRef()
			});
		}
		resolveDataRef(preferredDataRef = this.call?.dataRef) {
			const queryScope = this.readNormalizedQuerySpec()?.queryScope || this.readQueryScope();
			if (preferredDataRef) return preferredDataRef;
			if (typeof queryScope?.dataRef === "string" && queryScope.dataRef.length > 0) return queryScope.dataRef;
			const explicitTarget = this.resolveExplicitTarget();
			if (explicitTarget?.kind === "data") return explicitTarget.ref;
			if (explicitTarget?.kind === "widget") return this.resolveCompositePrimarySelectionDataRef(explicitTarget.widget) || explicitTarget.widget?.data?.currentDataRef || explicitTarget.widget?.data?.sourceDataRef || explicitTarget.widget?.primaryDataRef || null;
			const focusedWidgetRef = this.readFocusedWidgetRef();
			const focusedWidget = focusedWidgetRef ? this.store.getResolvedWidget?.(focusedWidgetRef) || null : null;
			return focusedWidget?.data?.currentDataRef || focusedWidget?.data?.sourceDataRef || focusedWidget?.primaryDataRef || null;
		}
		resolveDataHandle(dataRef = this.call?.dataRef) {
			const resolvedDataRef = this.resolveDataRef(dataRef);
			return resolvedDataRef ? readDataHandleFromStore(this.store, resolvedDataRef) : null;
		}
		resolveSelectionState(selectionRef) {
			if (!selectionRef) return null;
			return this.readSelectionRegistry()?.[selectionRef] || null;
		}
		resolveCompositePrimarySelectionDataRef(targetWidget = null) {
			if (targetWidget?.kind !== "custom") return null;
			const currentState = this.readCurrentState() || {};
			const selectionRegistry = this.readSelectionRegistry() || {};
			const primarySelectionRef = this.readPrimarySelectionRef();
			if (primarySelectionRef) {
				const dataRef = this.resolveSelectionDataRef(primarySelectionRef);
				if (dataRef) return dataRef;
			}
			const resolveWidgetDataRefForSelection = (selectionRef) => {
				if (!selectionRef) return null;
				const sourceWidget = Object.values(currentState?.widgets || {}).find((widget) => widget?.selections?.[selectionRef]) || null;
				return sourceWidget?.data?.currentDataRef || sourceWidget?.data?.sourceDataRef || sourceWidget?.primaryDataRef || null;
			};
			if (primarySelectionRef) {
				const sourceWidgetDataRef = resolveWidgetDataRefForSelection(primarySelectionRef);
				if (sourceWidgetDataRef) return sourceWidgetDataRef;
			}
			const focusedWidgetRef = this.readFocusedWidgetRef();
			const focusedWidget = focusedWidgetRef ? resolveWidgetRecordFromStore(this.store, focusedWidgetRef) : null;
			if (focusedWidget) {
				const implicitFocusedSelectionRef = resolveImplicitSelectionRefForWidget({
					targetWidget: focusedWidget,
					selectionRegistry,
					primarySelectionRef
				});
				const dataRef = this.resolveSelectionDataRef(implicitFocusedSelectionRef);
				if (dataRef) return dataRef;
				const sourceWidgetDataRef = resolveWidgetDataRefForSelection(implicitFocusedSelectionRef);
				if (sourceWidgetDataRef) return sourceWidgetDataRef;
				const focusedWidgetDataRef = focusedWidget?.data?.currentDataRef || focusedWidget?.data?.sourceDataRef || focusedWidget?.primaryDataRef || null;
				if (focusedWidgetDataRef) return focusedWidgetDataRef;
			}
			const selectionRefs = Object.keys(selectionRegistry).filter(Boolean);
			if (selectionRefs.length === 1) return this.resolveSelectionDataRef(selectionRefs[0]) || resolveWidgetDataRefForSelection(selectionRefs[0]);
			return null;
		}
		resolveRows(dataRef = this.call?.dataRef, options = {}) {
			const resolvedDataRef = this.resolveDataRef(dataRef);
			const runtimeData = this.readRuntimeData(resolvedDataRef);
			const baseRows = Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
			const normalizedOptions = this.normalizeScopedParams(options, { querySpec: options });
			const selectionRef = this.resolveImplicitSelectionRef({
				queryScope: normalizedOptions?.queryScope || this.readQueryScope() || null,
				dataRef: resolvedDataRef
			});
			if (!selectionRef) return baseRows;
			const selectionState = this.resolveSelectionState(selectionRef);
			if (!selectionState) return null;
			const predicates = Array.isArray(selectionState?.predicates) ? selectionState.predicates : [];
			if (predicates.length === 0) return baseRows;
			return baseRows.filter((row) => predicates.every((predicate) => applyPredicate$1(row, predicate)));
		}
		recordDataQuery({ affectedRefs = [], notes } = {}) {
			this.pendingDataQueryTrace = {
				call: this.call,
				affectedRefs,
				notes
			};
		}
		consumeRecordedDataQuery() {
			const pending = this.pendingDataQueryTrace;
			this.pendingDataQueryTrace = null;
			return pending;
		}
	};
	function applyPredicate$1(row, predicate) {
		const value = row?.[predicate?.field];
		if (predicate?.op === "between" && Array.isArray(predicate?.value)) return value >= predicate.value[0] && value <= predicate.value[1];
		if (predicate?.op === "gte" || predicate?.op === "greaterThanOrEqual") return value >= predicate?.value;
		if (predicate?.op === "lte" || predicate?.op === "lessThanOrEqual") return value <= predicate?.value;
		if (predicate?.op === "gt" || predicate?.op === "greaterThan") return value > predicate?.value;
		if (predicate?.op === "lt" || predicate?.op === "lessThan") return value < predicate?.value;
		if (predicate?.op === "equals" || predicate?.op === "eq") return value === predicate?.value;
		if (predicate?.op === "in" && Array.isArray(predicate?.value)) return predicate.value.includes(value);
		if (predicate?.op === "notIn" && Array.isArray(predicate?.value)) return !predicate.value.includes(value);
		return true;
	}
	function makeDataQueryExecutorEngineSummary(summary = {}) {
		return {
			kind: null,
			className: null,
			...summary
		};
	}
	function makeDataQueryExecutorCounts(counts = {}) {
		return {
			supportedQueryKindCount: 0,
			supportedQueryDescriptorCount: 0,
			...counts
		};
	}
	function makeDataQueryExecutorCapabilities(capabilities = {}) {
		return {
			schemaValidation: true,
			returnsValidation: true,
			traceRecording: false,
			runtimeDataReads: false,
			...capabilities
		};
	}
	function makeDataQueryExecutorSummary(summary = {}) {
		return {
			supportedQueryKinds: [],
			supportedQueryDescriptors: [],
			...summary,
			engine: makeDataQueryExecutorEngineSummary(summary?.engine),
			counts: makeDataQueryExecutorCounts(summary?.counts),
			capabilities: makeDataQueryExecutorCapabilities(summary?.capabilities)
		};
	}
	function buildDataQueryError({ dataRef, code, message, details, recoveryHints }) {
		return makeDataQueryResult({
			ok: false,
			dataRef,
			error: makeResultError({
				code,
				message,
				...details !== void 0 ? { details } : {}
			}),
			...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {}
		});
	}
	function recordDataQueryFailure(traceRecorder, call, { code, message, details, recoveryHints }) {
		traceRecorder?.recordDataQueryFailure?.({
			call,
			code,
			message,
			details,
			recoveryHints
		});
	}
	function buildDataQueryTraceNotes({ kind, selectionRef = null, dataRef = null, rowCount = null } = {}) {
		return {
			userVisibleSummary: `Data query: ${kind}${selectionRef ? ` scoped to ${selectionRef}` : ""}${Number.isFinite(rowCount) ? ` over ${rowCount} row${rowCount === 1 ? "" : "s"}` : ""}`,
			rationale: selectionRef ? "A selection-scoped data query was requested for the current runtime data view." : "A data query was requested for the current runtime data view.",
			...dataRef ? { verification: `Query executed against ${dataRef}.` } : {}
		};
	}
	function buildSupportedQueryDescriptors(kinds = []) {
		return kinds.map((kind) => makeDataQueryDescriptor({
			name: kind,
			...DATA_QUERY_DESCRIPTOR_TEMPLATES[kind],
			inputSchema: DATA_QUERY_SCHEMAS[kind] || {
				type: "object",
				additionalProperties: true,
				properties: {}
			}
		}));
	}
	function supportsUnifiedEngineQuery(engine) {
		if (!engine || typeof engine.query !== "function") return false;
		if (!(engine instanceof DataQueryEngine)) return true;
		if (engine.query !== DataQueryEngine.prototype.query) return true;
		if (typeof engine.resolveSource === "function") return true;
		if (typeof engine.options?.resolveSource === "function") return true;
		return false;
	}
	var DataQueryExecutor = class {
		constructor({ store, dataQueryEngine, traceRecorder, hostBridge } = {}) {
			this.store = store || {
				readDescription() {
					return {
						widgets: [],
						dataHandles: []
					};
				},
				readState() {
					return { shared: { focusedWidget: null } };
				},
				getResolvedWidgetForTarget() {
					return null;
				},
				getResolvedWidget() {
					return null;
				},
				getDataHandle() {
					return null;
				},
				readRuntimeData() {
					return null;
				},
				resolveSelectionDataRef() {
					return null;
				}
			};
			this.dataQueryEngine = dataQueryEngine || {
				kind: "manual",
				listSupportedQueryKinds() {
					return [];
				}
			};
			this.traceRecorder = traceRecorder || null;
			this.hostBridge = hostBridge || createDefaultWidgetVAHostBridge();
		}
		run(call = {}) {
			try {
				validateAgainstSchema(call, DATA_QUERY_CALL_SCHEMA, "dataQuery");
			} catch (error) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "INVALID_QUERY_SPEC",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the data query call schema in describeWorkspace() before retrying."]
				});
				return buildDataQueryError({
					dataRef: call?.dataRef || null,
					code: "INVALID_QUERY_SPEC",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the data query call schema in describeWorkspace() before retrying."]
				});
			}
			const ctx = this.createContext(call);
			const querySpec = ctx.readNormalizedQuerySpec();
			const queryScope = ctx.readQueryScope();
			const targetRef = ctx.readTargetWidgetRef() || null;
			const explicitTargetRef = targetRef || queryScope?.dataRef || null;
			const explicitTarget = ctx.resolveExplicitTarget({
				dataRef: call?.dataRef || querySpec?.queryScope?.dataRef || null,
				targetRef
			});
			if (!call?.dataRef && !querySpec?.queryScope?.dataRef && explicitTargetRef && !explicitTarget) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "UNSUPPORTED_TARGET",
					message: `The requested data-query target ref is not available: ${explicitTargetRef}.`,
					details: { targetRef: explicitTargetRef },
					recoveryHints: ["Call describeWorkspace() to inspect valid widget refs and data refs before retrying.", "Retry the data query with a targetRef that resolves to a materialized widget or data handle."]
				});
				return buildDataQueryError({
					dataRef: null,
					code: "UNSUPPORTED_TARGET",
					message: `The requested data-query target ref is not available: ${explicitTargetRef}.`,
					details: { targetRef: explicitTargetRef },
					recoveryHints: ["Call describeWorkspace() to inspect valid widget refs and data refs before retrying.", "Retry the data query with a targetRef that resolves to a materialized widget or data handle."]
				});
			}
			const dataRef = ctx.resolveDataRef(call?.dataRef);
			const query = call?.query || {};
			const selectionRef = ctx.resolveImplicitSelectionRef({
				queryScope: querySpec?.queryScope || queryScope || null,
				dataRef
			});
			const runtimeData = ctx.readRuntimeData(dataRef);
			const dataHandle = ctx.resolveDataHandle(dataRef);
			if (!runtimeData) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "UNKNOWN_DATA_REF",
					message: `Unknown data ref: ${dataRef || "missing"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect materialized data handles and valid data refs.", "Retry the query with an explicit dataRef, a targetRef, or after focusing the widget whose data you want to inspect."]
				});
				return buildDataQueryError({
					dataRef,
					code: "UNKNOWN_DATA_REF",
					message: `Unknown data ref: ${dataRef || "missing"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect materialized data handles and valid data refs.", "Retry the query with an explicit dataRef, a targetRef, or after focusing the widget whose data you want to inspect."]
				});
			}
			const rows = ctx.resolveRows(dataRef, { queryScope: { selectionRef } });
			const kind = query.kind;
			if (!DATA_QUERY_KINDS.includes(kind)) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "UNKNOWN_QUERY_KIND",
					message: `Unsupported data query kind: ${kind || "missing"}.`,
					recoveryHints: ["Inspect supportedQueryDescriptors on the current data handle before retrying."]
				});
				return buildDataQueryError({
					dataRef,
					code: "UNKNOWN_QUERY_KIND",
					message: `Unsupported data query kind: ${kind || "missing"}.`,
					recoveryHints: ["Inspect supportedQueryDescriptors on the current data handle before retrying."]
				});
			}
			if (dataHandle && Array.isArray(dataHandle.supportedQueries) && !dataHandle.supportedQueries.includes(kind)) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "UNSUPPORTED_QUERY_KIND",
					message: `Data query kind ${kind} is not supported by the current data handle.`,
					details: {
						supportedQueries: dataHandle.supportedQueries,
						dataHandleRef: dataHandle.ref || null
					},
					recoveryHints: ["Inspect supportedQueries or supportedQueryDescriptors on the current data handle before retrying.", "Retry with a query kind exposed by the current widget data handle."]
				});
				return buildDataQueryError({
					dataRef,
					code: "UNSUPPORTED_QUERY_KIND",
					message: `Data query kind ${kind} is not supported by the current data handle.`,
					details: {
						supportedQueries: dataHandle.supportedQueries,
						dataHandleRef: dataHandle.ref || null
					},
					recoveryHints: ["Inspect supportedQueries or supportedQueryDescriptors on the current data handle before retrying.", "Retry with a query kind exposed by the current widget data handle."]
				});
			}
			if (selectionRef && rows == null) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "INVALID_QUERY_SPEC",
					message: `Selection ref ${selectionRef} is not active in the current workspace state.`,
					recoveryHints: ["Inspect shared.selections.registry in readState() or call perception.summarizeSelection() before retrying.", "Retry the data query with an active selectionRef or omit selectionRef to query the full current view."]
				});
				return buildDataQueryError({
					dataRef,
					code: "INVALID_QUERY_SPEC",
					message: `Selection ref ${selectionRef} is not active in the current workspace state.`,
					recoveryHints: ["Inspect shared.selections.registry in readState() or call perception.summarizeSelection() before retrying.", "Retry the data query with an active selectionRef or omit selectionRef to query the full current view."]
				});
			}
			const querySchema = DATA_QUERY_SCHEMAS[kind];
			if (querySchema) try {
				validateAgainstSchema(querySpec, querySchema, `query.spec.${kind}`);
			} catch (error) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "INVALID_QUERY_SPEC",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the selected data query descriptor inputSchema/examples before retrying."]
				});
				return buildDataQueryError({
					dataRef,
					code: "INVALID_QUERY_SPEC",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the selected data query descriptor inputSchema/examples before retrying."]
				});
			}
			const resultSchema = DATA_QUERY_RESULT_SCHEMAS[kind] || null;
			try {
				let result;
				const runUnifiedQuery = () => this.dataQueryEngine.query(dataRef, {
					kind,
					spec: querySpec
				});
				if (kind === "schema") result = runtimeData.handle?.schema || (supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.getSchema(rows));
				if (kind === "filter") {
					const predicates = Array.isArray(querySpec?.predicates) ? querySpec.predicates : [];
					const filteredRows = supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.filter(rows, predicates);
					result = {
						rows: Array.isArray(filteredRows?.rows) ? filteredRows.rows : filteredRows,
						rowCount: Array.isArray(filteredRows?.rows) ? filteredRows.rows.length : filteredRows.length,
						predicates
					};
				}
				if (kind === "sampleRows") result = supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.sampleRows(rows, querySpec?.limit || 20);
				if (kind === "summary") result = supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.summarize(rows, normalizeSummarySpec(querySpec));
				if (kind === "aggregate" || kind === "groupBy") {
					const aggregateSpec = normalizeAggregateSpec(querySpec);
					result = normalizeAggregateResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.aggregate(rows, aggregateSpec));
				}
				if (kind === "sql") result = supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.executeSql(rows, querySpec);
				if (kind === "computeCorrelation") result = normalizeCorrelationResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.computeCorrelation(rows, querySpec), querySpec);
				if (kind === "findExtremes") result = normalizeExtremesResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.findExtremes(rows, normalizeExtremesSpec(querySpec)), querySpec);
				if (kind === "findOutliers") result = normalizeOutliersResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.findOutliers(rows, querySpec), querySpec);
				if (kind === "compareGroups") result = normalizeCompareGroupsResult(supportsUnifiedEngineQuery(this.dataQueryEngine) ? runUnifiedQuery() : this.dataQueryEngine.compareGroups(rows, normalizeCompareGroupsSpec(querySpec)), querySpec);
				const output = makeDataQueryResult({
					ok: false,
					dataRef,
					error: null
				});
				if (result !== void 0) {
					if (result?.ok === false && result?.error) {
						recordDataQueryFailure(this.traceRecorder, call, {
							code: result.error.code || "RUNTIME_ERROR",
							message: result.error.message || "Data query execution failed.",
							...result?.error?.details !== void 0 ? { details: result.error.details } : {},
							recoveryHints: Array.isArray(result?.recoveryHints) ? result.recoveryHints : []
						});
						output.error = result.error;
						if (Array.isArray(result?.recoveryHints)) output.recoveryHints = result.recoveryHints;
						return output;
					}
					output.ok = true;
					output.result = result;
					if (resultSchema) validateAgainstSchema(output.result, resultSchema, `result.${kind}`);
					ctx.recordDataQuery({
						affectedRefs: runtimeData.widgetRef ? [runtimeData.widgetRef] : [dataRef],
						notes: buildDataQueryTraceNotes({
							kind,
							selectionRef,
							dataRef,
							rowCount: Array.isArray(rows) ? rows.length : null
						})
					});
					const tracePayload = ctx.consumeRecordedDataQuery?.() || null;
					if (tracePayload) this.traceRecorder?.recordDataQuery?.(tracePayload);
				} else {
					recordDataQueryFailure(this.traceRecorder, call, {
						code: "UNKNOWN_QUERY_KIND",
						message: `Unsupported data query kind: ${kind || "missing"}.`,
						recoveryHints: ["Inspect supportedQueryDescriptors on the current data handle before retrying."]
					});
					output.error = {
						code: "UNKNOWN_QUERY_KIND",
						message: `Unsupported data query kind: ${kind || "missing"}.`
					};
					output.recoveryHints = ["Inspect supportedQueryDescriptors on the current data handle before retrying."];
				}
				return output;
			} catch (error) {
				recordDataQueryFailure(this.traceRecorder, call, {
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the current data handle, target ref, and query kind in describeWorkspace() before retrying.", "Inspect the selected data query descriptor inputSchema/resultSchema/examples if the runtime result shape does not match the declared contract."]
				});
				return buildDataQueryError({
					dataRef,
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the current data handle, target ref, and query kind in describeWorkspace() before retrying.", "Inspect the selected data query descriptor inputSchema/resultSchema/examples if the runtime result shape does not match the declared contract."]
				});
			}
		}
		createContext(call = {}) {
			return new DataQueryHandlerContext({
				store: this.store,
				call,
				traceRecorder: this.traceRecorder,
				hostBridge: this.hostBridge
			});
		}
		describeExecutor() {
			const supportedQueryKinds = typeof this.dataQueryEngine?.listSupportedQueryKinds === "function" ? this.dataQueryEngine.listSupportedQueryKinds() : [...DATA_QUERY_KINDS];
			const supportedQueryDescriptors = buildSupportedQueryDescriptors(supportedQueryKinds);
			return makeDataQueryExecutorSummary({
				engine: makeDataQueryExecutorEngineSummary({
					kind: this.dataQueryEngine?.kind || null,
					className: this.dataQueryEngine?.constructor?.name || null
				}),
				counts: makeDataQueryExecutorCounts({
					supportedQueryKindCount: supportedQueryKinds.length,
					supportedQueryDescriptorCount: supportedQueryDescriptors.length
				}),
				capabilities: makeDataQueryExecutorCapabilities({
					schemaValidation: true,
					returnsValidation: true,
					traceRecording: typeof this.traceRecorder?.recordDataQuery === "function",
					runtimeDataReads: typeof this.store?.readRuntimeData === "function"
				}),
				supportedQueryKinds,
				supportedQueryDescriptors
			});
		}
		describeContext() {
			return DataQueryHandlerContext.describeContract();
		}
	};
	function normalizeAggregateSpec(spec = {}) {
		const groupBy = Array.isArray(spec.groupBy) ? spec.groupBy : [];
		const measures = Array.isArray(spec.measures) ? spec.measures : Array.isArray(spec.metrics) ? spec.metrics : [];
		return {
			...spec,
			groupBy,
			measures
		};
	}
	function normalizeAggregateResult(result) {
		return {
			rows: Array.isArray(result?.rows) ? result.rows : [],
			rowCount: Number.isFinite(result?.rowCount) ? result.rowCount : Array.isArray(result?.rows) ? result.rows.length : 0
		};
	}
	function buildSummaryMeasures({ fields = [], metrics = [] } = {}) {
		const normalizedFields = Array.isArray(fields) ? fields.filter((field) => typeof field === "string" && field.length > 0) : [];
		const normalizedMetrics = Array.isArray(metrics) ? metrics.filter((metric) => typeof metric === "string" && metric.length > 0) : [];
		const measures = [];
		for (const metric of normalizedMetrics) {
			if (metric === "count") {
				measures.push({
					op: "count",
					as: "count"
				});
				continue;
			}
			for (const field of normalizedFields) measures.push({
				op: metric,
				field,
				as: `${field}_${metric}`
			});
		}
		return measures;
	}
	function normalizeSummarySpec(spec = {}) {
		const measures = Array.isArray(spec.measures) && spec.measures.length > 0 ? spec.measures : buildSummaryMeasures({
			fields: spec.fields,
			metrics: spec.metrics
		});
		return {
			...spec,
			measures
		};
	}
	function normalizeExtremesSpec(spec = {}) {
		const direction = typeof spec.direction === "string" ? spec.direction : null;
		return {
			...spec,
			order: direction === "min" ? "ascending" : "descending"
		};
	}
	function normalizeExtremesResult(result, spec = {}) {
		const direction = typeof spec.direction === "string" ? spec.direction : spec.order === "ascending" ? "min" : "max";
		return {
			field: typeof spec.field === "string" ? spec.field : null,
			direction,
			rows: Array.isArray(result?.rows) ? result.rows : []
		};
	}
	function normalizeOutliersResult(result, spec = {}) {
		return {
			field: typeof spec.field === "string" ? spec.field : null,
			method: typeof spec.method === "string" ? spec.method : "zscore",
			rows: Array.isArray(result?.rows) ? result.rows : [],
			...result?.summary ? { summary: result.summary } : {}
		};
	}
	function normalizeCompareGroupsSpec(spec = {}) {
		if (Array.isArray(spec.groups) && spec.groups.length >= 2) return spec;
		return {
			...spec,
			groups: [spec.leftGroup, spec.rightGroup].filter((value) => value != null)
		};
	}
	function normalizeCompareGroupsResult(result, spec = {}) {
		const groups = Array.isArray(result?.groups) ? result.groups : [];
		const groupField = typeof spec.groupField === "string" ? spec.groupField : null;
		const valueField = typeof spec.valueField === "string" ? spec.valueField : null;
		let comparison = null;
		if (groups.length >= 2) {
			const [left, right] = groups;
			const leftMean = typeof left?.mean === "number" ? left.mean : null;
			const rightMean = typeof right?.mean === "number" ? right.mean : null;
			comparison = {
				groupField,
				valueField,
				leftGroup: left?.group ?? null,
				rightGroup: right?.group ?? null,
				deltaMean: leftMean != null && rightMean != null ? leftMean - rightMean : null
			};
		}
		return {
			groupField,
			valueField,
			groups,
			comparison
		};
	}
	function normalizeCorrelationResult(result, spec = {}) {
		return {
			xField: typeof spec.xField === "string" ? spec.xField : result?.fields?.[0] || null,
			yField: typeof spec.yField === "string" ? spec.yField : result?.fields?.[1] || null,
			correlation: typeof result?.coefficient === "number" ? result.coefficient : null,
			sampleSize: Number.isFinite(result?.sampleSize) ? result.sampleSize : 0
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/pagePort/installPagePort.js
	function clone$25(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function firstFunction(...candidates) {
		return candidates.find((candidate) => typeof candidate === "function") || null;
	}
	function installWidgetVAPagePort({ root = globalThis.window, api = {}, describeWorkspace = null, readState = null, readView = null, readSnapshot = null, listStateHistory = null, listBranches = null, executeAction = null, executeVerifiedAction = null, jumpToState = null, branchFromState = null, replay = null, queryPerception = null, queryData = null, runDataQuery = null, readTrace = null, getInteractionTrace = null, getTraceGraph = null, getLatestAgentResponse = null, listAgentResponses = null, evaluateLinkPropagation = null, recordAgentResponse = null } = {}) {
		if (!root) throw new Error("installWidgetVAPagePort requires a root object such as window.");
		const describeWorkspaceFn = firstFunction(api.describeWorkspace?.bind(api), describeWorkspace);
		const readStateFn = firstFunction(api.readState?.bind(api), readState);
		const readViewFn = firstFunction(api.readView?.bind(api), readView, readStateFn);
		const readSnapshotFn = firstFunction(api.readSnapshot?.bind(api), readSnapshot);
		const listStateHistoryFn = firstFunction(api.listStateHistory?.bind(api), listStateHistory);
		const listBranchesFn = firstFunction(api.listBranches?.bind(api), listBranches);
		const executeActionFn = firstFunction(api.executeAction?.bind(api), executeAction);
		const executeVerifiedActionFn = firstFunction(api.executeVerifiedAction?.bind(api), executeVerifiedAction);
		const jumpToStateFn = firstFunction(api.jumpToState?.bind(api), jumpToState);
		const branchFromStateFn = firstFunction(api.branchFromState?.bind(api), branchFromState);
		const replayFn = firstFunction(api.replay?.bind(api), replay, jumpToStateFn);
		const queryPerceptionFn = firstFunction(api.queryPerception?.bind(api), queryPerception);
		const queryDataFn = firstFunction(api.queryData?.bind(api), api.runDataQuery?.bind(api), queryData, runDataQuery);
		const readTraceFn = firstFunction(api.readTrace?.bind(api), api.getInteractionTrace?.bind(api), readTrace, getInteractionTrace);
		const getTraceGraphFn = firstFunction(api.getTraceGraph?.bind(api), getTraceGraph);
		const getLatestAgentResponseFn = firstFunction(api.getLatestAgentResponse?.bind(api), getLatestAgentResponse);
		const listAgentResponsesFn = firstFunction(api.listAgentResponses?.bind(api), listAgentResponses);
		const evaluateLinkPropagationFn = firstFunction(api.evaluateLinkPropagation?.bind(api), evaluateLinkPropagation);
		const recordAgentResponseFn = firstFunction(api.recordAgentResponse?.bind(api), recordAgentResponse);
		const port = {
			async describePagePort() {
				const methods = PAGE_PORT_METHODS.filter((name) => typeof port[name] === "function");
				const methodSet = new Set(methods);
				const aliases = Object.fromEntries(Object.entries(PAGE_PORT_ALIASES).filter(([, method]) => methodSet.has(method)));
				const aliasSet = new Set(Object.keys(aliases));
				return {
					version: "1.0.0",
					methods,
					aliases,
					transportHints: {
						recommendedTools: [
							"workspace_describe",
							"view_read",
							"action_run",
							"perception_query",
							"interaction_trace_read"
						].filter((alias) => aliasSet.has(alias)),
						optionalTools: [
							"data_query",
							"trace_graph_read",
							"read_snapshot",
							"state_history_read",
							"branch_list",
							"verified_action_run",
							"jump_to_state",
							"branch_from_state",
							"link_propagation_evaluate",
							"agent_response_read",
							"agent_response_list",
							"agent_response_record"
						].filter((alias) => aliasSet.has(alias)),
						note: "External transports should call stable page-port methods only; runtime internals are intentionally not exposed."
					},
					methodDescriptors: Object.fromEntries(methods.map((name) => {
						const descriptor = clone$25(PAGE_PORT_METHOD_DESCRIPTORS[name] || {
							stability: "stable",
							aliases: [],
							inputSchema: {
								type: "object",
								additionalProperties: true,
								properties: {}
							},
							returns: {
								kind: "result",
								description: ""
							},
							errors: []
						});
						descriptor.aliases = Object.entries(PAGE_PORT_ALIASES).filter(([, methodName]) => methodName === name).map(([alias]) => alias);
						return [name, descriptor];
					})),
					errorCatalog: clone$25(PAGE_PORT_ERROR_CODES)
				};
			},
			...describeWorkspaceFn ? { describeWorkspace: (options = {}) => describeWorkspaceFn(options) } : {},
			parseRef: (options = {}) => parseRef(options?.ref),
			...readStateFn || readViewFn ? {
				readState: (options = {}) => (readStateFn || readViewFn)(options),
				readView: (options = {}) => (readViewFn || readStateFn)(options)
			} : {},
			...readSnapshotFn ? { readSnapshot: (options = {}) => readSnapshotFn(options) } : {},
			...listStateHistoryFn ? { listStateHistory: (options = {}) => listStateHistoryFn(options) } : {},
			...listBranchesFn ? { listBranches: (options = {}) => listBranchesFn(options) } : {},
			...executeActionFn ? { executeAction: (call = {}) => executeActionFn(call) } : {},
			...executeVerifiedActionFn ? { executeVerifiedAction: (call = {}, options = {}) => executeVerifiedActionFn(call, options) } : {},
			...jumpToStateFn ? {
				jumpToState: (options = {}) => jumpToStateFn(options),
				replay: (options = {}) => replayFn(options)
			} : {},
			...branchFromStateFn ? { branchFromState: (options = {}) => branchFromStateFn(options) } : {},
			...queryPerceptionFn ? { queryPerception: (call = {}) => queryPerceptionFn(call) } : {},
			...queryDataFn ? {
				runDataQuery: (call = {}) => queryDataFn(call),
				queryData: (call = {}) => queryDataFn(call)
			} : {},
			...readTraceFn ? {
				readTrace: (options = {}) => readTraceFn(options),
				getInteractionTrace: (options = {}) => readTraceFn(options)
			} : {},
			...getTraceGraphFn ? { getTraceGraph: (options = {}) => getTraceGraphFn(options) } : {},
			...getLatestAgentResponseFn ? { getLatestAgentResponse: (options = {}) => getLatestAgentResponseFn(options) } : {},
			...listAgentResponsesFn ? { listAgentResponses: (options = {}) => listAgentResponsesFn(options) } : {},
			...evaluateLinkPropagationFn ? { evaluateLinkPropagation: (options = {}) => evaluateLinkPropagationFn(options) } : {},
			...recordAgentResponseFn ? { recordAgentResponse: (record = {}) => recordAgentResponseFn(record) } : {}
		};
		for (const [alias, methodName] of Object.entries(PAGE_PORT_ALIASES)) if (typeof port[methodName] === "function") port[alias] = port[methodName];
		root.__widgetVA = port;
		return () => {
			if (root.__widgetVA === port) delete root.__widgetVA;
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/shapes/responseRecorderShapes.js
	function makeAgentResponseUsage(usage = {}) {
		return {
			promptTokens: null,
			completionTokens: null,
			totalTokens: null,
			tokenCost: null,
			tokenCostUnit: null,
			...usage
		};
	}
	function makeAgentResponseRecord(record = {}) {
		return {
			responseId: "",
			runId: null,
			sessionId: null,
			workspaceId: null,
			actor: "agent",
			mode: null,
			query: null,
			content: "",
			stateId: null,
			branchId: null,
			evidenceRefs: [],
			coordinationEvidence: null,
			usage: makeAgentResponseUsage(),
			createdAt: (/* @__PURE__ */ new Date()).toISOString(),
			...record,
			usage: record?.usage ? makeAgentResponseUsage(record.usage) : makeAgentResponseUsage()
		};
	}
	function makeResponseRecorderCapabilities(capabilities = {}) {
		return {
			recordsFinalResponses: true,
			latestResponseRead: true,
			responseHistoryRead: true,
			workspaceScopedReads: true,
			lineageTracking: true,
			...capabilities
		};
	}
	function makeResponseRecorderCounters(counters = {}) {
		return {
			workspaceId: null,
			responseCount: 0,
			latestResponseId: null,
			latestRunId: null,
			latestStateId: null,
			latestBranchId: null,
			latestActor: null,
			latestMode: null,
			latestQuery: null,
			latestEvidenceRefCount: 0,
			...counters
		};
	}
	function makeResponseRecorderSummary(summary = {}) {
		return {
			...summary,
			capabilities: makeResponseRecorderCapabilities(summary?.capabilities),
			counters: makeResponseRecorderCounters(summary?.counters)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/TraceRecorder.js
	function makeTraceRecorderCapabilities(capabilities = {}) {
		return {
			recordsActions: true,
			recordsPerceptionQueries: true,
			recordsPerceptionQueryFailures: true,
			recordsDataQueries: true,
			recordsDataQueryFailures: true,
			normalizedQueryFamily: true,
			recordsSystemTransitions: true,
			lineageTracking: true,
			...capabilities
		};
	}
	function makeTraceRecorderCounters(counters = {}) {
		return {
			traceCount: 0,
			latestStateId: null,
			latestBranchId: null,
			latestEventKind: null,
			latestEventFamily: null,
			latestQuerySurface: null,
			latestOutcome: null,
			...counters
		};
	}
	function makeTraceRecorderSummary(summary = {}) {
		return {
			eventKinds: [],
			eventFamilies: [],
			querySurfaces: [],
			...summary,
			capabilities: makeTraceRecorderCapabilities(summary?.capabilities),
			counters: makeTraceRecorderCounters(summary?.counters)
		};
	}
	var TraceRecorder = class {
		constructor({ store }) {
			this.store = store;
		}
		buildLineageFields(stateId) {
			const snapshot = stateId ? readSnapshotEntryFromStore(this.store, stateId) : readCurrentSnapshotMetaFromStore(this.store);
			return {
				parentStateId: snapshot?.parentStateId || null,
				branchId: snapshot?.branchId || null
			};
		}
		appendTrace(record) {
			return appendTraceRecordToStore(this.store, record);
		}
		recordAction({ call, updatedRefs, stateId, statePatch, primitive, notes }) {
			const lineage = this.buildLineageFields(stateId);
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "action",
				action: call,
				primitive: primitive || null,
				affectedRefs: updatedRefs,
				stateId,
				statePatch,
				...lineage,
				notes: {
					outcome: "success",
					...notes || {}
				}
			}));
		}
		recordActionFailure({ call, primitive = null, code, message, details, recoveryHints = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			const targetRef = readNormalizedQueryScope({ call }).widgetRef || null;
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "action",
				action: call,
				primitive,
				affectedRefs: targetRef ? [targetRef] : [],
				stateId: currentStateId,
				statePatch: {},
				...lineage,
				notes: {
					outcome: "failure",
					errorCode: code || "RUNTIME_ERROR",
					errorMessage: message || "Action execution failed.",
					...details !== void 0 ? { details } : {},
					...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {},
					...notes || {}
				}
			}));
		}
		recordQuery({ call, affectedRefs = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "perceptionQuery",
				query: call,
				affectedRefs,
				stateId: currentStateId,
				...lineage,
				notes: {
					outcome: "success",
					...notes || {}
				}
			}));
		}
		recordQueryFailure({ call, code, message, details, recoveryHints = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			const targetRef = readNormalizedQueryScope({ call }).widgetRef || null;
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "perceptionQuery",
				query: call,
				affectedRefs: targetRef ? [targetRef] : [],
				stateId: currentStateId,
				statePatch: {},
				...lineage,
				notes: {
					outcome: "failure",
					errorCode: code || "RUNTIME_ERROR",
					errorMessage: message || "Perception query execution failed.",
					...details !== void 0 ? { details } : {},
					...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {},
					...notes || {}
				}
			}));
		}
		recordDataQuery({ call, affectedRefs = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "dataQuery",
				query: call,
				affectedRefs,
				stateId: currentStateId,
				...lineage,
				notes: {
					outcome: "success",
					...notes || {}
				}
			}));
		}
		recordDataQueryFailure({ call, code, message, details, recoveryHints = [], notes }) {
			const currentStateId = readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			const normalizedScope = readNormalizedQueryScope({
				call,
				querySpec: call?.query?.spec || null
			});
			const dataRef = normalizedScope.dataRef || call?.dataRef || null;
			const affectedRefs = [normalizedScope.widgetRef || null, dataRef].filter(Boolean);
			this.appendTrace(makeInteractionTraceRecord({
				actor: call?.actor || "agent",
				eventKind: "dataQuery",
				query: call,
				affectedRefs,
				stateId: currentStateId,
				statePatch: {},
				...lineage,
				notes: {
					outcome: "failure",
					errorCode: code || "RUNTIME_ERROR",
					errorMessage: message || "Data query execution failed.",
					...details !== void 0 ? { details } : {},
					...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {},
					...notes || {}
				}
			}));
		}
		recordSystemTransition({ actor = "system", transitionType = "continue", stateId, affectedRefs = [], statePatch = {}, notes }) {
			const currentStateId = stateId || readWorkspaceStateFromStore(this.store)?.stateId || "unknown";
			const lineage = this.buildLineageFields(currentStateId);
			this.appendTrace(makeInteractionTraceRecord({
				actor,
				eventKind: "systemTransition",
				primitive: transitionType,
				affectedRefs,
				stateId: currentStateId,
				statePatch,
				...lineage,
				notes
			}));
		}
		recordResponse({ responseId, runId = null, sessionId = null, actor = "agent", mode = null, query = null, content, evidenceRefs = [], usage = null } = {}) {
			const safeContent = String(content || "").trim();
			if (!safeContent) throw new Error("TraceRecorder.recordResponse requires non-empty content.");
			const state = this.store.readState();
			const record = makeAgentResponseRecord({
				responseId: responseId || `response_${Date.now()}`,
				runId: runId || null,
				sessionId: sessionId || this.store.workspaceId || null,
				workspaceId: this.store.workspaceId || null,
				actor,
				mode: mode || null,
				query: query ? String(query) : null,
				content: safeContent,
				stateId: state?.stateId || null,
				branchId: this.store.currentBranchId || null,
				evidenceRefs: Array.isArray(evidenceRefs) ? [...evidenceRefs] : [],
				usage: usage && typeof usage === "object" ? usage : null,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			});
			this.store.appendResponse(record);
			return record;
		}
		readLatestResponse(options = {}) {
			return this.store.readLatestResponse(options);
		}
		listResponses(limit = 20, options = {}) {
			return this.store.listResponses(limit, options);
		}
		describeResponseRecorder() {
			const workspaceId = this.store.workspaceId || null;
			const latest = this.store.readLatestResponse({ workspaceId }) || null;
			const responses = Array.isArray(this.store?.responseHistory) ? this.store.responseHistory.filter((record) => {
				if (!workspaceId) return true;
				return record?.workspaceId === workspaceId;
			}) : [];
			return makeResponseRecorderSummary({
				capabilities: makeResponseRecorderCapabilities({
					recordsFinalResponses: true,
					latestResponseRead: true,
					responseHistoryRead: true,
					workspaceScopedReads: true,
					lineageTracking: true
				}),
				counters: makeResponseRecorderCounters({
					workspaceId,
					responseCount: responses.length,
					latestResponseId: latest?.responseId || null,
					latestRunId: latest?.runId || null,
					latestStateId: latest?.stateId || null,
					latestBranchId: latest?.branchId || null,
					latestActor: latest?.actor || null,
					latestMode: latest?.mode || null,
					latestQuery: latest?.query || null,
					latestEvidenceRefCount: Array.isArray(latest?.evidenceRefs) ? latest.evidenceRefs.length : 0
				})
			});
		}
		describeRecorder() {
			const trace = Array.isArray(this.store?.interactionTrace) ? this.store.interactionTrace : [];
			const latestRecord = trace[trace.length - 1] || null;
			return makeTraceRecorderSummary({
				capabilities: makeTraceRecorderCapabilities({
					recordsActions: true,
					recordsPerceptionQueries: true,
					recordsPerceptionQueryFailures: true,
					recordsDataQueries: true,
					recordsDataQueryFailures: true,
					normalizedQueryFamily: true,
					recordsSystemTransitions: true,
					lineageTracking: true
				}),
				counters: makeTraceRecorderCounters({
					traceCount: trace.length,
					latestStateId: latestRecord?.stateId || null,
					latestBranchId: latestRecord?.branchId || null,
					latestEventKind: latestRecord?.eventKind || null,
					latestEventFamily: latestRecord?.eventFamily || null,
					latestQuerySurface: latestRecord?.querySurface || null,
					latestOutcome: latestRecord?.notes?.outcome || null
				}),
				eventKinds: [
					"action",
					"perceptionQuery",
					"dataQuery",
					"systemTransition"
				],
				eventFamilies: [
					"action",
					"query",
					"systemTransition"
				],
				querySurfaces: ["perception", "data"]
			});
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/contracts/coordination-contracts.js
	function cloneValue$1(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeCoordinationRelation(relation = {}) {
		return {
			ref: relation?.ref || relation?.id || null,
			sourceStateRef: typeof relation?.sourceStateRef === "string" ? relation.sourceStateRef : null,
			targetStateRef: typeof relation?.targetStateRef === "string" ? relation.targetStateRef : null,
			relation: relation?.relation || "controls",
			transform: relation?.transform && typeof relation.transform === "object" && !Array.isArray(relation.transform) ? cloneValue$1(relation.transform) : null,
			activation: relation?.activation || relation?.activationPolicy || "automatic"
		};
	}
	function makeCoordinationRelationMap(relations = {}) {
		if (Array.isArray(relations)) return Object.fromEntries(relations.map((relation) => makeCoordinationRelation(relation)).filter((relation) => relation.ref && relation.sourceStateRef && relation.targetStateRef).map((relation) => [relation.ref, relation]));
		if (!relations || typeof relations !== "object") return {};
		return Object.fromEntries(Object.entries(relations).map(([ref, relation]) => {
			const normalizedRelation = makeCoordinationRelation({
				...relation || {},
				ref
			});
			return [normalizedRelation.ref, normalizedRelation];
		}).filter(([, relation]) => relation.ref && relation.sourceStateRef && relation.targetStateRef));
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/coordination/linkSemantics.js
	function clone$24(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readLinkActivationPolicy(link) {
		return makeWidgetLink(link)?.activationPolicy === "manual" ? "manual" : "automatic";
	}
	function readLinkEffectConstraint(link) {
		const normalizedLink = makeWidgetLink(link);
		return normalizedLink?.effectConstraint === "highlightOnly" || normalizedLink?.effectConstraint === "focusOnly" ? normalizedLink.effectConstraint : null;
	}
	function readLinkResponseSpec(link) {
		const normalizedLink = makeWidgetLink(link);
		return normalizedLink?.responseSpec && typeof normalizedLink.responseSpec === "object" ? clone$24(normalizedLink.responseSpec) : null;
	}
	function readDeclaredLinkEffect(link) {
		const normalizedLink = makeWidgetLink(link);
		if (typeof normalizedLink?.effect === "string" && normalizedLink.effect.length > 0) return normalizedLink.effect;
		if (normalizedLink?.kind === "filter") return "applyFilter";
		if (normalizedLink?.kind === "highlight") return "applyHighlight";
		if (normalizedLink?.kind === "syncDomain") return "syncDomain";
		if (normalizedLink?.kind === "sharesSelection") return "shareSelection";
		if (normalizedLink?.kind === "drillDown" || normalizedLink?.kind === "reencode") return "transformView";
		if (normalizedLink?.kind === "aggregate") return "transformDataView";
		if (normalizedLink?.kind === "structure") return "transformStructure";
		return null;
	}
	function readAppliedLinkEffect(link) {
		const effectConstraint = readLinkEffectConstraint(link);
		if (effectConstraint === "highlightOnly") return "applyHighlight";
		if (effectConstraint === "focusOnly") return "focusTarget";
		return readDeclaredLinkEffect(link);
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/coordination/propagationReasons.js
	var PROPAGATION_SKIP_REASONS = [
		"missing_target_ref",
		"manual_activation_policy",
		"missing_target_widget",
		"unsupported_effect",
		"unsupported_advanced_response",
		"advanced_response_params_invalid",
		"advanced_response_verification_unavailable",
		"no_active_selection",
		"mapping_unresolved",
		"effect_not_applied",
		"no_applicable_link"
	];
	function normalizePropagationSkipReason(reason, fallback = "effect_not_applied") {
		return PROPAGATION_SKIP_REASONS.includes(reason) ? reason : fallback;
	}
	var CANONICAL_PROPAGATION_SKIP_REASONS = [
		"noApplicableLink",
		"manualLink",
		"sourceMismatch",
		"mappingUnavailable",
		"mappingFailed",
		"unsupportedEffect",
		"effectDowngradeUnavailable",
		"constraintBlocked"
	];
	var PROPAGATION_SKIP_REASON_ALIASES = {
		missing_target_ref: "sourceMismatch",
		manual_activation_policy: "manualLink",
		missing_target_widget: "sourceMismatch",
		unsupported_effect: "unsupportedEffect",
		unsupported_advanced_response: "unsupportedEffect",
		advanced_response_params_invalid: "mappingFailed",
		advanced_response_verification_unavailable: "mappingFailed",
		no_active_selection: "sourceMismatch",
		mapping_unresolved: "mappingUnavailable",
		effect_not_applied: "mappingFailed",
		no_applicable_link: "noApplicableLink"
	};
	function normalizeCanonicalPropagationSkipReason(reason, fallback = "mappingFailed") {
		if (CANONICAL_PROPAGATION_SKIP_REASONS.includes(reason)) return reason;
		if (typeof reason === "string" && reason in PROPAGATION_SKIP_REASON_ALIASES) return PROPAGATION_SKIP_REASON_ALIASES[reason];
		if (CANONICAL_PROPAGATION_SKIP_REASONS.includes(fallback)) return fallback;
		if (typeof fallback === "string" && fallback in PROPAGATION_SKIP_REASON_ALIASES) return PROPAGATION_SKIP_REASON_ALIASES[fallback];
		return "mappingFailed";
	}
	function isLegacyPropagationSkipReason(reason, expectedLegacyReason) {
		return reason === expectedLegacyReason;
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/coordination/CoordinationEngine.js
	function clone$23(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function valuesEqual(left, right) {
		return JSON.stringify(left ?? null) === JSON.stringify(right ?? null);
	}
	function buildLinkStatePatch({ beforeState, afterState, affectedRefs = [] } = {}) {
		const patch = {};
		const refs = [...new Set((Array.isArray(affectedRefs) ? affectedRefs : []).filter(Boolean))];
		for (const ref of refs) if (afterState?.widgets?.[ref]) patch[ref] = clone$23(afterState.widgets[ref]);
		if (!valuesEqual(beforeState?.shared, afterState?.shared)) patch.shared = clone$23(afterState?.shared || {});
		return patch;
	}
	function arraysEqual(left, right) {
		return JSON.stringify(left ?? null) === JSON.stringify(right ?? null);
	}
	function uniqueValues(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => value != null))];
	}
	function makeCoordinationEngineLinkKindEntry(entry = {}) {
		return {
			name: entry?.name || "",
			appliedStatePaths: Array.isArray(entry?.appliedStatePaths) ? [...entry.appliedStatePaths] : []
		};
	}
	function makeCoordinationEngineCapabilities(capabilities = {}) {
		return {
			propagationExecution: false,
			propagationPlan: false,
			effectCollection: false,
			consistencyEvaluation: false,
			...capabilities
		};
	}
	function makeCoordinationEngineSummary(summary = {}) {
		return {
			linkKindCount: summary?.linkKindCount || 0,
			linkKinds: Array.isArray(summary?.linkKinds) ? summary.linkKinds.map((entry) => makeCoordinationEngineLinkKindEntry(entry)) : [],
			linkCount: summary?.linkCount || 0,
			coordinationLinkCount: summary?.coordinationLinkCount || 0,
			structuralLinkCount: summary?.structuralLinkCount || 0,
			automaticLinkCount: summary?.automaticLinkCount || 0,
			manualLinkCount: summary?.manualLinkCount || 0,
			topology: summary?.topology || null,
			capabilities: makeCoordinationEngineCapabilities(summary?.capabilities)
		};
	}
	function readWidgetIdFromRef(ref) {
		if (typeof ref !== "string" || ref.length === 0) return null;
		const index = ref.lastIndexOf("/widget/");
		if (index < 0) return null;
		return ref.slice(index + 8).split("/")[0] || null;
	}
	function isCoordinationRelation(link) {
		return Boolean(link && typeof link === "object" && typeof link.sourceStateRef === "string" && typeof link.targetStateRef === "string");
	}
	function readRelationActivationPolicy(relation) {
		return (relation?.activation || relation?.activationPolicy) === "manual" ? "manual" : "automatic";
	}
	function readEffectFromCoordinationTransform(transform = {}) {
		switch (transform?.kind) {
			case "selectionToFilter":
			case "intervalToFilter":
			case "domainToFilter": return "applyFilter";
			case "selectionToHighlight": return "applyHighlight";
			case "selectionToSelection":
			case "identity": return "shareSelection";
			case "intervalToDomain": return "syncDomain";
			default: return null;
		}
	}
	function makeCoordinationRelationLink(relation) {
		const normalizedRelation = makeCoordinationRelation(relation);
		const sourceWidgetId = readWidgetIdFromRef(normalizedRelation.sourceStateRef);
		const targetWidgetRef = readWidgetRefFromStateRef(normalizedRelation.targetStateRef);
		const targetWidgetId = readWidgetIdFromRef(targetWidgetRef);
		const transform = normalizedRelation.transform && typeof normalizedRelation.transform === "object" ? clone$23(normalizedRelation.transform) : {};
		return {
			...clone$23(normalizedRelation),
			kind: transform.kind || normalizedRelation.relation || "coordination",
			effect: readEffectFromCoordinationTransform(transform),
			from: normalizedRelation.sourceStateRef,
			to: targetWidgetRef,
			sourceStateRef: normalizedRelation.sourceStateRef,
			targetStateRef: normalizedRelation.targetStateRef,
			sourceWidgetId,
			targetWidgetId,
			relation: normalizedRelation.relation || "controls",
			transform,
			fieldMapping: Array.isArray(transform.fieldMapping) ? clone$23(transform.fieldMapping) : [],
			activationPolicy: readRelationActivationPolicy(normalizedRelation),
			effectConstraint: null,
			responseSpec: null,
			isCoordinationRelation: true
		};
	}
	function stableKey(value) {
		try {
			return JSON.stringify(value);
		} catch {
			return String(value);
		}
	}
	function categoricalValuesFromSelection(selection) {
		return uniqueValues((Array.isArray(selection?.predicates) ? selection.predicates : []).filter((predicate) => predicate?.op === "equals" || predicate?.op === "eq" || predicate?.op === "in").flatMap((predicate) => Array.isArray(predicate?.value) ? predicate.value : [predicate?.value]));
	}
	function readChannelDomain(selection = {}, sourceChannel = null) {
		if (!sourceChannel) return null;
		const channelState = selection?.channels?.[sourceChannel];
		if (Array.isArray(channelState?.domain)) return channelState.domain;
		const domainKey = `${sourceChannel}Domain`;
		if (Array.isArray(selection?.domain?.[domainKey])) return selection.domain[domainKey];
		if (Array.isArray(selection?.domain?.[sourceChannel])) return selection.domain[sourceChannel];
		if (Array.isArray(selection?.value?.[sourceChannel])) return selection.value[sourceChannel];
		return null;
	}
	function mapIntervalSelectionToFilter(selection = {}, channelMapping = []) {
		const predicates = [];
		for (const mapping of Array.isArray(channelMapping) ? channelMapping : []) {
			const sourceChannel = mapping?.sourceChannel;
			const targetField = mapping?.targetField;
			const domain = readChannelDomain(selection, sourceChannel);
			if (typeof targetField === "string" && targetField.length > 0 && Array.isArray(domain) && domain.length >= 2) predicates.push({
				field: targetField,
				op: "between",
				value: domain
			});
		}
		if (predicates.length === 0) return null;
		return {
			...selection,
			predicates,
			domain: selection?.domain || null
		};
	}
	function mapIntervalSelectionToDomain(selection = {}, channelMapping = []) {
		const domain = {};
		for (const mapping of Array.isArray(channelMapping) ? channelMapping : []) {
			const sourceChannel = mapping?.sourceChannel;
			const targetChannel = mapping?.targetChannel || sourceChannel;
			const sourceDomain = readChannelDomain(selection, sourceChannel);
			if (Array.isArray(sourceDomain) && sourceDomain.length >= 2) domain[`${targetChannel}Domain`] = sourceDomain;
		}
		if (Object.keys(domain).length === 0) return null;
		return {
			...selection,
			domain
		};
	}
	function normalizeDomainValue(value) {
		if (Array.isArray(value)) return value;
		if (value && typeof value === "object" && Array.isArray(value.domain)) return value.domain;
		return null;
	}
	function buildPredicateFromDomain(targetField, domain) {
		if (typeof targetField !== "string" || targetField.length === 0) return null;
		if (!Array.isArray(domain) || domain.length < 2) return null;
		const lower = domain[0];
		const upper = domain[1];
		const hasLower = lower != null;
		const hasUpper = upper != null;
		if (hasLower && hasUpper) return {
			field: targetField,
			op: "between",
			value: [lower, upper]
		};
		if (hasLower) return {
			field: targetField,
			op: "gte",
			value: lower
		};
		if (hasUpper) return {
			field: targetField,
			op: "lte",
			value: upper
		};
		return null;
	}
	function mapDomainStateToFilter(sourceValue = {}, channelMapping = []) {
		const predicates = [];
		const zoom = sourceValue?.zoom && typeof sourceValue.zoom === "object" ? sourceValue.zoom : sourceValue;
		for (const mapping of Array.isArray(channelMapping) ? channelMapping : []) {
			const sourceChannel = mapping?.sourceChannel;
			const targetField = mapping?.targetField;
			const predicate = buildPredicateFromDomain(targetField, normalizeDomainValue(sourceChannel === "x" ? sourceValue?.xDomain || zoom?.xDomain || zoom?.domain?.xDomain : sourceChannel === "y" ? sourceValue?.yDomain || zoom?.yDomain || zoom?.domain?.yDomain : sourceValue?.[`${sourceChannel}Domain`] || zoom?.[`${sourceChannel}Domain`]));
			if (predicate) predicates.push(predicate);
		}
		if (predicates.length === 0) return null;
		return {
			kind: "predicate",
			predicates,
			domain: {
				...sourceValue?.xDomain ? { xDomain: sourceValue.xDomain } : {},
				...sourceValue?.yDomain ? { yDomain: sourceValue.yDomain } : {}
			}
		};
	}
	function mapCoordinationSourceToTarget({ activeSelection, sourceValue, link }) {
		const transform = link?.transform || {};
		switch (transform.kind) {
			case "intervalToFilter": return mapIntervalSelectionToFilter(activeSelection, transform.channelMapping);
			case "intervalToDomain": return mapIntervalSelectionToDomain(activeSelection, transform.channelMapping);
			case "domainToFilter": return mapDomainStateToFilter(sourceValue, transform.channelMapping);
			default: return activeSelection ? mapSelectionToTargetSelection(activeSelection, link?.fieldMapping) : null;
		}
	}
	var EFFECT_METADATA = {
		applyFilter: {
			effectKind: "filtersWidget",
			defaultDescription: "Filter propagation",
			appliedStatePaths: [
				"transforms",
				"data.currentDataRef",
				"feedback.inboundLinkIds"
			]
		},
		applyHighlight: {
			effectKind: "highlightsItems",
			defaultDescription: "Highlight propagation",
			appliedStatePaths: ["feedback.highlightedKeys", "feedback.inboundLinkIds"]
		},
		focusTarget: {
			effectKind: "focusesWidget",
			defaultDescription: "Focus propagation",
			appliedStatePaths: ["shared.focusedWidget", "shared.focus"]
		},
		syncDomain: {
			effectKind: "updatesViewDomain",
			defaultDescription: "Domain synchronization",
			appliedStatePaths: [
				"view.xDomain",
				"view.yDomain",
				"feedback.inboundLinkIds"
			]
		},
		shareSelection: {
			effectKind: "sharesSelectionState",
			defaultDescription: "Selection sharing",
			appliedStatePaths: [
				"selections",
				"data.selectedCount",
				"feedback.linkedSourceRefs"
			]
		},
		transformView: {
			effectKind: "transformsView",
			defaultDescription: "View transformation",
			appliedStatePaths: [
				"transforms",
				"feedback.inboundLinkIds",
				"feedback.linkedSourceRefs"
			]
		},
		transformDataView: {
			effectKind: "transformsDataView",
			defaultDescription: "Data-view transformation",
			appliedStatePaths: [
				"transforms",
				"feedback.inboundLinkIds",
				"feedback.linkedSourceRefs"
			]
		},
		transformStructure: {
			effectKind: "transformsStructure",
			defaultDescription: "Structure transformation",
			appliedStatePaths: [
				"transforms",
				"feedback.inboundLinkIds",
				"feedback.linkedSourceRefs"
			]
		}
	};
	function markRows(rows, marker, predicate) {
		return (Array.isArray(rows) ? rows : []).map((row) => ({
			...row,
			[marker]: Boolean(predicate?.(row))
		}));
	}
	function countRowsMatching(rows, predicate) {
		if (!Array.isArray(rows) || typeof predicate !== "function") return 0;
		return rows.filter((row) => predicate(row)).length;
	}
	function readRuntimeRows(runtimeEntry = null) {
		const baseRows = Array.isArray(runtimeEntry?.baseRows) ? runtimeEntry.baseRows : Array.isArray(runtimeEntry?.rows) ? runtimeEntry.rows : [];
		return {
			baseRows,
			visibleRows: Array.isArray(runtimeEntry?.rows) ? runtimeEntry.rows : baseRows
		};
	}
	function readEffectMetadata(effect) {
		return EFFECT_METADATA[effect] || null;
	}
	function describeLinkEffect(link) {
		const metadata = readEffectMetadata(readAppliedLinkEffect(link));
		if (!metadata) return null;
		return {
			kind: metadata.effectKind,
			targetRef: link.to,
			description: link.description || metadata.defaultDescription
		};
	}
	function describeAppliedStatePathsForEffect(effect) {
		return [...readEffectMetadata(effect)?.appliedStatePaths || []];
	}
	var CoordinationEngine = class {
		constructor(args = {}) {
			const store = args?.store || args;
			this.store = store;
		}
		resolveWidgetRefById(widgetId) {
			if (typeof widgetId !== "string" || widgetId.length === 0) return null;
			const describedWidget = (this.store.readDescription?.()?.widgets || []).find((widget) => widget?.widgetId === widgetId);
			if (typeof describedWidget?.ref === "string" && describedWidget.ref.length > 0) return describedWidget.ref;
			const listedWidget = this.store.listWidgetDescriptions?.()?.find((widget) => widget?.widgetId === widgetId);
			return typeof listedWidget?.ref === "string" && listedWidget.ref.length > 0 ? listedWidget.ref : null;
		}
		resolveWidgetEndpointRef(refOrWidgetId) {
			if (typeof refOrWidgetId !== "string" || refOrWidgetId.length === 0) return null;
			if (refOrWidgetId.includes("/widget/")) return refOrWidgetId;
			return this.resolveWidgetRefById(refOrWidgetId) || refOrWidgetId;
		}
		listLinks() {
			const deduped = /* @__PURE__ */ new Map();
			const stateRelations = makeCoordinationRelationMap(this.store.readState?.()?.coordination?.relations || {});
			const rawLinks = [...this.store.listLinks?.() || [], ...Object.values(stateRelations)];
			for (const rawLink of rawLinks) {
				const normalizedLink = isCoordinationRelation(rawLink) ? makeCoordinationRelationLink(rawLink) : makeWidgetLink(rawLink);
				const hydratedLink = {
					...normalizedLink,
					from: normalizedLink?.isCoordinationRelation ? normalizedLink.sourceStateRef : this.resolveWidgetEndpointRef(normalizedLink?.from) || this.resolveWidgetRefById(normalizedLink?.sourceWidgetId),
					to: normalizedLink?.isCoordinationRelation ? normalizedLink.to : this.resolveWidgetEndpointRef(normalizedLink?.to) || this.resolveWidgetRefById(normalizedLink?.targetWidgetId)
				};
				const dedupeKey = [
					hydratedLink?.from || null,
					hydratedLink?.to || null,
					hydratedLink?.kind || null,
					hydratedLink?.effect || null,
					hydratedLink?.activationPolicy || null,
					hydratedLink?.effectConstraint || null,
					stableKey(hydratedLink?.fieldMapping || []),
					hydratedLink?.sourceStateRef || null,
					hydratedLink?.targetStateRef || null
				].join("::");
				if (!deduped.has(dedupeKey)) deduped.set(dedupeKey, hydratedLink);
			}
			return Array.from(deduped.values());
		}
		listLinkKinds() {
			return [
				"filter",
				"highlight",
				"syncDomain",
				"sharesSelection",
				"drillDown",
				"reencode",
				"aggregate",
				"structure"
			];
		}
		describeLinks(options = {}) {
			const includeInternal = options?.includeInternal === true;
			const descriptionWidgets = this.store.readDescription?.()?.widgets || [];
			const widgetsByRef = new Map(descriptionWidgets.map((widget) => [widget?.ref, widget]).filter(([ref]) => typeof ref === "string"));
			const widgetsById = new Map(descriptionWidgets.map((widget) => [widget?.widgetId, widget]).filter(([widgetId]) => typeof widgetId === "string"));
			return this.listLinks().filter(isTopologyLink).map((link) => {
				const sourceWidget = widgetsByRef.get(link.from) || widgetsById.get(link.sourceWidgetId) || null;
				const targetWidget = widgetsByRef.get(link.to) || widgetsById.get(link.targetWidgetId) || null;
				const targetEffect = readAppliedLinkEffect(link);
				const sourceWidgetId = link.sourceWidgetId || sourceWidget?.widgetId || null;
				const targetWidgetId = link.targetWidgetId || targetWidget?.widgetId || null;
				const summary = {
					linkId: link.linkId || this.resolveLinkId(link.ref),
					linkRef: link.ref || null,
					sourceRef: link.from || null,
					targetRef: link.to || null,
					sourceWidgetId,
					sourceWidgetKind: link.sourceWidgetKind || sourceWidget?.kind || null,
					targetWidgetId,
					targetWidgetKind: link.targetWidgetKind || targetWidget?.kind || null,
					linkKind: link.kind || null,
					targetEffect,
					activationPolicy: readLinkActivationPolicy(link),
					triggerSummary: `${sourceWidgetId || "source"} selection drives ${targetWidgetId || "target"} ${targetEffect || "effect"}`,
					description: link.description || null
				};
				if (!includeInternal) return summary;
				return {
					...summary,
					effectConstraint: readLinkEffectConstraint(link),
					fieldMapping: clone$23(link.fieldMapping || []),
					responseSpec: clone$23(readLinkResponseSpec(link))
				};
			});
		}
		describeEngine() {
			const links = this.listLinks();
			const coordinationLinks = links.filter(isTopologyLink);
			const topology = deriveWorkspaceTopology({
				widgets: (this.store.readDescription?.() || {}).widgets || [],
				links
			});
			return makeCoordinationEngineSummary({
				linkKindCount: this.listLinkKinds().length,
				linkKinds: this.listLinkKinds().map((name) => makeCoordinationEngineLinkKindEntry({
					name,
					appliedStatePaths: this.describeAppliedStatePaths(name)
				})),
				linkCount: links.length,
				coordinationLinkCount: coordinationLinks.length,
				structuralLinkCount: links.length - coordinationLinks.length,
				automaticLinkCount: links.filter((link) => readLinkActivationPolicy(link) !== "manual").length,
				manualLinkCount: links.filter((link) => readLinkActivationPolicy(link) === "manual").length,
				topology,
				capabilities: makeCoordinationEngineCapabilities({
					propagationExecution: true,
					propagationPlan: true,
					effectCollection: true,
					consistencyEvaluation: true
				})
			});
		}
		buildPlanEntry({ link, state = this.store.readState(), sourceRef = null }) {
			const sourceSelectionRef = sourceRef || link?.from || null;
			const activeSelection = this.resolveActiveSelection({
				sourceRef: sourceSelectionRef,
				state
			});
			const sourceValue = link?.sourceStateRef ? readStateByRef(state, link.sourceStateRef) : null;
			const mappedSelection = mapCoordinationSourceToTarget({
				activeSelection,
				sourceValue,
				link
			});
			return {
				linkRef: link?.ref || null,
				linkKind: link?.kind || null,
				declaredEffect: readDeclaredLinkEffect(link),
				appliedEffect: readAppliedLinkEffect(link),
				responseSpec: readLinkResponseSpec(link),
				targetRef: link?.to || null,
				activationPolicy: readLinkActivationPolicy(link),
				effectConstraint: readLinkEffectConstraint(link),
				sourceSelectionRef,
				sourceStateRef: link?.sourceStateRef || sourceSelectionRef,
				targetStateRef: link?.targetStateRef || null,
				sourceWidgetId: link?.sourceWidgetId || null,
				targetWidgetId: link?.targetWidgetId || null,
				relation: link?.relation || null,
				transform: clone$23(link?.transform || null),
				isCoordinationRelation: Boolean(link?.isCoordinationRelation),
				hasActiveSelection: Boolean(activeSelection),
				hasSourceValue: Boolean(sourceValue),
				mappedSelection: mappedSelection ? clone$23(mappedSelection) : null,
				effect: describeLinkEffect(link)
			};
		}
		listPlanEntriesForTargetEffect({ targetRef, effect, state = this.store.readState(), includeManual = false }) {
			if (!targetRef || !effect) return [];
			return this.listLinks().filter((link) => link.to === targetRef && readAppliedLinkEffect(link) === effect && (includeManual || readLinkActivationPolicy(link) !== "manual")).map((link) => this.buildPlanEntry({
				link,
				state,
				sourceRef: link.from || null
			}));
		}
		findOutgoing({ sourceRef, state = this.store.readState() }) {
			if (!sourceRef) return [];
			const activeSelection = this.resolveActiveSelection({
				sourceRef,
				state
			});
			return this.listLinks().filter((link) => {
				if (link.sourceStateRef === sourceRef) return true;
				if (link.from === sourceRef) return true;
				if (!activeSelection) return false;
				if (link.sourceWidgetId && link.sourceWidgetId === activeSelection.sourceWidgetId) return true;
				if (link.from && activeSelection.sourceWidgetRef && link.from === activeSelection.sourceWidgetRef) return true;
				return false;
			});
		}
		findAffectedRefs({ sourceRef, state = this.store.readState() }) {
			const affected = /* @__PURE__ */ new Set();
			for (const link of this.findOutgoing({
				sourceRef,
				state
			})) if (link.to) affected.add(link.to);
			return Array.from(affected);
		}
		describePropagation({ sourceRef }) {
			const state = this.store.readState?.() || {};
			return this.findOutgoing({
				sourceRef,
				state
			}).map((link) => {
				const planEntry = this.buildPlanEntry({
					link,
					state,
					sourceRef
				});
				const applicability = this.assessPropagationEntry({
					entry: planEntry,
					state,
					activeSelection: this.resolveActiveSelection({
						sourceRef,
						state
					})
				});
				return {
					...planEntry,
					canApply: applicability.canApply,
					skippedReason: applicability.reason
				};
			});
		}
		collectEffects({ sourceRef }) {
			return this.findOutgoing({ sourceRef }).map((link) => describeLinkEffect(link)).filter(Boolean);
		}
		describeAppliedStatePaths(linkKind) {
			return uniqueValues(this.listLinks().filter((link) => link?.kind === linkKind).flatMap((link) => describeAppliedStatePathsForEffect(readAppliedLinkEffect(link))));
		}
		resolveActiveSelection({ sourceRef, state = this.store.readState() }) {
			if (!sourceRef) return null;
			const shared = state?.shared || {};
			const registry = readSelectionRegistry(shared);
			if (registry?.[sourceRef]) return registry[sourceRef];
			const byWidget = readSelectionByWidgetView(shared);
			if (byWidget?.[sourceRef]) return byWidget[sourceRef];
			const sourceWidgetId = readWidgetIdFromRef(sourceRef);
			if (sourceWidgetId && byWidget?.[sourceWidgetId]) return byWidget[sourceWidgetId];
			return Object.values(registry || {}).find((selection) => selection?.sourceWidgetRef === sourceRef || sourceWidgetId && selection?.sourceWidgetId === sourceWidgetId) || null;
		}
		buildMirroredSelectionRef(targetRef, sourceSelectionRef) {
			return `${targetRef}/selection/shared_${String(sourceSelectionRef || "selection").split("/").pop()}`;
		}
		buildLinkedFeedback(targetState, entry) {
			const feedback = targetState?.feedback || {};
			return {
				feedback,
				inboundLinkIds: uniqueValues([...feedback.inboundLinkIds || [], this.resolveLinkId(entry.linkRef)]),
				linkedSourceRefs: uniqueValues([...feedback.linkedSourceRefs || [], entry.sourceSelectionRef])
			};
		}
		buildPropagationPlan({ sourceRef, state = this.store.readState() }) {
			return this.findOutgoing({
				sourceRef,
				state
			}).map((link) => this.buildPlanEntry({
				link,
				state,
				sourceRef
			}));
		}
		assessPropagationEntry({ entry, state = this.store.readState(), activeSelection = null, allowManual = false }) {
			if (!entry?.targetRef) return {
				canApply: false,
				reason: normalizePropagationSkipReason("missing_target_ref")
			};
			if (entry.activationPolicy === "manual" && !allowManual) return {
				canApply: false,
				reason: normalizePropagationSkipReason("manual_activation_policy")
			};
			if (!(this.store.getWidgetState?.(entry.targetRef) || state?.widgets?.[entry.targetRef] || null)) return {
				canApply: false,
				reason: normalizePropagationSkipReason("missing_target_widget")
			};
			if (!entry.appliedEffect) return {
				canApply: false,
				reason: normalizePropagationSkipReason("unsupported_effect")
			};
			if (!activeSelection && entry.transform?.kind !== "domainToFilter") return {
				canApply: false,
				reason: normalizePropagationSkipReason("no_active_selection")
			};
			if (entry.appliedEffect === "transformView" || entry.appliedEffect === "transformDataView" || entry.appliedEffect === "transformStructure") return {
				canApply: false,
				reason: normalizePropagationSkipReason("unsupported_advanced_response")
			};
			if (entry.appliedEffect === "focusTarget") return {
				canApply: true,
				reason: null
			};
			if (entry.appliedEffect === "syncDomain") {
				const hasDomain = Array.isArray(entry.mappedSelection?.domain?.xDomain) || Array.isArray(entry.mappedSelection?.domain?.yDomain);
				return {
					canApply: hasDomain,
					reason: hasDomain ? null : normalizePropagationSkipReason("mapping_unresolved")
				};
			}
			if (!entry.mappedSelection) return {
				canApply: false,
				reason: normalizePropagationSkipReason("mapping_unresolved")
			};
			return {
				canApply: true,
				reason: null
			};
		}
		propagate(input) {
			const directSourceRef = typeof input === "string" ? input : null;
			const sourceRef = directSourceRef || input?.sourceRef || null;
			const state = directSourceRef ? this.store.readState() : input?.state || this.store.readState();
			const beforeState = clone$23(state);
			const activeSelection = this.resolveActiveSelection({
				sourceRef,
				state
			});
			const plan = this.buildPropagationPlan({
				sourceRef,
				state
			});
			const candidateRefs = [...new Set(plan.map((entry) => entry.targetRef).filter(Boolean))];
			this.store.clearWidgetPatches?.(candidateRefs);
			let currentState = this.store.readState();
			const appliedLinks = [];
			const skippedTargets = [];
			for (const entry of plan) {
				const applicability = this.assessPropagationEntry({
					entry,
					state: currentState,
					activeSelection
				});
				if (!applicability.canApply) {
					skippedTargets.push({
						linkRef: entry.linkRef,
						linkKind: entry.linkKind,
						declaredEffect: entry.declaredEffect,
						appliedEffect: entry.appliedEffect,
						responseSpec: clone$23(entry.responseSpec),
						targetRef: entry.targetRef,
						activationPolicy: entry.activationPolicy,
						effectConstraint: entry.effectConstraint,
						reason: normalizePropagationSkipReason(applicability.reason)
					});
					continue;
				}
				const patchResult = this.applyPropagationEntry({
					entry,
					state: currentState,
					activeSelection
				});
				if (patchResult?.applied) currentState = patchResult.nextState || this.store.readState();
				else {
					skippedTargets.push({
						linkRef: entry.linkRef,
						linkKind: entry.linkKind,
						declaredEffect: entry.declaredEffect,
						appliedEffect: entry.appliedEffect,
						responseSpec: clone$23(entry.responseSpec),
						targetRef: entry.targetRef,
						activationPolicy: entry.activationPolicy,
						effectConstraint: entry.effectConstraint,
						reason: normalizePropagationSkipReason(patchResult?.reason)
					});
					continue;
				}
				appliedLinks.push({
					linkRef: entry.linkRef,
					linkKind: entry.linkKind,
					declaredEffect: entry.declaredEffect,
					appliedEffect: entry.appliedEffect,
					responseSpec: clone$23(entry.responseSpec),
					targetRef: entry.targetRef,
					activationPolicy: entry.activationPolicy,
					effectConstraint: entry.effectConstraint,
					hasActiveSelection: entry.hasActiveSelection,
					appliedStatePaths: describeAppliedStatePathsForEffect(entry.appliedEffect),
					effect: clone$23(entry.effect)
				});
			}
			const affectedRefs = [...new Set(appliedLinks.map((entry) => entry.targetRef).filter(Boolean))];
			const propagation = {
				sourceRef,
				activeSelection: clone$23(activeSelection),
				affectedRefs,
				patch: buildLinkStatePatch({
					beforeState,
					afterState: currentState,
					affectedRefs
				}),
				linkCount: plan.length,
				appliedLinkCount: appliedLinks.length,
				skippedLinkCount: skippedTargets.length,
				nextState: currentState,
				links: appliedLinks,
				skippedTargets,
				effects: plan.map((entry) => clone$23(entry.effect)).filter(Boolean)
			};
			return directSourceRef ? propagation.affectedRefs : propagation;
		}
		applyLink({ linkRef, sourceRef = null, state = this.store.readState() } = {}) {
			const beforeState = clone$23(state);
			const link = this.listLinks().find((entry) => entry?.ref === linkRef || entry?.linkId === linkRef || this.resolveLinkId(entry?.ref) === linkRef);
			if (!link) return {
				ok: false,
				reason: normalizePropagationSkipReason("missing_link"),
				linkRef: linkRef || null,
				affectedRefs: [],
				appliedLinkCount: 0,
				skippedLinkCount: 1
			};
			const resolvedSourceRef = sourceRef || link.from || null;
			const activeSelection = this.resolveActiveSelection({
				sourceRef: resolvedSourceRef,
				state
			});
			const entry = this.buildPlanEntry({
				link,
				state,
				sourceRef: resolvedSourceRef
			});
			const applicability = this.assessPropagationEntry({
				entry,
				state,
				activeSelection,
				allowManual: true
			});
			if (!applicability.canApply) return {
				ok: false,
				reason: normalizePropagationSkipReason(applicability.reason),
				sourceRef: resolvedSourceRef,
				activeSelection: clone$23(activeSelection),
				affectedRefs: [],
				linkCount: 1,
				appliedLinkCount: 0,
				skippedLinkCount: 1,
				links: [],
				skippedTargets: [{
					linkRef: entry.linkRef,
					linkKind: entry.linkKind,
					declaredEffect: entry.declaredEffect,
					appliedEffect: entry.appliedEffect,
					responseSpec: clone$23(entry.responseSpec),
					targetRef: entry.targetRef,
					activationPolicy: entry.activationPolicy,
					effectConstraint: entry.effectConstraint,
					reason: normalizePropagationSkipReason(applicability.reason)
				}],
				effects: entry.effect ? [clone$23(entry.effect)] : []
			};
			this.store.clearWidgetPatches?.([entry.targetRef].filter(Boolean));
			const patchResult = this.applyPropagationEntry({
				entry,
				state,
				activeSelection,
				allowManual: true
			});
			if (!patchResult?.applied) return {
				ok: false,
				reason: normalizePropagationSkipReason(patchResult?.reason),
				sourceRef: resolvedSourceRef,
				activeSelection: clone$23(activeSelection),
				affectedRefs: [],
				linkCount: 1,
				appliedLinkCount: 0,
				skippedLinkCount: 1,
				links: [],
				skippedTargets: [{
					linkRef: entry.linkRef,
					linkKind: entry.linkKind,
					declaredEffect: entry.declaredEffect,
					appliedEffect: entry.appliedEffect,
					responseSpec: clone$23(entry.responseSpec),
					targetRef: entry.targetRef,
					activationPolicy: entry.activationPolicy,
					effectConstraint: entry.effectConstraint,
					reason: normalizePropagationSkipReason(patchResult?.reason)
				}],
				effects: entry.effect ? [clone$23(entry.effect)] : []
			};
			return {
				ok: true,
				sourceRef: resolvedSourceRef,
				activeSelection: clone$23(activeSelection),
				affectedRefs: entry.targetRef ? [entry.targetRef] : [],
				patch: buildLinkStatePatch({
					beforeState,
					afterState: patchResult.nextState || this.store.readState(),
					affectedRefs: entry.targetRef ? [entry.targetRef] : []
				}),
				linkCount: 1,
				appliedLinkCount: 1,
				skippedLinkCount: 0,
				nextState: patchResult.nextState || this.store.readState(),
				links: [{
					linkRef: entry.linkRef,
					linkKind: entry.linkKind,
					declaredEffect: entry.declaredEffect,
					appliedEffect: entry.appliedEffect,
					responseSpec: clone$23(entry.responseSpec),
					targetRef: entry.targetRef,
					activationPolicy: entry.activationPolicy,
					effectConstraint: entry.effectConstraint,
					hasActiveSelection: entry.hasActiveSelection,
					appliedStatePaths: describeAppliedStatePathsForEffect(entry.appliedEffect),
					effect: clone$23(entry.effect)
				}],
				skippedTargets: [],
				effects: entry.effect ? [clone$23(entry.effect)] : []
			};
		}
		applyPropagationEntry({ entry, state, activeSelection, allowManual = false }) {
			const targetRef = entry?.targetRef;
			if (!targetRef) return {
				applied: false,
				nextState: state,
				reason: normalizePropagationSkipReason("missing_target_ref")
			};
			const targetState = this.store.getWidgetState?.(targetRef) || state?.widgets?.[targetRef] || null;
			if (!targetState) return {
				applied: false,
				nextState: state,
				reason: normalizePropagationSkipReason("missing_target_widget")
			};
			const { feedback, inboundLinkIds, linkedSourceRefs } = this.buildLinkedFeedback(targetState, entry);
			if (entry.appliedEffect === "applyFilter") {
				const filterEntries = this.listPlanEntriesForTargetEffect({
					targetRef,
					effect: "applyFilter",
					state,
					includeManual: allowManual
				}).filter((item) => item.mappedSelection);
				const filterLinkRefs = new Set(filterEntries.map((item) => item.linkRef));
				let nextTransformState = {
					...state,
					widgets: {
						...state?.widgets || {},
						[targetRef]: {
							...targetState,
							transforms: (Array.isArray(targetState.transforms) ? targetState.transforms : []).filter((transform) => !filterLinkRefs.has(transform?.spec?.linkRef))
						}
					}
				};
				const visibleDataRef = targetState.data?.currentDataRef || null;
				const { baseRows } = readRuntimeRows(visibleDataRef ? this.store.readRuntimeData?.(visibleDataRef) : null);
				for (const filterEntry of filterEntries) {
					const predicates = Array.isArray(filterEntry.mappedSelection?.predicates) ? clone$23(filterEntry.mappedSelection.predicates) : [];
					const canonicalSourceRef = filterEntry.sourceStateRef || filterEntry.sourceSelectionRef;
					const canonicalTransformRef = filterEntry.targetStateRef || `${targetRef}/transform/${this.resolveLinkId(filterEntry.linkRef) || "coordination-filter"}`;
					const nextTransform = {
						kind: "filter",
						source: filterEntry.isCoordinationRelation ? "coordination" : filterEntry.sourceSelectionRef,
						sourceRef: canonicalSourceRef,
						sourceWidgetId: filterEntry.sourceWidgetId || null,
						sourceSelectionRef: filterEntry.sourceSelectionRef,
						linkId: this.resolveLinkId(filterEntry.linkRef),
						...canonicalTransformRef ? { ref: canonicalTransformRef } : {},
						...predicates.length === 1 ? { predicate: predicates[0] } : {},
						...predicates.length > 1 ? { predicate: predicates } : {},
						params: {
							relationRef: filterEntry.linkRef,
							transformKind: filterEntry.transform?.kind || null,
							domain: clone$23(filterEntry.mappedSelection?.domain || null)
						},
						spec: {
							linkRef: filterEntry.linkRef,
							sourceSelectionRef: filterEntry.sourceSelectionRef,
							sourceStateRef: canonicalSourceRef,
							targetStateRef: canonicalTransformRef,
							predicates,
							domain: clone$23(filterEntry.mappedSelection?.domain || null)
						}
					};
					nextTransformState = patchStateByRef(nextTransformState, canonicalTransformRef, nextTransform);
				}
				const patchedTargetState = nextTransformState.widgets?.[targetRef] || targetState;
				const nextVisibleRows = filterEntries.reduce((rows, filterEntry) => rows.filter((row) => rowMatchesSelection(row, filterEntry.mappedSelection)), baseRows);
				const selectedCount = countSelectedRowsForSelections(nextVisibleRows, Object.values(targetState.selections || {}).filter(Boolean));
				if (visibleDataRef) this.store.updateRuntimeData?.(visibleDataRef, (currentEntry) => ({
					...currentEntry,
					rows: nextVisibleRows,
					baseRows: Array.isArray(currentEntry?.baseRows) ? currentEntry.baseRows : baseRows,
					handle: {
						...currentEntry?.handle || {},
						stats: {
							...currentEntry?.handle?.stats || {},
							rowCount: baseRows.length,
							visibleCount: nextVisibleRows.length,
							selectedCount
						}
					}
				}));
				return {
					applied: true,
					nextState: this.store.patchWidget(targetRef, {
						transforms: patchedTargetState.transforms,
						data: {
							...targetState.data || {},
							visibleCount: nextVisibleRows.length,
							selectedCount
						},
						feedback: {
							...feedback,
							inboundLinkIds,
							linkedSourceRefs
						}
					})
				};
			}
			if (entry.appliedEffect === "applyHighlight") {
				const highlightEntries = this.listPlanEntriesForTargetEffect({
					targetRef,
					effect: "applyHighlight",
					state,
					includeManual: allowManual
				}).filter((item) => item.mappedSelection);
				const highlightedKeys = uniqueValues(highlightEntries.flatMap((item) => categoricalValuesFromSelection(item.mappedSelection)));
				const visibleDataRef = targetState.data?.currentDataRef || null;
				if (visibleDataRef) this.store.updateRuntimeData?.(visibleDataRef, (currentEntry) => {
					const { visibleRows } = readRuntimeRows(currentEntry);
					return {
						...currentEntry,
						rows: markRows(visibleRows, "__widgetva_highlight", (row) => highlightEntries.some((item) => rowMatchesSelection(row, item.mappedSelection)))
					};
				});
				const nextState = this.store.patchWidget(targetRef, {
					rawSpec: Array.isArray(targetState.rawSpec?.data?.values) ? {
						...clone$23(targetState.rawSpec),
						data: {
							...targetState.rawSpec.data || {},
							values: markRows(targetState.rawSpec.data.values, "__widgetva_highlight", (row) => highlightEntries.some((item) => rowMatchesSelection(row, item.mappedSelection)))
						}
					} : targetState.rawSpec,
					...entry.isCoordinationRelation ? { view: {
						...targetState.view || {},
						highlight: {
							sourceAction: "coordination.selectionToHighlight",
							relationRef: entry.linkRef,
							sourceStateRef: entry.sourceStateRef || entry.sourceSelectionRef,
							targetStateRef: entry.targetStateRef || null,
							predicates: highlightEntries.flatMap((item) => item.mappedSelection?.predicates || []),
							values: highlightedKeys
						}
					} } : {},
					feedback: {
						...feedback,
						inboundLinkIds,
						linkedSourceRefs,
						highlightedKeys
					}
				});
				return {
					applied: true,
					nextState: updateSharedStateInStore(this.store, (shared) => withHighlightSubmodel(shared, deriveHighlightState(nextState)))
				};
			}
			if (entry.appliedEffect === "focusTarget") {
				const nextState = this.store.patchWidget(targetRef, { feedback: {
					...feedback,
					inboundLinkIds,
					linkedSourceRefs,
					focusLinkIds: uniqueValues([...feedback.focusLinkIds || [], this.resolveLinkId(entry.linkRef)]),
					focusedBySelectionRef: entry.sourceSelectionRef || null
				} });
				return {
					applied: true,
					nextState: updateSharedStateInStore(this.store, (shared) => withFocusSubmodel(shared, {
						widgetRef: targetRef,
						widgetId: targetState.widgetId || null,
						source: "link"
					}, nextState?.widgets || state?.widgets || {}))
				};
			}
			if (entry.appliedEffect === "syncDomain") {
				const activeSyncEntries = [entry, ...this.listPlanEntriesForTargetEffect({
					targetRef,
					effect: "syncDomain",
					state,
					includeManual: allowManual
				}).filter((item) => item.linkRef !== entry.linkRef)].filter((item) => item.mappedSelection?.domain);
				const resolvedXDomain = [...activeSyncEntries].reverse().find((item) => Array.isArray(item.mappedSelection?.domain?.xDomain))?.mappedSelection?.domain?.xDomain || null;
				const resolvedYDomain = [...activeSyncEntries].reverse().find((item) => Array.isArray(item.mappedSelection?.domain?.yDomain))?.mappedSelection?.domain?.yDomain || null;
				return {
					applied: true,
					nextState: this.store.patchWidget(targetRef, {
						view: {
							...targetState.view || {},
							xDomain: resolvedXDomain,
							yDomain: resolvedYDomain,
							zoom: buildViewZoomState({
								widgetSpec: targetState.rawSpec,
								xDomain: resolvedXDomain,
								yDomain: resolvedYDomain
							})
						},
						feedback: {
							...feedback,
							inboundLinkIds,
							linkedSourceRefs
						}
					})
				};
			}
			if (entry.appliedEffect === "shareSelection") {
				const shareEntries = this.listPlanEntriesForTargetEffect({
					targetRef,
					effect: "shareSelection",
					state,
					includeManual: allowManual
				}).filter((item) => item.mappedSelection);
				const shareSelectionRefs = new Set(shareEntries.map((item) => this.buildMirroredSelectionRef(targetRef, item.sourceSelectionRef)));
				const visibleDataRef = targetState.data?.currentDataRef || null;
				const { visibleRows } = readRuntimeRows(visibleDataRef ? this.store.readRuntimeData?.(visibleDataRef) : null);
				const mirroredSelectionEntries = shareEntries.map((shareEntry) => {
					const selectionRef = shareEntry.isCoordinationRelation && shareEntry.targetStateRef ? shareEntry.targetStateRef : this.buildMirroredSelectionRef(targetRef, shareEntry.sourceSelectionRef);
					const selectionId = selectionRef.split("/").pop() || null;
					return [selectionRef, makeSelectionState({
						...buildSelectionStateInput(shareEntry.mappedSelection),
						selectionRef,
						selectionId,
						sourceWidgetRef: targetRef,
						sourceWidgetId: targetState.widgetId || targetRef
					})];
				});
				const selectedCount = countRowsMatching(visibleRows, (row) => shareEntries.some((item) => rowMatchesSelection(row, item.mappedSelection)));
				if (visibleDataRef) this.store.updateRuntimeData?.(visibleDataRef, (currentEntry) => {
					const { visibleRows: currentRows } = readRuntimeRows(currentEntry);
					return {
						...currentEntry,
						rows: markRows(currentRows, "__widgetva_selected", (row) => shareEntries.some((item) => rowMatchesSelection(row, item.mappedSelection))),
						handle: {
							...currentEntry?.handle || {},
							stats: {
								...currentEntry?.handle?.stats || {},
								selectedCount
							}
						}
					};
				});
				const baseSelections = Object.fromEntries(Object.entries(targetState.selections || {}).filter(([selectionRef]) => !shareSelectionRefs.has(selectionRef)));
				const nextState = this.store.patchWidget(targetRef, {
					selections: {
						...baseSelections,
						...Object.fromEntries(mirroredSelectionEntries)
					},
					data: {
						...targetState.data || {},
						selectedCount
					},
					rawSpec: Array.isArray(targetState.rawSpec?.data?.values) ? {
						...clone$23(targetState.rawSpec),
						data: {
							...targetState.rawSpec.data || {},
							values: markRows(targetState.rawSpec.data.values, "__widgetva_selected", (row) => shareEntries.some((item) => rowMatchesSelection(row, item.mappedSelection)))
						}
					} : targetState.rawSpec,
					feedback: {
						...feedback,
						inboundLinkIds,
						linkedSourceRefs,
						sharedSelectionSourceWidgetId: shareEntries[0]?.sourceWidgetId || null
					}
				});
				return {
					applied: true,
					nextState: updateSharedStateInStore(this.store, (shared) => {
						const registry = { ...readSelectionRegistry(shared) };
						for (const selectionRef of shareSelectionRefs) delete registry[selectionRef];
						for (const [selectionRef, selectionState] of mirroredSelectionEntries) registry[selectionRef] = selectionState;
						const byWidget = { ...readSelectionByWidgetView(shared) };
						const widgetKey = targetState.widgetId || targetRef;
						if (mirroredSelectionEntries.length > 0) {
							const [selectionRef, selectionState] = mirroredSelectionEntries[mirroredSelectionEntries.length - 1];
							byWidget[widgetKey] = {
								...selectionState,
								selectionRef
							};
						} else delete byWidget[widgetKey];
						return withSelectionSubmodel(shared, {
							registry,
							primary: void 0,
							byWidget
						});
					}) || nextState
				};
			}
			if (entry.appliedEffect === "transformView" || entry.appliedEffect === "transformDataView" || entry.appliedEffect === "transformStructure") return {
				applied: false,
				nextState: state,
				reason: normalizePropagationSkipReason("unsupported_advanced_response")
			};
			return {
				applied: false,
				nextState: state,
				reason: normalizePropagationSkipReason("unsupported_effect")
			};
		}
		resolveLinkId(linkRef) {
			const link = this.store.getLink?.(linkRef) || this.listLinks().find((item) => item.ref === linkRef) || null;
			if (link?.linkId) return link.linkId;
			if (typeof linkRef === "string" && linkRef.includes("/link/")) return linkRef.split("/").pop() || null;
			return null;
		}
		evaluatePropagation({ sourceRef, state = this.store.readState() }) {
			const activeSelection = this.resolveActiveSelection({
				sourceRef,
				state
			});
			const results = this.buildPropagationPlan({
				sourceRef,
				state
			}).map((entry) => this.evaluatePlanEntry({
				entry,
				state,
				activeSelection
			}));
			const passedCount = results.filter((item) => item.passed).length;
			return {
				ok: results.every((item) => item.passed),
				sourceRef,
				activeSelection: clone$23(activeSelection),
				linkCount: results.length,
				passedCount,
				consistencyScore: results.length === 0 ? 1 : passedCount / results.length,
				results
			};
		}
		evaluatePlanEntry({ entry, state, activeSelection }) {
			const resolvedLinkId = this.resolveLinkId(entry.linkRef);
			const targetState = entry.targetRef ? state?.widgets?.[entry.targetRef] || null : null;
			const checks = [];
			const applicability = this.assessPropagationEntry({
				entry,
				state,
				activeSelection
			});
			checks.push({
				name: "targetExists",
				passed: Boolean(targetState),
				actual: Boolean(targetState),
				expected: true
			});
			if (targetState && applicability.canApply && entry.appliedEffect !== "focusTarget") checks.push({
				name: "inboundLinkRegistered",
				passed: Array.isArray(targetState.feedback?.inboundLinkIds) && targetState.feedback.inboundLinkIds.includes(resolvedLinkId),
				actual: targetState.feedback?.inboundLinkIds || [],
				expected: resolvedLinkId
			});
			if (targetState && applicability.canApply) {
				if (entry.appliedEffect === "applyFilter") {
					const matchingTransform = (Array.isArray(targetState.transforms) ? targetState.transforms : []).find((transform) => transform?.kind === "filter" && transform?.spec?.linkRef === entry.linkRef);
					checks.push({
						name: "filterTransformRegistered",
						passed: Boolean(matchingTransform),
						actual: Boolean(matchingTransform),
						expected: true
					});
				}
				if (entry.appliedEffect === "shareSelection" && entry.mappedSelection) {
					const mirroredSelection = Object.values(targetState.selections || {}).find((selection) => selection?.summary === entry.mappedSelection.summary);
					const runtimeData = this.store.readRuntimeData?.(targetState.data?.currentDataRef);
					const { visibleRows } = readRuntimeRows(runtimeData);
					const expectedSelectedCount = countSelectedRows(visibleRows, entry.mappedSelection);
					checks.push({
						name: "mirroredSelectionExists",
						passed: Boolean(mirroredSelection),
						actual: Boolean(mirroredSelection),
						expected: true
					});
					checks.push({
						name: "mirroredSelectionCountMatches",
						passed: targetState.data?.selectedCount === expectedSelectedCount,
						actual: targetState.data?.selectedCount ?? 0,
						expected: expectedSelectedCount
					});
				}
				if (entry.appliedEffect === "syncDomain") {
					checks.push({
						name: "xDomainSynced",
						passed: arraysEqual(targetState.view?.xDomain, activeSelection?.domain?.xDomain),
						actual: targetState.view?.xDomain || null,
						expected: activeSelection?.domain?.xDomain || null
					});
					checks.push({
						name: "yDomainSynced",
						passed: arraysEqual(targetState.view?.yDomain, activeSelection?.domain?.yDomain),
						actual: targetState.view?.yDomain || null,
						expected: activeSelection?.domain?.yDomain || null
					});
				}
				if (entry.appliedEffect === "applyHighlight") {
					const highlightedKeys = targetState.feedback?.highlightedKeys || [];
					const runtimeData = this.store.readRuntimeData?.(targetState.data?.currentDataRef);
					const { visibleRows } = readRuntimeRows(runtimeData);
					const highlightedRows = visibleRows.filter((row) => row?.__widgetva_highlight === true).length;
					checks.push({
						name: "highlightFeedbackPresent",
						passed: highlightedKeys.length > 0 || highlightedRows > 0,
						actual: {
							highlightedKeys: highlightedKeys.length,
							highlightedRows
						},
						expected: "at least one highlight feedback channel should be populated"
					});
				}
				if (entry.appliedEffect === "focusTarget") {
					const focusState = state?.shared?.focus || null;
					checks.push({
						name: "focusedWidgetMatchesTarget",
						passed: state?.shared?.focusedWidget === entry.targetRef,
						actual: state?.shared?.focusedWidget || null,
						expected: entry.targetRef
					});
					checks.push({
						name: "focusStateMatchesTarget",
						passed: focusState?.widgetRef === entry.targetRef,
						actual: focusState?.widgetRef || null,
						expected: entry.targetRef
					});
				}
			}
			checks.push({
				name: "linkCanApply",
				passed: applicability.canApply,
				actual: applicability.reason || "applicable",
				expected: "applicable"
			});
			return {
				linkRef: entry.linkRef,
				linkKind: entry.linkKind,
				declaredEffect: entry.declaredEffect,
				appliedEffect: entry.appliedEffect,
				responseSpec: clone$23(entry.responseSpec),
				targetRef: entry.targetRef,
				activationPolicy: entry.activationPolicy,
				effectConstraint: entry.effectConstraint,
				reason: applicability.reason,
				passed: checks.every((check) => check.passed),
				checks
			};
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/workspace/widgetRegistry.js
	function clone$22(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeWidgetRegistryCounts(counts = {}) {
		return {
			widgetCount: 0,
			dataHandleCount: 0,
			linkCount: 0,
			adapterCount: 0,
			...counts
		};
	}
	function makeWidgetRegistryRefs(refs = {}) {
		return {
			widgetRefs: [],
			dataRefs: [],
			linkRefs: [],
			...refs
		};
	}
	function makeWidgetRegistrySummary(summary = {}) {
		return {
			widgets: [],
			dataHandles: [],
			links: [],
			...summary,
			counts: makeWidgetRegistryCounts(summary?.counts),
			refs: makeWidgetRegistryRefs(summary?.refs),
			links: Array.isArray(summary?.links) ? summary.links.map((link) => makeWidgetLink(link)) : []
		};
	}
	function summarizeWidgetEntry(widgetDescription, widgetState, widgetAdapter) {
		const humanInteraction = {
			...widgetAdapter?.getHumanInteractionConfig?.() || {},
			...widgetState?.humanInteraction || {},
			...widgetDescription?.humanInteraction || {}
		};
		return {
			ref: widgetDescription?.ref || "",
			widgetId: widgetDescription?.widgetId || "",
			kind: widgetDescription?.kind || "",
			role: widgetDescription?.role || "",
			title: widgetDescription?.title || "",
			description: widgetDescription?.description || "",
			analyticRoles: Array.isArray(widgetDescription?.analyticRoles) ? [...widgetDescription.analyticRoles] : [],
			recognizedKinds: Array.isArray(widgetDescription?.recognizedKinds) ? [...widgetDescription.recognizedKinds] : [],
			sourceKind: widgetDescription?.sourceKind || null,
			supportsSpecMutation: widgetDescription?.supportsSpecMutation === true,
			primaryDataRef: widgetDescription?.primaryDataRef || widgetState?.data?.sourceDataRef || null,
			sourceDataRef: widgetState?.data?.sourceDataRef || null,
			currentDataRef: widgetState?.data?.currentDataRef || null,
			usageNotes: Array.isArray(widgetDescription?.usageNotes) ? [...widgetDescription.usageNotes] : [],
			actionNames: Array.isArray(widgetDescription?.actionNames) ? [...widgetDescription.actionNames] : [],
			perceptionQueryNames: Array.isArray(widgetDescription?.perceptionQueryNames) ? [...widgetDescription.perceptionQueryNames] : [],
			adapterProvider: widgetAdapter?.provider || null,
			providerCapabilities: widgetAdapter?.providerCapabilities ? clone$22(widgetAdapter.providerCapabilities) : null,
			adapterCapabilities: widgetAdapter ? {
				canDescribe: typeof widgetAdapter.getDescription === "function",
				canReadState: typeof widgetAdapter.getState === "function",
				canApplyState: typeof widgetAdapter.applyState === "function",
				canBindHumanInteractions: typeof widgetAdapter.bindHumanInteractions === "function",
				canRegisterActions: typeof widgetAdapter.registerActions === "function",
				canRegisterPerceptionQueries: typeof widgetAdapter.registerPerceptionQueries === "function"
			} : null,
			humanInteractionMode: humanInteraction.mode || null,
			humanInteractionActionName: humanInteraction.actionName || null,
			supportsDirectManipulation: humanInteraction?.supportsDirectManipulation === true
		};
	}
	function buildResolvedWidget(widgetDescription, widgetState, widgetAdapter) {
		if (!widgetDescription && !widgetState) return null;
		return {
			...widgetDescription || {},
			...widgetState || {},
			ref: widgetDescription?.ref || widgetState?.ref || "",
			kind: widgetDescription?.kind || widgetState?.kind || "",
			title: widgetDescription?.title || "",
			description: widgetDescription?.description || "",
			primaryDataRef: widgetDescription?.primaryDataRef || widgetState?.data?.sourceDataRef || null,
			widgetId: widgetDescription?.widgetId || widgetState?.widgetId || "",
			role: widgetDescription?.role || widgetState?.role || "",
			recognizedKinds: Array.isArray(widgetDescription?.recognizedKinds) ? widgetDescription.recognizedKinds : [],
			actionNames: Array.isArray(widgetDescription?.actionNames) ? widgetDescription.actionNames : [],
			perceptionQueryNames: Array.isArray(widgetDescription?.perceptionQueryNames) ? widgetDescription.perceptionQueryNames : [],
			usageNotes: Array.isArray(widgetDescription?.usageNotes) ? widgetDescription.usageNotes : [],
			adapterProvider: widgetAdapter?.provider || null,
			providerCapabilities: widgetAdapter?.providerCapabilities || null,
			humanInteraction: widgetDescription?.humanInteraction || widgetState?.humanInteraction || null
		};
	}
	function widgetUsesDataRef(widgetDescription, widgetState, dataRef) {
		if (!dataRef) return false;
		return widgetDescription?.primaryDataRef === dataRef || widgetState?.data?.currentDataRef === dataRef || widgetState?.data?.sourceDataRef === dataRef;
	}
	var WidgetRegistry = class {
		constructor() {
			this.widgetDescriptions = {};
			this.widgetStates = {};
			this.dataHandles = {};
			this.links = {};
			this.widgetAdapters = {};
			this.widgetEntries = [];
		}
		rebuildWidgetEntries() {
			this.widgetEntries = Object.values(this.widgetDescriptions).map((widgetDescription) => summarizeWidgetEntry(widgetDescription, this.widgetStates?.[widgetDescription.ref] || null, this.widgetAdapters?.[widgetDescription.ref] || null));
		}
		setState(state) {
			this.widgetStates = Object.fromEntries(Object.entries(state?.widgets || {}).map(([ref, widgetState]) => {
				return [ref, makeWidgetState({
					ref,
					...widgetState
				})];
			}));
			this.rebuildWidgetEntries();
		}
		updateDataHandle(ref, handle) {
			if (!ref || !handle) return;
			this.dataHandles = {
				...this.dataHandles || {},
				[ref]: handle
			};
		}
		removeDataHandle(ref) {
			if (!ref || !this.dataHandles?.[ref]) return;
			const nextHandles = { ...this.dataHandles || {} };
			delete nextHandles[ref];
			this.dataHandles = nextHandles;
		}
		replaceWorkspace({ description, state, widgetAdapters } = {}) {
			this.widgetDescriptions = Object.fromEntries((description?.widgets || []).map((item) => {
				const normalizedDescription = makeWidgetDescription(item);
				return [normalizedDescription.ref, normalizedDescription];
			}));
			this.dataHandles = Object.fromEntries((description?.dataHandles || []).map((item) => [item.ref, item]));
			this.links = Object.fromEntries((description?.links || []).map((item) => {
				const normalizedLink = makeWidgetLink(item);
				return [normalizedLink.ref, normalizedLink];
			}));
			this.widgetAdapters = Object.fromEntries((Array.isArray(widgetAdapters) ? widgetAdapters : []).map((adapter) => [adapter.widgetRef, adapter]));
			this.setState(state);
		}
		listWidgetRefs() {
			return Object.keys(this.widgetDescriptions);
		}
		listDataRefs() {
			return Object.keys(this.dataHandles);
		}
		listLinkRefs() {
			return Object.keys(this.links);
		}
		getWidget(ref) {
			return ref ? this.widgetDescriptions?.[ref] || null : null;
		}
		getWidgetState(ref) {
			return ref ? this.widgetStates?.[ref] || null : null;
		}
		getWidgetEntry(ref) {
			if (!ref) return null;
			const description = this.getWidget(ref);
			if (!description) return null;
			return summarizeWidgetEntry(description, this.getWidgetState(ref), this.getWidgetAdapter(ref));
		}
		getResolvedWidget(ref) {
			if (!ref) return null;
			return buildResolvedWidget(this.getWidget(ref), this.getWidgetState(ref), this.getWidgetAdapter(ref));
		}
		findWidgetRefForDataRef(dataRef) {
			if (!dataRef) return null;
			const widgetRefs = this.listWidgetRefs();
			for (const widgetRef of widgetRefs) if (widgetUsesDataRef(this.getWidget(widgetRef), this.getWidgetState(widgetRef), dataRef)) return widgetRef;
			return null;
		}
		findWidgetRefForSelectionRef(selectionRef) {
			if (!selectionRef) return null;
			const selectionParts = parseRef(selectionRef);
			if (!selectionParts || selectionParts.kind !== "selection" || !selectionParts.widgetId) return null;
			const widgetRefs = this.listWidgetRefs();
			for (const widgetRef of widgetRefs) {
				const widgetDescription = this.getWidget(widgetRef);
				const widgetState = this.getWidgetState(widgetRef);
				if (widgetDescription?.widgetId === selectionParts.widgetId || widgetState?.widgetId === selectionParts.widgetId) return widgetRef;
			}
			return null;
		}
		getResolvedWidgetForTarget(ref) {
			if (!ref) return null;
			const directWidget = this.getResolvedWidget(ref);
			if (directWidget) return directWidget;
			const selectionWidgetRef = this.findWidgetRefForSelectionRef(ref);
			if (selectionWidgetRef) return this.getResolvedWidget(selectionWidgetRef);
			const widgetRef = this.findWidgetRefForDataRef(ref);
			return widgetRef ? this.getResolvedWidget(widgetRef) : null;
		}
		getDataHandle(ref) {
			return ref ? this.dataHandles?.[ref] || null : null;
		}
		getLink(ref) {
			return ref ? this.links?.[ref] || null : null;
		}
		getWidgetAdapter(ref) {
			return ref ? this.widgetAdapters?.[ref] || null : null;
		}
		describe() {
			return clone$22(makeWidgetRegistrySummary({
				counts: makeWidgetRegistryCounts({
					widgetCount: this.listWidgetRefs().length,
					dataHandleCount: this.listDataRefs().length,
					linkCount: this.listLinkRefs().length,
					adapterCount: Object.keys(this.widgetAdapters).length
				}),
				refs: makeWidgetRegistryRefs({
					widgetRefs: this.listWidgetRefs(),
					dataRefs: this.listDataRefs(),
					linkRefs: this.listLinkRefs()
				}),
				widgets: this.widgetEntries,
				dataHandles: Object.values(this.dataHandles || {}),
				links: Object.values(this.links || {})
			}));
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/summaries/summarizeWorkspaceState.js
	function clone$21(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function makeRuntimeStoreCurrentStateSummary$1(summary = {}) {
		return {
			stateId: null,
			focusedWidgetRef: null,
			focusedWidgetKind: null,
			focusedWidgetTitle: null,
			visibleCount: null,
			selectedCount: null,
			selectionCount: 0,
			activeSelectionRefs: [],
			primarySelectionRef: null,
			primarySelectionSummary: "",
			primarySelectionPredicates: [],
			comparisonTargetCount: 0,
			globalFilterCount: 0,
			taskMode: null,
			coordinationScope: null,
			evidenceType: null,
			interactionHorizon: null,
			replayRunMode: null,
			replayUserIntent: "",
			changedRefs: [],
			removedRefs: [],
			sharedChanged: false,
			taskContextChanged: false,
			replayContextChanged: false,
			...summary
		};
	}
	function summarizeWorkspaceState(state) {
		if (!state || typeof state !== "object") return null;
		const widgets = Object.values(state.widgets || {});
		const focusedWidgetRef = state.shared?.focusedWidget || widgets[0]?.ref || null;
		const focusedWidget = focusedWidgetRef ? state.widgets?.[focusedWidgetRef] || null : widgets[0] || null;
		const activeSelections = readSelectionRegistry(state.shared || {});
		const selectionEntries = Object.entries(activeSelections);
		const primarySelectionEntry = resolvePrimarySelectionEntry(state.shared || {});
		const changedRefs = Array.isArray(state.delta?.changedRefs) ? state.delta.changedRefs : [];
		const removedRefs = Array.isArray(state.delta?.removedRefs) ? state.delta.removedRefs : [];
		return makeRuntimeStoreCurrentStateSummary$1({
			stateId: state.stateId || null,
			focusedWidgetRef,
			focusedWidgetKind: focusedWidget?.kind || null,
			focusedWidgetTitle: focusedWidget?.title || null,
			visibleCount: focusedWidget?.data?.visibleCount ?? focusedWidget?.data?.rowCount ?? null,
			selectedCount: focusedWidget?.data?.selectedCount ?? null,
			selectionCount: selectionEntries.length,
			activeSelectionRefs: selectionEntries.map(([selectionRef]) => selectionRef),
			primarySelectionRef: primarySelectionEntry?.[0] || null,
			primarySelectionSummary: primarySelectionEntry?.[1]?.summary || "",
			primarySelectionPredicates: Array.isArray(primarySelectionEntry?.[1]?.predicates) ? clone$21(primarySelectionEntry[1].predicates) : [],
			globalFilterCount: Object.keys(state.shared?.globalFilters || {}).length,
			taskMode: state.taskContext?.taskMode || null,
			coordinationScope: state.taskContext?.coordinationScope || null,
			evidenceType: state.taskContext?.evidenceType || null,
			interactionHorizon: state.taskContext?.interactionHorizon || null,
			replayRunMode: state.replayContext?.runMode || null,
			replayUserIntent: state.replayContext?.userIntent || "",
			changedRefs,
			removedRefs,
			sharedChanged: changedRefs.includes("shared"),
			taskContextChanged: changedRefs.includes("taskContext"),
			replayContextChanged: changedRefs.includes("replayContext")
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/RuntimeStore.js
	function makeDescriptorKey(name, targetRef) {
		return `${name || "unknown"}::${targetRef || "workspace"}`;
	}
	function clone$20(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function isPlainObject$2(value) {
		return value != null && typeof value === "object" && !Array.isArray(value);
	}
	function mergeRuntimePatch(base, patch) {
		if (!isPlainObject$2(base) || !isPlainObject$2(patch)) return clone$20(patch);
		const nextValue = { ...clone$20(base) };
		for (const [key, value] of Object.entries(patch)) if (isPlainObject$2(value) && isPlainObject$2(nextValue[key])) nextValue[key] = mergeRuntimePatch(nextValue[key], value);
		else nextValue[key] = clone$20(value);
		return nextValue;
	}
	function applyWidgetStatePatches(state, patches = {}) {
		if (!state?.widgets || !isPlainObject$2(patches) || Object.keys(patches).length === 0) return state;
		const nextWidgets = { ...state.widgets || {} };
		let changed = false;
		for (const [ref, patch] of Object.entries(patches)) {
			if (!nextWidgets[ref] || !isPlainObject$2(patch)) continue;
			nextWidgets[ref] = mergeRuntimePatch(nextWidgets[ref], patch);
			changed = true;
		}
		return changed ? {
			...state,
			widgets: nextWidgets
		} : state;
	}
	function normalizeWidgetStateMap(widgets = {}) {
		return Object.fromEntries(Object.entries(widgets || {}).map(([ref, widgetState]) => [ref, makeWidgetState({
			ref,
			...clone$20(widgetState)
		})]));
	}
	function normalizeWorkspaceState(state) {
		return makeWorkspaceState({
			...clone$20(state),
			widgets: normalizeWidgetStateMap(state?.widgets || {})
		});
	}
	function getTraceRecordPriority(record) {
		const eventKind = record?.eventKind || null;
		if (eventKind === "action") return 3;
		if (eventKind === "perceptionQuery" || eventKind === "dataQuery") return 2;
		if (eventKind === "systemTransition") return 1;
		return 0;
	}
	function pickRepresentativeTraceRecord(currentRecord, nextRecord) {
		if (!currentRecord) return nextRecord || null;
		if (!nextRecord) return currentRecord;
		return getTraceRecordPriority(nextRecord) >= getTraceRecordPriority(currentRecord) ? nextRecord : currentRecord;
	}
	function buildResponsePreview(content, maxLength = 160) {
		const text = String(content || "").trim();
		if (!text) return null;
		if (!Number.isFinite(maxLength) || maxLength <= 0 || text.length <= maxLength) return text;
		return `${text.slice(0, maxLength - 3)}...`;
	}
	function normalizeActorFilter(actors) {
		if (!Array.isArray(actors) || actors.length === 0) return [];
		return Array.from(new Set(actors.filter((actor) => typeof actor === "string" && actor.length > 0)));
	}
	function createMutableMapFacade({ keys, getEntry, setEntry, deleteEntry }) {
		return new Proxy({}, {
			get(_target, prop) {
				if (typeof prop === "symbol") return void 0;
				return getEntry(prop);
			},
			set(_target, prop, value) {
				if (typeof prop === "symbol") return true;
				setEntry(prop, value);
				return true;
			},
			deleteProperty(_target, prop) {
				if (typeof prop === "symbol") return true;
				deleteEntry(prop);
				return true;
			},
			ownKeys() {
				return keys();
			},
			has(_target, prop) {
				if (typeof prop === "symbol") return false;
				return keys().includes(prop);
			},
			getOwnPropertyDescriptor(_target, prop) {
				if (typeof prop === "symbol") return void 0;
				const value = getEntry(prop);
				if (value == null) return void 0;
				return {
					configurable: true,
					enumerable: true,
					writable: true,
					value
				};
			}
		});
	}
	function makeRuntimeStoreCurrentStateSummary(summary = {}) {
		return {
			stateId: null,
			focusedWidgetRef: null,
			focusedWidgetKind: null,
			focusedWidgetTitle: null,
			visibleCount: null,
			selectedCount: null,
			selectionCount: 0,
			activeSelectionRefs: [],
			primarySelectionRef: null,
			primarySelectionSummary: "",
			primarySelectionPredicates: [],
			comparisonTargetCount: 0,
			globalFilterCount: 0,
			taskMode: null,
			coordinationScope: null,
			evidenceType: null,
			interactionHorizon: null,
			replayRunMode: null,
			replayUserIntent: "",
			changedRefs: [],
			removedRefs: [],
			sharedChanged: false,
			taskContextChanged: false,
			replayContextChanged: false,
			...summary
		};
	}
	function makeRuntimeStoreHistoryRetention(retention = {}) {
		return {
			snapshotMax: 0,
			traceMax: 0,
			responseMax: 0,
			...retention
		};
	}
	function makeRuntimeStoreIdentity(identity = {}) {
		return {
			appId: "",
			workspaceId: "",
			...identity
		};
	}
	function makeRuntimeStoreStateSummary(summary = {}) {
		return {
			stateId: null,
			currentBranchId: null,
			previousStateId: null,
			version: 0,
			...summary
		};
	}
	function makeRuntimeStoreIndexes(indexes = {}) {
		return {
			widgetCount: 0,
			dataHandleCount: 0,
			linkCount: 0,
			widgetAdapterCount: 0,
			actionDescriptorCount: 0,
			perceptionDescriptorCount: 0,
			widgetPatchCount: 0,
			...indexes
		};
	}
	function makeRuntimeStoreHistory(history = {}) {
		return {
			snapshotCount: 0,
			traceCount: 0,
			responseCount: 0,
			branchCount: 0,
			retention: makeRuntimeStoreHistoryRetention(),
			maxSnapshotRetention: 0,
			maxTraceRetention: 0,
			maxResponseRetention: 0,
			...history
		};
	}
	function makeRuntimeStoreCapabilities(capabilities = {}) {
		return {
			deltaTracking: false,
			snapshotHistory: false,
			actorScopedHistory: false,
			branchReplay: false,
			traceGraph: false,
			runtimeDataIndex: false,
			widgetAdapterIndex: false,
			widgetStatePatching: false,
			...capabilities
		};
	}
	function makeRuntimeStoreSummary(summary = {}) {
		return {
			currentStateSummary: null,
			...summary,
			identity: makeRuntimeStoreIdentity(summary?.identity),
			state: makeRuntimeStoreStateSummary(summary?.state),
			currentStateSummary: summary?.currentStateSummary == null ? null : makeRuntimeStoreCurrentStateSummary(summary.currentStateSummary),
			indexes: makeRuntimeStoreIndexes(summary?.indexes),
			history: makeRuntimeStoreHistory(summary?.history),
			capabilities: makeRuntimeStoreCapabilities(summary?.capabilities)
		};
	}
	var WidgetVARuntimeStore = class {
		constructor({ appId = "widgetva-app", workspaceId = "main" } = {}) {
			this.stateManager = new StateManager();
			this.widgetRegistry = new WidgetRegistry();
			this.appId = appId;
			this.workspaceId = workspaceId;
			this.description = makeWorkspaceDescription({
				appId: this.appId,
				workspaceId: this.workspaceId,
				generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
				workspaceCapabilities: ["singleWidgetAnalysis"]
			});
			this.state = makeWorkspaceState({
				stateId: `${this.workspaceId}:empty`,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			});
			this.previousState = null;
			this.stateSnapshots = [];
			this.descriptionIndex = {};
			this.dataHandleIndex = {};
			this.linkIndex = {};
			this.widgetAdapterList = [];
			this.widgetAdapterIndex = {};
			this.actionDescriptors = [];
			this.actionIndex = {};
			this.actionNameIndex = {};
			this.perceptionDescriptors = [];
			this.perceptionIndex = {};
			this.perceptionNameIndex = {};
			this.runtimeData = {};
			this.widgetStatePatches = {};
			this.interactionTrace = [];
			this.responseHistory = [];
			this.replayContext = null;
			this.currentBranchId = "main";
			this.version = 0;
			this.maxSnapshotRetention = 50;
			this.maxTraceRetention = 200;
			this.maxResponseRetention = 100;
			this.branchRegistry = { main: {
				branchId: "main",
				label: "Main Branch",
				originStateId: null,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			} };
			this.pendingTransition = null;
			this.listeners = /* @__PURE__ */ new Set();
			this.descriptions = createMutableMapFacade({
				keys: () => Object.keys(this.descriptionIndex || {}),
				getEntry: (ref) => this.descriptionIndex?.[ref] || null,
				setEntry: (ref, description) => this.upsertWidgetDescription(ref, description),
				deleteEntry: (ref) => this.removeWidgetDescription(ref)
			});
			this.widgets = createMutableMapFacade({
				keys: () => Object.keys(this.state?.widgets || {}),
				getEntry: (ref) => this.state?.widgets?.[ref] || null,
				setEntry: (ref, widgetState) => this.upsertWidgetState(ref, widgetState),
				deleteEntry: (ref) => this.removeWidgetState(ref)
			});
			this.dataHandles = createMutableMapFacade({
				keys: () => Object.keys(this.dataHandleIndex || {}),
				getEntry: (ref) => this.dataHandleIndex?.[ref] || null,
				setEntry: (ref, handle) => this.upsertDataHandle(ref, handle),
				deleteEntry: (ref) => this.removeDataHandleDefinition(ref)
			});
			this.links = createMutableMapFacade({
				keys: () => Object.keys(this.linkIndex || {}),
				getEntry: (ref) => this.linkIndex?.[ref] || null,
				setEntry: (ref, link) => this.upsertLinkDefinition(ref, link),
				deleteEntry: (ref) => this.removeLinkDefinition(ref)
			});
			this.widgetAdapters = createMutableMapFacade({
				keys: () => Object.keys(this.widgetAdapterIndex || {}),
				getEntry: (ref) => this.widgetAdapterIndex?.[ref] || null,
				setEntry: (ref, adapter) => this.upsertWidgetAdapter(ref, adapter),
				deleteEntry: (ref) => this.removeWidgetAdapter(ref)
			});
			this.actions = createMutableMapFacade({
				keys: () => Object.keys(this.actionNameIndex || {}),
				getEntry: (name) => this.getActionDescriptor(name) || null,
				setEntry: (name, descriptor) => this.upsertActionDescriptor(name, descriptor),
				deleteEntry: (name) => this.removeActionDescriptor(name)
			});
			this.perceptionQueries = createMutableMapFacade({
				keys: () => Object.keys(this.perceptionNameIndex || {}),
				getEntry: (name) => this.getPerceptionDescriptor(name) || null,
				setEntry: (name, descriptor) => this.upsertPerceptionDescriptor(name, descriptor),
				deleteEntry: (name) => this.removePerceptionDescriptor(name)
			});
		}
		get stateId() {
			return this.state?.stateId || null;
		}
		subscribe(listener) {
			if (typeof listener !== "function") return () => {};
			this.listeners.add(listener);
			return () => {
				this.listeners.delete(listener);
			};
		}
		emitChange() {
			for (const listener of this.listeners) try {
				listener();
			} catch {}
		}
		syncRegistryWorkspace() {
			this.widgetRegistry.replaceWorkspace({
				description: this.description,
				state: this.state,
				widgetAdapters: this.widgetAdapterList
			});
		}
		upsertWidgetDescription(ref, description) {
			if (!ref || !description || typeof description !== "object") return null;
			const nextDescription = makeWidgetDescription({
				ref,
				...clone$20(description)
			});
			this.descriptionIndex = {
				...this.descriptionIndex || {},
				[ref]: nextDescription
			};
			this.description = makeWorkspaceDescription({
				...this.description,
				widgets: Object.values(this.descriptionIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
			return nextDescription;
		}
		removeWidgetDescription(ref) {
			if (!ref || !this.descriptionIndex?.[ref]) return;
			const nextIndex = { ...this.descriptionIndex || {} };
			delete nextIndex[ref];
			this.descriptionIndex = nextIndex;
			this.description = makeWorkspaceDescription({
				...this.description,
				widgets: Object.values(this.descriptionIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		commitWidgetStateMap(nextWidgets, transition = {}) {
			const stateMeta = this.stateManager.createStateMeta({
				workspaceId: this.workspaceId,
				previousState: this.state,
				nextWidgets,
				nextShared: this.state?.shared || {},
				nextTaskContext: this.state?.taskContext,
				nextReplayContext: this.replayContext,
				branchId: this.currentBranchId
			});
			const nextState = makeWorkspaceState({
				...clone$20(this.state),
				stateId: stateMeta.stateId,
				createdAt: stateMeta.createdAt,
				widgets: nextWidgets
			});
			nextState.shared = {
				...nextState.shared || {},
				globalFilters: deriveGlobalFiltersFromState(nextState)
			};
			return this.commitState(nextState, {
				transitionType: transition.transitionType || "continue",
				parentStateId: transition.parentStateId,
				branchId: transition.branchId,
				branchLabel: transition.branchLabel
			});
		}
		upsertWidgetState(ref, widgetState) {
			if (!ref || !widgetState || typeof widgetState !== "object") return null;
			const nextWidgets = {
				...this.state?.widgets || {},
				[ref]: {
					ref,
					...clone$20(widgetState)
				}
			};
			this.commitWidgetStateMap(nextWidgets);
			return this.state.widgets[ref];
		}
		removeWidgetState(ref) {
			if (!ref || !this.state?.widgets?.[ref]) return;
			const nextWidgets = { ...this.state?.widgets || {} };
			delete nextWidgets[ref];
			this.commitWidgetStateMap(nextWidgets);
		}
		upsertDataHandle(ref, handle) {
			if (!ref || !handle || typeof handle !== "object") return null;
			const nextHandle = makeDataHandle({
				ref,
				...clone$20(handle)
			});
			this.dataHandleIndex = {
				...this.dataHandleIndex || {},
				[ref]: nextHandle
			};
			this.description = makeWorkspaceDescription({
				...this.description,
				dataHandles: Object.values(this.dataHandleIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
			return nextHandle;
		}
		removeDataHandleDefinition(ref) {
			if (!ref || !this.dataHandleIndex?.[ref]) return;
			const nextIndex = { ...this.dataHandleIndex || {} };
			delete nextIndex[ref];
			this.dataHandleIndex = nextIndex;
			this.description = makeWorkspaceDescription({
				...this.description,
				dataHandles: Object.values(this.dataHandleIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		upsertLinkDefinition(ref, link) {
			if (!ref || !link || typeof link !== "object") return null;
			const nextLink = makeWidgetLink({
				ref,
				...clone$20(link)
			});
			this.linkIndex = {
				...this.linkIndex || {},
				[ref]: nextLink
			};
			this.description = makeWorkspaceDescription({
				...this.description,
				links: Object.values(this.linkIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
			return nextLink;
		}
		removeLinkDefinition(ref) {
			if (!ref || !this.linkIndex?.[ref]) return;
			const nextIndex = { ...this.linkIndex || {} };
			delete nextIndex[ref];
			this.linkIndex = nextIndex;
			this.description = makeWorkspaceDescription({
				...this.description,
				links: Object.values(this.linkIndex)
			});
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		upsertWidgetAdapter(ref, adapter) {
			if (!ref || !adapter || typeof adapter !== "object") return null;
			const nextAdapter = adapter.widgetRef === ref ? adapter : {
				...adapter,
				widgetRef: ref
			};
			this.widgetAdapterIndex = {
				...this.widgetAdapterIndex || {},
				[ref]: nextAdapter
			};
			this.widgetAdapterList = Object.values(this.widgetAdapterIndex);
			this.syncRegistryWorkspace();
			this.emitChange();
			return nextAdapter;
		}
		removeWidgetAdapter(ref) {
			if (!ref || !this.widgetAdapterIndex?.[ref]) return;
			const nextIndex = { ...this.widgetAdapterIndex || {} };
			delete nextIndex[ref];
			this.widgetAdapterIndex = nextIndex;
			this.widgetAdapterList = Object.values(nextIndex);
			this.syncRegistryWorkspace();
			this.emitChange();
		}
		rebuildActionIndexes(actionDescriptors = []) {
			this.actionDescriptors = actionDescriptors.map((item) => makeActionDescriptor(item));
			this.actionIndex = Object.fromEntries(this.actionDescriptors.map((item) => [makeDescriptorKey(item.name, item.targetRef), item]));
			this.actionNameIndex = this.actionDescriptors.reduce((acc, item) => {
				if (!acc[item.name]) acc[item.name] = [];
				acc[item.name].push(item);
				return acc;
			}, {});
		}
		rebuildPerceptionIndexes(perceptionDescriptors = []) {
			this.perceptionDescriptors = perceptionDescriptors.map((item) => makePerceptionDescriptor(item));
			this.perceptionIndex = Object.fromEntries(this.perceptionDescriptors.map((item) => [makeDescriptorKey(item.name, item.targetRef), item]));
			this.perceptionNameIndex = this.perceptionDescriptors.reduce((acc, item) => {
				if (!acc[item.name]) acc[item.name] = [];
				acc[item.name].push(item);
				return acc;
			}, {});
		}
		upsertActionDescriptor(name, descriptor) {
			const descriptorName = descriptor?.name || name;
			if (!descriptorName || !descriptor || typeof descriptor !== "object") return null;
			const nextDescriptor = {
				name: descriptorName,
				...clone$20(descriptor)
			};
			const nextKey = makeDescriptorKey(nextDescriptor.name, nextDescriptor.targetRef);
			const remainingDescriptors = this.actionDescriptors.filter((item) => makeDescriptorKey(item?.name, item?.targetRef) !== nextKey);
			this.rebuildActionIndexes([...remainingDescriptors, nextDescriptor]);
			this.description = makeWorkspaceDescription({
				...this.description,
				actions: [...this.actionDescriptors]
			});
			this.emitChange();
			return nextDescriptor;
		}
		removeActionDescriptor(name) {
			if (!name || !this.actionNameIndex?.[name]?.length) return;
			this.rebuildActionIndexes(this.actionDescriptors.filter((item) => item?.name !== name));
			this.description = makeWorkspaceDescription({
				...this.description,
				actions: [...this.actionDescriptors]
			});
			this.emitChange();
		}
		upsertPerceptionDescriptor(name, descriptor) {
			const descriptorName = descriptor?.name || name;
			if (!descriptorName || !descriptor || typeof descriptor !== "object") return null;
			const nextDescriptor = {
				name: descriptorName,
				...clone$20(descriptor)
			};
			const nextKey = makeDescriptorKey(nextDescriptor.name, nextDescriptor.targetRef);
			const remainingDescriptors = this.perceptionDescriptors.filter((item) => makeDescriptorKey(item?.name, item?.targetRef) !== nextKey);
			this.rebuildPerceptionIndexes([...remainingDescriptors, nextDescriptor]);
			this.description = makeWorkspaceDescription({
				...this.description,
				perceptionQueries: [...this.perceptionDescriptors]
			});
			this.emitChange();
			return nextDescriptor;
		}
		removePerceptionDescriptor(name) {
			if (!name || !this.perceptionNameIndex?.[name]?.length) return;
			this.rebuildPerceptionIndexes(this.perceptionDescriptors.filter((item) => item?.name !== name));
			this.description = makeWorkspaceDescription({
				...this.description,
				perceptionQueries: [...this.perceptionDescriptors]
			});
			this.emitChange();
		}
		registerWidget(description, widgetState) {
			const ref = description?.ref || widgetState?.ref || null;
			if (!ref || !description || !widgetState) return null;
			const nextDescription = makeWidgetDescription({
				ref,
				...clone$20(description)
			});
			const nextWidgetState = {
				ref,
				...clone$20(widgetState)
			};
			const nextDescriptionIndex = {
				...this.descriptionIndex || {},
				[ref]: nextDescription
			};
			const nextWidgets = {
				...this.state?.widgets || {},
				[ref]: nextWidgetState
			};
			const stateMeta = this.stateManager.createStateMeta({
				workspaceId: this.workspaceId,
				previousState: this.state,
				nextWidgets,
				nextShared: this.state?.shared || {},
				nextTaskContext: this.state?.taskContext,
				nextReplayContext: this.replayContext,
				branchId: this.currentBranchId
			});
			this.descriptionIndex = nextDescriptionIndex;
			this.description = makeWorkspaceDescription({
				...this.description,
				widgets: Object.values(nextDescriptionIndex)
			});
			const nextState = makeWorkspaceState({
				...clone$20(this.state),
				stateId: stateMeta.stateId,
				createdAt: stateMeta.createdAt,
				widgets: nextWidgets
			});
			nextState.shared = {
				...nextState.shared || {},
				globalFilters: deriveGlobalFiltersFromState(nextState)
			};
			this.commitState(nextState, { transitionType: "continue" });
			this.syncRegistryWorkspace();
			return this.state.widgets?.[ref] || null;
		}
		registerLink(link) {
			const ref = link?.ref || null;
			if (!ref || !link || typeof link !== "object") return null;
			const nextLink = makeWidgetLink({
				ref,
				...clone$20(link)
			});
			this.linkIndex = {
				...this.linkIndex || {},
				[ref]: nextLink
			};
			this.description = makeWorkspaceDescription({
				...this.description,
				links: Object.values(this.linkIndex)
			});
			this.syncRegistryWorkspace();
			this.version += 1;
			this.emitChange();
			return nextLink;
		}
		replaceWorkspace(nextWorkspace) {
			const transition = this.pendingTransition || {};
			const normalizedDescriptions = Array.isArray(nextWorkspace.description?.widgets) ? nextWorkspace.description.widgets.map((item) => makeWidgetDescription(item)) : [];
			const normalizedActions = Array.isArray(nextWorkspace.description?.actions) ? nextWorkspace.description.actions.map((item) => makeActionDescriptor(item)) : [];
			const normalizedPerceptionQueries = Array.isArray(nextWorkspace.description?.perceptionQueries) ? nextWorkspace.description.perceptionQueries.map((item) => makePerceptionDescriptor(item)) : [];
			this.appId = nextWorkspace.description?.appId || this.appId;
			this.workspaceId = nextWorkspace.description?.workspaceId || this.workspaceId;
			this.descriptionIndex = Object.fromEntries(normalizedDescriptions.map((item) => [item.ref, item]));
			this.dataHandleIndex = Object.fromEntries((nextWorkspace.description?.dataHandles || []).map((item) => {
				const normalizedHandle = makeDataHandle(item);
				return [normalizedHandle.ref, normalizedHandle];
			}));
			this.linkIndex = Object.fromEntries((nextWorkspace.description?.links || []).map((item) => {
				const normalizedLink = makeWidgetLink(item);
				return [normalizedLink.ref, normalizedLink];
			}));
			this.description = makeWorkspaceDescription({
				...nextWorkspace.description,
				widgets: Object.values(this.descriptionIndex),
				dataHandles: Object.values(this.dataHandleIndex),
				links: Object.values(this.linkIndex),
				actions: normalizedActions,
				perceptionQueries: normalizedPerceptionQueries
			});
			this.widgetAdapterList = Array.isArray(nextWorkspace.widgetAdapters) ? nextWorkspace.widgetAdapters : [];
			this.widgetAdapterIndex = Object.fromEntries(this.widgetAdapterList.map((adapter) => [adapter.widgetRef, adapter]));
			this.rebuildActionIndexes(normalizedActions);
			this.rebuildPerceptionIndexes(normalizedPerceptionQueries);
			this.runtimeData = nextWorkspace.runtimeData || {};
			this.replayContext = clone$20(nextWorkspace.replayContext || null);
			const patchedState = applyWidgetStatePatches(nextWorkspace.state, this.widgetStatePatches);
			this.widgetRegistry.replaceWorkspace({
				description: this.description,
				state: patchedState,
				widgetAdapters: this.widgetAdapterList
			});
			this.commitState(patchedState, transition);
			this.syncCurrentViewRuntimeData();
			this.syncCurrentSelectionRuntimeData();
		}
		readDescription() {
			return this.description;
		}
		listWidgetDescriptions() {
			return Object.values(this.descriptionIndex);
		}
		getWidgetDescription(ref) {
			return ref ? this.widgetRegistry.getWidget(ref) : null;
		}
		getWidgetState(ref) {
			return ref ? this.widgetRegistry.getWidgetState(ref) : null;
		}
		getWidgetEntry(ref) {
			return ref ? this.widgetRegistry.getWidgetEntry(ref) : null;
		}
		getResolvedWidget(ref) {
			return ref ? this.widgetRegistry.getResolvedWidget(ref) : null;
		}
		getResolvedWidgetForTarget(ref) {
			if (!ref) return null;
			const directTarget = this.widgetRegistry.getResolvedWidgetForTarget(ref);
			if (directTarget) return directTarget;
			const widgetRef = this.readRuntimeData(ref)?.widgetRef || null;
			return widgetRef ? this.widgetRegistry.getResolvedWidget(widgetRef) : null;
		}
		listDataHandles() {
			return Object.values(this.dataHandleIndex);
		}
		getDataHandle(ref) {
			return ref ? this.widgetRegistry.getDataHandle(ref) : null;
		}
		listWidgetAdapters() {
			return [...this.widgetAdapterList];
		}
		getWidgetAdapter(widgetRef) {
			return widgetRef ? this.widgetAdapterIndex?.[widgetRef] || null : null;
		}
		describeWidgetRegistry() {
			return this.widgetRegistry.describe();
		}
		listLinks() {
			const links = Object.values(this.linkIndex);
			const relationLinks = Object.values(makeCoordinationRelationMap(this.state?.coordination?.relations || {}));
			const deduped = /* @__PURE__ */ new Map();
			for (const link of [...links, ...relationLinks]) {
				if (!link?.ref) continue;
				deduped.set(link.ref, link);
			}
			return Array.from(deduped.values());
		}
		getLink(ref) {
			return ref ? this.widgetRegistry.getLink(ref) : null;
		}
		listActions() {
			return [...this.actionDescriptors];
		}
		listPerceptionQueries() {
			return [...this.perceptionDescriptors];
		}
		getActionDescriptor(name, targetRef) {
			if (!name) return null;
			if (targetRef) return this.actionIndex?.[makeDescriptorKey(name, targetRef)] || null;
			const matches = this.actionNameIndex?.[name] || [];
			if (matches.length === 1) return matches[0];
			return matches.find((item) => item.scope === "workspace") || matches[0] || null;
		}
		getPerceptionDescriptor(name, targetRef) {
			if (!name) return null;
			if (targetRef) return this.perceptionIndex?.[makeDescriptorKey(name, targetRef)] || null;
			const matches = this.perceptionNameIndex?.[name] || [];
			if (matches.length === 1) return matches[0];
			return matches.find((item) => item.scope === "workspace") || matches[0] || null;
		}
		readState(options = {}) {
			if (options.deltaSince) {
				const baseSnapshot = this.stateSnapshots.find((snapshot) => snapshot.stateId === options.deltaSince);
				if (baseSnapshot) {
					const delta = this.stateManager.createDelta({
						previousState: baseSnapshot.state,
						nextState: this.state
					});
					return this.stateManager.pickRefs({
						state: {
							...this.state,
							delta
						},
						replayContext: this.replayContext,
						refs: Array.isArray(options.refs) && options.refs.length > 0 ? (delta.changedRefs || []).filter((ref) => options.refs.includes(ref)) : delta.changedRefs || []
					});
				}
			}
			if (options.deltaSince && this.previousState?.stateId === options.deltaSince) return this.stateManager.pickRefs({
				state: this.state,
				replayContext: this.replayContext,
				refs: Array.isArray(options.refs) && options.refs.length > 0 ? (this.state?.delta?.changedRefs || []).filter((ref) => options.refs.includes(ref)) : this.state?.delta?.changedRefs || []
			});
			if (Array.isArray(options.refs) && options.refs.length > 0) return this.stateManager.pickRefs({
				state: this.state,
				replayContext: this.replayContext,
				refs: options.refs
			});
			return this.state;
		}
		buildStatePatch(refs) {
			return this.stateManager.buildStatePatch({
				state: this.state,
				replayContext: this.replayContext,
				refs
			});
		}
		summarizeState(state = this.state) {
			return summarizeWorkspaceState(state === this.state ? {
				...state,
				replayContext: clone$20(this.replayContext)
			} : state);
		}
		patchWidget(ref, patch = {}) {
			const currentWidget = this.state?.widgets?.[ref];
			if (!currentWidget) throw new Error(`Unknown widget: ${ref}`);
			this.widgetStatePatches = {
				...this.widgetStatePatches,
				[ref]: mergeRuntimePatch(this.widgetStatePatches?.[ref] || {}, patch)
			};
			const nextWidgets = {
				...this.state?.widgets || {},
				[ref]: mergeRuntimePatch({
					...clone$20(currentWidget),
					version: (currentWidget.version || 0) + 1,
					updatedAt: (/* @__PURE__ */ new Date()).toISOString()
				}, patch)
			};
			const stateMeta = this.stateManager.createStateMeta({
				workspaceId: this.workspaceId,
				previousState: this.state,
				nextWidgets,
				nextShared: this.state?.shared || {},
				nextTaskContext: this.state?.taskContext,
				nextReplayContext: this.replayContext,
				branchId: this.currentBranchId
			});
			const nextState = makeWorkspaceState({
				...clone$20(this.state),
				stateId: stateMeta.stateId,
				createdAt: stateMeta.createdAt,
				widgets: nextWidgets
			});
			nextState.shared = {
				...nextState.shared || {},
				globalFilters: deriveGlobalFiltersFromState(nextState)
			};
			this.commitState(nextState, { transitionType: "continue" });
			return nextState;
		}
		resetWidgetPatches() {
			this.widgetStatePatches = {};
		}
		clearWidgetPatches(refs = []) {
			if (!Array.isArray(refs) || refs.length === 0) return;
			const nextPatches = { ...this.widgetStatePatches || {} };
			let changed = false;
			for (const ref of refs) {
				if (!ref || !Object.prototype.hasOwnProperty.call(nextPatches, ref)) continue;
				delete nextPatches[ref];
				changed = true;
			}
			if (changed) this.widgetStatePatches = nextPatches;
		}
		readRuntimeData(ref) {
			return ref ? this.runtimeData?.[ref] || null : this.runtimeData;
		}
		resolveSelectionDataRef(selectionRef) {
			const parts = parseRef(selectionRef);
			if (!parts?.widgetId || !parts?.selectionId) return null;
			return makeSelectionScopedDataRef({
				appId: parts.appId || this.appId,
				workspaceId: parts.workspaceId || this.workspaceId,
				widgetId: parts.widgetId,
				selectionId: parts.selectionId
			});
		}
		updateRuntimeData(ref, updater) {
			if (!ref || typeof updater !== "function") return null;
			const currentEntry = this.runtimeData?.[ref];
			if (!currentEntry) return null;
			const nextEntry = updater(clone$20(currentEntry));
			if (!nextEntry || typeof nextEntry !== "object") return currentEntry;
			this.upsertRuntimeData(ref, nextEntry);
			this.emitChange();
			return nextEntry;
		}
		upsertRuntimeData(ref, entry) {
			if (!ref || !entry || typeof entry !== "object") return null;
			this.runtimeData = {
				...this.runtimeData || {},
				[ref]: entry
			};
			const nextHandle = entry.handle || this.dataHandleIndex?.[ref] || null;
			if (nextHandle) {
				this.dataHandleIndex = {
					...this.dataHandleIndex || {},
					[ref]: nextHandle
				};
				this.widgetRegistry.updateDataHandle?.(ref, nextHandle);
			}
			if (Array.isArray(this.description?.dataHandles)) {
				const hasExistingHandle = this.description.dataHandles.some((handle) => handle?.ref === ref);
				this.description = {
					...this.description,
					dataHandles: hasExistingHandle ? this.description.dataHandles.map((handle) => handle?.ref === ref ? nextHandle || handle : handle) : nextHandle ? [...this.description.dataHandles, nextHandle] : this.description.dataHandles
				};
			}
			return entry;
		}
		removeRuntimeData(ref) {
			if (!ref || !this.runtimeData?.[ref]) return;
			const nextRuntimeData = { ...this.runtimeData || {} };
			delete nextRuntimeData[ref];
			this.runtimeData = nextRuntimeData;
			if (this.dataHandleIndex?.[ref]) {
				const nextDataHandleIndex = { ...this.dataHandleIndex || {} };
				delete nextDataHandleIndex[ref];
				this.dataHandleIndex = nextDataHandleIndex;
				this.widgetRegistry.removeDataHandle?.(ref);
			}
			if (Array.isArray(this.description?.dataHandles)) this.description = {
				...this.description,
				dataHandles: this.description.dataHandles.filter((handle) => handle?.ref !== ref)
			};
		}
		syncCurrentSelectionRuntimeData() {
			const currentSelectionDataRef = makeCurrentSelectionDataRef({
				appId: this.appId,
				workspaceId: this.workspaceId
			});
			const focusedWidgetRef = this.state?.shared?.focusedWidget || Object.keys(this.state?.widgets || {})[0] || this.listWidgetDescriptions()[0]?.ref || null;
			const focusedWidgetId = (focusedWidgetRef ? this.getWidgetState(focusedWidgetRef) : null)?.widgetId || null;
			const focusedSelectionDataRef = focusedWidgetId ? makeWidgetSelectionDataRef({
				appId: this.appId,
				workspaceId: this.workspaceId,
				widgetId: focusedWidgetId
			}) : null;
			const focusedSelectionEntry = focusedSelectionDataRef ? this.readRuntimeData(focusedSelectionDataRef) : null;
			const focusedSelectionRef = focusedWidgetRef ? Object.keys(this.state?.widgets?.[focusedWidgetRef]?.selections || {})[0] || null : null;
			if (!focusedSelectionEntry?.widgetRef || !Array.isArray(focusedSelectionEntry?.rows)) {
				this.removeRuntimeData(currentSelectionDataRef);
				return null;
			}
			const currentSelectionHandle = buildDerivedDataHandle({
				ref: currentSelectionDataRef,
				title: "Current Selection Data",
				description: "Current rows for the focused widget selection.",
				rows: focusedSelectionEntry.rows,
				selectedCount: focusedSelectionEntry.rows.length,
				kind: "selectionData",
				scope: "workspaceCurrent",
				widgetRef: focusedSelectionEntry.widgetRef,
				sourceSelectionRef: focusedSelectionRef
			});
			const nextEntry = {
				ref: currentSelectionDataRef,
				rows: focusedSelectionEntry.rows,
				baseRows: focusedSelectionEntry.baseRows,
				handle: currentSelectionHandle,
				widgetRef: focusedSelectionEntry.widgetRef,
				sourceSelectionRef: focusedSelectionRef,
				kind: "selectionData",
				scope: "workspaceCurrent"
			};
			this.upsertRuntimeData(currentSelectionDataRef, nextEntry);
			return nextEntry;
		}
		syncCurrentViewRuntimeData() {
			const currentViewDataRef = makeCurrentViewDataRef({
				appId: this.appId,
				workspaceId: this.workspaceId
			});
			const focusedWidgetRef = this.state?.shared?.focusedWidget || Object.keys(this.state?.widgets || {})[0] || this.listWidgetDescriptions()[0]?.ref || null;
			const focusedWidget = focusedWidgetRef ? this.getWidgetState(focusedWidgetRef) : null;
			const focusedDataRef = focusedWidget?.data?.currentDataRef || focusedWidget?.data?.sourceDataRef || null;
			const focusedDataEntry = focusedDataRef ? this.readRuntimeData(focusedDataRef) : null;
			if (!focusedDataEntry?.widgetRef || !Array.isArray(focusedDataEntry?.rows)) {
				this.removeRuntimeData(currentViewDataRef);
				return null;
			}
			const currentViewHandle = buildDerivedDataHandle({
				ref: currentViewDataRef,
				title: "Current View Data",
				description: "Current visible rows for the focused widget.",
				rows: focusedDataEntry.rows,
				selectedCount: focusedDataEntry.handle?.stats?.selectedCount || 0,
				kind: "dataView",
				scope: "workspaceCurrentView",
				widgetRef: focusedDataEntry.widgetRef
			});
			const nextEntry = {
				ref: currentViewDataRef,
				rows: focusedDataEntry.rows,
				baseRows: focusedDataEntry.baseRows,
				handle: currentViewHandle,
				widgetRef: focusedDataEntry.widgetRef,
				kind: "dataView",
				scope: "workspaceCurrentView"
			};
			this.upsertRuntimeData(currentViewDataRef, nextEntry);
			return nextEntry;
		}
		syncSelectionRuntimeData(widgetRef) {
			if (!widgetRef) return null;
			const widgetState = this.getWidgetState(widgetRef);
			const widgetDescription = this.getWidgetDescription(widgetRef);
			const widgetId = widgetState?.widgetId || widgetDescription?.widgetId || null;
			const managedSelectionDataRefs = Object.values(this.runtimeData || {}).filter((entry) => entry?.widgetRef === widgetRef && entry?.kind === "selectionData").map((entry) => entry?.ref).filter((ref) => typeof ref === "string");
			const currentDataRef = widgetState?.data?.currentDataRef || null;
			const currentDataEntry = currentDataRef ? this.readRuntimeData(currentDataRef) : null;
			if (!widgetId || !currentDataEntry) {
				for (const managedRef of managedSelectionDataRefs) this.removeRuntimeData(managedRef);
				this.syncCurrentViewRuntimeData();
				this.syncCurrentSelectionRuntimeData();
				this.emitChange();
				return null;
			}
			const activeSelectionEntries = Object.entries(widgetState?.selections || {}).filter(([selectionRef, selectionState]) => Boolean(selectionRef) && Boolean(selectionState));
			const selectionDataRef = makeWidgetSelectionDataRef({
				appId: this.appId,
				workspaceId: this.workspaceId,
				widgetId
			});
			if (activeSelectionEntries.length === 0) {
				for (const managedRef of managedSelectionDataRefs) this.removeRuntimeData(managedRef);
				this.syncCurrentViewRuntimeData();
				this.syncCurrentSelectionRuntimeData();
				this.emitChange();
				return null;
			}
			const visibleRows = Array.isArray(currentDataEntry?.rows) ? currentDataEntry.rows : [];
			const selections = activeSelectionEntries.map(([, selectionState]) => selectionState);
			const selectedRows = visibleRows.filter((row) => rowMatchesAnySelection(row, selections));
			const nextManagedRefs = new Set([selectionDataRef]);
			const nextEntry = {
				ref: selectionDataRef,
				rows: selectedRows,
				baseRows: visibleRows,
				handle: buildDerivedDataHandle({
					ref: selectionDataRef,
					title: `${widgetDescription?.title || widgetId} Selection Data`,
					description: `Current rows selected on widget ${widgetId}.`,
					rows: selectedRows,
					selectedCount: selectedRows.length,
					kind: "selectionData",
					scope: "combined",
					widgetRef
				}),
				widgetRef,
				kind: "selectionData",
				scope: "combined"
			};
			this.upsertRuntimeData(selectionDataRef, nextEntry);
			for (const [selectionRef, selectionState] of activeSelectionEntries) {
				const selectionScopedDataRef = this.resolveSelectionDataRef(selectionRef);
				if (!selectionScopedDataRef) continue;
				nextManagedRefs.add(selectionScopedDataRef);
				const selectionId = selectionRef.split("/").pop() || "selection";
				const selectionRows = visibleRows.filter((row) => rowMatchesSelection(row, selectionState));
				const selectionScopedHandle = buildDerivedDataHandle({
					ref: selectionScopedDataRef,
					title: `${widgetDescription?.title || widgetId} Selection ${selectionId} Data`,
					description: `Current rows for selection ${selectionId} on widget ${widgetId}.`,
					rows: selectionRows,
					selectedCount: selectionRows.length,
					kind: "selectionData",
					scope: "selection",
					widgetRef,
					sourceSelectionRef: selectionRef
				});
				this.upsertRuntimeData(selectionScopedDataRef, {
					ref: selectionScopedDataRef,
					rows: selectionRows,
					baseRows: visibleRows,
					handle: selectionScopedHandle,
					widgetRef,
					sourceSelectionRef: selectionRef,
					kind: "selectionData",
					scope: "selection"
				});
			}
			for (const managedRef of managedSelectionDataRefs) if (!nextManagedRefs.has(managedRef)) this.removeRuntimeData(managedRef);
			this.syncCurrentViewRuntimeData();
			this.syncCurrentSelectionRuntimeData();
			this.emitChange();
			return nextEntry;
		}
		readSnapshot(stateId) {
			const snapshot = this.stateSnapshots.find((item) => item.stateId === stateId);
			return snapshot ? clone$20(snapshot.state) : null;
		}
		readSnapshotEntry(stateId) {
			const snapshot = this.stateSnapshots.find((item) => item.stateId === stateId);
			return snapshot ? clone$20(snapshot) : null;
		}
		readCurrentSnapshotMeta() {
			const stateId = this.state?.stateId;
			return stateId ? this.readSnapshotEntry(stateId) : null;
		}
		describeStore() {
			return makeRuntimeStoreSummary({
				identity: makeRuntimeStoreIdentity({
					appId: this.appId || null,
					workspaceId: this.workspaceId || null
				}),
				state: makeRuntimeStoreStateSummary({
					stateId: this.readState()?.stateId || null,
					currentBranchId: this.currentBranchId || null,
					previousStateId: this.previousState?.stateId || null,
					version: this.version || 0
				}),
				currentStateSummary: this.summarizeState(),
				indexes: makeRuntimeStoreIndexes({
					widgetCount: this.listWidgetDescriptions().length,
					dataHandleCount: this.listDataHandles().length,
					linkCount: this.listLinks().length,
					widgetAdapterCount: this.listWidgetAdapters().length,
					actionDescriptorCount: this.listActions().length,
					perceptionDescriptorCount: this.listPerceptionQueries().length,
					widgetPatchCount: Object.keys(this.widgetStatePatches || {}).length
				}),
				history: makeRuntimeStoreHistory({
					snapshotCount: Array.isArray(this.stateSnapshots) ? this.stateSnapshots.length : 0,
					traceCount: Array.isArray(this.interactionTrace) ? this.interactionTrace.length : 0,
					responseCount: Array.isArray(this.responseHistory) ? this.responseHistory.length : 0,
					branchCount: this.listBranches().length,
					retention: makeRuntimeStoreHistoryRetention({
						snapshotMax: this.maxSnapshotRetention,
						traceMax: this.maxTraceRetention,
						responseMax: this.maxResponseRetention
					}),
					maxSnapshotRetention: this.maxSnapshotRetention,
					maxTraceRetention: this.maxTraceRetention,
					maxResponseRetention: this.maxResponseRetention
				}),
				capabilities: makeRuntimeStoreCapabilities({
					deltaTracking: true,
					snapshotHistory: true,
					actorScopedHistory: true,
					branchReplay: true,
					traceGraph: true,
					runtimeDataIndex: true,
					widgetAdapterIndex: true,
					widgetStatePatching: true
				})
			});
		}
		buildRepresentativeTraceMap() {
			const traceByStateId = /* @__PURE__ */ new Map();
			for (const record of this.interactionTrace) {
				if (!record?.stateId) continue;
				traceByStateId.set(record.stateId, pickRepresentativeTraceRecord(traceByStateId.get(record.stateId), record));
			}
			return traceByStateId;
		}
		listStateSnapshots(options = 50) {
			const normalizedOptions = typeof options === "number" ? { limit: options } : options || {};
			const limit = Number.isFinite(normalizedOptions.limit) ? normalizedOptions.limit : 50;
			const sinceStateId = normalizedOptions.sinceStateId || null;
			const allowedActors = normalizeActorFilter(normalizedOptions.actors);
			const traceByStateId = this.buildRepresentativeTraceMap();
			const fullSnapshots = Array.isArray(this.stateSnapshots) ? this.stateSnapshots : [];
			const scopedSnapshots = sinceStateId ? (() => {
				const idx = fullSnapshots.findIndex((snapshot) => snapshot?.stateId === sinceStateId);
				return idx >= 0 ? fullSnapshots.slice(idx + 1) : fullSnapshots;
			})() : fullSnapshots;
			return (allowedActors.length > 0 ? scopedSnapshots.filter((snapshot) => {
				const actor = traceByStateId.get(snapshot?.stateId)?.actor || "system";
				return allowedActors.includes(actor);
			}) : scopedSnapshots).slice(-limit).map((item) => {
				const representativeTrace = traceByStateId.get(item.stateId);
				return makeStateSnapshotMeta({
					stateId: item.stateId,
					createdAt: item.createdAt || item.state?.createdAt || null,
					baseStateId: (item.parentStateId ?? item.state?.delta?.baseStateId) || null,
					branchId: item.branchId || null,
					transitionType: item.transitionType || "continue",
					branchLabel: item.branchLabel || null,
					actor: representativeTrace?.actor || "system",
					changedRefs: Array.isArray(item.state?.delta?.changedRefs) ? [...item.state.delta.changedRefs] : [],
					removedRefs: Array.isArray(item.state?.delta?.removedRefs) ? [...item.state.delta.removedRefs] : []
				});
			});
		}
		commitState(nextState, transition = {}) {
			const previousReplayContext = clone$20(this.state?.replayContext);
			this.previousState = this.state;
			this.state = normalizeWorkspaceState(nextState);
			this.state.replayContext = clone$20(this.replayContext);
			this.widgetRegistry.setState(this.state);
			this.version += 1;
			this.state.delta = this.stateManager.createDelta({
				previousState: this.previousState,
				nextState: this.state,
				previousReplayContext,
				nextReplayContext: this.replayContext
			});
			const snapshotEntry = {
				stateId: this.state.stateId,
				state: clone$20(this.state),
				replayContext: clone$20(this.replayContext || null),
				branchId: transition.branchId || this.currentBranchId,
				parentStateId: transition.parentStateId ?? this.previousState?.stateId ?? null,
				transitionType: transition.transitionType || "continue",
				branchLabel: transition.branchLabel || null,
				createdAt: this.state.createdAt || (/* @__PURE__ */ new Date()).toISOString()
			};
			const hasStateChange = (this.state.delta?.changedRefs?.length || 0) > 0 || (this.state.delta?.removedRefs?.length || 0) > 0;
			const latestSnapshot = this.stateSnapshots[this.stateSnapshots.length - 1] || null;
			if (!latestSnapshot || latestSnapshot.stateId !== snapshotEntry.stateId || snapshotEntry.transitionType !== "continue" || hasStateChange) {
				this.stateSnapshots.push(snapshotEntry);
				if (this.stateSnapshots.length > this.maxSnapshotRetention) this.stateSnapshots = this.stateSnapshots.slice(-this.maxSnapshotRetention);
			}
			this.syncCurrentViewRuntimeData();
			this.syncCurrentSelectionRuntimeData();
			this.pendingTransition = null;
			this.emitChange();
			return this.state;
		}
		listBranches() {
			return Object.values(this.branchRegistry).map((entry) => makeBranchSummary(clone$20(entry)));
		}
		beginTransition({ transitionType = "continue", parentStateId = void 0, branchId = void 0, branchLabel = void 0 } = {}) {
			this.pendingTransition = {
				transitionType,
				parentStateId,
				branchId,
				branchLabel
			};
		}
		beginBranchFromState({ stateId, label } = {}) {
			if (!stateId) throw new Error("beginBranchFromState requires a stateId.");
			const existing = this.readSnapshotEntry(stateId);
			if (!existing) throw new Error(`Unknown branch origin state: ${stateId}`);
			const branchId = `branch_${Date.now()}`;
			const branchLabel = label || `Branch from ${stateId}`;
			this.branchRegistry[branchId] = {
				branchId,
				label: branchLabel,
				originStateId: stateId,
				parentBranchId: existing.branchId || this.currentBranchId,
				createdAt: (/* @__PURE__ */ new Date()).toISOString()
			};
			this.currentBranchId = branchId;
			this.beginTransition({
				transitionType: "branch",
				parentStateId: stateId,
				branchId,
				branchLabel
			});
			return this.branchRegistry[branchId];
		}
		buildTraceGraph(options = 50) {
			const normalizedOptions = typeof options === "number" ? { limit: options } : options || {};
			const limit = Number.isFinite(normalizedOptions.limit) ? normalizedOptions.limit : 50;
			const sinceStateId = normalizedOptions.sinceStateId || null;
			const allowedActors = normalizeActorFilter(normalizedOptions.actors);
			const fullSnapshots = Array.isArray(this.stateSnapshots) ? this.stateSnapshots : [];
			const scopedSnapshots = sinceStateId ? (() => {
				const idx = fullSnapshots.findIndex((snapshot) => snapshot?.stateId === sinceStateId);
				return idx >= 0 ? fullSnapshots.slice(idx + 1) : fullSnapshots;
			})() : fullSnapshots;
			const traceByStateId = this.buildRepresentativeTraceMap();
			const snapshots = (allowedActors.length > 0 ? scopedSnapshots.filter((snapshot) => {
				const actor = traceByStateId.get(snapshot?.stateId)?.actor || "system";
				return allowedActors.includes(actor);
			}) : scopedSnapshots).slice(-limit);
			const latestResponseByStateId = /* @__PURE__ */ new Map();
			for (const response of this.responseHistory) {
				if (!response?.stateId) continue;
				latestResponseByStateId.set(response.stateId, response);
			}
			const nodes = snapshots.map((snapshot) => {
				const record = traceByStateId.get(snapshot.stateId);
				const response = latestResponseByStateId.get(snapshot.stateId);
				const queryName = getInteractionTraceQueryName(record?.query);
				return makeTraceGraphNode({
					id: snapshot.stateId,
					stateId: snapshot.stateId,
					parentStateId: snapshot.parentStateId || null,
					branchId: snapshot.branchId || "main",
					branchLabel: snapshot.branchLabel || null,
					timestamp: snapshot.createdAt || snapshot.state?.createdAt || null,
					actor: record?.actor || "system",
					eventFamily: record?.eventFamily || getInteractionTraceEventFamily(record),
					querySurface: record?.querySurface || getInteractionTraceQuerySurface(record),
					actionName: record?.action?.name || null,
					queryName,
					responseId: response?.responseId || null,
					responseActor: response?.actor || null,
					responsePreview: buildResponsePreview(response?.content),
					label: record?.action?.name || queryName || snapshot.transitionType || "state",
					transitionType: snapshot.transitionType || "continue",
					current: snapshot.stateId === this.state?.stateId
				});
			});
			const nodeIds = new Set(nodes.map((node) => node.id));
			const edges = snapshots.filter((snapshot) => snapshot.parentStateId).filter((snapshot) => nodeIds.has(snapshot.stateId) && nodeIds.has(snapshot.parentStateId)).map((snapshot) => makeTraceGraphEdge({
				id: `${snapshot.parentStateId}->${snapshot.stateId}`,
				from_id: snapshot.parentStateId,
				to_id: snapshot.stateId,
				edge_type: snapshot.transitionType || "continue",
				branchId: snapshot.branchId || "main",
				label: snapshot.transitionType || "continue",
				timestamp: snapshot.createdAt || snapshot.state?.createdAt || null
			}));
			return makeTraceGraph({
				current_state_id: this.state?.stateId || null,
				current_branch_id: this.currentBranchId,
				sinceStateId,
				actors: allowedActors,
				branches: this.listBranches(),
				nodes,
				edges
			});
		}
		readTrace(limit = 50) {
			return this.interactionTrace.slice(-limit);
		}
		readTraceWindow({ limit = 50, sinceStateId = null, actors = [] } = {}) {
			const fullTrace = Array.isArray(this.interactionTrace) ? this.interactionTrace : [];
			const scopedTrace = sinceStateId ? (() => {
				const idx = fullTrace.findIndex((record) => record?.stateId === sinceStateId);
				return idx >= 0 ? fullTrace.slice(idx + 1) : fullTrace;
			})() : fullTrace;
			const allowedActors = normalizeActorFilter(actors);
			const actorScopedTrace = allowedActors.length > 0 ? scopedTrace.filter((record) => allowedActors.includes(record?.actor || "system")) : scopedTrace;
			if (!Number.isFinite(limit) || limit <= 0) return [...actorScopedTrace];
			return actorScopedTrace.slice(-limit);
		}
		appendTrace(record) {
			this.interactionTrace.push(makeInteractionTraceRecord(record));
			if (this.interactionTrace.length > this.maxTraceRetention) this.interactionTrace = this.interactionTrace.slice(-this.maxTraceRetention);
			this.version += 1;
			this.emitChange();
		}
		listResponses(limit = 20, options = {}) {
			const workspaceId = options.workspaceId || this.workspaceId || null;
			return (workspaceId ? this.responseHistory.filter((item) => item?.workspaceId === workspaceId) : this.responseHistory).slice(-limit).map((item) => clone$20(item));
		}
		readLatestResponse(options = {}) {
			return this.listResponses(1, options)[0] || null;
		}
		appendResponse(record) {
			this.responseHistory.push(makeAgentResponseRecord({
				workspaceId: this.workspaceId || null,
				branchId: this.currentBranchId || null,
				...record
			}));
			if (this.responseHistory.length > this.maxResponseRetention) this.responseHistory = this.responseHistory.slice(-this.maxResponseRetention);
			this.version += 1;
			this.emitChange();
		}
	};
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/verification/actionEffectVerification.js
	function isNonBlockingPropagationResult(entry = {}) {
		return entry?.linkKind === "usesData" || entry?.reason === "manual_activation_policy";
	}
	function evaluateActionEffectVerification({ store, getFinalWorkspaceSnapshot, evaluateLinkPropagation, actionName = null, stateId = null, refs = [], targetRef = null, traceLimit = 50 } = {}) {
		const trace = readInteractionTraceFromStore(store, { limit: traceLimit });
		const expectedRefs = Array.isArray(refs) ? refs : [];
		const matchingRecord = [...trace].reverse().find((record) => {
			if (stateId && record?.stateId !== stateId) return false;
			if (actionName && record?.action?.name !== actionName) return false;
			return true;
		});
		const affectedRefs = Array.isArray(matchingRecord?.affectedRefs) ? matchingRecord.affectedRefs : [];
		const missingRefs = expectedRefs.filter((ref) => !affectedRefs.includes(ref));
		const matchedActionName = matchingRecord?.action?.name || actionName || null;
		const verificationPayload = buildActionVerificationPayload({
			descriptor: resolveActionDescriptorForVerification({
				store,
				actionName: matchedActionName,
				targetRef: matchingRecord?.action?.targetRef || targetRef || null,
				affectedRefs
			}),
			updatedRefs: expectedRefs.length > 0 ? expectedRefs : affectedRefs
		});
		const finalSnapshot = getFinalWorkspaceSnapshot({
			stateId,
			refs: expectedRefs.length > 0 ? expectedRefs : void 0,
			includeMeta: true
		}) || null;
		const fullFinalSnapshot = getFinalWorkspaceSnapshot({
			stateId,
			includeMeta: true
		}) || finalSnapshot;
		const linkPropagation = Object.keys(readSelectionRegistry(fullFinalSnapshot?.shared || {})).map((sourceRef) => evaluateLinkPropagation({
			sourceRef,
			state: fullFinalSnapshot
		}) || {
			ok: false,
			sourceRef,
			linkCount: 0,
			passedCount: 0,
			consistencyScore: 0,
			results: []
		});
		const propagationOk = linkPropagation.every((entry) => {
			const relevantResults = (Array.isArray(entry?.results) ? entry.results : []).filter((result) => !isNonBlockingPropagationResult(result));
			if (relevantResults.length === 0) return true;
			return relevantResults.every((result) => result?.passed !== false);
		});
		const snapshotEntry = stateId ? readSnapshotEntryFromStore(store, stateId) : readCurrentSnapshotMetaFromStore(store);
		const updatedState = stateId ? snapshotEntry?.state || null : readWorkspaceStateFromStore(store);
		const replayContext = stateId ? snapshotEntry?.replayContext || null : store?.replayContext || null;
		const statePatch = expectedRefs.length > 0 ? store?.stateManager?.buildStatePatch?.({
			state: updatedState,
			replayContext,
			refs: expectedRefs
		}) || {} : {};
		return makeActionVerificationResult({
			verified: !!matchingRecord && missingRefs.length === 0 && propagationOk,
			matchedStateId: matchingRecord?.stateId || null,
			matchedActionName,
			affectedRefs,
			missingRefs,
			expectedPostconditions: verificationPayload.expectedPostconditions,
			verificationHints: verificationPayload.verificationHints,
			traceEvidence: matchingRecord || null,
			statePatch,
			finalSnapshot: fullFinalSnapshot || finalSnapshot,
			linkPropagation,
			semanticVerification: null
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/PerceptionExecutor.js
	function widgetMatchesKind(widget, kind) {
		if (!widget || !kind) return Boolean(widget);
		if (widget.kind === kind) return true;
		return Array.isArray(widget.recognizedKinds) && widget.recognizedKinds.includes(kind);
	}
	function makePerceptionHandlerContextCapabilities(capabilities = {}) {
		return {
			workspaceRead: false,
			runtimeDataRead: false,
			widgetTargetResolution: false,
			dataHandleResolution: false,
			selectionScopedQueries: false,
			traceRecording: false,
			...capabilities
		};
	}
	function makePerceptionHandlerContextIntegrations(integrations = {}) {
		return {
			store: false,
			traceRecorder: false,
			...integrations
		};
	}
	function makePerceptionHandlerContextSummary(summary = {}) {
		return {
			methods: Array.isArray(summary?.methods) ? [...summary.methods] : [],
			capabilities: makePerceptionHandlerContextCapabilities(summary?.capabilities),
			integrations: makePerceptionHandlerContextIntegrations(summary?.integrations)
		};
	}
	var PerceptionHandlerContext = class {
		constructor({ store, descriptor, call, traceRecorder, hostBridge }) {
			this.store = store;
			this.descriptor = descriptor;
			this.call = call;
			this.traceRecorder = traceRecorder;
			this.hostBridge = hostBridge || createDefaultWidgetVAHostBridge();
			this.pendingQueryTrace = null;
		}
		static describeContract() {
			return makePerceptionHandlerContextSummary({
				methods: [
					"readDescription",
					"readCurrentState",
					"readSelectionRegistry",
					"readFocusState",
					"readHighlightState",
					"readQueryScope",
					"readCallParams",
					"readFocusedWidgetRef",
					"resolveTargetWidget",
					"requireTargetWidget",
					"resolveSelectionDataRef",
					"resolveDataRefForWidget",
					"resolveRowsForWidget",
					"recordQuery"
				],
				capabilities: makePerceptionHandlerContextCapabilities({
					workspaceRead: true,
					runtimeDataRead: true,
					widgetTargetResolution: true,
					dataHandleResolution: true,
					selectionScopedQueries: true,
					traceRecording: true
				}),
				integrations: makePerceptionHandlerContextIntegrations({
					store: true,
					traceRecorder: true
				})
			});
		}
		readDescription() {
			return readWorkspaceDescriptionFromStore(this.store);
		}
		readCurrentState(options = {}) {
			return readWorkspaceStateFromStore(this.store, options);
		}
		readSelectionRegistry() {
			const hostRegistry = this.hostBridge.readSelectionRegistry?.();
			if (hostRegistry && typeof hostRegistry === "object" && !Array.isArray(hostRegistry) && Object.keys(hostRegistry).length > 0) return hostRegistry;
			return readSelectionRegistry(this.readCurrentState()?.shared || {});
		}
		readFocusState() {
			const hostFocusState = this.hostBridge.readFocusState?.();
			if (hostFocusState && typeof hostFocusState === "object" && !Array.isArray(hostFocusState)) return hostFocusState;
			const currentState = this.readCurrentState();
			return readFocusState(currentState?.shared || {}, currentState?.widgets || {});
		}
		readHighlightState() {
			const hostHighlightState = this.hostBridge.readHighlightState?.();
			if (hostHighlightState && typeof hostHighlightState === "object" && !Array.isArray(hostHighlightState)) return hostHighlightState;
			return deriveHighlightState(this.readCurrentState() || {});
		}
		readQueryScope() {
			return readNormalizedQueryScope({
				call: this.call,
				descriptor: this.descriptor
			});
		}
		readCallParams() {
			return this.normalizeScopedParams(this.call?.params || {});
		}
		readFocusedWidgetRef() {
			const hostFocusedWidgetRef = this.hostBridge.readFocusedWidgetRef?.();
			if (typeof hostFocusedWidgetRef === "string" && hostFocusedWidgetRef.length > 0) return hostFocusedWidgetRef;
			const hostFocusWidgetRef = this.readFocusState()?.widgetRef;
			if (typeof hostFocusWidgetRef === "string" && hostFocusWidgetRef.length > 0) return hostFocusWidgetRef;
			return this.readCurrentState()?.shared?.focusedWidget || null;
		}
		readPrimarySelectionRef() {
			const hostPrimarySelectionRef = this.hostBridge.readPrimarySelectionRef?.();
			if (typeof hostPrimarySelectionRef === "string" && hostPrimarySelectionRef.length > 0) return hostPrimarySelectionRef;
			return this.readCurrentState()?.shared?.selections?.views?.primary?.selectionRef || null;
		}
		resolveImplicitSelectionRefForWidget(targetWidget, queryScope = null) {
			return resolveImplicitSelectionRefForWidget({
				targetWidget,
				explicitSelectionRef: queryScope?.selectionRef || null,
				selectionRegistry: this.readSelectionRegistry() || {},
				primarySelectionRef: this.readPrimarySelectionRef()
			});
		}
		resolveTargetWidget({ kind, targetRef } = {}) {
			const resolvedRef = targetRef || this.readQueryScope()?.widgetRef || null;
			if (resolvedRef) {
				const explicitTarget = resolveWidgetRecordFromStore(this.store, resolvedRef);
				if (explicitTarget && widgetMatchesKind(explicitTarget, kind)) return explicitTarget;
				return null;
			}
			const focusedWidgetRef = this.readFocusedWidgetRef();
			if (focusedWidgetRef) {
				const focusedWidget = resolveWidgetRecordFromStore(this.store, focusedWidgetRef);
				if (focusedWidget && widgetMatchesKind(focusedWidget, kind)) return focusedWidget;
			}
			const widgets = listStoreWidgets(this.store);
			if (kind) {
				const match = widgets.find((widget) => widgetMatchesKind(widget, kind)) || null;
				return match ? resolveWidgetRecordFromStore(this.store, match.ref) || match : null;
			}
			const firstWidget = widgets[0] || null;
			return firstWidget ? resolveWidgetRecordFromStore(this.store, firstWidget.ref) || firstWidget : null;
		}
		requireTargetWidget({ kind = null, targetRef = null, message = null } = {}) {
			const targetWidget = this.resolveTargetWidget({
				kind,
				targetRef
			});
			if (targetWidget) return targetWidget;
			if (message) throw new Error(message);
			if (kind) throw new Error(`No active ${kind} widget in the current workspace.`);
			throw new Error("No active widget/spec in the current workspace.");
		}
		resolveSelectionDataRef(selectionRef) {
			if (!selectionRef) return null;
			const resolvedDataRef = resolveSelectionDataRefFromStore(this.store, selectionRef);
			if (!resolvedDataRef) return null;
			if (readDataHandleFromStore(this.store, resolvedDataRef) || readRuntimeDataFromStore(this.store, resolvedDataRef)) return resolvedDataRef;
			return null;
		}
		resolveCompositePrimarySelectionDataRef(targetWidget = null) {
			if (targetWidget?.kind !== "custom") return null;
			const currentState = this.readCurrentState() || {};
			const selectionRegistry = this.readSelectionRegistry() || {};
			const primarySelectionRef = this.readPrimarySelectionRef();
			if (primarySelectionRef) {
				const dataRef = this.resolveSelectionDataRef(primarySelectionRef);
				if (dataRef) return dataRef;
			}
			const resolveWidgetDataRefForSelection = (selectionRef) => {
				if (!selectionRef) return null;
				const sourceWidget = Object.values(currentState?.widgets || {}).find((widget) => widget?.selections?.[selectionRef]) || null;
				return sourceWidget?.data?.currentDataRef || sourceWidget?.data?.sourceDataRef || sourceWidget?.primaryDataRef || null;
			};
			if (primarySelectionRef) {
				const sourceWidgetDataRef = resolveWidgetDataRefForSelection(primarySelectionRef);
				if (sourceWidgetDataRef) return sourceWidgetDataRef;
			}
			const focusedWidgetRef = this.readFocusedWidgetRef();
			const focusedWidget = focusedWidgetRef ? resolveWidgetRecordFromStore(this.store, focusedWidgetRef) : null;
			if (focusedWidget) {
				const implicitFocusedSelectionRef = resolveImplicitSelectionRefForWidget({
					targetWidget: focusedWidget,
					selectionRegistry,
					primarySelectionRef
				});
				const dataRef = this.resolveSelectionDataRef(implicitFocusedSelectionRef);
				if (dataRef) return dataRef;
				const sourceWidgetDataRef = resolveWidgetDataRefForSelection(implicitFocusedSelectionRef);
				if (sourceWidgetDataRef) return sourceWidgetDataRef;
				const focusedWidgetDataRef = focusedWidget?.data?.currentDataRef || focusedWidget?.data?.sourceDataRef || focusedWidget?.primaryDataRef || null;
				if (focusedWidgetDataRef) return focusedWidgetDataRef;
			}
			const selectionRefs = Object.keys(selectionRegistry).filter(Boolean);
			if (selectionRefs.length === 1) return this.resolveSelectionDataRef(selectionRefs[0]) || resolveWidgetDataRefForSelection(selectionRefs[0]);
			return null;
		}
		normalizeScopedParams(params = {}, options = {}) {
			return buildScopedParams({
				params,
				call: options?.call || this.call,
				descriptor: options?.descriptor || this.descriptor,
				querySpec: options?.querySpec || null
			});
		}
		resolveDataRefForWidget(targetWidget, params = {}) {
			const queryScope = this.normalizeScopedParams(params)?.queryScope || null;
			const explicitDataRef = queryScope?.dataRef || null;
			if (typeof explicitDataRef === "string" && explicitDataRef.length > 0) return explicitDataRef;
			const callTargetRef = this.readQueryScope()?.widgetRef || null;
			const implicitSelectionRef = this.resolveImplicitSelectionRefForWidget(targetWidget, queryScope);
			const selectionScopedDataRef = this.resolveSelectionDataRef(implicitSelectionRef || callTargetRef);
			if (selectionScopedDataRef) return selectionScopedDataRef;
			const compositePrimarySelectionDataRef = this.resolveCompositePrimarySelectionDataRef(targetWidget);
			if (compositePrimarySelectionDataRef) return compositePrimarySelectionDataRef;
			if (typeof callTargetRef === "string" && (readDataHandleFromStore(this.store, callTargetRef) || readRuntimeDataFromStore(this.store, callTargetRef))) return callTargetRef;
			return targetWidget?.data?.currentDataRef || targetWidget?.data?.sourceDataRef || targetWidget?.primaryDataRef || null;
		}
		resolveRowsForWidget(targetWidget, params = {}) {
			const scopedParams = this.normalizeScopedParams(params);
			const queryScope = scopedParams?.queryScope || null;
			const targetDataRef = this.resolveDataRefForWidget(targetWidget, scopedParams);
			const runtimeData = readRuntimeDataFromStore(this.store, targetDataRef);
			let rows = Array.isArray(runtimeData?.rows) ? runtimeData.rows : [];
			const selectionRef = this.resolveImplicitSelectionRefForWidget(targetWidget, queryScope);
			if (selectionRef) {
				const selectionState = this.readSelectionRegistry()?.[selectionRef] || null;
				const baseDataRef = targetWidget?.data?.currentDataRef || targetWidget?.data?.sourceDataRef || targetWidget?.primaryDataRef || null;
				const baseRuntimeData = readRuntimeDataFromStore(this.store, baseDataRef);
				const baseRows = Array.isArray(baseRuntimeData?.rows) ? baseRuntimeData.rows : [];
				const predicates = Array.isArray(selectionState?.predicates) ? selectionState.predicates : [];
				if ((!runtimeData || targetDataRef === baseDataRef) && predicates.length > 0) rows = baseRows.filter((row) => predicates.every((predicate) => applyPredicate(row, predicate)));
			}
			return {
				dataRef: targetDataRef,
				rows
			};
		}
		recordQuery({ affectedRefs = [], notes } = {}) {
			this.pendingQueryTrace = {
				call: this.call,
				affectedRefs,
				notes
			};
		}
		consumeRecordedQuery() {
			const pending = this.pendingQueryTrace;
			this.pendingQueryTrace = null;
			return pending;
		}
	};
	function applyPredicate(row, predicate) {
		const value = row?.[predicate?.field];
		if (predicate?.op === "between" && Array.isArray(predicate?.value)) return value >= predicate.value[0] && value <= predicate.value[1];
		if (predicate?.op === "equals" || predicate?.op === "eq") return value === predicate?.value;
		if (predicate?.op === "in" && Array.isArray(predicate?.value)) return predicate.value.includes(value);
		if (predicate?.op === "notIn" && Array.isArray(predicate?.value)) return !predicate.value.includes(value);
		return true;
	}
	function makePerceptionRegistryCounts(counts = {}) {
		return {
			descriptorCount: 0,
			handlerEntryCount: 0,
			...counts
		};
	}
	function makePerceptionRegistryCapabilities(capabilities = {}) {
		return {
			paramsValidation: true,
			returnsValidation: true,
			traceRecording: false,
			linkPropagationEvidence: false,
			...capabilities
		};
	}
	function makePerceptionRegistryQueryEntry(entry = {}) {
		return {
			name: "",
			category: null,
			targetRef: null,
			handlerVariantCount: 0,
			sideEffectFree: true,
			evidenceKinds: [],
			verificationTargets: [],
			supportedWidgetKinds: null,
			...entry
		};
	}
	function makePerceptionRegistrySummary(summary = {}) {
		return {
			queries: [],
			...summary,
			counts: makePerceptionRegistryCounts(summary?.counts),
			capabilities: makePerceptionRegistryCapabilities(summary?.capabilities),
			queries: Array.isArray(summary?.queries) ? summary.queries.map((query) => makePerceptionRegistryQueryEntry(query)) : []
		};
	}
	function buildQueryError({ call, code, message, details, recoveryHints }) {
		return makePerceptionResult({
			ok: false,
			callId: call?.callId || `query_${Date.now()}`,
			queryName: call?.name || "unknown",
			error: makeResultError({
				code,
				message,
				...details !== void 0 ? { details } : {}
			}),
			...Array.isArray(recoveryHints) && recoveryHints.length > 0 ? { recoveryHints } : {}
		});
	}
	function resolveCallTargetRef(call, descriptor) {
		return readNormalizedQueryScope({
			call,
			descriptor
		}).widgetRef || null;
	}
	function resolveWidgetSelections(targetWidget) {
		return Object.entries(targetWidget?.selections || {}).map(([selectionRef, selectionState]) => ({
			ref: selectionRef,
			selection: selectionState
		})).filter((entry) => entry.ref && entry.selection);
	}
	function resolveRequestedSelectionEntries(targetWidget, requestedSelectionRef = null) {
		const selectionEntries = resolveWidgetSelections(targetWidget);
		if (!requestedSelectionRef) return selectionEntries;
		return selectionEntries.filter((entry) => entry.ref === requestedSelectionRef);
	}
	function formatSummaryText({ rowCount = 0, groupCount = 0, fallback = "" } = {}) {
		if (typeof fallback === "string" && fallback.trim().length > 0) return fallback.trim();
		if (groupCount > 0) return `${groupCount} grouped summaries over ${rowCount} rows`;
		return `${rowCount} rows`;
	}
	function cloneValue(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function hasEncodingChannels(encoding = null) {
		return Boolean(encoding && typeof encoding === "object" && !Array.isArray(encoding) && Object.keys(encoding).length > 0);
	}
	function collectSpecEncodings(spec = null, path = "spec", entries = []) {
		if (!spec || typeof spec !== "object") return entries;
		if (hasEncodingChannels(spec.encoding)) entries.push({
			path,
			channels: Object.keys(spec.encoding),
			encoding: cloneValue(spec.encoding)
		});
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) {
			if (!Array.isArray(spec[key])) continue;
			spec[key].forEach((child, index) => {
				collectSpecEncodings(child, `${path}.${key}[${index}]`, entries);
			});
		}
		if (spec.spec && typeof spec.spec === "object") collectSpecEncodings(spec.spec, `${path}.spec`, entries);
		return entries;
	}
	function resolveViewConfigEncodings(targetWidget = null) {
		if (hasEncodingChannels(targetWidget?.encodings)) return cloneValue(targetWidget.encodings);
		const entries = collectSpecEncodings(targetWidget?.currentSpec || targetWidget?.rawSpec || targetWidget?.spec || null);
		if (entries.length === 0) return cloneValue(targetWidget?.encodings || {});
		if (entries.length === 1) return entries[0].encoding;
		return {
			representative: entries[0].encoding,
			views: entries
		};
	}
	function readRootEncoding(spec) {
		if (Array.isArray(spec?.layer) && spec.layer.length > 0) return spec.layer[0]?.encoding || spec.encoding || {};
		return spec?.encoding || {};
	}
	function isParallelFoldSpec(spec = null) {
		if (!(Array.isArray(spec?.transform) ? spec.transform : []).some((transform) => Array.isArray(transform?.fold) && transform.fold.length > 0)) return false;
		const encoding = readRootEncoding(spec);
		return [
			"dimension",
			"key",
			"variable"
		].includes(encoding?.x?.field);
	}
	function countSemanticSelectionRows({ widgetKind = null, spec = null, rows = [] } = {}) {
		const safeRows = Array.isArray(rows) ? rows : [];
		if (widgetKind !== "parallelCoordinates" && !isParallelFoldSpec(spec)) return safeRows.length;
		const encoding = readRootEncoding(spec);
		const recordField = typeof encoding?.detail?.field === "string" && encoding.detail.field.length > 0 ? encoding.detail.field : null;
		if (!recordField) return safeRows.length;
		return new Set(safeRows.map((row) => row?.[recordField]).filter((value) => value != null).map((value) => JSON.stringify(value))).size;
	}
	function normalizeSemanticVisibleRows({ widgetKind = null, spec = null, rows = [] } = {}) {
		const safeRows = Array.isArray(rows) ? rows : [];
		if (widgetKind !== "parallelCoordinates" && !isParallelFoldSpec(spec)) return {
			visibleRows: safeRows,
			visibleCount: safeRows.length
		};
		const encoding = readRootEncoding(spec);
		const recordField = typeof encoding?.detail?.field === "string" && encoding.detail.field.length > 0 ? encoding.detail.field : null;
		if (!recordField) return {
			visibleRows: safeRows,
			visibleCount: safeRows.length
		};
		const dedupedRows = [];
		const seen = /* @__PURE__ */ new Set();
		for (const row of safeRows) {
			const recordValue = row?.[recordField];
			if (recordValue == null) continue;
			const key = JSON.stringify(recordValue);
			if (seen.has(key)) continue;
			seen.add(key);
			dedupedRows.push(row);
		}
		return {
			visibleRows: dedupedRows,
			visibleCount: countSemanticSelectionRows({
				widgetKind,
				spec,
				rows: safeRows
			})
		};
	}
	function buildPerceptionTraceNotes({ userVisibleSummary, rationale = null, verification = null } = {}) {
		return {
			...typeof userVisibleSummary === "string" && userVisibleSummary.trim().length > 0 ? { userVisibleSummary: userVisibleSummary.trim() } : {},
			...typeof rationale === "string" && rationale.trim().length > 0 ? { rationale: rationale.trim() } : {},
			...typeof verification === "string" && verification.trim().length > 0 ? { verification: verification.trim() } : {}
		};
	}
	function summarizeSupportedWidgetKinds(entries = []) {
		if (!Array.isArray(entries) || entries.length === 0) return null;
		const collectedKinds = Array.from(new Set(entries.flatMap((entry) => Array.isArray(entry?.supportedWidgetKinds) ? entry.supportedWidgetKinds : []).filter((kind) => typeof kind === "string" && kind.length > 0)));
		return collectedKinds.length > 0 ? collectedKinds : null;
	}
	function buildHandlerInput(call, descriptor) {
		const params = buildScopedParams({
			params: call?.params || {},
			call,
			descriptor
		});
		const queryScope = readScopedQueryScope({
			call,
			descriptor
		});
		const scopedParams = {
			...params,
			...hasScopedQueryScopeValues(queryScope) && !params.queryScope ? { queryScope } : {}
		};
		const { targetRef: _legacyTargetRef, dataRef: _legacyDataRef, params: _legacyParams, queryScope: _legacyQueryScope, ...rawCall } = call && typeof call === "object" ? call : {};
		const input = {
			...rawCall,
			...scopedParams,
			params: scopedParams,
			queryScope: scopedParams.queryScope || null
		};
		delete input.targetRef;
		delete input.dataRef;
		return input;
	}
	function buildPerceptionCallSchemaInput(call) {
		const { targetRef: _legacyTargetRef, dataRef: _legacyDataRef, queryScope: _legacyQueryScope, ...rawCall } = call && typeof call === "object" ? call : {};
		const queryScope = readScopedQueryScope({ call });
		return {
			...rawCall,
			...hasScopedQueryScopeValues(queryScope) ? { queryScope } : {}
		};
	}
	var PerceptionExecutor = class {
		constructor({ store, dataQueryEngine, dataQueryExecutor, traceRecorder, coordinationEngine, hostBridge } = {}) {
			this.store = store || {
				listPerceptionQueries() {
					return [];
				},
				getPerceptionDescriptor() {
					return null;
				},
				listWidgetDescriptions() {
					return [];
				},
				getResolvedWidgetForTarget() {
					return null;
				},
				getResolvedWidget() {
					return null;
				},
				getWidgetDescription() {
					return null;
				},
				readState() {
					return {};
				}
			};
			this.dataQueryEngine = dataQueryEngine || null;
			this.traceRecorder = traceRecorder || null;
			this.hostBridge = hostBridge || createDefaultWidgetVAHostBridge();
			this.dataQueryExecutor = dataQueryExecutor || (this.dataQueryEngine ? new DataQueryExecutor({
				store: this.store,
				dataQueryEngine: this.dataQueryEngine,
				traceRecorder: this.traceRecorder,
				hostBridge: this.hostBridge
			}) : null);
			this.coordinationEngine = coordinationEngine || null;
			this.builtinDescriptors = /* @__PURE__ */ new Map();
			this.handlerEntries = /* @__PURE__ */ new Map();
			this.registerBuiltinQueries();
		}
		register(descriptor, handler, options = {}) {
			if (!descriptor?.name || typeof handler !== "function") throw new Error("PerceptionExecutor.register requires a descriptor.name and handler.");
			const supportedWidgetKinds = normalizeSupportedWidgetKinds(options.supportedWidgetKinds || descriptor.supportedWidgetKinds || null);
			const entries = this.handlerEntries.get(descriptor.name) || [];
			if (entries.some((entry) => sameSupportedKinds(entry.supportedWidgetKinds, supportedWidgetKinds))) throw new Error(`Duplicate perception query registration: ${descriptor.name}`);
			this.builtinDescriptors.set(descriptor.name, descriptor);
			this.handlerEntries.set(descriptor.name, [...entries, {
				descriptor,
				handler,
				supportedWidgetKinds
			}]);
		}
		list() {
			const descriptors = this.store.listPerceptionQueries();
			if (descriptors.length > 0) return descriptors.filter((descriptor) => this.handlerEntries.has(descriptor.name));
			return Array.from(this.builtinDescriptors.values());
		}
		has(name, options = {}) {
			const entries = this.handlerEntries.get(name) || [];
			const supportedWidgetKinds = normalizeSupportedWidgetKinds(options.supportedWidgetKinds || null);
			if (!supportedWidgetKinds) return entries.length > 0;
			return entries.some((entry) => sameSupportedKinds(entry.supportedWidgetKinds, supportedWidgetKinds));
		}
		describeRegistry() {
			const queries = this.list().map((descriptor) => {
				const handlerEntries = this.handlerEntries.get(descriptor.name) || [];
				return makePerceptionRegistryQueryEntry({
					name: descriptor.name,
					category: descriptor.category || null,
					targetRef: descriptor.targetRef || null,
					sideEffectFree: descriptor.sideEffectFree !== false,
					evidenceKinds: Array.isArray(descriptor.evidenceKinds) ? [...descriptor.evidenceKinds] : [],
					verificationTargets: Array.isArray(descriptor.verificationTargets) ? [...descriptor.verificationTargets] : [],
					supportedWidgetKinds: summarizeSupportedWidgetKinds(handlerEntries),
					handlerVariantCount: handlerEntries.length
				});
			});
			let handlerEntryCount = 0;
			for (const entries of this.handlerEntries.values()) handlerEntryCount += Array.isArray(entries) ? entries.length : 0;
			return makePerceptionRegistrySummary({
				counts: makePerceptionRegistryCounts({
					descriptorCount: queries.length,
					handlerEntryCount
				}),
				capabilities: makePerceptionRegistryCapabilities({
					paramsValidation: true,
					returnsValidation: true,
					traceRecording: typeof this.traceRecorder?.recordQuery === "function",
					linkPropagationEvidence: Boolean(this.coordinationEngine)
				}),
				queries
			});
		}
		describeContext() {
			return PerceptionHandlerContext.describeContract();
		}
		resolveDescriptor(call) {
			return this.store.getPerceptionDescriptor(call?.name, resolveCallTargetRef(call)) || this.builtinDescriptors.get(call?.name) || null;
		}
		createContext(call, descriptor) {
			const normalizedCall = buildHandlerInput(call, descriptor);
			return new PerceptionHandlerContext({
				store: this.store,
				descriptor,
				call: normalizedCall,
				traceRecorder: this.traceRecorder,
				hostBridge: this.hostBridge
			});
		}
		resolveHandlerEntry(call, descriptor, targetWidget) {
			const entries = this.handlerEntries.get(call?.name) || [];
			if (entries.length === 0) return null;
			const widgetKind = targetWidget?.kind || null;
			const targetRef = resolveCallTargetRef(call, descriptor);
			if (widgetKind) {
				const exactKindEntry = entries.find((entry) => entry.supportedWidgetKinds?.includes(widgetKind));
				if (exactKindEntry) return exactKindEntry;
			}
			if (targetRef) {
				const scopedEntry = entries.find((entry) => entry.descriptor?.targetRef === targetRef);
				if (scopedEntry) return scopedEntry;
			}
			return entries.find((entry) => !entry.supportedWidgetKinds || entry.supportedWidgetKinds.length === 0) || entries[0];
		}
		async query(call) {
			try {
				validateAgainstSchema(buildPerceptionCallSchemaInput(call), PERCEPTION_QUERY_CALL_SCHEMA, "query");
			} catch (error) {
				this.traceRecorder?.recordQueryFailure?.({
					call,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the perception query call schema in describeWorkspace() before retrying."]
				});
				return buildQueryError({
					call,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the perception query call schema in describeWorkspace() before retrying."]
				});
			}
			const descriptor = this.resolveDescriptor(call);
			const ctx = this.createContext(call, descriptor);
			const targetWidget = ctx.resolveTargetWidget();
			const handlerEntry = this.resolveHandlerEntry(call, descriptor, targetWidget);
			if (!handlerEntry?.handler) {
				this.traceRecorder?.recordQueryFailure?.({
					call,
					code: "UNKNOWN_QUERY",
					message: `Unsupported WidgetVA perception query: ${call?.name || "unknown"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect currently supported perception queries."]
				});
				return buildQueryError({
					call,
					code: "UNKNOWN_QUERY",
					message: `Unsupported WidgetVA perception query: ${call?.name || "unknown"}.`,
					recoveryHints: ["Call describeWorkspace() to inspect currently supported perception queries."]
				});
			}
			if (descriptor?.paramsSchema) try {
				validateAgainstSchema(ctx.readCallParams(), descriptor.paramsSchema, "params");
			} catch (error) {
				this.traceRecorder?.recordQueryFailure?.({
					call,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the query paramsSchema in describeWorkspace() before retrying."]
				});
				return buildQueryError({
					call,
					code: "INVALID_PARAMS",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Inspect the query paramsSchema in describeWorkspace() before retrying."]
				});
			}
			try {
				const output = await handlerEntry.handler(buildHandlerInput(call, descriptor), ctx);
				if (descriptor?.returnsSchema) validateAgainstSchema(output?.result, descriptor.returnsSchema, "result");
				const tracePayload = ctx.consumeRecordedQuery?.() || null;
				if (tracePayload) this.traceRecorder?.recordQuery?.(tracePayload);
				return makePerceptionResult({
					ok: true,
					callId: call?.callId || `call_${Date.now()}`,
					queryName: call?.name || "unknown",
					result: output?.result
				});
			} catch (error) {
				this.traceRecorder?.recordQueryFailure?.({
					call,
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: ["Call readState() or describeWorkspace() to verify the current target widget and data refs.", "Retry with a focused widget/query combination that is declared in the current workspace."]
				});
				return buildQueryError({
					call,
					code: "RUNTIME_ERROR",
					message: error instanceof Error ? error.message : String(error),
					recoveryHints: [
						"Call readState() or describeWorkspace() to verify the current target widget and data refs.",
						"Retry with a focused widget/query combination that is declared in the current workspace.",
						"Inspect the query returnsSchema if the runtime result shape does not match the declared contract."
					]
				});
			}
		}
		async run(call) {
			return this.query(call);
		}
		registerBuiltinQueries() {
			this.register({ name: "perception.inspectViewConfig" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: `Inspected the ${targetWidget.kind || "widget"} view configuration.`,
						rationale: "View configuration evidence was requested for the current target widget."
					})
				});
				return { result: {
					ref: targetWidget.ref,
					kind: targetWidget.kind,
					encodings: resolveViewConfigEncodings(targetWidget),
					transforms: targetWidget.transforms,
					view: targetWidget.view,
					selections: targetWidget.selections,
					feedback: targetWidget.feedback || null
				} };
			});
			this.register({ name: "perception.inspectSelection" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const requestedSelectionRef = params?.queryScope?.selectionRef || null;
				const selectionEntries = resolveRequestedSelectionEntries(targetWidget, requestedSelectionRef);
				if (requestedSelectionRef && selectionEntries.length === 0) throw new Error(`Selection ref ${requestedSelectionRef} is not active on the target widget.`);
				const selections = selectionEntries.map((entry) => entry.selection);
				const { rows: visibleRows, dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				const currentSpec = targetWidget?.currentSpec || targetWidget?.rawSpec || null;
				const filteredRows = selections.length > 0 ? selections.reduce((rows, activeSelection) => this.dataQueryEngine.filter(rows, activeSelection.predicates || []), visibleRows) : [];
				const selectedCount = selections.length > 0 ? countSemanticSelectionRows({
					widgetKind: targetWidget?.kind || null,
					spec: currentSpec,
					rows: filteredRows
				}) : 0;
				const result = {
					hasSelection: selectionEntries.length > 0,
					selectionCount: selectionEntries.length,
					selectionRefs: selectionEntries.map((entry) => entry.ref),
					dataRef,
					selectedCount,
					predicates: selectionEntries.flatMap((entry) => entry?.selection?.predicates || []),
					selectionSummaries: selectionEntries.map((entry) => entry?.selection?.summary || "").filter((summary) => typeof summary === "string" && summary.trim().length > 0),
					selections: selectionEntries.map((entry) => ({
						ref: entry.ref,
						kind: entry?.selection?.kind || null,
						summary: entry?.selection?.summary || "",
						predicates: cloneValue(entry?.selection?.predicates || []),
						value: cloneValue(entry?.selection?.value ?? null)
					}))
				};
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: result.hasSelection ? `Inspected ${result.selectionCount} active selection${result.selectionCount === 1 ? "" : "s"} covering ${result.selectedCount} row${result.selectedCount === 1 ? "" : "s"}.` : "Confirmed that there is no active selection on the target widget.",
						rationale: "Selection payload evidence was requested for the target widget."
					})
				});
				return { result };
			});
			this.register({ name: "perception.summarizeSelection" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const requestedSelectionRef = params?.queryScope?.selectionRef || null;
				const selectionEntries = resolveRequestedSelectionEntries(targetWidget, requestedSelectionRef);
				if (requestedSelectionRef && selectionEntries.length === 0) throw new Error(`Selection ref ${requestedSelectionRef} is not active on the target widget.`);
				const selections = selectionEntries.map((entry) => entry.selection);
				const { rows: visibleRows, dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				const currentSpec = targetWidget?.currentSpec || targetWidget?.rawSpec || null;
				let result;
				if (selections.length > 0) {
					const filteredRows = selections.reduce((rows, activeSelection) => this.dataQueryEngine.filter(rows, activeSelection.predicates || []), visibleRows);
					const semanticSelectedCount = countSemanticSelectionRows({
						widgetKind: targetWidget?.kind || null,
						spec: currentSpec,
						rows: filteredRows
					});
					const summary = this.dataQueryEngine.summarize(filteredRows, params);
					const summaryRows = Array.isArray(summary?.rows) ? summary.rows : [];
					const selectionSummaries = selections.map((selection) => selection.summary || "").filter(Boolean);
					result = {
						hasSelection: true,
						selectionCount: selections.length,
						selectionRefs: selectionEntries.map((entry) => entry.ref),
						dataRef,
						selectedCount: semanticSelectedCount,
						summary: formatSummaryText({
							rowCount: semanticSelectedCount,
							groupCount: summaryRows.length,
							fallback: selectionSummaries.join(" | ")
						}),
						selectionSummaries,
						predicates: selections.flatMap((selection) => selection.predicates || []),
						selectionPredicates: selections.map((selection) => selection.predicates || []),
						groups: summaryRows,
						aggregates: summaryRows
					};
				} else result = {
					hasSelection: false,
					selectionCount: 0,
					selectionRefs: [],
					dataRef,
					selectedCount: 0,
					summary: "",
					selectionSummaries: [],
					groups: [],
					aggregates: [],
					predicates: [],
					selectionPredicates: []
				};
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: result.hasSelection ? `Summarized ${result.selectedCount} selected rows across ${result.selectionCount} active selection${result.selectionCount === 1 ? "" : "s"}.` : "Confirmed that there is no active selection on the target widget.",
						rationale: "Selection evidence was requested for the target widget."
					})
				});
				return { result };
			});
			this.register({ name: "perception.summarizeVisible" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const { rows: resolvedRows, dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				const activeSpec = (ctx.readCurrentState()?.widgets?.[targetWidget.ref] || null)?.rawSpec || null;
				const selectionRef = ctx.resolveImplicitSelectionRefForWidget?.(targetWidget, params?.queryScope || null) || ctx.readPrimarySelectionRef?.() || null;
				const selectionState = selectionRef ? ctx.readSelectionRegistry?.()?.[selectionRef] || null : null;
				const selectionPredicates = Array.isArray(selectionState?.predicates) ? selectionState.predicates : [];
				const rawVisibleRows = selectionPredicates.length > 0 ? this.dataQueryEngine.filter(resolvedRows, selectionPredicates) : resolvedRows;
				const { visibleRows, visibleCount } = normalizeSemanticVisibleRows({
					widgetKind: targetWidget?.kind || null,
					spec: activeSpec,
					rows: rawVisibleRows
				});
				const summary = this.dataQueryEngine.summarize(visibleRows, params);
				const summaryRows = Array.isArray(summary?.rows) ? summary.rows : [];
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: `Summarized ${visibleCount} visible rows for the target widget.`,
						rationale: "Visible-view evidence was requested for the target widget."
					})
				});
				return { result: {
					dataRef,
					rowCount: visibleCount,
					groups: summaryRows,
					aggregates: summaryRows,
					summary: formatSummaryText({
						rowCount: visibleCount,
						groupCount: summaryRows.length
					})
				} };
			});
			this.register({ name: "perception.findExtremes" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const { dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				const rows = this.dataQueryExecutor.run({
					dataRef,
					query: {
						kind: "findExtremes",
						spec: params
					}
				})?.result?.rows || [];
				const direction = typeof params?.direction === "string" ? params.direction : "max";
				const field = typeof params?.field === "string" ? params.field : "value";
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: `Found ${rows.length} ${direction} extreme row${rows.length === 1 ? "" : "s"} for ${field}.`,
						rationale: "An extremes query was requested for the current target widget."
					})
				});
				return { result: {
					dataRef,
					field: typeof params?.field === "string" ? params.field : null,
					direction,
					rows
				} };
			});
			this.register({ name: "perception.inspectVisibleRows" }, async (call, ctx) => {
				const targetWidget = ctx.requireTargetWidget();
				const params = ctx.readCallParams();
				const { rows: resolvedRows, dataRef } = ctx.resolveRowsForWidget(targetWidget, params);
				const activeSpec = (ctx.readCurrentState()?.widgets?.[targetWidget.ref] || null)?.rawSpec || null;
				const selectionRef = ctx.resolveImplicitSelectionRefForWidget?.(targetWidget, params?.queryScope || null) || ctx.readPrimarySelectionRef?.() || null;
				const selectionState = selectionRef ? ctx.readSelectionRegistry?.()?.[selectionRef] || null : null;
				const selectionPredicates = Array.isArray(selectionState?.predicates) ? selectionState.predicates : [];
				const rawVisibleRows = selectionPredicates.length > 0 ? this.dataQueryEngine.filter(resolvedRows, selectionPredicates) : resolvedRows;
				const { visibleRows, visibleCount } = normalizeSemanticVisibleRows({
					widgetKind: targetWidget?.kind || null,
					spec: activeSpec,
					rows: rawVisibleRows
				});
				const limit = Number.isFinite(params?.limit) ? params.limit : 20;
				ctx.recordQuery({
					affectedRefs: [targetWidget.ref],
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: `Inspected ${visibleCount} visible rows and returned a sample.`,
						rationale: "Visible-row evidence was requested for the target widget."
					})
				});
				return { result: {
					ref: targetWidget.ref,
					dataRef,
					visibleCount,
					rows: visibleRows.slice(0, limit)
				} };
			});
			this.register({ name: "perception.verifyActionEffect" }, async (call, ctx) => {
				const params = ctx.readCallParams();
				const verification = evaluateActionEffectVerification({
					store: this.store,
					getFinalWorkspaceSnapshot: (options = {}) => readSnapshotFromStore(this.store, options?.stateId || params?.stateId || null, options),
					evaluateLinkPropagation: (options) => this.coordinationEngine?.evaluatePropagation?.(options) || null,
					actionName: params?.actionName || null,
					actionParams: params?.actionParams || {},
					stateId: params?.stateId || null,
					refs: params?.refs || [],
					targetRef: call?.target?.widgetRef || null,
					traceLimit: 50
				});
				ctx.recordQuery({
					affectedRefs: Array.isArray(params?.refs) && params.refs.length > 0 ? params.refs : verification.affectedRefs,
					notes: buildPerceptionTraceNotes({
						userVisibleSummary: verification.verified ? `Verified the effect of ${verification.matchedActionName || "the action"} on the requested runtime refs.` : `Verification for ${verification.matchedActionName || "the action"} found missing refs or propagation mismatches.`,
						rationale: "Post-action verification evidence was requested from the runtime trace and state patch.",
						verification: verification.verified ? "Trace evidence, state patch, and link propagation checks all matched the requested action effect." : [
							`Missing refs: ${verification.missingRefs.length}.`,
							`Propagation ok: ${verification.linkPropagation.every((entry) => entry.ok)}.`,
							verification?.semanticVerification?.ok === false ? verification.semanticVerification.summary : null
						].filter(Boolean).join(" ")
					})
				});
				return { result: verification };
			});
		}
	};
	function normalizeSupportedWidgetKinds(value) {
		if (!Array.isArray(value) || value.length === 0) return null;
		return [...new Set(value.filter((item) => typeof item === "string" && item.length > 0))].sort();
	}
	function sameSupportedKinds(left, right) {
		const a = normalizeSupportedWidgetKinds(left);
		const b = normalizeSupportedWidgetKinds(right);
		if (!a && !b) return true;
		if (!a || !b || a.length !== b.length) return false;
		return a.every((value, index) => value === b[index]);
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/shared/widgetSemanticPatches.js
	function replaceActionTransform(transforms, actionName, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !(transform?.source === "action" && transform?.spec?.actionName === actionName));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function replaceActionHighlightTransformForField(transforms, field, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !(transform?.kind === "highlight" && transform?.source === "action:widget.highlightValues" && transform?.spec?.field === field));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function uniqueWidgetValues(values) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => value != null))];
	}
	function buildWidgetFilterByValuesPatch({ targetWidget, currentState, field, values, visibleCount }) {
		return buildWidgetFilterPatch({
			targetWidget,
			currentState,
			actionName: "widget.filterByValues",
			field,
			values,
			predicates: [{
				field,
				op: "in",
				value: values
			}],
			visibleCount
		});
	}
	function buildWidgetFilterByRangePatch({ targetWidget, currentState, field, range, visibleCount }) {
		return buildWidgetFilterPatch({
			targetWidget,
			currentState,
			actionName: "widget.filterByRange",
			field,
			range,
			predicates: [{
				field,
				op: "between",
				value: range
			}],
			visibleCount
		});
	}
	function buildWidgetAggregatePatch({ targetWidget, groupBy, measures }) {
		const actionName = "widget.aggregateData";
		return { [targetWidget.ref]: {
			transforms: replaceActionTransform(targetWidget?.transforms, actionName, makeTransformState({
				kind: "aggregate",
				source: "action",
				sourceWidgetId: targetWidget.widgetId || null,
				spec: {
					actionName,
					groupBy,
					measures
				}
			})),
			view: {
				...targetWidget?.view || {},
				aggregate: {
					sourceAction: actionName,
					groupBy,
					measures
				}
			}
		} };
	}
	function buildWidgetSortEncodingPatch({ targetWidget, channel, order, field, aggregate }) {
		return { [targetWidget.ref]: { view: {
			...targetWidget?.view || {},
			sort: {
				sourceAction: "widget.sortEncoding",
				channel,
				mode: "direction",
				order,
				...field ? { field } : {},
				...aggregate ? { aggregate } : {}
			}
		} } };
	}
	function buildWidgetChangeEncodingPatch({ targetWidget, channel, field, type, aggregate }) {
		return { [targetWidget.ref]: { encodings: {
			...targetWidget?.encodings || {},
			[channel]: {
				...targetWidget?.encodings?.[channel] || {},
				field,
				...type ? { type } : {},
				...aggregate ? { aggregate } : {}
			}
		} } };
	}
	function buildWidgetHighlightValuesPatch({ targetWidget, currentState, field, values, rows }) {
		const targetWidgetRef = targetWidget?.ref || null;
		const targetWidgetState = currentState?.widgets?.[targetWidgetRef] || targetWidget || {};
		const highlightedValues = uniqueWidgetValues(values);
		const highlightedCount = (Array.isArray(rows) ? rows : []).filter((row) => highlightedValues.includes(row?.[field])).length;
		const widgetPatch = {
			transforms: replaceActionHighlightTransformForField(targetWidgetState?.transforms, field, makeTransformState({
				kind: "highlight",
				source: "action:widget.highlightValues",
				spec: {
					field,
					values: highlightedValues
				}
			})),
			feedback: {
				...targetWidgetState?.feedback || {},
				highlightedKeys: highlightedValues
			}
		};
		const nextState = {
			...currentState,
			widgets: {
				...currentState?.widgets || {},
				[targetWidgetRef]: {
					...targetWidgetState || {},
					...widgetPatch
				}
			}
		};
		return {
			patch: {
				[targetWidgetRef]: widgetPatch,
				shared: withHighlightSubmodel(currentState?.shared || {}, deriveHighlightState(nextState))
			},
			highlightedCount
		};
	}
	function buildWidgetZoomDomainPatch({ targetWidget, xDomain, yDomain }) {
		return { [targetWidget.ref]: { view: {
			...targetWidget?.view || {},
			...xDomain ? { xDomain } : {},
			...yDomain ? { yDomain } : {},
			zoom: buildViewZoomState({
				widgetSpec: targetWidget.rawSpec || targetWidget.currentSpec,
				xDomain,
				yDomain
			})
		} } };
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/shared/sharedWidgetRuntimeActions.js
	var GENERIC_WIDGET_ACTIONS = [
		"widget.highlightValues",
		"widget.aggregateData",
		"widget.filterByRange",
		"widget.sortEncoding",
		"widget.changeEncoding",
		"widget.zoomDomain",
		"widget.filterByValues",
		"widget.resetView",
		"widget.undoView"
	];
	function clone$19(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readTargetWidgetPrecondition({ call, ctx, message } = {}) {
		const targetRef = readNormalizedQueryScope({ call }).widgetRef || null;
		if ((ctx?.resolveTargetWidget?.({ targetRef }) || null)?.ref) return { ok: true };
		return {
			ok: false,
			message: message || "The requested target widget is not available in the current workspace.",
			details: { targetRef },
			recoveryHints: ["Call describeWorkspace() to inspect valid widget refs before retrying.", "Retry the action with a targetRef that resolves to a materialized widget."]
		};
	}
	function registerTargetWidgetPrecondition(actionExecutor, actionName) {
		actionExecutor?.registerPrecondition?.(actionName, (call, ctx) => readTargetWidgetPrecondition({
			call,
			ctx,
			message: `${actionName} requires a valid target widget.`
		}));
	}
	function registerAction(actionExecutor, descriptor, handler) {
		if (!actionExecutor || typeof actionExecutor.register !== "function") return;
		if (typeof actionExecutor.has === "function" && actionExecutor.has(descriptor.name)) return;
		actionExecutor.register(descriptor, handler, { runtimeHandler: true });
	}
	function makeResetViewState(currentState, targetWidget, store = null) {
		const targetRef = targetWidget?.ref || null;
		if (!targetRef) return currentState;
		const currentWidget = currentState?.widgets?.[targetRef] || targetWidget;
		const nextWidgets = { ...currentState?.widgets || {} };
		const rowCount = currentWidget?.data?.rowCount;
		const nextData = {
			...currentWidget?.data || {},
			selectedCount: 0
		};
		if (Number.isFinite(rowCount)) nextData.visibleCount = rowCount;
		nextWidgets[targetRef] = {
			...clone$19(currentWidget),
			version: (currentWidget?.version || 0) + 1,
			updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			transforms: [],
			selections: {},
			feedback: {},
			data: nextData,
			view: {}
		};
		const currentShared = currentState?.shared || {};
		const registry = readSelectionRegistry(currentShared);
		const refsToRemove = new Set(Object.keys(currentWidget?.selections || {}));
		const nextRegistry = {};
		for (const [selectionRef, selection] of Object.entries(registry)) {
			if (refsToRemove.has(selectionRef) || selection?.sourceWidgetRef === targetRef || selection?.source_widget_ref === targetRef) continue;
			nextRegistry[selectionRef] = selection;
		}
		const byWidget = readSelectionByWidgetView(currentShared);
		const nextByWidget = {};
		const targetWidgetKey = targetWidget?.widgetId || targetRef;
		for (const [widgetKey, selectionView] of Object.entries(byWidget)) {
			if (widgetKey === targetWidgetKey || widgetKey === targetRef) continue;
			if (selectionView?.selectionRef && refsToRemove.has(selectionView.selectionRef)) continue;
			nextByWidget[widgetKey] = selectionView;
		}
		const primary = readSelectionPrimaryView(currentShared);
		const nextPrimary = primary?.selectionRef && refsToRemove.has(primary.selectionRef) ? null : primary;
		const stateMeta = typeof store?.stateManager?.createStateMeta === "function" ? store.stateManager.createStateMeta({
			workspaceId: store.workspaceId || "main",
			previousState: currentState,
			nextWidgets,
			nextShared: withSelectionSubmodel(currentShared, {
				registry: nextRegistry,
				primary: nextPrimary,
				byWidget: nextByWidget
			}),
			nextTaskContext: currentState?.taskContext,
			nextReplayContext: store?.replayContext ?? currentState?.replayContext,
			branchId: store?.currentBranchId || "main"
		}) : null;
		return {
			...clone$19(currentState) || {},
			stateId: stateMeta?.stateId || currentState?.stateId || null,
			createdAt: stateMeta?.createdAt || (/* @__PURE__ */ new Date()).toISOString(),
			widgets: nextWidgets,
			shared: withSelectionSubmodel(currentShared, {
				registry: nextRegistry,
				primary: nextPrimary,
				byWidget: nextByWidget
			})
		};
	}
	function commitViewState(ctx, nextState, transitionType) {
		if (typeof ctx?.store?.commitState === "function") return ctx.store.commitState(nextState, { transitionType });
		if (ctx?.store && typeof ctx.store === "object") {
			ctx.store.state = nextState;
			ctx.store.widgets = nextState?.widgets || {};
			ctx.store.shared = nextState?.shared || {};
			ctx.store.version = (ctx.store.version || 0) + 1;
			ctx.store.stateId = nextState?.stateId || ctx.store.stateId;
		}
		return nextState;
	}
	function readPreviousStateSnapshot(ctx) {
		const currentStateId = ctx?.readCurrentState?.()?.stateId || null;
		const history = typeof ctx?.store?.listStateSnapshots === "function" ? ctx.store.listStateSnapshots(24) : Array.isArray(ctx?.store?.stateSnapshots) ? ctx.store.stateSnapshots : [];
		const snapshots = Array.isArray(history) ? history : [];
		if (snapshots.length === 0) return null;
		const currentIndex = currentStateId ? snapshots.findIndex((entry) => entry?.stateId === currentStateId) : snapshots.length - 1;
		if (currentIndex > 0) return snapshots[currentIndex - 1];
		return snapshots.length > 1 ? snapshots[snapshots.length - 2] : null;
	}
	function registerSharedWidgetRuntimeActions(actionExecutor) {
		for (const actionName of GENERIC_WIDGET_ACTIONS) registerTargetWidgetPrecondition(actionExecutor, actionName);
		registerAction(actionExecutor, { name: "widget.updateSelection" }, async (params, ctx) => {
			const selectionType = typeof params.selection_type === "string" ? params.selection_type : null;
			if (!selectionType) throw new Error("widget.updateSelection requires a selection_type.");
			const targetWidget = ctx.targetWidget();
			const selection = {
				selection_id: typeof params.selection_id === "string" ? params.selection_id : `sel_${Date.now()}`,
				source_widget_id: typeof params.source_widget_id === "string" ? params.source_widget_id : targetWidget?.widgetId || void 0,
				selection_type: selectionType,
				...params.domain && typeof params.domain === "object" ? { domain: params.domain } : {},
				...Array.isArray(params.fields) ? { fields: params.fields } : {},
				...params.value && typeof params.value === "object" && !Array.isArray(params.value) ? { value: params.value } : {},
				...typeof params.keyField === "string" ? { keyField: params.keyField } : {},
				...Array.isArray(params.keys) ? { keys: params.keys } : {},
				...typeof params.field === "string" ? { field: params.field } : {},
				...Array.isArray(params.values) ? { values: params.values } : {},
				predicates: Array.isArray(params.predicates) ? params.predicates : [],
				count: Number.isFinite(params.count) ? params.count : 0,
				summary: typeof params.summary === "string" ? params.summary : ""
			};
			return {
				patch: buildRuntimeSelectionPatch({
					state: ctx.readCurrentState(),
					targetWidget,
					selection
				}),
				affectedRefs: [targetWidget.ref],
				propagateFromSelection: true,
				result: {
					selection,
					widgetId: targetWidget?.widgetId || null
				},
				verificationHints: ["Read the widget selection state and confirm it matches the supplied runtime selection payload.", "Read linked widgets to confirm propagation follows the updated selection."],
				notes: { userVisibleSummary: selection.summary || "Human-driven selection was applied through the shared action pipeline." }
			};
		});
		registerAction(actionExecutor, { name: "widget.clearSelection" }, (params, ctx) => {
			const targetWidget = ctx.resolveTargetWidget();
			const patch = buildClearSelectionPatch({
				state: ctx.readCurrentState(),
				targetWidget
			});
			return {
				patch,
				affectedRefs: Object.keys(patch).filter((ref) => ref !== "shared"),
				result: { cleared: true },
				verificationHints: ["Read the widget state and confirm there is no active selection."]
			};
		});
		registerAction(actionExecutor, { name: "widget.resetView" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			return {
				nextState: commitViewState(ctx, makeResetViewState(ctx.readCurrentState(), targetWidget, ctx.store), "reset_view"),
				updatedRefs: [targetWidget.ref],
				affectedRefs: [targetWidget.ref],
				result: {
					reset: true,
					widgetId: targetWidget.widgetId || null
				},
				verificationHints: ["Read the target widget state and confirm view, transforms, and local selections returned to baseline.", "Read the widget render payload and confirm the provider spec no longer contains runtime view projections."],
				transition: {
					type: "reset_view",
					notes: { userVisibleSummary: "Widget view state was reset to its baseline." }
				}
			};
		});
		registerAction(actionExecutor, { name: "widget.undoView" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const snapshot = readPreviousStateSnapshot(ctx);
			const snapshotState = snapshot?.state || snapshot || null;
			if (!snapshotState) throw new Error("widget.undoView requires a previous runtime state snapshot.");
			return {
				nextState: commitViewState(ctx, snapshotState, "undo_view"),
				updatedRefs: [targetWidget.ref],
				affectedRefs: [targetWidget.ref],
				result: {
					restored: true,
					widgetId: targetWidget.widgetId || null,
					restoredStateId: snapshotState.stateId || snapshot?.stateId || null
				},
				verificationHints: ["Read the target widget state and confirm the previous view state was restored.", "Read the widget render payload and confirm the provider spec matches the restored state."],
				transition: {
					type: "undo_view",
					parentStateId: snapshotState.stateId || snapshot?.stateId || null,
					notes: { userVisibleSummary: "Widget view state was restored from the previous runtime snapshot." }
				}
			};
		});
		registerAction(actionExecutor, { name: "widget.aggregateData" }, async (params, ctx) => {
			const groupBy = Array.isArray(params.groupBy) ? params.groupBy.filter((field) => typeof field === "string" && field.trim()) : [];
			const measures = Array.isArray(params.measures) ? params.measures.map((measure) => ({
				op: typeof measure?.op === "string" ? measure.op : null,
				field: typeof measure?.field === "string" ? measure.field : void 0,
				as: typeof measure?.as === "string" ? measure.as : null
			})).filter((measure) => measure.op && measure.as) : [];
			const supportedMeasureOps = new Set([
				"count",
				"sum",
				"mean",
				"min",
				"max",
				"median"
			]);
			if (groupBy.length === 0 || measures.length === 0 || measures.some((measure) => !supportedMeasureOps.has(measure.op))) throw new Error("widget.aggregateData requires non-empty groupBy and supported measures (count, sum, mean, min, max, or median) with explicit aliases.");
			const targetWidget = ctx.targetWidget();
			return {
				patch: buildWidgetAggregatePatch({
					targetWidget,
					groupBy,
					measures
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidget.widgetId,
					groupBy,
					measures
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the target spec now includes the requested aggregate transform.", "Call perception.inspectVisibleRows or summarizeVisible to confirm the widget now exposes grouped summary rows."]
			};
		});
		registerAction(actionExecutor, { name: "widget.highlightValues" }, async (params, ctx) => {
			const field = typeof params.field === "string" ? params.field : null;
			const values = uniqueWidgetValues(params.values);
			const targetWidget = ctx.targetWidget();
			const targetWidgetRef = targetWidget?.ref || null;
			if (!field || values.length === 0 || !targetWidgetRef) throw new Error("widget.highlightValues requires a target widget, field, and at least one value.");
			const rows = ctx.readRows(targetWidgetRef);
			const highlightPatch = buildWidgetHighlightValuesPatch({
				targetWidget,
				currentState: ctx.readCurrentState(),
				rows,
				field,
				values
			});
			return {
				patch: highlightPatch.patch,
				affectedRefs: [targetWidgetRef, "shared"],
				result: {
					widgetId: targetWidget?.widgetId || null,
					field,
					values,
					highlightedCount: highlightPatch.highlightedCount
				},
				verificationHints: ["Call perception.inspectVisibleRows to verify matching rows now carry highlight markers.", "Read the target widget state and confirm feedback.highlightedKeys contains the requested values."]
			};
		});
		registerAction(actionExecutor, { name: "widget.filterByValues" }, async (params, ctx) => {
			const field = typeof params.field === "string" ? params.field : null;
			const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
			const targetWidget = ctx.targetWidget();
			const targetWidgetId = targetWidget?.widgetId || null;
			if (!field || values.length === 0 || !targetWidgetId) throw new Error("widget.filterByValues requires a target widget, field, and at least one value.");
			const visibleCount = ctx.readRows(targetWidget.ref).filter((row) => values.includes(row?.[field])).length;
			return {
				patch: buildWidgetFilterByValuesPatch({
					targetWidget,
					currentState: ctx.readCurrentState(),
					field,
					values,
					visibleCount
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidgetId,
					field,
					values
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the target spec now includes the requested categorical filter transform.", "Read the target widget state or visible rows to confirm the filtered subset propagated."]
			};
		});
		registerAction(actionExecutor, { name: "widget.filterByRange" }, async (params, ctx) => {
			const field = typeof params.field === "string" ? params.field : null;
			const range = Array.isArray(params.range) ? params.range : null;
			const targetWidget = ctx.targetWidget();
			const targetWidgetId = targetWidget?.widgetId || null;
			if (!field || !Array.isArray(range) || range.length !== 2 || !targetWidgetId) throw new Error("widget.filterByRange requires a target widget, field, and a two-value range.");
			const normalizedRange = [Math.min(...range), Math.max(...range)];
			const visibleCount = ctx.readRows(targetWidget.ref).filter((row) => {
				const value = row?.[field];
				return typeof value === "number" && value >= normalizedRange[0] && value <= normalizedRange[1];
			}).length;
			return {
				patch: buildWidgetFilterByRangePatch({
					targetWidget,
					currentState: ctx.readCurrentState(),
					field,
					range: normalizedRange,
					visibleCount
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidgetId,
					field,
					range: normalizedRange
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the target spec now includes the requested range filter transform.", "Read the target widget state or visible rows to confirm the filtered numeric interval propagated."]
			};
		});
		registerAction(actionExecutor, { name: "widget.sortEncoding" }, async (params, ctx) => {
			const channel = typeof params.channel === "string" ? params.channel : null;
			const order = typeof params.order === "string" ? params.order : null;
			const field = typeof params.field === "string" ? params.field : null;
			const aggregate = typeof params.aggregate === "string" ? params.aggregate : null;
			const targetWidget = ctx.targetWidget();
			const targetWidgetId = targetWidget?.widgetId || null;
			if (!channel || !order || !targetWidgetId) throw new Error("widget.sortEncoding requires a target widget, channel, and order.");
			return {
				patch: buildWidgetSortEncodingPatch({
					targetWidget,
					channel,
					order,
					field,
					aggregate
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidgetId,
					channel,
					order,
					...field ? { field } : {},
					...aggregate ? { aggregate } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the target encoding now includes the requested sort rule.", "Read the target widget state to confirm the sort update propagated."]
			};
		});
		registerAction(actionExecutor, { name: "widget.changeEncoding" }, async (params, ctx) => {
			const channel = typeof params.channel === "string" ? params.channel : null;
			const field = typeof params.field === "string" ? params.field : null;
			const type = typeof params.type === "string" ? params.type : null;
			const aggregate = typeof params.aggregate === "string" ? params.aggregate : null;
			const targetWidget = ctx.targetWidget();
			const targetWidgetId = targetWidget?.widgetId || null;
			if (!channel || !field || !targetWidgetId) throw new Error("widget.changeEncoding requires a target widget, channel, and field.");
			return {
				patch: buildWidgetChangeEncodingPatch({
					targetWidget,
					channel,
					field,
					type,
					aggregate
				}),
				affectedRefs: [targetWidget.ref],
				result: {
					widgetId: targetWidgetId,
					channel,
					field,
					...type ? { type } : {},
					...aggregate ? { aggregate } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the encoding channel now points to the new field.", "Read the target widget state to confirm the encoding update propagated."]
			};
		});
		registerAction(actionExecutor, { name: "widget.zoomDomain" }, async (params, ctx) => {
			const targetWidget = ctx.targetWidget();
			const xDomain = Array.isArray(params.xDomain) ? params.xDomain : null;
			const yDomain = Array.isArray(params.yDomain) ? params.yDomain : null;
			if (!targetWidget || !xDomain && !yDomain) throw new Error("widget.zoomDomain requires a target widget and at least one domain range.");
			return {
				patch: buildWidgetZoomDomainPatch({
					targetWidget,
					xDomain,
					yDomain
				}),
				affectedRefs: [targetWidget.ref],
				propagateFromRef: `${targetWidget.ref}/view/zoom`,
				result: {
					widgetId: targetWidget.widgetId,
					...xDomain ? { xDomain } : {},
					...yDomain ? { yDomain } : {}
				},
				verificationHints: ["Call perception.inspectViewConfig to verify the target domain was updated.", "Read the target widget view state to confirm the new x/y domain values."]
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/specModel.js
	function ensureObjectSpec(spec, message) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) throw new Error(message);
		return spec;
	}
	function readMarkType(mark) {
		return typeof mark === "string" ? mark : mark?.type || null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/specTree.js
	function collectNestedSpecs(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return [];
		const nested = [];
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(spec?.[key])) nested.push(...spec[key].filter((entry) => entry && typeof entry === "object"));
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) nested.push(spec.spec);
		return nested;
	}
	function findRepresentativeSpec(spec, matcher) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return null;
		if (matcher(spec)) return spec;
		if (Array.isArray(spec?.layer)) {
			const layeredMatch = spec.layer.find((entry) => entry && typeof entry === "object" && matcher(entry));
			if (layeredMatch) return layeredMatch;
		}
		for (const child of collectNestedSpecs(spec)) {
			const match = findRepresentativeSpec(child, matcher);
			if (match) return match;
		}
		return null;
	}
	function updateRepresentativeSpec(spec, matcher, updater, message) {
		ensureObjectSpec(spec, message);
		if (matcher(spec)) return updater(spec);
		if (Array.isArray(spec?.layer)) {
			let updated = false;
			const nextLayer = spec.layer.map((entry) => {
				if (updated || !entry || typeof entry !== "object") return entry;
				if (matcher(entry)) {
					updated = true;
					return updater(entry);
				}
				const nestedMatch = findRepresentativeSpec(entry, matcher);
				if (nestedMatch && nestedMatch !== entry) {
					updated = true;
					return updateRepresentativeSpec(entry, matcher, updater, message);
				}
				return entry;
			});
			if (updated) return {
				...spec,
				layer: nextLayer
			};
		}
		for (const key of [
			"vconcat",
			"hconcat",
			"concat"
		]) {
			if (!Array.isArray(spec?.[key])) continue;
			let updated = false;
			const nextChildren = spec[key].map((entry) => {
				if (updated || !entry || typeof entry !== "object") return entry;
				if (matcher(entry)) {
					updated = true;
					return updater(entry);
				}
				const nestedMatch = findRepresentativeSpec(entry, matcher);
				if (nestedMatch && nestedMatch !== entry) {
					updated = true;
					return updateRepresentativeSpec(entry, matcher, updater, message);
				}
				return entry;
			});
			if (updated) return {
				...spec,
				[key]: nextChildren
			};
		}
		if (spec?.spec && typeof spec.spec === "object" && !Array.isArray(spec.spec)) {
			if (matcher(spec.spec)) return {
				...spec,
				spec: updater(spec.spec)
			};
			const nestedMatch = findRepresentativeSpec(spec.spec, matcher);
			if (nestedMatch && nestedMatch !== spec.spec) return {
				...spec,
				spec: updateRepresentativeSpec(spec.spec, matcher, updater, message)
			};
		}
		throw new Error(message);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/specMutators.js
	function replaceTaggedLayer(layers, tag, nextLayer) {
		const nextLayers = (Array.isArray(layers) ? layers : []).filter((layer) => layer?._widgetvaTag !== tag);
		return nextLayer ? [...nextLayers, nextLayer] : nextLayers;
	}
	function replaceTaggedTransform(transforms, tag, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => transform?._widgetvaTag !== tag);
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function replaceFilterTransformForField(transforms, field, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => {
			const filter = transform?.filter;
			return (filter?.field || filter?.not?.field || null) !== field;
		});
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	function replaceAggregateTransforms(transforms, nextTransform) {
		const nextTransforms = (Array.isArray(transforms) ? transforms : []).filter((transform) => !Array.isArray(transform?.aggregate));
		return nextTransform ? [...nextTransforms, nextTransform] : nextTransforms;
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/families/widgetSpecActions.js
	function hasEncoding(spec) {
		return spec?.encoding && typeof spec.encoding === "object" && !Array.isArray(spec.encoding);
	}
	function markHighlightedRows(rows, field, values) {
		const highlightedValues = new Set(Array.isArray(values) ? values : []);
		return (Array.isArray(rows) ? rows : []).map((row) => ({
			...row,
			__widgetva_highlight: highlightedValues.has(row?.[field])
		}));
	}
	function executeVegaLiteChangeEncoding(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for encoding updates.");
		const channel = typeof params.channel === "string" ? params.channel : null;
		const field = typeof params.field === "string" ? params.field : null;
		if (!channel || !field) throw new Error("widget.changeEncoding requires a channel and field.");
		return updateRepresentativeSpec(spec, hasEncoding, (viewSpec) => ({
			...viewSpec,
			encoding: {
				...viewSpec.encoding || {},
				[channel]: {
					...viewSpec.encoding?.[channel] || {},
					field,
					...typeof params.type === "string" ? { type: params.type } : {},
					...params.aggregate ? { aggregate: params.aggregate } : {}
				}
			}
		}), "No representative encoded subview could be found for widget.changeEncoding.");
	}
	function executeVegaLiteZoomDomain(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for domain updates.");
		const normalizeDomain = (domain, channel) => {
			if (!Array.isArray(domain) || domain.length < 2) return null;
			if (domain[0] == null && domain[1] == null) return null;
			const field = spec?.encoding?.[channel]?.field;
			const existingDomain = Array.isArray(spec?.encoding?.[channel]?.scale?.domain) ? spec.encoding[channel].scale.domain : null;
			const values = Array.isArray(spec?.data?.values) && typeof field === "string" ? spec.data.values.map((row) => row?.[field]).filter((value) => typeof value === "number" && Number.isFinite(value)) : [];
			const extent = values.length > 0 ? [Math.min(...values), Math.max(...values)] : null;
			const lower = domain[0] == null ? existingDomain?.[0] ?? extent?.[0] : domain[0];
			const upper = domain[1] == null ? existingDomain?.[1] ?? extent?.[1] : domain[1];
			if (lower == null || upper == null) return null;
			if (typeof lower === "number" && typeof upper === "number" && (!Number.isFinite(lower) || !Number.isFinite(upper))) return null;
			if (lower === upper) return null;
			return [lower, upper];
		};
		const xDomain = normalizeDomain(params.xDomain, "x");
		const yDomain = normalizeDomain(params.yDomain, "y");
		if (!xDomain && !yDomain) throw new Error("widget.zoomDomain requires at least one domain.");
		const nextEncoding = { ...spec.encoding || {} };
		if (xDomain && nextEncoding.x) nextEncoding.x = {
			...nextEncoding.x,
			scale: {
				...nextEncoding.x.scale || {},
				domain: xDomain
			}
		};
		if (yDomain && nextEncoding.y) nextEncoding.y = {
			...nextEncoding.y,
			scale: {
				...nextEncoding.y.scale || {},
				domain: yDomain
			}
		};
		const nextMark = typeof spec.mark === "string" ? {
			type: spec.mark,
			clip: true
		} : spec.mark && typeof spec.mark === "object" ? {
			...spec.mark,
			clip: true
		} : spec.mark;
		return {
			...spec,
			...nextMark ? { mark: nextMark } : {},
			encoding: nextEncoding
		};
	}
	function executeVegaLiteFilterByValues(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for filter updates.");
		const field = typeof params.field === "string" ? params.field : null;
		const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
		if (!field || values.length === 0) throw new Error("widget.filterByValues requires a field and values.");
		return {
			...spec,
			transform: replaceFilterTransformForField(spec.transform, field, { filter: {
				field,
				oneOf: values
			} })
		};
	}
	function executeVegaLiteFilterByRange(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for filter updates.");
		const field = typeof params.field === "string" ? params.field : null;
		const range = Array.isArray(params.range) ? params.range : null;
		if (!field || !range || range.length !== 2) throw new Error("widget.filterByRange requires a field and two-value range.");
		return {
			...spec,
			transform: replaceFilterTransformForField(spec.transform, field, { filter: {
				field,
				range
			} })
		};
	}
	function executeVegaLiteAggregateData(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for aggregate updates.");
		const groupBy = Array.isArray(params.groupBy) ? params.groupBy.filter((field) => typeof field === "string" && field.trim()) : [];
		const measures = Array.isArray(params.measures) ? params.measures.map((measure) => ({
			op: typeof measure?.op === "string" ? measure.op : null,
			field: typeof measure?.field === "string" ? measure.field : void 0,
			as: typeof measure?.as === "string" ? measure.as : null
		})).filter((measure) => measure.op && measure.as) : [];
		if (groupBy.length === 0 || measures.length === 0) throw new Error("widget.aggregateData requires non-empty groupBy and measures.");
		return {
			...spec,
			transform: replaceAggregateTransforms(spec.transform, {
				aggregate: measures.map((measure) => ({
					op: measure.op,
					...measure.field ? { field: measure.field } : {},
					as: measure.as
				})),
				groupby: groupBy
			})
		};
	}
	function executeVegaLiteSortEncoding(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for sort updates.");
		const channel = typeof params.channel === "string" ? params.channel : null;
		const order = typeof params.order === "string" ? params.order : null;
		const field = typeof params.field === "string" ? params.field : null;
		const aggregate = typeof params.aggregate === "string" ? params.aggregate : null;
		if (!channel || !order) throw new Error("widget.sortEncoding requires channel and order.");
		const currentChannel = spec.encoding?.[channel];
		if (!currentChannel || typeof currentChannel !== "object") throw new Error(`The active spec does not define encoding channel "${channel}".`);
		const sortField = field || currentChannel.field;
		return {
			...spec,
			encoding: {
				...spec.encoding || {},
				[channel]: {
					...currentChannel,
					sort: sortField ? {
						field: sortField,
						order,
						...aggregate ? { op: aggregate } : {}
					} : order
				}
			}
		};
	}
	function executeVegaLiteHighlightValues(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for highlight updates.");
		const field = typeof params.field === "string" ? params.field : null;
		const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
		if (!field || values.length === 0) throw new Error("widget.highlightValues requires a field and values.");
		if (!Array.isArray(spec?.data?.values)) return spec;
		return {
			...spec,
			data: {
				...spec.data || {},
				values: markHighlightedRows(spec.data.values, field, values)
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/families/specActionShared.js
	function hasInlineObjectRows(spec) {
		return Array.isArray(spec?.data?.values) && spec.data.values.some((row) => row && typeof row === "object");
	}
	function readInlineRows(spec) {
		return hasInlineObjectRows(spec) ? spec.data.values.filter((row) => row && typeof row === "object") : [];
	}
	function datumRef(field) {
		return `datum['${String(field).replace(/\\/g, "\\\\").replace(/'/g, "\\'")}']`;
	}
	function valueList(values = []) {
		return (Array.isArray(values) ? values : []).map((value) => JSON.stringify(value)).join(",");
	}
	function expressionEqualsAny(field, values = []) {
		return `indexof([${valueList(values)}], ${datumRef(field)}) >= 0`;
	}
	function expressionNotEqualsAny(field, values = []) {
		return `indexof([${valueList(values)}], ${datumRef(field)}) < 0`;
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/families/scatterSpecActions.js
	function isScatterFamilySpec(spec) {
		const markType = readMarkType(spec?.mark);
		return (markType === "point" || markType === "circle" || markType === "square") && spec?.encoding && typeof spec.encoding === "object" && !Array.isArray(spec.encoding);
	}
	function buildDatumBetweenTest(field, min, max) {
		return `${datumRef(field)} >= ${min} && ${datumRef(field)} <= ${max}`;
	}
	function withBrushOpacity(layer, { xField, yField, xMin, xMax, yMin, yMax }) {
		const encoding = layer?.encoding || {};
		const inBrushTest = [buildDatumBetweenTest(xField, xMin, xMax), buildDatumBetweenTest(yField, yMin, yMax)].join(" && ");
		return {
			...layer,
			encoding: {
				...encoding,
				opacity: {
					condition: {
						test: inBrushTest,
						value: 1
					},
					value: .68
				}
			}
		};
	}
	function applyBrushVisualToScatterSpec(scatterSpec, brush) {
		if (Array.isArray(scatterSpec.layer) && scatterSpec.layer.length > 0) {
			let applied = false;
			return {
				...scatterSpec,
				layer: scatterSpec.layer.filter((layer) => layer?._widgetvaTag !== "scatter.brushRegion").map((layer) => {
					if (!applied && !layer?._widgetvaTag) {
						const markType = readMarkType(layer?.mark);
						if (!markType || markType === "point" || markType === "circle" || markType === "square") {
							applied = true;
							return withBrushOpacity(layer, brush);
						}
					}
					return layer;
				}),
				_scatter_brush_state: {
					fields: [brush.xField, brush.yField],
					xRange: [brush.xMin, brush.xMax],
					yRange: [brush.yMin, brush.yMax]
				}
			};
		}
		return {
			...scatterSpec,
			encoding: withBrushOpacity({ encoding: scatterSpec.encoding || {} }, brush).encoding,
			_scatter_brush_state: {
				fields: [brush.xField, brush.yField],
				xRange: [brush.xMin, brush.xMax],
				yRange: [brush.yMin, brush.yMax]
			}
		};
	}
	function euclideanDistanceSquared(a, b) {
		const dx = a[0] - b[0];
		const dy = a[1] - b[1];
		return dx * dx + dy * dy;
	}
	function assignPointsToCenters(points, centers) {
		return points.map((point) => {
			let bestIndex = 0;
			let bestDistance = Number.POSITIVE_INFINITY;
			centers.forEach((center, index) => {
				const distance = euclideanDistanceSquared(point, center);
				if (distance < bestDistance) {
					bestDistance = distance;
					bestIndex = index;
				}
			});
			return bestIndex;
		});
	}
	function recomputeCenters(points, labels, clusterCount, previousCenters) {
		return Array.from({ length: clusterCount }, (_, clusterIndex) => {
			const clusterPoints = points.filter((_, pointIndex) => labels[pointIndex] === clusterIndex);
			if (!clusterPoints.length) return previousCenters[clusterIndex];
			const sums = clusterPoints.reduce((acc, point) => [acc[0] + point[0], acc[1] + point[1]], [0, 0]);
			return [sums[0] / clusterPoints.length, sums[1] / clusterPoints.length];
		});
	}
	function runKMeans(points, clusterCount, maxIterations = 25) {
		const safeClusterCount = Math.max(1, Math.min(clusterCount, points.length));
		let centers = points.slice(0, safeClusterCount).map((point) => [...point]);
		let labels = assignPointsToCenters(points, centers);
		for (let iteration = 0; iteration < maxIterations; iteration += 1) {
			const nextCenters = recomputeCenters(points, labels, safeClusterCount, centers);
			const nextLabels = assignPointsToCenters(points, nextCenters);
			const unchanged = nextLabels.every((label, index) => label === labels[index]);
			centers = nextCenters;
			labels = nextLabels;
			if (unchanged) break;
		}
		return {
			labels,
			centers
		};
	}
	function updateScatterDataValues(spec, buildNextValues) {
		if (hasInlineObjectRows(spec)) return {
			...spec,
			data: {
				...spec.data || {},
				values: buildNextValues(spec)
			}
		};
		return updateRepresentativeSpec(spec, (entry) => isScatterFamilySpec(entry) && hasInlineObjectRows(entry), (scatterSpec) => ({
			...scatterSpec,
			data: {
				...scatterSpec.data || {},
				values: buildNextValues(scatterSpec)
			}
		}), "No active inline scatter data is available for scatter data updates.");
	}
	function executeVegaLiteScatterBrushLike(spec, params = {}, selectionId = "brush") {
		ensureObjectSpec(spec, "No active base spec is available for scatter brush visuals.");
		const xRange = Array.isArray(params.xRange) ? params.xRange : null;
		const yRange = Array.isArray(params.yRange) ? params.yRange : null;
		if (!params.xField || !params.yField || !xRange || !yRange) throw new Error("scatter.brushRegion requires xField, yField, xRange, and yRange.");
		const brush = {
			xField: params.xField,
			yField: params.yField,
			xMin: Math.min(...xRange),
			xMax: Math.max(...xRange),
			yMin: Math.min(...yRange),
			yMax: Math.max(...yRange)
		};
		return updateRepresentativeSpec(spec, isScatterFamilySpec, (scatterSpec) => ({
			...applyBrushVisualToScatterSpec(scatterSpec, brush),
			_widgetva_selection_id: selectionId
		}), "No representative scatter subview could be found in the active spec.");
	}
	function executeVegaLiteScatterFilterCategorical(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for scatter categorical filtering.");
		const representative = findRepresentativeSpec(spec, isScatterFamilySpec) || spec;
		const field = typeof params.field === "string" ? params.field : representative?.encoding?.color?.field || null;
		const values = Array.isArray(params.categoriesToRemove) ? params.categoriesToRemove.filter((value) => value != null) : [];
		if (!field || values.length === 0) throw new Error("scatter.filterCategorical requires a field and categoriesToRemove.");
		return updateRepresentativeSpec(spec, isScatterFamilySpec, (scatterSpec) => ({
			...scatterSpec,
			transform: replaceFilterTransformForField(scatterSpec.transform, field, {
				filter: { not: {
					field,
					oneOf: values
				} },
				_widgetvaTag: "scatter.filterCategorical"
			})
		}), "No representative scatter subview could be found in the active spec.");
	}
	function executeVegaLiteScatterIdentifyClusters(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for scatter clustering.");
		const representative = findRepresentativeSpec(spec, isScatterFamilySpec) || spec;
		const encoding = representative?.encoding || {};
		const xField = encoding?.x?.field;
		const yField = encoding?.y?.field;
		const points = (readInlineRows(representative).length > 0 ? readInlineRows(representative) : readInlineRows(spec)).map((row, index) => ({
			index,
			point: [Number(row?.[xField]), Number(row?.[yField])]
		})).filter((entry) => Number.isFinite(entry.point[0]) && Number.isFinite(entry.point[1]));
		if (!xField || !yField || points.length === 0) throw new Error("scatter.identifyClusters requires inline rows and quantitative x/y fields.");
		const clusterCount = Number.isFinite(params.nClusters) ? Math.max(1, Math.floor(params.nClusters)) : 3;
		const { labels } = runKMeans(points.map((entry) => entry.point), clusterCount);
		const labelsByRowIndex = new Map(points.map((entry, pointIndex) => [entry.index, labels[pointIndex]]));
		return updateRepresentativeSpec(updateScatterDataValues(spec, (sourceSpec) => readInlineRows(sourceSpec).map((row, index) => ({
			...row,
			__widgetva_cluster: labelsByRowIndex.get(index) ?? null
		}))), isScatterFamilySpec, (scatterSpec) => ({
			...scatterSpec,
			encoding: {
				...scatterSpec.encoding || {},
				color: {
					field: "__widgetva_cluster",
					type: "nominal"
				}
			},
			_scatter_cluster_state: {
				nClusters: clusterCount,
				method: params.method || "kmeans"
			}
		}), "No representative scatter subview could be found in the active spec.");
	}
	function executeVegaLiteScatterShowRegression(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for scatter regression.");
		return updateRepresentativeSpec(spec, isScatterFamilySpec, (scatterSpec) => {
			const encoding = scatterSpec.encoding || {};
			const xField = encoding?.x?.field;
			const yField = encoding?.y?.field;
			if (!xField || !yField) throw new Error("scatter.showRegression requires x and y encodings.");
			const points = (readInlineRows(scatterSpec).length > 0 ? readInlineRows(scatterSpec) : readInlineRows(spec)).map((row) => ({
				x: Number(row?.[xField]),
				y: Number(row?.[yField])
			})).filter((point) => Number.isFinite(point.x) && Number.isFinite(point.y));
			if (points.length < 2) throw new Error("scatter.showRegression requires at least two finite x/y points.");
			const mean = (values) => values.reduce((sum, value) => sum + value, 0) / values.length;
			const xs = points.map((point) => point.x);
			const ys = points.map((point) => point.y);
			const mx = mean(xs);
			const my = mean(ys);
			const denominator = xs.reduce((sum, x) => sum + (x - mx) ** 2, 0);
			const slope = denominator === 0 ? 0 : points.reduce((sum, point) => sum + (point.x - mx) * (point.y - my), 0) / denominator;
			const intercept = my - slope * mx;
			const minX = Math.min(...xs);
			const maxX = Math.max(...xs);
			const regressionLayer = {
				_widgetvaTag: "scatter.showRegression",
				data: { values: [{
					[xField]: minX,
					[yField]: intercept + slope * minX
				}, {
					[xField]: maxX,
					[yField]: intercept + slope * maxX
				}] },
				mark: {
					type: "line",
					color: "#d62728",
					strokeWidth: 2
				},
				encoding: {
					x: { ...encoding.x },
					y: { ...encoding.y }
				}
			};
			const baseLayer = Array.isArray(scatterSpec.layer) ? scatterSpec.layer.filter((layer) => layer?._widgetvaTag !== "scatter.showRegression") : [{
				mark: scatterSpec.mark || "point",
				encoding
			}];
			const { mark: _removedMark, encoding: _removedEncoding, ...layeredSpec } = scatterSpec;
			return {
				...layeredSpec,
				layer: replaceTaggedLayer(baseLayer, "scatter.showRegression", regressionLayer),
				_scatter_regression_state: { method: params.method || "linear" }
			};
		}, "No representative scatter subview could be found in the active spec.");
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/families/barSpecActions.js
	function isBarFamilySpec(spec) {
		return readMarkType(spec?.mark) === "bar" && spec?.encoding && typeof spec.encoding === "object" && !Array.isArray(spec.encoding);
	}
	function detectBarCategoryField(spec, explicitField = null) {
		if (typeof explicitField === "string" && explicitField.trim().length > 0) return explicitField;
		const encoding = spec?.encoding || {};
		const xField = encoding?.x?.field || null;
		const yField = encoding?.y?.field || null;
		const xType = encoding?.x?.type || null;
		const yType = encoding?.y?.type || null;
		if ((xType === "nominal" || xType === "ordinal") && xField) return xField;
		if ((yType === "nominal" || yType === "ordinal") && yField) return yField;
		return xField || yField || null;
	}
	function detectBarChannels(spec, requestedChannel) {
		const encoding = spec?.encoding || {};
		const xType = encoding?.x?.type || null;
		const yType = encoding?.y?.type || null;
		const xField = encoding?.x?.field || null;
		const yField = encoding?.y?.field || null;
		const xAggregate = encoding?.x?.aggregate || null;
		const yAggregate = encoding?.y?.aggregate || null;
		const normalizedRequested = requestedChannel === "x" || requestedChannel === "y" ? requestedChannel : null;
		if (normalizedRequested) return {
			categoryChannel: normalizedRequested,
			valueChannel: normalizedRequested === "x" ? "y" : "x"
		};
		if (xAggregate && yField && !yAggregate) return {
			categoryChannel: "y",
			valueChannel: "x"
		};
		if (yAggregate && xField && !xAggregate) return {
			categoryChannel: "x",
			valueChannel: "y"
		};
		if ((yType === "nominal" || yType === "ordinal") && xType === "quantitative") return {
			categoryChannel: "y",
			valueChannel: "x"
		};
		return {
			categoryChannel: "x",
			valueChannel: "y"
		};
	}
	function detectSubcategoryField(spec, explicitField = null) {
		const encoding = spec?.encoding || {};
		const inferredField = encoding?.xOffset?.field || encoding?.color?.field || null;
		if (typeof explicitField === "string" && explicitField.trim().length > 0) return explicitField === inferredField ? explicitField : inferredField;
		return inferredField;
	}
	function aggregateBarValues(rows, { categoryField, valueField, aggregate, colorField, bySubcategory }) {
		const normalizedAggregate = typeof aggregate === "string" && aggregate.trim().length > 0 ? aggregate.toLowerCase() : "mean";
		const groupedValues = /* @__PURE__ */ new Map();
		const pushValue = (key, value) => {
			if (!groupedValues.has(key)) groupedValues.set(key, []);
			groupedValues.get(key).push(value);
		};
		for (const row of rows) {
			if (!row || typeof row !== "object") continue;
			const category = row[categoryField];
			const value = Number(row[valueField]);
			if (category == null || !Number.isFinite(value)) continue;
			if (bySubcategory != null) {
				if (!colorField || row[colorField] !== bySubcategory) continue;
				pushValue(category, value);
			} else if (colorField) pushValue(JSON.stringify([category, row[colorField]]), value);
			else pushValue(category, value);
		}
		const applyAggregate = (values) => {
			if (!values.length) return 0;
			if (normalizedAggregate === "sum") return values.reduce((sum, value) => sum + value, 0);
			if (normalizedAggregate === "count") return values.length;
			if (normalizedAggregate === "min") return Math.min(...values);
			if (normalizedAggregate === "max") return Math.max(...values);
			if (normalizedAggregate === "median") {
				const sorted = [...values].sort((left, right) => left - right);
				const mid = Math.floor(sorted.length / 2);
				return sorted.length % 2 === 0 ? (sorted[mid - 1] + sorted[mid]) / 2 : sorted[mid];
			}
			return values.reduce((sum, value) => sum + value, 0) / values.length;
		};
		if (bySubcategory != null || !colorField) return new Map([...groupedValues.entries()].map(([key, values]) => [key, applyAggregate(values)]));
		const groupedByCategory = /* @__PURE__ */ new Map();
		for (const [compositeKey, values] of groupedValues.entries()) {
			const [category] = JSON.parse(compositeKey);
			groupedByCategory.set(category, (groupedByCategory.get(category) || 0) + applyAggregate(values));
		}
		return groupedByCategory;
	}
	function readBarRows(spec, barSpec) {
		const directRows = readInlineRows(barSpec);
		return directRows.length > 0 ? directRows : readInlineRows(spec);
	}
	function updateBar(spec, updater) {
		return updateRepresentativeSpec(spec, isBarFamilySpec, updater, "No representative bar subview could be found in the active spec.");
	}
	function executeVegaLiteBarFilterCategories(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for bar category filtering.");
		const field = detectBarCategoryField(findRepresentativeSpec(spec, isBarFamilySpec) || spec, params.field);
		const categories = Array.isArray(params.categories) ? params.categories.filter((value) => value != null) : [];
		if (!field || categories.length === 0) throw new Error("bar.filterCategories requires categories and a field.");
		return updateBar(spec, (barSpec) => ({
			...barSpec,
			transform: replaceFilterTransformForField(barSpec.transform, field, {
				filter: {
					field,
					oneOf: categories
				},
				_widgetvaTag: "bar.filterCategories"
			})
		}));
	}
	function executeVegaLiteBarSortBars(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for bar sort updates.");
		const representative = findRepresentativeSpec(spec, isBarFamilySpec) || spec;
		const { categoryChannel, valueChannel } = detectBarChannels(representative, params.channel);
		const categoryEncoding = representative?.encoding?.[categoryChannel];
		const valueEncoding = representative?.encoding?.[valueChannel];
		const categoryField = categoryEncoding?.field;
		const valueField = params.field || valueEncoding?.field;
		const order = params.order === "ascending" ? "ascending" : "descending";
		const rows = readBarRows(spec, representative);
		const sortedCategories = rows.length > 0 && categoryField && valueField ? [...new Set(rows.map((row) => row[categoryField]).filter((value) => value != null))].sort((left, right) => {
			const scores = aggregateBarValues(rows, {
				categoryField,
				valueField,
				aggregate: params.aggregate || valueEncoding?.aggregate || "mean",
				colorField: representative?.encoding?.color?.field || null,
				bySubcategory: params.bySubcategory
			});
			const leftScore = scores.get(left) ?? 0;
			const rightScore = scores.get(right) ?? 0;
			if (leftScore === rightScore) return String(left).localeCompare(String(right));
			return order === "ascending" ? leftScore - rightScore : rightScore - leftScore;
		}) : order;
		return updateBar(spec, (barSpec) => ({
			...barSpec,
			encoding: {
				...barSpec.encoding || {},
				[categoryChannel]: {
					...barSpec.encoding?.[categoryChannel] || {},
					sort: sortedCategories
				}
			}
		}));
	}
	function executeVegaLiteBarHighlightTopN(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for bar highlight updates.");
		const representative = findRepresentativeSpec(spec, isBarFamilySpec) || spec;
		const { categoryChannel, valueChannel } = detectBarChannels(representative, params.channel);
		const categoryEncoding = representative?.encoding?.[categoryChannel];
		const valueEncoding = representative?.encoding?.[valueChannel];
		const categoryField = params.categoryField || params.field || categoryEncoding?.field;
		const measureField = params.measureField || valueEncoding?.field;
		const rows = readBarRows(spec, representative);
		const n = Number.isFinite(params.n) ? Math.max(1, Math.floor(params.n)) : 3;
		const order = params.order === "ascending" ? "ascending" : "descending";
		if (!categoryField || !measureField || rows.length === 0) throw new Error("bar.highlightTopN requires category/measure fields and inline bar rows.");
		const highlightedCategories = [...aggregateBarValues(rows, {
			categoryField,
			valueField: measureField,
			aggregate: params.aggregate || valueEncoding?.aggregate || "mean",
			colorField: representative?.encoding?.color?.field || null,
			bySubcategory: params.bySubcategory
		}).entries()].sort(([leftCategory, leftScore], [rightCategory, rightScore]) => {
			if (leftScore === rightScore) return String(leftCategory).localeCompare(String(rightCategory));
			return order === "ascending" ? leftScore - rightScore : rightScore - leftScore;
		}).slice(0, n).map(([category]) => category);
		if (highlightedCategories.length === 0) throw new Error("bar.highlightTopN could not derive any categories from the current bar data.");
		return updateBar(spec, (barSpec) => ({
			...barSpec,
			encoding: {
				...barSpec.encoding || {},
				opacity: {
					condition: {
						test: expressionEqualsAny(categoryField, highlightedCategories),
						value: 1
					},
					value: Number.isFinite(params.dimOpacity) ? params.dimOpacity : .28
				},
				stroke: {
					condition: {
						test: expressionEqualsAny(categoryField, highlightedCategories),
						value: "#202124"
					},
					value: null
				},
				strokeWidth: {
					condition: {
						test: expressionEqualsAny(categoryField, highlightedCategories),
						value: 1.5
					},
					value: 0
				}
			},
			_bar_highlight_top_n_state: {
				n,
				order,
				categoryField,
				measureField,
				highlightedCategories
			}
		}));
	}
	function executeVegaLiteBarAddRemoveBars(spec, params = {}, mode = "add") {
		ensureObjectSpec(spec, "No active base spec is available for bar visibility updates.");
		const representative = findRepresentativeSpec(spec, isBarFamilySpec) || spec;
		const field = detectBarCategoryField(representative, params.field);
		const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
		const explicitVisibleValues = Array.isArray(params.visibleValues) ? params.visibleValues.filter((value) => value != null) : null;
		if (!field || values.length === 0 && !explicitVisibleValues) throw new Error("bar.addBars/removeBars requires values or visibleValues and a field.");
		const rows = readBarRows(spec, representative);
		const existingCategories = new Set(rows.map((row) => row[field]).filter((value) => value != null));
		const nextVisible = explicitVisibleValues ? [...new Set(explicitVisibleValues)].sort((left, right) => String(left).localeCompare(String(right))) : (() => {
			const previous = representative?._bar_visibility_state;
			const visible = previous?.mode === "x" && previous?.x_field === field && Array.isArray(previous.visible_x) ? new Set(previous.visible_x) : new Set(existingCategories);
			for (const value of values) if (mode === "add") {
				if (existingCategories.size === 0 || existingCategories.has(value)) visible.add(value);
			} else visible.delete(value);
			return [...visible].sort((left, right) => String(left).localeCompare(String(right)));
		})();
		if (nextVisible.length === 0) throw new Error("bar.addBars/removeBars requires at least one visible value after applying the update.");
		return updateBar(spec, (barSpec) => ({
			...barSpec,
			transform: replaceTaggedTransform(barSpec.transform, "bar.addBars", {
				filter: {
					field,
					oneOf: nextVisible
				},
				_widgetvaTag: "bar.addBars"
			}),
			_bar_visibility_state: {
				mode: "x",
				x_field: field,
				visible_x: nextVisible,
				last_operation: mode === "add" ? "add" : "remove"
			}
		}));
	}
	function executeVegaLiteBarAddRemoveItems(spec, params = {}, mode = "add") {
		ensureObjectSpec(spec, "No active base spec is available for bar item visibility updates.");
		const representative = findRepresentativeSpec(spec, isBarFamilySpec) || spec;
		const xField = detectBarCategoryField(representative, params.xField);
		const subField = detectSubcategoryField(representative, params.subField);
		const items = Array.isArray(params.items) ? params.items.filter((item) => item && typeof item === "object" && item.x != null && item.sub != null) : [];
		const explicitVisibleItems = Array.isArray(params.visibleItems) ? params.visibleItems.filter((item) => item && typeof item === "object" && item.x != null && item.sub != null) : null;
		if (!xField || !subField || items.length === 0 && !explicitVisibleItems) throw new Error("bar.addBarItems/removeBarItems requires items or visibleItems, xField, and subField.");
		const rows = readBarRows(spec, representative);
		const existingPairs = new Set(rows.map((row) => row[xField] != null && row[subField] != null ? JSON.stringify([row[xField], row[subField]]) : null).filter(Boolean));
		const nextVisibleItems = explicitVisibleItems ? explicitVisibleItems : (() => {
			const previous = representative?._bar_visibility_state;
			const visiblePairs = previous?.mode === "item" && previous?.x_field === xField && previous?.sub_field === subField && Array.isArray(previous.visible_items) ? new Set(previous.visible_items.map((pair) => JSON.stringify(pair))) : new Set(existingPairs);
			for (const item of items) {
				const pairKey = JSON.stringify([item.x, item.sub]);
				if (mode === "add") {
					if (existingPairs.size === 0 || existingPairs.has(pairKey)) visiblePairs.add(pairKey);
				} else visiblePairs.delete(pairKey);
			}
			return [...visiblePairs].map((pairKey) => JSON.parse(pairKey));
		})();
		if (nextVisibleItems.length === 0) throw new Error("bar.addBarItems/removeBarItems requires at least one visible item after applying the update.");
		return updateBar(spec, (barSpec) => ({
			...barSpec,
			transform: replaceTaggedTransform(barSpec.transform, "bar.addBarItems", {
				filter: `indexof(${JSON.stringify(nextVisibleItems)}, [datum[${JSON.stringify(xField)}], datum[${JSON.stringify(subField)}]]) >= 0`,
				_widgetvaTag: "bar.addBarItems"
			}),
			_bar_visibility_state: {
				mode: "item",
				x_field: xField,
				sub_field: subField,
				visible_items: nextVisibleItems,
				last_operation: mode === "add" ? "add" : "remove"
			}
		}));
	}
	function executeVegaLiteBarFilterSubcategories(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for bar subcategory filtering.");
		const subField = detectSubcategoryField(findRepresentativeSpec(spec, isBarFamilySpec) || spec, params.subField);
		const values = Array.isArray(params.subcategoriesToRemove) ? params.subcategoriesToRemove.filter((value) => value != null) : [];
		if (!subField || values.length === 0) throw new Error("bar.filterSubcategories requires subcategoriesToRemove and subField.");
		return updateBar(spec, (barSpec) => ({
			...barSpec,
			transform: replaceFilterTransformForField(barSpec.transform, subField, {
				filter: { not: {
					field: subField,
					oneOf: values
				} },
				_widgetvaTag: "bar.filterSubcategories"
			})
		}));
	}
	function executeVegaLiteBarExpandStack(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for stacked bar expansion.");
		const representative = findRepresentativeSpec(spec, isBarFamilySpec) || spec;
		const categoryField = detectBarCategoryField(representative, null);
		const subField = detectSubcategoryField(representative, null);
		if (!categoryField || !subField || params.category == null) throw new Error("bar.expandStack requires a category and stacked/grouped fields.");
		return updateBar(spec, (barSpec) => {
			const { valueChannel } = detectBarChannels(barSpec, null);
			return {
				...barSpec,
				transform: replaceTaggedTransform(barSpec.transform, "bar.expandStack", {
					filter: {
						field: categoryField,
						oneOf: [params.category]
					},
					_widgetvaTag: "bar.expandStack"
				}),
				encoding: {
					...barSpec.encoding || {},
					x: {
						...barSpec.encoding?.x || {},
						field: subField,
						type: "nominal"
					},
					xOffset: void 0,
					[valueChannel]: {
						...barSpec.encoding?.[valueChannel] || {},
						stack: null
					}
				},
				_bar_expand_stack_state: {
					category: params.category,
					category_field: categoryField,
					sub_field: subField
				}
			};
		});
	}
	function executeVegaLiteBarToggleStackMode(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for bar stack mode updates.");
		const mode = params.mode === "stacked" ? "stacked" : "grouped";
		const colorField = (findRepresentativeSpec(spec, isBarFamilySpec) || spec)?.encoding?.color?.field || null;
		if (!colorField) throw new Error("bar.toggleStackMode requires a color/subcategory field.");
		return updateBar(spec, (barSpec) => ({
			...barSpec,
			encoding: mode === "grouped" ? {
				...barSpec.encoding || {},
				xOffset: { field: colorField },
				y: {
					...barSpec.encoding?.y || {},
					stack: null
				}
			} : {
				...barSpec.encoding || {},
				xOffset: void 0,
				y: {
					...barSpec.encoding?.y || {},
					stack: true
				}
			},
			_bar_stack_mode_state: {
				mode,
				sub_field: colorField
			}
		}));
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/families/lineSpecActions.js
	function isLineFamilySpec(spec) {
		const markType = readMarkType(spec?.mark);
		return (markType === "line" || markType === "area") && spec?.encoding && typeof spec.encoding === "object" && !Array.isArray(spec.encoding);
	}
	function updateLine(spec, updater) {
		return updateRepresentativeSpec(spec, isLineFamilySpec, updater, "No representative line subview could be found in the active spec.");
	}
	function inferLineRootEncoding(spec) {
		return spec?.layer?.[0]?.encoding || spec?.encoding || {};
	}
	function inferRawTimeField(spec, rootEncoding, originalTransforms) {
		const taggedTimeUnit = (Array.isArray(originalTransforms) ? originalTransforms : []).find((transform) => transform && typeof transform === "object" && "timeUnit" in transform && typeof transform.field === "string");
		if (taggedTimeUnit?.field) return taggedTimeUnit.field;
		const xField = rootEncoding?.x?.field;
		if (typeof xField !== "string" || xField.length === 0) return null;
		if (!xField.includes("_")) return xField;
		const candidate = xField.split("_").at(-1);
		const rows = spec?.data?.values;
		if (Array.isArray(rows) && rows.length > 0 && candidate && Object.prototype.hasOwnProperty.call(rows[0], candidate)) return candidate;
		return xField;
	}
	function inferRawValueField(_spec, rootEncoding, originalTransforms) {
		const aggregateTransform = (Array.isArray(originalTransforms) ? originalTransforms : []).find((transform) => transform && typeof transform === "object" && Array.isArray(transform.aggregate) && transform.aggregate.length > 0);
		if (aggregateTransform?.aggregate?.[0]?.field) return aggregateTransform.aggregate[0].field;
		const yField = rootEncoding?.y?.field;
		if (typeof yField !== "string" || yField.length === 0) return null;
		if (yField.startsWith("total_")) return yField.slice(6);
		if (yField.startsWith("sum_")) return yField.slice(4);
		return yField;
	}
	var MONTH_MAP = {
		Jan: 0,
		Feb: 1,
		Mar: 2,
		Apr: 3,
		May: 4,
		Jun: 5,
		Jul: 6,
		Aug: 7,
		Sep: 8,
		Oct: 9,
		Nov: 10,
		Dec: 11,
		January: 0,
		February: 1,
		March: 2,
		April: 3,
		May: 4,
		June: 5,
		July: 6,
		August: 7,
		September: 8,
		October: 9,
		November: 10,
		December: 11
	};
	function normalizeTimeUnitValues(field, values, timeUnit) {
		const safeValues = Array.isArray(values) ? values.filter((value) => value != null) : [];
		if (!timeUnit) return {
			valueListExpr: valueList(safeValues),
			datumExpr: datumRef(field)
		};
		const normalizedTimeUnit = String(timeUnit).toLowerCase().trim();
		if (normalizedTimeUnit === "date") {
			const numericValues = safeValues.map((value) => Number.parseInt(value, 10)).filter(Number.isFinite);
			return {
				valueListExpr: valueList(numericValues.length > 0 ? numericValues : safeValues),
				datumExpr: `date(${datumRef(field)})`
			};
		}
		if (normalizedTimeUnit === "month") {
			const monthValues = safeValues.map((value) => {
				if (typeof value === "string" && Object.prototype.hasOwnProperty.call(MONTH_MAP, value)) return MONTH_MAP[value];
				const parsed = Number.parseInt(value, 10);
				return Number.isFinite(parsed) ? parsed - 1 : null;
			}).filter(Number.isFinite);
			return {
				valueListExpr: valueList(monthValues.length > 0 ? monthValues : safeValues),
				datumExpr: `month(${datumRef(field)})`
			};
		}
		if (normalizedTimeUnit === "year") {
			const numericValues = safeValues.map((value) => Number.parseInt(value, 10)).filter(Number.isFinite);
			return {
				valueListExpr: valueList(numericValues.length > 0 ? numericValues : safeValues),
				datumExpr: `year(${datumRef(field)})`
			};
		}
		return {
			valueListExpr: valueList(safeValues),
			datumExpr: `${normalizedTimeUnit}(${datumRef(field)})`
		};
	}
	function executeVegaLiteLineZoomXRegion(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for line x-domain updates.");
		const start = params.start ?? params.xMin ?? params.min;
		const end = params.end ?? params.xMax ?? params.max;
		if (start == null || end == null) throw new Error("line.zoomXRegion requires start and end.");
		return updateLine(spec, (lineSpec) => ({
			...lineSpec,
			mark: typeof lineSpec.mark === "string" ? {
				type: lineSpec.mark,
				clip: true
			} : {
				...lineSpec.mark || { type: "line" },
				clip: true
			},
			encoding: {
				...lineSpec.encoding || {},
				x: {
					...lineSpec.encoding?.x || {},
					scale: {
						...lineSpec.encoding?.x?.scale || {},
						domain: [start, end]
					}
				}
			},
			_line_zoom_state: {
				start,
				end
			}
		}));
	}
	function executeVegaLiteLineFocusLines(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for line focus updates.");
		const lines = Array.isArray(params.lines) ? params.lines.filter((line) => line != null) : [];
		if (lines.length === 0) throw new Error("line.focusLines requires lines.");
		return updateLine(spec, (lineSpec) => {
			const lineField = params.lineField || lineSpec.encoding?.color?.field || lineSpec.encoding?.detail?.field;
			if (!lineField) throw new Error("line.focusLines requires a lineField.");
			return {
				...lineSpec,
				encoding: {
					...lineSpec.encoding || {},
					opacity: {
						condition: {
							test: expressionEqualsAny(lineField, lines),
							value: 1
						},
						value: Number.isFinite(params.dimOpacity) ? params.dimOpacity : .12
					}
				},
				_line_focus_state: {
					lineField,
					lines
				}
			};
		});
	}
	function executeVegaLiteLineHighlightTrend(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for line trend updates.");
		return updateLine(spec, (lineSpec) => {
			const encoding = inferLineRootEncoding(lineSpec);
			const trendLayer = {
				_widgetvaTag: "line.highlightTrend",
				mark: {
					type: "line",
					color: "#d62728",
					strokeDash: [4, 3],
					strokeWidth: 2
				},
				transform: [{
					regression: encoding?.y?.field,
					on: encoding?.x?.field,
					method: params.method || "linear"
				}],
				encoding: {
					x: { ...encoding.x },
					y: { ...encoding.y }
				}
			};
			return {
				...lineSpec,
				layer: replaceTaggedLayer(Array.isArray(lineSpec.layer) ? lineSpec.layer : [{
					mark: lineSpec.mark || "line",
					encoding
				}], "line.highlightTrend", trendLayer),
				_line_trend_state: { trendType: params.trendType || params.method || "linear" }
			};
		});
	}
	function executeVegaLiteLineShowMovingAverage(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for line moving average updates.");
		return updateLine(spec, (lineSpec) => {
			const encoding = inferLineRootEncoding(lineSpec);
			const xField = encoding?.x?.field;
			const yField = encoding?.y?.field;
			if (!xField || !yField) throw new Error("line.showMovingAverage requires x and y encodings.");
			const windowSize = Number.isFinite(params.windowSize) ? Math.max(1, Math.floor(params.windowSize)) : 3;
			const movingAverageLayer = {
				_widgetvaTag: "line.showMovingAverage",
				mark: {
					type: "line",
					color: "#ff7f0e",
					strokeWidth: 2
				},
				transform: [{
					window: [{
						op: "mean",
						field: yField,
						as: "__widgetva_moving_average"
					}],
					frame: [1 - windowSize, 0],
					sort: [{ field: xField }],
					...encoding?.color?.field ? { groupby: [encoding.color.field] } : {}
				}],
				encoding: {
					x: { ...encoding.x },
					y: {
						...encoding.y,
						field: "__widgetva_moving_average"
					},
					...encoding?.color ? { color: { ...encoding.color } } : {}
				}
			};
			return {
				...lineSpec,
				layer: replaceTaggedLayer(Array.isArray(lineSpec.layer) ? lineSpec.layer : [{
					mark: lineSpec.mark || "line",
					encoding
				}], "line.showMovingAverage", movingAverageLayer),
				_line_moving_average_state: {
					sourceAction: "line.showMovingAverage",
					windowSize
				}
			};
		});
	}
	function executeVegaLiteLineDrilldown(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for line drill-down updates.");
		return updateLine(spec, (lineSpec) => {
			const encoding = inferLineRootEncoding(lineSpec);
			const transforms = Array.isArray(lineSpec.transform) ? lineSpec.transform : [];
			const rawTimeField = inferRawTimeField(lineSpec, encoding, transforms);
			const rawValueField = inferRawValueField(lineSpec, encoding, transforms);
			const level = typeof params.level === "string" ? params.level : "year";
			const value = params.value;
			if (!rawTimeField || !rawValueField || value == null) throw new Error("line.drillDownXAxis requires level, value, and temporal/value fields.");
			const { valueListExpr, datumExpr } = normalizeTimeUnitValues(rawTimeField, [value], level);
			return {
				...lineSpec,
				transform: replaceTaggedTransform(transforms, "line.drillDownXAxis", {
					filter: `indexof([${valueListExpr}], ${datumExpr}) >= 0`,
					_widgetvaTag: "line.drillDownXAxis"
				}),
				_line_drilldown_state: {
					sourceAction: "line.drillDownXAxis",
					level,
					value,
					rawTimeField,
					rawValueField
				}
			};
		});
	}
	function executeVegaLiteLineResetDrilldown(spec) {
		ensureObjectSpec(spec, "No active base spec is available for resetting line drill-down.");
		return updateLine(spec, (lineSpec) => {
			const { _line_drilldown_state: _removed, ...rest } = lineSpec;
			return {
				...rest,
				transform: replaceTaggedTransform(lineSpec.transform, "line.drillDownXAxis", null)
			};
		});
	}
	function executeVegaLiteLineResample(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for line resampling.");
		const granularity = typeof params.granularity === "string" ? params.granularity : "month";
		const aggregate = typeof params.agg === "string" ? params.agg : "mean";
		return updateLine(spec, (lineSpec) => {
			const encoding = inferLineRootEncoding(lineSpec);
			const transforms = Array.isArray(lineSpec.transform) ? lineSpec.transform : [];
			const rawTimeField = inferRawTimeField(lineSpec, encoding, transforms);
			const rawValueField = inferRawValueField(lineSpec, encoding, transforms);
			if (!rawTimeField || !rawValueField) throw new Error("line.resampleXAxis requires temporal/value fields.");
			const timeAs = `${granularity}_${rawTimeField}`;
			const valueAs = `${aggregate}_${rawValueField}`;
			return {
				...lineSpec,
				transform: replaceTaggedTransform(transforms, "line.resampleXAxis", {
					timeUnit: granularity,
					field: rawTimeField,
					as: timeAs,
					_widgetvaTag: "line.resampleXAxis"
				}).concat({
					aggregate: [{
						op: aggregate,
						field: rawValueField,
						as: valueAs
					}],
					groupby: [timeAs, ...encoding?.color?.field ? [encoding.color.field] : []],
					_widgetvaTag: "line.resampleXAxis.aggregate"
				}),
				encoding: {
					...lineSpec.encoding || {},
					x: {
						...encoding.x || {},
						field: timeAs,
						timeUnit: void 0
					},
					y: {
						...encoding.y || {},
						field: valueAs,
						aggregate: void 0
					}
				},
				_line_resample_state: {
					sourceAction: "line.resampleXAxis",
					granularity,
					aggregate,
					rawTimeField,
					rawValueField
				}
			};
		});
	}
	function executeVegaLiteLineResetResample(spec) {
		ensureObjectSpec(spec, "No active base spec is available for resetting line resample.");
		return updateLine(spec, (lineSpec) => {
			const { _line_resample_state: _removed, ...rest } = lineSpec;
			return {
				...rest,
				transform: (Array.isArray(lineSpec.transform) ? lineSpec.transform : []).filter((transform) => !String(transform?._widgetvaTag || "").startsWith("line.resampleXAxis"))
			};
		});
	}
	function executeVegaLiteLineBoldLines(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for line bolding.");
		return updateLine(spec, (lineSpec) => {
			const lineField = params.lineField || lineSpec.encoding?.color?.field || lineSpec.encoding?.detail?.field;
			const lineNames = Array.isArray(params.lineNames) ? params.lineNames.filter((name) => name != null) : [];
			if (!lineField || lineNames.length === 0) throw new Error("line.boldLines requires lineNames and lineField.");
			return {
				...lineSpec,
				encoding: {
					...lineSpec.encoding || {},
					strokeWidth: {
						condition: {
							test: expressionEqualsAny(lineField, lineNames),
							value: params.boldWidth || 4
						},
						value: params.baseWidth || 1
					}
				},
				_line_bold_state: {
					lineField,
					lineNames
				}
			};
		});
	}
	function executeVegaLiteLineFilterLines(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for line filtering.");
		return updateLine(spec, (lineSpec) => {
			const lineField = params.lineField || lineSpec.encoding?.color?.field || lineSpec.encoding?.detail?.field;
			const linesToRemove = Array.isArray(params.linesToRemove) ? params.linesToRemove.filter((line) => line != null) : [];
			if (!lineField || linesToRemove.length === 0) throw new Error("line.filterLines requires linesToRemove and lineField.");
			return {
				...lineSpec,
				transform: replaceFilterTransformForField(lineSpec.transform, lineField, {
					filter: expressionNotEqualsAny(lineField, linesToRemove),
					_widgetvaTag: "line.filterLines"
				}),
				_line_filter_state: {
					lineField,
					linesToRemove
				}
			};
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/families/parallelCoordinatesSpecActions.js
	function isParallelCoordinatesSpec(spec) {
		if (!spec || typeof spec !== "object" || Array.isArray(spec)) return false;
		const xField = (spec?.encoding)?.x?.field;
		const hasFoldTransform = (Array.isArray(spec?.transform) ? spec.transform : []).some((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
		return spec?.kind === "parallelCoordinates" || hasFoldTransform && Array.isArray(spec?.layer) || (readMarkType(spec?.mark) === "line" || hasFoldTransform || Array.isArray(spec?.layer)) && typeof xField === "string" && [
			"dimension",
			"key",
			"variable"
		].includes(xField);
	}
	function updateParallel(spec, updater) {
		return updateRepresentativeSpec(spec, isParallelCoordinatesSpec, updater, "No representative parallel-coordinates subview could be found in the active spec.");
	}
	function updateParallelXOrder(node, dimensionOrder) {
		if (!node || typeof node !== "object") return;
		if (Array.isArray(node)) {
			for (const entry of node) updateParallelXOrder(entry, dimensionOrder);
			return;
		}
		if (node.encoding && typeof node.encoding === "object") {
			const xEncoding = node.encoding.x;
			if (xEncoding && typeof xEncoding === "object" && [
				"dimension",
				"key",
				"variable"
			].includes(xEncoding.field)) node.encoding.x = {
				...xEncoding,
				sort: dimensionOrder,
				scale: {
					...xEncoding.scale || {},
					domain: dimensionOrder
				}
			};
		}
		for (const value of Object.values(node)) updateParallelXOrder(value, dimensionOrder);
	}
	function findFoldTransform(transforms = []) {
		return (Array.isArray(transforms) ? transforms : []).find((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold)) || null;
	}
	function readDimensionOrder(spec) {
		const sort = spec?.encoding?.x?.sort;
		if (Array.isArray(sort) && sort.length > 0) return sort.filter((dimension) => typeof dimension === "string");
		const domain = spec?.encoding?.x?.scale?.domain;
		if (Array.isArray(domain) && domain.length > 0) return domain.filter((dimension) => typeof dimension === "string");
		const fold = findFoldTransform(spec?.transform);
		if (Array.isArray(fold?.fold) && fold.fold.length > 0) return fold.fold.filter((dimension) => typeof dimension === "string");
		const rows = Array.isArray(spec?.data?.values) ? spec.data.values : [];
		return [...new Set(rows.map((row) => row?.dimension).filter((dimension) => typeof dimension === "string"))];
	}
	var DIMENSION_VISIBILITY_TAG = "parallelCoordinates.dimensionVisibility";
	function dimensionVisibilityFilter(visibleDimensions) {
		const dimensionField = visibleDimensions?.field || "dimension";
		return {
			filter: `indexof(${JSON.stringify(Array.isArray(visibleDimensions) ? visibleDimensions : [])}, datum.${dimensionField}) >= 0`,
			_widgetvaTag: DIMENSION_VISIBILITY_TAG
		};
	}
	function rewriteDimensionVisibilityFilter(node, visibleDimensions = null, dimensionField = "dimension") {
		if (!node || typeof node !== "object") return node;
		if (Array.isArray(node)) return node.map((entry) => rewriteDimensionVisibilityFilter(entry, visibleDimensions, dimensionField));
		const next = { ...node };
		const hasDimensionRows = Array.isArray(next?.data?.values) && next.data.values.some((row) => typeof row?.[dimensionField] === "string");
		const usesDimensionX = next?.encoding?.x?.field === dimensionField;
		if (hasDimensionRows || usesDimensionX) next.transform = replaceTaggedTransform(next.transform, DIMENSION_VISIBILITY_TAG, visibleDimensions ? dimensionVisibilityFilter(Object.assign([...visibleDimensions], { field: dimensionField })) : null);
		for (const key of [
			"layer",
			"vconcat",
			"hconcat",
			"concat"
		]) if (Array.isArray(next[key])) next[key] = next[key].map((entry) => rewriteDimensionVisibilityFilter(entry, visibleDimensions, dimensionField));
		if (next.spec && typeof next.spec === "object" && !Array.isArray(next.spec)) next.spec = rewriteDimensionVisibilityFilter(next.spec, visibleDimensions, dimensionField);
		return next;
	}
	function executeVegaLiteParallelReorderDimensions(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for parallel-coordinate reordering.");
		const dimensions = Array.isArray(params.dimensions) ? params.dimensions.filter((dimension) => typeof dimension === "string" && dimension.trim()) : Array.isArray(params.dimensionOrder) ? params.dimensionOrder.filter((dimension) => typeof dimension === "string" && dimension.trim()) : [];
		if (dimensions.length === 0) throw new Error("parallelCoordinates.reorderDimensions requires dimensions.");
		return updateParallel(spec, (parallelSpec) => {
			const nextSpec = {
				...parallelSpec,
				transform: Array.isArray(parallelSpec.transform) ? [...parallelSpec.transform] : []
			};
			const foldIndex = nextSpec.transform.findIndex((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
			if (foldIndex >= 0) nextSpec.transform[foldIndex] = {
				...nextSpec.transform[foldIndex],
				fold: dimensions
			};
			updateParallelXOrder(nextSpec, dimensions);
			nextSpec._pc_reencode_state = {
				mode: "dimensionOrder",
				dimensions
			};
			return nextSpec;
		});
	}
	function executeVegaLiteParallelFilterDimension(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for parallel-coordinate dimension filtering.");
		const dimension = typeof params.dimension === "string" ? params.dimension : null;
		const range = Array.isArray(params.range) ? params.range : null;
		if (!dimension || !range || range.length !== 2) throw new Error("parallelCoordinates.filterDimension requires dimension and range.");
		return updateParallel(spec, (parallelSpec) => {
			const transforms = Array.isArray(parallelSpec.transform) ? [...parallelSpec.transform] : [];
			const filterTransform = {
				filter: `${datumRef("dimension")} === ${JSON.stringify(dimension)} ? (${datumRef("value")} >= ${Math.min(...range)} && ${datumRef("value")} <= ${Math.max(...range)}) : true`,
				_widgetvaTag: "parallelCoordinates.filterDimension"
			};
			const nextTransforms = transforms.filter((transform) => transform?._widgetvaTag !== "parallelCoordinates.filterDimension");
			const foldIndex = nextTransforms.findIndex((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
			nextTransforms.splice(foldIndex >= 0 ? foldIndex + 1 : 0, 0, filterTransform);
			return {
				...parallelSpec,
				transform: nextTransforms
			};
		});
	}
	function executeVegaLiteParallelFilterByCategory(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for parallel-category filtering.");
		const field = typeof params.field === "string" ? params.field : null;
		const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
		if (!field || values.length === 0) throw new Error("parallelCoordinates.filterByCategory requires field and values.");
		return updateParallel(spec, (parallelSpec) => ({
			...parallelSpec,
			transform: replaceFilterTransformForField(parallelSpec.transform, field, {
				filter: { not: {
					field,
					oneOf: values
				} },
				_widgetvaTag: "parallelCoordinates.filterByCategory"
			})
		}));
	}
	function executeVegaLiteParallelHighlightCategory(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for parallel-category highlighting.");
		const field = typeof params.field === "string" ? params.field : null;
		const values = Array.isArray(params.values) ? params.values.filter((value) => value != null) : [];
		if (!field || values.length === 0) throw new Error("parallelCoordinates.highlightCategory requires field and values.");
		return updateParallel(spec, (parallelSpec) => ({
			...parallelSpec,
			encoding: {
				...parallelSpec.encoding || {},
				opacity: {
					condition: {
						test: expressionEqualsAny(field, values),
						value: 1
					},
					value: .1
				}
			},
			_pc_highlight_state: {
				field,
				values
			}
		}));
	}
	function executeVegaLiteParallelHideDimensions(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for hiding parallel-coordinate dimensions.");
		const dimensions = Array.isArray(params.dimensions) ? params.dimensions.filter((dimension) => typeof dimension === "string" && dimension.trim()) : [];
		const mode = params.mode === "show" ? "show" : "hide";
		if (dimensions.length === 0) throw new Error("parallelCoordinates.hideDimensions requires dimensions.");
		return updateParallel(spec, (parallelSpec) => {
			const transforms = Array.isArray(parallelSpec.transform) ? [...parallelSpec.transform] : [];
			const fold = findFoldTransform(transforms);
			const storedState = parallelSpec._pc_hidden_state && typeof parallelSpec._pc_hidden_state === "object" ? parallelSpec._pc_hidden_state : {};
			const allDimensions = Array.isArray(storedState.all_dimensions) && storedState.all_dimensions.length > 0 ? storedState.all_dimensions : fold?.fold || readDimensionOrder(parallelSpec);
			const hiddenSet = new Set(Array.isArray(storedState.hidden) ? storedState.hidden : []);
			for (const dimension of dimensions) if (mode === "hide") hiddenSet.add(dimension);
			else hiddenSet.delete(dimension);
			const visibleDimensions = allDimensions.filter((dimension) => !hiddenSet.has(dimension));
			const foldIndex = transforms.findIndex((transform) => transform === fold);
			if (foldIndex >= 0) transforms[foldIndex] = {
				...transforms[foldIndex],
				fold: visibleDimensions
			};
			let nextSpec = {
				...parallelSpec,
				transform: transforms,
				_pc_hidden_state: {
					hidden: allDimensions.filter((dimension) => hiddenSet.has(dimension)),
					all_dimensions: allDimensions
				},
				_pc_reencode_state: {
					mode: "dimensionVisibility",
					operation: mode,
					visible_dimensions: visibleDimensions
				}
			};
			updateParallelXOrder(nextSpec, visibleDimensions);
			if (!fold) {
				const dimensionField = parallelSpec?.encoding?.x?.field || "dimension";
				nextSpec = rewriteDimensionVisibilityFilter(nextSpec, visibleDimensions, dimensionField);
			}
			return nextSpec;
		});
	}
	function executeVegaLiteParallelResetHiddenDimensions(spec) {
		ensureObjectSpec(spec, "No active base spec is available for resetting hidden parallel-coordinate dimensions.");
		return updateParallel(spec, (parallelSpec) => {
			const hiddenState = parallelSpec._pc_hidden_state && typeof parallelSpec._pc_hidden_state === "object" ? parallelSpec._pc_hidden_state : null;
			const allDimensions = Array.isArray(hiddenState?.all_dimensions) && hiddenState.all_dimensions.length > 0 ? hiddenState.all_dimensions : readDimensionOrder(parallelSpec);
			const transforms = Array.isArray(parallelSpec.transform) ? [...parallelSpec.transform] : [];
			const foldIndex = transforms.findIndex((transform) => transform && typeof transform === "object" && Array.isArray(transform.fold));
			if (foldIndex >= 0 && allDimensions.length > 0) transforms[foldIndex] = {
				...transforms[foldIndex],
				fold: allDimensions
			};
			const { _pc_hidden_state: _removed, ...rest } = parallelSpec;
			let nextSpec = {
				...rest,
				transform: transforms,
				_pc_reencode_state: {
					mode: "dimensionVisibility",
					operation: "reset",
					hidden_dimensions: [],
					visible_dimensions: allDimensions
				}
			};
			if (allDimensions.length > 0) updateParallelXOrder(nextSpec, allDimensions);
			nextSpec = rewriteDimensionVisibilityFilter(nextSpec, null, parallelSpec?.encoding?.x?.field || "dimension");
			return nextSpec;
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/families/heatmapSpecActions.js
	function isHeatmapFamilySpec(spec) {
		return readMarkType(spec?.mark) === "rect" && spec?.encoding && typeof spec.encoding === "object" && !Array.isArray(spec.encoding);
	}
	function updateHeatmap(spec, updater) {
		return updateRepresentativeSpec(spec, isHeatmapFamilySpec, updater, "No representative heatmap subview could be found in the active spec.");
	}
	function resolveHeatmapAxisFieldAlias(spec, requestedField, axis) {
		if (typeof requestedField !== "string" || requestedField.length === 0) return null;
		const encodingField = typeof spec?.encoding?.[axis]?.field === "string" ? spec.encoding[axis].field : null;
		if (!encodingField) return requestedField;
		if (requestedField === axis || requestedField === encodingField) return encodingField;
		return requestedField;
	}
	function readAxisFields(heatmapSpec) {
		return {
			xField: heatmapSpec?.encoding?.x?.field || null,
			yField: heatmapSpec?.encoding?.y?.field || null,
			colorField: heatmapSpec?.encoding?.color?.field || null
		};
	}
	function executeVegaLiteHeatmapFilterCells(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap filtering.");
		return updateHeatmap(spec, (heatmapSpec) => {
			const { xField, yField } = readAxisFields(heatmapSpec);
			const xValues = Array.isArray(params.xValues) ? params.xValues.filter((value) => value != null) : [];
			const yValues = Array.isArray(params.yValues) ? params.yValues.filter((value) => value != null) : [];
			const nextTransforms = (Array.isArray(heatmapSpec.transform) ? heatmapSpec.transform : []).filter((transform) => transform?._widgetvaTag !== "heatmap.filterCells");
			if (xField && xValues.length > 0) nextTransforms.push({
				filter: {
					field: xField,
					oneOf: xValues
				},
				_widgetvaTag: "heatmap.filterCells"
			});
			if (yField && yValues.length > 0) nextTransforms.push({
				filter: {
					field: yField,
					oneOf: yValues
				},
				_widgetvaTag: "heatmap.filterCells"
			});
			return {
				...heatmapSpec,
				transform: nextTransforms
			};
		});
	}
	function executeVegaLiteHeatmapDrilldownAxis(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap drill-down.");
		return updateHeatmap(spec, (heatmapSpec) => {
			const axis = params.axis === "y" ? "y" : "x";
			const field = resolveHeatmapAxisFieldAlias(heatmapSpec, params.field || axis, axis);
			const value = params.value;
			if (!field || value == null) throw new Error("heatmap.drilldownAxis requires field/axis and value.");
			return {
				...heatmapSpec,
				transform: replaceFilterTransformForField(heatmapSpec.transform, field, {
					filter: {
						field,
						oneOf: [value]
					},
					_widgetvaTag: "heatmap.drilldownAxis"
				}),
				_heatmap_drilldown_state: {
					axis,
					field,
					value
				}
			};
		});
	}
	function executeVegaLiteHeatmapResetDrilldown(spec) {
		ensureObjectSpec(spec, "No active base spec is available for resetting heatmap drill-down.");
		return updateHeatmap(spec, (heatmapSpec) => {
			const field = heatmapSpec._heatmap_drilldown_state?.field || heatmapSpec.encoding?.x?.field || heatmapSpec.encoding?.y?.field;
			const { _heatmap_drilldown_state: _removed, ...rest } = heatmapSpec;
			return {
				...rest,
				transform: field ? replaceFilterTransformForField(heatmapSpec.transform, field, null) : heatmapSpec.transform
			};
		});
	}
	function executeVegaLiteHeatmapAddMarginalBars(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap marginal bars.");
		return updateHeatmap(spec, (heatmapSpec) => {
			const { xField, yField, colorField } = readAxisFields(heatmapSpec);
			if (!xField || !yField || !colorField) throw new Error("heatmap.addMarginalBars requires x, y, and color fields.");
			const op = params.op || params.aggregate || "sum";
			const barSize = Number.isFinite(params.barSize) ? params.barSize : 70;
			const barColor = params.barColor || "#666666";
			const showTop = params.showTop !== false;
			const showRight = params.showRight !== false;
			const baseHeatmap = {
				...heatmapSpec.data ? { data: heatmapSpec.data } : {},
				mark: heatmapSpec.mark || "rect",
				encoding: heatmapSpec.encoding,
				...Array.isArray(heatmapSpec.transform) ? { transform: heatmapSpec.transform } : {},
				...heatmapSpec.width ? { width: heatmapSpec.width } : {},
				...heatmapSpec.height ? { height: heatmapSpec.height } : {}
			};
			const topBar = {
				_widgetvaTag: "heatmap.addMarginalBars.top",
				...heatmapSpec.data ? { data: heatmapSpec.data } : {},
				mark: {
					type: "bar",
					color: barColor
				},
				height: barSize,
				encoding: {
					x: {
						...heatmapSpec.encoding?.x || {},
						axis: null
					},
					y: {
						aggregate: op,
						field: colorField,
						type: "quantitative",
						title: `${op} of ${colorField}`
					}
				}
			};
			const rightBar = {
				_widgetvaTag: "heatmap.addMarginalBars.right",
				...heatmapSpec.data ? { data: heatmapSpec.data } : {},
				mark: {
					type: "bar",
					color: barColor
				},
				width: barSize,
				encoding: {
					y: {
						...heatmapSpec.encoding?.y || {},
						axis: null
					},
					x: {
						aggregate: op,
						field: colorField,
						type: "quantitative",
						title: `${op} of ${colorField}`
					}
				}
			};
			const body = showRight ? {
				hconcat: [baseHeatmap, rightBar],
				spacing: 8
			} : baseHeatmap;
			return {
				...showTop ? {
					vconcat: [topBar, body],
					spacing: 8
				} : body,
				...heatmapSpec.$schema ? { $schema: heatmapSpec.$schema } : {},
				...heatmapSpec.title ? { title: heatmapSpec.title } : {},
				_heatmap_marginal_state: {
					showTop,
					showRight,
					op
				}
			};
		});
	}
	function executeVegaLiteHeatmapHighlightRegion(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap region highlighting.");
		return updateHeatmap(spec, (heatmapSpec) => {
			const { xField, yField } = readAxisFields(heatmapSpec);
			const xValues = Array.isArray(params.xValues) ? params.xValues.filter((value) => value != null) : [];
			const yValues = Array.isArray(params.yValues) ? params.yValues.filter((value) => value != null) : [];
			const tests = [];
			if (xField && xValues.length > 0) tests.push(expressionEqualsAny(xField, xValues));
			if (yField && yValues.length > 0) tests.push(expressionEqualsAny(yField, yValues));
			if (tests.length === 0) throw new Error("heatmap.highlightRegion requires xValues or yValues.");
			return {
				...heatmapSpec,
				encoding: {
					...heatmapSpec.encoding || {},
					opacity: {
						condition: {
							test: tests.join(" && "),
							value: 1
						},
						value: .2
					}
				},
				_heatmap_highlight_state: {
					xValues,
					yValues
				}
			};
		});
	}
	function executeVegaLiteHeatmapAdjustColorScale(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap color scale updates.");
		const domain = Array.isArray(params.domain) ? params.domain : [params.min, params.max].filter((value) => value != null);
		return updateHeatmap(spec, (heatmapSpec) => ({
			...heatmapSpec,
			encoding: {
				...heatmapSpec.encoding || {},
				color: {
					...heatmapSpec.encoding?.color || {},
					scale: {
						...heatmapSpec.encoding?.color?.scale || {},
						...domain.length === 2 ? { domain } : {},
						...params.scheme ? { scheme: params.scheme } : {}
					}
				}
			}
		}));
	}
	function executeVegaLiteHeatmapThresholdMask(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap threshold mask.");
		return updateHeatmap(spec, (heatmapSpec) => {
			const { colorField } = readAxisFields(heatmapSpec);
			const hasMin = params.minValue != null && params.minValue !== "";
			const hasMax = params.maxValue != null && params.maxValue !== "";
			const minValue = hasMin ? Number(params.minValue) : null;
			const maxValue = hasMax ? Number(params.maxValue) : null;
			if (colorField && (hasMin && Number.isFinite(minValue) || hasMax && Number.isFinite(maxValue))) {
				const tests = [...Number.isFinite(minValue) ? [`${datumRef(colorField)} >= ${minValue}`] : [], ...Number.isFinite(maxValue) ? [`${datumRef(colorField)} <= ${maxValue}`] : []];
				return {
					...heatmapSpec,
					encoding: {
						...heatmapSpec.encoding || {},
						opacity: {
							condition: {
								test: tests.join(" && "),
								value: 1
							},
							value: Number.isFinite(params.outsideOpacity) ? params.outsideOpacity : .1
						}
					},
					_heatmap_threshold_state: {
						colorField,
						minValue,
						maxValue,
						mode: "range"
					}
				};
			}
			const threshold = Number(params.threshold);
			if (!colorField || !Number.isFinite(threshold)) throw new Error("heatmap.thresholdMask requires threshold/range and color field.");
			const op = params.mode === "below" ? ">=" : "<=";
			return {
				...heatmapSpec,
				transform: replaceFilterTransformForField(heatmapSpec.transform, colorField, {
					filter: `datum[${JSON.stringify(colorField)}] ${op} ${threshold}`,
					_widgetvaTag: "heatmap.thresholdMask"
				}),
				_heatmap_threshold_state: {
					colorField,
					threshold,
					mode: params.mode || "above"
				}
			};
		});
	}
	function executeVegaLiteHeatmapFilterCellsByRegion(spec, params = {}) {
		return executeVegaLiteHeatmapFilterCells(spec, {
			xValues: params.xValues || params.columns || params.x,
			yValues: params.yValues || params.rows || params.y
		});
	}
	function executeVegaLiteHeatmapHighlightRegionByValue(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap value highlighting.");
		return updateHeatmap(spec, (heatmapSpec) => {
			const { colorField } = readAxisFields(heatmapSpec);
			const hasMin = params.minValue != null && params.minValue !== "";
			const hasMax = params.maxValue != null && params.maxValue !== "";
			const minValue = hasMin ? Number(params.minValue) : null;
			const maxValue = hasMax ? Number(params.maxValue) : null;
			if (!colorField || hasMin && !Number.isFinite(minValue) || hasMax && !Number.isFinite(maxValue) || !hasMin && !hasMax) throw new Error("heatmap.highlightRegionByValue requires minValue and/or maxValue plus a color field.");
			const tests = [...Number.isFinite(minValue) ? [`${datumRef(colorField)} >= ${minValue}`] : [], ...Number.isFinite(maxValue) ? [`${datumRef(colorField)} <= ${maxValue}`] : []];
			return {
				...heatmapSpec,
				encoding: {
					...heatmapSpec.encoding || {},
					opacity: {
						condition: {
							test: tests.join(" && "),
							value: 1
						},
						value: Number.isFinite(params.outsideOpacity) ? params.outsideOpacity : .12
					}
				},
				_heatmap_value_highlight_state: {
					colorField,
					minValue,
					maxValue
				}
			};
		});
	}
	function executeVegaLiteHeatmapClusterRowsCols(spec, params = {}) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap clustering.");
		return updateHeatmap(spec, (heatmapSpec) => ({
			...heatmapSpec,
			encoding: {
				...heatmapSpec.encoding || {},
				x: {
					...heatmapSpec.encoding?.x || {},
					sort: params.xOrder || params.columnOrder || void 0
				},
				y: {
					...heatmapSpec.encoding?.y || {},
					sort: params.yOrder || params.rowOrder || void 0
				}
			},
			_heatmap_cluster_state: {
				xOrder: params.xOrder || params.columnOrder || null,
				yOrder: params.yOrder || params.rowOrder || null
			}
		}));
	}
	function executeVegaLiteHeatmapTranspose(spec) {
		ensureObjectSpec(spec, "No active base spec is available for heatmap transpose.");
		return updateHeatmap(spec, (heatmapSpec) => ({
			...heatmapSpec,
			encoding: {
				...heatmapSpec.encoding || {},
				x: heatmapSpec.encoding?.y,
				y: heatmapSpec.encoding?.x
			},
			_heatmap_transpose_state: { transposed: !heatmapSpec._heatmap_transpose_state?.transposed }
		}));
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/families/sankeySpecActions.js
	function findNamedDataSource(spec, name) {
		const data = Array.isArray(spec?.data) ? spec.data : [];
		const index = data.findIndex((entry) => entry && typeof entry === "object" && entry.name === name);
		if (index < 0) return {
			index: -1,
			values: null
		};
		return {
			index,
			values: Array.isArray(data[index]?.values) ? data[index].values : null
		};
	}
	function findNamedMark(spec, name) {
		const marks = Array.isArray(spec?.marks) ? spec.marks : [];
		for (const mark of marks) {
			if (mark?.name === name) return mark;
			if (mark?.type === "group" && Array.isArray(mark.marks)) {
				const nested = mark.marks.find((entry) => entry?.name === name);
				if (nested) return nested;
			}
		}
		return null;
	}
	function setSignalValue(spec, name, value) {
		const signals = Array.isArray(spec?.signals) ? [...spec.signals] : [];
		const index = signals.findIndex((entry) => entry && typeof entry === "object" && entry.name === name);
		if (index >= 0) {
			signals[index] = {
				...signals[index],
				value
			};
			return {
				...spec,
				signals
			};
		}
		return {
			...spec,
			signals: [...signals, {
				name,
				value
			}]
		};
	}
	function computeNodeFlows(links) {
		const flows = /* @__PURE__ */ new Map();
		const ensure = (name) => {
			if (!flows.has(name)) flows.set(name, {
				inflow: 0,
				outflow: 0,
				total: 0
			});
			return flows.get(name);
		};
		for (const link of Array.isArray(links) ? links : []) {
			if (!link || typeof link !== "object") continue;
			const value = Number(link.value || 0);
			ensure(link.source).outflow += value;
			ensure(link.target).inflow += value;
		}
		for (const entry of flows.values()) entry.total = Math.max(entry.inflow, entry.outflow);
		return flows;
	}
	function cloneFirstAvailableDataWithValues(spec, sourceNames, updateValues) {
		for (const sourceName of sourceNames) {
			const { index, values } = findNamedDataSource(spec, sourceName);
			if (index >= 0 && Array.isArray(values)) {
				const nextData = [...spec.data];
				nextData[index] = {
					...nextData[index],
					values: updateValues(values)
				};
				return {
					...spec,
					data: nextData
				};
			}
		}
		throw new Error(`Sankey source data "${sourceNames.join("\" or \"")}" is not available.`);
	}
	function readLinks(spec) {
		return findNamedDataSource(spec, "rawLinks").values || findNamedDataSource(spec, "links").values || findNamedDataSource(spec, "link-data").values || findNamedDataSource(spec, "filteredLinks").values || findNamedDataSource(spec, "edges").values || [];
	}
	function readNodes(spec) {
		return findNamedDataSource(spec, "nodeConfig").values || findNamedDataSource(spec, "nodes").values || findNamedDataSource(spec, "node-data").values || [];
	}
	function updateNodeConfig(spec, updateValues) {
		return cloneFirstAvailableDataWithValues(spec, [
			"nodeConfig",
			"nodes",
			"node-data"
		], updateValues);
	}
	function nodeName(row) {
		return row?.name || row?.id || row?.label || null;
	}
	function nodeDepth(row) {
		const depth = Number(row?.depth);
		return Number.isFinite(depth) ? depth : null;
	}
	function withCollapsedNodeConfig(spec, nodes, collapsedName) {
		const collapsedSet = new Set(nodes);
		return updateNodeConfig(spec, (rows) => {
			const collapsedRows = rows.filter((row) => collapsedSet.has(nodeName(row)));
			if (collapsedRows.length === 0) return rows;
			const depth = nodeDepth(collapsedRows[0]) ?? 0;
			const order = Math.min(...collapsedRows.map((row) => Number(row?.order)).filter(Number.isFinite), 0);
			return [...rows.filter((row) => !collapsedSet.has(nodeName(row)) && nodeName(row) !== collapsedName), {
				...collapsedRows[0],
				name: collapsedName,
				id: collapsedName,
				label: collapsedName,
				depth,
				order,
				collapsed: nodes
			}];
		});
	}
	function executeVegaSankeyFilterFlow(spec, params = {}) {
		ensureObjectSpec(spec, "No active Vega spec is available for sankey flow filtering.");
		const source = params.source ?? params.sourceNode ?? null;
		const target = params.target ?? params.targetNode ?? null;
		const minValue = Number.isFinite(params.minValue) ? params.minValue : null;
		return cloneFirstAvailableDataWithValues(spec, [
			"links",
			"rawLinks",
			"filteredLinks"
		], (links) => links.filter((link) => {
			if (source != null && link.source !== source) return false;
			if (target != null && link.target !== target) return false;
			if (minValue != null && Number(link.value || 0) < minValue) return false;
			return true;
		}));
	}
	function executeVegaSankeyCollapseNodes(spec, params = {}) {
		ensureObjectSpec(spec, "No active Vega spec is available for sankey node collapse.");
		const nodes = Array.isArray(params.nodes) ? params.nodes.filter((node) => node != null) : [];
		if (nodes.length === 0) throw new Error("sankey.collapseNodes requires nodes.");
		const collapsedName = params.collapsedName || params.aggregateName || params.groupName || nodes.join("+");
		const collapsedSet = new Set(nodes);
		let nextSpec = cloneFirstAvailableDataWithValues(spec, [
			"rawLinks",
			"links",
			"filteredLinks"
		], (links) => links.map((link) => ({
			...link,
			source: collapsedSet.has(link.source) ? collapsedName : link.source,
			target: collapsedSet.has(link.target) ? collapsedName : link.target
		})).filter((link) => link.source !== link.target));
		try {
			nextSpec = withCollapsedNodeConfig(nextSpec, nodes, collapsedName);
			return setSignalValue(nextSpec, "selectedNode", collapsedName);
		} catch {
			return {
				...nextSpec,
				_sankey_collapse_state: {
					nodes,
					collapsedName
				}
			};
		}
	}
	function executeVegaSankeyExpandNode(spec, params = {}) {
		ensureObjectSpec(spec, "No active Vega spec is available for sankey node expansion.");
		const node = params.node || params.collapsedName || null;
		if (!node) throw new Error("sankey.expandNode requires a node.");
		return {
			...spec,
			_sankey_expand_state: { node },
			_sankey_collapse_state: null
		};
	}
	function executeVegaSankeyHighlightPath(spec, params = {}) {
		ensureObjectSpec(spec, "No active Vega spec is available for sankey path highlighting.");
		const nodes = Array.isArray(params.nodes) ? params.nodes.filter((node) => node != null) : [];
		const links = Array.isArray(params.links) ? params.links : [];
		if (nodes.length === 0 && links.length === 0) throw new Error("sankey.highlightPath requires nodes or links.");
		return {
			...spec,
			_sankey_highlight_state: {
				nodes,
				links
			}
		};
	}
	function executeVegaSankeyTraceNode(spec, params = {}) {
		ensureObjectSpec(spec, "No active Vega spec is available for sankey node tracing.");
		const node = params.node || params.nodeName || params.nodeId || null;
		if (!node) throw new Error("sankey.traceNode requires a node.");
		const reachableLinks = readLinks(spec).filter((link) => link.source === node || link.target === node);
		return setSignalValue({
			...spec,
			_sankey_trace_state: {
				node,
				links: reachableLinks
			}
		}, "selectedNode", node);
	}
	function executeVegaSankeyColorFlows(spec, params = {}) {
		ensureObjectSpec(spec, "No active Vega spec is available for sankey flow coloring.");
		const nodes = Array.isArray(params.nodes) ? params.nodes.filter((node) => node != null) : [];
		const color = params.color || "#e74c3c";
		const field = params.field || "source";
		const scheme = params.scheme || "category10";
		const linkMark = findNamedMark(spec, "edgeMark") || findNamedMark(spec, "links") || findNamedMark(spec, "link") || null;
		const nextSpec = {
			...spec,
			marks: Array.isArray(spec.marks) ? spec.marks.map((mark) => ({ ...mark })) : spec.marks
		};
		const nextLinkMark = findNamedMark(nextSpec, "edgeMark") || findNamedMark(nextSpec, "links") || findNamedMark(nextSpec, "link") || null;
		const highlightExpr = nodes.length > 0 ? `indexof(${JSON.stringify(nodes)}, datum.source) >= 0 || indexof(${JSON.stringify(nodes)}, datum.target) >= 0` : "false";
		const fallbackFill = linkMark?.encode?.update?.fill || {
			scale: "color",
			field
		};
		if (linkMark) nextLinkMark.encode = {
			...nextLinkMark.encode || {},
			update: {
				...nextLinkMark.encode?.update || {},
				fill: { signal: `${highlightExpr} ? ${JSON.stringify(color)} : (${fallbackFill.signal || `scale('color', datum.${field})`})` },
				stroke: { signal: `${highlightExpr} ? ${JSON.stringify(color)} : (${fallbackFill.signal || `scale('color', datum.${field})`})` }
			}
		};
		return {
			...nextSpec,
			scales: [...Array.isArray(spec.scales) ? spec.scales : []],
			_sankey_color_state: {
				field,
				scheme,
				nodes,
				color
			}
		};
	}
	function executeVegaSankeyReorderNodesInLayer(spec, params = {}) {
		ensureObjectSpec(spec, "No active Vega spec is available for sankey node reordering.");
		const layer = params.layer ?? params.rank ?? params.depth ?? null;
		const order = Array.isArray(params.order) ? params.order.filter((node) => node != null) : [];
		if (layer == null || order.length === 0) throw new Error("sankey.reorderNodesInLayer requires layer and order.");
		return updateNodeConfig(spec, (rows) => rows.map((row) => {
			if (nodeDepth(row) !== Number(layer)) return row;
			const index = order.indexOf(nodeName(row));
			return index >= 0 ? {
				...row,
				order: index
			} : row;
		}));
	}
	function executeVegaSankeyAutoCollapseByRank(spec, params = {}) {
		ensureObjectSpec(spec, "No active Vega spec is available for sankey auto-collapse.");
		const requestedMaxNodes = Number.isFinite(params.maxNodes) ? params.maxNodes : params.topN;
		const topN = Number.isFinite(requestedMaxNodes) ? Math.max(1, Math.floor(requestedMaxNodes)) : 8;
		const flows = computeNodeFlows(readLinks(spec));
		const configRows = readNodes(spec);
		const keepSet = /* @__PURE__ */ new Set();
		const collapseByNode = /* @__PURE__ */ new Map();
		const rowsByDepth = /* @__PURE__ */ new Map();
		for (const row of configRows) {
			const depth = nodeDepth(row);
			if (depth == null) continue;
			const bucket = rowsByDepth.get(depth) || [];
			bucket.push(row);
			rowsByDepth.set(depth, bucket);
		}
		for (const [depth, rows] of rowsByDepth.entries()) {
			const sorted = [...rows].sort((left, right) => (flows.get(nodeName(right))?.total || 0) - (flows.get(nodeName(left))?.total || 0));
			for (const row of sorted.slice(0, topN)) keepSet.add(nodeName(row));
			for (const row of sorted.slice(topN)) collapseByNode.set(nodeName(row), `Other ${depth}`);
		}
		let nextSpec = cloneFirstAvailableDataWithValues(spec, [
			"rawLinks",
			"links",
			"filteredLinks"
		], (sourceLinks) => sourceLinks.map((link) => ({
			...link,
			source: collapseByNode.get(link.source) || link.source,
			target: collapseByNode.get(link.target) || link.target
		})).filter((link) => link.source !== link.target));
		nextSpec = updateNodeConfig(nextSpec, (rows) => {
			const keptRows = rows.filter((row) => keepSet.has(nodeName(row)));
			const otherRows = [...new Set([...collapseByNode.values()])].map((name) => {
				const depth = Number(String(name).replace(/^Other\s+/, ""));
				return {
					...rows.find((row) => nodeDepth(row) === depth) || {},
					name,
					id: name,
					label: name,
					depth,
					order: topN
				};
			});
			return [...keptRows, ...otherRows];
		});
		return {
			...nextSpec,
			_sankey_auto_collapse_state: {
				topN,
				keepNodes: [...keepSet],
				collapsedNodes: [...collapseByNode.keys()]
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vegaLite/vegaLiteSpecActionExecutor.js
	function executeVegaLiteSpecAction({ actionName = null, spec = null, params = {} } = {}) {
		switch (actionName) {
			case "widget.aggregateData": return {
				handled: true,
				nextSpec: executeVegaLiteAggregateData(spec, params)
			};
			case "widget.filterByValues": return {
				handled: true,
				nextSpec: executeVegaLiteFilterByValues(spec, params)
			};
			case "widget.filterByRange": return {
				handled: true,
				nextSpec: executeVegaLiteFilterByRange(spec, params)
			};
			case "widget.highlightValues": return {
				handled: true,
				nextSpec: executeVegaLiteHighlightValues(spec, params)
			};
			case "widget.sortEncoding": return {
				handled: true,
				nextSpec: executeVegaLiteSortEncoding(spec, params)
			};
			case "widget.changeEncoding": return {
				handled: true,
				nextSpec: executeVegaLiteChangeEncoding(spec, params)
			};
			case "widget.zoomDomain": return {
				handled: true,
				nextSpec: executeVegaLiteZoomDomain(spec, params)
			};
			case "scatter.brushRegion": return {
				handled: true,
				nextSpec: executeVegaLiteScatterBrushLike(spec, params, "brush")
			};
			case "scatter.selectRegion": return {
				handled: true,
				nextSpec: executeVegaLiteScatterBrushLike(spec, params, "region")
			};
			case "scatter.filterCategorical": return {
				handled: true,
				nextSpec: executeVegaLiteScatterFilterCategorical(spec, params)
			};
			case "scatter.identifyClusters": return {
				handled: true,
				nextSpec: executeVegaLiteScatterIdentifyClusters(spec, params)
			};
			case "scatter.showRegression": return {
				handled: true,
				nextSpec: executeVegaLiteScatterShowRegression(spec, params)
			};
			case "bar.filterCategories": return {
				handled: true,
				nextSpec: executeVegaLiteBarFilterCategories(spec, params)
			};
			case "bar.sortBars": return {
				handled: true,
				nextSpec: executeVegaLiteBarSortBars(spec, params)
			};
			case "bar.highlightTopN": return {
				handled: true,
				nextSpec: executeVegaLiteBarHighlightTopN(spec, params)
			};
			case "bar.addBars": return {
				handled: true,
				nextSpec: executeVegaLiteBarAddRemoveBars(spec, params, "add")
			};
			case "bar.removeBars": return {
				handled: true,
				nextSpec: executeVegaLiteBarAddRemoveBars(spec, params, "remove")
			};
			case "bar.addBarItems": return {
				handled: true,
				nextSpec: executeVegaLiteBarAddRemoveItems(spec, params, "add")
			};
			case "bar.removeBarItems": return {
				handled: true,
				nextSpec: executeVegaLiteBarAddRemoveItems(spec, params, "remove")
			};
			case "bar.filterSubcategories": return {
				handled: true,
				nextSpec: executeVegaLiteBarFilterSubcategories(spec, params)
			};
			case "bar.expandStack": return {
				handled: true,
				nextSpec: executeVegaLiteBarExpandStack(spec, params)
			};
			case "bar.toggleStackMode": return {
				handled: true,
				nextSpec: executeVegaLiteBarToggleStackMode(spec, params)
			};
			case "line.zoomXRegion": return {
				handled: true,
				nextSpec: executeVegaLiteLineZoomXRegion(spec, params)
			};
			case "line.focusLines": return {
				handled: true,
				nextSpec: executeVegaLiteLineFocusLines(spec, params)
			};
			case "line.highlightTrend": return {
				handled: true,
				nextSpec: executeVegaLiteLineHighlightTrend(spec, params)
			};
			case "line.showMovingAverage": return {
				handled: true,
				nextSpec: executeVegaLiteLineShowMovingAverage(spec, params)
			};
			case "line.drillDownXAxis": return {
				handled: true,
				nextSpec: executeVegaLiteLineDrilldown(spec, params)
			};
			case "line.resetDrilldownXAxis": return {
				handled: true,
				nextSpec: executeVegaLiteLineResetDrilldown(spec, params)
			};
			case "line.resampleXAxis": return {
				handled: true,
				nextSpec: executeVegaLiteLineResample(spec, params)
			};
			case "line.resetResampleXAxis": return {
				handled: true,
				nextSpec: executeVegaLiteLineResetResample(spec, params)
			};
			case "line.boldLines": return {
				handled: true,
				nextSpec: executeVegaLiteLineBoldLines(spec, params)
			};
			case "line.filterLines": return {
				handled: true,
				nextSpec: executeVegaLiteLineFilterLines(spec, params)
			};
			case "parallelCoordinates.reorderDimensions": return {
				handled: true,
				nextSpec: executeVegaLiteParallelReorderDimensions(spec, params)
			};
			case "parallelCoordinates.filterDimension": return {
				handled: true,
				nextSpec: executeVegaLiteParallelFilterDimension(spec, params)
			};
			case "parallelCoordinates.filterByCategory": return {
				handled: true,
				nextSpec: executeVegaLiteParallelFilterByCategory(spec, params)
			};
			case "parallelCoordinates.highlightCategory": return {
				handled: true,
				nextSpec: executeVegaLiteParallelHighlightCategory(spec, params)
			};
			case "parallelCoordinates.hideDimensions": return {
				handled: true,
				nextSpec: executeVegaLiteParallelHideDimensions(spec, params)
			};
			case "parallelCoordinates.resetHiddenDimensions": return {
				handled: true,
				nextSpec: executeVegaLiteParallelResetHiddenDimensions(spec, params)
			};
			case "heatmap.filterCells": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapFilterCells(spec, params)
			};
			case "heatmap.drilldownAxis": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapDrilldownAxis(spec, params)
			};
			case "heatmap.resetDrilldown": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapResetDrilldown(spec, params)
			};
			case "heatmap.addMarginalBars": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapAddMarginalBars(spec, params)
			};
			case "heatmap.highlightRegion": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapHighlightRegion(spec, params)
			};
			case "heatmap.adjustColorScale": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapAdjustColorScale(spec, params)
			};
			case "heatmap.thresholdMask": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapThresholdMask(spec, params)
			};
			case "heatmap.filterCellsByRegion": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapFilterCellsByRegion(spec, params)
			};
			case "heatmap.highlightRegionByValue": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapHighlightRegionByValue(spec, params)
			};
			case "heatmap.clusterRowsCols": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapClusterRowsCols(spec, params)
			};
			case "heatmap.transpose": return {
				handled: true,
				nextSpec: executeVegaLiteHeatmapTranspose(spec, params)
			};
			case "sankey.filterFlow": return {
				handled: true,
				nextSpec: executeVegaSankeyFilterFlow(spec, params)
			};
			case "sankey.collapseNodes": return {
				handled: true,
				nextSpec: executeVegaSankeyCollapseNodes(spec, params)
			};
			case "sankey.expandNode": return {
				handled: true,
				nextSpec: executeVegaSankeyExpandNode(spec, params)
			};
			case "sankey.highlightPath": return {
				handled: true,
				nextSpec: executeVegaSankeyHighlightPath(spec, params)
			};
			case "sankey.traceNode": return {
				handled: true,
				nextSpec: executeVegaSankeyTraceNode(spec, params)
			};
			case "sankey.colorFlows": return {
				handled: true,
				nextSpec: executeVegaSankeyColorFlows(spec, params)
			};
			case "sankey.reorderNodesInLayer": return {
				handled: true,
				nextSpec: executeVegaSankeyReorderNodesInLayer(spec, params)
			};
			case "sankey.autoCollapseByRank": return {
				handled: true,
				nextSpec: executeVegaSankeyAutoCollapseByRank(spec, params)
			};
			default: return {
				handled: false,
				reason: "unsupported_action"
			};
		}
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/vegaLiteOfficialPageMaterializer.js
	function clone$18(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function isPlainObject$1(value) {
		return !!value && typeof value === "object" && !Array.isArray(value);
	}
	function readRuntimeTransformTag(transform = {}) {
		return transform?._widgetvaTag || transform?.spec?.actionName || transform?.linkId || transform?.sourceRef || null;
	}
	function buildVegaLiteFilterForPredicate(predicate = {}) {
		const field = typeof predicate?.field === "string" ? predicate.field : null;
		if (!field) return null;
		if ((predicate.op === "equals" || predicate.op === "eq") && Object.prototype.hasOwnProperty.call(predicate, "value")) return {
			field,
			equal: clone$18(predicate.value)
		};
		if (predicate.op === "in" && Array.isArray(predicate.value) && predicate.value.length > 0) return {
			field,
			oneOf: clone$18(predicate.value)
		};
		if (predicate.op === "notIn" && Array.isArray(predicate.value) && predicate.value.length > 0) return { not: {
			field,
			oneOf: clone$18(predicate.value)
		} };
		if (predicate.op === "between" && Array.isArray(predicate.value) && predicate.value.length >= 2) return {
			field,
			range: clone$18(predicate.value.slice(0, 2))
		};
		if ((predicate.op === "gte" || predicate.op === "greaterThanOrEqual") && Object.prototype.hasOwnProperty.call(predicate, "value")) return {
			field,
			gte: clone$18(predicate.value)
		};
		if ((predicate.op === "lte" || predicate.op === "lessThanOrEqual") && Object.prototype.hasOwnProperty.call(predicate, "value")) return {
			field,
			lte: clone$18(predicate.value)
		};
		if ((predicate.op === "gt" || predicate.op === "greaterThan") && Object.prototype.hasOwnProperty.call(predicate, "value")) return {
			field,
			gt: clone$18(predicate.value)
		};
		if ((predicate.op === "lt" || predicate.op === "lessThan") && Object.prototype.hasOwnProperty.call(predicate, "value")) return {
			field,
			lt: clone$18(predicate.value)
		};
		return null;
	}
	function buildVegaLiteTransformsFromRuntimeState(state = {}) {
		return (Array.isArray(state?.transforms) ? state.transforms : []).flatMap((transform) => {
			if (transform?._widgetvaRuntimeSelection === true) return [];
			const directPredicate = isPlainObject$1(transform?.predicate) ? [transform.predicate] : Array.isArray(transform?.predicate) ? transform.predicate.filter(isPlainObject$1) : [];
			const filters = (directPredicate.length > 0 ? directPredicate : Array.isArray(transform?.spec?.predicates) ? transform.spec.predicates : []).map((predicate) => buildVegaLiteFilterForPredicate(predicate)).filter(Boolean);
			if (filters.length === 0) return [];
			const tag = readRuntimeTransformTag(transform);
			return filters.map((filter) => ({
				_widgetvaRuntimeTransform: true,
				...tag ? { _widgetvaTag: tag } : {},
				filter
			}));
		});
	}
	function readActionTransformName(transform = {}) {
		return typeof transform?.spec?.actionName === "string" ? transform.spec.actionName : null;
	}
	function readTransformValues(transform = {}) {
		if (Array.isArray(transform?.spec?.values)) return clone$18(transform.spec.values);
		const predicate = Array.isArray(transform?.spec?.predicates) ? transform.spec.predicates.find((entry) => Array.isArray(entry?.value)) : null;
		return predicate ? clone$18(predicate.value) : [];
	}
	function readTransformField(transform = {}) {
		if (typeof transform?.spec?.field === "string" && transform.spec.field.length > 0) return transform.spec.field;
		return (Array.isArray(transform?.spec?.predicates) ? transform.spec.predicates.find((entry) => typeof entry?.field === "string" && entry.field.length > 0) : null)?.field || null;
	}
	function isKnownProviderProjectedTransform(transform = {}) {
		return typeof readActionTransformName(transform) === "string";
	}
	function applyVegaLiteSpecAction(spec, actionName, params = {}) {
		try {
			const result = executeVegaLiteSpecAction({
				actionName,
				spec,
				params
			});
			return result?.handled ? {
				handled: true,
				nextSpec: result.nextSpec
			} : {
				handled: false,
				nextSpec: spec
			};
		} catch {
			return {
				handled: false,
				nextSpec: spec
			};
		}
	}
	function compactObject(value = {}) {
		return Object.fromEntries(Object.entries(value).filter(([, entry]) => entry !== void 0 && entry !== null));
	}
	function readTransformFields(transform = {}) {
		return Array.isArray(transform?.spec?.fields) ? transform.spec.fields.filter((field) => typeof field === "string" && field.length > 0) : [];
	}
	function readValueList(value) {
		if (Array.isArray(value)) return value;
		return value == null ? void 0 : [value];
	}
	function buildProviderActionParamsFromTransform(transform = {}) {
		const actionName = readActionTransformName(transform);
		if (!actionName) return null;
		const spec = isPlainObject$1(transform?.spec) ? clone$18(transform.spec) : {};
		delete spec.actionName;
		const field = readTransformField(transform);
		const fields = readTransformFields(transform);
		const values = readTransformValues(transform);
		switch (actionName) {
			case "scatter.filterCategorical": return values.length > 0 ? compactObject({
				field,
				categoriesToRemove: values
			}) : null;
			case "bar.filterCategories": return values.length > 0 ? compactObject({
				field,
				categories: values
			}) : null;
			case "bar.filterSubcategories": return values.length > 0 ? compactObject({
				subField: field,
				subcategoriesToRemove: values
			}) : null;
			case "bar.addBars":
			case "bar.removeBars": return compactObject({
				field,
				values: Array.isArray(spec.changedValues) && spec.changedValues.length > 0 ? clone$18(spec.changedValues) : values,
				visibleValues: values
			});
			case "bar.addBarItems":
			case "bar.removeBarItems": return compactObject({
				xField: fields[0],
				subField: fields[1],
				items: Array.isArray(spec.changedItems) && spec.changedItems.length > 0 ? clone$18(spec.changedItems) : values,
				visibleItems: values
			});
			case "line.filterLines": return values.length > 0 ? compactObject({
				lineField: field,
				linesToRemove: values
			}) : null;
			case "line.boldLines": return compactObject({
				lineField: field,
				lineNames: values,
				boldWidth: spec.boldWidth,
				baseWidth: spec.baseWidth
			});
			case "line.zoomXRegion": return compactObject({
				start: spec.start ?? spec.xMin ?? spec.min,
				end: spec.end ?? spec.xMax ?? spec.max
			});
			case "parallelCoordinates.filterByCategory": return values.length > 0 ? compactObject({
				field,
				values
			}) : null;
			case "parallelCoordinates.filterDimension": return compactObject({
				dimension: spec.dimension || field,
				range: spec.range || values
			});
			case "parallelCoordinates.hideDimensions": return compactObject({
				dimensions: Array.isArray(spec.dimensions) && spec.dimensions.length > 0 ? spec.dimensions : spec.hiddenDimensions,
				mode: spec.operation
			});
			case "heatmap.filterCells":
			case "heatmap.filterCellsByRegion": return compactObject({
				xValues: Array.isArray(spec.xValues) ? spec.xValues : Array.isArray(spec.x) ? spec.x : readValueList(fields[0] ? spec.value?.[fields[0]] : void 0),
				yValues: Array.isArray(spec.yValues) ? spec.yValues : Array.isArray(spec.y) ? spec.y : readValueList(fields[1] ? spec.value?.[fields[1]] : void 0)
			});
			case "heatmap.drilldownAxis": return compactObject({
				axis: spec.axis,
				field,
				value: spec.value
			});
			case "heatmap.thresholdMask": return compactObject({
				threshold: spec.threshold ?? spec.minValue,
				minValue: spec.minValue,
				maxValue: spec.maxValue,
				mode: spec.mode,
				outsideOpacity: spec.outsideOpacity
			});
			default: return spec;
		}
	}
	function applyVegaLiteFilterState(spec, transforms = []) {
		const safeTransforms = Array.isArray(transforms) ? transforms : [];
		let nextSpec = spec;
		const projectedTransforms = /* @__PURE__ */ new Set();
		for (const transform of safeTransforms) {
			const actionName = readActionTransformName(transform);
			const params = buildProviderActionParamsFromTransform(transform);
			if (!actionName || !params) continue;
			const result = applyVegaLiteSpecAction(nextSpec, actionName, params);
			if (result.handled) {
				nextSpec = result.nextSpec;
				projectedTransforms.add(transform);
			}
		}
		const genericTransforms = safeTransforms.filter((transform) => !projectedTransforms.has(transform) && !isKnownProviderProjectedTransform(transform));
		return genericTransforms.length > 0 ? injectRuntimeTransformsIntoSpec(nextSpec, { transforms: genericTransforms }) : nextSpec;
	}
	function applyVegaLiteViewState(spec, view = {}) {
		const xDomain = Array.isArray(view?.xDomain) ? view.xDomain : null;
		const yDomain = Array.isArray(view?.yDomain) ? view.yDomain : null;
		let nextSpec = spec;
		if (xDomain || yDomain) {
			const zoomActionName = view?.zoom?.sourceAction === "line.zoomXRegion" ? "line.zoomXRegion" : "widget.zoomDomain";
			nextSpec = applyVegaLiteSpecAction(spec, zoomActionName, zoomActionName === "line.zoomXRegion" ? {
				start: view.zoom?.start ?? xDomain?.[0],
				end: view.zoom?.end ?? xDomain?.[1]
			} : {
				...xDomain ? { xDomain } : {},
				...yDomain ? { yDomain } : {}
			}).nextSpec;
		}
		if (isPlainObject$1(view?.sort)) {
			const actionName = view.sort.sourceAction || "widget.sortEncoding";
			nextSpec = applyVegaLiteSpecAction(nextSpec, actionName, view.sort).nextSpec;
		}
		if (isPlainObject$1(view?.drillDown)) {
			const actionName = view.drillDown.sourceAction || "line.drillDownXAxis";
			nextSpec = applyVegaLiteSpecAction(nextSpec, actionName, view.drillDown).nextSpec;
		}
		return nextSpec;
	}
	function applyVegaLiteReencodeState(spec, reencode = null) {
		if (!isPlainObject$1(reencode)) return spec;
		const actionName = reencode.sourceAction || null;
		if (!actionName) return spec;
		switch (actionName) {
			case "line.resampleXAxis": return applyVegaLiteSpecAction(spec, actionName, {
				granularity: reencode.granularity || reencode.timeUnit || "month",
				agg: reencode.agg || reencode.aggregate || "mean"
			}).nextSpec;
			case "line.highlightTrend":
			case "scatter.identifyClusters":
			case "scatter.showRegression":
			case "heatmap.adjustColorScale":
			case "heatmap.clusterRowsCols":
			case "heatmap.transpose": return applyVegaLiteSpecAction(spec, actionName, reencode).nextSpec;
			case "bar.expandStack": return applyVegaLiteSpecAction(spec, actionName, {
				category: reencode.category,
				categoryField: reencode.categoryField,
				subField: reencode.subField
			}).nextSpec;
			case "bar.toggleStackMode": return applyVegaLiteSpecAction(spec, actionName, {
				mode: reencode.layout || reencode.mode,
				subField: reencode.subField
			}).nextSpec;
			case "parallelCoordinates.reorderDimensions": return applyVegaLiteSpecAction(spec, actionName, { dimensions: reencode.dimensionOrder || reencode.dimensions }).nextSpec;
			case "parallelCoordinates.hideDimensions": return applyVegaLiteSpecAction(spec, actionName, {
				dimensions: reencode.dimensions || reencode.hiddenDimensions,
				mode: reencode.operation
			}).nextSpec;
			case "parallelCoordinates.resetHiddenDimensions": return applyVegaLiteSpecAction(spec, actionName, {}).nextSpec;
			default: return applyVegaLiteSpecAction(spec, actionName, reencode).nextSpec;
		}
	}
	function applyVegaLiteEmphasisState(spec, highlight = null) {
		if (!isPlainObject$1(highlight)) return spec;
		const actionName = highlight.sourceAction || null;
		if (!actionName) return spec;
		switch (actionName) {
			case "line.boldLines": return applyVegaLiteSpecAction(spec, actionName, {
				lineField: highlight.lineField,
				lineNames: Array.isArray(highlight.lineNames) ? highlight.lineNames : [],
				...Number.isFinite(highlight.boldWidth) ? { boldWidth: highlight.boldWidth } : {},
				...Number.isFinite(highlight.baseWidth) ? { baseWidth: highlight.baseWidth } : {}
			}).nextSpec;
			case "bar.highlightTopN": return applyVegaLiteSpecAction(spec, actionName, highlight).nextSpec;
			case "parallelCoordinates.highlightCategory": return applyVegaLiteSpecAction(spec, actionName, highlight).nextSpec;
			case "heatmap.highlightRegion":
			case "heatmap.highlightRegionByValue":
			case "heatmap.thresholdMask": return applyVegaLiteSpecAction(spec, actionName, highlight).nextSpec;
			default: return applyVegaLiteSpecAction(spec, actionName, highlight).nextSpec;
		}
	}
	function applyVegaLiteAddRemoveState(spec, addRemove = null) {
		if (!isPlainObject$1(addRemove)) return spec;
		const actionName = addRemove.sourceAction || null;
		if (!actionName) return spec;
		switch (actionName) {
			case "bar.addBars":
			case "bar.removeBars": return applyVegaLiteSpecAction(spec, actionName, {
				field: addRemove.field,
				values: addRemove.changedValues,
				visibleValues: addRemove.visibleValues
			}).nextSpec;
			case "bar.addBarItems":
			case "bar.removeBarItems": return applyVegaLiteSpecAction(spec, actionName, {
				xField: addRemove.xField,
				subField: addRemove.subField,
				items: addRemove.changedItems,
				visibleItems: addRemove.visibleItems
			}).nextSpec;
			case "line.showMovingAverage":
			case "heatmap.addMarginalBars": return applyVegaLiteSpecAction(spec, actionName, addRemove).nextSpec;
			default: return applyVegaLiteSpecAction(spec, actionName, addRemove).nextSpec;
		}
	}
	function injectRuntimeTransformsIntoSpec(spec = {}, state = {}) {
		if (!isPlainObject$1(spec)) return spec;
		const runtimeTransforms = buildVegaLiteTransformsFromRuntimeState(state);
		if (runtimeTransforms.length === 0) return spec;
		const runtimeTags = new Set(runtimeTransforms.map((transform) => transform._widgetvaTag).filter(Boolean));
		const existingTransforms = (Array.isArray(spec?.transform) ? spec.transform : []).filter((transform) => {
			if (transform?._widgetvaRuntimeTransform === true) return false;
			return !(transform?._widgetvaTag && runtimeTags.has(transform._widgetvaTag));
		});
		return {
			...spec,
			transform: [...existingTransforms, ...runtimeTransforms]
		};
	}
	function readSelectionId(selection = {}) {
		return selection?.selectionId || selection?.selection_id || null;
	}
	function normalizeSelectionMap(selections = []) {
		const map = /* @__PURE__ */ new Map();
		for (const selection of Array.isArray(selections) ? selections : []) {
			const selectionId = readSelectionId(selection);
			if (typeof selectionId === "string" && selectionId.length > 0) map.set(selectionId, selection);
		}
		return map;
	}
	function collectScaleBoundParamNames(spec, collected = /* @__PURE__ */ new Set()) {
		if (!isPlainObject$1(spec)) return collected;
		for (const param of Array.isArray(spec?.params) ? spec.params : []) if (isPlainObject$1(param) && typeof param?.name === "string" && param.name.length > 0 && param?.bind === "scales") collected.add(param.name);
		if (Array.isArray(spec.layer)) for (const child of spec.layer) collectScaleBoundParamNames(child, collected);
		if (Array.isArray(spec.vconcat)) for (const child of spec.vconcat) collectScaleBoundParamNames(child, collected);
		if (Array.isArray(spec.hconcat)) for (const child of spec.hconcat) collectScaleBoundParamNames(child, collected);
		if (Array.isArray(spec.concat)) for (const child of spec.concat) collectScaleBoundParamNames(child, collected);
		if (isPlainObject$1(spec.spec)) collectScaleBoundParamNames(spec.spec, collected);
		return collected;
	}
	function escapeDatumField(field) {
		return String(field).replace(/\\/g, "\\\\").replace(/'/g, "\\'");
	}
	function isTemporalLikeValue(value) {
		if (value instanceof Date && Number.isFinite(value.getTime())) return true;
		if (typeof value === "string") {
			const parsed = Date.parse(value);
			return Number.isFinite(parsed);
		}
		return false;
	}
	function buildComparableExpression(value) {
		if (value instanceof Date && Number.isFinite(value.getTime())) return `toDate(${JSON.stringify(value.toISOString())})`;
		if (isTemporalLikeValue(value)) return `toDate(${JSON.stringify(value)})`;
		return JSON.stringify(value);
	}
	function buildDatumFieldExpression(field, predicate = null) {
		const datumRef = `datum['${escapeDatumField(field)}']`;
		const value = predicate?.value;
		return isTemporalLikeValue(Array.isArray(value) ? value[0] : value) ? `toDate(${datumRef})` : datumRef;
	}
	function buildPredicateExpression(predicate) {
		const field = typeof predicate?.field === "string" ? predicate.field : null;
		if (!field) return null;
		const datumRef = buildDatumFieldExpression(field, predicate);
		if (predicate.op === "equals" || predicate.op === "eq") return `${datumRef} === ${buildComparableExpression(predicate.value)}`;
		if (predicate.op === "in" && Array.isArray(predicate.value) && predicate.value.length > 0) return `indexof([${predicate.value.map((entry) => buildComparableExpression(entry)).join(", ")}], ${datumRef}) >= 0`;
		if (predicate.op === "between" && Array.isArray(predicate.value) && predicate.value.length >= 2) return `${datumRef} >= ${buildComparableExpression(predicate.value[0])} && ${datumRef} <= ${buildComparableExpression(predicate.value[1])}`;
		if (predicate.op === "gte" || predicate.op === "greaterThanOrEqual") return `${datumRef} >= ${buildComparableExpression(predicate.value)}`;
		if (predicate.op === "lte" || predicate.op === "lessThanOrEqual") return `${datumRef} <= ${buildComparableExpression(predicate.value)}`;
		if (predicate.op === "gt" || predicate.op === "greaterThan") return `${datumRef} > ${buildComparableExpression(predicate.value)}`;
		if (predicate.op === "lt" || predicate.op === "lessThan") return `${datumRef} < ${buildComparableExpression(predicate.value)}`;
		return null;
	}
	function buildSelectionExpression(selection) {
		const predicateClauses = (Array.isArray(selection?.predicates) ? selection.predicates : []).map((predicate) => buildPredicateExpression(predicate)).filter(Boolean);
		return predicateClauses.length > 0 ? predicateClauses.join(" && ") : null;
	}
	function rewriteParamTestExpression(testSpec, selectionMap, consumedSelectionIds) {
		if (!isPlainObject$1(testSpec)) return null;
		if (typeof testSpec.param === "string") {
			const selection = selectionMap.get(testSpec.param) || null;
			const expression = buildSelectionExpression(selection);
			const emptyFalse = testSpec.empty === false;
			if (!selection) return emptyFalse ? "false" : null;
			const selectionId = readSelectionId(selection);
			if (selectionId) consumedSelectionIds.add(selectionId);
			return expression || (emptyFalse ? "false" : null);
		}
		if (Array.isArray(testSpec.and) && testSpec.and.length > 0) {
			const parts = testSpec.and.map((entry) => rewriteParamTestExpression(entry, selectionMap, consumedSelectionIds)).filter((entry) => typeof entry === "string" && entry.length > 0);
			return parts.length > 0 ? parts.map((entry) => `(${entry})`).join(" && ") : null;
		}
		if (Array.isArray(testSpec.or) && testSpec.or.length > 0) {
			const parts = testSpec.or.map((entry) => rewriteParamTestExpression(entry, selectionMap, consumedSelectionIds)).filter((entry) => typeof entry === "string" && entry.length > 0);
			return parts.length > 0 ? parts.map((entry) => `(${entry})`).join(" || ") : null;
		}
		if (isPlainObject$1(testSpec.not)) {
			const inner = rewriteParamTestExpression(testSpec.not, selectionMap, consumedSelectionIds);
			return typeof inner === "string" && inner.length > 0 ? `!(${inner})` : null;
		}
		return null;
	}
	function readSelectionDomainForChannel(selection, channelDef, channelName = null) {
		if (!isPlainObject$1(selection) || !isPlainObject$1(channelDef)) return null;
		const explicitDomain = channelName === "x" ? selection?.domain?.xDomain : channelName === "y" ? selection?.domain?.yDomain : null;
		if (Array.isArray(explicitDomain) && explicitDomain.length >= 2) return clone$18(explicitDomain);
		const fieldName = typeof channelDef?.field === "string" ? channelDef.field : null;
		if (!fieldName) return null;
		const matchingPredicate = (Array.isArray(selection?.predicates) ? selection.predicates : []).find((predicate) => predicate?.field === fieldName && predicate?.op === "between" && Array.isArray(predicate?.value) && predicate.value.length >= 2);
		return matchingPredicate ? clone$18(matchingPredicate.value) : null;
	}
	function rewriteParamFilterTransform(transform, selectionMap, consumedSelectionIds) {
		if (!isPlainObject$1(transform) || !isPlainObject$1(transform.filter)) return {
			transform,
			rewritten: false
		};
		if (typeof transform.filter.param !== "string") {
			const rewrittenExpression = rewriteParamTestExpression(transform.filter, selectionMap, consumedSelectionIds);
			if (typeof rewrittenExpression !== "string" || rewrittenExpression.length === 0) return {
				transform,
				rewritten: false
			};
			return {
				transform: {
					...transform,
					filter: rewrittenExpression
				},
				rewritten: true
			};
		}
		const paramName = transform.filter.param;
		const selection = selectionMap.get(paramName) || null;
		if (!selection) return {
			transform,
			rewritten: false
		};
		const expression = buildSelectionExpression(selection);
		if (!expression) return {
			transform,
			rewritten: false
		};
		const selectionId = readSelectionId(selection);
		if (selectionId) consumedSelectionIds.add(selectionId);
		return {
			transform: {
				...transform,
				filter: expression
			},
			rewritten: true
		};
	}
	function rewriteParamCondition(channelDef, selectionMap, consumedSelectionIds) {
		if (!isPlainObject$1(channelDef) || !isPlainObject$1(channelDef.condition)) return channelDef;
		if (typeof channelDef.condition.param !== "string") {
			if (!isPlainObject$1(channelDef.condition.test)) return channelDef;
			const rewrittenTest = rewriteParamTestExpression(channelDef.condition.test, selectionMap, consumedSelectionIds);
			if (typeof rewrittenTest !== "string" || rewrittenTest.length === 0) return channelDef;
			return {
				...channelDef,
				condition: {
					...channelDef.condition,
					test: rewrittenTest
				}
			};
		}
		const paramName = channelDef.condition.param;
		const selection = selectionMap.get(paramName) || null;
		const { param, empty, ...conditionBody } = channelDef.condition;
		const nextChannel = { ...channelDef };
		if (!selection) return channelDef;
		delete nextChannel.condition;
		const expression = buildSelectionExpression(selection);
		const selectionId = readSelectionId(selection);
		if (selectionId) consumedSelectionIds.add(selectionId);
		return {
			...nextChannel,
			condition: {
				...conditionBody,
				test: expression || "false"
			}
		};
	}
	function rewriteParamScaleDomain(channelDef, channelName, selectionMap, consumedSelectionIds) {
		if (!isPlainObject$1(channelDef) || !isPlainObject$1(channelDef.scale) || !isPlainObject$1(channelDef.scale.domain) || typeof channelDef.scale.domain.param !== "string") return channelDef;
		const paramName = channelDef.scale.domain.param;
		const selection = selectionMap.get(paramName) || null;
		if (!selection) return channelDef;
		const domain = readSelectionDomainForChannel(selection, channelDef, channelName);
		if (!Array.isArray(domain) || domain.length < 2) return channelDef;
		const selectionId = readSelectionId(selection);
		if (selectionId) consumedSelectionIds.add(selectionId);
		return {
			...channelDef,
			scale: {
				...channelDef.scale,
				domain
			}
		};
	}
	function injectScaleBoundDomain(channelDef, channelName, selectionMap, scaleBoundParamNames, consumedSelectionIds) {
		if (!isPlainObject$1(channelDef) || scaleBoundParamNames.size === 0) return channelDef;
		if (channelName === "x" ? Array.isArray(channelDef?.scale?.domain) ? channelDef.scale.domain : null : channelName === "y" ? Array.isArray(channelDef?.scale?.domain) ? channelDef.scale.domain : null : null) return channelDef;
		for (const paramName of scaleBoundParamNames) {
			const selection = selectionMap.get(paramName) || null;
			const domain = readSelectionDomainForChannel(selection, channelDef, channelName);
			if (!Array.isArray(domain) || domain.length < 2) continue;
			const selectionId = readSelectionId(selection);
			if (selectionId) consumedSelectionIds.add(selectionId);
			return {
				...channelDef,
				scale: {
					...isPlainObject$1(channelDef.scale) ? channelDef.scale : {},
					domain
				}
			};
		}
		return channelDef;
	}
	function rewriteSpecEncodings(spec, selectionMap, scaleBoundParamNames, consumedSelectionIds) {
		if (!isPlainObject$1(spec?.encoding)) return spec;
		const nextEncoding = {};
		for (const [channel, channelDef] of Object.entries(spec.encoding)) nextEncoding[channel] = rewriteParamCondition(injectScaleBoundDomain(rewriteParamScaleDomain(channelDef, channel, selectionMap, consumedSelectionIds), channel, selectionMap, scaleBoundParamNames, consumedSelectionIds), selectionMap, consumedSelectionIds);
		return {
			...spec,
			encoding: nextEncoding
		};
	}
	function rewriteSpecTransforms(spec, selectionMap, consumedSelectionIds) {
		if (!Array.isArray(spec?.transform)) return spec;
		const nextTransforms = [];
		let rewrittenAny = false;
		for (const transform of spec.transform) {
			const { transform: nextTransform, rewritten } = rewriteParamFilterTransform(transform, selectionMap, consumedSelectionIds);
			rewrittenAny = rewrittenAny || rewritten;
			if (nextTransform) nextTransforms.push(nextTransform);
		}
		if (!rewrittenAny) return spec;
		const nextSpec = { ...spec };
		if (nextTransforms.length > 0) nextSpec.transform = nextTransforms;
		else delete nextSpec.transform;
		return nextSpec;
	}
	function rewriteSpecForParamSelections(spec, selectionMap, scaleBoundParamNames, consumedSelectionIds) {
		if (!isPlainObject$1(spec)) return spec;
		let nextSpec = rewriteSpecTransforms(spec, selectionMap, consumedSelectionIds);
		nextSpec = rewriteSpecEncodings(nextSpec, selectionMap, scaleBoundParamNames, consumedSelectionIds);
		if (Array.isArray(nextSpec.layer)) nextSpec = {
			...nextSpec,
			layer: nextSpec.layer.map((child) => rewriteSpecForParamSelections(child, selectionMap, scaleBoundParamNames, consumedSelectionIds))
		};
		if (Array.isArray(nextSpec.vconcat)) nextSpec = {
			...nextSpec,
			vconcat: nextSpec.vconcat.map((child) => rewriteSpecForParamSelections(child, selectionMap, scaleBoundParamNames, consumedSelectionIds))
		};
		if (Array.isArray(nextSpec.hconcat)) nextSpec = {
			...nextSpec,
			hconcat: nextSpec.hconcat.map((child) => rewriteSpecForParamSelections(child, selectionMap, scaleBoundParamNames, consumedSelectionIds))
		};
		if (Array.isArray(nextSpec.concat)) nextSpec = {
			...nextSpec,
			concat: nextSpec.concat.map((child) => rewriteSpecForParamSelections(child, selectionMap, scaleBoundParamNames, consumedSelectionIds))
		};
		if (isPlainObject$1(nextSpec.spec)) nextSpec = {
			...nextSpec,
			spec: rewriteSpecForParamSelections(nextSpec.spec, selectionMap, scaleBoundParamNames, consumedSelectionIds)
		};
		return nextSpec;
	}
	function deriveSelectionValueFromPredicates(selection = {}) {
		const predicates = Array.isArray(selection?.predicates) ? selection.predicates : [];
		if (predicates.length === 0) return null;
		const value = {};
		for (const predicate of predicates) {
			const field = typeof predicate?.field === "string" ? predicate.field : null;
			if (!field) continue;
			if (predicate?.op === "between" && Array.isArray(predicate?.value) && predicate.value.length >= 2) {
				value[field] = clone$18(predicate.value.slice(0, 2));
				continue;
			}
			if ((predicate?.op === "in" || predicate?.op === "eq" || predicate?.op === "equals") && predicate?.value !== void 0) value[field] = clone$18(predicate.value);
		}
		return Object.keys(value).length > 0 ? value : null;
	}
	function readParamSelectionType(param = {}) {
		if (typeof param?.selectionType === "string" && param.selectionType.length > 0) return param.selectionType;
		if (typeof param?.select === "string" && param.select.length > 0) return param.select;
		if (isPlainObject$1(param?.select) && typeof param.select.type === "string" && param.select.type.length > 0) return param.select.type;
		return null;
	}
	function readParamEncodings(param = {}) {
		if (Array.isArray(param?.encodings)) return param.encodings.filter((entry) => typeof entry === "string" && entry.length > 0);
		if (Array.isArray(param?.select?.encodings)) return param.select.encodings.filter((entry) => typeof entry === "string" && entry.length > 0);
		return [];
	}
	function readEncodingChannelDef(spec = {}, channel = null) {
		if (!isPlainObject$1(spec?.encoding) || typeof channel !== "string" || channel.length === 0) return null;
		return isPlainObject$1(spec.encoding[channel]) ? spec.encoding[channel] : null;
	}
	function readEncodingFieldDefByField(spec = {}, field = null) {
		if (!isPlainObject$1(spec?.encoding) || typeof field !== "string" || field.length === 0) return null;
		return Object.values(spec.encoding).find((channelDef) => isPlainObject$1(channelDef) && typeof channelDef.field === "string" && channelDef.field === field) || null;
	}
	function isTemporalEncoding(channelDef = {}) {
		return Boolean(isPlainObject$1(channelDef) && (channelDef.type === "temporal" || typeof channelDef.timeUnit === "string" && channelDef.timeUnit.length > 0));
	}
	function normalizeTemporalSelectionInitScalar(value) {
		if (value instanceof Date && Number.isFinite(value.getTime())) return value.getTime();
		if (typeof value === "number" && Number.isFinite(value)) return value;
		if (typeof value === "string") {
			const parsed = Date.parse(value);
			if (Number.isFinite(parsed)) return parsed;
		}
		return clone$18(value);
	}
	function normalizeSelectionInitValueForEncoding(value, channelDef = null) {
		if (!isTemporalEncoding(channelDef)) return clone$18(value);
		if (Array.isArray(value)) return value.map((entry) => normalizeTemporalSelectionInitScalar(entry));
		return normalizeTemporalSelectionInitScalar(value);
	}
	function readIntervalRangeForField(selection = {}, field = null) {
		if (typeof field !== "string" || field.length === 0) return null;
		const matchingPredicate = (Array.isArray(selection?.predicates) ? selection.predicates : []).find((predicate) => predicate?.field === field && predicate?.op === "between" && Array.isArray(predicate?.value) && predicate.value.length >= 2);
		return matchingPredicate ? clone$18(matchingPredicate.value.slice(0, 2)) : null;
	}
	function deriveParamValueForSelectionParam(param = {}, selection = {}, spec = {}) {
		if (!param || typeof param !== "object" || !selection || typeof selection !== "object") return null;
		const selectionType = readParamSelectionType(param);
		const projectedEncodings = readParamEncodings(param);
		if (selectionType === "point") {
			let pointValue = null;
			if (selection?.value && typeof selection.value === "object" && !Array.isArray(selection.value)) pointValue = clone$18(selection.value);
			else {
				const derived = deriveSelectionValueFromPredicates(selection);
				pointValue = derived ? clone$18(derived) : null;
			}
			if (!pointValue || typeof pointValue !== "object" || Array.isArray(pointValue)) return null;
			for (const [field, fieldValue] of Object.entries(pointValue)) {
				const fieldDef = readEncodingFieldDefByField(spec, field);
				pointValue[field] = normalizeSelectionInitValueForEncoding(fieldValue, fieldDef);
			}
			if (projectedEncodings.length > 0) {
				const entries = Object.entries(pointValue);
				for (const [index, encoding] of projectedEncodings.entries()) {
					const channelDef = readEncodingChannelDef(spec, encoding);
					if (Object.prototype.hasOwnProperty.call(pointValue, encoding)) {
						pointValue[encoding] = normalizeSelectionInitValueForEncoding(pointValue[encoding], channelDef);
						continue;
					}
					const match = entries[index] || entries[0] || null;
					if (!match) continue;
					pointValue[encoding] = normalizeSelectionInitValueForEncoding(match[1], channelDef);
				}
			}
			return pointValue;
		}
		if (selectionType === "interval") {
			const pointLikeValue = {};
			if (projectedEncodings.length > 0) {
				for (const encoding of projectedEncodings) {
					const channelDef = readEncodingChannelDef(spec, encoding);
					let range = null;
					if (encoding === "x") range = Array.isArray(selection?.domain?.xDomain) ? selection.domain.xDomain.slice(0, 2) : readIntervalRangeForField(selection, channelDef?.field);
					else if (encoding === "y") range = Array.isArray(selection?.domain?.yDomain) ? selection.domain.yDomain.slice(0, 2) : readIntervalRangeForField(selection, channelDef?.field);
					else if (typeof channelDef?.field === "string") range = readIntervalRangeForField(selection, channelDef.field);
					if (!Array.isArray(range) || range.length < 2) continue;
					pointLikeValue[encoding] = normalizeSelectionInitValueForEncoding(range, channelDef);
				}
				if (Object.keys(pointLikeValue).length > 0) return pointLikeValue;
			}
			const predicateValue = deriveSelectionValueFromPredicates(selection);
			if (predicateValue && typeof predicateValue === "object") for (const [field, value] of Object.entries(predicateValue)) pointLikeValue[field] = normalizeSelectionInitValueForEncoding(value, readEncodingFieldDefByField(spec, field));
			return Object.keys(pointLikeValue).length > 0 ? pointLikeValue : null;
		}
		return null;
	}
	function injectParamValuesIntoSpec(spec, selectionMap, consumedSelectionIds) {
		if (!isPlainObject$1(spec)) return spec;
		let nextSpec = spec;
		if (Array.isArray(spec.params) && spec.params.length > 0) {
			let changed = false;
			const nextParams = spec.params.map((param) => {
				if (!isPlainObject$1(param) || typeof param?.name !== "string" || param.name.length === 0) return param;
				const selection = selectionMap.get(param.name) || null;
				if (!selection) return param;
				const nextValue = deriveParamValueForSelectionParam(param, selection, spec);
				const selectionId = readSelectionId(selection);
				if (selectionId) consumedSelectionIds.add(selectionId);
				if (nextValue == null) {
					if (!Object.prototype.hasOwnProperty.call(param, "value")) return param;
					changed = true;
					const { value, ...rest } = param;
					return rest;
				}
				changed = true;
				return {
					...param,
					value: nextValue
				};
			});
			if (changed) nextSpec = {
				...nextSpec,
				params: nextParams
			};
		}
		if (Array.isArray(nextSpec.layer)) nextSpec = {
			...nextSpec,
			layer: nextSpec.layer.map((child) => injectParamValuesIntoSpec(child, selectionMap, consumedSelectionIds))
		};
		if (Array.isArray(nextSpec.vconcat)) nextSpec = {
			...nextSpec,
			vconcat: nextSpec.vconcat.map((child) => injectParamValuesIntoSpec(child, selectionMap, consumedSelectionIds))
		};
		if (Array.isArray(nextSpec.hconcat)) nextSpec = {
			...nextSpec,
			hconcat: nextSpec.hconcat.map((child) => injectParamValuesIntoSpec(child, selectionMap, consumedSelectionIds))
		};
		if (Array.isArray(nextSpec.concat)) nextSpec = {
			...nextSpec,
			concat: nextSpec.concat.map((child) => injectParamValuesIntoSpec(child, selectionMap, consumedSelectionIds))
		};
		if (isPlainObject$1(nextSpec.spec)) nextSpec = {
			...nextSpec,
			spec: injectParamValuesIntoSpec(nextSpec.spec, selectionMap, consumedSelectionIds)
		};
		return nextSpec;
	}
	function normalizeWidgetSelections(state) {
		return Object.values(state?.selections || {}).filter(Boolean);
	}
	function normalizeSelectionKind(selection) {
		return selection?.kind || selection?.selection_type || null;
	}
	function isCompositeDataMaterializingSelection(selection) {
		const kind = normalizeSelectionKind(selection);
		if (kind === "interval" || kind === "region" || kind === "cell") return true;
		if (Boolean(selection?.domain?.xDomain || selection?.domain?.yDomain)) return true;
		return (Array.isArray(selection?.predicates) ? selection.predicates : []).some((predicate) => predicate?.op === "between");
	}
	function hasCompositeChildren(spec) {
		return Array.isArray(spec?.concat) || Array.isArray(spec?.vconcat) || Array.isArray(spec?.hconcat) || Array.isArray(spec?.layer);
	}
	function readChildSpecs(spec = {}) {
		return [
			...Array.isArray(spec?.vconcat) ? spec.vconcat : [],
			...Array.isArray(spec?.hconcat) ? spec.hconcat : [],
			...Array.isArray(spec?.concat) ? spec.concat : [],
			...Array.isArray(spec?.layer) ? spec.layer : []
		].filter((child) => child && typeof child === "object");
	}
	function findFirstContinuousXYEncoding(spec = {}) {
		const xField = typeof spec?.encoding?.x?.field === "string" ? spec.encoding.x.field : null;
		const yField = typeof spec?.encoding?.y?.field === "string" ? spec.encoding.y.field : null;
		const xType = typeof spec?.encoding?.x?.type === "string" ? spec.encoding.x.type : null;
		const yType = typeof spec?.encoding?.y?.type === "string" ? spec.encoding.y.type : null;
		if (xField && yField && (xType === "quantitative" || xType === "temporal") && (yType === "quantitative" || yType === "temporal")) return {
			xField,
			yField,
			xType,
			yType
		};
		for (const child of readChildSpecs(spec)) {
			const resolved = findFirstContinuousXYEncoding(child);
			if (resolved) return resolved;
		}
		return null;
	}
	function normalizeComparableValue(value, type = null) {
		if (value == null) return null;
		if (type === "temporal") {
			if (value instanceof Date && Number.isFinite(value.getTime())) return value.getTime();
			const parsed = Date.parse(value);
			return Number.isFinite(parsed) ? parsed : null;
		}
		if (typeof value === "number" && Number.isFinite(value)) return value;
		const numeric = Number(value);
		return Number.isFinite(numeric) ? numeric : null;
	}
	function rowMatchesSelectionDomain(row = {}, selection = {}, encoding = null) {
		if (!encoding || !selection?.domain || !row || typeof row !== "object") return false;
		const xDomain = Array.isArray(selection.domain?.xDomain) ? selection.domain.xDomain : null;
		const yDomain = Array.isArray(selection.domain?.yDomain) ? selection.domain.yDomain : null;
		const hasXDomain = Array.isArray(xDomain) && xDomain.length >= 2;
		const hasYDomain = Array.isArray(yDomain) && yDomain.length >= 2;
		if (!hasXDomain && !hasYDomain) return false;
		if (hasXDomain) {
			const rowX = normalizeComparableValue(row?.[encoding.xField], encoding.xType);
			const domainX0 = normalizeComparableValue(xDomain[0], encoding.xType);
			const domainX1 = normalizeComparableValue(xDomain[1], encoding.xType);
			if (![
				rowX,
				domainX0,
				domainX1
			].every(Number.isFinite)) return false;
			if (rowX < Math.min(domainX0, domainX1) || rowX > Math.max(domainX0, domainX1)) return false;
		}
		if (hasYDomain) {
			const rowY = normalizeComparableValue(row?.[encoding.yField], encoding.yType);
			const domainY0 = normalizeComparableValue(yDomain[0], encoding.yType);
			const domainY1 = normalizeComparableValue(yDomain[1], encoding.yType);
			if (![
				rowY,
				domainY0,
				domainY1
			].every(Number.isFinite)) return false;
			if (rowY < Math.min(domainY0, domainY1) || rowY > Math.max(domainY0, domainY1)) return false;
		}
		return true;
	}
	function rowMatchesCompositeSelection(row = {}, activeSelections = [], encoding = null) {
		const selections = Array.isArray(activeSelections) ? activeSelections.filter(Boolean) : [];
		if (selections.length === 0) return false;
		return selections.some((selection) => {
			const hasDomain = Boolean(selection?.domain?.xDomain || selection?.domain?.yDomain);
			const hasPredicates = Array.isArray(selection?.predicates) && selection.predicates.length > 0;
			if (hasDomain && encoding) return rowMatchesSelectionDomain(row, selection, encoding);
			if (hasPredicates) return rowMatchesAnySelection(row, [selection]);
			return hasDomain && encoding ? rowMatchesSelectionDomain(row, selection, encoding) : false;
		});
	}
	function isRuntimeMaterializedTransform(transform) {
		if (!transform || typeof transform !== "object") return false;
		return Array.isArray(transform.aggregate) || Array.isArray(transform.fold) && transform.fold.length > 0 || extractPredicatesFromFilterTransform(transform).length > 0;
	}
	function hasDistinctRuntimeDataRef(state = {}) {
		const sourceDataRef = state?.data?.sourceDataRef || null;
		const currentDataRef = state?.data?.currentDataRef || null;
		return Boolean(currentDataRef && currentDataRef !== sourceDataRef);
	}
	function runtimeRowsMatchVisibleState(state = {}, runtimeRows = null) {
		const visibleCount = state?.data?.visibleCount;
		return !Number.isFinite(visibleCount) || Array.isArray(runtimeRows) && runtimeRows.length === visibleCount;
	}
	function resolveSelectionMaterialization({ semanticSpec, state, runtime }) {
		const dataRef = state?.data?.currentDataRef || state?.data?.sourceDataRef || null;
		const runtimeRows = dataRef ? runtime?.store?.readRuntimeData?.(dataRef)?.rows : null;
		const hasRuntimeMaterializedTransforms = Array.isArray(semanticSpec?.transform) && semanticSpec.transform.some((transform) => isRuntimeMaterializedTransform(transform));
		if (hasDistinctRuntimeDataRef(state) && hasRuntimeMaterializedTransforms && Array.isArray(runtimeRows) && runtimeRowsMatchVisibleState(state, runtimeRows)) return {
			rows: clone$18(runtimeRows),
			stripMaterializedTransforms: true
		};
		if (Array.isArray(semanticSpec?.data?.values)) return {
			rows: clone$18(semanticSpec.data.values),
			stripMaterializedTransforms: false
		};
		return Array.isArray(runtimeRows) ? {
			rows: clone$18(runtimeRows),
			stripMaterializedTransforms: false
		} : null;
	}
	function hasRuntimeDataProjection(state = {}) {
		if (hasDistinctRuntimeDataRef(state)) return true;
		return Array.isArray(state?.transforms) && state.transforms.length > 0;
	}
	function buildRuntimeDataProjectionSpec({ semanticSpec, state, runtime }) {
		const dataRef = state?.data?.currentDataRef || state?.data?.sourceDataRef || null;
		const runtimeRows = dataRef ? runtime?.store?.readRuntimeData?.(dataRef)?.rows : null;
		const usesRuntimeProjection = hasDistinctRuntimeDataRef(state) && Array.isArray(runtimeRows);
		const materialization = resolveSelectionMaterialization({
			semanticSpec,
			state,
			runtime
		});
		const rows = usesRuntimeProjection ? clone$18(runtimeRows) : materialization?.rows;
		if (!Array.isArray(rows)) return null;
		const nextSpec = {
			...semanticSpec,
			data: { values: rows }
		};
		if (materialization?.stripMaterializedTransforms && Array.isArray(semanticSpec?.transform)) nextSpec.transform = semanticSpec.transform.filter((transform) => !isRuntimeMaterializedTransform(transform));
		return nextSpec;
	}
	function buildVegaLiteRenderSpecFromRuntimeState({ semanticSpec, state, runtime }) {
		let baseSpec = clone$18(semanticSpec);
		baseSpec = applyVegaLiteFilterState(baseSpec, state?.transforms);
		baseSpec = applyVegaLiteViewState(baseSpec, state?.view);
		baseSpec = applyVegaLiteReencodeState(baseSpec, state?.view?.reencode);
		baseSpec = applyVegaLiteEmphasisState(baseSpec, state?.view?.highlight);
		baseSpec = applyVegaLiteAddRemoveState(baseSpec, state?.view?.addRemove);
		const activeSelections = normalizeWidgetSelections(state);
		if (!baseSpec) return baseSpec;
		if (activeSelections.length === 0) return hasRuntimeDataProjection(state) ? buildRuntimeDataProjectionSpec({
			semanticSpec: baseSpec,
			state,
			runtime
		}) || baseSpec : baseSpec;
		const selectionMap = normalizeSelectionMap(activeSelections);
		const consumedSelectionIds = /* @__PURE__ */ new Set();
		const paramValueInjectedSpec = injectParamValuesIntoSpec(baseSpec, selectionMap, consumedSelectionIds);
		if (activeSelections.filter((selection) => {
			const selectionId = readSelectionId(selection);
			return !selectionId || !consumedSelectionIds.has(selectionId);
		}).length === 0) return paramValueInjectedSpec;
		const paramRewrittenSpec = rewriteSpecForParamSelections(paramValueInjectedSpec, selectionMap, collectScaleBoundParamNames(paramValueInjectedSpec), consumedSelectionIds);
		const finalRemainingSelections = activeSelections.filter((selection) => {
			const selectionId = readSelectionId(selection);
			return !selectionId || !consumedSelectionIds.has(selectionId);
		});
		const compositeSelections = hasCompositeChildren(paramRewrittenSpec) ? finalRemainingSelections.filter(isCompositeDataMaterializingSelection) : finalRemainingSelections;
		if (hasCompositeChildren(paramRewrittenSpec) && compositeSelections.length === 0) return applySelectionToSpec({
			widgetSpec: buildRuntimeDataProjectionSpec({
				semanticSpec: paramRewrittenSpec,
				state,
				runtime
			}) || paramRewrittenSpec,
			activeSelections: finalRemainingSelections,
			selectionEnabled: true
		});
		const rows = resolveSelectionMaterialization({
			semanticSpec: paramRewrittenSpec,
			state,
			runtime
		})?.rows;
		if (!Array.isArray(rows) || rows.length === 0) return paramRewrittenSpec;
		const nextSpec = buildRuntimeDataProjectionSpec({
			semanticSpec: paramRewrittenSpec,
			state,
			runtime
		}) || paramRewrittenSpec;
		if (hasCompositeChildren(paramRewrittenSpec)) {
			const encoding = findFirstContinuousXYEncoding(paramRewrittenSpec);
			nextSpec.data = { values: rows.filter((row) => rowMatchesCompositeSelection(row, compositeSelections, encoding)) };
			return nextSpec;
		}
		return applySelectionToSpec({
			widgetSpec: nextSpec,
			activeSelections: finalRemainingSelections,
			selectionEnabled: true
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/materializers/providerRenderPayload.js
	function clone$17(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readProvider(widgetDescription = null, widgetState = null) {
		const rawSpec = widgetState?.currentSpec || widgetState?.rawSpec || widgetDescription?.source?.spec || null;
		return widgetDescription?.provider || widgetState?.provider || widgetDescription?.source?.providerSpec?.provider || widgetState?.source?.providerSpec?.provider || (rawSpec?.$schema && String(rawSpec.$schema).includes("vega-lite") ? "vega-lite" : null) || (rawSpec?.mark || rawSpec?.encoding || rawSpec?.vconcat || rawSpec?.hconcat || rawSpec?.layer ? "vega-lite" : null) || null;
	}
	function readVegaLiteSemanticSpec(widgetDescription = null, widgetState = null) {
		return clone$17(widgetState?.currentSpec || widgetState?.rawSpec || widgetDescription?.source?.providerSpec?.spec || widgetDescription?.source?.spec || null);
	}
	function readEChartsOption(widgetDescription = null, widgetState = null) {
		return clone$17(widgetState?.currentOption || widgetState?.rawOption || widgetDescription?.source?.providerSpec?.option || null);
	}
	function readVgplotScriptText(widgetDescription = null, widgetState = null) {
		return typeof widgetState?.scriptText === "string" ? widgetState.scriptText : widgetDescription?.source?.providerSpec?.scriptText || "";
	}
	function buildWidgetRenderPayload({ widgetDescription = null, widgetState = null, runtime = null } = {}) {
		const provider = readProvider(widgetDescription, widgetState);
		const widgetId = widgetDescription?.widgetId || widgetState?.widgetId || null;
		const widgetRef = widgetDescription?.ref || widgetState?.ref || null;
		const kind = widgetDescription?.kind || widgetState?.kind || null;
		if (provider === "vega-lite") {
			const spec = buildVegaLiteRenderSpecFromRuntimeState({
				semanticSpec: readVegaLiteSemanticSpec(widgetDescription, widgetState),
				state: widgetState || {},
				runtime
			});
			return {
				widgetId,
				widgetRef,
				kind,
				provider: "vega-lite",
				providerSpec: {
					...widgetDescription?.source?.providerSpec || {},
					provider: "vega-lite",
					spec
				}
			};
		}
		if (provider === "echarts") return {
			widgetId,
			widgetRef,
			kind,
			provider: "echarts",
			providerSpec: {
				...widgetDescription?.source?.providerSpec || {},
				provider: "echarts",
				option: readEChartsOption(widgetDescription, widgetState)
			}
		};
		if (provider === "vgplot") return {
			widgetId,
			widgetRef,
			kind,
			provider: "vgplot",
			providerSpec: {
				...widgetDescription?.source?.providerSpec || {},
				provider: "vgplot",
				scriptText: readVgplotScriptText(widgetDescription, widgetState)
			}
		};
		return {
			widgetId,
			widgetRef,
			kind,
			provider,
			providerSpec: clone$17(widgetDescription?.source?.providerSpec || null)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/runtime/RuntimeOrchestrator.js
	function createRuntimeStore(options = {}) {
		return new WidgetVARuntimeStore(options);
	}
	function createWidgetVARuntime(options = {}) {
		return createRuntimeOrchestrator(options);
	}
	function clone$16(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readActionTargetRef(call = {}, result = {}) {
		return call?.target?.widgetRef || call?.targetRef || call?.params?.targetRef || result?.updatedRefs?.[0] || result?.affectedRefs?.[0] || null;
	}
	async function defaultReadRecoverableState(controller = null) {
		if (!controller || typeof controller !== "object") return null;
		if (typeof controller.readRecoverableState === "function") return controller.readRecoverableState();
		if (typeof controller.readState === "function") return controller.readState();
		if (typeof controller?.pagePort?.readState === "function") return controller.pagePort.readState();
		if (typeof controller?.runtime?.readState === "function") return controller.runtime.readState();
		return null;
	}
	async function defaultRestoreRecoverableState() {
		return {
			restored: false,
			reason: "no-restore-handler"
		};
	}
	function defaultResolveRuntime(controller = null, binding = null) {
		if (controller?.runtime) return controller.runtime;
		if (controller?.widget?.runtime) return controller.widget.runtime;
		if (binding?.runtime) return binding.runtime;
		if (controller?.pagePort?.runtime) return controller.pagePort.runtime;
		return null;
	}
	function createRuntimeControllerManager({ readRecoverableState = defaultReadRecoverableState, resolveRuntime = defaultResolveRuntime, restoreRecoverableState = defaultRestoreRecoverableState } = {}) {
		let activeRuntime = null;
		let activeController = null;
		let activeBinding = null;
		let attachmentMetadata = null;
		let recoverableState = null;
		let replacementCount = 0;
		async function captureRecoverableState(controller = activeController) {
			const nextState = await readRecoverableState(controller);
			if (nextState != null) recoverableState = clone$16(nextState);
			return clone$16(recoverableState);
		}
		async function attachController(controller, { runtime = null, binding = null, metadata = null } = {}) {
			activeController = controller || null;
			activeBinding = binding || controller?.pagePort || null;
			activeRuntime = runtime || resolveRuntime(activeController, activeBinding) || null;
			attachmentMetadata = metadata ? clone$16(metadata) : null;
			return activeController;
		}
		function seedRecoverableState(state) {
			recoverableState = clone$16(state);
			return clone$16(recoverableState);
		}
		async function replaceController(nextController, { runtime = null, binding = null, metadata = null, capturePreviousState = true, captureNextState = false, restorePreviousState = false, disposePrevious = true } = {}) {
			const previousRuntime = activeRuntime;
			const previousController = activeController;
			const previousBinding = activeBinding;
			const previousMetadata = attachmentMetadata ? clone$16(attachmentMetadata) : null;
			if (capturePreviousState && previousController) await captureRecoverableState(previousController);
			activeController = nextController || null;
			activeBinding = binding || nextController?.pagePort || null;
			activeRuntime = runtime || resolveRuntime(activeController, activeBinding) || null;
			attachmentMetadata = metadata ? clone$16(metadata) : null;
			replacementCount += 1;
			if (captureNextState && activeController) await captureRecoverableState(activeController);
			let restoreResult = {
				restored: false,
				reason: restorePreviousState ? "missing-controller-or-state" : "not-requested"
			};
			if (restorePreviousState && activeController && recoverableState != null) restoreResult = await restoreRecoverableState(activeController, clone$16(recoverableState));
			if (disposePrevious && previousController && typeof previousController.dispose === "function") previousController.dispose();
			return {
				previousRuntime,
				previousController,
				previousBinding,
				previousMetadata,
				activeRuntime,
				activeController,
				activeBinding,
				attachmentMetadata: clone$16(attachmentMetadata),
				recoverableState: clone$16(recoverableState),
				restoreResult,
				replacementCount
			};
		}
		async function clearController({ capturePreviousState = true, disposePrevious = true, preserveRecoverableState = true } = {}) {
			const result = await replaceController(null, {
				capturePreviousState,
				captureNextState: false,
				disposePrevious
			});
			if (!preserveRecoverableState) recoverableState = null;
			activeBinding = null;
			attachmentMetadata = null;
			return result;
		}
		function canContinueRequest({ requirePagePort = false } = {}) {
			if (!activeController) return false;
			if (!requirePagePort) return true;
			return Boolean(activeBinding || activeController?.pagePort);
		}
		async function runWithActiveController(handler) {
			if (typeof handler !== "function") throw new Error("RuntimeOrchestrator controller manager requires a handler function.");
			if (!activeController) throw new Error("RuntimeOrchestrator does not have an active controller.");
			return handler(activeController);
		}
		function describeRuntimeManager() {
			return {
				hasActiveRuntime: Boolean(activeRuntime),
				hasActiveController: Boolean(activeController),
				hasActiveBinding: Boolean(activeBinding),
				recoverableStateId: recoverableState?.stateId || null,
				replacementCount
			};
		}
		return {
			attachController,
			replaceController,
			clearController,
			captureRecoverableState,
			runWithActiveController,
			readActiveRuntime() {
				return activeRuntime;
			},
			readActiveController() {
				return activeController;
			},
			readActiveBinding() {
				return activeBinding;
			},
			readAttachmentMetadata() {
				return clone$16(attachmentMetadata);
			},
			readRecoverableState() {
				return clone$16(recoverableState);
			},
			seedRecoverableState,
			describeRuntimeManager,
			canContinueRequest
		};
	}
	function normalizeReplayOptions(input) {
		if (typeof input === "string") return { stateId: input };
		if (input && typeof input === "object") return { ...input };
		return {};
	}
	function createResponseRecorderSurface(traceRecorder) {
		return {
			recordResponse: traceRecorder.recordResponse.bind(traceRecorder),
			readLatestResponse: traceRecorder.readLatestResponse.bind(traceRecorder),
			listResponses: traceRecorder.listResponses.bind(traceRecorder),
			describeRecorder: traceRecorder.describeResponseRecorder.bind(traceRecorder)
		};
	}
	function makeVerifiedRuntimeActionResult(result = {}) {
		return {
			ok: false,
			beforeStateId: null,
			actionResult: null,
			afterView: null,
			verification: null,
			verificationHints: [],
			traceEvidence: null,
			finalSnapshot: null,
			linkPropagation: [],
			latestCoordinationResult: null,
			...clone$16(result)
		};
	}
	function createRuntimeOrchestrator(options = {}) {
		const defaultHostBridge = createDefaultWidgetVAHostBridge();
		const providedHostBridge = options.hostBridge || null;
		const derivedHostBridge = providedHostBridge ? null : createWidgetVAHostBridge({
			getState: options.getState,
			getAppState: options.getAppState,
			subscribe: options.subscribe,
			subscribeAppState: options.subscribeAppState
		});
		const hostBridge = providedHostBridge ? {
			...defaultHostBridge,
			...providedHostBridge
		} : derivedHostBridge || defaultHostBridge;
		const store = createRuntimeStore();
		const runtimeManager = createRuntimeControllerManager();
		const traceRecorder = new TraceRecorder({ store });
		const responseRecorder = createResponseRecorderSurface(traceRecorder);
		const dataQueryEngine = options.dataQueryEngine || createDataQueryEngine({ kind: "js_array" });
		const dataQueryExecutor = new DataQueryExecutor({
			store,
			dataQueryEngine,
			traceRecorder,
			hostBridge
		});
		const coordinationEngine = new CoordinationEngine({ store });
		const buildReplayContext = () => ({
			baselineSpec: hostBridge.readBaselineSpec() ?? null,
			currentSpec: hostBridge.readCurrentSpec() ?? null,
			workspaceSpec: hostBridge.readWorkspaceSpec() ?? null,
			planningRequest: hostBridge.readPlanningRequest() ?? null,
			runMode: hostBridge.readRunMode() || "goal_oriented",
			userIntent: hostBridge.readUserIntent() || ""
		});
		const sync = () => {
			const replayContext = buildReplayContext();
			const workspace = materializeWorkspace({
				appId: "widgetva-app",
				workspaceId: hostBridge.readSessionId() || "main",
				sessionId: hostBridge.readSessionId(),
				spec: hostBridge.readCurrentSpec(),
				workspaceSpec: hostBridge.readWorkspaceSpec(),
				planningRequest: {
					...hostBridge.readPlanningRequest() || {},
					runMode: hostBridge.readRunMode(),
					userIntent: hostBridge.readUserIntent() || null
				},
				selection: hostBridge.readCurrentSelection(),
				selections: hostBridge.readCurrentSelections(),
				focusedWidgetRef: hostBridge.readFocusedWidgetRef(),
				focusState: hostBridge.readFocusState(),
				highlightState: hostBridge.readHighlightState(),
				viewportState: normalizeViewportState(hostBridge.readViewportState()),
				previousState: store.readState(),
				currentBranchId: store.currentBranchId,
				stateManager: store.stateManager,
				replayContext
			});
			store.replaceWorkspace(workspace);
		};
		sync();
		const unsubscribe = hostBridge.subscribe(sync);
		const actionExecutor = new ActionExecutor({
			store,
			sync,
			hostBridge,
			coordinationEngine,
			traceRecorder
		});
		registerSharedWidgetRuntimeActions(actionExecutor);
		const executeActionRaw = actionExecutor.run.bind(actionExecutor);
		const runDataQuery = dataQueryExecutor.run.bind(dataQueryExecutor);
		const perceptionExecutor = new PerceptionExecutor({
			store,
			dataQueryEngine,
			dataQueryExecutor,
			traceRecorder,
			coordinationEngine,
			hostBridge
		});
		const queryPerception = perceptionExecutor.query.bind(perceptionExecutor);
		const describeWorkspace = () => readWorkspaceDescriptionFromStore(store, {
			actionExecutor,
			perceptionExecutor
		});
		const readView = (options = {}) => readWorkspaceStateFromStore(store, options);
		const readState = (options = {}) => readWorkspaceStateFromStore(store, options);
		const readWidgetRenderPayload = (widgetIdOrRef = null) => {
			const widgetDescription = store.listWidgetDescriptions().find((widget) => widget?.widgetId === widgetIdOrRef || widget?.ref === widgetIdOrRef) || null;
			const widgetRef = widgetDescription?.ref || widgetIdOrRef || null;
			return buildWidgetRenderPayload({
				widgetDescription,
				widgetState: widgetRef ? store.getWidgetState?.(widgetRef) || null : null,
				runtime
			});
		};
		const readLatestCoordinationResult = () => typeof options.readLatestCoordinationResult === "function" ? options.readLatestCoordinationResult() : null;
		const readTrace = (options = {}) => readInteractionTraceFromStore(store, options);
		const captureCurrentRecoverableState = () => {
			runtimeManager.seedRecoverableState(readState());
		};
		const materializeProviderAction = async (call, result) => {
			if (options.materializeProviderActions !== true || result?.ok !== true) return result;
			if (typeof hostBridge.executeProviderAction !== "function") return result;
			const targetRef = readActionTargetRef(call, result);
			const targetWidget = targetRef ? store.getResolvedWidgetForTarget?.(targetRef) || store.getResolvedWidget?.(targetRef) || store.getWidgetState?.(targetRef) || null : null;
			try {
				const materialization = await hostBridge.executeProviderAction({
					targetWidget,
					actionName: result?.actionName || call?.name || null,
					params: call?.params || {},
					statePatch: result?.statePatch || null,
					result
				});
				if (materialization?.handled !== true) return result;
				return {
					...result,
					providerMaterialization: materialization
				};
			} catch (error) {
				return {
					...result,
					ok: false,
					error: {
						code: "PROVIDER_MATERIALIZATION_FAILED",
						message: error instanceof Error ? error.message : String(error)
					}
				};
			}
		};
		const executeAction = async (call) => {
			const result = await materializeProviderAction(call, await executeActionRaw(call));
			captureCurrentRecoverableState();
			return result;
		};
		const jumpToState = async (options = {}) => executeAction({
			callId: options.callId || `jump_${Date.now()}`,
			name: "workspace.jumpToState",
			actor: options.actor || "agent",
			params: { stateId: options.stateId || null }
		});
		const branchFromState = async (options = {}) => executeAction({
			callId: options.callId || `branch_${Date.now()}`,
			name: "workspace.branchFromState",
			actor: options.actor || "agent",
			params: {
				stateId: options.stateId || null,
				branchLabel: options.branchLabel || null
			}
		});
		const replay = async (stateIdOrOptions) => jumpToState(normalizeReplayOptions(stateIdOrOptions));
		if (options.registerDefaultWidgetFamilies ?? false) registerDefaultWidgetFamiliesRuntime({
			actionExecutor,
			perceptionExecutor
		});
		const registerWidgetFamily = (family) => registerRuntimeWidgetFamily(family, {
			actionExecutor,
			perceptionExecutor,
			store
		});
		for (const family of Array.isArray(options.widgetFamilies) ? options.widgetFamilies : []) registerWidgetFamily(family);
		const executeVerifiedAction = async (call, verifyOptions = {}) => {
			const beforeStateId = readState()?.stateId || null;
			const actionResult = await executeAction(call);
			captureCurrentRecoverableState();
			const latestCoordinationResult = readLatestCoordinationResult();
			if (!actionResult?.ok) return makeVerifiedRuntimeActionResult({
				ok: false,
				beforeStateId,
				actionResult,
				latestCoordinationResult
			});
			const afterView = readState({
				refs: actionResult.updatedRefs,
				deltaSince: verifyOptions?.includeDeltaSince ? beforeStateId : void 0
			});
			const verification = verifyOptions?.verify === false ? null : await queryPerception({
				callId: `${call?.callId || "call"}_verify`,
				name: "perception.verifyActionEffect",
				actor: call?.actor || "agent",
				...call?.target?.widgetRef ? { target: { widgetRef: call.target.widgetRef } } : {},
				params: {
					actionName: call?.name,
					actionParams: call?.params || {},
					stateId: actionResult.stateId,
					refs: actionResult.updatedRefs
				}
			});
			const finalSnapshot = actionResult.stateId ? readSnapshotFromStore(store, actionResult.stateId, {
				refs: verifyOptions?.includeFinalSnapshotRefs === false ? void 0 : actionResult.updatedRefs,
				includeMeta: true
			}) : null;
			const verificationOk = verification?.ok !== false && (verification?.result?.verified ?? true);
			return makeVerifiedRuntimeActionResult({
				ok: Boolean(actionResult?.ok) && verificationOk,
				beforeStateId,
				actionResult,
				afterView,
				verification,
				verificationHints: Array.isArray(actionResult?.verificationHints) ? actionResult.verificationHints : [],
				finalSnapshot,
				latestCoordinationResult
			});
		};
		const uninstallPagePort = installWidgetVAPagePort({
			api: {
				describeWorkspace,
				readState,
				readView,
				readSnapshot: (snapshotOptions = {}) => readSnapshotFromStore(store, snapshotOptions.stateId, snapshotOptions),
				listStateHistory: (historyOptions = {}) => listStateSnapshotsFromStore(store, historyOptions),
				listBranches: () => listBranchesFromStore(store),
				readTrace,
				getInteractionTrace: readTrace,
				getTraceGraph: (graphOptions = {}) => buildTraceGraphFromStore(store, graphOptions),
				getLatestAgentResponse: (responseOptions = {}) => responseRecorder.readLatestResponse(responseOptions),
				listAgentResponses: (responseOptions = {}) => responseRecorder.listResponses(responseOptions.limit, responseOptions),
				evaluateLinkPropagation: (propagationOptions = {}) => coordinationEngine.evaluatePropagation({
					sourceRef: propagationOptions.sourceRef || null,
					state: propagationOptions.state || readState()
				}),
				recordAgentResponse: (record = {}) => responseRecorder.recordResponse(record)
			},
			executeAction,
			executeVerifiedAction,
			jumpToState,
			branchFromState,
			replay,
			queryPerception,
			queryData: (call) => dataQueryExecutor.run(call)
		});
		function inferHumanActionName(selection) {
			if (!selection) return "widget.clearSelection";
			const sourceWidgetId = selection?.source_widget_id;
			const declaredAction = store.listWidgetDescriptions().find((widget) => widget.widgetId === sourceWidgetId)?.humanInteraction?.actionName;
			if (declaredAction) return declaredAction;
			return "widget.updateSelection";
		}
		async function applyHumanSelection(selection) {
			const actionName = inferHumanActionName(selection);
			const sourceWidget = selection?.source_widget_id ? store.listWidgetDescriptions().find((widget) => widget.widgetId === selection.source_widget_id) || null : null;
			const result = await executeAction({
				callId: `human_${Date.now()}`,
				name: actionName,
				actor: "human",
				targetRef: sourceWidget?.ref || void 0,
				params: selection || {}
			});
			return {
				updatedRefs: result?.updatedRefs || [],
				stateId: result?.stateId || null
			};
		}
		const runtime = {
			store,
			actionExecutor,
			coordinationEngine,
			dataQueryExecutor,
			perceptionExecutor,
			responseRecorder,
			runtimeManager,
			registerWidgetFamily,
			describeRuntimeManager: runtimeManager.describeRuntimeManager,
			readRecoverableState: runtimeManager.readRecoverableState,
			applyHumanSelection,
			describeWorkspace,
			readWidgetRenderPayload,
			readView,
			readState,
			readTrace,
			executeAction,
			executeVerifiedAction,
			queryPerception,
			queryData: runDataQuery,
			runDataQuery,
			getTrace: readTrace,
			jumpToState,
			branchFromState,
			replay,
			dispose() {
				captureCurrentRecoverableState();
				runtimeManager.clearController({
					capturePreviousState: false,
					disposePrevious: false
				});
				unsubscribe?.();
				uninstallPagePort?.();
			}
		};
		const runtimeController = {
			readRecoverableState: () => readState(),
			readState,
			pagePort: globalThis.window?.__widgetVA || null,
			runtime
		};
		runtimeManager.attachController(runtimeController, { runtime });
		captureCurrentRecoverableState();
		return runtime;
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/rendering/WidgetRendererBridge.js
	function noop() {}
	function bindRuntimeStateSubscription({ runtime, widgetRef, adapter, view, surface, spec, interactionConfig, initialWidgetState }) {
		const store = runtime?.store || null;
		if (!store || typeof store.subscribe !== "function") return noop;
		if (!widgetRef || typeof adapter?.applyState !== "function") return noop;
		if (!view && !surface) return noop;
		let lastAppliedState = initialWidgetState || null;
		return store.subscribe(() => {
			const nextWidgetState = store.getWidgetState?.(widgetRef) || null;
			if (!nextWidgetState || nextWidgetState === lastAppliedState) return;
			lastAppliedState = nextWidgetState;
			Promise.resolve(adapter.applyState({
				runtime,
				view,
				surface,
				spec,
				state: nextWidgetState,
				interactionConfig
			})).catch(() => {});
		});
	}
	function attachWidgetRendererBridge({ runtime, widgetRef, widgetAdapter = null, widgetState, view, surface, spec, interactionConfig, selectionSourceWidgetId, actionTargetRef, onActionCall, onSelectionChange, bindHumanInteractions = true }) {
		const adapter = widgetAdapter || runtime?.store?.getWidgetAdapter?.(widgetRef) || null;
		const bindCleanup = bindHumanInteractions ? adapter?.bindHumanInteractions?.({
			view,
			surface,
			spec,
			state: widgetState,
			interactionConfig,
			selectionSourceWidgetId,
			actionTargetRef,
			onActionCall,
			onSelectionChange
		}) || noop : noop;
		const unsubscribe = bindRuntimeStateSubscription({
			runtime,
			widgetRef,
			adapter,
			view,
			surface,
			spec,
			interactionConfig,
			initialWidgetState: widgetState
		});
		return {
			adapter,
			cleanup() {
				if (typeof bindCleanup === "function") bindCleanup();
				unsubscribe();
			}
		};
	}
	async function applyWidgetRuntimeState({ runtime, widgetRef, widgetAdapter = null, widgetState, view, surface, spec, interactionConfig }) {
		const adapter = widgetAdapter || runtime?.store?.getWidgetAdapter?.(widgetRef) || null;
		if (!adapter?.applyState || !widgetState || !view && !surface) return;
		await adapter.applyState({
			runtime,
			view,
			surface,
			spec,
			state: widgetState,
			interactionConfig
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vgplot/vgplotBinding.js
	function clone$15(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function normalizeStringArray(values) {
		return Array.isArray(values) ? values.filter((value) => typeof value === "string" && value.length > 0) : [];
	}
	function normalizeVgplotBinding(binding = null) {
		if (!binding || typeof binding !== "object" || Array.isArray(binding)) return null;
		return {
			widgetRef: typeof binding.widgetRef === "string" ? binding.widgetRef : null,
			dataRef: typeof binding.dataRef === "string" ? binding.dataRef : null,
			widgetKind: typeof binding.widgetKind === "string" ? binding.widgetKind : null,
			selectionNames: normalizeStringArray(binding.selectionNames),
			paramNames: normalizeStringArray(binding.paramNames),
			plotBindings: Array.isArray(binding.plotBindings) ? binding.plotBindings.filter((entry) => entry && typeof entry === "object" && !Array.isArray(entry)).map((entry) => ({
				plotId: typeof entry.plotId === "string" ? entry.plotId : null,
				role: typeof entry.role === "string" ? entry.role : null,
				xField: typeof entry.xField === "string" ? entry.xField : null,
				yField: typeof entry.yField === "string" ? entry.yField : null,
				colorField: typeof entry.colorField === "string" ? entry.colorField : null
			})) : [],
			fields: normalizeStringArray(binding.fields)
		};
	}
	function attachVgplotBinding({ view = null, runtime = null, binding = null } = {}) {
		const normalized = normalizeVgplotBinding(binding);
		if (!normalized) return null;
		if (view && typeof view === "object") {
			view.__widgetvaVgplotBinding = clone$15(normalized);
			if (view.__widgetvaVgplotRuntime && typeof view.__widgetvaVgplotRuntime === "object") view.__widgetvaVgplotRuntime.binding = clone$15(normalized);
		}
		if (runtime && typeof runtime === "object" && runtime.__widgetvaVgplotRuntime && typeof runtime.__widgetvaVgplotRuntime === "object") runtime.__widgetvaVgplotRuntime.binding = clone$15(normalized);
		return normalized;
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vgplot/vgplotRuntimeCapture.js
	function ensureMap(value) {
		if (value instanceof Map) return new Map(value);
		if (Array.isArray(value)) return new Map(value);
		if (value != null && typeof value === "object" && !Array.isArray(value)) return new Map(Object.entries(value));
		return /* @__PURE__ */ new Map();
	}
	function isObjectRecord(value) {
		return value != null && typeof value === "object" && !Array.isArray(value);
	}
	function looksLikePlot(value) {
		return isObjectRecord(value) && (typeof value.getAttribute === "function" || typeof value.setAttribute === "function" || typeof value.addAttributeListener === "function");
	}
	function clone$14(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readMetadataBag(target) {
		if (!isObjectRecord(target)) return null;
		return target.__widgetvaVgplotMeta || target.__widgetvaVgplot || target.widgetvaVgplot || null;
	}
	function normalizeNamedEntries(value, { valueKey = "value", additionalKeys = [] } = {}) {
		if (value instanceof Map) return [...value.entries()];
		if (Array.isArray(value)) return value.flatMap((entry, index) => {
			if (Array.isArray(entry) && entry.length >= 2 && typeof entry[0] === "string") return [[entry[0], entry[1]]];
			if (isObjectRecord(entry)) {
				const entryName = typeof entry.name === "string" ? entry.name : typeof entry.id === "string" ? entry.id : typeof entry.key === "string" ? entry.key : null;
				if (!entryName) return [];
				return [[entryName, entry[valueKey] || additionalKeys.map((key) => entry[key]).find(Boolean) || entry.param || entry.selection || entry.plot || entry]];
			}
			if (typeof entry === "string" && entry.length > 0) return [[entry, entry]];
			return [[`entry_${index}`, entry]];
		});
		if (isObjectRecord(value)) return Object.entries(value);
		return [];
	}
	function mergeNamedEntries(target, entries = []) {
		for (const [name, value] of entries) {
			if (typeof name !== "string" || name.length === 0 || value == null) continue;
			if (!target.has(name)) target.set(name, value);
		}
	}
	function readContextCandidates({ view = null, runtime = null, apiContext = null } = {}) {
		const candidates = [
			apiContext,
			runtime?.context,
			runtime?.apiContext,
			runtime?.__widgetvaVgplotContext,
			view?.context,
			view?.apiContext,
			view?.__widgetvaVgplotContext,
			view?.ownerDocument?.defaultView?.vg?.context,
			globalThis?.vg?.context
		].filter(isObjectRecord);
		const uniqueContexts = [];
		for (const candidate of candidates) if (!uniqueContexts.includes(candidate)) uniqueContexts.push(candidate);
		return uniqueContexts;
	}
	function collectNamedPlotsFromContext(context = null) {
		if (!isObjectRecord(context)) return /* @__PURE__ */ new Map();
		const plots = /* @__PURE__ */ new Map();
		mergeNamedEntries(plots, normalizeNamedEntries(context.namedPlots, { valueKey: "plot" }));
		mergeNamedEntries(plots, normalizeNamedEntries(context.plotsByName, { valueKey: "plot" }));
		return new Map([...plots.entries()].filter(([, value]) => looksLikePlot(value)));
	}
	function readPlotWidgetKind(plotEntry = null) {
		if (typeof plotEntry?.widgetKind === "string" && plotEntry.widgetKind.length > 0) return plotEntry.widgetKind;
		const metadata = readMetadataBag(plotEntry);
		if (typeof metadata?.widgetKind === "string" && metadata.widgetKind.length > 0) return metadata.widgetKind;
		return null;
	}
	function collectPlotEntries({ view = null, runtime = null, contexts = [] } = {}) {
		const plots = /* @__PURE__ */ new Map();
		for (const context of contexts) for (const [plotId, plot] of collectNamedPlotsFromContext(context).entries()) plots.set(plotId, {
			plot,
			widgetKind: readPlotWidgetKind(plot) || null
		});
		if (looksLikePlot(view) && !plots.has("plot_main")) plots.set("plot_main", {
			plot: view,
			widgetKind: readPlotWidgetKind(view) || null
		});
		if (looksLikePlot(runtime) && !plots.has("plot_runtime")) plots.set("plot_runtime", {
			plot: runtime,
			widgetKind: readPlotWidgetKind(runtime) || null
		});
		return plots;
	}
	function collectContextLevelEntries(contexts = [], keyNames = []) {
		const entries = /* @__PURE__ */ new Map();
		for (const context of contexts) {
			for (const keyName of keyNames) mergeNamedEntries(entries, normalizeNamedEntries(context?.[keyName]));
			const metadata = readMetadataBag(context);
			for (const keyName of keyNames) mergeNamedEntries(entries, normalizeNamedEntries(metadata?.[keyName]));
		}
		return entries;
	}
	function collectPlotLevelEntries(plots = /* @__PURE__ */ new Map(), keyNames = []) {
		const entries = /* @__PURE__ */ new Map();
		for (const [plotId, entry] of plots.entries()) {
			const plot = entry?.plot || null;
			const metadata = readMetadataBag(plot);
			for (const keyName of keyNames) {
				mergeNamedEntries(entries, normalizeNamedEntries(plot?.[keyName]));
				mergeNamedEntries(entries, normalizeNamedEntries(metadata?.[keyName]));
			}
			if (metadata?.plotId && !entries.has(metadata.plotId) && keyNames.includes("plots")) entries.set(metadata.plotId, plot);
			if (!entries.has(plotId) && keyNames.includes("plots")) entries.set(plotId, plot);
		}
		return entries;
	}
	function inferSelectionType(value) {
		if (typeof value?.type === "string" && value.type.length > 0) return value.type;
		if (typeof value?.selectionType === "string" && value.selectionType.length > 0) return value.selectionType;
		return null;
	}
	function inferParamType(value) {
		if (typeof value?.type === "string" && value.type.length > 0) return value.type;
		if (typeof value?.paramType === "string" && value.paramType.length > 0) return value.paramType;
		return null;
	}
	function finalizeSelectionEntries(entries = /* @__PURE__ */ new Map()) {
		const selections = /* @__PURE__ */ new Map();
		for (const [name, value] of entries.entries()) {
			if (!isObjectRecord(value)) continue;
			if (!inferSelectionType(value) && typeof value.get !== "function" && !Object.prototype.hasOwnProperty.call(value, "value")) continue;
			selections.set(name, value);
		}
		return selections;
	}
	function finalizeParamEntries(entries = /* @__PURE__ */ new Map()) {
		const params = /* @__PURE__ */ new Map();
		for (const [name, value] of entries.entries()) {
			if (!isObjectRecord(value)) continue;
			if (!inferParamType(value) && typeof value.get !== "function" && !Object.prototype.hasOwnProperty.call(value, "value")) continue;
			params.set(name, value);
		}
		return params;
	}
	function createEmptyVgplotRuntime() {
		return {
			provider: "vgplot",
			plots: /* @__PURE__ */ new Map(),
			selections: /* @__PURE__ */ new Map(),
			params: /* @__PURE__ */ new Map(),
			context: null,
			binding: null
		};
	}
	function normalizeVgplotRuntime(runtime = null) {
		if (!isObjectRecord(runtime)) return createEmptyVgplotRuntime();
		return {
			provider: "vgplot",
			plots: ensureMap(runtime.plots),
			selections: ensureMap(runtime.selections),
			params: ensureMap(runtime.params),
			context: runtime.context || null,
			binding: runtime.binding && typeof runtime.binding === "object" && !Array.isArray(runtime.binding) ? clone$14(runtime.binding) : null
		};
	}
	function captureStructuredVgplotRuntime({ view = null, runtime = null, apiContext = null } = {}) {
		const contexts = readContextCandidates({
			view,
			runtime,
			apiContext
		});
		const plots = collectPlotEntries({
			view,
			runtime,
			contexts
		});
		const selectionEntries = /* @__PURE__ */ new Map();
		const paramEntries = /* @__PURE__ */ new Map();
		mergeNamedEntries(selectionEntries, collectContextLevelEntries(contexts, [
			"selections",
			"namedSelections",
			"selectionsByName"
		]).entries());
		mergeNamedEntries(paramEntries, collectContextLevelEntries(contexts, [
			"params",
			"namedParams",
			"paramsByName"
		]).entries());
		mergeNamedEntries(selectionEntries, collectPlotLevelEntries(plots, [
			"selections",
			"namedSelections",
			"selectionSet",
			"interactors",
			"interactorSet"
		]).entries());
		mergeNamedEntries(paramEntries, collectPlotLevelEntries(plots, [
			"params",
			"namedParams",
			"paramSet",
			"interactors",
			"interactorSet"
		]).entries());
		return normalizeVgplotRuntime({
			provider: "vgplot",
			plots,
			selections: finalizeSelectionEntries(selectionEntries),
			params: finalizeParamEntries(paramEntries),
			context: contexts[0] || null,
			binding: null
		});
	}
	function captureVgplotRuntime({ view = null, runtime = null, apiContext = null } = {}) {
		if (isObjectRecord(runtime?.__widgetvaVgplotRuntime)) return normalizeVgplotRuntime(runtime.__widgetvaVgplotRuntime);
		if (isObjectRecord(view?.__widgetvaVgplotRuntime)) return normalizeVgplotRuntime(view.__widgetvaVgplotRuntime);
		return captureStructuredVgplotRuntime({
			view,
			runtime,
			apiContext
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vgplot/vgplotSupportedKinds.js
	var VGPLOT_SUPPORTED_WIDGET_KINDS = Object.freeze([
		"scatter",
		"bar",
		"line",
		"heatmap"
	]);
	function isVgplotSupportedWidgetKind(widgetKind) {
		return typeof widgetKind === "string" && VGPLOT_SUPPORTED_WIDGET_KINDS.includes(widgetKind);
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vgplot/vgplotCapabilityResolver.js
	var PERCEPTION_NAMES_BY_WIDGET_KIND = Object.freeze({
		scatter: [
			"perception.computeCorrelation",
			"perception.findOutliers",
			"perception.findExtremes"
		],
		bar: ["perception.compareGroups", "perception.findExtremes"],
		line: [
			"perception.detectAnomalies",
			"perception.findExtremes",
			"perception.compareGroups"
		],
		heatmap: ["perception.findExtremes", "perception.findOutliers"]
	});
	var CONFIG_ACTIONS_BY_WIDGET_KIND = Object.freeze({
		scatter: ["scatter.identifyClusters", "scatter.showRegression"],
		bar: [
			"bar.toggleStackMode",
			"bar.sortBars",
			"bar.expandStack",
			"bar.highlightTopN",
			"bar.filterSubcategories",
			"bar.addBars",
			"bar.removeBars",
			"bar.addBarItems",
			"bar.removeBarItems"
		],
		line: [
			"line.highlightTrend",
			"line.showMovingAverage",
			"line.boldLines",
			"line.filterLines",
			"line.drillDownXAxis",
			"line.resetDrilldownXAxis",
			"line.resampleXAxis",
			"line.resetResampleXAxis"
		],
		heatmap: [
			"heatmap.adjustColorScale",
			"heatmap.transpose",
			"heatmap.highlightRegionByValue",
			"heatmap.drilldownAxis",
			"heatmap.resetDrilldown",
			"heatmap.addMarginalBars",
			"heatmap.thresholdMask",
			"heatmap.clusterRowsCols"
		]
	});
	function readSelectionType(selection) {
		const value = typeof selection?.get === "function" ? selection.get() : selection?.value;
		if (value && typeof value === "object" && typeof value.type === "string") return value.type;
		if (typeof selection?.type === "string") return selection.type;
		return null;
	}
	function readParamType(param) {
		const value = typeof param?.get === "function" ? param.get() : param?.value;
		if (value && typeof value === "object" && typeof value.type === "string") return value.type;
		if (typeof param?.type === "string") return param.type;
		return null;
	}
	function readPlotDomains(plot) {
		if (!plot) return {
			hasXDomain: false,
			hasYDomain: false
		};
		if (typeof plot.getAttribute === "function") return {
			hasXDomain: plot.getAttribute("xDomain") != null,
			hasYDomain: plot.getAttribute("yDomain") != null
		};
		if (plot.attributes instanceof Map) return {
			hasXDomain: plot.attributes.has("xDomain"),
			hasYDomain: plot.attributes.has("yDomain")
		};
		return {
			hasXDomain: false,
			hasYDomain: false
		};
	}
	function unique(values = []) {
		return [...new Set(values.filter((value) => typeof value === "string" && value.length > 0))];
	}
	function hasAnySelectionType(selectionTypes = [], allowedTypes = []) {
		return allowedTypes.some((selectionType) => selectionTypes.includes(selectionType));
	}
	function readPlotMetadata$1(entry = null) {
		const plot = entry?.plot || null;
		return plot?.__widgetvaVgplotMeta || plot?.__widgetvaVgplot || plot?.widgetvaVgplot || null;
	}
	function readConfigActionNames(entry = null) {
		const metadata = readPlotMetadata$1(entry);
		const configActions = entry?.configActions || metadata?.configActions || null;
		if (!configActions) return [];
		if (configActions instanceof Map) return [...configActions.keys()].filter((name) => typeof name === "string" && name.length > 0);
		if (typeof configActions === "object" && !Array.isArray(configActions)) return Object.keys(configActions).filter((name) => typeof name === "string" && name.length > 0);
		return [];
	}
	function buildSelectionTypeToNamesMap(selections = /* @__PURE__ */ new Map()) {
		const result = {};
		for (const [selectionName, selection] of selections.entries()) {
			const selectionType = readSelectionType(selection);
			if (typeof selectionType !== "string" || selectionType.length === 0) continue;
			if (!Array.isArray(result[selectionType])) result[selectionType] = [];
			result[selectionType].push(selectionName);
		}
		return result;
	}
	function buildParamTypeToNamesMap(params = /* @__PURE__ */ new Map()) {
		const result = {};
		for (const [paramName, param] of params.entries()) {
			const paramType = readParamType(param);
			if (typeof paramType !== "string" || paramType.length === 0) continue;
			if (!Array.isArray(result[paramType])) result[paramType] = [];
			result[paramType].push(paramName);
		}
		return result;
	}
	function resolveVgplotCapabilities({ widgetKind = null, view = null, runtime = null } = {}) {
		const capturedRuntime = captureVgplotRuntime({
			view,
			runtime
		});
		const selectionTypes = unique([...capturedRuntime.selections.values()].map(readSelectionType));
		const paramTypes = unique([...capturedRuntime.params.values()].map(readParamType));
		const plotStates = [...capturedRuntime.plots.values()].map((entry) => readPlotDomains(entry?.plot || null));
		const supportsXDomain = plotStates.some((state) => state.hasXDomain);
		const supportsYDomain = plotStates.some((state) => state.hasYDomain);
		const supportsPanZoom = paramTypes.includes("panZoom");
		const supportsPanZoomX = paramTypes.includes("panZoomX");
		const supportsPanZoomY = paramTypes.includes("panZoomY");
		const supported = isVgplotSupportedWidgetKind(widgetKind);
		const configActionNames = unique([...capturedRuntime.plots.values()].flatMap((entry) => readConfigActionNames(entry)));
		const actionNames = [];
		if (supported && widgetKind === "scatter" && hasAnySelectionType(selectionTypes, [
			"intervalXY",
			"intervalX",
			"intervalY",
			"region"
		])) actionNames.push("scatter.brushRegion", "scatter.selectRegion");
		if (supported && widgetKind === "scatter" && selectionTypes.some((type) => type?.startsWith?.("toggle"))) actionNames.push("scatter.filterCategorical");
		if (supported && widgetKind === "scatter" && (supportsXDomain && supportsYDomain || supportsPanZoom)) actionNames.push("scatter.zoomDomain");
		if (supported && widgetKind === "bar" && selectionTypes.some((type) => type?.startsWith?.("toggle"))) actionNames.push("bar.clickCategory", "bar.selectCategory", "bar.filterCategories");
		if (supported && Array.isArray(CONFIG_ACTIONS_BY_WIDGET_KIND[widgetKind])) {
			for (const actionName of CONFIG_ACTIONS_BY_WIDGET_KIND[widgetKind]) if (configActionNames.includes(actionName)) actionNames.push(actionName);
		}
		if (supported && widgetKind === "line" && selectionTypes.some((type) => type?.startsWith?.("toggle"))) actionNames.push("line.selectSeries");
		if (supported && widgetKind === "line" && selectionTypes.some((type) => type?.startsWith?.("toggle"))) actionNames.push("line.focusLines");
		if (supported && widgetKind === "line" && selectionTypes.includes("nearestX")) actionNames.push("line.selectXValue");
		if (supported && widgetKind === "line" && (supportsXDomain || supportsPanZoom || supportsPanZoomX)) actionNames.push("line.zoomXRegion");
		if (supported && widgetKind === "heatmap" && selectionTypes.some((type) => type?.startsWith?.("toggle"))) actionNames.push("heatmap.filterCells", "heatmap.selectCell");
		if (supported && widgetKind === "heatmap" && hasAnySelectionType(selectionTypes, [
			"intervalXY",
			"intervalX",
			"intervalY",
			"region"
		])) actionNames.push("heatmap.selectSubmatrix", "heatmap.filterCellsByRegion");
		if (supported && widgetKind === "heatmap" && hasAnySelectionType(selectionTypes, [
			"intervalXY",
			"intervalX",
			"intervalY",
			"region"
		])) actionNames.push("heatmap.highlightRegion");
		return {
			widgetKind,
			supported: Boolean(supported),
			supportedActionNames: unique(actionNames),
			supportedPerceptionNames: supported ? [...PERCEPTION_NAMES_BY_WIDGET_KIND[widgetKind] || []] : [],
			selectionTypes,
			selectionTypeToNames: buildSelectionTypeToNamesMap(capturedRuntime.selections),
			paramTypes,
			paramTypeToNames: buildParamTypeToNamesMap(capturedRuntime.params),
			plotIds: [...capturedRuntime.plots.keys()],
			selectionNames: [...capturedRuntime.selections.keys()],
			paramNames: [...capturedRuntime.params.keys()],
			configActionNames,
			supportsXDomain,
			supportsYDomain,
			supportsPanZoom,
			supportsPanZoomX,
			supportsPanZoomY
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vgplot/vgplotController.js
	function readPlotEntry(runtime, plotId) {
		return runtime?.plots?.get?.(plotId) || null;
	}
	function readPlotObject(runtime, plotId) {
		return readPlotEntry(runtime, plotId)?.plot || null;
	}
	function readRuntimeValue(target) {
		if (target == null) return null;
		if (typeof target.get === "function") try {
			return target.get();
		} catch {
			return null;
		}
		if (typeof target === "object" && Object.prototype.hasOwnProperty.call(target, "value")) return target.value;
		return target;
	}
	function safeCall(listener, payload) {
		try {
			listener?.(payload);
		} catch {}
	}
	function readPlotMetadata(plotEntry = null) {
		const plot = plotEntry?.plot || null;
		return plot?.__widgetvaVgplotMeta || plot?.__widgetvaVgplot || plot?.widgetvaVgplot || null;
	}
	function readConfigActionHandler(plotEntry = null, actionName = null) {
		if (typeof actionName !== "string" || actionName.length === 0) return null;
		const metadata = readPlotMetadata(plotEntry);
		const configActions = plotEntry?.configActions || metadata?.configActions || null;
		if (!configActions) return null;
		if (configActions instanceof Map) return configActions.get(actionName) || null;
		if (typeof configActions === "object" && !Array.isArray(configActions)) return configActions[actionName] || null;
		return null;
	}
	function createVgplotController({ view = null, runtime = null } = {}) {
		const capturedRuntime = captureVgplotRuntime({
			view,
			runtime
		});
		return {
			describePlots() {
				return [...capturedRuntime.plots.entries()].map(([plotId, entry]) => ({
					plotId,
					widgetKind: entry?.widgetKind || null
				}));
			},
			readPlotState(plotId) {
				const plot = readPlotEntry(capturedRuntime, plotId)?.plot || null;
				if (!plot) return null;
				return {
					plotId,
					xDomain: typeof plot.getAttribute === "function" ? plot.getAttribute("xDomain") ?? null : null,
					yDomain: typeof plot.getAttribute === "function" ? plot.getAttribute("yDomain") ?? null : null
				};
			},
			readParamState(name) {
				if (typeof name !== "string" || name.length === 0) return null;
				const param = capturedRuntime.params.get(name);
				if (!param) return null;
				return {
					name,
					type: typeof param?.type === "string" ? param.type : null,
					value: readRuntimeValue(param)
				};
			},
			setPlotAttribute(plotId, name, value, options = {}) {
				const plot = readPlotObject(capturedRuntime, plotId);
				if (!plot || typeof name !== "string" || name.length === 0) return false;
				if (typeof plot.setAttribute === "function") {
					plot.setAttribute(name, value, options);
					return true;
				}
				if (plot.attributes instanceof Map) {
					plot.attributes.set(name, value);
					return true;
				}
				return false;
			},
			updateSelection(name, nextValue) {
				if (typeof name !== "string" || name.length === 0) return false;
				const selection = capturedRuntime.selections.get(name);
				if (selection && typeof selection.set === "function") {
					selection.set(nextValue);
					return true;
				}
				if (selection && typeof selection.update === "function") {
					selection.update(nextValue);
					return true;
				}
				if (selection && typeof selection === "object") {
					if (typeof nextValue?.type === "string" && Object.prototype.hasOwnProperty.call(nextValue, "value")) {
						selection.type = nextValue.type;
						selection.value = nextValue.value;
						return true;
					}
					selection.value = nextValue;
					return true;
				}
				capturedRuntime.selections.set(name, { value: nextValue });
				return true;
			},
			updateParam(name, nextValue) {
				if (typeof name !== "string" || name.length === 0) return false;
				const param = capturedRuntime.params.get(name);
				if (param && typeof param.set === "function") {
					param.set(nextValue);
					return true;
				}
				if (param && typeof param.update === "function") {
					param.update(nextValue);
					return true;
				}
				if (param && typeof param === "object") {
					if (typeof nextValue?.type === "string" && Object.prototype.hasOwnProperty.call(nextValue, "value")) {
						param.type = nextValue.type;
						param.value = nextValue.value;
						return true;
					}
					param.value = nextValue;
					return true;
				}
				capturedRuntime.params.set(name, { value: nextValue });
				return true;
			},
			subscribePlotAttribute(plotId, name, listener) {
				const plot = readPlotObject(capturedRuntime, plotId);
				if (!plot || typeof listener !== "function") return () => {};
				if (typeof plot.addAttributeListener === "function") {
					const disposable = plot.addAttributeListener(name, listener);
					return typeof disposable === "function" ? disposable : () => {};
				}
				if (plot.attributes instanceof Map) safeCall(listener, plot.attributes.get(name));
				return () => {};
			},
			executePlotConfigAction(plotId, actionName, params = {}) {
				const plotEntry = readPlotEntry(capturedRuntime, plotId);
				const plot = plotEntry?.plot || null;
				const handler = readConfigActionHandler(plotEntry, actionName);
				if (!plot || !handler) return false;
				if (typeof handler === "function") return handler({
					actionName,
					params,
					plot,
					plotId,
					controller: this,
					runtime: capturedRuntime
				}) !== false;
				if (typeof handler?.run === "function") return handler.run({
					actionName,
					params,
					plot,
					plotId,
					controller: this,
					runtime: capturedRuntime
				}) !== false;
				if (typeof handler?.execute === "function") return handler.execute({
					actionName,
					params,
					plot,
					plotId,
					controller: this,
					runtime: capturedRuntime
				}) !== false;
				return false;
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/vgplot/vgplotOrchestration.js
	function clone$13(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function attachVgplotRuntimeCapture({ view = null, runtime = null, runtimeCapture = null } = {}) {
		const normalizedRuntime = runtimeCapture ? normalizeVgplotRuntime(runtimeCapture) : captureVgplotRuntime({
			view,
			runtime
		});
		if (view && typeof view === "object") view.__widgetvaVgplotRuntime = normalizedRuntime;
		if (runtime && typeof runtime === "object") runtime.__widgetvaVgplotRuntime = normalizedRuntime;
		return normalizedRuntime;
	}
	function orchestrateVgplotView({ view = null, runtime = null, runtimeCapture = null, binding = null, widgetKind = null } = {}) {
		const normalizedRuntime = attachVgplotRuntimeCapture({
			view,
			runtime,
			runtimeCapture: runtimeCapture || view?.__widgetvaVgplotRuntime || runtime?.__widgetvaVgplotRuntime || null
		});
		const normalizedBinding = normalizeVgplotBinding(binding);
		if (normalizedBinding) attachVgplotBinding({
			view,
			runtime,
			binding: normalizedBinding
		});
		const runtimeHandle = { __widgetvaVgplotRuntime: normalizedRuntime };
		return {
			runtimeCapture: clone$13({
				provider: normalizedRuntime.provider,
				plotIds: [...normalizedRuntime.plots.keys()],
				selectionNames: [...normalizedRuntime.selections.keys()],
				paramNames: [...normalizedRuntime.params.keys()]
			}),
			binding: normalizedBinding ? clone$13(normalizedBinding) : null,
			controller: createVgplotController({
				view,
				runtime: runtimeHandle
			}),
			capabilities: resolveVgplotCapabilities({
				widgetKind,
				view,
				runtime: runtimeHandle
			})
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/adapters/installWidgetView.js
	async function installWidgetVAOnView({ runtime = null, view = null, surface = null, container = null, widgetRef = null, widgetState = null, spec = null, interactionConfig = null, selectionSourceWidgetId = null, actionTargetRef = null, onActionCall = null, onSelectionChange = null, bindHumanInteractions = true, widgetAdapter = null, provider = null, runtimeCapture = null, binding = null } = {}) {
		const effectiveSurface = surface || container || null;
		if (!view && !effectiveSurface) throw new Error("installWidgetVAOnView requires either a view, surface, or container.");
		const resolvedAdapter = widgetAdapter || null;
		if (!resolvedAdapter) throw new Error("installWidgetVAOnView requires an explicit widgetAdapter.");
		if ((resolvedAdapter?.provider || provider || null) === "vgplot") orchestrateVgplotView({
			view,
			runtime,
			runtimeCapture,
			binding,
			widgetKind: resolvedAdapter?.kind || widgetState?.kind || null
		});
		const resolvedInteractionConfig = interactionConfig || widgetState?.humanInteraction || null;
		const bridge = attachWidgetRendererBridge({
			runtime,
			widgetRef,
			widgetAdapter: resolvedAdapter,
			widgetState,
			view,
			surface: effectiveSurface,
			spec,
			interactionConfig: resolvedInteractionConfig,
			selectionSourceWidgetId,
			actionTargetRef: actionTargetRef || widgetRef,
			onActionCall,
			onSelectionChange,
			bindHumanInteractions
		});
		async function apply({ widgetState: nextWidgetState = widgetState, spec: nextSpec = spec, interactionConfig: nextInteractionConfig = resolvedInteractionConfig } = {}) {
			await applyWidgetRuntimeState({
				runtime,
				widgetRef,
				widgetAdapter: resolvedAdapter,
				widgetState: nextWidgetState,
				view,
				surface: effectiveSurface,
				spec: nextSpec,
				interactionConfig: nextInteractionConfig
			});
		}
		await apply();
		return {
			adapter: resolvedAdapter,
			apply,
			dispose: bridge.cleanup
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/rendering/widgetRuntimeSurface.js
	function makeCallId$1(prefix = "widget_call") {
		return `${prefix}_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
	}
	function resolveWidgetDescription(runtime, { widgetRef = null, widgetId = null } = {}) {
		const descriptions = runtime?.store?.listWidgetDescriptions?.() || [];
		if (widgetRef) return descriptions.find((entry) => entry?.ref === widgetRef) || null;
		if (widgetId) return descriptions.find((entry) => entry?.widgetId === widgetId) || null;
		return descriptions[0] || null;
	}
	function normalizeActionCall(call, widgetRef) {
		if (!call || typeof call !== "object") throw new Error("WidgetInstance.executeAction requires an action call object.");
		const normalizedQueryScope = call.queryScope && typeof call.queryScope === "object" && !Array.isArray(call.queryScope) ? { ...call.queryScope } : {};
		delete normalizedQueryScope.widgetRef;
		delete normalizedQueryScope.widget_ref;
		const normalizedTarget = call.target && typeof call.target === "object" && !Array.isArray(call.target) ? { ...call.target } : {};
		if (widgetRef && !normalizedTarget.widgetRef) normalizedTarget.widgetRef = widgetRef;
		const { targetRef: _legacyTargetRef, ...restCall } = call;
		return {
			actor: call.actor || "agent",
			callId: call.callId || makeCallId$1("widget_action"),
			...restCall,
			...Object.keys(normalizedTarget).length > 0 ? { target: normalizedTarget } : {},
			...Object.keys(normalizedQueryScope).length > 0 ? { queryScope: normalizedQueryScope } : {}
		};
	}
	function normalizePerceptionCall(call, widgetRef) {
		if (!call || typeof call !== "object") throw new Error("WidgetInstance.queryPerception requires a perception call object.");
		const normalizedQueryScope = call.queryScope && typeof call.queryScope === "object" && !Array.isArray(call.queryScope) ? { ...call.queryScope } : {};
		delete normalizedQueryScope.widgetRef;
		delete normalizedQueryScope.widget_ref;
		const normalizedTarget = call.target && typeof call.target === "object" && !Array.isArray(call.target) ? { ...call.target } : {};
		if (widgetRef && !normalizedTarget.widgetRef) normalizedTarget.widgetRef = widgetRef;
		const { targetRef: _legacyTargetRef, ...restCall } = call;
		return {
			actor: call.actor || "agent",
			callId: call.callId || makeCallId$1("widget_perception"),
			...restCall,
			...Object.keys(normalizedTarget).length > 0 ? { target: normalizedTarget } : {},
			...Object.keys(normalizedQueryScope).length > 0 ? { queryScope: normalizedQueryScope } : {}
		};
	}
	function normalizeDataQueryCall(call, widgetRef) {
		if (!call || typeof call !== "object") throw new Error("WidgetInstance.runDataQuery requires a data-query call object.");
		const normalizedQueryScope = call.queryScope && typeof call.queryScope === "object" && !Array.isArray(call.queryScope) ? { ...call.queryScope } : {};
		delete normalizedQueryScope.widgetRef;
		delete normalizedQueryScope.widget_ref;
		const normalizedTarget = call.target && typeof call.target === "object" && !Array.isArray(call.target) ? { ...call.target } : {};
		if (widgetRef && !normalizedTarget.widgetRef) normalizedTarget.widgetRef = widgetRef;
		const { targetRef: _legacyTargetRef, ...restCall } = call;
		return {
			actor: call.actor || "agent",
			callId: call.callId || makeCallId$1("widget_data_query"),
			...restCall,
			...Object.keys(normalizedTarget).length > 0 ? { target: normalizedTarget } : {},
			...Object.keys(normalizedQueryScope).length > 0 ? { queryScope: normalizedQueryScope } : {}
		};
	}
	function normalizeMountTarget({ mountTarget = null, renderTarget = null, view = null, surface = null, container = null } = {}) {
		const target = mountTarget || renderTarget || null;
		return {
			...target?.view || view ? { view: target?.view || view } : {},
			...target?.surface || surface || target?.container || container ? { surface: target?.surface || surface || target?.container || container } : {},
			...target?.container || container ? { container: target?.container || container } : {}
		};
	}
	function normalizeWidgetInstanceOptions({ mountOptions = {}, mountTarget = null, renderTarget = null, view = null, surface = null, container = null, ...options } = {}) {
		return {
			...options,
			mountOptions: {
				...mountOptions,
				...normalizeMountTarget({
					mountTarget,
					renderTarget,
					view,
					surface,
					container
				})
			}
		};
	}
	var WidgetInstance = class {
		constructor({ runtime = null, ownsRuntime = false, widgetAdapter = null, widgetRef = null, widgetId = null, spec = null, widgetState = null, mountOptions = {} } = {}) {
			if (!runtime) throw new Error("WidgetInstance requires a runtime.");
			if (!widgetAdapter) throw new Error("WidgetInstance requires a widgetAdapter.");
			this.runtime = runtime;
			this.widgetAdapter = widgetAdapter;
			this.widgetRef = widgetRef;
			this.widgetId = widgetId;
			this.spec = spec;
			this.widgetState = widgetState;
			this.mountOptions = { ...mountOptions };
			this.ownsRuntime = ownsRuntime;
			this.installation = null;
		}
		resolveWidgetDescription() {
			return resolveWidgetDescription(this.runtime, {
				widgetRef: this.widgetRef,
				widgetId: this.widgetId
			});
		}
		resolveWidgetRef() {
			return this.widgetRef || this.resolveWidgetDescription()?.ref || null;
		}
		resolveWidgetId() {
			return this.widgetId || this.resolveWidgetDescription()?.widgetId || null;
		}
		isMounted() {
			return Boolean(this.installation);
		}
		async mount(nextMountOptions = {}) {
			if (this.installation?.dispose) {
				this.installation.dispose();
				this.installation = null;
			}
			this.mountOptions = {
				...this.mountOptions,
				...normalizeMountTarget(nextMountOptions),
				...nextMountOptions
			};
			const widgetState = this.widgetState || this.readState();
			const widgetSpec = this.mountOptions.spec || this.spec;
			const interactionConfig = this.mountOptions.interactionConfig || widgetState?.humanInteraction || getWidgetFamilyHumanInteractionConfig(widgetState?.kind || this.resolveWidgetDescription()?.kind || this.widgetAdapter?.kind || null, {
				widgetRef: this.resolveWidgetRef(),
				widgetDef: this.resolveWidgetDescription(),
				widgetSpec
			}) || null;
			this.installation = await installWidgetVAOnView({
				runtime: this.runtime,
				widgetAdapter: this.widgetAdapter,
				widgetRef: this.resolveWidgetRef(),
				widgetState,
				spec: widgetSpec,
				interactionConfig,
				...this.mountOptions
			});
			return this;
		}
		unmount() {
			this.installation?.dispose?.();
			this.installation = null;
		}
		describe() {
			return this.resolveWidgetDescription();
		}
		readState() {
			const widgetRef = this.resolveWidgetRef();
			return widgetRef ? this.runtime?.store?.getWidgetState?.(widgetRef) || null : null;
		}
		readWorkspaceState(options = {}) {
			return this.runtime?.store?.readState?.(options) || null;
		}
		async executeAction(call) {
			const executeAction = this.runtime?.executeAction;
			if (typeof executeAction !== "function") throw new Error("WidgetInstance.executeAction requires runtime.executeAction support.");
			return executeAction(normalizeActionCall(call, this.resolveWidgetRef()));
		}
		async executeVerifiedAction(call, options = {}) {
			const normalizedCall = normalizeActionCall(call, this.resolveWidgetRef());
			const executeVerifiedAction = this.runtime?.executeVerifiedAction;
			if (typeof executeVerifiedAction !== "function") throw new Error("WidgetInstance.executeVerifiedAction requires runtime.executeVerifiedAction support.");
			return executeVerifiedAction(normalizedCall, options);
		}
		async queryPerception(call) {
			const queryPerception = this.runtime?.queryPerception;
			if (typeof queryPerception !== "function") throw new Error("WidgetInstance.queryPerception requires runtime.queryPerception support.");
			return queryPerception(normalizePerceptionCall(call, this.resolveWidgetRef()));
		}
		async runDataQuery(call) {
			const runDataQuery = this.runtime?.runDataQuery || this.runtime?.queryData;
			if (typeof runDataQuery !== "function") throw new Error("WidgetInstance.runDataQuery requires runtime.runDataQuery or runtime.queryData support.");
			return runDataQuery(normalizeDataQueryCall(call, this.resolveWidgetRef()));
		}
		readTrace(options = {}) {
			return this.getTrace(options);
		}
		getTrace(options = {}) {
			return readInteractionTraceFromStore(this.runtime?.store, options);
		}
		readSnapshot(stateId, options = {}) {
			return readSnapshotFromStore(this.runtime?.store, stateId, options);
		}
		readSnapshotEntry(stateId) {
			return readSnapshotEntryFromStore(this.runtime?.store, stateId);
		}
		async replay(stateId) {
			return this.runtime.executeAction({
				callId: makeCallId$1("widget_replay"),
				actor: "agent",
				name: "workspace.jumpToState",
				params: { stateId }
			});
		}
		dispose() {
			this.unmount();
			if (this.ownsRuntime) this.runtime?.dispose?.();
		}
	};
	function createWidgetInstance({ runtime = null, widgetAdapter = null, widgetRef = null, widgetId = null, spec = null, widgetState = null, mountTarget = null, renderTarget = null, view = null, surface = null, container = null, mountOptions = {}, runtimeOptions = {} } = {}) {
		const normalizedOptions = normalizeWidgetInstanceOptions({
			runtime,
			widgetAdapter,
			widgetRef,
			widgetId,
			spec,
			widgetState,
			mountTarget,
			renderTarget,
			view,
			surface,
			container,
			mountOptions,
			runtimeOptions
		});
		return new WidgetInstance({
			runtime: normalizedOptions.runtime || createWidgetVARuntime(normalizedOptions.runtimeOptions),
			ownsRuntime: !runtime,
			widgetAdapter: normalizedOptions.widgetAdapter,
			widgetRef: normalizedOptions.widgetRef,
			widgetId: normalizedOptions.widgetId,
			spec: normalizedOptions.spec,
			widgetState: normalizedOptions.widgetState,
			mountOptions: normalizedOptions.mountOptions
		});
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/linkStateModel.js
	function clone$12(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function cloneArray(value) {
		return Array.isArray(value) ? clone$12(value) : [];
	}
	function cloneObject$1(value) {
		return value && typeof value === "object" && !Array.isArray(value) ? clone$12(value) : {};
	}
	function readLinkDefinitions(shared = {}) {
		return cloneArray(shared?.links?.definitions);
	}
	function readLinkTopologyState(shared = {}) {
		return cloneObject$1(shared?.links?.topology);
	}
	function withLinkSubmodel(shared = {}, { definitions, topology } = {}) {
		const previousLinks = shared?.links || {};
		return {
			...shared || {},
			links: {
				...previousLinks,
				definitions: definitions === void 0 ? readLinkDefinitions(shared) : cloneArray(definitions),
				topology: topology === void 0 ? readLinkTopologyState(shared) : cloneObject$1(topology)
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/coordination/workspaceLinkOperations.js
	function resolveWorkspaceIdentity(runtime) {
		const description = runtime?.store?.readDescription?.() || {};
		return {
			appId: description?.appId || parseRef(description?.widgets?.[0]?.ref)?.appId || "widgetva-app",
			workspaceId: description?.workspaceId || parseRef(description?.widgets?.[0]?.ref)?.workspaceId || "main"
		};
	}
	function resolveLinkEndpointWidgetId(value, widgets = []) {
		if (typeof value !== "string" || value.length === 0) return null;
		const parsedWidgetId = parseRef(value)?.widgetId;
		if (parsedWidgetId) return parsedWidgetId;
		const byWidgetRef = widgets.find((widget) => widget?.resolveWidgetRef?.() === value)?.resolveWidgetId?.();
		if (byWidgetRef) return byWidgetRef;
		const byWidgetId = widgets.find((widget) => widget?.resolveWidgetId?.() === value)?.resolveWidgetId?.();
		if (byWidgetId) return byWidgetId;
		return value;
	}
	function inferLinkId(link = {}, widgets = []) {
		const explicitId = parseRef(link?.ref)?.linkId || link?.linkId || null;
		if (explicitId) return explicitId;
		const sourceRef = link?.from || link?.sourceRef || null;
		const targetRef = link?.to || link?.targetRef || null;
		const sourceWidgetId = link?.sourceWidgetId || resolveLinkEndpointWidgetId(sourceRef, widgets) || "source";
		const targetWidgetId = link?.targetWidgetId || resolveLinkEndpointWidgetId(targetRef, widgets) || "target";
		return `${sourceWidgetId}_${link?.kind || "link"}_${targetWidgetId}`;
	}
	function normalizeWorkspaceLink(link, widgets = [], runtime = null) {
		const { appId, workspaceId } = resolveWorkspaceIdentity(runtime);
		const linkId = inferLinkId(link, widgets);
		const sourceWidgetId = link?.sourceWidgetId || resolveLinkEndpointWidgetId(link?.from || link?.sourceRef || null, widgets) || null;
		const targetWidgetId = link?.targetWidgetId || resolveLinkEndpointWidgetId(link?.to || link?.targetRef || null, widgets) || null;
		return makeWidgetLink({
			...link,
			...sourceWidgetId ? { sourceWidgetId } : {},
			...targetWidgetId ? { targetWidgetId } : {},
			ref: link?.ref || makeLinkRef({
				appId,
				workspaceId,
				linkId
			})
		});
	}
	function listWorkspaceLinks(workspace) {
		const registeredLinks = workspace.runtime?.store?.listLinks?.() || [];
		if (registeredLinks.length > 0) return registeredLinks.map((link) => stripWidgetLinkCompatibilityFields(link));
		return workspace.links.map((link) => stripWidgetLinkCompatibilityFields(link));
	}
	function getWorkspaceLink(workspace, refOrId) {
		return listWorkspaceLinks(workspace).find((link) => link?.ref === refOrId || parseRef(link?.ref)?.linkId === refOrId || link?.linkId === refOrId) || null;
	}
	function readWorkspaceLinkTopology(workspace) {
		return deriveWorkspaceTopology({
			widgets: workspace.describe()?.widgets || workspace.listWidgets().map((widget) => widget?.describe?.()).filter(Boolean),
			links: listWorkspaceLinks(workspace)
		});
	}
	function writeSharedLinkState(workspace) {
		workspace.updateSharedCoordinationState((shared) => withLinkSubmodel(shared, {
			definitions: workspace.links,
			topology: readWorkspaceLinkTopology(workspace)
		}));
	}
	function registerWorkspaceLink(workspace, link) {
		if (!link || typeof link !== "object") throw new Error("WidgetWorkspace.registerLink requires a link object.");
		const normalizedLink = normalizeWorkspaceLink(link, workspace.widgets, workspace.runtime);
		if (typeof workspace.runtime?.store?.registerLink === "function") workspace.runtime.store.registerLink(normalizedLink);
		const existingIndex = workspace.links.findIndex((entry) => entry?.ref === normalizedLink.ref);
		if (existingIndex >= 0) workspace.links.splice(existingIndex, 1, normalizedLink);
		else workspace.links.push(normalizedLink);
		writeSharedLinkState(workspace);
		return normalizedLink;
	}
	function removeWorkspaceLink(workspace, refOrId) {
		const existing = getWorkspaceLink(workspace, refOrId);
		if (!existing) return null;
		if (typeof workspace.runtime?.store?.removeLinkDefinition === "function") workspace.runtime.store.removeLinkDefinition(existing.ref);
		else if (workspace.runtime?.store?.links && existing.ref in workspace.runtime.store.links) delete workspace.runtime.store.links[existing.ref];
		workspace.links = workspace.links.filter((entry) => entry?.ref !== existing.ref);
		writeSharedLinkState(workspace);
		return existing;
	}
	function describeWorkspaceComposition(workspace) {
		return {
			widgetRefs: workspace.listWidgets().map((widget) => widget?.resolveWidgetRef?.()).filter(Boolean),
			widgetIds: workspace.listWidgets().map((widget) => widget?.resolveWidgetId?.()).filter(Boolean),
			links: listWorkspaceLinks(workspace),
			topology: readWorkspaceLinkTopology(workspace)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/sharedAnalyticalStateModel.js
	function clone$11(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function isPlainObject(value) {
		return Boolean(value) && typeof value === "object" && !Array.isArray(value);
	}
	function normalizeDomain(value) {
		return Array.isArray(value) ? clone$11(value) : null;
	}
	function normalizeOptionalObject(value) {
		return isPlainObject(value) ? clone$11(value) : null;
	}
	function normalizeFocusKeys(value) {
		if (!isPlainObject(value)) return null;
		const next = Object.fromEntries(Object.entries(value).filter(([, entry]) => entry != null));
		return Object.keys(next).length > 0 ? next : null;
	}
	function readOperationMode(value, fallback = null) {
		if (!isPlainObject(value)) return fallback;
		if (typeof value.mode === "string" && value.mode.trim().length > 0) return value.mode.trim();
		if (typeof value.sourceAction === "string" && value.sourceAction.trim().length > 0) return value.sourceAction.trim();
		return fallback;
	}
	function buildOperationModesByKind({ sort = null, highlight = null, drillDown = null, aggregate = null, reencode = null, addRemove = null, navigate = null, focusKeys = null, xDomain = null, yDomain = null, zoom = null } = {}) {
		const next = {};
		const maybeSet = (kind, mode) => {
			if (typeof mode !== "string" || mode.length === 0) return;
			next[kind] = mode;
		};
		if (xDomain || yDomain || zoom) maybeSet("zoom", readOperationMode(zoom, "zoom"));
		if (sort) maybeSet("sort", readOperationMode(sort, "sort"));
		if (highlight) maybeSet("highlight", readOperationMode(highlight, "highlight"));
		if (drillDown) maybeSet("drillDown", readOperationMode(drillDown, "drillDown"));
		if (aggregate) maybeSet("aggregate", readOperationMode(aggregate, "aggregate"));
		if (reencode) maybeSet("reencode", readOperationMode(reencode, "reencode"));
		if (addRemove) maybeSet("addRemove", readOperationMode(addRemove, "addRemove"));
		if (navigate) maybeSet("navigate", readOperationMode(navigate, "navigate"));
		if (focusKeys) maybeSet("focus", "focus");
		return Object.keys(next).length > 0 ? next : null;
	}
	function isNonEmptyObject(value) {
		return isPlainObject(value) && Object.keys(value).length > 0;
	}
	function normalizePrimarySelection(value) {
		if (!isPlainObject(value)) return null;
		const predicates = Array.isArray(value.predicates) ? clone$11(value.predicates) : [];
		if (!([
			value.selectionRef,
			value.sourceWidgetRef,
			value.sourceWidgetId,
			value.summary,
			value.kind,
			value.scope
		].some((entry) => typeof entry === "string" && entry.length > 0) || predicates.length > 0)) return null;
		return {
			selectionRef: value.selectionRef || null,
			sourceWidgetRef: value.sourceWidgetRef || null,
			sourceWidgetId: value.sourceWidgetId || null,
			summary: value.summary || null,
			predicates,
			kind: value.kind || null,
			scope: value.scope || null
		};
	}
	function normalizeHighlightSummary(value) {
		const activeWidgetRefs = Array.isArray(value?.activeWidgetRefs) ? clone$11(value.activeWidgetRefs.filter((entry) => typeof entry === "string" && entry.length > 0)) : [];
		const summaries = Array.from(new Set((Array.isArray(value?.entries) ? value.entries : []).map((entry) => entry?.summary).filter((entry) => typeof entry === "string" && entry.length > 0)));
		if (activeWidgetRefs.length === 0 && summaries.length === 0) return null;
		return {
			activeWidgetRefs,
			summaries
		};
	}
	function normalizeViewportSummary(value) {
		if (!isPlainObject(value)) return null;
		const next = {
			sourceWidgetRef: value.sourceWidgetRef || null,
			xDomain: normalizeDomain(value.xDomain),
			yDomain: normalizeDomain(value.yDomain),
			zoom: normalizeOptionalObject(value.zoom)
		};
		return next.sourceWidgetRef || next.xDomain || next.yDomain || next.zoom ? next : null;
	}
	function buildActiveAnalyticalContext({ focusedWidgetRef = null, filters = {}, primarySelection = null, highlight = null, viewport = null, comparisonTargets = [], links = null, viewStatesByWidget = {} } = {}) {
		const globalFilters = isNonEmptyObject(filters) ? clone$11(filters) : null;
		const normalizedSelection = normalizePrimarySelection(primarySelection);
		const normalizedHighlight = normalizeHighlightSummary(highlight);
		const normalizedViewport = normalizeViewportSummary(viewport);
		const normalizedViewStates = isNonEmptyObject(viewStatesByWidget) ? clone$11(viewStatesByWidget) : null;
		const transformationContext = buildSharedTransformationContext({ viewStatesByWidget });
		const normalizedComparisonTargets = Array.isArray(comparisonTargets) && comparisonTargets.length > 0 ? clone$11(comparisonTargets) : null;
		const linkCount = Array.isArray(links?.definitions) ? links.definitions.length : 0;
		const activeContextKinds = [];
		if (focusedWidgetRef) activeContextKinds.push("focus");
		if (globalFilters) activeContextKinds.push("filters");
		if (normalizedSelection) activeContextKinds.push("selection");
		if (normalizedHighlight) activeContextKinds.push("highlight");
		if (normalizedViewport) activeContextKinds.push("viewport");
		if (normalizedViewStates) activeContextKinds.push("view");
		if (normalizedComparisonTargets || linkCount > 0) activeContextKinds.push("structure");
		return {
			activeContextKinds,
			focusedWidgetRef: focusedWidgetRef || null,
			globalFilters,
			primarySelection: normalizedSelection,
			highlight: normalizedHighlight,
			viewport: normalizedViewport,
			comparisonTargets: normalizedComparisonTargets,
			structure: { linkCount },
			transformationContext,
			viewStatesByWidget: normalizedViewStates
		};
	}
	function buildWidgetViewStateEntry(widgetRef, widgetState = {}) {
		const view = isPlainObject(widgetState?.view) ? widgetState.view : null;
		if (!view) return null;
		const entry = {
			widgetRef,
			widgetId: typeof widgetState?.widgetId === "string" && widgetState.widgetId.length > 0 ? widgetState.widgetId : null
		};
		const activeKinds = [];
		const xDomain = normalizeDomain(view.xDomain);
		const yDomain = normalizeDomain(view.yDomain);
		const zoom = normalizeOptionalObject(view.zoom);
		const sort = normalizeOptionalObject(view.sort);
		const highlight = normalizeOptionalObject(view.highlight);
		const drillDown = normalizeOptionalObject(view.drillDown);
		const aggregate = normalizeOptionalObject(view.aggregate);
		const reencode = normalizeOptionalObject(view.reencode);
		const addRemove = normalizeOptionalObject(view.addRemove);
		const navigate = normalizeOptionalObject(view.navigate);
		const focusKeys = normalizeFocusKeys(view.focusKeys);
		if (xDomain || yDomain || zoom) {
			activeKinds.push("zoom");
			if (xDomain) entry.xDomain = xDomain;
			if (yDomain) entry.yDomain = yDomain;
			if (zoom) entry.zoom = zoom;
		}
		if (sort) {
			activeKinds.push("sort");
			entry.sort = sort;
		}
		if (highlight) {
			activeKinds.push("highlight");
			entry.highlight = highlight;
		}
		if (drillDown) {
			activeKinds.push("drillDown");
			entry.drillDown = drillDown;
		}
		if (aggregate) {
			activeKinds.push("aggregate");
			entry.aggregate = aggregate;
		}
		if (reencode) {
			activeKinds.push("reencode");
			entry.reencode = reencode;
		}
		if (addRemove) {
			activeKinds.push("addRemove");
			entry.addRemove = addRemove;
		}
		if (navigate) {
			activeKinds.push("navigate");
			entry.navigate = navigate;
		}
		if (focusKeys) {
			activeKinds.push("focus");
			entry.focusKeys = focusKeys;
		}
		if (activeKinds.length === 0) return null;
		entry.activeKinds = activeKinds;
		const operationModesByKind = buildOperationModesByKind({
			sort,
			highlight,
			drillDown,
			aggregate,
			reencode,
			addRemove,
			navigate,
			focusKeys,
			xDomain,
			yDomain,
			zoom
		});
		if (operationModesByKind) entry.operationModesByKind = operationModesByKind;
		return entry;
	}
	function buildSharedViewStateByWidget(state = {}) {
		const next = {};
		for (const [widgetRef, widgetState] of Object.entries(state?.widgets || {})) {
			if (typeof widgetRef !== "string" || widgetRef.length === 0) continue;
			const entry = buildWidgetViewStateEntry(widgetRef, widgetState);
			if (!entry) continue;
			next[widgetRef] = entry;
		}
		return next;
	}
	function buildSharedViewContext({ viewStatesByWidget = {} } = {}) {
		const normalized = isPlainObject(viewStatesByWidget) ? clone$11(viewStatesByWidget) : {};
		return {
			activeWidgetRefs: Object.keys(normalized),
			widgets: normalized
		};
	}
	function buildSharedTransformationContext({ viewStatesByWidget = {} } = {}) {
		const normalized = isPlainObject(viewStatesByWidget) ? viewStatesByWidget : {};
		const transformationKinds = new Set([
			"sort",
			"drillDown",
			"aggregate",
			"reencode",
			"addRemove",
			"navigate"
		]);
		const next = {};
		for (const [widgetRef, entry] of Object.entries(normalized)) {
			if (!isPlainObject(entry)) continue;
			const activeKinds = (Array.isArray(entry.activeKinds) ? entry.activeKinds : []).filter((kind) => transformationKinds.has(kind));
			if (activeKinds.length === 0) continue;
			const nextEntry = {
				widgetRef,
				widgetId: typeof entry.widgetId === "string" && entry.widgetId.length > 0 ? entry.widgetId : null,
				activeKinds
			};
			if (entry.sort) nextEntry.sort = clone$11(entry.sort);
			if (entry.drillDown) nextEntry.drillDown = clone$11(entry.drillDown);
			if (entry.aggregate) nextEntry.aggregate = clone$11(entry.aggregate);
			if (entry.reencode) nextEntry.reencode = clone$11(entry.reencode);
			if (entry.addRemove) nextEntry.addRemove = clone$11(entry.addRemove);
			if (entry.navigate) nextEntry.navigate = clone$11(entry.navigate);
			if (isPlainObject(entry.operationModesByKind)) nextEntry.operationModesByKind = Object.fromEntries(activeKinds.filter((kind) => typeof entry.operationModesByKind[kind] === "string" && entry.operationModesByKind[kind].length > 0).map((kind) => [kind, entry.operationModesByKind[kind]]));
			next[widgetRef] = nextEntry;
		}
		return {
			activeWidgetRefs: Object.keys(next),
			widgets: next
		};
	}
	function buildSharedFilterContext({ filters = {}, primarySelection = null } = {}) {
		return {
			globalFilters: clone$11(filters || {}),
			selectionRef: primarySelection?.selectionRef || null,
			selectionPredicates: Array.isArray(primarySelection?.predicates) ? clone$11(primarySelection.predicates) : []
		};
	}
	function buildSharedViewportContext({ focusedWidgetRef = null, viewport = null, comparisonTargets = [] } = {}) {
		return {
			focusedWidgetRef: focusedWidgetRef || null,
			viewport: clone$11(viewport || null),
			comparisonTargets: clone$11(Array.isArray(comparisonTargets) ? comparisonTargets : [])
		};
	}
	function buildSharedSemanticFocus({ focusedWidgetRef = null, focus = null, primarySelection = null, highlight = null } = {}) {
		return {
			focusedWidgetRef: focusedWidgetRef || null,
			focus: clone$11(focus || null),
			primarySelection: clone$11(primarySelection || null),
			highlight: clone$11(highlight || {
				entries: [],
				activeWidgetRefs: []
			})
		};
	}
	function buildSharedStructuralContext({ links = null, comparisonTargets = [] } = {}) {
		return {
			links: clone$11(links || {
				definitions: [],
				topology: null
			}),
			comparisonTargets: clone$11(Array.isArray(comparisonTargets) ? comparisonTargets : [])
		};
	}
	function buildSharedAnalyticalStateModel(state = {}, { derivedTopology = {} } = {}) {
		const shared = state?.shared || {};
		const focusedWidgetRef = shared?.focusedWidget || null;
		const selections = {
			registry: readSelectionRegistry(shared),
			primary: readSelectionPrimaryView(shared),
			byWidget: readSelectionByWidgetView(shared)
		};
		const filters = clone$11(shared?.globalFilters || {});
		const focus = readFocusState(shared, state?.widgets || {});
		const highlight = deriveHighlightState(state);
		const viewport = readViewportState(shared);
		const comparisonTargets = clone$11(Array.isArray(shared?.comparisonTargets) ? shared.comparisonTargets : []);
		const viewStatesByWidget = buildSharedViewStateByWidget(state);
		const sharedTopology = readLinkTopologyState(shared);
		const links = {
			definitions: readLinkDefinitions(shared),
			topology: Object.keys(sharedTopology).length > 0 ? sharedTopology : clone$11(derivedTopology || {})
		};
		return {
			focusedWidgetRef,
			selections,
			filters,
			focus,
			highlight,
			viewStatesByWidget,
			viewport,
			comparisonTargets,
			links,
			sharedFilterContext: buildSharedFilterContext({
				filters,
				primarySelection: selections.primary
			}),
			sharedViewportContext: buildSharedViewportContext({
				focusedWidgetRef,
				viewport,
				comparisonTargets
			}),
			sharedViewContext: buildSharedViewContext({ viewStatesByWidget }),
			sharedTransformationContext: buildSharedTransformationContext({ viewStatesByWidget }),
			sharedSemanticFocus: buildSharedSemanticFocus({
				focusedWidgetRef,
				focus,
				primarySelection: selections.primary,
				highlight
			}),
			sharedStructuralContext: buildSharedStructuralContext({
				links,
				comparisonTargets
			}),
			activeAnalyticalContext: buildActiveAnalyticalContext({
				focusedWidgetRef,
				filters,
				primarySelection: selections.primary,
				highlight,
				viewport,
				comparisonTargets,
				links,
				viewStatesByWidget
			})
		};
	}
	function readSharedAnalyticalState(state = {}, options = {}) {
		return buildSharedAnalyticalStateModel(state, options);
	}
	function readSharedFilterContext(state = {}, options = {}) {
		return clone$11(readSharedAnalyticalState(state, options).sharedFilterContext);
	}
	function readSharedViewportContext(state = {}, options = {}) {
		return clone$11(readSharedAnalyticalState(state, options).sharedViewportContext);
	}
	function readSharedSemanticFocus(state = {}, options = {}) {
		return clone$11(readSharedAnalyticalState(state, options).sharedSemanticFocus);
	}
	function readSharedStructuralContext(state = {}, options = {}) {
		return clone$11(readSharedAnalyticalState(state, options).sharedStructuralContext);
	}
	function readActiveAnalyticalContext(state = {}, options = {}) {
		return clone$11(readSharedAnalyticalState(state, options).activeAnalyticalContext);
	}
	function readViewStatesByWidget(state = {}) {
		return buildSharedViewStateByWidget(state);
	}
	function readSharedViewContext(state = {}) {
		return buildSharedViewContext({ viewStatesByWidget: readViewStatesByWidget(state) });
	}
	function readSharedTransformationContext(state = {}) {
		return buildSharedTransformationContext({ viewStatesByWidget: readViewStatesByWidget(state) });
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/state/coordinationStateModel.js
	function clone$10(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readCoordinationRelations(state = {}, shared = {}) {
		const explicitRelations = state?.coordination?.relations;
		if (explicitRelations && typeof explicitRelations === "object" && !Array.isArray(explicitRelations)) return makeCoordinationRelationMap(explicitRelations);
		return makeCoordinationRelationMap(readLinkDefinitions(shared).filter((link) => typeof link?.sourceStateRef === "string" && typeof link?.targetStateRef === "string").map((link) => {
			return {
				ref: link.ref || link.linkId || link.id,
				sourceStateRef: link.sourceStateRef,
				targetStateRef: link.targetStateRef,
				relation: link.relation || "controls",
				transform: clone$10(link.transform || null),
				activation: link.activation || link.activationPolicy || "automatic"
			};
		}));
	}
	function buildCoordinationStateFromWorkspaceState(state = {}, { currentBranchId = null, derivedTopology = {} } = {}) {
		const shared = state?.shared || {};
		const sharedTopology = readLinkTopologyState(shared);
		return {
			stateId: state?.stateId || null,
			branchId: state?.branchId || currentBranchId || null,
			focusedWidgetRef: shared?.focusedWidget || null,
			selections: {
				registry: clone$10(readSelectionRegistry(shared)),
				views: {
					primary: clone$10(readSelectionPrimaryView(shared)),
					byWidget: clone$10(readSelectionByWidgetView(shared))
				}
			},
			focus: readFocusState(shared, state?.widgets || {}),
			highlight: deriveHighlightState(state),
			viewport: readViewportState(shared),
			globalFilters: clone$10(shared?.globalFilters || {}),
			coordination: { relations: readCoordinationRelations(state, shared) },
			links: {
				definitions: readLinkDefinitions(shared),
				topology: Object.keys(sharedTopology).length > 0 ? sharedTopology : derivedTopology
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/widgets/families/familyContractResolvers.js
	function describeFamilyVerificationContract(kind) {
		if (kind === "bar") return describeBarVerificationContract();
		if (kind === "line") return describeLineVerificationContract();
		if (kind === "scatter") return describeScatterVerificationContract();
		if (kind === "heatmap") return describeHeatmapVerificationContract();
		if (kind === "parallelCoordinates") return describeParallelCoordinatesVerificationContract();
		if (kind === "sankey") return describeSankeyVerificationContract();
		return null;
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/coordination/linkPropagationSummary.js
	function clone$9(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function stripCompatibilityFields(value) {
		if (Array.isArray(value)) return value.map((entry) => stripCompatibilityFields(entry));
		if (!value || typeof value !== "object") return value;
		const next = {};
		for (const [key, entryValue] of Object.entries(value)) {
			if (key === "propagationPolicy" || key === "trigger" || key === "automatic") continue;
			next[key] = stripCompatibilityFields(entryValue);
		}
		return next;
	}
	function normalizePublicPropagationEntries(entries = []) {
		return (Array.isArray(entries) ? entries : []).map((entry) => {
			if (!entry || typeof entry !== "object") return entry;
			if (typeof entry.skippedReason !== "string" || entry.skippedReason.length === 0) return entry;
			return {
				...entry,
				skippedReason: normalizeCanonicalPropagationSkipReason(entry.skippedReason, "mappingFailed")
			};
		});
	}
	function normalizePublicPropagationEvaluation(evaluation = null) {
		if (!evaluation || typeof evaluation !== "object") return evaluation;
		const results = Array.isArray(evaluation.results) ? evaluation.results : [];
		return {
			...evaluation,
			results: results.map((entry) => {
				if (!entry || typeof entry !== "object") return entry;
				if (typeof entry.reason !== "string" || entry.reason.length === 0) return entry;
				return {
					...entry,
					reason: normalizeCanonicalPropagationSkipReason(entry.reason, "mappingFailed")
				};
			})
		};
	}
	function uniqueStrings$1(values = []) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => typeof value === "string" && value.length > 0))];
	}
	function buildPropagationSummary({ state = {}, description = {}, coordinationEngine = null, sourceRef = null } = {}) {
		const shared = state?.shared || {};
		const registry = readSelectionRegistry(shared);
		const primarySelection = readSelectionPrimaryView(shared);
		const engineSummary = typeof coordinationEngine?.describeEngine === "function" ? clone$9(coordinationEngine.describeEngine()) : null;
		const candidateSourceRefs = uniqueStrings$1([
			sourceRef,
			primarySelection?.selectionRef || null,
			...Object.keys(registry || {}),
			shared?.focusedWidget || null,
			...(engineSummary?.links || []).map((entry) => entry?.sourceRef || null)
		]);
		const resolvedSourceRef = sourceRef || candidateSourceRefs[0] || null;
		const propagation = resolvedSourceRef && typeof coordinationEngine?.describePropagation === "function" ? normalizePublicPropagationEntries(stripCompatibilityFields(clone$9(coordinationEngine.describePropagation({ sourceRef: resolvedSourceRef })))) : [];
		const evaluation = resolvedSourceRef && typeof coordinationEngine?.evaluatePropagation === "function" ? normalizePublicPropagationEvaluation(stripCompatibilityFields(clone$9(coordinationEngine.evaluatePropagation({
			sourceRef: resolvedSourceRef,
			state
		})))) : null;
		return {
			sourceRef: resolvedSourceRef,
			candidateSourceRefs,
			topology: clone$9(description?.runtimeTopology || engineSummary?.topology || null),
			linkSummary: stripCompatibilityFields(engineSummary),
			propagation,
			evaluation
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/coordinationOperationResult.js
	function clone$8(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function uniqueStrings(values = []) {
		return [...new Set((Array.isArray(values) ? values : []).filter((value) => typeof value === "string" && value.length > 0))];
	}
	function mapEffectToVerificationType(effect, responseSpec = null) {
		if (effect === "applyFilter") return "filter";
		if (effect === "applyHighlight") return "highlight";
		if (effect === "focusTarget") return "focus";
		if (effect === "shareSelection") return "selection";
		if (effect === "syncDomain") return "zoom";
		if (effect === "transformView" || effect === "transformDataView" || effect === "transformStructure") return typeof responseSpec?.kind === "string" && responseSpec.kind.length > 0 ? responseSpec.kind : "transformation";
		return null;
	}
	function buildTargetVerificationGuidance(contract, effect, responseSpec = null) {
		const verificationType = mapEffectToVerificationType(effect, responseSpec);
		const responseHints = responseSpec?.verificationHints && typeof responseSpec.verificationHints === "object" ? responseSpec.verificationHints : {};
		const effectChecks = Array.isArray(responseHints.checks) ? [...responseHints.checks] : verificationType ? Array.isArray(contract?.effectChecks?.[verificationType]) ? [...contract.effectChecks[verificationType]] : [] : [];
		return {
			verificationType,
			preferredReadMethod: responseHints.preferredReadMethod || contract?.preferredReadMethod || null,
			effectChecks,
			responseKind: responseSpec?.kind || null,
			observationFields: Array.isArray(contract?.preferredObservationFields) ? [...contract.preferredObservationFields] : []
		};
	}
	function buildTargetVerificationStep(target = {}) {
		const preferredReadMethod = target?.verificationGuidance?.preferredReadMethod || null;
		const effectChecks = Array.isArray(target?.verificationGuidance?.effectChecks) ? [...target.verificationGuidance.effectChecks] : [];
		return {
			stepId: `verify_${target?.targetWidgetId || "target"}_${target?.effect || "effect"}`,
			targetRef: target?.targetRef || null,
			targetWidgetId: target?.targetWidgetId || null,
			effect: target?.effect || null,
			verificationType: target?.verificationGuidance?.verificationType || null,
			readMethod: preferredReadMethod,
			checks: effectChecks,
			instruction: preferredReadMethod ? `Call ${preferredReadMethod} on ${target?.targetWidgetId || "the target widget"} and inspect ${effectChecks.join(", ")}.` : `Inspect ${target?.targetWidgetId || "the target widget"} for ${target?.effect || "the propagated effect"}.`
		};
	}
	function readValueAtPath(value, path) {
		if (typeof path !== "string" || path.length === 0) return void 0;
		return path.split(".").reduce((current, segment) => current && typeof current === "object" ? current[segment] : void 0, value);
	}
	function executeVerificationSteps(store, verificationSteps = [], resolveTargetWidget = null) {
		return (Array.isArray(verificationSteps) ? verificationSteps : []).map((step) => {
			const targetWidget = step?.targetRef ? resolveTargetWidget?.(step.targetRef, step?.targetWidgetId || null) || store?.getResolvedWidgetForTarget?.(step.targetRef) || store?.getResolvedWidget?.(step.targetRef) || null : null;
			const verificationState = step?.readMethod === "readVerificationState" ? targetWidget?.readVerificationState?.() || null : null;
			const observedChecks = Object.fromEntries((Array.isArray(step?.checks) ? step.checks : []).map((path) => [path, readValueAtPath(verificationState, path)]));
			return {
				stepId: step?.stepId || null,
				targetRef: step?.targetRef || null,
				targetWidgetId: step?.targetWidgetId || null,
				effect: step?.effect || null,
				verificationType: step?.verificationType || null,
				readMethod: step?.readMethod || null,
				ok: Boolean(verificationState),
				checks: observedChecks,
				verificationState
			};
		});
	}
	function summarizeVerificationResults(verificationResults = []) {
		const normalized = Array.isArray(verificationResults) ? verificationResults : [];
		const total = normalized.length;
		const succeeded = normalized.filter((entry) => entry?.ok).length;
		const failed = total - succeeded;
		return {
			status: total === 0 ? "not_applicable" : failed === 0 ? "verified" : succeeded > 0 ? "partial" : "failed",
			total,
			succeeded,
			failed,
			summary: total === 0 ? "No verification steps were produced." : failed === 0 ? `Verified ${succeeded}/${total} target checks.` : succeeded > 0 ? `Partially verified ${succeeded}/${total} target checks.` : `Failed to verify any of ${total} target checks.`
		};
	}
	function buildEmptyComputedPropagationSummary() {
		return {
			active: false,
			sourceWidgetId: null,
			sourceSelectionRef: null,
			selectionRef: null,
			selectionSummary: null,
			targetWidgetIds: [],
			linkCount: 0,
			links: [],
			activatedLinks: [],
			affectedTargets: [],
			skippedTargets: [],
			verificationGuidance: [],
			verificationSteps: [],
			topology: null
		};
	}
	function buildCoordinationResult({ state = {}, description = {}, coordinationEngine = null, store = null, sourceRef = null, resolveTargetWidget = null } = {}) {
		const propagationSummary = buildPropagationSummary({
			state,
			description,
			coordinationEngine,
			sourceRef
		});
		const primarySelection = readSelectionPrimaryView(state?.shared || {});
		const widgetDescriptions = Array.isArray(description?.widgets) ? description.widgets : [];
		const widgetDescriptionByRef = Object.fromEntries(widgetDescriptions.filter((entry) => typeof entry?.ref === "string" && entry.ref.length > 0).map((entry) => [entry.ref, entry]));
		const widgetDescriptionById = Object.fromEntries(widgetDescriptions.filter((entry) => typeof entry?.widgetId === "string" && entry.widgetId.length > 0).map((entry) => [entry.widgetId, entry]));
		const linkDescriptions = Array.isArray(description?.links) ? description.links : [];
		const linkDescriptionByRef = Object.fromEntries(linkDescriptions.filter((entry) => typeof entry?.ref === "string" && entry.ref.length > 0).map((entry) => [entry.ref, entry]));
		const resolveTargetDescription = (targetRef, targetWidgetId = null, linkRef = null) => {
			const linkDescription = linkDescriptionByRef[linkRef || ""] || null;
			const resolvedTargetRef = targetRef || linkDescription?.to || linkDescription?.targetRef || null;
			const resolvedTargetWidgetId = targetWidgetId || linkDescription?.targetWidgetId || null;
			return widgetDescriptionByRef[resolvedTargetRef || ""] || widgetDescriptionById[resolvedTargetWidgetId || ""] || widgetDescriptionById[targetRef || ""] || null;
		};
		const isCoordinationEffect = (effect) => [
			"applyFilter",
			"applyHighlight",
			"focusTarget",
			"shareSelection",
			"transformView",
			"transformDataView",
			"transformStructure"
		].includes(effect);
		const propagationEntries = (Array.isArray(propagationSummary?.propagation) ? propagationSummary.propagation : []).filter((entry) => isCoordinationEffect(entry?.appliedEffect || entry?.declaredEffect || null));
		const activatedLinks = propagationEntries.map((entry) => {
			const targetDescription = resolveTargetDescription(entry?.targetRef || null, entry?.targetWidgetId || null, entry?.linkRef || null);
			const canonicalTargetRef = targetDescription?.ref || entry?.targetRef || null;
			const targetWidgetId = targetDescription?.widgetId || null;
			const coordinationEffect = entry?.appliedEffect || entry?.declaredEffect || null;
			const canResolveLegacyWidgetId = entry?.canApply === false && isLegacyPropagationSkipReason(entry?.skippedReason, "missing_target_widget") && Boolean(targetDescription);
			return isCoordinationEffect(coordinationEffect) && (entry?.canApply !== false || canResolveLegacyWidgetId) ? {
				linkRef: entry?.linkRef || null,
				linkId: typeof entry?.linkRef === "string" ? entry.linkRef.split("/").pop() || null : null,
				sourceRef: propagationSummary?.sourceRef || entry?.sourceSelectionRef || null,
				sourceSelectionRef: entry?.sourceSelectionRef || propagationSummary?.sourceRef || null,
				sourceWidgetId: entry?.sourceWidgetId || primarySelection?.sourceWidgetId || null,
				targetRef: canonicalTargetRef,
				targetWidgetId,
				linkKind: entry?.linkKind || null,
				effect: entry?.declaredEffect || null,
				declaredEffect: entry?.declaredEffect || null,
				appliedEffect: entry?.appliedEffect || null,
				responseSpec: clone$8(entry?.responseSpec || null),
				activationPolicy: entry?.activationPolicy || "automatic",
				effectConstraint: entry?.effectConstraint ?? null
			} : null;
		}).filter(Boolean);
		const skippedTargets = [...propagationEntries.map((entry) => {
			const targetDescription = resolveTargetDescription(entry?.targetRef || null, entry?.targetWidgetId || null, entry?.linkRef || null);
			if (!isCoordinationEffect(entry?.appliedEffect || entry?.declaredEffect || null)) return null;
			if (entry?.canApply === false && isLegacyPropagationSkipReason(entry?.skippedReason, "missing_target_widget") && targetDescription) return null;
			if (entry?.canApply !== false) return null;
			return {
				linkRef: entry?.linkRef || null,
				linkId: typeof entry?.linkRef === "string" ? entry.linkRef.split("/").pop() || null : null,
				targetRef: targetDescription?.ref || entry?.targetRef || null,
				targetWidgetId: targetDescription?.widgetId || null,
				linkKind: entry?.linkKind || null,
				declaredEffect: entry?.declaredEffect || null,
				appliedEffect: entry?.appliedEffect || null,
				responseSpec: clone$8(entry?.responseSpec || null),
				activationPolicy: entry?.activationPolicy || "automatic",
				effectConstraint: entry?.effectConstraint ?? null,
				reason: normalizeCanonicalPropagationSkipReason(entry?.skippedReason, "noApplicableLink")
			};
		}).filter(Boolean), ...widgetDescriptions.filter((widget) => widget?.widgetId && widget.widgetId !== (primarySelection?.sourceWidgetId || null)).filter((widget) => !activatedLinks.some((entry) => entry?.targetWidgetId === widget.widgetId)).filter((widget) => !propagationEntries.some((entry) => {
			return (resolveTargetDescription(entry?.targetRef || null, entry?.targetWidgetId || null, entry?.linkRef || null)?.widgetId || entry?.targetWidgetId || null) === widget.widgetId;
		})).map((widget) => ({
			linkRef: null,
			linkId: null,
			targetRef: widget.ref || null,
			targetWidgetId: widget.widgetId || null,
			linkKind: null,
			declaredEffect: null,
			appliedEffect: null,
			responseSpec: null,
			activationPolicy: null,
			effectConstraint: null,
			reason: normalizeCanonicalPropagationSkipReason("no_applicable_link", "noApplicableLink")
		}))];
		const affectedTargets = activatedLinks.map((link) => {
			const verificationContract = describeFamilyVerificationContract(resolveTargetDescription(link.targetRef || null, link.targetWidgetId || null, link.linkRef || null)?.kind || null);
			const verificationGuidance = buildTargetVerificationGuidance(verificationContract, link.appliedEffect, link.responseSpec || null);
			return {
				targetRef: link.targetRef,
				targetWidgetId: link.targetWidgetId,
				effect: link.appliedEffect,
				declaredEffect: link.declaredEffect,
				responseSpec: clone$8(link.responseSpec || null),
				activationPolicy: link.activationPolicy,
				effectConstraint: link.effectConstraint,
				linkId: link.linkId,
				verification: verificationContract,
				verificationGuidance
			};
		});
		const verificationGuidance = affectedTargets.map((target) => ({
			targetRef: target.targetRef,
			targetWidgetId: target.targetWidgetId,
			effect: target.effect,
			responseSpec: clone$8(target.responseSpec || null),
			activationPolicy: target.activationPolicy,
			effectConstraint: target.effectConstraint,
			verificationType: target.verificationGuidance.verificationType,
			preferredReadMethod: target.verificationGuidance.preferredReadMethod,
			effectChecks: [...target.verificationGuidance.effectChecks]
		}));
		const verificationSteps = affectedTargets.map((target) => buildTargetVerificationStep(target));
		const verificationResults = executeVerificationSteps(store, verificationSteps, resolveTargetWidget);
		const verification = summarizeVerificationResults(verificationResults);
		return {
			changed: false,
			propagationSummary: {
				active: activatedLinks.length > 0 || skippedTargets.length > 0,
				sourceRef: propagationSummary?.sourceRef || primarySelection?.selectionRef || null,
				sourceSelectionRef: primarySelection?.selectionRef || propagationSummary?.sourceRef || null,
				sourceWidgetId: primarySelection?.sourceWidgetId || null,
				selectionRef: primarySelection?.selectionRef || null,
				selectionSummary: primarySelection?.summary || null,
				targetWidgetIds: uniqueStrings(affectedTargets.map((target) => target.targetWidgetId)),
				linkCount: activatedLinks.length,
				links: activatedLinks,
				activatedLinks,
				affectedTargets,
				skippedTargets,
				verificationGuidance,
				verificationSteps,
				topology: propagationSummary?.topology || null
			},
			verificationSteps,
			verificationResults,
			verification
		};
	}
	function buildCoordinationOperationResult({ changed = false, coordinationState = null, coordinationResult = null } = {}) {
		return {
			changed,
			coordinationState,
			propagationSummary: clone$8(coordinationResult?.propagationSummary || null),
			verificationSteps: Array.isArray(coordinationResult?.verificationSteps) ? [...coordinationResult.verificationSteps] : [],
			verificationResults: Array.isArray(coordinationResult?.verificationResults) ? [...coordinationResult.verificationResults] : [],
			verification: clone$8(coordinationResult?.verification || null)
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/coordination/workspaceCoordinationOperations.js
	function clone$7(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function cloneObject(value) {
		return value && typeof value === "object" && !Array.isArray(value) ? clone$7(value) : {};
	}
	function syncGlobalFiltersFromControlState(workspace, controlState = {}, { rangeDomains = {} } = {}) {
		const globalFilters = workspace.buildGlobalFiltersFromControlState(controlState, { rangeDomains });
		workspace.setGlobalFilters(globalFilters);
		return globalFilters;
	}
	function setHighlightState(workspace, highlight = null) {
		return workspace.updateSharedCoordinationState((shared) => withHighlightSubmodel(shared, highlight));
	}
	function clearHighlightState(workspace) {
		const previousHighlightState = workspace.readHighlightState();
		if (!(Array.isArray(previousHighlightState?.entries) && previousHighlightState.entries.length > 0)) return {
			changed: false,
			previousHighlightState,
			nextHighlightState: previousHighlightState
		};
		setHighlightState(workspace, null);
		return {
			changed: true,
			previousHighlightState,
			nextHighlightState: workspace.readHighlightState()
		};
	}
	function commitClearHighlightState(workspace, options = {}) {
		const transition = clearHighlightState(workspace);
		return workspace.commitCoordinationOperationResult({
			...options,
			changed: transition?.changed === true
		});
	}
	function setSelectionPrimary(workspace, selection = null) {
		const currentShared = (workspace.readState() || {})?.shared || {};
		const normalizedSelection = selection == null ? null : normalizePrimarySelectionView(selection, readSelectionRegistry(currentShared));
		return workspace.updateSharedCoordinationState((shared) => withSelectionSubmodel(shared, { primary: normalizedSelection }));
	}
	function setSelectionViewsByWidget(workspace, selectionViewsByWidget = {}) {
		return workspace.updateSharedCoordinationState((shared) => withSelectionSubmodel(shared, { byWidget: cloneObject(selectionViewsByWidget) }));
	}
	function setSelectionRegistry(workspace, selectionRegistry = {}) {
		return workspace.updateSharedCoordinationState((shared) => withSelectionSubmodel(shared, { registry: cloneObject(selectionRegistry) }));
	}
	function upsertSelectionEntry(workspace, selectionEntry, options = {}) {
		if (!selectionEntry || typeof selectionEntry !== "object") throw new Error("WidgetWorkspace.upsertSelectionEntry requires a selection entry object.");
		const selectionRef = typeof selectionEntry.selectionRef === "string" && selectionEntry.selectionRef.length > 0 ? selectionEntry.selectionRef : null;
		if (!selectionRef) throw new Error("WidgetWorkspace.upsertSelectionEntry requires selectionEntry.selectionRef.");
		const sourceWidget = workspace.getWidget(selectionEntry.sourceWidgetId || selectionEntry.sourceWidgetRef || null);
		const sourceWidgetId = selectionEntry.sourceWidgetId || sourceWidget?.resolveWidgetId?.() || sourceWidget?.describe?.()?.widgetId || null;
		const sourceWidgetRef = selectionEntry.sourceWidgetRef || sourceWidget?.resolveWidgetRef?.() || sourceWidget?.describe?.()?.ref || null;
		if (!sourceWidgetId) throw new Error("WidgetWorkspace.upsertSelectionEntry requires a sourceWidgetId or resolvable source widget.");
		const makePrimary = options?.makePrimary !== false;
		const updateByWidget = options?.updateByWidget !== false;
		const focusSourceWidget = options?.focusSourceWidget !== false;
		const currentSelectionState = workspace.readSelectionState() || {
			registry: {},
			views: {
				primary: null,
				byWidget: {}
			}
		};
		const previousFocusedWidgetRef = workspace.readCoordinationState()?.focusedWidgetRef || null;
		const nextRegistry = {
			...currentSelectionState?.registry || {},
			[selectionRef]: clone$7({
				...selectionEntry,
				selectionRef,
				sourceWidgetId,
				sourceWidgetRef
			})
		};
		const nextByWidget = updateByWidget ? {
			...currentSelectionState?.views?.byWidget || {},
			[sourceWidgetId]: { selectionRef }
		} : currentSelectionState?.views?.byWidget || {};
		const nextPrimary = makePrimary ? normalizePrimarySelectionView({ selectionRef }, nextRegistry) : currentSelectionState?.views?.primary || null;
		workspace.updateSharedCoordinationState((shared) => {
			const nextShared = withSelectionSubmodel(shared, {
				registry: nextRegistry,
				byWidget: cloneObject(nextByWidget),
				primary: nextPrimary
			});
			if (focusSourceWidget) nextShared.focusedWidget = sourceWidgetRef || nextShared.focusedWidget || null;
			return nextShared;
		});
		const nextSelectionState = workspace.readSelectionState() || {
			registry: {},
			views: {
				primary: null,
				byWidget: {}
			}
		};
		const nextFocusedWidgetRef = workspace.readCoordinationState()?.focusedWidgetRef || null;
		return {
			changed: JSON.stringify(nextSelectionState) !== JSON.stringify(currentSelectionState) || nextFocusedWidgetRef !== previousFocusedWidgetRef,
			previousSelectionState: currentSelectionState,
			nextSelectionState,
			previousFocusedWidgetRef,
			nextFocusedWidgetRef,
			selectionRef,
			sourceWidgetId,
			sourceWidgetRef
		};
	}
	function syncPrimarySelectionEntry(workspace, selectionEntry = null, options = {}) {
		if (selectionEntry == null) {
			const transition = clearSelectionState(workspace);
			return workspace.commitCoordinationOperationResult({
				...options,
				changed: transition?.changed === true
			});
		}
		const transition = upsertSelectionEntry(workspace, selectionEntry, {
			makePrimary: options?.makePrimary !== false,
			updateByWidget: options?.updateByWidget !== false,
			focusSourceWidget: options?.focusSourceWidget !== false
		});
		return workspace.commitCoordinationOperationResult({
			...options,
			changed: transition?.changed === true
		});
	}
	function clearSelectionState(workspace) {
		const previousSelectionState = workspace.readSelectionState() || {
			registry: {},
			views: {
				primary: null,
				byWidget: {}
			}
		};
		if (!(Object.keys(previousSelectionState?.registry || {}).length > 0 || previousSelectionState?.views?.primary != null || Object.keys(previousSelectionState?.views?.byWidget || {}).length > 0)) return {
			changed: false,
			previousSelectionState,
			nextSelectionState: previousSelectionState
		};
		workspace.updateSharedCoordinationState((shared) => withSelectionSubmodel(shared, {
			primary: null,
			byWidget: {},
			registry: {}
		}));
		return {
			changed: true,
			previousSelectionState,
			nextSelectionState: workspace.readSelectionState() || {
				registry: {},
				views: {
					primary: null,
					byWidget: {}
				}
			}
		};
	}
	function promotePrimarySelectionToGlobalFilters(workspace, options = {}) {
		const primarySelection = workspace.readSelectionState()?.views?.primary || null;
		if (!primarySelection?.selectionRef) return {
			changed: false,
			globalFilterPatch: null,
			nextGlobalFilters: workspace.readGlobalFilters()
		};
		const globalFilterPatch = workspace.controlStateAdapter?.deriveGlobalFiltersFromSelection ? workspace.controlStateAdapter.deriveGlobalFiltersFromSelection(primarySelection, { rangeDomains: options?.rangeDomains || {} }) : deriveGlobalFiltersFromSelection(primarySelection, { rangeDomains: options?.rangeDomains || {} });
		if (!globalFilterPatch) return {
			changed: false,
			globalFilterPatch: null,
			nextGlobalFilters: workspace.readGlobalFilters()
		};
		const nextGlobalFilters = {
			...options?.baseGlobalFilters && typeof options.baseGlobalFilters === "object" && !Array.isArray(options.baseGlobalFilters) ? clone$7(options.baseGlobalFilters) : clone$7(workspace.readGlobalFilters() || {}),
			...globalFilterPatch
		};
		const clearSelection = options?.clearSelection !== false;
		const focusSourceWidget = options?.focusSourceWidget !== false;
		const focusedWidgetRef = focusSourceWidget ? workspace.resolveWorkspaceWidgetRef(primarySelection.sourceWidgetId || primarySelection.sourceWidgetRef, { allowNull: true }) : null;
		workspace.updateSharedCoordinationState((shared) => {
			const nextShared = {
				...shared || {},
				globalFilters: clone$7(nextGlobalFilters)
			};
			if (clearSelection) return withSelectionSubmodel({
				...nextShared,
				focusedWidget: focusedWidgetRef || nextShared.focusedWidget || null
			}, {
				primary: null,
				byWidget: {},
				registry: {}
			});
			if (focusSourceWidget) nextShared.focusedWidget = focusedWidgetRef || nextShared.focusedWidget || null;
			return nextShared;
		});
		return {
			changed: true,
			selectionRef: primarySelection.selectionRef,
			globalFilterPatch,
			nextGlobalFilters
		};
	}
	function commitPrimarySelectionToGlobalFilters(workspace, options = {}) {
		const promotionResult = promotePrimarySelectionToGlobalFilters(workspace, options);
		if (!promotionResult?.changed) return {
			...workspace.commitCoordinationOperationResult({
				...options,
				changed: false
			}),
			changed: false,
			selectionRef: promotionResult?.selectionRef || null,
			globalFilterPatch: promotionResult?.globalFilterPatch || null,
			nextGlobalFilters: promotionResult?.nextGlobalFilters || workspace.readGlobalFilters()
		};
		return {
			...workspace.commitCoordinationOperationResult({
				...options,
				changed: true
			}),
			changed: true,
			selectionRef: promotionResult.selectionRef || null,
			globalFilterPatch: promotionResult.globalFilterPatch || null,
			nextGlobalFilters: promotionResult.nextGlobalFilters || workspace.readGlobalFilters()
		};
	}
	function promotePrimarySelectionToHighlight(workspace, options = {}) {
		const primarySelection = workspace.readSelectionState()?.views?.primary || null;
		if (!primarySelection?.selectionRef) return {
			changed: false,
			highlightState: workspace.readHighlightState()
		};
		const highlightState = deriveHighlightStateFromSelection(primarySelection, workspace.listWidgets());
		const clearSelection = options?.clearSelection !== false;
		const focusSourceWidget = options?.focusSourceWidget !== false;
		const focusedWidgetRef = focusSourceWidget ? workspace.resolveWorkspaceWidgetRef(primarySelection.sourceWidgetId || primarySelection.sourceWidgetRef, { allowNull: true }) : null;
		workspace.updateSharedCoordinationState((shared) => {
			const nextShared = withHighlightSubmodel(shared, highlightState);
			if (clearSelection) return withSelectionSubmodel({
				...nextShared,
				focusedWidget: focusedWidgetRef || nextShared.focusedWidget || null
			}, {
				primary: null,
				byWidget: {},
				registry: {}
			});
			if (focusSourceWidget) nextShared.focusedWidget = focusedWidgetRef || nextShared.focusedWidget || null;
			return nextShared;
		});
		return {
			changed: true,
			selectionRef: primarySelection.selectionRef,
			highlightState: workspace.readHighlightState()
		};
	}
	function commitPrimarySelectionToHighlight(workspace, options = {}) {
		const promotionResult = promotePrimarySelectionToHighlight(workspace, options);
		if (!promotionResult?.changed) return {
			...workspace.commitCoordinationOperationResult({
				...options,
				changed: false
			}),
			changed: false,
			selectionRef: promotionResult?.selectionRef || null,
			highlightState: promotionResult?.highlightState || workspace.readHighlightState()
		};
		return {
			...workspace.commitCoordinationOperationResult({
				...options,
				changed: true
			}),
			changed: true,
			selectionRef: promotionResult.selectionRef || null,
			highlightState: promotionResult.highlightState || workspace.readHighlightState()
		};
	}
	function setFocusedWidget(workspace, refOrId) {
		const widgetRef = workspace.resolveWorkspaceWidgetRef(refOrId, { allowNull: true });
		return workspace.updateSharedCoordinationState((shared) => ({
			...shared,
			focusedWidget: widgetRef
		}));
	}
	function setGlobalFilters(workspace, globalFilters = {}) {
		const nextGlobalFilters = globalFilters && typeof globalFilters === "object" && !Array.isArray(globalFilters) ? clone$7(globalFilters) : {};
		return workspace.updateSharedCoordinationState((shared) => ({
			...shared,
			globalFilters: nextGlobalFilters
		}));
	}
	function clearGlobalFilters(workspace) {
		return setGlobalFilters(workspace, {});
	}
	//#endregion
	//#region ../../widgetva-kit/src/workspace/widgetWorkspace.js
	var WIDGET_WORKSPACE_PUBLIC_METHODS = [
		"listWidgetDescriptions",
		"listWidgets",
		"getWidget",
		"registerWidget",
		"removeWidget",
		"listLinks",
		"getLink",
		"registerLink",
		"removeLink",
		"describe",
		"describeWorkspace",
		"describeComposition",
		"readView",
		"readState",
		"readObservation",
		"readCoordinationState",
		"readSharedAnalyticalState",
		"readSharedFilterContext",
		"readSharedViewportContext",
		"readSharedSemanticFocus",
		"readSharedStructuralContext",
		"readActiveAnalyticalContext",
		"readViewStatesByWidget",
		"readSharedViewContext",
		"readSharedTransformationContext",
		"readPropagationSummary",
		"readComputedPropagationSummary",
		"readComputedCoordinationResult",
		"buildCoordinationOperationResult",
		"commitCoordinationOperationResult",
		"readLatestCoordinationResult",
		"readFocusState",
		"readHighlightState",
		"readFocusedWidget",
		"readFocusedWidgetId",
		"readSelectionState",
		"readGlobalFilters",
		"readCoordinationControlState",
		"readControlProjection",
		"buildGlobalFiltersFromControlState",
		"syncGlobalFiltersFromControlState",
		"setLatestCoordinationResult",
		"clearLatestCoordinationResult",
		"resetEphemeralCoordinationState",
		"setHighlightState",
		"clearHighlightState",
		"commitClearHighlightState",
		"setSelectionPrimary",
		"setSelectionViewsByWidget",
		"setSelectionRegistry",
		"upsertSelectionEntry",
		"syncPrimarySelectionEntry",
		"promotePrimarySelectionToGlobalFilters",
		"commitPrimarySelectionToGlobalFilters",
		"promotePrimarySelectionToHighlight",
		"commitPrimarySelectionToHighlight",
		"clearSelectionState",
		"setFocusedWidget",
		"setGlobalFilters",
		"clearGlobalFilters",
		"readStateHistory",
		"listBranches",
		"readCurrentSnapshotMeta",
		"readTraceGraph",
		"readLatestAgentResponse",
		"listAgentResponses",
		"readWidgetState",
		"readLinkTopology",
		"executeAction",
		"executeActionAndCommitCoordination",
		"queryPerception",
		"runDataQuery",
		"readTrace",
		"jumpToState",
		"jumpToStateAndClearLatestCoordinationResult",
		"branchFromState",
		"getTrace",
		"readSnapshot",
		"replay",
		"dispose"
	];
	function makeCallId(prefix = "workspace_call") {
		return `${prefix}_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
	}
	function clone$6(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function uniqueRefs(values = []) {
		return values.filter((value, index) => typeof value === "string" && value.length > 0 && values.indexOf(value) === index);
	}
	function defaultControlStateAdapter() {
		return {
			deriveCoordinationControlState({ globalFilters = {}, primarySelection = null, focusedWidgetId = null } = {}) {
				return {
					globalFilters: clone$6(globalFilters || {}),
					primarySelection: clone$6(primarySelection || null),
					focusedWidgetId: focusedWidgetId || null
				};
			},
			deriveControlProjection(controlState = {}, { fallbackSelectedWidgetId = null } = {}) {
				return {
					focusedWidgetId: controlState?.focusedWidgetId || fallbackSelectedWidgetId || null,
					globalFilters: clone$6(controlState?.globalFilters || {}),
					primarySelection: clone$6(controlState?.primarySelection || null)
				};
			},
			deriveGlobalFiltersFromControlState(controlState = {}) {
				if (controlState?.globalFilters && typeof controlState.globalFilters === "object" && !Array.isArray(controlState.globalFilters)) return clone$6(controlState.globalFilters);
				return {};
			},
			deriveGlobalFiltersFromSelection(primarySelection = null, { rangeDomains = {} } = {}) {
				return deriveGlobalFiltersFromSelection(primarySelection, { rangeDomains });
			}
		};
	}
	function resolveSharedRuntime(widgets = [], explicitRuntime = null) {
		if (explicitRuntime) return explicitRuntime;
		const runtimes = widgets.map((widget) => widget?.runtime || null).filter(Boolean);
		if (runtimes.length === 0) throw new Error("WidgetWorkspace requires either a runtime or at least one widget instance.");
		const [firstRuntime] = runtimes;
		if (runtimes.find((runtime) => runtime !== firstRuntime)) throw new Error("WidgetWorkspace requires all widget instances to share the same runtime.");
		return firstRuntime;
	}
	function uniqueWidgets(widgets = []) {
		const seen = /* @__PURE__ */ new Set();
		const result = [];
		for (const widget of widgets) {
			const key = widget?.resolveWidgetRef?.() || widget?.resolveWidgetId?.() || null;
			if (!key || seen.has(key)) continue;
			seen.add(key);
			result.push(widget);
		}
		return result;
	}
	var WidgetWorkspace = class {
		constructor({ widgets = [], links = [], runtime = null, disposeWidgetsOnDispose = false, controlStateAdapter = null } = {}) {
			this.widgets = uniqueWidgets(widgets);
			this.runtime = resolveSharedRuntime(this.widgets, runtime);
			this.disposeWidgetsOnDispose = disposeWidgetsOnDispose;
			this.controlStateAdapter = {
				...defaultControlStateAdapter(),
				...controlStateAdapter && typeof controlStateAdapter === "object" ? controlStateAdapter : {}
			};
			this.links = [];
			this.latestCoordinationResult = null;
			for (const link of links) this.registerLink(link);
		}
		static describeContract() {
			return {
				methods: [...WIDGET_WORKSPACE_PUBLIC_METHODS],
				constructorOptions: {
					widgets: "optional initial widget instances sharing one runtime",
					links: "optional initial coordination links to register on the shared runtime",
					runtime: "optional explicit shared runtime if widgets are added later",
					disposeWidgetsOnDispose: "whether workspace.dispose() should also dispose registered widgets",
					controlStateAdapter: "optional app-level adapter for mapping workspace coordination state to human control-state bindings and back"
				},
				composition: {
					widgets: "workspace registers and resolves widget instances by ref or widgetId",
					links: "workspace registers coordination links and exposes topology summaries",
					actions: "workspace-level executeAction/queryPerception delegate through the shared runtime public API"
				},
				coordinationState: {
					sharedAnalyticalState: "canonical workspace-level shared analytical state bundle, including shared sub-contexts for filter, viewport, semantic focus, structural context, shared view, and shared transformations",
					sharedFilterContext: "stable read surface for the shared filtering context that subsequent analysis turns should inherit",
					sharedViewportContext: "stable read surface for the shared viewport/focus region that subsequent analysis turns should inherit",
					sharedSemanticFocus: "stable read surface for the workspace-shared focus/selection/highlight context",
					sharedStructuralContext: "stable read surface for shared structural comparison context, including links and comparison targets",
					sharedViewContext: "workspace-level summary of currently active widget view-state overrides",
					sharedTransformationContext: "workspace-level summary of view/data/structure transformations that affect later analysis turns",
					activeAnalyticalContext: "compact workspace-level summary of which shared analytical contexts are currently active",
					focusedWidget: "workspace-level focus target",
					focus: "derived convenience read exposing normalized focus metadata",
					selections: {
						registry: "canonical shared selection registry",
						views: {
							primary: "workspace primary-selection convenience view",
							byWidget: "widget/source-grouped selection convenience view"
						}
					},
					globalFilters: "workspace-level shared filter state",
					highlight: "derived convenience read exposing cross-widget highlight feedback summaries",
					links: {
						definitions: "canonical registered workspace link definitions with explicit kind, effect, activationPolicy, effectConstraint, and optional advanced responseSpec",
						topology: "derived coordination topology read surface built from registered links and widget descriptions"
					},
					latestCoordinationResult: "ephemeral workspace-level runtime result bundle for the most recent coordination-driving interaction",
					computedPropagationSummary: "canonical workspace-owned read surface for the current computed propagation summary, with stable empty defaults"
				},
				coordinationWrites: {
					highlight: "workspace can set or clear shared highlight state separately from selection and filters",
					selection: "workspace can upsert a canonical shared selection entry and optionally promote it to primary focus",
					controlState: "workspace can derive UI-facing control/binding state from canonical coordination state and sync canonical filters back from that control state",
					selectionOperations: "workspace can atomically clear or sync a canonical primary selection entry and commit the corresponding coordination operation result",
					promotionOperations: "workspace can promote the current primary selection into canonical global-filter or highlight state and commit the resulting coordination operation bundle",
					actionOperations: "workspace can execute an action through the shared runtime and, when successful, commit the resulting coordination operation bundle from the workspace layer",
					highlightOperations: "workspace can clear shared highlight state and commit the resulting coordination operation bundle from the workspace layer",
					historyOperations: "workspace can perform history jumps and clear stale ephemeral coordination results from the workspace layer",
					ephemeralOperations: "workspace can explicitly reset ephemeral coordination read surfaces when a new first-party session view is initialized"
				}
			};
		}
		listWidgetDescriptions() {
			const descriptions = this.runtime?.store?.listWidgetDescriptions?.() || [];
			if (descriptions.length > 0) return descriptions;
			return this.listWidgets().map((widget) => widget?.describe?.()).filter(Boolean);
		}
		listWidgets() {
			return [...this.widgets];
		}
		getWidget(refOrId) {
			return this.widgets.find((widget) => {
				const description = widget?.describe?.() || null;
				return widget?.resolveWidgetRef?.() === refOrId || widget?.resolveWidgetId?.() === refOrId || description?.ref === refOrId || description?.widgetId === refOrId;
			}) || null;
		}
		registerWidget(widget) {
			if (!widget) throw new Error("WidgetWorkspace.registerWidget requires a widget instance.");
			const widgetRuntime = widget?.runtime || null;
			if (widgetRuntime && widgetRuntime !== this.runtime) throw new Error("WidgetWorkspace.registerWidget requires widget instances to share the same runtime.");
			this.widgets = uniqueWidgets([...this.widgets, widget]);
			return widget;
		}
		removeWidget(refOrId) {
			const existing = this.getWidget(refOrId);
			if (!existing) return null;
			this.widgets = this.widgets.filter((widget) => widget !== existing);
			return existing;
		}
		listLinks() {
			return listWorkspaceLinks(this);
		}
		getLink(refOrId) {
			return getWorkspaceLink(this, refOrId);
		}
		registerLink(link) {
			return registerWorkspaceLink(this, link);
		}
		removeLink(refOrId) {
			return removeWorkspaceLink(this, refOrId);
		}
		describe() {
			return this.runtime?.store ? readWorkspaceDescriptionFromStore(this.runtime.store) : null;
		}
		describeWorkspace() {
			return this.describe();
		}
		describeComposition() {
			return describeWorkspaceComposition(this);
		}
		readState(options = {}) {
			return this.runtime?.store?.readState?.(options) || null;
		}
		readView(options = {}) {
			return this.readState(options);
		}
		readObservation(options = {}) {
			return readObservation$1({
				query: options?.query || null,
				describeWorkspace: () => this.describeWorkspace(),
				readState: (readOptions = {}) => this.readState(readOptions),
				readSharedAnalyticalState: ({ state }) => this.readSharedAnalyticalState({ state }),
				view: options?.view || null,
				options
			});
		}
		readCoordinationState() {
			return buildCoordinationStateFromWorkspaceState(this.readState() || {}, {
				currentBranchId: this.runtime?.store?.currentBranchId || null,
				derivedTopology: this.readLinkTopology()
			});
		}
		readSharedAnalyticalState(options = {}) {
			return readSharedAnalyticalState(options?.state || this.readState() || {}, { derivedTopology: this.readLinkTopology() });
		}
		readSharedFilterContext(options = {}) {
			return readSharedFilterContext(options?.state || this.readState() || {}, { derivedTopology: this.readLinkTopology() });
		}
		readSharedViewportContext(options = {}) {
			return readSharedViewportContext(options?.state || this.readState() || {}, { derivedTopology: this.readLinkTopology() });
		}
		readSharedSemanticFocus(options = {}) {
			return readSharedSemanticFocus(options?.state || this.readState() || {}, { derivedTopology: this.readLinkTopology() });
		}
		readSharedStructuralContext(options = {}) {
			return readSharedStructuralContext(options?.state || this.readState() || {}, { derivedTopology: this.readLinkTopology() });
		}
		readActiveAnalyticalContext(options = {}) {
			return readActiveAnalyticalContext(options?.state || this.readState() || {}, { derivedTopology: this.readLinkTopology() });
		}
		readViewStatesByWidget(options = {}) {
			return readViewStatesByWidget(options?.state || this.readState() || {});
		}
		readSharedViewContext(options = {}) {
			return readSharedViewContext(options?.state || this.readState() || {});
		}
		readSharedTransformationContext(options = {}) {
			return readSharedTransformationContext(options?.state || this.readState() || {});
		}
		readPropagationSummary(options = {}) {
			return buildPropagationSummary({
				state: this.readState(),
				description: this.describeWorkspace(),
				coordinationEngine: this.runtime?.coordinationEngine || null,
				sourceRef: options?.sourceRef || null
			});
		}
		readComputedPropagationSummary(options = {}) {
			return clone$6(this.readComputedCoordinationResult(options)?.propagationSummary || buildEmptyComputedPropagationSummary());
		}
		readComputedCoordinationResult(options = {}) {
			return buildCoordinationResult({
				state: this.readState(),
				description: this.describeWorkspace(),
				coordinationEngine: this.runtime?.coordinationEngine || null,
				store: this.runtime?.store || null,
				sourceRef: options?.sourceRef || null,
				resolveTargetWidget: (targetRef, targetWidgetId) => (targetWidgetId ? this.getWidget(targetWidgetId) : null) || this.getWidget(targetRef) || null
			});
		}
		buildCoordinationOperationResult(options = {}) {
			const coordinationResult = this.readComputedCoordinationResult(options);
			return buildCoordinationOperationResult({
				changed: options?.changed === true,
				coordinationState: this.readCoordinationState(),
				coordinationResult
			});
		}
		commitCoordinationOperationResult(options = {}) {
			const result = this.buildCoordinationOperationResult(options);
			this.setLatestCoordinationResult(result);
			return result;
		}
		readLatestCoordinationResult() {
			return clone$6(this.latestCoordinationResult);
		}
		readFocusedWidget() {
			const focusedWidgetRef = this.readCoordinationState().focusedWidgetRef;
			return focusedWidgetRef ? this.getWidget(focusedWidgetRef) : null;
		}
		readFocusedWidgetId() {
			const focusedWidget = this.readFocusedWidget();
			return focusedWidget?.resolveWidgetId?.() || focusedWidget?.describe?.()?.widgetId || null;
		}
		readFocusState() {
			const state = this.readState() || {};
			const focusState = readFocusState(state?.shared || {}, state?.widgets || {});
			if (!focusState?.widgetRef || focusState?.widgetId) return focusState;
			const focusedWidget = this.getWidget(focusState.widgetRef);
			return {
				...focusState,
				widgetId: focusedWidget?.resolveWidgetId?.() || focusedWidget?.describe?.()?.widgetId || null
			};
		}
		readHighlightState() {
			return deriveHighlightState(this.readState() || {});
		}
		readSelectionState() {
			return this.readCoordinationState().selections;
		}
		readGlobalFilters() {
			return this.readCoordinationState().globalFilters;
		}
		readCoordinationControlState({ rangeDomains = {} } = {}) {
			const coordination = this.readCoordinationState();
			return this.controlStateAdapter.deriveCoordinationControlState({
				globalFilters: coordination?.globalFilters || {},
				primarySelection: coordination?.selections?.views?.primary || null,
				focusedWidgetId: this.readFocusedWidget()?.resolveWidgetId?.() || this.readFocusedWidget()?.describe?.()?.widgetId || null,
				rangeDomains
			});
		}
		readControlProjection({ rangeDomains = {}, fallbackSelectedWidgetId = null } = {}) {
			return this.controlStateAdapter.deriveControlProjection(this.readCoordinationControlState({ rangeDomains }), { fallbackSelectedWidgetId });
		}
		buildGlobalFiltersFromControlState(controlState = {}, { rangeDomains = {} } = {}) {
			return this.controlStateAdapter.deriveGlobalFiltersFromControlState(controlState, { rangeDomains });
		}
		syncGlobalFiltersFromControlState(controlState = {}, { rangeDomains = {} } = {}) {
			return syncGlobalFiltersFromControlState(this, controlState, { rangeDomains });
		}
		setLatestCoordinationResult(result = null) {
			this.latestCoordinationResult = result == null ? null : clone$6(result);
			return this.readLatestCoordinationResult();
		}
		clearLatestCoordinationResult() {
			this.latestCoordinationResult = null;
			return null;
		}
		resetEphemeralCoordinationState() {
			this.clearLatestCoordinationResult();
			return { latestCoordinationResult: null };
		}
		setHighlightState(highlight = null) {
			return setHighlightState(this, highlight);
		}
		clearHighlightState() {
			return clearHighlightState(this);
		}
		commitClearHighlightState(options = {}) {
			return commitClearHighlightState(this, options);
		}
		setSelectionPrimary(selection = null) {
			return setSelectionPrimary(this, selection);
		}
		setSelectionViewsByWidget(selectionViewsByWidget = {}) {
			return setSelectionViewsByWidget(this, selectionViewsByWidget);
		}
		setSelectionRegistry(selectionRegistry = {}) {
			return setSelectionRegistry(this, selectionRegistry);
		}
		upsertSelectionEntry(selectionEntry, options = {}) {
			return upsertSelectionEntry(this, selectionEntry, options);
		}
		syncPrimarySelectionEntry(selectionEntry = null, options = {}) {
			return syncPrimarySelectionEntry(this, selectionEntry, options);
		}
		clearSelectionState() {
			return clearSelectionState(this);
		}
		promotePrimarySelectionToGlobalFilters(options = {}) {
			return promotePrimarySelectionToGlobalFilters(this, options);
		}
		commitPrimarySelectionToGlobalFilters(options = {}) {
			return commitPrimarySelectionToGlobalFilters(this, options);
		}
		promotePrimarySelectionToHighlight(options = {}) {
			return promotePrimarySelectionToHighlight(this, options);
		}
		commitPrimarySelectionToHighlight(options = {}) {
			return commitPrimarySelectionToHighlight(this, options);
		}
		resolveWorkspaceWidgetRef(refOrId, { allowNull = false } = {}) {
			if (refOrId == null || refOrId === "") {
				if (allowNull) return null;
				throw new Error("WidgetWorkspace requires a widget ref or widgetId.");
			}
			const widget = this.getWidget(refOrId);
			if (!widget) throw new Error(`Unknown widget in workspace: ${refOrId}`);
			return widget.resolveWidgetRef?.() || widget.describe?.()?.ref || refOrId;
		}
		resolveWorkspaceWidgetRefs(refs = []) {
			if (!Array.isArray(refs)) throw new Error("WidgetWorkspace requires an array of widget refs or widgetIds.");
			return uniqueRefs(refs.map((ref) => this.resolveWorkspaceWidgetRef(ref)));
		}
		updateSharedCoordinationState(updater) {
			return updateSharedStateInStore(this.runtime?.store, updater);
		}
		setFocusedWidget(refOrId) {
			return setFocusedWidget(this, refOrId);
		}
		setGlobalFilters(globalFilters = {}) {
			return setGlobalFilters(this, globalFilters);
		}
		clearGlobalFilters() {
			return clearGlobalFilters(this);
		}
		readStateHistory(options = {}) {
			return listStateSnapshotsFromStore(this.runtime?.store, options);
		}
		listBranches() {
			return listBranchesFromStore(this.runtime?.store);
		}
		readCurrentSnapshotMeta() {
			return readCurrentSnapshotMetaFromStore(this.runtime?.store);
		}
		readTraceGraph(options = {}) {
			return buildTraceGraphFromStore(this.runtime?.store, options);
		}
		readLatestAgentResponse(options = {}) {
			return readLatestResponseFromStore(this.runtime?.store, { workspaceId: options.workspaceId || this.describe()?.workspaceId || null });
		}
		listAgentResponses(options = {}) {
			return listResponsesFromStore(this.runtime?.store, options.limit || 20, { workspaceId: options.workspaceId || this.describe()?.workspaceId || null });
		}
		readWidgetState(refOrId) {
			return this.getWidget(refOrId)?.readState?.() || null;
		}
		readLinkTopology() {
			return readWorkspaceLinkTopology(this);
		}
		async executeAction(call) {
			if (!call || typeof call !== "object") throw new Error("WidgetWorkspace.executeAction requires an action call object.");
			const executeAction = this.runtime?.executeAction;
			if (typeof executeAction !== "function") throw new Error("WidgetWorkspace.executeAction requires a runtime with executeAction support.");
			return executeAction({
				actor: call.actor || "agent",
				callId: call.callId || makeCallId("workspace_action"),
				...call
			});
		}
		async executeActionAndCommitCoordination(call, options = {}) {
			const result = await this.executeAction(call);
			if (!result?.ok) return result;
			const propagationSourceRef = result?.result?.propagationSourceRef || null;
			return {
				...result,
				coordinationResult: this.commitCoordinationOperationResult({
					...options,
					changed: options?.changed !== false,
					...propagationSourceRef ? { sourceRef: propagationSourceRef } : {}
				})
			};
		}
		async queryPerception(call) {
			if (!call || typeof call !== "object") throw new Error("WidgetWorkspace.queryPerception requires a perception call object.");
			const queryPerception = this.runtime?.queryPerception;
			if (typeof queryPerception !== "function") throw new Error("WidgetWorkspace.queryPerception requires a runtime with queryPerception support.");
			return queryPerception({
				actor: call.actor || "agent",
				callId: call.callId || makeCallId("workspace_perception"),
				...call
			});
		}
		async runDataQuery(call) {
			if (!call || typeof call !== "object") throw new Error("WidgetWorkspace.runDataQuery requires a data-query call object.");
			const runDataQuery = this.runtime?.runDataQuery || this.runtime?.queryData;
			if (typeof runDataQuery !== "function") throw new Error("WidgetWorkspace.runDataQuery requires a runtime with data-query support.");
			return runDataQuery({
				actor: call.actor || "agent",
				callId: call.callId || makeCallId("workspace_data_query"),
				...call
			});
		}
		readTrace(options = {}) {
			return this.getTrace(options);
		}
		getTrace(options = {}) {
			return readInteractionTraceFromStore(this.runtime?.store, options);
		}
		readSnapshot(stateId, options = {}) {
			return readSnapshotFromStore(this.runtime?.store, stateId, options);
		}
		async replay(stateId) {
			return this.jumpToState(typeof stateId === "string" ? { stateId } : stateId);
		}
		async jumpToState(options = {}) {
			const normalizedOptions = typeof options === "string" ? { stateId: options } : options || {};
			const executeAction = this.runtime?.executeAction;
			if (typeof executeAction !== "function") throw new Error("WidgetWorkspace.jumpToState requires a runtime with executeAction support.");
			return executeAction({
				callId: normalizedOptions.callId || makeCallId("workspace_replay"),
				actor: normalizedOptions.actor || "agent",
				name: "workspace.jumpToState",
				params: { stateId: normalizedOptions.stateId || null }
			});
		}
		async jumpToStateAndClearLatestCoordinationResult(options = {}) {
			const result = await this.jumpToState(options);
			this.clearLatestCoordinationResult();
			return result;
		}
		async branchFromState(options = {}) {
			const normalizedOptions = options && typeof options === "object" ? options : {};
			const executeAction = this.runtime?.executeAction;
			if (typeof executeAction !== "function") throw new Error("WidgetWorkspace.branchFromState requires a runtime with executeAction support.");
			return executeAction({
				callId: normalizedOptions.callId || makeCallId("workspace_branch"),
				actor: normalizedOptions.actor || "agent",
				name: "workspace.branchFromState",
				params: {
					stateId: normalizedOptions.stateId || null,
					branchLabel: normalizedOptions.branchLabel || null
				}
			});
		}
		dispose() {
			if (this.disposeWidgetsOnDispose) for (const widget of this.widgets) widget?.dispose?.();
			this.widgets = [];
			this.links = [];
		}
	};
	function createWidgetWorkspace(options = {}) {
		return new WidgetWorkspace(options);
	}
	//#endregion
	//#region ../../widgetva-kit/src/integrations/officialPages/observableD3Surface.js
	function normalizeRoot(root) {
		if (!root || typeof root !== "object") throw new Error("A browser-like root object is required.");
		return root;
	}
	function readDocument(root) {
		if (root?.document) return root.document;
		if (typeof root?.querySelectorAll === "function") return root;
		return null;
	}
	function asArray(value) {
		return Array.isArray(value) ? value : [];
	}
	function readNumber(value) {
		const numeric = Number(value);
		return Number.isFinite(numeric) ? numeric : null;
	}
	function readElements(doc, selector) {
		if (!doc?.querySelectorAll) return [];
		return [...doc.querySelectorAll(selector)];
	}
	function readBoundingBoxLike(node) {
		try {
			if (typeof node?.getBoundingClientRect === "function") {
				const rect = node.getBoundingClientRect();
				return {
					left: readNumber(rect?.left) || 0,
					top: readNumber(rect?.top) || 0,
					width: readNumber(rect?.width) || 0,
					height: readNumber(rect?.height) || 0
				};
			}
		} catch {}
		return {
			left: 0,
			top: 0,
			width: 0,
			height: 0
		};
	}
	function computeArea(node) {
		const rect = readBoundingBoxLike(node);
		return rect.width * rect.height;
	}
	function pickLargestNode(nodes = []) {
		return asArray(nodes).reduce((best, node) => {
			if (!best) return node;
			return computeArea(node) > computeArea(best) ? node : best;
		}, null);
	}
	function findPrimaryObservableD3Surface(root = globalThis.window) {
		const doc = readDocument(normalizeRoot(root));
		const svgs = readElements(doc, "svg");
		const canvases = readElements(doc, "canvas");
		const primarySvg = pickLargestNode(svgs);
		const primaryCanvas = pickLargestNode(canvases);
		if (!primarySvg && !primaryCanvas) return null;
		if (!primaryCanvas) return primarySvg;
		if (!primarySvg) return primaryCanvas;
		return computeArea(primarySvg) >= computeArea(primaryCanvas) ? primarySvg : primaryCanvas;
	}
	function isFiniteNumber(value) {
		return typeof value === "number" && Number.isFinite(value);
	}
	function collectScatterNumericFieldValues(rows = [], fieldName = "") {
		return (Array.isArray(rows) ? rows : []).map((row) => row?.[fieldName]).filter(isFiniteNumber);
	}
	function computePearsonCorrelation(left = [], right = []) {
		if (!Array.isArray(left) || !Array.isArray(right) || left.length !== right.length || left.length < 2) return null;
		const count = left.length;
		const leftMean = left.reduce((sum, value) => sum + value, 0) / count;
		const rightMean = right.reduce((sum, value) => sum + value, 0) / count;
		let numerator = 0;
		let leftVariance = 0;
		let rightVariance = 0;
		for (let index = 0; index < count; index += 1) {
			const leftDelta = left[index] - leftMean;
			const rightDelta = right[index] - rightMean;
			numerator += leftDelta * rightDelta;
			leftVariance += leftDelta * leftDelta;
			rightVariance += rightDelta * rightDelta;
		}
		if (leftVariance === 0 || rightVariance === 0) return null;
		return numerator / Math.sqrt(leftVariance * rightVariance);
	}
	function rankScatterFieldCorrelation(rows = [], screenField = "__screenX") {
		const normalizedRows = Array.isArray(rows) ? rows : [];
		if (normalizedRows.length < 2) return [];
		return Object.keys(normalizedRows[0] || {}).filter((fieldName) => fieldName !== "id" && !fieldName.startsWith("__")).filter((fieldName) => collectScatterNumericFieldValues(normalizedRows, fieldName).length >= 2).map((fieldName) => {
			const pairs = normalizedRows.map((row) => {
				const semanticValue = row?.[fieldName];
				const screenValue = row?.[screenField];
				return isFiniteNumber(semanticValue) && isFiniteNumber(screenValue) ? [semanticValue, screenValue] : null;
			}).filter(Boolean);
			if (pairs.length < 2) return null;
			const correlation = computePearsonCorrelation(pairs.map(([semanticValue]) => semanticValue), pairs.map(([, screenValue]) => screenValue));
			if (correlation == null) return null;
			return {
				field: fieldName,
				score: Math.abs(correlation),
				correlation
			};
		}).filter(Boolean).sort((left, right) => right.score - left.score);
	}
	function inferObservableD3ScatterFieldBindings(rows = []) {
		const xCandidates = rankScatterFieldCorrelation(rows, "__screenX");
		const yCandidates = rankScatterFieldCorrelation(rows, "__screenY");
		const xField = xCandidates[0]?.field || null;
		let yField = yCandidates[0]?.field || null;
		if (xField && yField === xField) yField = yCandidates.find((candidate) => candidate.field !== xField)?.field || yField;
		return {
			xField,
			yField
		};
	}
	function isDateLikeLineValue(value) {
		if (value == null || value === "") return false;
		if (value instanceof Date && Number.isFinite(value.getTime())) return true;
		if (typeof value === "number") return false;
		const parsed = Date.parse(String(value));
		return Number.isFinite(parsed);
	}
	function inferObservableD3LineBindings(rows = []) {
		const normalizedRows = Array.isArray(rows) ? rows : [];
		const fieldNames = [...new Set(normalizedRows.flatMap((row) => Object.keys(row || {})))].filter((fieldName) => typeof fieldName === "string" && fieldName !== "id" && fieldName !== "series" && fieldName !== "xValue" && !fieldName.startsWith("__"));
		const summarizeField = (fieldName) => {
			const values = normalizedRows.map((row) => row?.[fieldName]).filter((value) => value != null && value !== "");
			const numericValues = values.map((value) => typeof value === "number" ? value : Number(value)).filter((value) => Number.isFinite(value));
			const distinctValues = new Set(values.map((value) => String(value)));
			const lowerName = fieldName.toLowerCase();
			const dateLikeCount = values.filter((value) => isDateLikeLineValue(value)).length;
			return {
				fieldName,
				values,
				numericValues,
				distinctCount: distinctValues.size,
				allNumeric: values.length > 0 && numericValues.length === values.length,
				dateLikeCount,
				isTemporalName: /(date|time|year|month|day|week|quarter)/.test(lowerName),
				isSeriesLikeName: /(series|group|symbol|category|name|label)/.test(lowerName),
				isValueLikeName: /(value|count|amount|price|rate|total|close|open|high|low|volume|index)/.test(lowerName)
			};
		};
		const fieldStats = fieldNames.map(summarizeField);
		const xField = fieldStats.map((stats) => ({
			...stats,
			score: (stats.dateLikeCount > 0 ? 8 : 0) + (stats.isTemporalName ? 6 : 0) + (stats.distinctCount > 1 ? 2 : 0) + (!stats.isSeriesLikeName ? 1 : 0)
		})).sort((left, right) => right.score - left.score)[0]?.fieldName || null;
		const yField = fieldStats.filter((stats) => stats.fieldName !== xField).filter((stats) => stats.numericValues.length > 0).map((stats) => ({
			...stats,
			score: (stats.allNumeric ? 8 : 0) + (stats.isValueLikeName ? 5 : 0) + (stats.distinctCount > 1 ? 2 : 0)
		})).sort((left, right) => right.score - left.score)[0]?.fieldName || null;
		const xStats = fieldStats.find((stats) => stats.fieldName === xField) || null;
		return {
			xField,
			yField,
			xType: xStats?.dateLikeCount > 0 || xStats?.isTemporalName ? "temporal" : xStats?.allNumeric ? "quantitative" : "nominal"
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/integrations/officialPages/observableD3Materializers.js
	function clone$5(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	var OBSERVABLE_D3_WORKER_REQUEST = "widgetva:observable-d3-worker-request";
	var OBSERVABLE_D3_WORKER_RESPONSE = "widgetva:observable-d3-worker-response";
	var OBSERVABLE_D3_TOP_SOURCE = "widgetva-observable-d3-top";
	var OBSERVABLE_D3_WORKER_SOURCE = "widgetva-observable-d3-worker";
	function resolveObservableD3MessageTarget(host) {
		if (host?.contentWindow && typeof host.contentWindow.postMessage === "function") return host.contentWindow;
		if (host && typeof host.postMessage === "function") return host;
		return null;
	}
	async function waitForObservableD3RpcHost(root, { timeoutMs = 5e3, pollMs = 25 } = {}) {
		const startedAt = Date.now();
		while (Date.now() - startedAt <= timeoutMs) {
			const workerFrame = findObservableWorkerFrame(root);
			if (resolveObservableD3MessageTarget(workerFrame)) return workerFrame;
			if (findPrimaryObservableD3Surface(root) && resolveObservableD3MessageTarget(root)) return root;
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for an Observable D3 interaction host after ${timeoutMs}ms.`);
	}
	async function invokeObservableWorker(host, method, params = null, { timeoutMs = 5e3 } = {}) {
		const targetWindow = resolveObservableD3MessageTarget(host);
		if (!targetWindow) throw new Error("Observable D3 interaction host is not available for postMessage RPC.");
		const listenerRoot = globalThis.window;
		const requestId = `observable_d3_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
		return new Promise((resolve, reject) => {
			const timeout = setTimeout(() => {
				cleanup();
				reject(/* @__PURE__ */ new Error(`Timed out waiting for Observable worker RPC: ${method}.`));
			}, timeoutMs);
			const cleanup = () => {
				clearTimeout(timeout);
				listenerRoot?.removeEventListener?.("message", handleMessage);
			};
			const handleMessage = (event) => {
				const message = event?.data;
				if (!message || message.source !== OBSERVABLE_D3_WORKER_SOURCE || message.type !== OBSERVABLE_D3_WORKER_RESPONSE || message.id !== requestId) return;
				cleanup();
				if (message.ok === false) {
					reject(new Error(message?.error?.message || `Observable worker RPC failed: ${method}.`));
					return;
				}
				resolve(message.result || null);
			};
			listenerRoot?.addEventListener?.("message", handleMessage);
			targetWindow.postMessage({
				source: OBSERVABLE_D3_TOP_SOURCE,
				type: OBSERVABLE_D3_WORKER_REQUEST,
				id: requestId,
				method,
				params
			}, "*");
		});
	}
	async function waitForObservableWorkerScatterSurface(frame, { timeoutMs = 5e3, pollMs = 25, notebook = null, semanticHints = null } = {}) {
		const startedAt = Date.now();
		let lastSurface = null;
		let lastRowCount = 0;
		while (Date.now() - startedAt <= timeoutMs) {
			try {
				const surface = await invokeObservableWorker(frame, "describeSurface", { notebook }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const workerRows = await invokeObservableWorker(frame, "readScatterRows", { semanticHints }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const rows = Array.isArray(workerRows?.rows) ? workerRows.rows : [];
				lastSurface = surface || null;
				lastRowCount = rows.length;
				const inferredKind = typeof surface?.inferredKind === "string" ? surface.inferredKind : null;
				if ((inferredKind === "scatter" || !["bar", "line"].includes(inferredKind)) && rows.length > 0) return {
					surface,
					rows
				};
			} catch {}
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for a ready Observable D3 scatter surface after ${timeoutMs}ms (last inferred kind: ${lastSurface?.inferredKind || "unknown"}, last row count: ${lastRowCount}).`);
	}
	async function waitForObservableWorkerScatterMatrixSurface(frame, { timeoutMs = 5e3, pollMs = 25, notebook = null, semanticHints = null } = {}) {
		const startedAt = Date.now();
		let lastSurface = null;
		let lastCellCount = 0;
		while (Date.now() - startedAt <= timeoutMs) {
			try {
				const surface = await invokeObservableWorker(frame, "describeSurface", { notebook }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const matrix = await invokeObservableWorker(frame, "readScatterMatrix", { semanticHints }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const cells = Array.isArray(matrix?.cells) ? matrix.cells : [];
				lastSurface = surface || null;
				lastCellCount = cells.length;
				if (cells.length > 1) return {
					surface,
					matrix,
					cells
				};
			} catch {}
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for a ready Observable D3 scatterplot matrix after ${timeoutMs}ms (last inferred kind: ${lastSurface?.inferredKind || "unknown"}, last cell count: ${lastCellCount}).`);
	}
	async function waitForObservableWorkerBarSurface(frame, { timeoutMs = 5e3, pollMs = 25, notebook = null } = {}) {
		const startedAt = Date.now();
		let lastSurface = null;
		let lastRowCount = 0;
		while (Date.now() - startedAt <= timeoutMs) {
			try {
				const surface = await invokeObservableWorker(frame, "describeSurface", { notebook }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const workerRows = await invokeObservableWorker(frame, "readBarRows", null, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const rows = Array.isArray(workerRows?.rows) ? workerRows.rows : [];
				lastSurface = surface || null;
				lastRowCount = rows.length;
				const inferredKind = typeof surface?.inferredKind === "string" ? surface.inferredKind : null;
				if ((inferredKind === "bar" || !["line", "scatter"].includes(inferredKind)) && rows.length > 0) return {
					surface,
					rows
				};
			} catch {}
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for a ready Observable D3 bar surface after ${timeoutMs}ms (last inferred kind: ${lastSurface?.inferredKind || "unknown"}, last row count: ${lastRowCount}).`);
	}
	async function waitForObservableWorkerLineSurface(frame, { timeoutMs = 5e3, pollMs = 25, notebook = null } = {}) {
		const startedAt = Date.now();
		let lastSurface = null;
		let lastRowCount = 0;
		while (Date.now() - startedAt <= timeoutMs) {
			try {
				const surface = await invokeObservableWorker(frame, "describeSurface", { notebook }, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const workerRows = await invokeObservableWorker(frame, "readLineRows", null, { timeoutMs: Math.min(timeoutMs, 1e3) });
				const rows = Array.isArray(workerRows?.rows) ? workerRows.rows : [];
				lastSurface = surface || null;
				lastRowCount = rows.length;
				const inferredKind = typeof surface?.inferredKind === "string" ? surface.inferredKind : null;
				if ((inferredKind === "line" || !["bar", "scatter"].includes(inferredKind)) && rows.length > 0) return {
					surface,
					rows
				};
			} catch {}
			await new Promise((resolve) => setTimeout(resolve, pollMs));
		}
		throw new Error(`Timed out waiting for a ready Observable D3 line surface after ${timeoutMs}ms (last inferred kind: ${lastSurface?.inferredKind || "unknown"}, last row count: ${lastRowCount}).`);
	}
	function createObservableScatterSurfaceWrapper({ frame, currentSpecRef = null, nativeBrushBindings = [] } = {}) {
		let currentSelection = null;
		let currentViewport = null;
		let currentClusterAnnotation = null;
		let currentRegressionAnnotation = null;
		const nativeBrushBindingByTargetRef = new Map((Array.isArray(nativeBrushBindings) ? nativeBrushBindings : []).filter((binding) => typeof binding?.targetRef === "string" && typeof binding?.bindingId === "string").map((binding) => [binding.targetRef, binding]));
		function readSelectionDomains(selection = null) {
			if (!selection || typeof selection !== "object") return null;
			const xDomain = Array.isArray(selection?.domain?.xDomain) ? selection.domain.xDomain : Array.isArray(selection?.xDomain) ? selection.xDomain : Array.isArray(selection?.xRange) ? selection.xRange : Array.isArray(selection?.channels?.x?.domain) ? selection.channels.x.domain : null;
			const yDomain = Array.isArray(selection?.domain?.yDomain) ? selection.domain.yDomain : Array.isArray(selection?.yDomain) ? selection.yDomain : Array.isArray(selection?.yRange) ? selection.yRange : Array.isArray(selection?.channels?.y?.domain) ? selection.channels.y.domain : null;
			if (!Array.isArray(xDomain) || !Array.isArray(yDomain) || xDomain.length < 2 || yDomain.length < 2) return null;
			return {
				xDomain: clone$5(xDomain.slice(0, 2)),
				yDomain: clone$5(yDomain.slice(0, 2))
			};
		}
		function resolveNativeBrushBinding(selection = null) {
			const targetRef = typeof selection?.targetRef === "string" && selection.targetRef.length > 0 ? selection.targetRef : typeof selection?.sourceWidgetRef === "string" && selection.sourceWidgetRef.length > 0 ? selection.sourceWidgetRef : null;
			if (targetRef && nativeBrushBindingByTargetRef.has(targetRef)) return nativeBrushBindingByTargetRef.get(targetRef);
			const bindings = [...nativeBrushBindingByTargetRef.values()];
			return bindings.length === 1 ? bindings[0] : null;
		}
		async function applySelection(selection = null) {
			currentSelection = selection && typeof selection === "object" ? clone$5(selection) : null;
			const binding = resolveNativeBrushBinding(currentSelection);
			if (!currentSelection) {
				if (!binding) return void 0;
				return invokeObservableWorker(frame, "clearNativeBrush", {
					bindingId: binding.bindingId,
					targetRef: binding.targetRef
				});
			}
			const domains = readSelectionDomains(currentSelection);
			if (!binding || !domains) throw new Error("Observable D3 page does not expose a native scatter brush binding for this selection.");
			return invokeObservableWorker(frame, "applyNativeBrushRegion", {
				bindingId: binding.bindingId,
				targetRef: binding.targetRef,
				xDomain: domains.xDomain,
				yDomain: domains.yDomain
			});
		}
		async function applyViewport(viewport = null) {
			const normalizedViewport = viewport && typeof viewport === "object" ? {
				...Array.isArray(viewport.xDomain) ? { xDomain: clone$5(viewport.xDomain) } : {},
				...Array.isArray(viewport.yDomain) ? { yDomain: clone$5(viewport.yDomain) } : {}
			} : null;
			currentViewport = normalizedViewport && Object.keys(normalizedViewport).length > 0 ? normalizedViewport : null;
			await invokeObservableWorker(frame, "applyScatterViewport", { viewport: currentViewport });
		}
		async function applyClusterAnnotation(cluster = null) {
			currentClusterAnnotation = cluster && typeof cluster === "object" ? {
				...Number.isFinite(cluster.nClusters) ? { nClusters: Number(cluster.nClusters) } : {},
				...typeof cluster.clusterField === "string" ? { clusterField: cluster.clusterField } : {},
				...typeof cluster.method === "string" ? { method: cluster.method } : {}
			} : null;
			await invokeObservableWorker(frame, "applyScatterClusters", { cluster: currentClusterAnnotation });
		}
		async function applyRegressionAnnotation(regression = null) {
			currentRegressionAnnotation = regression && typeof regression === "object" ? { ...typeof regression.method === "string" ? { method: regression.method } : {} } : null;
			await invokeObservableWorker(frame, "applyScatterRegression", { regression: currentRegressionAnnotation });
		}
		function readClusterState(widgetState = {}) {
			const clusterState = widgetState?.rawSpec?._scatter_cluster_state || currentSpecRef?.current?._scatter_cluster_state;
			if (!clusterState || typeof clusterState !== "object") return null;
			return {
				nClusters: Number.isFinite(clusterState.n_clusters) ? Number(clusterState.n_clusters) : 3,
				clusterField: clusterState.cluster_field || null,
				method: clusterState.method || "kmeans"
			};
		}
		function readRegressionState(widgetState = {}) {
			const regressionState = widgetState?.rawSpec?._scatter_regression_state || currentSpecRef?.current?._scatter_regression_state;
			if (!regressionState || typeof regressionState !== "object") return null;
			return { method: regressionState.method || "linear" };
		}
		return {
			getState() {
				return {
					view: {
						...currentViewport ? clone$5(currentViewport) : {},
						...currentClusterAnnotation ? { cluster: clone$5(currentClusterAnnotation) } : {},
						...currentRegressionAnnotation ? { regression: clone$5(currentRegressionAnnotation) } : {}
					},
					selections: currentSelection ? { localBrush: clone$5(currentSelection) } : {}
				};
			},
			async renderFromState(widgetState = {}) {
				const nextViewport = widgetState?.view && typeof widgetState.view === "object" ? {
					...Array.isArray(widgetState.view.xDomain) ? { xDomain: widgetState.view.xDomain } : {},
					...Array.isArray(widgetState.view.yDomain) ? { yDomain: widgetState.view.yDomain } : {}
				} : null;
				if (currentViewport || nextViewport && Object.keys(nextViewport).length > 0) await applyViewport(nextViewport);
				const selection = Object.values(widgetState?.selections || {}).find((entry) => entry?.kind === "interval") || null;
				if (currentSelection || selection) await applySelection(selection);
				const clusterState = readClusterState(widgetState);
				if (currentClusterAnnotation || clusterState) await applyClusterAnnotation(clusterState);
				const regressionState = readRegressionState(widgetState);
				if (currentRegressionAnnotation || regressionState) await applyRegressionAnnotation(regressionState);
			},
			async setBrush(selection) {
				return applySelection(selection);
			},
			async setSelection(selection) {
				return applySelection(selection);
			},
			async setViewport(viewport) {
				return applyViewport(viewport);
			},
			async setClusterAnnotation(cluster) {
				return applyClusterAnnotation(cluster);
			},
			async setRegressionAnnotation(regression) {
				return applyRegressionAnnotation(regression);
			},
			getViewport() {
				return currentViewport ? clone$5(currentViewport) : null;
			},
			async readDebugSnapshot() {
				return invokeObservableWorker(frame, "readDebugSnapshot", null);
			}
		};
	}
	function createObservableScatterMatrixSurfaceWrapper({ frame, cellRefByWidgetRef = {}, nativeBrushBindings = [] } = {}) {
		let currentBrush = null;
		const nativeBrushBindingByTargetRef = new Map((Array.isArray(nativeBrushBindings) ? nativeBrushBindings : []).filter((binding) => typeof binding?.targetRef === "string" && typeof binding?.bindingId === "string").map((binding) => [binding.targetRef, binding]));
		function normalizeBrush(brush = null) {
			if (!brush || typeof brush !== "object") return null;
			const targetRef = typeof brush.targetRef === "string" && brush.targetRef.length > 0 ? brush.targetRef : null;
			const xDomain = Array.isArray(brush.xDomain) ? clone$5(brush.xDomain.slice(0, 2)) : Array.isArray(brush.xRange) ? clone$5(brush.xRange.slice(0, 2)) : null;
			const yDomain = Array.isArray(brush.yDomain) ? clone$5(brush.yDomain.slice(0, 2)) : Array.isArray(brush.yRange) ? clone$5(brush.yRange.slice(0, 2)) : null;
			if (!targetRef || !xDomain || !yDomain || xDomain.length < 2 || yDomain.length < 2) return null;
			return {
				targetRef,
				xDomain,
				yDomain
			};
		}
		function toPageBrush(brush = null) {
			if (!brush) return null;
			return {
				...brush,
				targetRef: cellRefByWidgetRef?.[brush.targetRef] || brush.targetRef
			};
		}
		function toNativeBrushCommand(pageBrush = null) {
			if (!pageBrush) return null;
			const binding = nativeBrushBindingByTargetRef.get(pageBrush.targetRef);
			if (!binding?.bindingId) throw new Error(`Observable D3 page does not expose a native brush binding for ${pageBrush.targetRef}.`);
			return {
				bindingId: binding.bindingId,
				targetRef: pageBrush.targetRef,
				xDomain: clone$5(pageBrush.xDomain),
				yDomain: clone$5(pageBrush.yDomain)
			};
		}
		async function applyBrush(brush = null) {
			const previousBrush = currentBrush;
			currentBrush = normalizeBrush(brush);
			const pageBrush = toPageBrush(currentBrush);
			if (!pageBrush) {
				const previousPageBrush = toPageBrush(previousBrush);
				if (!previousPageBrush) return void 0;
				const binding = nativeBrushBindingByTargetRef.get(previousPageBrush.targetRef);
				if (!binding?.bindingId) throw new Error(`Observable D3 page does not expose a native brush binding for ${previousPageBrush.targetRef}.`);
				return invokeObservableWorker(frame, "clearNativeBrush", {
					bindingId: binding.bindingId,
					targetRef: previousPageBrush.targetRef
				}, { timeoutMs: 15e3 });
			}
			return invokeObservableWorker(frame, "applyNativeBrushRegion", toNativeBrushCommand(pageBrush), { timeoutMs: 15e3 });
		}
		function readBrushFromState(widgetState = {}) {
			const selections = Object.values(widgetState?.selections || {});
			const selection = selections.find((entry) => entry?.kind === "interval" && typeof entry?.targetRef === "string") || selections.find((entry) => entry?.kind === "interval" && typeof entry?.sourceWidgetRef === "string") || null;
			if (!selection) return null;
			const xDomain = Array.isArray(selection?.domain?.xDomain) ? selection.domain.xDomain : Array.isArray(selection?.xDomain) ? selection.xDomain : Array.isArray(selection?.channels?.x?.domain) ? selection.channels.x.domain : null;
			const yDomain = Array.isArray(selection?.domain?.yDomain) ? selection.domain.yDomain : Array.isArray(selection?.yDomain) ? selection.yDomain : Array.isArray(selection?.channels?.y?.domain) ? selection.channels.y.domain : null;
			return normalizeBrush({
				targetRef: selection.targetRef || selection.sourceWidgetRef || widgetState?.ref || null,
				xDomain,
				yDomain
			});
		}
		return {
			getState() {
				return {
					view: {},
					selections: currentBrush ? { matrixBrush: {
						kind: "interval",
						targetRef: currentBrush.targetRef,
						domain: {
							xDomain: clone$5(currentBrush.xDomain),
							yDomain: clone$5(currentBrush.yDomain)
						}
					} } : {}
				};
			},
			async renderFromState(widgetState = {}) {
				const nextBrush = readBrushFromState(widgetState);
				if (nextBrush) return applyBrush(nextBrush);
				if (currentBrush?.targetRef && currentBrush.targetRef === widgetState?.ref) return applyBrush(null);
			},
			async setBrush(brush) {
				return applyBrush(brush);
			},
			async setSelection(selection) {
				return applyBrush(selection);
			},
			async clearBrush() {
				return applyBrush(null);
			},
			readRecoverableState() {
				return { brush: currentBrush ? clone$5(currentBrush) : null };
			},
			async restoreRecoverableState(state = {}) {
				return applyBrush(state?.brush || null);
			},
			async readDebugSnapshot() {
				return invokeObservableWorker(frame, "readDebugSnapshot", null);
			}
		};
	}
	function createObservableBarSurfaceWrapper({ frame, currentSpecRef = null }) {
		let currentSelection = null;
		let currentFilter = null;
		let currentSort = null;
		let currentHighlight = null;
		function normalizeSelection(selection = null) {
			if (!selection || typeof selection !== "object") return null;
			const values = Array.isArray(selection.values) ? selection.values : Array.isArray(selection.categories) ? selection.categories : selection.value != null ? [selection.value] : [];
			if (values.length === 0) return null;
			const field = typeof selection.field === "string" ? selection.field : "category";
			const normalizedSelection = {
				...clone$5(selection),
				kind: typeof selection.kind === "string" ? selection.kind : "category",
				field,
				values: clone$5(values)
			};
			if (typeof selection.summary === "string") normalizedSelection.summary = selection.summary;
			if (Array.isArray(selection.predicates)) normalizedSelection.predicates = clone$5(selection.predicates);
			return normalizedSelection;
		}
		async function applySelection(selection = null) {
			currentSelection = normalizeSelection(selection);
			await invokeObservableWorker(frame, "applyBarSelection", { selection: currentSelection });
		}
		async function applyFilter(filter = null) {
			const normalizedFilter = filter && typeof filter === "object" ? {
				field: typeof filter.field === "string" ? filter.field : "category",
				categories: Array.isArray(filter.categories) ? clone$5(filter.categories) : []
			} : null;
			currentFilter = normalizedFilter && currentFilter?.categories?.length !== 0 ? normalizedFilter : normalizedFilter;
			await invokeObservableWorker(frame, "applyBarFilter", { filter: currentFilter });
		}
		async function applySort(sort = null) {
			const normalizedSort = sort && typeof sort === "object" ? {
				...typeof sort.channel === "string" ? { channel: sort.channel } : {},
				...typeof sort.field === "string" ? { field: sort.field } : {},
				...typeof sort.mode === "string" ? { mode: sort.mode } : {},
				...typeof sort.order === "string" ? { order: sort.order } : {},
				...Array.isArray(sort.values) ? { values: clone$5(sort.values) } : {}
			} : null;
			currentSort = normalizedSort && Array.isArray(normalizedSort.values) && normalizedSort.values.length > 0 ? normalizedSort : null;
			await invokeObservableWorker(frame, "applyBarSort", { sort: currentSort });
		}
		async function applyHighlight(highlight = null) {
			const normalizedHighlight = highlight && typeof highlight === "object" ? {
				...typeof highlight.field === "string" ? { field: highlight.field } : {},
				...Array.isArray(highlight.categories) ? { categories: clone$5(highlight.categories) } : {},
				...Number.isFinite(highlight.n) ? { n: Number(highlight.n) } : {},
				...typeof highlight.order === "string" ? { order: highlight.order } : {}
			} : null;
			currentHighlight = normalizedHighlight && Array.isArray(normalizedHighlight.categories) && normalizedHighlight.categories.length > 0 ? normalizedHighlight : null;
			await invokeObservableWorker(frame, "applyBarHighlight", { highlight: currentHighlight });
		}
		function readFilterFromSpec() {
			const categoricalFilter = (Array.isArray(currentSpecRef?.current?.transform) ? currentSpecRef.current.transform : []).find((transform) => transform?.filter?.field && Array.isArray(transform?.filter?.oneOf));
			if (!categoricalFilter) return null;
			return {
				field: categoricalFilter.filter.field,
				categories: clone$5(categoricalFilter.filter.oneOf)
			};
		}
		function readFilterFromWidgetState(widgetState = {}) {
			const rawSpecFilter = (Array.isArray(widgetState?.rawSpec?.transform) ? widgetState.rawSpec.transform : []).find((transform) => transform?.filter?.field && Array.isArray(transform?.filter?.oneOf));
			if (rawSpecFilter) return {
				field: rawSpecFilter.filter.field,
				categories: clone$5(rawSpecFilter.filter.oneOf)
			};
			const runtimeFilter = (Array.isArray(widgetState?.transforms) ? widgetState.transforms : []).find((transform) => transform?.spec?.filter?.field && Array.isArray(transform?.spec?.filter?.oneOf));
			if (runtimeFilter) return {
				field: runtimeFilter.spec.filter.field,
				categories: clone$5(runtimeFilter.spec.filter.oneOf)
			};
			return null;
		}
		function readSortFromWidgetState(widgetState = {}) {
			const viewSort = widgetState?.view?.sort;
			if (viewSort && typeof viewSort === "object" && Array.isArray(viewSort.values) && viewSort.values.length > 0) return clone$5(viewSort);
			const rawSpecEncoding = widgetState?.rawSpec?.encoding || {};
			for (const channel of ["x", "y"]) if (Array.isArray(rawSpecEncoding?.[channel]?.sort) && rawSpecEncoding[channel].sort.length > 0) return {
				channel,
				field: rawSpecEncoding?.[channel]?.field || null,
				mode: "explicitOrder",
				values: clone$5(rawSpecEncoding[channel].sort)
			};
			return null;
		}
		function readHighlightFromWidgetState(widgetState = {}) {
			const highlightState = (widgetState?.rawSpec || currentSpecRef?.current || {})?._bar_highlight_state;
			if (!highlightState || typeof highlightState !== "object") return null;
			return {
				categories: Array.isArray(highlightState.categories) ? clone$5(highlightState.categories) : [],
				field: highlightState.category_field || "category",
				...Number.isFinite(highlightState.n) ? { n: Number(highlightState.n) } : {},
				...typeof highlightState.order === "string" ? { order: highlightState.order } : {}
			};
		}
		return {
			getState() {
				return {
					view: {
						...currentSort ? { sort: clone$5(currentSort) } : {},
						...currentHighlight ? { highlight: clone$5(currentHighlight) } : {}
					},
					selections: currentSelection ? { localSelection: clone$5(currentSelection) } : {}
				};
			},
			async renderFromState(widgetState = {}) {
				const selection = Object.values(widgetState?.selections || {}).find((entry) => Array.isArray(entry?.values)) || null;
				if (selection) await applySelection(selection);
				await applyFilter(readFilterFromWidgetState(widgetState) || readFilterFromSpec());
				await applySort(readSortFromWidgetState(widgetState));
				await applyHighlight(readHighlightFromWidgetState(widgetState));
			},
			async setSelection(selection) {
				return applySelection(selection);
			},
			async setFilter(filter) {
				return applyFilter(filter);
			},
			async setHighlight(highlight) {
				return applyHighlight(highlight);
			},
			async syncCurrentSpec() {
				return applyFilter(readFilterFromSpec());
			},
			async readDebugSnapshot() {
				return invokeObservableWorker(frame, "readDebugSnapshot", null);
			}
		};
	}
	function createObservableLineSurfaceWrapper({ frame, currentSpecRef = null }) {
		let currentSelection = null;
		let currentFocus = null;
		let currentTrend = null;
		let currentMovingAverage = null;
		let currentDrilldown = null;
		let currentResample = null;
		let currentViewport = null;
		let currentBold = null;
		let currentFilter = null;
		async function applySelection(selection = null) {
			currentSelection = selection && typeof selection === "object" ? clone$5(selection) : null;
			await invokeObservableWorker(frame, "applyLineSelection", { selection: currentSelection });
		}
		async function applyFocus(focus = null) {
			const normalizedFocus = focus && typeof focus === "object" ? {
				...Array.isArray(focus.lines) ? { lines: clone$5(focus.lines) } : {},
				...typeof focus.lineField === "string" ? { lineField: focus.lineField } : {},
				...Number.isFinite(focus.dimOpacity) ? { dimOpacity: Number(focus.dimOpacity) } : {}
			} : null;
			currentFocus = normalizedFocus && Array.isArray(normalizedFocus.lines) && normalizedFocus.lines.length > 0 ? normalizedFocus : null;
			await invokeObservableWorker(frame, "applyLineFocus", { focus: currentFocus });
		}
		async function applyTrend(trend = null) {
			currentTrend = trend && typeof trend === "object" ? { ...typeof trend.trendType === "string" ? { trendType: trend.trendType } : {} } : null;
			await invokeObservableWorker(frame, "applyLineTrend", { trend: currentTrend });
		}
		async function applyMovingAverage(movingAverage = null) {
			currentMovingAverage = movingAverage && typeof movingAverage === "object" ? { ...Number.isFinite(movingAverage.windowSize) ? { windowSize: Number(movingAverage.windowSize) } : {} } : null;
			await invokeObservableWorker(frame, "applyLineMovingAverage", { movingAverage: currentMovingAverage });
		}
		async function applyDrilldown(drilldown = null) {
			const normalizedDrilldown = drilldown && typeof drilldown === "object" ? {
				...typeof drilldown.level === "string" ? { level: drilldown.level } : {},
				...Number.isFinite(drilldown.value) ? { value: Number(drilldown.value) } : {},
				...drilldown.parent && typeof drilldown.parent === "object" && !Array.isArray(drilldown.parent) ? { parent: clone$5(drilldown.parent) } : {},
				...typeof drilldown.title === "string" ? { title: drilldown.title } : {}
			} : null;
			currentDrilldown = normalizedDrilldown && typeof normalizedDrilldown.level === "string" ? normalizedDrilldown : null;
			await invokeObservableWorker(frame, "applyLineDrilldown", { drilldown: currentDrilldown });
		}
		async function applyResample(resample = null) {
			const normalizedResample = resample && typeof resample === "object" ? {
				...typeof resample.granularity === "string" ? { granularity: resample.granularity } : {},
				...typeof resample.agg === "string" ? { agg: resample.agg } : {},
				...typeof resample.xField === "string" ? { xField: resample.xField } : {},
				...typeof resample.yField === "string" ? { yField: resample.yField } : {}
			} : null;
			currentResample = normalizedResample && typeof normalizedResample.granularity === "string" ? normalizedResample : null;
			await invokeObservableWorker(frame, "applyLineResample", { resample: currentResample });
		}
		async function applyViewport(viewport = null) {
			const normalizedViewport = viewport && typeof viewport === "object" ? { ...Array.isArray(viewport.xDomain) ? { xDomain: clone$5(viewport.xDomain) } : {} } : null;
			currentViewport = normalizedViewport && Array.isArray(normalizedViewport.xDomain) ? normalizedViewport : null;
			await invokeObservableWorker(frame, "applyLineViewport", { viewport: currentViewport });
		}
		async function applyBold(bold = null) {
			const normalizedBold = bold && typeof bold === "object" ? {
				...Array.isArray(bold.lineNames) ? { lineNames: clone$5(bold.lineNames) } : {},
				...typeof bold.lineField === "string" ? { lineField: bold.lineField } : {},
				...Number.isFinite(bold.boldWidth) ? { boldWidth: Number(bold.boldWidth) } : {},
				...Number.isFinite(bold.baseWidth) ? { baseWidth: Number(bold.baseWidth) } : {}
			} : null;
			currentBold = normalizedBold && Array.isArray(normalizedBold.lineNames) && normalizedBold.lineNames.length > 0 ? normalizedBold : null;
			await invokeObservableWorker(frame, "applyLineBold", { bold: currentBold });
		}
		async function applyFilter(filter = null) {
			const normalizedFilter = filter && typeof filter === "object" ? {
				...typeof filter.lineField === "string" ? { lineField: filter.lineField } : {},
				...Array.isArray(filter.linesToRemove) ? { linesToRemove: clone$5(filter.linesToRemove) } : {}
			} : null;
			currentFilter = normalizedFilter && Array.isArray(normalizedFilter.linesToRemove) && normalizedFilter.linesToRemove.length > 0 ? normalizedFilter : null;
			await invokeObservableWorker(frame, "applyLineFilter", { filter: currentFilter });
		}
		function readFocusState(widgetState = {}) {
			const rawSpec = widgetState?.rawSpec || currentSpecRef?.current || {};
			const focusState = rawSpec?._line_focus_state;
			if (!focusState || typeof focusState !== "object") return null;
			const opacityCondition = rawSpec?.encoding?.opacity?.condition;
			const dimOpacity = Number(rawSpec?.encoding?.opacity?.value);
			return {
				lines: Array.isArray(focusState.lines) ? clone$5(focusState.lines) : [],
				lineField: focusState.line_field || "series",
				...Number.isFinite(dimOpacity) ? { dimOpacity } : {},
				...typeof opacityCondition?.test === "string" ? { test: opacityCondition.test } : {}
			};
		}
		function readTrendState(widgetState = {}) {
			const trendState = (widgetState?.rawSpec || currentSpecRef?.current || {})?._line_trend_state;
			if (!trendState || typeof trendState !== "object") return null;
			return { trendType: trendState.trendType || "regression" };
		}
		function readMovingAverageState(widgetState = {}) {
			const movingAverageState = (widgetState?.rawSpec || currentSpecRef?.current || {})?._line_moving_average_state;
			if (!movingAverageState || typeof movingAverageState !== "object") return null;
			return { windowSize: Number.isFinite(movingAverageState.windowSize) ? Number(movingAverageState.windowSize) : 3 };
		}
		function readDrilldownState(widgetState = {}) {
			const rawSpec = widgetState?.rawSpec || currentSpecRef?.current || {};
			const drillState = rawSpec?._line_drilldown_state;
			if (!drillState || typeof drillState !== "object") return null;
			const parent = drillState.parent && typeof drillState.parent === "object" && !Array.isArray(drillState.parent) ? clone$5(drillState.parent) : {};
			if (Number.isFinite(parent.month)) return {
				level: "month",
				value: Number(parent.month),
				parent: Number.isFinite(parent.year) ? { year: Number(parent.year) } : {},
				title: rawSpec?.title || ""
			};
			if (Number.isFinite(parent.year)) return {
				level: "year",
				value: Number(parent.year),
				parent: {},
				title: rawSpec?.title || ""
			};
			return {
				level: "drilldown",
				parent,
				title: rawSpec?.title || ""
			};
		}
		function readViewportState(widgetState = {}) {
			const xDomain = (widgetState?.rawSpec || currentSpecRef?.current || {})?.encoding?.x?.scale?.domain;
			if (!Array.isArray(xDomain) || xDomain.length !== 2) return null;
			return { xDomain: clone$5(xDomain) };
		}
		function readResampleState(widgetState = {}) {
			const rawSpec = widgetState?.rawSpec || currentSpecRef?.current || {};
			const resampleState = rawSpec?._resample_state;
			if (!resampleState || typeof resampleState !== "object") return null;
			const bindings = inferObservableD3LineBindings(Array.isArray(rawSpec?.data?.values) ? rawSpec.data.values : []);
			return {
				...typeof resampleState.current_granularity === "string" ? { granularity: resampleState.current_granularity } : {},
				...typeof resampleState.current_agg === "string" ? { agg: resampleState.current_agg } : {},
				...typeof bindings?.xField === "string" ? { xField: bindings.xField } : {},
				...typeof bindings?.yField === "string" ? { yField: bindings.yField } : {}
			};
		}
		function readBoldState(widgetState = {}) {
			const boldState = (widgetState?.rawSpec || currentSpecRef?.current || {})?._line_bold_state;
			if (!boldState || typeof boldState !== "object") return null;
			return {
				lineNames: Array.isArray(boldState.line_names) ? clone$5(boldState.line_names) : [],
				lineField: boldState.line_field || "series",
				...Number.isFinite(boldState.bold_width) ? { boldWidth: Number(boldState.bold_width) } : {},
				...Number.isFinite(boldState.base_width) ? { baseWidth: Number(boldState.base_width) } : {}
			};
		}
		function readFilterState(widgetState = {}) {
			const filterState = (widgetState?.rawSpec || currentSpecRef?.current || {})?._line_filter_state;
			if (!filterState || typeof filterState !== "object") return null;
			const field = filterState.lineField || filterState.line_field || null;
			const linesToRemove = Array.isArray(filterState.linesToRemove) ? clone$5(filterState.linesToRemove) : Array.isArray(filterState.lines_to_remove) ? clone$5(filterState.lines_to_remove) : [];
			if (!field || linesToRemove.length === 0) return null;
			return {
				lineField: field,
				linesToRemove
			};
		}
		return {
			getState() {
				return {
					view: {
						...currentViewport ? clone$5(currentViewport) : {},
						...currentFocus ? { focus: clone$5(currentFocus) } : {},
						...currentBold ? { bold: clone$5(currentBold) } : {},
						...currentFilter ? { filter: clone$5(currentFilter) } : {},
						...currentTrend ? { trend: clone$5(currentTrend) } : {},
						...currentMovingAverage ? { movingAverage: clone$5(currentMovingAverage) } : {},
						...currentDrilldown ? { drilldown: clone$5(currentDrilldown) } : {},
						...currentResample ? { resample: clone$5(currentResample) } : {}
					},
					selections: currentSelection ? { localSelection: clone$5(currentSelection) } : {}
				};
			},
			async renderFromState(widgetState = {}) {
				await applySelection(Object.values(widgetState?.selections || {}).find((entry) => Array.isArray(entry?.values)) || null);
				await applyViewport(readViewportState(widgetState));
				await applyFocus(readFocusState(widgetState));
				await applyBold(readBoldState(widgetState));
				await applyFilter(readFilterState(widgetState));
				await applyTrend(readTrendState(widgetState));
				await applyMovingAverage(readMovingAverageState(widgetState));
				await applyDrilldown(readDrilldownState(widgetState));
				await applyResample(readResampleState(widgetState));
			},
			async setSelection(selection) {
				return applySelection(selection);
			},
			async setFocus(focus) {
				return applyFocus(focus);
			},
			async setBold(bold) {
				return applyBold(bold);
			},
			async setFilter(filter) {
				return applyFilter(filter);
			},
			async setTrend(trend) {
				return applyTrend(trend);
			},
			async setMovingAverage(movingAverage) {
				return applyMovingAverage(movingAverage);
			},
			async setDrilldown(drilldown) {
				return applyDrilldown(drilldown);
			},
			async setResample(resample) {
				return applyResample(resample);
			},
			async setViewport(viewport) {
				return applyViewport(viewport);
			},
			async readDebugSnapshot() {
				return invokeObservableWorker(frame, "readDebugSnapshot", null);
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/integrations/officialPages/observableD3NativeContract.js
	function clone$4(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function readNonEmptyString(value) {
		return typeof value === "string" && value.trim().length > 0 ? value.trim() : null;
	}
	function normalizeBrushBinding(binding = null) {
		const bindingId = readNonEmptyString(binding?.bindingId);
		const targetRef = readNonEmptyString(binding?.targetRef);
		if (!bindingId || !targetRef) return null;
		return {
			bindingId,
			targetRef,
			kind: readNonEmptyString(binding?.kind) || "scatter",
			interaction: "brush",
			fields: binding?.fields && typeof binding.fields === "object" && !Array.isArray(binding.fields) ? clone$4(binding.fields) : {},
			domain: binding?.domain && typeof binding.domain === "object" && !Array.isArray(binding.domain) ? clone$4(binding.domain) : {}
		};
	}
	function readObservableD3NativeBrushBindings(captureSnapshot = {}) {
		return (Array.isArray(captureSnapshot?.brushBindings) ? captureSnapshot.brushBindings : []).map(normalizeBrushBinding).filter(Boolean);
	}
	function buildObservableD3NativeContract(captureSnapshot = {}) {
		const brushBindings = readObservableD3NativeBrushBindings(captureSnapshot);
		return {
			provider: "d3",
			actions: brushBindings.map((binding) => ({
				name: "scatter.brushRegion",
				targetRef: binding.targetRef,
				native: {
					provider: "d3",
					kind: "brush",
					bindingId: binding.bindingId
				},
				paramsSchema: {
					type: "object",
					required: [
						"xField",
						"yField",
						"xRange",
						"yRange"
					],
					properties: {
						xField: { type: "string" },
						yField: { type: "string" },
						xRange: {
							type: "array",
							items: { type: "number" },
							minItems: 2,
							maxItems: 2
						},
						yRange: {
							type: "array",
							items: { type: "number" },
							minItems: 2,
							maxItems: 2
						}
					}
				}
			})),
			perceptions: [
				{ name: "perception.inspectViewConfig" },
				{ name: "perception.summarizeSelection" },
				{ name: "perception.summarizeVisible" }
			],
			native: { brushBindings }
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/integrations/officialPages/observableD3Examples.js
	function clone$3(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function createD3FamilyAdapter(kind) {
		return createD3WidgetAdapter({ kind: getWidgetFamily(kind).kind });
	}
	function buildObservableScatterSpec(rows = []) {
		const fieldBindings = inferObservableD3ScatterFieldBindings(rows);
		const xField = fieldBindings?.xField || "__screenX";
		const yField = fieldBindings?.yField || "__screenY";
		return {
			data: { values: rows },
			mark: "point",
			encoding: {
				x: {
					field: xField,
					type: "quantitative"
				},
				y: {
					field: yField,
					type: "quantitative"
				}
			}
		};
	}
	function normalizeIdToken(value, fallback = "field") {
		return String(value || "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "_").replace(/^_+|_+$/g, "") || fallback;
	}
	function buildObservableScatterMatrixCellWidgetId(cell = {}, index = 0) {
		return `cell_${normalizeIdToken(cell.xField, `x_${index + 1}`)}_${normalizeIdToken(cell.yField, `y_${index + 1}`)}`;
	}
	function buildObservableScatterMatrixCellSpec(cell = {}, rows = []) {
		return {
			data: { values: rows },
			mark: "point",
			encoding: {
				x: {
					field: cell.xField || "__screenX",
					type: "quantitative"
				},
				y: {
					field: cell.yField || "__screenY",
					type: "quantitative"
				}
			}
		};
	}
	function readNumericExtent(rows = [], fieldName = null) {
		if (!fieldName) return null;
		const values = (Array.isArray(rows) ? rows : []).map((row) => row?.[fieldName]).filter((value) => typeof value === "number" && Number.isFinite(value));
		if (values.length === 0) return null;
		return [Math.min(...values), Math.max(...values)];
	}
	function buildObservableScatterMatrixWorkspaceSpec({ cells = [], rows = [], nativeContract = null } = {}) {
		const nativeActionsByTargetRef = /* @__PURE__ */ new Map();
		for (const action of Array.isArray(nativeContract?.actions) ? nativeContract.actions : []) {
			if (typeof action?.targetRef !== "string" || typeof action?.name !== "string") continue;
			const names = nativeActionsByTargetRef.get(action.targetRef) || [];
			names.push(action.name);
			nativeActionsByTargetRef.set(action.targetRef, names);
		}
		const widgets = cells.map((cell, index) => {
			const nativeActionNames = nativeActionsByTargetRef.get(cell.ref) || [];
			const exposedActionNames = nativeActionNames.length > 0 ? [...new Set([...nativeActionNames, "widget.clearSelection"])] : [];
			return {
				widgetId: buildObservableScatterMatrixCellWidgetId(cell, index),
				role: index === 0 ? "primary" : "linkedScatter",
				kind: "scatter",
				provider: "d3",
				title: `${cell.yField || "y"} vs ${cell.xField || "x"}`,
				description: "Observable D3 scatterplot matrix cell.",
				exposedActionNames,
				exposedPerceptionNames: [
					"perception.inspectViewConfig",
					"perception.summarizeSelection",
					"perception.summarizeVisible"
				],
				source: {
					kind: "templateSpec",
					spec: buildObservableScatterMatrixCellSpec(cell, rows)
				},
				usageNotes: exposedActionNames.includes("scatter.brushRegion") ? ["This widget is one cell in the Observable D3 scatterplot matrix.", "Use scatter.brushRegion with this widget ref to drive the page native D3 brush for this cell."] : ["This widget is one cell in the Observable D3 scatterplot matrix.", "This official page cell did not expose a captured native D3 brush action."]
			};
		});
		const links = [];
		for (const source of widgets) for (const target of widgets) {
			if (source.widgetId === target.widgetId) continue;
			links.push({
				linkId: `${source.widgetId}_shares_selection_${target.widgetId}`,
				kind: "sharesSelection",
				sourceWidgetId: source.widgetId,
				targetWidgetId: target.widgetId,
				activationPolicy: "automatic",
				description: `${source.title} shares selected rows with ${target.title}.`
			});
		}
		return {
			topology: "T3",
			widgets,
			links
		};
	}
	async function readObservableD3NativeContract(rpcHost, { timeoutMs = 5e3 } = {}) {
		try {
			return buildObservableD3NativeContract(await invokeObservableWorker(rpcHost, "readNativeCapture", null, { timeoutMs: Math.min(timeoutMs, 1e3) }) || {});
		} catch {
			return buildObservableD3NativeContract({ brushBindings: [] });
		}
	}
	function buildObservableScatterMatrixAgentCells(cellModels = []) {
		return cellModels.map((model) => {
			const rows = Array.isArray(model.cell?.rows) ? model.cell.rows : Array.isArray(model.spec?.data?.values) ? model.spec.data.values : [];
			const xField = model.cell?.xField || null;
			const yField = model.cell?.yField || null;
			return {
				ref: model.widgetRef,
				xField,
				yField,
				xDomain: readNumericExtent(rows, xField),
				yDomain: readNumericExtent(rows, yField)
			};
		});
	}
	function summarizeObservableScatterMatrix({ cells = [], brush = null } = {}) {
		return `Observable D3 scatterplot matrix with ${Array.isArray(cells) ? cells.length : 0} targetable scatter cells.${brush?.targetRef ? ` Current brush targets ${brush.targetRef}.` : " No brush is currently active."}`;
	}
	function buildObservableScatterMatrixObservationMeta({ cells = [], brush = null } = {}) {
		return {
			provider: "d3",
			kind: "scatterMatrix",
			cellCount: Array.isArray(cells) ? cells.length : 0,
			cells: clone$3(cells),
			currentBrush: brush ? clone$3(brush) : null,
			summary: summarizeObservableScatterMatrix({
				cells,
				brush
			})
		};
	}
	function normalizeAllowedActionNamesByWidgetRef(allowedActionNamesByWidgetRef = {}) {
		const entries = allowedActionNamesByWidgetRef instanceof Map ? [...allowedActionNamesByWidgetRef.entries()] : Object.entries(allowedActionNamesByWidgetRef || {});
		return new Map(entries.map(([widgetRef, names]) => [widgetRef, new Set((Array.isArray(names) ? names : []).filter((name) => typeof name === "string" && name.length > 0))]));
	}
	function filterObservableD3WidgetActionNames(widget = {}, allowedByWidgetRef = /* @__PURE__ */ new Map()) {
		const allowedNames = allowedByWidgetRef.get(widget?.ref) || /* @__PURE__ */ new Set();
		return {
			...widget || {},
			actionNames: (Array.isArray(widget?.actionNames) ? widget.actionNames : []).filter((name) => allowedNames.has(name))
		};
	}
	function filterObservableD3WorkspaceDescription(description = {}, allowedByWidgetRef = /* @__PURE__ */ new Map()) {
		const allowedNames = new Set([...allowedByWidgetRef.values()].flatMap((names) => [...names]));
		return {
			...description || {},
			widgets: (Array.isArray(description?.widgets) ? description.widgets : []).map((widget) => filterObservableD3WidgetActionNames(widget, allowedByWidgetRef)),
			actions: (Array.isArray(description?.actions) ? description.actions : []).filter((action) => allowedNames.has(action?.name))
		};
	}
	function assertObservableD3ActionAllowed(call = {}, allowedByWidgetRef = /* @__PURE__ */ new Map()) {
		const actionName = typeof call?.name === "string" ? call.name : null;
		if (!actionName) return;
		const targetRef = call?.target?.widgetRef || call?.queryScope?.widgetRef || null;
		if (targetRef && allowedByWidgetRef.get(targetRef)?.has(actionName)) return;
		if (!targetRef && [...allowedByWidgetRef.values()].some((names) => names.has(actionName))) return;
		throw new Error(`Unsupported Observable D3 official-page action: ${actionName}.`);
	}
	function createObservableD3ActionFilteredWorkspaceApi(workspaceApi, { allowedActionNamesByWidgetRef = {} } = {}) {
		if (!workspaceApi || typeof workspaceApi !== "object") return workspaceApi;
		const allowedByWidgetRef = normalizeAllowedActionNamesByWidgetRef(allowedActionNamesByWidgetRef);
		return new Proxy(workspaceApi, { get(target, prop, receiver) {
			if (prop === "describeWorkspace" || prop === "describe") {
				const original = Reflect.get(target, prop, receiver);
				if (typeof original !== "function") return original;
				return (...args) => filterObservableD3WorkspaceDescription(original.apply(target, args), allowedByWidgetRef);
			}
			if (prop === "readObservation") {
				const original = Reflect.get(target, prop, receiver);
				if (typeof original !== "function") return original;
				return (...args) => {
					const observation = original.apply(target, args);
					return {
						...observation || {},
						state: {
							...observation?.state || {},
							widgets: Array.isArray(observation?.state?.widgets) ? observation.state.widgets.map((widget) => filterObservableD3WidgetActionNames(widget, allowedByWidgetRef)) : observation?.state?.widgets
						}
					};
				};
			}
			if (prop === "executeAction" || prop === "executeActionAndCommitCoordination") {
				const original = Reflect.get(target, prop, receiver);
				if (typeof original !== "function") return original;
				return async (call, ...args) => {
					assertObservableD3ActionAllowed(call, allowedByWidgetRef);
					return original.call(target, call, ...args);
				};
			}
			const value = Reflect.get(target, prop, receiver);
			return typeof value === "function" ? value.bind(target) : value;
		} });
	}
	function createObservableScatterMatrixWorkspaceApi(workspaceApi, { cells = [], readBrush = null, allowedActionNamesByWidgetRef = {} } = {}) {
		if (!workspaceApi || typeof workspaceApi !== "object") return workspaceApi;
		const allowedByWidgetRef = normalizeAllowedActionNamesByWidgetRef(allowedActionNamesByWidgetRef);
		function readMatrixMeta() {
			return buildObservableScatterMatrixObservationMeta({
				cells,
				brush: typeof readBrush === "function" ? readBrush() : null
			});
		}
		return new Proxy(workspaceApi, { get(target, prop, receiver) {
			if (prop === "describeWorkspace" || prop === "describe") {
				const original = Reflect.get(target, prop, receiver);
				if (typeof original !== "function") return original;
				return (...args) => {
					return filterObservableD3WorkspaceDescription({
						...original.apply(target, args) || {},
						provider: "d3",
						workspaceKind: "scatterMatrix",
						matrix: readMatrixMeta()
					}, allowedByWidgetRef);
				};
			}
			if (prop === "readObservation") {
				const original = Reflect.get(target, prop, receiver);
				if (typeof original !== "function") return original;
				return (...args) => {
					const observation = original.apply(target, args);
					const matrix = readMatrixMeta();
					return {
						...observation || {},
						state: {
							...observation?.state || {},
							widgets: Array.isArray(observation?.state?.widgets) ? observation.state.widgets.map((widget) => filterObservableD3WidgetActionNames(widget, allowedByWidgetRef)) : observation?.state?.widgets,
							summary: matrix.summary,
							matrix
						},
						view: {
							...observation?.view || {},
							summary: matrix.summary
						}
					};
				};
			}
			if (prop === "executeAction" || prop === "executeActionAndCommitCoordination") {
				const original = Reflect.get(target, prop, receiver);
				if (typeof original !== "function") return original;
				return async (call, ...args) => {
					assertObservableD3ActionAllowed(call, allowedByWidgetRef);
					return original.call(target, call, ...args);
				};
			}
			const value = Reflect.get(target, prop, receiver);
			return typeof value === "function" ? value.bind(target) : value;
		} });
	}
	function buildObservableBarSpec(rows = []) {
		return {
			data: { values: rows },
			mark: "bar",
			encoding: {
				x: {
					field: "category",
					type: "nominal"
				},
				y: {
					field: "__barHeight",
					type: "quantitative"
				}
			}
		};
	}
	function buildObservableLineSpec(rows = []) {
		const bindings = inferObservableD3LineBindings(rows);
		const encoding = {
			x: {
				field: bindings?.xField || "xValue",
				type: bindings?.xType || "nominal"
			},
			y: {
				field: bindings?.yField || "__seriesIndex",
				type: "quantitative"
			}
		};
		if (Array.isArray(rows) && rows.some((row) => row?.series != null && row.series !== "")) encoding.color = {
			field: "series",
			type: "nominal"
		};
		return {
			data: { values: rows },
			mark: "line",
			encoding
		};
	}
	function buildSelectionFromRowSummary(summary) {
		if (!summary || summary.count === 0) return null;
		const xSpan = summary.xMax - summary.xMin;
		const ySpan = summary.yMax - summary.yMin;
		return {
			kind: "interval",
			domain: {
				xDomain: [summary.xMin + xSpan * .25, summary.xMax - xSpan * .25],
				yDomain: [summary.yMin + ySpan * .25, summary.yMax - ySpan * .25]
			}
		};
	}
	function createOfficialPageAgentLoopRunner(agentTarget) {
		return async function runOfficialPageAgentLoop(options = {}) {
			if (!agentTarget || typeof agentTarget.describeWorkspace !== "function") throw new Error("WidgetVA page port is not ready for official-page agent-loop execution.");
			return runAgentLoopOnTarget(agentTarget, options);
		};
	}
	async function attachWidgetVAToObservableD3ScatterPage({ root = globalThis.window, sessionId = null, userIntent = "Analyze and manipulate the current official Observable D3 scatterplot through WidgetVA structured actions.", timeoutMs = 5e3, pollMs = 25 } = {}) {
		const pageUrl = root?.location?.href || "";
		if (!isObservableD3NotebookPage(pageUrl)) throw new Error(`Unsupported Observable D3 URL: ${pageUrl}`);
		const pageShape = describeObservableD3PageShape(root);
		const scatterSemanticHints = inferObservableD3ScatterSemanticHints(pageShape);
		const rpcHost = await waitForObservableD3RpcHost(root, {
			root,
			timeoutMs,
			pollMs
		});
		const scatterSurface = await waitForObservableWorkerScatterSurface(rpcHost, {
			timeoutMs,
			pollMs,
			notebook: pageShape?.notebook || null,
			semanticHints: scatterSemanticHints
		});
		const surfaceDescription = scatterSurface?.surface || null;
		const route = rpcHost === root ? "top-page" : "worker";
		const rows = Array.isArray(scatterSurface?.rows) ? scatterSurface.rows : [];
		if (rows.length === 0) throw new Error("No scatter points were detected on the Observable D3 surface.");
		const nativeContract = await readObservableD3NativeContract(rpcHost, { timeoutMs });
		const spec = buildObservableScatterSpec(rows);
		const widgetAdapter = createD3FamilyAdapter("scatter");
		const currentSpecRef = { current: clone$3(spec) };
		const wrapper = createObservableScatterSurfaceWrapper({
			frame: rpcHost,
			currentSpecRef,
			nativeBrushBindings: nativeContract?.native?.brushBindings || []
		});
		const widget = createWidgetInstance({
			widgetAdapter,
			spec,
			runtimeOptions: {
				hostBridge: createOfficialPageHostBridge({
					sessionId: sessionId || `official-observable-d3-${pageShape?.notebook?.slug || "scatterplot"}`,
					baselineSpec: spec,
					currentSpecRef,
					userIntent,
					emitOnWrite: true
				}),
				registerDefaultWidgetFamilies: true
			}
		});
		await widget.mount({
			view: wrapper,
			surface: root.document?.documentElement || null,
			spec
		});
		const syncSpecDrivenState = async (actionContext = null) => syncObservableD3ScatterControllerState({
			widget,
			wrapper,
			currentSpecRef,
			actionContext
		});
		const widgetApi = createPostActionSyncProxy(widget, syncSpecDrivenState);
		const pagePort = root?.__widgetVA ? createPostActionSyncProxy(root.__widgetVA, syncSpecDrivenState) : null;
		if (pagePort) root.__widgetVA = pagePort;
		const workspace = createWidgetWorkspace({
			runtime: widget.runtime,
			widgets: [widgetApi]
		});
		const workspaceApi = createPostActionSyncProxy(workspace, syncSpecDrivenState);
		const widgetRef = widget.resolveWidgetRef?.() || widgetApi.resolveWidgetRef?.() || null;
		const hasNativeBrushAction = (Array.isArray(nativeContract?.actions) ? nativeContract.actions : []).some((action) => action?.name === "scatter.brushRegion");
		const observedWorkspaceApi = createObservableD3ActionFilteredWorkspaceApi(workspaceApi, { allowedActionNamesByWidgetRef: widgetRef ? { [widgetRef]: hasNativeBrushAction ? ["scatter.brushRegion", "widget.clearSelection"] : [] } : {} });
		await syncSpecDrivenState();
		async function readDebugSnapshot() {
			return {
				provider: "d3",
				kind: "scatter",
				pageShape,
				surface: surfaceDescription,
				route,
				...await wrapper.readDebugSnapshot()
			};
		}
		async function previewVisibleBrush() {
			const snapshot = await readDebugSnapshot();
			const selection = buildSelectionFromRowSummary(snapshot?.rowSummary || null);
			if (!selection) throw new Error("Unable to preview brush because no scatter rows were detected.");
			return {
				ok: true,
				selection,
				selectionResult: await wrapper.setSelection(selection) || null,
				snapshot
			};
		}
		async function renderDebugProbe() {
			return invokeObservableWorker(rpcHost, "renderDebugProbe", null, { timeoutMs });
		}
		return {
			provider: "d3",
			kind: "scatter",
			pageShape,
			surface: surfaceDescription,
			rows,
			widget: widgetApi,
			workspace: observedWorkspaceApi,
			widgetAdapter,
			runAgentLoop: createOfficialPageAgentLoopRunner(observedWorkspaceApi),
			readRecoverableState() {
				return readControllerRecoverableState(widget);
			},
			async restoreRecoverableState(state) {
				return restoreControllerRecoverableState(widget, state, "Observable D3 scatter controller");
			},
			readDebugSnapshot,
			previewVisibleBrush,
			renderDebugProbe,
			dispose() {
				workspace.dispose();
				widget.dispose();
			}
		};
	}
	async function attachWidgetVAToObservableD3ScatterMatrixPage({ root = globalThis.window, sessionId = null, userIntent = "Analyze and manipulate the current official Observable D3 scatterplot matrix through WidgetVA structured actions.", timeoutMs = 5e3, pollMs = 25 } = {}) {
		const pageUrl = root?.location?.href || "";
		if (!isObservableD3NotebookPage(pageUrl)) throw new Error(`Unsupported Observable D3 URL: ${pageUrl}`);
		const pageShape = describeObservableD3PageShape(root);
		const scatterSemanticHints = inferObservableD3ScatterSemanticHints(pageShape);
		const rpcHost = await waitForObservableD3RpcHost(root, {
			root,
			timeoutMs,
			pollMs
		});
		const matrixSurface = await waitForObservableWorkerScatterMatrixSurface(rpcHost, {
			timeoutMs,
			pollMs,
			notebook: pageShape?.notebook || null,
			semanticHints: scatterSemanticHints
		});
		const cells = Array.isArray(matrixSurface?.cells) ? matrixSurface.cells : [];
		const rows = Array.isArray(matrixSurface?.matrix?.rows) ? matrixSurface.matrix.rows : [];
		if (cells.length < 2) throw new Error("No scatterplot matrix cells were detected on the Observable D3 surface.");
		if (rows.length === 0) throw new Error("No scatterplot matrix rows were detected on the Observable D3 surface.");
		const workspaceId = sessionId || `official-observable-d3-${pageShape?.notebook?.slug || "scatterplot-matrix"}`;
		const nativeContract = await readObservableD3NativeContract(rpcHost, { timeoutMs });
		const workspaceSpec = buildObservableScatterMatrixWorkspaceSpec({
			cells,
			rows,
			nativeContract
		});
		const primarySpec = buildObservableScatterMatrixCellSpec(cells[0], rows);
		const currentSpecRef = { current: clone$3(primarySpec) };
		const cellModels = cells.map((cell, index) => {
			const widgetId = buildObservableScatterMatrixCellWidgetId(cell, index);
			return {
				cell,
				widgetId,
				widgetRef: makeWidgetRef({
					workspaceId,
					widgetId
				}),
				spec: buildObservableScatterMatrixCellSpec(cell, rows)
			};
		});
		const cellRefByWidgetRef = Object.fromEntries(cellModels.map((model) => [model.widgetRef, model.cell.ref]));
		const nativeActionNamesByTargetRef = /* @__PURE__ */ new Map();
		for (const action of Array.isArray(nativeContract?.actions) ? nativeContract.actions : []) {
			if (typeof action?.targetRef !== "string" || typeof action?.name !== "string") continue;
			const names = nativeActionNamesByTargetRef.get(action.targetRef) || [];
			names.push(action.name);
			nativeActionNamesByTargetRef.set(action.targetRef, names);
		}
		const allowedActionNamesByWidgetRef = Object.fromEntries(cellModels.map((model) => {
			const nativeActionNames = nativeActionNamesByTargetRef.get(model.cell.ref) || [];
			return [model.widgetRef, nativeActionNames.length > 0 ? [...new Set([...nativeActionNames, "widget.clearSelection"])] : []];
		}));
		const agentCells = buildObservableScatterMatrixAgentCells(cellModels);
		const wrapper = createObservableScatterMatrixSurfaceWrapper({
			frame: rpcHost,
			cellRefByWidgetRef,
			nativeBrushBindings: nativeContract?.native?.brushBindings || []
		});
		const hostBridge = createOfficialPageHostBridge({
			sessionId: workspaceId,
			baselineSpec: primarySpec,
			currentSpecRef,
			workspaceSpec,
			userIntent,
			emitOnWrite: true
		});
		const widgets = [];
		let runtime = null;
		for (const model of cellModels) {
			const widget = createWidgetInstance({
				runtime,
				widgetAdapter: createD3FamilyAdapter("scatter"),
				widgetRef: model.widgetRef,
				widgetId: model.widgetId,
				spec: model.spec,
				runtimeOptions: runtime ? {} : {
					hostBridge,
					registerDefaultWidgetFamilies: true
				}
			});
			runtime = runtime || widget.runtime;
			await widget.mount({
				view: wrapper,
				surface: root.document?.documentElement || null,
				spec: model.spec
			});
			widgets.push(widget);
		}
		const widgetApis = widgets.map((widget) => createPostActionSyncProxy(widget, async () => {
			await wrapper.renderFromState(widget.readState?.() || null);
			return { recoverableState: readRecoverableState() };
		}));
		const workspace = createWidgetWorkspace({
			runtime,
			widgets: widgetApis
		});
		const syncWorkspaceAction = async ({ args = [], result = null } = {}) => {
			const targetRef = (args[0] || {})?.target?.widgetRef || result?.actionResult?.updatedRefs?.[0] || result?.updatedRefs?.[0] || null;
			const targetWidget = targetRef ? widgets.find((widget) => widget.resolveWidgetRef?.() === targetRef) : null;
			if (targetWidget) await wrapper.renderFromState(targetWidget.readState?.() || null);
			return { recoverableState: readRecoverableState() };
		};
		const observedWorkspaceApi = createObservableScatterMatrixWorkspaceApi(createPostActionSyncProxy(workspace, syncWorkspaceAction), {
			cells: agentCells,
			readBrush: () => wrapper.readRecoverableState?.()?.brush || null,
			allowedActionNamesByWidgetRef
		});
		const pagePort = root?.__widgetVA ? createPostActionSyncProxy(root.__widgetVA, async () => {
			const brush = wrapper.readRecoverableState()?.brush || null;
			const targetWidget = brush?.targetRef ? widgets.find((widget) => widget.resolveWidgetRef?.() === brush.targetRef) : null;
			if (targetWidget) await wrapper.renderFromState(targetWidget.readState?.() || null);
			return { recoverableState: readRecoverableState() };
		}) : null;
		if (pagePort) root.__widgetVA = pagePort;
		async function readDebugSnapshot() {
			const snapshot = await wrapper.readDebugSnapshot();
			return {
				provider: "d3",
				kind: "scatterMatrix",
				pageShape,
				surface: matrixSurface?.surface || null,
				route: rpcHost === root ? "top-page" : "worker",
				cells,
				...snapshot
			};
		}
		function readRecoverableState() {
			return {
				stateId: workspace.readState?.()?.stateId || null,
				...wrapper.readRecoverableState?.() || {}
			};
		}
		async function restoreRecoverableState(state = {}) {
			if (state?.stateId) await workspace.jumpToState({ stateId: state.stateId });
			await wrapper.restoreRecoverableState({ brush: state && Object.prototype.hasOwnProperty.call(state, "brush") ? state.brush : wrapper.readRecoverableState?.()?.brush || null });
			return {
				ok: true,
				restored: true,
				stateId: state?.stateId || workspace.readState?.()?.stateId || null,
				brush: wrapper.readRecoverableState?.()?.brush || null
			};
		}
		async function renderDebugProbe() {
			return invokeObservableWorker(rpcHost, "renderDebugProbe", null, { timeoutMs });
		}
		return {
			provider: "d3",
			kind: "scatterMatrix",
			pageShape,
			surface: matrixSurface?.surface || null,
			rows,
			cells,
			widgets: widgetApis,
			widget: widgetApis[0] || null,
			workspace: observedWorkspaceApi,
			widgetAdapter: widgetApis[0]?.widgetAdapter || null,
			runAgentLoop: createOfficialPageAgentLoopRunner(observedWorkspaceApi),
			readRecoverableState,
			restoreRecoverableState,
			readDebugSnapshot,
			renderDebugProbe,
			dispose() {
				workspace.dispose();
				for (const widget of widgets) widget.dispose();
			}
		};
	}
	async function attachWidgetVAToObservableD3BarPage({ root = globalThis.window, sessionId = null, userIntent = "Analyze and manipulate the current official Observable D3 bar chart through WidgetVA structured actions.", timeoutMs = 5e3, pollMs = 25 } = {}) {
		const pageUrl = root?.location?.href || "";
		if (!isObservableD3NotebookPage(pageUrl)) throw new Error(`Unsupported Observable D3 URL: ${pageUrl}`);
		const pageShape = describeObservableD3PageShape(root);
		const rpcHost = await waitForObservableD3RpcHost(root, {
			root,
			timeoutMs,
			pollMs
		});
		const barSurface = await waitForObservableWorkerBarSurface(rpcHost, {
			timeoutMs,
			pollMs,
			notebook: pageShape?.notebook || null
		});
		const surfaceDescription = barSurface?.surface || null;
		const route = rpcHost === root ? "top-page" : "worker";
		const rows = Array.isArray(barSurface?.rows) ? barSurface.rows : [];
		if (rows.length === 0) throw new Error("No bars were detected on the Observable D3 surface.");
		const spec = buildObservableBarSpec(rows);
		const widgetAdapter = createD3FamilyAdapter("bar");
		const currentSpecRef = { current: clone$3(spec) };
		const wrapper = createObservableBarSurfaceWrapper({
			frame: rpcHost,
			currentSpecRef
		});
		const widget = createWidgetInstance({
			widgetAdapter,
			spec,
			runtimeOptions: {
				hostBridge: createOfficialPageHostBridge({
					sessionId: sessionId || `official-observable-d3-${pageShape?.notebook?.slug || "bar-chart"}`,
					baselineSpec: spec,
					currentSpecRef,
					userIntent,
					emitOnWrite: true
				}),
				registerDefaultWidgetFamilies: true
			}
		});
		await widget.mount({
			view: wrapper,
			surface: root.document?.documentElement || null,
			spec
		});
		const syncSpecDrivenState = async (actionContext = null) => syncObservableD3BarControllerState({
			widget,
			wrapper,
			actionContext
		});
		const widgetApi = createPostActionSyncProxy(widget, syncSpecDrivenState);
		const pagePort = root?.__widgetVA ? createPostActionSyncProxy(root.__widgetVA, syncSpecDrivenState) : null;
		if (pagePort) root.__widgetVA = pagePort;
		const workspace = createWidgetWorkspace({
			runtime: widget.runtime,
			widgets: [widgetApi]
		});
		await syncSpecDrivenState();
		async function readDebugSnapshot() {
			return {
				provider: "d3",
				kind: "bar",
				pageShape,
				surface: surfaceDescription,
				route,
				...await wrapper.readDebugSnapshot()
			};
		}
		async function renderDebugProbe() {
			return invokeObservableWorker(rpcHost, "renderDebugProbe", null, { timeoutMs });
		}
		return {
			provider: "d3",
			kind: "bar",
			pageShape,
			surface: surfaceDescription,
			rows,
			widget: widgetApi,
			workspace,
			widgetAdapter,
			runAgentLoop: createOfficialPageAgentLoopRunner(workspace),
			readRecoverableState() {
				return readControllerRecoverableState(widget);
			},
			async restoreRecoverableState(state) {
				return restoreControllerRecoverableState(widget, state, "Observable D3 bar controller");
			},
			readDebugSnapshot,
			renderDebugProbe,
			dispose() {
				workspace.dispose();
				widget.dispose();
			}
		};
	}
	async function attachWidgetVAToObservableD3LinePage({ root = globalThis.window, sessionId = null, userIntent = "Analyze and manipulate the current official Observable D3 line chart through WidgetVA structured actions.", timeoutMs = 5e3, pollMs = 25 } = {}) {
		const pageUrl = root?.location?.href || "";
		if (!isObservableD3NotebookPage(pageUrl)) throw new Error(`Unsupported Observable D3 URL: ${pageUrl}`);
		const pageShape = describeObservableD3PageShape(root);
		const rpcHost = await waitForObservableD3RpcHost(root, {
			root,
			timeoutMs,
			pollMs
		});
		const lineSurface = await waitForObservableWorkerLineSurface(rpcHost, {
			timeoutMs,
			pollMs,
			notebook: pageShape?.notebook || null
		});
		const surfaceDescription = lineSurface?.surface || null;
		const route = rpcHost === root ? "top-page" : "worker";
		const rows = Array.isArray(lineSurface?.rows) ? lineSurface.rows : [];
		if (rows.length === 0) throw new Error("No line rows were detected on the Observable D3 surface.");
		const spec = buildObservableLineSpec(rows);
		const widgetAdapter = createD3FamilyAdapter("line");
		const currentSpecRef = { current: clone$3(spec) };
		const wrapper = createObservableLineSurfaceWrapper({
			frame: rpcHost,
			currentSpecRef
		});
		const widget = createWidgetInstance({
			widgetAdapter,
			spec,
			runtimeOptions: {
				hostBridge: createOfficialPageHostBridge({
					sessionId: sessionId || `official-observable-d3-${pageShape?.notebook?.slug || "line-chart"}`,
					baselineSpec: spec,
					currentSpecRef,
					userIntent,
					emitOnWrite: true
				}),
				registerDefaultWidgetFamilies: true
			}
		});
		await widget.mount({
			view: wrapper,
			surface: root.document?.documentElement || null,
			spec
		});
		const syncState = async (actionContext = null) => syncObservableD3LineControllerState({
			widget,
			wrapper,
			currentSpecRef,
			actionContext
		});
		const widgetApi = createPostActionSyncProxy(widget, syncState);
		const pagePort = root?.__widgetVA ? createPostActionSyncProxy(root.__widgetVA, syncState) : null;
		if (pagePort) root.__widgetVA = pagePort;
		await syncState();
		const workspace = createWidgetWorkspace({
			runtime: widget.runtime,
			widgets: [widgetApi]
		});
		async function readDebugSnapshot() {
			return {
				provider: "d3",
				kind: "line",
				pageShape,
				surface: surfaceDescription,
				route,
				...await wrapper.readDebugSnapshot()
			};
		}
		async function renderDebugProbe() {
			return invokeObservableWorker(rpcHost, "renderDebugProbe", null, { timeoutMs });
		}
		return {
			provider: "d3",
			kind: "line",
			pageShape,
			surface: surfaceDescription,
			rows,
			widget: widgetApi,
			workspace,
			widgetAdapter,
			runAgentLoop: createOfficialPageAgentLoopRunner(workspace),
			readRecoverableState() {
				return readControllerRecoverableState(widget);
			},
			async restoreRecoverableState(state) {
				return restoreControllerRecoverableState(widget, state, "Observable D3 line controller");
			},
			readDebugSnapshot,
			renderDebugProbe,
			dispose() {
				workspace.dispose();
				widget.dispose();
			}
		};
	}
	async function attachWidgetVAToObservableD3Page(options = {}) {
		const root = options?.root || globalThis.window;
		const pageShape = describeObservableD3PageShape(root);
		const notebook = pageShape?.notebook || null;
		const timeoutMs = options?.timeoutMs || 5e3;
		const pollMs = options?.pollMs || 25;
		const notebookSlug = notebook?.slug || "";
		if (notebookSlug.includes("scatterplot-matrix") || notebookSlug.includes("matrix")) return attachWidgetVAToObservableD3ScatterMatrixPage(options);
		let inferredKind = null;
		try {
			const rpcHost = await waitForObservableD3RpcHost(root, {
				root,
				timeoutMs,
				pollMs
			});
			const scatterSemanticHints = inferObservableD3ScatterSemanticHints(pageShape);
			const matrixResult = await invokeObservableWorker(rpcHost, "readScatterMatrix", { semanticHints: scatterSemanticHints }, { timeoutMs: Math.min(timeoutMs, 1e3) }).catch(() => null);
			if (Array.isArray(matrixResult?.cells) && matrixResult.cells.length > 1) return attachWidgetVAToObservableD3ScatterMatrixPage(options);
			const surface = await invokeObservableWorker(rpcHost, "describeSurface", { notebook }, { timeoutMs: Math.min(timeoutMs, 1e3) });
			inferredKind = typeof surface?.inferredKind === "string" ? surface.inferredKind : null;
			if (![
				"bar",
				"line",
				"scatter"
			].includes(inferredKind)) {
				const [barRowsResult, lineRowsResult, scatterRowsResult] = await Promise.all([
					invokeObservableWorker(rpcHost, "readBarRows", null, { timeoutMs: Math.min(timeoutMs, 1e3) }).catch(() => null),
					invokeObservableWorker(rpcHost, "readLineRows", null, { timeoutMs: Math.min(timeoutMs, 1e3) }).catch(() => null),
					invokeObservableWorker(rpcHost, "readScatterRows", { semanticHints: scatterSemanticHints }, { timeoutMs: Math.min(timeoutMs, 1e3) }).catch(() => null)
				]);
				const barRows = Array.isArray(barRowsResult?.rows) ? barRowsResult.rows : [];
				const lineRows = Array.isArray(lineRowsResult?.rows) ? lineRowsResult.rows : [];
				const scatterRows = Array.isArray(scatterRowsResult?.rows) ? scatterRowsResult.rows : [];
				if (barRows.length > 0) inferredKind = "bar";
				else if (lineRows.length > 0) inferredKind = "line";
				else if (scatterRows.length > 0) inferredKind = "scatter";
			}
		} catch {}
		if (inferredKind === "bar") return attachWidgetVAToObservableD3BarPage(options);
		if (inferredKind === "line") return attachWidgetVAToObservableD3LinePage(options);
		if (inferredKind === "scatter") return attachWidgetVAToObservableD3ScatterPage(options);
		const inferredSlug = notebook?.slug || "";
		if (inferredSlug.includes("bar")) return attachWidgetVAToObservableD3BarPage(options);
		if (inferredSlug.includes("line") || inferredSlug.includes("index-chart")) return attachWidgetVAToObservableD3LinePage(options);
		return attachWidgetVAToObservableD3ScatterPage(options);
	}
	async function bootstrapObservableD3Page({ root = globalThis.window, sessionId = null, userIntent, timeoutMs = 5e3, pollMs = 25, enableExtensionBridge = true, forceReattach = false } = {}) {
		const controller = await attachWidgetVAToObservableD3Page({
			root,
			sessionId,
			userIntent,
			timeoutMs,
			pollMs,
			forceReattach
		});
		const disposeBridge = enableExtensionBridge ? installBrowserExtensionBridge({ root }) : () => {};
		return {
			...controller,
			pagePort: root?.__widgetVA || null,
			runAgentLoop: createOfficialPageAgentLoopRunner(controller.workspace),
			dispose() {
				try {
					disposeBridge?.();
				} finally {
					controller.dispose();
				}
			}
		};
	}
	//#endregion
	//#region ../../widgetva-kit/src/core/agent/planning/naturalLanguagePlanner.js
	var DEFAULT_OPENROUTER_AGENT_MODEL = "deepseek/deepseek-v4-flash";
	var TEMP_DEMO_WEATHER_CATEGORIES = [
		"rain",
		"fog",
		"snow"
	];
	function clone$2(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function stripPromptExamples(value) {
		if (Array.isArray(value)) return value.map((entry) => stripPromptExamples(entry));
		if (!value || typeof value !== "object") return value;
		const next = {};
		for (const [key, entry] of Object.entries(value)) {
			if (key === "examples") continue;
			next[key] = stripPromptExamples(entry);
		}
		return next;
	}
	function normalizeKnowledgeForPrompt(knowledge = null) {
		return stripPromptExamples(clone$2(knowledge));
	}
	var PROMPT_DROP_KEYS = new Set([
		"bodyText",
		"fullRows",
		"pageText",
		"rawCode",
		"rawRows",
		"rawSpec",
		"rowCount",
		"rows",
		"snapshot",
		"source",
		"sourceCode",
		"spec"
	]);
	var PROMPT_ARRAY_LIMITS = {
		cells: 64,
		fieldValues: 16,
		fields: 32,
		turns: 12,
		widgets: 24
	};
	function promptArrayLimitForKey(key = null) {
		return PROMPT_ARRAY_LIMITS[key] || 24;
	}
	function sanitizePromptValue(value, key = null) {
		if (Array.isArray(value)) return value.slice(0, promptArrayLimitForKey(key)).map((entry) => sanitizePromptValue(entry));
		if (!value || typeof value !== "object") return value;
		const next = {};
		for (const [entryKey, entryValue] of Object.entries(value)) {
			if (PROMPT_DROP_KEYS.has(entryKey)) continue;
			next[entryKey] = sanitizePromptValue(entryValue, entryKey);
		}
		return next;
	}
	function compactMatrixForPrompt(matrix = null) {
		if (!matrix || typeof matrix !== "object" || Array.isArray(matrix)) return matrix;
		return {
			provider: matrix.provider || null,
			kind: matrix.kind || null,
			cellCount: Number.isFinite(matrix.cellCount) ? matrix.cellCount : null,
			cells: (Array.isArray(matrix.cells) ? matrix.cells : []).slice(0, PROMPT_ARRAY_LIMITS.cells).map((cell) => ({
				ref: cell?.ref || null,
				xField: cell?.xField || null,
				yField: cell?.yField || null,
				xDomain: Array.isArray(cell?.xDomain) ? [...cell.xDomain] : null,
				yDomain: Array.isArray(cell?.yDomain) ? [...cell.yDomain] : null
			})).filter((cell) => cell.ref),
			currentBrush: matrix.currentBrush ? sanitizePromptValue(matrix.currentBrush) : null,
			summary: matrix.summary || null
		};
	}
	function sanitizeAgentObservationForPrompt(observe = null) {
		const sanitized = sanitizePromptValue(clone$2(observe));
		if (sanitized?.state?.matrix) sanitized.state.matrix = compactMatrixForPrompt(sanitized.state.matrix);
		if (sanitized?.matrix) sanitized.matrix = compactMatrixForPrompt(sanitized.matrix);
		if (sanitized?.view && typeof sanitized.view === "object") delete sanitized.view.snapshot;
		return sanitized;
	}
	function summarizePromptParams(params = null) {
		if (!params || typeof params !== "object" || Array.isArray(params)) return null;
		const parts = [];
		for (const [key, value] of Object.entries(params).slice(0, 6)) if (Array.isArray(value)) parts.push(`${key}=[${value.slice(0, 6).join(", ")}]`);
		else if (value && typeof value === "object") parts.push(`${key}={...}`);
		else if (value != null) parts.push(`${key}=${value}`);
		return parts.length > 0 ? parts.join("; ") : null;
	}
	function normalizeHistoryTurnForPrompt(turn = null, index = 0) {
		if (!turn || typeof turn !== "object") return turn;
		const operation = turn.operation || turn.act?.operation || turn.plan?.operation || turn.plan || turn.act || null;
		const status = turn.status || {};
		return {
			turnId: turn.turnId || `turn_${index + 1}`,
			operation: operation ? {
				kind: operation.kind || null,
				name: operation.name || null,
				target: operation.target ? sanitizePromptValue(operation.target) : null,
				paramsSummary: operation.paramsSummary || summarizePromptParams(operation.params)
			} : null,
			status: {
				outcome: status.outcome || (turn.verify?.ok === false ? "failed" : null) || (turn.act?.ok === false ? "failed" : null) || null,
				resultSummary: status.resultSummary || turn.act?.outputSummary || turn.resultSummary || null,
				stateSummary: status.stateSummary || turn.verify?.summary || turn.stateSummary || null,
				reasonSummary: status.reasonSummary || turn.reason?.answer || turn.reasonSummary || null
			}
		};
	}
	function normalizeHistoryForPrompt(history = null) {
		if (!history || typeof history !== "object") return history;
		if (Array.isArray(history.turns)) return { turns: history.turns.slice(-PROMPT_ARRAY_LIMITS.turns).map(normalizeHistoryTurnForPrompt) };
		return sanitizePromptValue(history);
	}
	function isTemporarySeattleWeatherDemo({ objective = null, observe = null } = {}) {
		const objectiveText = typeof objective === "string" ? objective.toLowerCase() : "";
		const observeText = JSON.stringify(observe || {}).toLowerCase();
		const looksLikeSeattleWeather = observeText.includes("seattle") || observeText.includes("weather") || observeText.includes("temp_max");
		const mentionsExplicitDemoCategories = objectiveText.includes("rain") && objectiveText.includes("fog") && objectiveText.includes("snow");
		const asksWeatherTemperaturePattern = (objectiveText.includes("different weather") || objectiveText.includes("weather condition") || objectiveText.includes("weather conditions")) && (objectiveText.includes("temperature") || objectiveText.includes("temp")) && (objectiveText.includes("over the year") || objectiveText.includes("across the year") || objectiveText.includes("season") || objectiveText.includes("year in seattle"));
		return looksLikeSeattleWeather && (mentionsExplicitDemoCategories || asksWeatherTemperaturePattern);
	}
	function buildTemporaryDemoWorkflowHints({ objective = null, observe = null } = {}) {
		if (!isTemporarySeattleWeatherDemo({
			objective,
			observe
		})) return [];
		return ["Temporary Seattle Weather demo workflow: when the objective asks how weather conditions relate to temperature over the year, every unfinished turn must execute exactly one action that changes the visible weather category. Do not use perception-only or data-query-only operations as substitutes for these category view changes. Work category-by-category across turns: first call bar.filterCategories with field weather and categories [\"rain\"], then call bar.filterCategories with categories [\"fog\"], then call bar.filterCategories with categories [\"snow\"]. After each action, summarize what the resulting single-weather view shows. Only synthesize the comparison after rain, fog, and snow have each been shown in separate rendered views. Use history to remember which weather category has already been inspected."];
	}
	function buildTemporaryWorkflowHints({ objective = null, observe = null } = {}) {
		return [...buildTemporaryDemoWorkflowHints({
			objective,
			observe
		})];
	}
	function readTemporaryDemoCompletedCategories({ history = null, plan = null } = {}) {
		const operationPayloads = [];
		if (Array.isArray(history?.turns)) for (const turn of history.turns) operationPayloads.push(turn?.operation || null, turn?.act?.operation || null, turn?.plan?.operation || null, turn?.act || null, turn?.plan?.step || null);
		operationPayloads.push(plan?.operation || plan || null);
		const operationText = JSON.stringify(operationPayloads.filter(Boolean)).toLowerCase();
		return TEMP_DEMO_WEATHER_CATEGORIES.filter((category) => operationText.includes(category));
	}
	function readTemporaryDemoNextCategory(history = null) {
		const completedCategories = readTemporaryDemoCompletedCategories({ history });
		return TEMP_DEMO_WEATHER_CATEGORIES.find((category) => !completedCategories.includes(category)) || null;
	}
	function hasAvailableTemporaryDemoAction({ observe = null, knowledge = null } = {}) {
		const observeText = JSON.stringify(observe || {}).toLowerCase();
		const knowledgeText = JSON.stringify(knowledge || {}).toLowerCase();
		return observeText.includes("bar.filtercategories") || knowledgeText.includes("bar.filtercategories");
	}
	function matchesTemporaryDemoOperation({ operation = null, objective = null, observe = null, history = null } = {}) {
		if (!isTemporarySeattleWeatherDemo({
			objective,
			observe
		})) return true;
		const nextCategory = readTemporaryDemoNextCategory(history);
		if (!nextCategory) return true;
		if (!operation || operation.kind !== "action" || operation.name !== "bar.filterCategories") return false;
		const paramsText = JSON.stringify(operation.params || {}).toLowerCase();
		return paramsText.includes("weather") && paramsText.includes(nextCategory);
	}
	function buildTemporaryDemoFallbackOperation({ objective = null, observe = null, knowledge = null, history = null } = {}) {
		const nextCategory = readTemporaryDemoNextCategory(history);
		if (!nextCategory || !hasAvailableTemporaryDemoAction({
			observe,
			knowledge
		})) return null;
		const focusedWidgetRef = readFocusedWidgetRef(observe);
		if (!focusedWidgetRef) return null;
		return {
			assistantMessage: `I will filter the weather view to ${nextCategory} so this category can be inspected as its own rendered view.`,
			rationale: "The temporary Seattle Weather demo workflow requires one weather-category action per turn before synthesis.",
			kind: "action",
			name: "bar.filterCategories",
			target: { widgetRef: focusedWidgetRef },
			params: {
				field: "weather",
				categories: [nextCategory]
			},
			dataRef: null,
			query: null
		};
	}
	function shouldContinueTemporaryDemo({ objective = null, observe = null, history = null, plan = null, result = null } = {}) {
		if (!isTemporarySeattleWeatherDemo({
			objective,
			observe
		})) return false;
		const completedCategories = readTemporaryDemoCompletedCategories({
			history,
			plan
		});
		return TEMP_DEMO_WEATHER_CATEGORIES.some((category) => !completedCategories.includes(category));
	}
	function buildAgentMessages({ objective, observe, knowledge = null, history = null }) {
		const systemPrompt = [
			"You are an analyst agent operating a widget-based visual analytics workspace.",
			"Your job is to convert the user objective into exactly one next structured operation.",
			"Use only the actions, perceptions, and data-query surfaces already exposed by the provided knowledge and current observation.",
			"Use history to avoid repeating operations unless repetition is necessary with different parameters.",
			"If the objective requires comparing multiple subsets, use history to track which subset has already been inspected.",
			"Treat action names as semantic widget operations: the runtime will update widget/workspace state and the provider will apply that state to the active visualization environment.",
			"For page-linked visualizations, use the page-linked action surface for brush, overview-detail, and domain-sync semantics when those actions are exposed.",
			"For page-linked visualizations, use the page-linked action surface for click, select, hover, and bound-parameter semantics when those actions are exposed.",
			"Prefer shared-state updates that let the provider rematerialize the page instead of trying to mutate private provider internals directly.",
			"Do not reason as if you need to touch private Vega runtime internals, tuple stores, or signal handles.",
			"When bar actions are available, use bar.clickCategory for explicit click/tap language that should preserve the page’s existing category-linked interaction, and use bar.selectCategory for general selection semantics.",
			"If the user asks to update linked views, propagate a bar choice, drive cross-widget filtering, create a source selection, or use existing coordination links from a bar chart, choose bar.selectCategory unless the language explicitly says click/tap.",
			"Use bar.filterCategories only when the user explicitly wants to keep only, filter to, exclude, or remove categories from the visible view, and never for linked-view propagation.",
			"When the user asks to reset or undo a chart/view/zoom/filter view state, use widget.resetView or widget.undoView when those actions are exposed; do not substitute workspace.resetWorkspace or widget.undoSelection for view reset/undo requests.",
			"Return JSON only.",
			"The JSON must contain assistantMessage, rationale, and operation.",
			"operation.kind must be exactly one of: action, perception, data_query.",
			"If operation.kind is action or perception, include operation.name.",
			"If operation.kind is action or perception, include operation.target.widgetRef. Choose exactly one widgetRef from observe.state.widgets[].ref.",
			"For action/perception operations, put arguments in operation.params.",
			"When an action requires category, series, or line identifiers, choose exact values from observe.state.widgets[].data.fieldValues when available.",
			"For data_query operations, include operation.dataRef and operation.query.",
			"Do not return markdown fences.",
			...buildTemporaryWorkflowHints({
				objective,
				observe
			})
		].join(" ");
		const normalizedObserve = normalizeObserveForPrompt(observe, { objective });
		const userPrompt = JSON.stringify({
			objective,
			knowledge: normalizeKnowledgeForPrompt(knowledge),
			observe: normalizedObserve,
			history: normalizeHistoryForPrompt(history)
		});
		return [{
			role: "system",
			content: systemPrompt
		}, {
			role: "user",
			content: userPrompt
		}];
	}
	function buildRepairMessages({ objective, observe, knowledge = null, history = null, previousContent = "" }) {
		const systemPrompt = [
			"You previously returned an invalid plan for a widget-based visual analytics agent.",
			"Remember: use only currently exposed actions, perceptions, and data-query surfaces from the provided knowledge and observation.",
			"Remember: action names are semantic widget operations; do not touch private Vega runtime internals, tuple stores, or signal handles.",
			"Remember: bar.clickCategory is for explicit in-page category click semantics, bar.selectCategory is for general selection semantics, and bar.filterCategories is for keep-only/filter-visible semantics.",
			"Remember: linked-view bar interactions must use bar.selectCategory or bar.clickCategory because bar.filterCategories is view-local and does not create a source selection for coordination links.",
			"Remember: reset or undo chart/view/zoom/filter view requests should use widget.resetView or widget.undoView when exposed, not workspace.resetWorkspace or widget.undoSelection.",
			"Return JSON only.",
			"Your JSON must contain assistantMessage, rationale, and operation.",
			"operation.kind must be exactly one of: action, perception, data_query.",
			"If operation.kind is action or perception, include operation.name.",
			"If operation.kind is action or perception, include operation.target.widgetRef. Choose exactly one widgetRef from observe.state.widgets[].ref.",
			"When required params need category, series, or line identifiers, choose exact values from observe.state.widgets[].data.fieldValues when available.",
			...buildTemporaryWorkflowHints({
				objective,
				observe
			}),
			"Do not include markdown fences or explanatory prose."
		].join(" ");
		const userPrompt = JSON.stringify({
			objective,
			knowledge: normalizeKnowledgeForPrompt(knowledge),
			observe: normalizeObserveForPrompt(observe, { objective }),
			history: normalizeHistoryForPrompt(history),
			previousContent,
			requiredShape: {
				assistantMessage: "string",
				rationale: "string",
				operation: {
					kind: "action|perception|data_query",
					name: "string when kind is action/perception",
					target: { widgetRef: "string" },
					params: {},
					dataRef: "string when kind is data_query",
					query: {}
				}
			}
		});
		return [{
			role: "system",
			content: systemPrompt
		}, {
			role: "user",
			content: userPrompt
		}];
	}
	function buildReasonMessages({ objective, observe, history, plan, result, verification, latestCoordinationResult } = {}) {
		const systemPrompt = [
			"You are the answer stage of a widget-based visual analytics agent.",
			"You have already observed, planned, executed, and verified one analysis step.",
			"Write a concise progress note for this turn, not necessarily the final answer for the whole session.",
			"Use history to avoid repeating earlier progress unless the latest step changes the conclusion.",
			"Use the actual execution result and verification outcome.",
			"Do not invent computed values that are not present in the runtime result.",
			"Return JSON only.",
			"The JSON must contain answer.",
			"If the overall user objective has been sufficiently answered, include completion.status as \"answered\".",
			"If another turn is still needed, omit completion or set completion.status to \"continue\".",
			...buildTemporaryWorkflowHints({
				objective,
				observe
			})
		].join(" ");
		const userPrompt = JSON.stringify({
			objective,
			observe: normalizeObserveForPrompt(observe, { objective }),
			history: normalizeHistoryForPrompt(history),
			plan: normalizePlanForPrompt(plan),
			result: normalizeResultForPrompt(plan, result),
			verification: normalizeVerificationForPrompt(plan, result, verification, observe),
			latestCoordinationResult: normalizeCoordinationResultForPrompt(latestCoordinationResult)
		});
		return [{
			role: "system",
			content: systemPrompt
		}, {
			role: "user",
			content: userPrompt
		}];
	}
	function buildFinalSynthesisMessages({ objective, history, turns, stopReason } = {}) {
		const systemPrompt = [
			"You are the final synthesis stage of a widget-based visual analytics agent.",
			"Synthesize the final answer from the user objective and all completed turns.",
			"Do not propose new actions.",
			"Do not mention raw provider internals unless necessary.",
			"Ground the answer in WidgetVA observations and action/perception results.",
			"Return JSON only.",
			"The JSON must contain answer."
		].join(" ");
		const compactTurns = Array.isArray(turns) ? turns.map((turn) => ({
			index: turn?.index,
			operation: turn?.act ? {
				kind: turn.act.kind || null,
				name: turn.act.name || null,
				params: clone$2(turn.act.params || {})
			} : null,
			resultSummary: turn?.act?.outputSummary || null,
			verificationSummary: turn?.verify?.summary || null,
			reasonSummary: turn?.reason?.answer || null,
			completion: clone$2(turn?.reason?.completion || null)
		})) : [];
		const userPrompt = JSON.stringify({
			objective,
			history: normalizeHistoryForPrompt(history),
			turns: compactTurns,
			stopReason
		});
		return [{
			role: "system",
			content: systemPrompt
		}, {
			role: "user",
			content: userPrompt
		}];
	}
	function looksLikeAgentObservation(observe = null) {
		return Boolean(observe && typeof observe === "object" && !Array.isArray(observe) && !("observation" in observe) && !("workspace" in observe) && (Object.prototype.hasOwnProperty.call(observe, "query") || Object.prototype.hasOwnProperty.call(observe, "state") || Object.prototype.hasOwnProperty.call(observe, "view")));
	}
	function normalizeObserveForPrompt(observe = null, { objective = null } = {}) {
		if (!observe || typeof observe !== "object") return observe;
		if (looksLikeAgentObservation(observe)) return sanitizeAgentObservationForPrompt(observe);
		if (observe.agentObservation && typeof observe.agentObservation === "object") return sanitizeAgentObservationForPrompt(buildAgentObservation(observe.agentObservation));
		return sanitizeAgentObservationForPrompt(buildAgentObservation({
			query: objective,
			state: null,
			view: null
		}));
	}
	function normalizePlanForPrompt(plan = null) {
		if (!plan || typeof plan !== "object") return plan;
		if (plan?.step && !plan?.operation) return clone$2(plan);
		return buildFormalPlanPayload(plan);
	}
	function summarizePromptPayload(payload = null) {
		if (!payload || typeof payload !== "object") return null;
		if (typeof payload.summary === "string" && payload.summary.length > 0) return payload.summary;
		if (typeof payload.message === "string" && payload.message.length > 0) return payload.message;
		if (typeof payload?.actionResult?.summary === "string" && payload.actionResult.summary.length > 0) return payload.actionResult.summary;
		if (typeof payload?.actionResult?.error?.message === "string" && payload.actionResult.error.message.length > 0) return payload.actionResult.error.message;
		if (typeof payload?.error?.message === "string" && payload.error.message.length > 0) return payload.error.message;
		if (typeof payload.result === "string" && payload.result.length > 0) return payload.result;
		return null;
	}
	function normalizeResultForPrompt(plan = null, result = null) {
		if (!result || typeof result !== "object") return result;
		if (Object.prototype.hasOwnProperty.call(result, "outputSummary")) return sanitizePromptValue({
			kind: result.kind || plan?.operation?.kind || null,
			name: result.name || plan?.operation?.name || null,
			ok: typeof result.ok === "boolean" ? result.ok : null,
			outputSummary: result.outputSummary || null,
			stateId: result.stateId || null,
			updatedRefs: Array.isArray(result.updatedRefs) ? result.updatedRefs : [],
			error: result.error ? sanitizePromptValue(result.error) : null
		});
		const formal = buildFormalActPayload(plan || {}, result || {});
		return sanitizePromptValue({
			kind: formal.kind || null,
			name: formal.name || null,
			params: formal.params || null,
			target: formal.target || null,
			ok: typeof formal.ok === "boolean" ? formal.ok : null,
			stateId: formal.stateId || null,
			updatedRefs: Array.isArray(formal.updatedRefs) ? formal.updatedRefs : [],
			error: formal.error || null,
			recoveryHints: Array.isArray(formal.recoveryHints) ? formal.recoveryHints : [],
			outputSummary: formal.outputSummary || summarizePromptPayload(result)
		});
	}
	function normalizeVerificationForPrompt(plan = null, result = null, verification = null, observe = null) {
		if (!verification || typeof verification !== "object") return verification;
		const compactChecks = verification?.checks && typeof verification.checks === "object" && !Array.isArray(verification.checks) ? Object.fromEntries(Object.entries(verification.checks).map(([name, check]) => [name, {
			ok: typeof check?.ok === "boolean" ? check.ok : null,
			summary: typeof check?.summary === "string" ? check.summary.slice(0, 500) : null
		}])) : null;
		if (compactChecks || Object.prototype.hasOwnProperty.call(verification, "summary")) return sanitizePromptValue({
			ok: typeof verification.ok === "boolean" ? verification.ok : null,
			summary: typeof verification.summary === "string" ? verification.summary.slice(0, 700) : summarizePromptPayload(verification),
			checks: compactChecks
		});
		return buildFormalVerifyPayload({
			plan: plan || {},
			act: normalizeResultForPrompt(plan, result),
			verification,
			observe
		});
	}
	function normalizeCoordinationResultForPrompt(result = null) {
		if (!result || typeof result !== "object") return result;
		return {
			summary: result?.verification?.summary || result?.propagationSummary?.selectionSummary || result?.summary || null,
			verification: result?.verification ? {
				status: result.verification.status || null,
				summary: result.verification.summary || null
			} : null,
			propagation: result?.propagationSummary ? {
				active: Boolean(result.propagationSummary.active),
				sourceRef: result.propagationSummary.sourceRef || null,
				targetWidgetIds: Array.isArray(result.propagationSummary.targetWidgetIds) ? [...result.propagationSummary.targetWidgetIds] : [],
				linkCount: Number.isFinite(result.propagationSummary.linkCount) ? result.propagationSummary.linkCount : 0
			} : null
		};
	}
	function extractJsonObject(text = "") {
		if (typeof text !== "string") return null;
		try {
			return JSON.parse(text);
		} catch {
			const start = text.indexOf("{");
			const end = text.lastIndexOf("}");
			if (start >= 0 && end > start) try {
				return JSON.parse(text.slice(start, end + 1));
			} catch {
				return null;
			}
			return null;
		}
	}
	function readPlannedOperation(plan = {}) {
		if (plan?.operation) return plan.operation;
		for (const [key, value] of Object.entries(plan || {})) {
			const normalizedKey = key.toLowerCase();
			if (value && typeof value === "object" && (normalizedKey === "action" || normalizedKey === "perception" || normalizedKey === "data_query" || normalizedKey === "dataquery")) return {
				kind: normalizedKey === "dataquery" ? "data_query" : normalizedKey,
				...value
			};
		}
		return {};
	}
	function readObservationWidgets(observe = {}) {
		const candidates = [observe?.state?.widgets, observe?.view?.widgets];
		for (const widgets of candidates) if (Array.isArray(widgets)) return widgets;
		return [];
	}
	function readFocusedWidgetRef(observe = {}) {
		const widgets = readObservationWidgets(observe);
		return widgets.find((widget) => widget?.focused)?.ref || widgets[0]?.ref || null;
	}
	function inferOperationKind(plan = {}, operation = {}) {
		const explicitKind = operation.kind || plan.kind;
		if (explicitKind) return explicitKind;
		if (operation.dataRef || operation.query) return "data_query";
		if (operation.name) {
			if (String(operation.name).startsWith("perception.")) return "perception";
			return "action";
		}
		return null;
	}
	function normalizeOperation(plan = {}, observe = {}) {
		const operation = readPlannedOperation(plan);
		const flatWidgetRef = operation["target.widgetRef"] || operation["target.widget_ref"] || null;
		const widgetRef = operation.target?.widgetRef || operation.target?.widget_ref || flatWidgetRef || null;
		return {
			assistantMessage: typeof plan?.assistantMessage === "string" ? plan.assistantMessage : "Planned one next analysis step.",
			rationale: typeof plan?.rationale === "string" ? plan.rationale : "",
			kind: inferOperationKind(plan, operation),
			name: operation.name || null,
			target: widgetRef ? { widgetRef } : void 0,
			params: clone$2(operation.params || {}),
			dataRef: operation.dataRef || null,
			query: clone$2(operation.query || null)
		};
	}
	function isExecutableOperation(operation = {}) {
		if (!operation || typeof operation !== "object") return false;
		if (operation.kind === "action" || operation.kind === "perception") return typeof operation.name === "string" && operation.name.trim().length > 0 && typeof operation?.target?.widgetRef === "string" && operation.target.widgetRef.length > 0;
		if (operation.kind === "data_query") return typeof operation.dataRef === "string" && operation.dataRef.length > 0 && Boolean(operation.query);
		return false;
	}
	function readWidgetKindForRef(observe = {}, widgetRef = null) {
		if (!widgetRef) return null;
		const widget = readObservationWidgets(observe).find((entry) => entry?.ref === widgetRef) || null;
		if (!widget) return null;
		return typeof widget.kind === "string" && widget.kind.length > 0 ? widget.kind : null;
	}
	function readFamilyNamesForWidget(knowledge = null, widgetKind = null, fieldName = "") {
		if (!widgetKind || !fieldName) return [];
		const family = (Array.isArray(knowledge?.widgetFamilies) ? knowledge.widgetFamilies : []).find((entry) => entry?.kind === widgetKind) || null;
		return (Array.isArray(family?.[fieldName]) ? family[fieldName] : []).map((descriptor) => descriptor?.name).filter((name, index, names) => typeof name === "string" && name.length > 0 && names.indexOf(name) === index);
	}
	function readCommonToolNames(knowledge = null, fieldName = "") {
		if (!fieldName) return [];
		return (Array.isArray(knowledge?.commonTools?.[fieldName]) ? knowledge.commonTools[fieldName] : []).map((descriptor) => descriptor?.name).filter((name, index, names) => typeof name === "string" && name.length > 0 && names.indexOf(name) === index);
	}
	function choosePreferredName(names = [], preferredNames = []) {
		for (const preferredName of preferredNames) if (names.includes(preferredName)) return preferredName;
		return names.find((name) => typeof name === "string" && name.length > 0) || null;
	}
	function buildSafeFallbackOperation(observe = {}, knowledge = null) {
		const focusedWidgetRef = readFocusedWidgetRef(observe);
		const fallbackPerception = choosePreferredName([...readFamilyNamesForWidget(knowledge, readWidgetKindForRef(observe, focusedWidgetRef), "perceptions"), ...readCommonToolNames(knowledge, "perceptions")].filter((name, index, names) => names.indexOf(name) === index), [
			"perception.inspectVisibleRows",
			"perception.inspectSelection",
			"perception.inspectViewConfig"
		]);
		if (fallbackPerception) return {
			assistantMessage: "I will inspect the current widget before taking a stronger interaction step.",
			rationale: "The model response did not yield a valid executable operation, so the runtime is falling back to a perception that this workspace actually exposes.",
			kind: "perception",
			name: fallbackPerception,
			...focusedWidgetRef ? { target: { widgetRef: focusedWidgetRef } } : {},
			params: {},
			dataRef: null,
			query: null
		};
		return {
			assistantMessage: "I could not produce a valid operation for the current workspace.",
			rationale: "The model response did not yield a valid executable operation, and the current workspace catalog does not expose a fallback perception for the focused widget. WidgetVA will not guess a state-changing fallback action.",
			kind: null,
			name: null,
			...focusedWidgetRef ? { target: { widgetRef: focusedWidgetRef } } : {},
			params: {},
			dataRef: null,
			query: null
		};
	}
	function createNaturalLanguagePlanner({ completeChat, model = DEFAULT_OPENROUTER_AGENT_MODEL, temperature = .2 } = {}) {
		if (typeof completeChat !== "function") throw new Error("createNaturalLanguagePlanner requires completeChat().");
		return async function planner({ objective = null, observe = null, knowledge = null, history = null } = {}) {
			const safeObjective = typeof objective === "string" && objective.trim().length > 0 ? objective.trim() : "Inspect the current visualization workspace and take the next useful step.";
			const primaryResponse = await completeChat({
				model,
				temperature,
				messages: buildAgentMessages({
					objective: safeObjective,
					knowledge,
					observe,
					history
				})
			});
			const primaryContent = primaryResponse?.content || "";
			const primaryPlan = extractJsonObject(primaryContent);
			if (!primaryPlan) throw new Error("Agent response did not contain valid JSON.");
			let normalizedOperation = normalizeOperation(primaryPlan, observe);
			let resolvedResponse = primaryResponse;
			let resolvedPlan = primaryPlan;
			if (!isExecutableOperation(normalizedOperation) || !matchesTemporaryDemoOperation({
				operation: normalizedOperation,
				objective: safeObjective,
				observe,
				history
			})) {
				const repairedResponse = await completeChat({
					model,
					temperature,
					messages: buildRepairMessages({
						objective: safeObjective,
						knowledge,
						observe,
						history,
						previousContent: primaryContent
					})
				});
				const repairedPlan = extractJsonObject(repairedResponse?.content || "");
				const repairedOperation = repairedPlan ? normalizeOperation(repairedPlan, observe) : null;
				if (repairedPlan && isExecutableOperation(repairedOperation) && matchesTemporaryDemoOperation({
					operation: repairedOperation,
					objective: safeObjective,
					observe,
					history
				})) {
					resolvedResponse = repairedResponse;
					resolvedPlan = repairedPlan;
					normalizedOperation = repairedOperation;
				} else normalizedOperation = buildTemporaryDemoFallbackOperation({
					objective: safeObjective,
					observe,
					knowledge,
					history
				}) || buildSafeFallbackOperation(observe, knowledge);
			}
			if (!isExecutableOperation(normalizedOperation)) throw new Error(normalizedOperation?.rationale || "Agent planning did not yield an executable operation.");
			return {
				assistantMessage: normalizedOperation.assistantMessage,
				rationale: normalizedOperation.rationale,
				operation: {
					kind: normalizedOperation.kind,
					...normalizedOperation.name ? { name: normalizedOperation.name } : {},
					...normalizedOperation.target ? { target: normalizedOperation.target } : {},
					...normalizedOperation.params ? { params: normalizedOperation.params } : {},
					...normalizedOperation.dataRef ? { dataRef: normalizedOperation.dataRef } : {},
					...normalizedOperation.query ? { query: normalizedOperation.query } : {}
				},
				source: "planner",
				model,
				rawResponse: clone$2(resolvedResponse?.raw || null),
				rawContent: resolvedResponse?.content || "",
				rawPlan: clone$2(resolvedPlan)
			};
		};
	}
	function createNaturalLanguageReasoner({ completeChat, model = DEFAULT_OPENROUTER_AGENT_MODEL, temperature = .2 } = {}) {
		if (typeof completeChat !== "function") throw new Error("createNaturalLanguageReasoner requires completeChat().");
		return async function reasoner({ objective = null, observe = null, history = null, plan = null, result = null, verification = null, latestCoordinationResult = null } = {}) {
			const response = await completeChat({
				model,
				temperature,
				messages: buildReasonMessages({
					objective,
					observe,
					history,
					plan,
					result,
					verification,
					latestCoordinationResult
				})
			});
			const content = response?.content || "";
			const parsed = extractJsonObject(content);
			if (parsed && typeof parsed.answer === "string" && parsed.answer.trim().length > 0) {
				let completionStatus = typeof parsed?.completion?.status === "string" && parsed.completion.status.trim().length > 0 ? parsed.completion.status.trim() : null;
				if (completionStatus === "answered" && shouldContinueTemporaryDemo({
					objective,
					observe,
					history,
					plan,
					result
				})) completionStatus = "continue";
				return {
					answer: parsed.answer.trim(),
					...completionStatus ? { completion: { status: completionStatus } } : {},
					rawResponse: clone$2(response?.raw || null),
					rawContent: content
				};
			}
			return {
				answer: (typeof result?.summary === "string" && result.summary.length > 0 ? result.summary : null) || (typeof result?.message === "string" && result.message.length > 0 ? result.message : null) || (typeof plan?.assistantMessage === "string" && plan.assistantMessage.length > 0 ? plan.assistantMessage : null) || "Completed one agent step.",
				rawResponse: clone$2(response?.raw || null),
				rawContent: content
			};
		};
	}
	function createNaturalLanguageFinalSynthesizer({ completeChat, model = DEFAULT_OPENROUTER_AGENT_MODEL, temperature = .2 } = {}) {
		if (typeof completeChat !== "function") throw new Error("createNaturalLanguageFinalSynthesizer requires completeChat().");
		return async function finalSynthesizer({ objective = null, history = null, turns = [], stopReason = null } = {}) {
			const response = await completeChat({
				model,
				temperature,
				messages: buildFinalSynthesisMessages({
					objective,
					history,
					turns,
					stopReason
				})
			});
			const content = response?.content || "";
			const parsed = extractJsonObject(content);
			const fallbackAnswer = Array.isArray(turns) && turns.length > 0 ? turns.at(-1)?.reason?.answer || "Completed the requested WidgetVA analysis." : "Completed the requested WidgetVA analysis.";
			return {
				answer: typeof parsed?.answer === "string" && parsed.answer.trim().length > 0 ? parsed.answer.trim() : fallbackAnswer,
				rawResponse: clone$2(response?.raw || null),
				rawContent: content
			};
		};
	}
	async function runNaturalLanguageAgentSession(target, { objective = null, completeChat, model = DEFAULT_OPENROUTER_AGENT_MODEL, temperature = .2, ...loopOptions } = {}) {
		const planner = createNaturalLanguagePlanner({
			completeChat,
			model,
			temperature
		});
		const reasoner = createNaturalLanguageReasoner({
			completeChat,
			model,
			temperature
		});
		const finalSynthesizer = typeof loopOptions?.finalSynthesizer === "function" ? loopOptions.finalSynthesizer : createNaturalLanguageFinalSynthesizer({
			completeChat,
			model,
			temperature
		});
		return runAgentSession(target, {
			...loopOptions,
			objective,
			planner,
			reasoner,
			finalSynthesizer
		});
	}
	async function runNaturalLanguageAgentTurn(target, { objective = null, completeChat, model = DEFAULT_OPENROUTER_AGENT_MODEL, temperature = .2, ...loopOptions } = {}) {
		const planner = createNaturalLanguagePlanner({
			completeChat,
			model,
			temperature
		});
		const reasoner = createNaturalLanguageReasoner({
			completeChat,
			model,
			temperature
		});
		return runAgentTurn(target, {
			...loopOptions,
			objective,
			planner,
			reasoner
		});
	}
	[
		...buildBarActionDescriptors(),
		...buildHeatmapActionDescriptors(),
		...buildLineActionDescriptors(),
		...buildScatterActionDescriptors(),
		makeActionDescriptor({
			name: "widget.clearSelection",
			title: "Clear selection",
			description: "Clear the active page-backed selection state and rematerialize the official page view.",
			primitive: "reset",
			category: "selection",
			paramsSchema: {
				type: "object",
				properties: {}
			},
			effects: [makeSelectionEffect(null, "Clears the current page-backed selection.")]
		}),
		makeActionDescriptor({
			name: "widget.resetView",
			title: "Reset view",
			description: "Reset the official page view by clearing page-backed selection and domain parameters.",
			primitive: "reset",
			category: "viewTransform",
			paramsSchema: {
				type: "object",
				properties: {}
			},
			effects: [makeSelectionEffect(null, "Clears the current page-backed selections."), makeDomainEffect(null, "Clears page-backed domain selections.")]
		}),
		makeActionDescriptor({
			name: "widget.filterByValues",
			title: "Filter by values",
			description: "Filter the official page view to rows matching one or more categorical values when the underlying Vega-Lite spec exposes a point selection parameter.",
			primitive: "filter",
			category: "dataTransform",
			paramsSchema: {
				type: "object",
				required: ["field", "values"],
				properties: {
					field: { type: "string" },
					values: {
						type: "array",
						items: {},
						minItems: 1
					}
				}
			},
			effects: [makeSelectionEffect(null, "Updates the page-backed point selection used for filtering.")]
		})
	];
	//#endregion
	//#region ../../widgetva-kit/src/index.js
	var DEFAULT_WIDGETVA_AGENT_MODEL = DEFAULT_OPENROUTER_AGENT_MODEL;
	async function runWidgetVAAgentTurn({ target = null, completeChat = null, ...options } = {}) {
		const port = resolveAgentTargetPort(target, options);
		if (typeof completeChat === "function") return runNaturalLanguageAgentTurn(port, {
			...options,
			completeChat
		});
		return runAgentTurn(port, options);
	}
	async function runWidgetVAAgentSession({ target = null, completeChat = null, ...options } = {}) {
		const port = resolveAgentTargetPort(target, options);
		if (typeof completeChat === "function") return runNaturalLanguageAgentSession(port, {
			...options,
			completeChat
		});
		return runAgentSession(port, options);
	}
	//#endregion
	//#region extension/src/shared/officialPageAgentBridge.js
	var WIDGETVA_AGENT_BRIDGE_SOURCE_PAGE = "widgetva-official-page-agent-page";
	var WIDGETVA_AGENT_BRIDGE_REQUEST = "widgetva:official-page-agent-request";
	//#endregion
	//#region extension/src/page/officialPageAgentClient.js
	var DEFAULT_OFFICIAL_PAGE_AGENT_BRIDGE_TIMEOUT_MS = 15e3;
	var DEFAULT_OFFICIAL_PAGE_AGENT_CHAT_TIMEOUT_MS = 45e3;
	function createOfficialPageBridgeError({ code, method, message } = {}) {
		const error = new Error(message || `WidgetVA official-page bridge failed: ${method || "unknown"}.`);
		error.name = "WidgetVAOfficialPageBridgeError";
		error.code = code || "bridge_error";
		error.bridgeMethod = method || null;
		return error;
	}
	function shouldRetryOfficialPageBridgeError(error) {
		return error?.name === "WidgetVAOfficialPageBridgeError" && (error?.code === "bridge_timeout" || error?.code === "bridge_post_error" || error?.code === "bridge_response_error");
	}
	function isRecoverableOfficialPageBridgeError(error) {
		return shouldRetryOfficialPageBridgeError(error);
	}
	function wait(ms) {
		return new Promise((resolve) => {
			setTimeout(resolve, ms);
		});
	}
	function invokeBridgeOnce(root, method, params = null, { timeoutMs = DEFAULT_OFFICIAL_PAGE_AGENT_BRIDGE_TIMEOUT_MS } = {}) {
		const requestId = `widgetva_agent_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
		return new Promise((resolve, reject) => {
			const timeout = setTimeout(() => {
				cleanup();
				reject(createOfficialPageBridgeError({
					code: "bridge_timeout",
					method,
					message: `Timed out waiting for WidgetVA official-page agent bridge: ${method} after ${timeoutMs}ms.`
				}));
			}, timeoutMs);
			const cleanup = () => {
				clearTimeout(timeout);
				root.removeEventListener("message", handleMessage);
			};
			const handleMessage = (event) => {
				const message = event?.data;
				if (event.source !== root || !message || message.source !== "widgetva-official-page-agent-content" || message.type !== "widgetva:official-page-agent-response" || message.id !== requestId) return;
				cleanup();
				if (message.ok === false) {
					reject(createOfficialPageBridgeError({
						code: "bridge_response_error",
						method,
						message: message?.error?.message || `WidgetVA official-page agent bridge failed: ${method}.`
					}));
					return;
				}
				resolve(message.result ?? null);
			};
			root.addEventListener("message", handleMessage);
			try {
				root.postMessage({
					source: WIDGETVA_AGENT_BRIDGE_SOURCE_PAGE,
					type: WIDGETVA_AGENT_BRIDGE_REQUEST,
					id: requestId,
					method,
					params
				}, "*");
			} catch (error) {
				cleanup();
				reject(createOfficialPageBridgeError({
					code: "bridge_post_error",
					method,
					message: error?.message || `Failed to post WidgetVA official-page bridge request: ${method}.`
				}));
			}
		});
	}
	async function invokeBridge(root, method, params = null, { timeoutMs = DEFAULT_OFFICIAL_PAGE_AGENT_BRIDGE_TIMEOUT_MS, retryCount = 0, retryDelayMs = 150 } = {}) {
		let attempt = 0;
		while (true) try {
			return await invokeBridgeOnce(root, method, params, { timeoutMs });
		} catch (error) {
			if (attempt >= retryCount || !shouldRetryOfficialPageBridgeError(error)) throw error;
			attempt += 1;
			await wait(retryDelayMs);
		}
	}
	function configureOfficialPageAgent(root, params = {}, options = {}) {
		return invokeBridge(root, "configure", params, {
			retryCount: 1,
			retryDelayMs: 150,
			...options
		});
	}
	function readOfficialPageAgentConfig(root, options = {}) {
		return invokeBridge(root, "readConfig", null, {
			retryCount: 1,
			retryDelayMs: 150,
			...options
		});
	}
	async function completeOfficialPageAgentChat(root, request = {}) {
		const { timeoutMs = DEFAULT_OFFICIAL_PAGE_AGENT_CHAT_TIMEOUT_MS, retryCount = 1, retryDelayMs = 150, ...chatRequest } = request || {};
		const result = await invokeBridge(root, "chat", chatRequest, {
			timeoutMs,
			retryCount,
			retryDelayMs
		});
		return {
			model: result?.model || chatRequest?.model || null,
			raw: result?.raw || null,
			content: result?.content || ""
		};
	}
	//#endregion
	//#region extension/src/page/officialPageRuntimeManager.js
	function clone$1(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	async function readOfficialPageRecoverableState(controller = null) {
		if (!controller || typeof controller !== "object") return null;
		if (typeof controller.readRecoverableState === "function") return controller.readRecoverableState();
		if (typeof controller.readState === "function") return controller.readState();
		if (typeof controller?.pagePort?.readState === "function") return controller.pagePort.readState();
		if (typeof controller?.runtime?.readState === "function") return controller.runtime.readState();
		return null;
	}
	function resolveOfficialPageRuntime(controller = null, binding = null) {
		return controller?.runtime || controller?.widget?.runtime || binding?.runtime || controller?.pagePort?.runtime || null;
	}
	function createOfficialPageControllerManager({ readRecoverableState = readOfficialPageRecoverableState, restoreRecoverableState = tryRestoreOfficialPageRecoverableState } = {}) {
		let activeRuntime = null;
		let activeController = null;
		let activeBinding = null;
		let attachmentMetadata = null;
		let recoverableState = null;
		let replacementCount = 0;
		async function captureRecoverableState(controller = activeController) {
			const nextState = await readRecoverableState(controller);
			if (nextState != null) recoverableState = clone$1(nextState);
			return clone$1(recoverableState);
		}
		async function attachController(controller, { runtime = null, binding = null, metadata = null } = {}) {
			activeController = controller || null;
			activeBinding = binding || controller?.pagePort || null;
			activeRuntime = runtime || resolveOfficialPageRuntime(activeController, activeBinding);
			attachmentMetadata = metadata ? clone$1(metadata) : null;
			return activeController;
		}
		async function replaceController(nextController, { runtime = null, binding = null, metadata = null, capturePreviousState = true, captureNextState = false, restorePreviousState = false, disposePrevious = true } = {}) {
			const previousRuntime = activeRuntime;
			const previousController = activeController;
			const previousBinding = activeBinding;
			const previousMetadata = clone$1(attachmentMetadata);
			if (capturePreviousState && previousController) await captureRecoverableState(previousController);
			activeController = nextController || null;
			activeBinding = binding || nextController?.pagePort || null;
			activeRuntime = runtime || resolveOfficialPageRuntime(activeController, activeBinding);
			attachmentMetadata = metadata ? clone$1(metadata) : null;
			replacementCount += 1;
			if (captureNextState && activeController) await captureRecoverableState(activeController);
			let restoreResult = {
				restored: false,
				reason: restorePreviousState ? "missing-controller-or-state" : "not-requested"
			};
			if (restorePreviousState && activeController && recoverableState != null) restoreResult = await restoreRecoverableState(activeController, clone$1(recoverableState));
			if (disposePrevious && previousController && typeof previousController.dispose === "function") previousController.dispose();
			return {
				previousRuntime,
				previousController,
				previousBinding,
				previousMetadata,
				activeRuntime,
				activeController,
				activeBinding,
				attachmentMetadata: clone$1(attachmentMetadata),
				recoverableState: clone$1(recoverableState),
				restoreResult,
				replacementCount
			};
		}
		async function clearController({ capturePreviousState = true, disposePrevious = true, preserveRecoverableState = true } = {}) {
			const result = await replaceController(null, {
				capturePreviousState,
				captureNextState: false,
				disposePrevious
			});
			if (!preserveRecoverableState) recoverableState = null;
			activeBinding = null;
			attachmentMetadata = null;
			return result;
		}
		function canContinueRequest({ requirePagePort = false } = {}) {
			if (!activeController) return false;
			return !requirePagePort || Boolean(activeBinding || activeController?.pagePort);
		}
		async function runWithActiveController(handler) {
			if (typeof handler !== "function") throw new Error("WidgetVA official page runtime manager requires a handler function.");
			if (!activeController) throw new Error("WidgetVA official page runtime manager does not have an active controller.");
			return handler(activeController);
		}
		function describeRuntimeManager() {
			return {
				hasActiveRuntime: Boolean(activeRuntime),
				hasActiveController: Boolean(activeController),
				hasActiveBinding: Boolean(activeBinding),
				recoverableStateId: recoverableState?.stateId || null,
				replacementCount
			};
		}
		return {
			attachController,
			replaceController,
			clearController,
			captureRecoverableState,
			runWithActiveController,
			readActiveRuntime: () => activeRuntime,
			readActiveController: () => activeController,
			readActiveBinding: () => activeBinding,
			readAttachmentMetadata: () => clone$1(attachmentMetadata),
			readRecoverableState: () => clone$1(recoverableState),
			describeRuntimeManager,
			canContinueRequest
		};
	}
	function ensureOfficialPageRuntimeManager(entry) {
		if (!entry || typeof entry !== "object") throw new Error("ensureOfficialPageRuntimeManager requires a bootstrap entry object.");
		if (!entry.manager || typeof entry.manager !== "object") entry.manager = createOfficialPageControllerManager();
		return entry.manager;
	}
	function readManagedController(entry) {
		return entry?.manager?.readActiveController?.() ?? null;
	}
	function readManagedRuntime(entry) {
		return entry?.manager?.readActiveRuntime?.() ?? null;
	}
	function readManagedBinding(entry) {
		return entry?.manager?.readActiveBinding?.() ?? null;
	}
	function createManagedPagePort(entry) {
		return new Proxy({}, { get(_target, property) {
			const resolveBinding = () => readManagedBinding(entry) || readManagedController(entry)?.pagePort || null;
			const value = resolveBinding()?.[property];
			if (typeof value !== "function") return value;
			return (...args) => {
				const currentBinding = resolveBinding();
				const currentValue = currentBinding?.[property];
				if (typeof currentValue !== "function") throw new Error(`WidgetVA official page runtime does not expose pagePort.${String(property)}().`);
				return currentValue.apply(currentBinding, args);
			};
		} });
	}
	function canContinueOfficialPageRequest(entry) {
		const manager = entry?.manager;
		if (!manager || typeof manager.canContinueRequest !== "function") return Boolean(readManagedController(entry));
		return manager.canContinueRequest({ requirePagePort: true });
	}
	function isRecoverableOfficialPageRuntimeError(error) {
		const message = error?.message || "";
		return message.includes("WidgetVA official page runtime is not ready.") || message.includes("WidgetVA official page runtime does not expose pagePort.") || message.includes("RuntimeManager does not have an active controller.");
	}
	async function ensureOfficialPageRequestReady(entry, rebootstrap = null, { forceReattach = false } = {}) {
		if (typeof rebootstrap === "function") await rebootstrap({ forceReattach });
		if (!canContinueOfficialPageRequest(entry)) throw new Error("WidgetVA official page runtime is not ready.");
	}
	async function attachOfficialPageController(entry, controller, { restoreRecoverableState = true, runtime = null, binding = null, metadata = null } = {}) {
		const manager = ensureOfficialPageRuntimeManager(entry);
		const hasManagedController = Boolean(readManagedController(entry));
		const hasRecoverableState = manager.readRecoverableState() != null;
		return manager.replaceController(controller, {
			runtime,
			binding,
			metadata,
			captureNextState: !hasManagedController && !hasRecoverableState,
			restorePreviousState: restoreRecoverableState
		});
	}
	async function clearOfficialPageRuntime(entry, { disposePrevious = true, capturePreviousState = false, preserveRecoverableState = false } = {}) {
		return ensureOfficialPageRuntimeManager(entry).clearController({
			disposePrevious,
			capturePreviousState,
			preserveRecoverableState
		});
	}
	async function runOfficialPageWithManager(entry, handler, { rebootstrap = null, retryRecoverableBridgeError = false, isRecoverableBridgeError = null } = {}) {
		const manager = ensureOfficialPageRuntimeManager(entry);
		await ensureOfficialPageRequestReady(entry, rebootstrap);
		const run = () => manager.runWithActiveController(handler);
		try {
			return await run();
		} catch (error) {
			const recoverableBridgeError = retryRecoverableBridgeError && typeof isRecoverableBridgeError === "function" && isRecoverableBridgeError(error);
			const recoverableRuntimeError = isRecoverableOfficialPageRuntimeError(error);
			if (!recoverableBridgeError && !recoverableRuntimeError) throw error;
			await ensureOfficialPageRequestReady(entry, rebootstrap, { forceReattach: true });
			return run();
		}
	}
	async function tryRestoreOfficialPageRecoverableState(controller, recoverableState) {
		if (!controller || !recoverableState) return {
			restored: false,
			reason: "missing-controller-or-state"
		};
		if (typeof controller.restoreRecoverableState === "function") try {
			await controller.restoreRecoverableState(recoverableState);
			return {
				restored: true,
				method: "restoreRecoverableState"
			};
		} catch (error) {
			return {
				restored: false,
				method: "restoreRecoverableState",
				error: summarizeManagedRuntimeError(error)
			};
		}
		const stateId = recoverableState?.stateId || recoverableState?.shared?.stateId || null;
		const pagePort = controller.pagePort || null;
		if (stateId && typeof pagePort?.jumpToState === "function") try {
			await pagePort.jumpToState({ stateId });
			return {
				restored: true,
				method: "jumpToState",
				stateId
			};
		} catch (error) {
			return {
				restored: false,
				method: "jumpToState",
				stateId,
				error: summarizeManagedRuntimeError(error)
			};
		}
		return {
			restored: false,
			reason: "no-restore-surface"
		};
	}
	function summarizeManagedRuntimeError(error) {
		if (error instanceof Error) return {
			name: error.name,
			message: error.message,
			...error.code ? { code: error.code } : {}
		};
		return {
			name: "Error",
			message: String(error || "Unknown WidgetVA official page runtime error.")
		};
	}
	//#endregion
	//#region extension/src/page/officialPageAgentRunners.js
	function readOfficialPageChatTimeout(normalized) {
		if (!normalized || typeof normalized !== "object") return;
		return normalized.chatTimeoutMs ?? normalized.timeoutMs;
	}
	function readOfficialPageMaxTurns(normalized) {
		if (!normalized || typeof normalized !== "object") return;
		return normalized.maxTurns;
	}
	function resolveManagedAgentTarget(controller = null) {
		return typeof controller?.workspace?.readObservation === "function" ? controller.workspace : controller;
	}
	function readManagedAgentTarget(controller = null) {
		const target = resolveManagedAgentTarget(controller);
		if (!target) throw new Error("WidgetVA official page agent target is unavailable.");
		return target;
	}
	function createOfficialPageNaturalLanguageTurnRunner(root) {
		return async (target, options = {}) => {
			const normalized = typeof options === "string" ? { objective: options } : { ...options || {} };
			return runWidgetVAAgentTurn({
				target,
				objective: normalized.objective || normalized.prompt || null,
				model: normalized.model || DEFAULT_WIDGETVA_AGENT_MODEL,
				temperature: normalized.temperature,
				completeChat: (request) => completeOfficialPageAgentChat(root, {
					...request,
					timeoutMs: readOfficialPageChatTimeout(normalized)
				})
			});
		};
	}
	function createOfficialPageNaturalLanguageLoopRunner(root) {
		return async (target, options = {}) => {
			const normalized = typeof options === "string" ? { objective: options } : { ...options || {} };
			return runWidgetVAAgentSession({
				target,
				objective: normalized.objective || normalized.prompt || null,
				model: normalized.model || DEFAULT_WIDGETVA_AGENT_MODEL,
				temperature: normalized.temperature,
				maxTurns: readOfficialPageMaxTurns(normalized),
				onTurn: typeof normalized.onTurn === "function" ? normalized.onTurn : void 0,
				completeChat: (request) => completeOfficialPageAgentChat(root, {
					...request,
					timeoutMs: readOfficialPageChatTimeout(normalized)
				})
			});
		};
	}
	function createOfficialPageManagedRequestRunner({ entry, root, pagePort, runAgentLoop, runNaturalLanguageTurn, runNaturalLanguageLoop, rebootstrap, configureAgent, readAgentConfig } = {}) {
		const runPagePortMethod = (methodName, payload = null) => runOfficialPageWithManager(entry, async () => {
			if (typeof pagePort?.[methodName] !== "function") throw new Error(`WidgetVA official page pagePort.${methodName}() is unavailable.`);
			return payload == null ? pagePort[methodName]() : pagePort[methodName](payload);
		}, { rebootstrap });
		return async function request(incoming = {}) {
			if (typeof incoming === "string") return runOfficialPageWithManager(entry, (currentController) => runNaturalLanguageTurn(readManagedAgentTarget(currentController), incoming), {
				rebootstrap,
				retryRecoverableBridgeError: true,
				isRecoverableBridgeError: isRecoverableOfficialPageBridgeError
			});
			const normalized = incoming && typeof incoming === "object" ? { ...incoming } : {};
			const mode = normalized.mode || normalized.kind || "naturalLanguageSession";
			if (mode === "runAgentLoop" || mode === "agentLoop") return runOfficialPageWithManager(entry, (currentController) => {
				if (typeof currentController?.runAgentLoop !== "function") throw new Error("WidgetVA official page agent loop is unavailable.");
				return runAgentLoop(currentController, normalized.options || {});
			}, { rebootstrap });
			if (mode === "runNaturalLanguageTurn" || mode === "naturalLanguageTurn") return runOfficialPageWithManager(entry, (currentController) => runNaturalLanguageTurn(readManagedAgentTarget(currentController), normalized.options || normalized), {
				rebootstrap,
				retryRecoverableBridgeError: true,
				isRecoverableBridgeError: isRecoverableOfficialPageBridgeError
			});
			if (mode === "runNaturalLanguageLoop" || mode === "naturalLanguageSession") return runOfficialPageWithManager(entry, (currentController) => runNaturalLanguageLoop(readManagedAgentTarget(currentController), normalized.options || normalized), {
				rebootstrap,
				retryRecoverableBridgeError: true,
				isRecoverableBridgeError: isRecoverableOfficialPageBridgeError
			});
			if (mode === "describeWorkspace") return runPagePortMethod("describeWorkspace", normalized.options || normalized.payload || void 0);
			if (mode === "readObservation") return runPagePortMethod("readObservation", normalized.options || normalized.payload || void 0);
			if (mode === "executeAction") return runPagePortMethod("executeAction", normalized.options || normalized.call || normalized.payload || {});
			if (mode === "executeVerifiedAction" || mode === "runVerifiedAction") return runPagePortMethod("executeVerifiedAction", normalized.options || normalized.call || normalized.payload || {});
			if (mode === "queryPerception" || mode === "perceptionQuery") return runPagePortMethod("queryPerception", normalized.options || normalized.call || normalized.payload || {});
			if (mode === "runDataQuery" || mode === "queryData") return runPagePortMethod("runDataQuery", normalized.options || normalized.call || normalized.payload || {});
			if (mode === "readLatestCoordinationResult" || mode === "latestCoordinationResult") return runPagePortMethod("readLatestCoordinationResult");
			if (mode === "configureAgent" || mode === "configure") return configureAgent(root, normalized.options || normalized);
			if (mode === "readAgentConfig" || mode === "readConfig") return readAgentConfig(root);
			throw new Error(`Unsupported WidgetVA official page request mode: ${mode}.`);
		};
	}
	//#endregion
	//#region extension/src/page/officialPageRuntimeBindings.js
	function readControllerWorkspace(controller = null) {
		if (typeof controller?.describeAgentContract === "function") {
			const contract = controller.describeAgentContract();
			if (contract?.workspace && typeof contract.workspace === "object") return contract.workspace;
			if (contract?.widget && typeof contract.widget === "object") return { widgets: [contract.widget] };
		}
		return null;
	}
	async function readControllerWorkspaceAsync(controller = null) {
		const syncWorkspace = readControllerWorkspace(controller);
		if (syncWorkspace) return syncWorkspace;
		if (typeof controller?.pagePort?.describeWorkspace === "function") return controller.pagePort.describeWorkspace();
		return null;
	}
	function readControllerPrimaryWidget(controller = null) {
		const workspace = readControllerWorkspace(controller);
		if (Array.isArray(workspace?.widgets) && workspace.widgets.length > 0) return workspace.widgets[0] || null;
		return null;
	}
	async function readControllerPrimaryWidgetAsync(controller = null) {
		const syncWidget = readControllerPrimaryWidget(controller);
		if (syncWidget) return syncWidget;
		const workspace = await readControllerWorkspaceAsync(controller);
		if (Array.isArray(workspace?.widgets) && workspace.widgets.length > 0) return workspace.widgets[0] || null;
		return null;
	}
	async function readOfficialPageObservation(controller = null) {
		if (typeof controller?.pagePort?.readObservation === "function") return controller.pagePort.readObservation();
		if (typeof controller?.widget?.readObservation === "function") return controller.widget.readObservation();
		return null;
	}
	function extractSharedSelectionsFromObservation(observation = null) {
		return observation?.sharedAnalyticalState?.selections || observation?.sharedAnalyticalState?.activeSelections || observation?.state?.shared?.activeSelections || observation?.state?.sharedAnalyticalState?.selections || null;
	}
	async function resolveOfficialPageActionScope(controller = null, explicitQueryScope = null) {
		const normalizedQueryScope = explicitQueryScope && typeof explicitQueryScope === "object" && !Array.isArray(explicitQueryScope) ? { ...explicitQueryScope } : {};
		if (!normalizedQueryScope.widgetRef && !normalizedQueryScope.widget_ref) {
			const widget = await readControllerPrimaryWidgetAsync(controller);
			if (widget?.ref) normalizedQueryScope.widgetRef = widget.ref;
		}
		return normalizedQueryScope;
	}
	function normalizeOfficialPageActionCall(actionNameOrCall = null, params = null, options = {}) {
		if (actionNameOrCall && typeof actionNameOrCall === "object" && !Array.isArray(actionNameOrCall)) return { ...actionNameOrCall };
		if (typeof actionNameOrCall !== "string" || actionNameOrCall.length === 0) throw new Error("WidgetVA official page action helpers require an action name or action-call object.");
		const normalized = { name: actionNameOrCall };
		if (params && typeof params === "object" && !Array.isArray(params)) normalized.params = { ...params };
		if (options?.callId) normalized.callId = options.callId;
		if (options?.actor) normalized.actor = options.actor;
		if (options?.queryScope && typeof options.queryScope === "object" && !Array.isArray(options.queryScope)) normalized.queryScope = { ...options.queryScope };
		return normalized;
	}
	function normalizeOfficialPageObjectiveCall(objectiveOrOptions = null, options = {}) {
		if (objectiveOrOptions && typeof objectiveOrOptions === "object" && !Array.isArray(objectiveOrOptions)) return { ...objectiveOrOptions };
		if (typeof objectiveOrOptions !== "string" || objectiveOrOptions.length === 0) throw new Error("WidgetVA official page natural-language helpers require an objective string or options object.");
		return {
			objective: objectiveOrOptions,
			...options && typeof options === "object" && !Array.isArray(options) ? { ...options } : {}
		};
	}
	function normalizeOfficialPageQueryCall(queryNameOrCall = null, params = null, options = {}) {
		if (queryNameOrCall && typeof queryNameOrCall === "object" && !Array.isArray(queryNameOrCall)) return { ...queryNameOrCall };
		if (typeof queryNameOrCall !== "string" || queryNameOrCall.length === 0) throw new Error("WidgetVA official page query helpers require a query name or query-call object.");
		const normalized = { name: queryNameOrCall };
		if (params && typeof params === "object" && !Array.isArray(params)) normalized.params = { ...params };
		if (options?.callId) normalized.callId = options.callId;
		if (options?.actor) normalized.actor = options.actor;
		if (options?.queryScope && typeof options.queryScope === "object" && !Array.isArray(options.queryScope)) normalized.queryScope = { ...options.queryScope };
		return normalized;
	}
	function buildPointParamCall({ paramName, field, values, record, ...options } = {}) {
		if (typeof paramName !== "string" || paramName.length === 0) throw new Error("WidgetVA official page point-param helper requires paramName.");
		return normalizeOfficialPageActionCall("vegaLite.setPointParam", {
			paramName,
			...typeof field === "string" && field.length > 0 ? { field } : {},
			...Array.isArray(values) ? { values } : {},
			...record && typeof record === "object" && !Array.isArray(record) ? { record } : {}
		}, options);
	}
	function buildIntervalParamCall({ paramName, xField, xRange, yField, yRange, ...options } = {}) {
		if (typeof paramName !== "string" || paramName.length === 0) throw new Error("WidgetVA official page interval-param helper requires paramName.");
		return normalizeOfficialPageActionCall("vegaLite.setIntervalParam", {
			paramName,
			...typeof xField === "string" && Array.isArray(xRange) ? {
				xField,
				xRange
			} : {},
			...typeof yField === "string" && Array.isArray(yRange) ? {
				yField,
				yRange
			} : {}
		}, options);
	}
	function buildClearParamCall({ paramName, ...options } = {}) {
		if (typeof paramName !== "string" || paramName.length === 0) throw new Error("WidgetVA official page clear-param helper requires paramName.");
		return normalizeOfficialPageActionCall("vegaLite.clearParam", { paramName }, options);
	}
	var LEGACY_OFFICIAL_PAGE_GLOBAL_HELPERS = {
		readEntry: "__widgetVAOfficialPageReadEntry",
		readController: "__widgetVAOfficialPageReadController",
		readWorkspace: "__widgetVAOfficialPageReadWorkspace",
		readWidget: "__widgetVAOfficialPageReadWidget",
		readWidgetRef: "__widgetVAOfficialPageReadWidgetRef",
		readObservation: "__widgetVAOfficialPageReadObservation",
		readSelections: "__widgetVAOfficialPageReadSelections",
		executeAction: "__widgetVAOfficialPageExecuteAction",
		executeVerifiedAction: "__widgetVAOfficialPageExecuteVerifiedAction",
		queryPerception: "__widgetVAOfficialPageQueryPerception",
		runDataQuery: "__widgetVAOfficialPageRunDataQuery",
		readLatestCoordinationResult: "__widgetVAOfficialPageReadLatestCoordinationResult",
		runAction: "__widgetVAOfficialPageRunAction",
		runVerifiedAction: "__widgetVAOfficialPageRunVerifiedAction",
		runObjective: "__widgetVAOfficialPageRunObjective",
		runObjectiveLoop: "__widgetVAOfficialPageRunObjectiveLoop",
		setPointParam: "__widgetVAOfficialPageSetPointParam",
		setIntervalParam: "__widgetVAOfficialPageSetIntervalParam",
		clearParam: "__widgetVAOfficialPageClearParam",
		clearSelection: "__widgetVAOfficialPageClearSelection",
		inspectVisibleRows: "__widgetVAOfficialPageInspectVisibleRows",
		summarizeVisible: "__widgetVAOfficialPageSummarizeVisible",
		inspectSelection: "__widgetVAOfficialPageInspectSelection",
		summarizeSelection: "__widgetVAOfficialPageSummarizeSelection",
		request: "__widgetVAOfficialPageRequest",
		runAgentLoop: "__widgetVAOfficialPageRunAgentLoop",
		runNaturalLanguageAgentTurn: "__widgetVAOfficialPageRunNaturalLanguageAgentTurn",
		runNaturalLanguageAgentLoop: "__widgetVAOfficialPageRunNaturalLanguageAgentLoop",
		configureAgent: "__widgetVAOfficialPageConfigureAgent",
		readAgentConfig: "__widgetVAOfficialPageReadAgentConfig"
	};
	var LEGACY_OFFICIAL_PAGE_GLOBAL_METHODS = Object.fromEntries(Object.entries(LEGACY_OFFICIAL_PAGE_GLOBAL_HELPERS).map(([methodName, globalName]) => [globalName, methodName]));
	function installOfficialPageHelperGlobals(root, stableBindings, names = []) {
		for (const globalName of names) {
			const methodName = LEGACY_OFFICIAL_PAGE_GLOBAL_METHODS[globalName];
			if (!methodName) continue;
			root[globalName] = stableBindings[methodName];
		}
	}
	function clearOfficialPageHelperGlobals(root, names = []) {
		for (const globalName of names) root[globalName] = void 0;
	}
	function bindOfficialPageAgentRuntime({ entry, root, controller, request, runAgentLoop, runNaturalLanguageAgentTurn, runNaturalLanguageAgentLoop, configureAgent, readAgentConfig, extraBindings, stableSurfaceName = null, exposeLegacyWindowEntry = true, exposeLegacyWindowController = true, exposeLegacyWindowHelpers = true } = {}) {
		const activeController = readManagedController(entry) || controller || null;
		entry.controller = activeController;
		entry.runtime = readManagedRuntime(entry) || activeController?.runtime || activeController?.widget?.runtime || null;
		entry.pagePort = activeController?.pagePort || root?.__widgetVA || null;
		entry.request = typeof request === "function" ? request : null;
		entry.runAgentLoop = typeof runAgentLoop === "function" ? runAgentLoop : null;
		entry.runNaturalLanguageAgentTurn = typeof runNaturalLanguageAgentTurn === "function" ? runNaturalLanguageAgentTurn : null;
		entry.runNaturalLanguageAgentLoop = typeof runNaturalLanguageAgentLoop === "function" ? runNaturalLanguageAgentLoop : null;
		entry.configureAgent = configureAgent || null;
		entry.readAgentConfig = readAgentConfig || null;
		const stableBindings = {
			readEntry: () => entry,
			readController: () => readManagedController(entry) || activeController || null,
			readWorkspace: async () => readControllerWorkspaceAsync(readManagedController(entry) || activeController || null),
			readWidget: async () => readControllerPrimaryWidgetAsync(readManagedController(entry) || activeController || null),
			readWidgetRef: async () => {
				return (await readControllerPrimaryWidgetAsync(readManagedController(entry) || activeController || null))?.ref || null;
			},
			readObservation: async () => readOfficialPageObservation(readManagedController(entry) || activeController || null),
			readSelections: async () => {
				return extractSharedSelectionsFromObservation(await readOfficialPageObservation(readManagedController(entry) || activeController || null));
			},
			executeAction: async (call = {}) => {
				const currentController = readManagedController(entry) || activeController || null;
				const pagePort = currentController?.pagePort || entry.pagePort || root.__widgetVA || null;
				if (typeof pagePort?.executeAction !== "function") throw new Error("WidgetVA official page runtime does not expose executeAction().");
				const queryScope = await resolveOfficialPageActionScope(currentController, call?.queryScope);
				return pagePort.executeAction({
					...call || {},
					...Object.keys(queryScope).length > 0 ? { queryScope } : {}
				});
			},
			executeVerifiedAction: async (call = {}) => {
				const currentController = readManagedController(entry) || activeController || null;
				const pagePort = currentController?.pagePort || entry.pagePort || root.__widgetVA || null;
				if (typeof pagePort?.executeVerifiedAction !== "function") throw new Error("WidgetVA official page runtime does not expose executeVerifiedAction().");
				const queryScope = await resolveOfficialPageActionScope(currentController, call?.queryScope);
				return pagePort.executeVerifiedAction({
					...call || {},
					...Object.keys(queryScope).length > 0 ? { queryScope } : {}
				});
			},
			queryPerception: async (queryNameOrCall, params = null, options = {}) => {
				const currentController = readManagedController(entry) || activeController || null;
				const pagePort = currentController?.pagePort || entry.pagePort || root.__widgetVA || null;
				if (typeof pagePort?.queryPerception !== "function") throw new Error("WidgetVA official page runtime does not expose queryPerception().");
				const call = normalizeOfficialPageQueryCall(queryNameOrCall, params, options);
				const queryScope = await resolveOfficialPageActionScope(currentController, call?.queryScope);
				return pagePort.queryPerception({
					...call,
					...Object.keys(queryScope).length > 0 ? { queryScope } : {}
				});
			},
			runDataQuery: async (queryNameOrCall, params = null, options = {}) => {
				const currentController = readManagedController(entry) || activeController || null;
				const pagePort = currentController?.pagePort || entry.pagePort || root.__widgetVA || null;
				if (typeof pagePort?.runDataQuery !== "function") throw new Error("WidgetVA official page runtime does not expose runDataQuery().");
				const call = normalizeOfficialPageQueryCall(queryNameOrCall, params, options);
				const queryScope = await resolveOfficialPageActionScope(currentController, call?.queryScope);
				return pagePort.runDataQuery({
					...call,
					...Object.keys(queryScope).length > 0 ? { queryScope } : {}
				});
			},
			readLatestCoordinationResult: async () => {
				const pagePort = (readManagedController(entry) || activeController || null)?.pagePort || entry.pagePort || root.__widgetVA || null;
				if (typeof pagePort?.readLatestCoordinationResult !== "function") throw new Error("WidgetVA official page runtime does not expose readLatestCoordinationResult().");
				return pagePort.readLatestCoordinationResult();
			}
		};
		stableBindings.restoreRecoverableState = async (state = {}) => {
			const currentController = readManagedController(entry) || activeController || null;
			if (typeof currentController?.restoreRecoverableState === "function") return currentController.restoreRecoverableState(state);
			const pagePort = currentController?.pagePort || entry.pagePort || root.__widgetVA || null;
			if (typeof pagePort?.jumpToState === "function") return pagePort.jumpToState({ stateId: state?.stateId });
			throw new Error("WidgetVA official page runtime does not expose restoreRecoverableState().");
		};
		stableBindings.jumpToState = async (options = {}) => stableBindings.restoreRecoverableState(options);
		stableBindings.runAction = async (actionNameOrCall, params = null, options = {}) => {
			const call = normalizeOfficialPageActionCall(actionNameOrCall, params, options);
			return stableBindings.executeAction(call);
		};
		stableBindings.runVerifiedAction = async (actionNameOrCall, params = null, options = {}) => {
			const call = normalizeOfficialPageActionCall(actionNameOrCall, params, options);
			return stableBindings.executeVerifiedAction(call);
		};
		stableBindings.runObjective = async (objectiveOrOptions, options = {}) => {
			const normalized = normalizeOfficialPageObjectiveCall(objectiveOrOptions, options);
			if (typeof entry.runNaturalLanguageAgentTurn !== "function") throw new Error("WidgetVA official page runtime does not expose runNaturalLanguageAgentTurn().");
			return entry.runNaturalLanguageAgentTurn(normalized);
		};
		stableBindings.runObjectiveLoop = async (objectiveOrOptions, options = {}) => {
			const normalized = normalizeOfficialPageObjectiveCall(objectiveOrOptions, options);
			if (typeof entry.runNaturalLanguageAgentLoop !== "function") throw new Error("WidgetVA official page runtime does not expose runNaturalLanguageAgentLoop().");
			return entry.runNaturalLanguageAgentLoop(normalized);
		};
		stableBindings.setPointParam = async (options = {}) => {
			const call = buildPointParamCall(options);
			return stableBindings.executeVerifiedAction(call);
		};
		stableBindings.setIntervalParam = async (options = {}) => {
			const call = buildIntervalParamCall(options);
			return stableBindings.executeVerifiedAction(call);
		};
		stableBindings.clearParam = async (options = {}) => {
			const call = buildClearParamCall(options);
			return stableBindings.executeVerifiedAction(call);
		};
		stableBindings.clearSelection = async (options = {}) => {
			const call = normalizeOfficialPageActionCall("widget.clearSelection", null, options);
			return stableBindings.executeVerifiedAction(call);
		};
		stableBindings.inspectVisibleRows = async (params = {}, options = {}) => stableBindings.queryPerception("perception.inspectVisibleRows", params, options);
		stableBindings.summarizeVisible = async (params = {}, options = {}) => stableBindings.queryPerception("perception.summarizeVisible", params, options);
		stableBindings.inspectSelection = async (params = {}, options = {}) => stableBindings.queryPerception("perception.inspectSelection", params, options);
		stableBindings.summarizeSelection = async (params = {}, options = {}) => stableBindings.queryPerception("perception.summarizeSelection", params, options);
		stableBindings.request = entry.request;
		stableBindings.runAgentLoop = entry.runAgentLoop;
		stableBindings.runNaturalLanguageAgentTurn = entry.runNaturalLanguageAgentTurn;
		stableBindings.runNaturalLanguageAgentLoop = entry.runNaturalLanguageAgentLoop;
		stableBindings.configureAgent = entry.configureAgent;
		stableBindings.readAgentConfig = entry.readAgentConfig;
		entry.api = stableBindings;
		entry.publicBindingConfig = {
			stableSurfaceName: typeof stableSurfaceName === "string" && stableSurfaceName.length > 0 ? stableSurfaceName : null,
			exposeLegacyWindowEntry: exposeLegacyWindowEntry !== false,
			exposeLegacyWindowController: exposeLegacyWindowController !== false,
			helperNames: exposeLegacyWindowHelpers === false ? [] : Object.values(LEGACY_OFFICIAL_PAGE_GLOBAL_HELPERS)
		};
		if (entry.publicBindingConfig.exposeLegacyWindowEntry) {
			root.__widgetVAOfficialPage = entry;
			root.__widgetVAOfficialPageEntry = entry;
		}
		if (entry.publicBindingConfig.exposeLegacyWindowController) root.__widgetVAOfficialPageController = activeController;
		if (entry.publicBindingConfig.stableSurfaceName) root[entry.publicBindingConfig.stableSurfaceName] = stableBindings;
		installOfficialPageHelperGlobals(root, stableBindings, entry.publicBindingConfig.helperNames);
		if (typeof extraBindings === "function") extraBindings(activeController);
	}
	function clearOfficialPageAgentRuntime({ entry, root, clearWidgetVA = false, clearExtraBindings } = {}) {
		entry.controller = null;
		entry.runtime = null;
		entry.pagePort = null;
		entry.request = null;
		entry.runAgentLoop = null;
		entry.runNaturalLanguageAgentTurn = null;
		entry.runNaturalLanguageAgentLoop = null;
		entry.configureAgent = null;
		entry.readAgentConfig = null;
		entry.api = null;
		const publicBindingConfig = entry.publicBindingConfig || null;
		entry.publicBindingConfig = null;
		entry.promise = null;
		if (publicBindingConfig?.exposeLegacyWindowEntry) {
			root.__widgetVAOfficialPage = void 0;
			root.__widgetVAOfficialPageEntry = void 0;
		}
		if (publicBindingConfig?.exposeLegacyWindowController) root.__widgetVAOfficialPageController = void 0;
		if (publicBindingConfig?.stableSurfaceName) root[publicBindingConfig.stableSurfaceName] = void 0;
		clearOfficialPageHelperGlobals(root, publicBindingConfig?.helperNames || []);
		if (typeof clearExtraBindings === "function") clearExtraBindings();
		if (clearWidgetVA && root?.__widgetVA) try {
			delete root.__widgetVA;
		} catch {
			root.__widgetVA = void 0;
		}
	}
	//#endregion
	//#region extension/src/page/officialPageBootstrapLifecycle.js
	async function clearOfficialPageBootstrapRuntime({ entry, root, capturePreviousState = false, preserveRecoverableState = false, clearWidgetVA = false, clearExtraBindings, resetEntry } = {}) {
		await clearOfficialPageRuntime(entry, {
			capturePreviousState,
			preserveRecoverableState
		});
		if (typeof resetEntry === "function") resetEntry(entry);
		clearOfficialPageAgentRuntime({
			entry,
			root,
			clearWidgetVA,
			clearExtraBindings
		});
	}
	function resetOfficialPageBootstrapBindings({ entry, root, clearWidgetVA = false, clearExtraBindings, resetEntry } = {}) {
		if (typeof resetEntry === "function") resetEntry(entry);
		clearOfficialPageAgentRuntime({
			entry,
			root,
			clearWidgetVA,
			clearExtraBindings
		});
	}
	//#endregion
	//#region extension/src/page/observableD3Bootstrap.js
	var OBSERVABLE_D3_BOOTSTRAP_KEY = "__widgetVAOfficialPageBootstrap";
	var OBSERVABLE_D3_BOOTSTRAP_ENTRY = "observableD3";
	function ensureBootstrapState(root) {
		if (!root["__widgetVAOfficialPageBootstrap"] || typeof root["__widgetVAOfficialPageBootstrap"] !== "object") root[OBSERVABLE_D3_BOOTSTRAP_KEY] = {};
		const state = root[OBSERVABLE_D3_BOOTSTRAP_KEY];
		if (!state["observableD3"] || typeof state["observableD3"] !== "object") state[OBSERVABLE_D3_BOOTSTRAP_ENTRY] = {
			provider: "d3",
			pageType: "official-observable-notebook",
			status: "idle",
			pageShape: null,
			workerFrame: null,
			surface: null,
			controller: null,
			pagePort: null,
			request: null,
			runAgentLoop: null,
			runNaturalLanguageAgentTurn: null,
			runNaturalLanguageAgentLoop: null,
			configureAgent: null,
			readAgentConfig: null,
			error: null,
			pageUrl: null,
			startedAt: null,
			completedAt: null,
			promise: null,
			bootstrapConfig: null
		};
		ensureOfficialPageRuntimeManager(state[OBSERVABLE_D3_BOOTSTRAP_ENTRY]);
		return state[OBSERVABLE_D3_BOOTSTRAP_ENTRY];
	}
	async function clearEntryRuntime(entry, root, { capturePreviousState = false, preserveRecoverableState = false } = {}) {
		await clearOfficialPageBootstrapRuntime({
			entry,
			root,
			capturePreviousState,
			preserveRecoverableState,
			clearWidgetVA: true,
			resetEntry(currentEntry) {
				currentEntry.workerFrame = null;
				currentEntry.surface = null;
			},
			clearExtraBindings() {
				root.__widgetVAObservableD3Debug = void 0;
				root.__widgetVAObservableD3PreviewBrush = void 0;
				root.__widgetVAObservableD3Probe = void 0;
			}
		});
	}
	function resetEntryBindingsForRebootstrap(entry, root) {
		resetOfficialPageBootstrapBindings({
			entry,
			root,
			clearWidgetVA: true,
			resetEntry(currentEntry) {
				currentEntry.workerFrame = null;
				currentEntry.surface = null;
			},
			clearExtraBindings() {
				root.__widgetVAObservableD3Debug = void 0;
				root.__widgetVAObservableD3PreviewBrush = void 0;
				root.__widgetVAObservableD3Probe = void 0;
			}
		});
	}
	function bindReadyEntry(root, entry, controller) {
		const runTurn = createOfficialPageNaturalLanguageTurnRunner(root);
		const runLoop = createOfficialPageNaturalLanguageLoopRunner(root);
		const pagePort = createManagedPagePort(entry);
		const rebootstrap = ({ forceReattach = false } = {}) => ensureObservableD3PageBootstrap({
			...entry.bootstrapConfig || {},
			...forceReattach ? { forceReattach: true } : {}
		});
		const request = createOfficialPageManagedRequestRunner({
			entry,
			root,
			pagePort,
			runAgentLoop: (currentController, options = {}) => currentController.runAgentLoop(options),
			runNaturalLanguageTurn: runTurn,
			runNaturalLanguageLoop: runLoop,
			rebootstrap,
			configureAgent: configureOfficialPageAgent,
			readAgentConfig: readOfficialPageAgentConfig
		});
		bindOfficialPageAgentRuntime({
			entry,
			root,
			controller,
			request,
			runAgentLoop: (options = {}) => request({
				mode: "runAgentLoop",
				options
			}),
			runNaturalLanguageAgentTurn: (options = {}) => request(typeof options === "string" ? {
				mode: "runNaturalLanguageTurn",
				options
			} : {
				mode: "runNaturalLanguageTurn",
				options: options || {}
			}),
			runNaturalLanguageAgentLoop: (options = {}) => request(typeof options === "string" ? {
				mode: "runNaturalLanguageLoop",
				options
			} : {
				mode: "runNaturalLanguageLoop",
				options: options || {}
			}),
			configureAgent: (options = {}) => request({
				mode: "configureAgent",
				options: options || {}
			}),
			readAgentConfig: () => request({ mode: "readAgentConfig" }),
			extraBindings(activeController) {
				root.__widgetVAObservableD3Debug = () => activeController?.readDebugSnapshot?.();
				root.__widgetVAObservableD3PreviewBrush = () => activeController?.previewVisibleBrush?.();
				root.__widgetVAObservableD3Probe = () => activeController?.renderDebugProbe?.();
			}
		});
	}
	async function ensureObservableD3PageBootstrap({ root = globalThis.window, isSupportedPage, describePage, waitForWorkerFrame, bootstrapPage, timeoutMs = 15e3, pollMs = 25, forceReattach = false } = {}) {
		if (!root || typeof root !== "object") throw new Error("ensureObservableD3PageBootstrap requires a page-like root object.");
		if (typeof isSupportedPage !== "function") throw new Error("ensureObservableD3PageBootstrap requires isSupportedPage().");
		if (typeof describePage !== "function") throw new Error("ensureObservableD3PageBootstrap requires describePage().");
		if (typeof bootstrapPage !== "function") throw new Error("ensureObservableD3PageBootstrap requires bootstrapPage().");
		const entry = ensureBootstrapState(root);
		const pageUrl = root?.location?.href || "";
		entry.bootstrapConfig = {
			root,
			isSupportedPage,
			describePage,
			waitForWorkerFrame,
			bootstrapPage,
			timeoutMs,
			pollMs
		};
		if (!isSupportedPage(pageUrl)) {
			if (entry.pageUrl && entry.pageUrl !== pageUrl) await clearEntryRuntime(entry, root);
			entry.status = "unsupported";
			entry.error = null;
			entry.pageUrl = pageUrl;
			entry.promise = null;
			return entry;
		}
		if (entry.pageUrl && entry.pageUrl !== pageUrl) {
			resetEntryBindingsForRebootstrap(entry, root);
			entry.status = "idle";
			entry.error = null;
		}
		if (forceReattach && readManagedController(entry)) {
			await clearEntryRuntime(entry, root, {
				capturePreviousState: true,
				preserveRecoverableState: true
			});
			entry.status = "idle";
			entry.error = null;
		}
		const activeController = readManagedController(entry);
		if (entry.status === "ready" && activeController) {
			entry.pageUrl = pageUrl;
			bindReadyEntry(root, entry, activeController);
			return entry;
		}
		if (entry.promise) return entry.promise;
		entry.status = "booting";
		entry.error = null;
		entry.pageUrl = pageUrl;
		entry.startedAt = Date.now();
		entry.completedAt = null;
		entry.promise = (async () => {
			try {
				const pageShape = describePage(root);
				const controller = await bootstrapPage({
					root,
					sessionId: `official-observable-d3-${pageShape?.notebook?.slug || "page"}`,
					enableExtensionBridge: true,
					timeoutMs,
					pollMs,
					forceReattach
				});
				let workerFrame = null;
				if (typeof waitForWorkerFrame === "function") try {
					workerFrame = await waitForWorkerFrame({
						root,
						timeoutMs: Math.min(timeoutMs, 1e3)
					});
				} catch {}
				await attachOfficialPageController(entry, controller, {
					binding: root.__widgetVA || controller?.pagePort || null,
					metadata: {
						provider: "d3",
						pageType: "official-observable-notebook",
						pageUrl
					}
				});
				entry.status = "ready";
				entry.pageShape = pageShape;
				entry.workerFrame = workerFrame ? { src: workerFrame.getAttribute?.("src") || workerFrame.src || null } : null;
				entry.surface = readManagedController(entry)?.surface || controller?.surface || null;
				bindReadyEntry(root, entry, readManagedController(entry));
				entry.completedAt = Date.now();
				entry.promise = null;
				return entry;
			} catch (error) {
				entry.status = "error";
				entry.error = summarizeManagedRuntimeError(error);
				await clearOfficialPageBootstrapRuntime({
					entry,
					root,
					capturePreviousState: false,
					preserveRecoverableState: false,
					clearWidgetVA: true,
					resetEntry(currentEntry) {
						currentEntry.workerFrame = null;
						currentEntry.surface = null;
					},
					clearExtraBindings() {
						root.__widgetVAObservableD3Debug = void 0;
						root.__widgetVAObservableD3PreviewBrush = void 0;
						root.__widgetVAObservableD3Probe = void 0;
					}
				});
				entry.completedAt = Date.now();
				entry.promise = null;
				throw error;
			}
		})();
		return entry.promise;
	}
	var WIDGETVA_DOCK_BRIDGE_SOURCE_PAGE = "widgetva-dock-page";
	var WIDGETVA_DOCK_BRIDGE_RESPONSE = "widgetva:dock-response";
	//#endregion
	//#region extension/src/page/officialPageDockBridge.js
	function clone(value) {
		return value == null ? value : JSON.parse(JSON.stringify(value));
	}
	function summarizeDockError(error) {
		return {
			name: error?.name || "Error",
			message: error?.message || String(error || "WidgetVA Dock bridge failed.")
		};
	}
	function compactNames(items = []) {
		return Array.from(new Set((Array.isArray(items) ? items : []).map((item) => typeof item === "string" ? item : item?.name).filter((name) => typeof name === "string" && name.length > 0)));
	}
	function labelLink(link = null) {
		if (!link || typeof link !== "object") return "link";
		return `${link.sourceAction || link.sourceStep || link.sourceStateRef || link.sourceWidgetId || link.sourceRef || link.source || "source"} -> ${link.targetAction || link.targetStep || link.targetStateRef || link.targetWidgetId || link.targetRef || link.target || "target"}`;
	}
	function readRoute(root, explicitRoute = null) {
		if (explicitRoute && typeof explicitRoute === "object") return clone(explicitRoute);
		const href = root?.location?.href || "";
		if (href.includes("vega.github.io/vega-lite/examples/")) return {
			provider: "vega-lite",
			pageType: "official-vega-lite-page"
		};
		if (/https:\/\/observablehq\.com\/@[^/]+\/[^/?#]+/.test(href)) return {
			provider: "d3",
			pageType: "official-observable-notebook"
		};
		return {
			provider: "unknown",
			pageType: "unknown-page"
		};
	}
	function readStableSurface(root) {
		return root?.__widgetVAOfficialVegaLitePage || root?.__widgetVAOfficialPageApi || root?.__widgetVAOfficialPage?.api || null;
	}
	function readEntry(root, handlers = {}, explicitEntry = null) {
		if (explicitEntry && typeof explicitEntry === "object") return explicitEntry;
		if (typeof handlers.readEntry === "function") return handlers.readEntry();
		const stableSurface = readStableSurface(root);
		if (typeof stableSurface?.readEntry === "function") return stableSurface.readEntry();
		return root?.__widgetVAOfficialPage || root?.__widgetVAOfficialPageEntry || null;
	}
	function readRuntimeSurface(root, entry = null) {
		return readStableSurface(root) || entry?.api || null;
	}
	async function readWorkspace(root, entry = null) {
		const surface = readRuntimeSurface(root, entry);
		if (typeof surface?.readWorkspace === "function") return surface.readWorkspace();
		return null;
	}
	async function readObservation(root, entry = null) {
		const surface = readRuntimeSurface(root, entry);
		if (typeof surface?.readObservation === "function") return surface.readObservation();
		return null;
	}
	async function readStateHistory(root, entry = null) {
		const surface = readRuntimeSurface(root, entry);
		if (typeof surface?.listStateHistory === "function") return surface.listStateHistory({ limit: 24 });
		return [];
	}
	async function readTrace(root, entry = null) {
		const surface = readRuntimeSurface(root, entry);
		if (typeof surface?.readTrace === "function") return surface.readTrace({ limit: 24 });
		return [];
	}
	async function readRecoverableState(root, entry = null) {
		const surface = readRuntimeSurface(root, entry);
		if (typeof surface?.readRecoverableState === "function") return surface.readRecoverableState();
		if (typeof entry?.readRecoverableState === "function") return entry.readRecoverableState();
		if (typeof entry?.manager?.readRecoverableState === "function") return entry.manager.readRecoverableState();
		return null;
	}
	function readControllerFromSurface(surface = null, entry = null) {
		if (entry?.controller) return entry.controller;
		if (typeof surface?.readController !== "function") return null;
		return surface.readController();
	}
	async function restoreOfficialPageState(root, handlers = {}, recoverableState = {}) {
		const entry = readEntry(root, handlers);
		const surface = readRuntimeSurface(root, entry);
		const controller = readControllerFromSurface(surface, entry);
		const state = recoverableState && typeof recoverableState === "object" && !Array.isArray(recoverableState) ? clone(recoverableState) : {};
		const stateId = state.stateId || null;
		const candidates = [
			[
				"surface.restoreRecoverableState",
				surface?.restoreRecoverableState,
				surface,
				state
			],
			[
				"entry.restoreRecoverableState",
				entry?.restoreRecoverableState,
				entry,
				state
			],
			[
				"controller.restoreRecoverableState",
				controller?.restoreRecoverableState,
				controller,
				state
			],
			[
				"surface.jumpToState",
				surface?.jumpToState,
				surface,
				{ stateId }
			],
			[
				"entry.pagePort.jumpToState",
				entry?.pagePort?.jumpToState,
				entry?.pagePort,
				{ stateId }
			],
			[
				"controller.pagePort.jumpToState",
				controller?.pagePort?.jumpToState,
				controller?.pagePort,
				{ stateId }
			],
			[
				"root.__widgetVA.jumpToState",
				root?.__widgetVA?.jumpToState,
				root?.__widgetVA,
				{ stateId }
			]
		];
		for (const [method, restore, owner, payload] of candidates) {
			if (typeof restore !== "function") continue;
			return {
				restore: clone(await restore.call(owner, payload) || {
					ok: true,
					stateId,
					method
				}),
				snapshot: await buildDockSnapshot(root, handlers, entry)
			};
		}
		throw new Error("WidgetVA official page does not expose a restorable state surface.");
	}
	function summarizeWorkspace(workspace = null) {
		const safeWorkspace = workspace && typeof workspace === "object" ? workspace : {};
		const widget = Array.isArray(safeWorkspace.widgets) ? safeWorkspace.widgets[0] || null : null;
		return {
			widget: widget ? {
				ref: widget.ref || widget.widgetRef || null,
				kind: widget.kind || widget.family || widget.type || null,
				label: widget.title || widget.name || widget.ref || widget.widgetId || widget.kind || "Widget"
			} : {
				ref: null,
				kind: null,
				label: "Widget"
			},
			links: (Array.isArray(safeWorkspace.links) ? safeWorkspace.links : []).map((link) => ({
				label: labelLink(link),
				kind: link?.kind || link?.relation || null,
				source: link?.sourceAction || link?.sourceWidgetId || link?.sourceRef || link?.source || null,
				target: link?.targetAction || link?.targetWidgetId || link?.targetRef || link?.target || null
			})),
			tools: {
				actions: compactNames(safeWorkspace.actions),
				perceptions: compactNames(safeWorkspace.perceptionQueries || safeWorkspace.perceptions || safeWorkspace.queries)
			}
		};
	}
	async function buildDockSnapshot(root, handlers = {}, explicitEntry = null) {
		const route = readRoute(root, handlers.route);
		const entry = readEntry(root, handlers, explicitEntry);
		const workspace = await readWorkspace(root, entry);
		const observation = await readObservation(root, entry);
		const recoverableState = await readRecoverableState(root, entry);
		const history = await readStateHistory(root, entry);
		const trace = await readTrace(root, entry);
		const summary = summarizeWorkspace(workspace);
		return {
			route,
			status: entry?.status || "idle",
			workspace: clone(workspace || {
				widgets: [],
				links: [],
				actions: [],
				perceptionQueries: []
			}),
			observation: clone(observation),
			state: clone(observation?.state || null),
			recoverableState: clone(recoverableState),
			history: clone(history),
			trace: clone(trace),
			...summary
		};
	}
	function postResponse(root, payload) {
		root.postMessage({
			source: WIDGETVA_DOCK_BRIDGE_SOURCE_PAGE,
			type: WIDGETVA_DOCK_BRIDGE_RESPONSE,
			...payload
		}, "*");
	}
	async function runDockMethod(root, handlers, method, params = {}, { requestId = null } = {}) {
		if (method === "probe" || method === "read") return buildDockSnapshot(root, handlers);
		if (method === "bind") {
			if (typeof handlers.bind !== "function") throw new Error("WidgetVA Dock bridge does not have a page bind handler.");
			return buildDockSnapshot(root, handlers, await handlers.bind(params || {}));
		}
		if (method === "readAgentConfig") return readOfficialPageAgentConfig(root);
		if (method === "configureAgent") return configureOfficialPageAgent(root, params || {});
		if (method === "runSession") {
			const surface = readRuntimeSurface(root, readEntry(root, handlers));
			const objective = params?.objective || params?.prompt || "";
			if (typeof objective !== "string" || objective.trim().length === 0) throw new Error("WidgetVA Dock runSession requires an objective.");
			if (typeof surface?.runObjectiveLoop !== "function") throw new Error("WidgetVA page is not bound to a natural-language agent loop.");
			return {
				session: clone(await surface.runObjectiveLoop({
					objective,
					maxTurns: params?.maxTurns,
					model: params?.model,
					temperature: params?.temperature,
					timeoutMs: params?.timeoutMs,
					chatTimeoutMs: params?.chatTimeoutMs,
					onTurn: requestId ? async (progress) => {
						postResponse(root, {
							id: requestId,
							ok: true,
							event: "turn",
							result: {
								progress: clone(progress),
								snapshot: await buildDockSnapshot(root, handlers)
							}
						});
					} : void 0
				})),
				snapshot: await buildDockSnapshot(root, handlers)
			};
		}
		if (method === "restore") {
			const state = params?.state && typeof params.state === "object" && !Array.isArray(params.state) ? params.state : { stateId: params?.stateId };
			const stateId = state?.stateId || params?.stateId;
			if (typeof stateId !== "string" || stateId.length === 0) throw new Error("WidgetVA Dock restore requires stateId.");
			return restoreOfficialPageState(root, handlers, {
				...clone(state),
				stateId
			});
		}
		throw new Error(`Unsupported WidgetVA Dock bridge method: ${String(method)}.`);
	}
	function createOfficialPageDockBridgeMessageHandler(root, handlers = {}) {
		return async (event) => {
			if (event.source !== root) return;
			const message = event.data;
			if (!message || message.source !== "widgetva-dock-content" || message.type !== "widgetva:dock-request") return;
			try {
				const result = await runDockMethod(root, typeof handlers.readCurrentHandlers === "function" ? handlers.readCurrentHandlers() : handlers, message.method, message.params || {}, { requestId: message.id });
				postResponse(root, {
					id: message.id,
					ok: true,
					result
				});
			} catch (error) {
				postResponse(root, {
					id: message.id,
					ok: false,
					error: summarizeDockError(error)
				});
			}
		};
	}
	function installOfficialPageDockBridge(root = window, handlers = {}) {
		if (!root) return null;
		const installKey = "__widgetVADockBridgeInstalled";
		if (root[installKey]) {
			root[installKey].updateHandlers?.(handlers);
			return root[installKey];
		}
		const bridgeState = { handlers };
		const handler = createOfficialPageDockBridgeMessageHandler(root, { readCurrentHandlers: () => bridgeState.handlers });
		root.addEventListener("message", handler);
		root[installKey] = {
			updateHandlers(nextHandlers = {}) {
				bridgeState.handlers = nextHandlers && typeof nextHandlers === "object" ? nextHandlers : {};
			},
			dispose() {
				root.removeEventListener("message", handler);
				if (root[installKey]) root[installKey] = null;
			}
		};
		return root[installKey];
	}
	//#endregion
	//#region extension/src/page/observableD3PageScript.js
	function installObservableD3DevHelpers(root = window) {
		const helpers = { async bootstrapOfficialPage(options = {}) {
			return ensureObservableD3PageBootstrap({
				root,
				isSupportedPage: isObservableD3NotebookPage,
				describePage: describeObservableD3PageShape,
				waitForWorkerFrame: waitForObservableWorkerFrame,
				bootstrapPage: bootstrapObservableD3Page,
				timeoutMs: 15e3,
				...options || {}
			});
		} };
		root.__widgetVAObservableD3Dev = helpers;
		return helpers;
	}
	function installObservableD3DockBridge(root = window) {
		const helpers = root.__widgetVAObservableD3Dev || installObservableD3DevHelpers(root);
		return installOfficialPageDockBridge(root, {
			route: {
				provider: "d3",
				pageType: "official-observable-notebook"
			},
			bind: (options = {}) => helpers.bootstrapOfficialPage({
				forceReattach: true,
				...options || {}
			})
		});
	}
	async function startWidgetVAOnObservableD3Pages(options = {}) {
		const helpers = installObservableD3DevHelpers(window);
		installObservableD3DockBridge(window);
		return helpers.bootstrapOfficialPage({
			forceReattach: true,
			...options || {}
		});
	}
	async function bootstrapWidgetVAOnObservableD3Page(options = {}) {
		return ensureObservableD3PageBootstrap({
			root: window,
			isSupportedPage: isObservableD3NotebookPage,
			describePage: describeObservableD3PageShape,
			waitForWorkerFrame: waitForObservableWorkerFrame,
			bootstrapPage: bootstrapObservableD3Page,
			timeoutMs: 15e3,
			forceReattach: true,
			...options || {}
		});
	}
	function createObservableD3RouteWatcher(root = window) {
		let lastHref = window.location.href;
		let scheduled = false;
		const runBootstrap = async () => {
			if ((root?.__widgetVAOfficialPageBootstrap?.observableD3 || null)?.status !== "ready") return;
			await startWidgetVAOnObservableD3Pages();
		};
		const scheduleBootstrap = () => {
			if (scheduled) return;
			scheduled = true;
			queueMicrotask(() => {
				scheduled = false;
				const currentHref = window.location.href;
				if (currentHref === lastHref) return;
				lastHref = currentHref;
				runBootstrap().catch((error) => {
					console.error("[WidgetVA] Failed to rebind after Observable route change.", error);
				});
			});
		};
		const wrapHistoryMethod = (name) => {
			const original = window.history?.[name];
			if (typeof original !== "function") return;
			window.history[name] = function wrappedHistoryMethod(...args) {
				const result = original.apply(this, args);
				scheduleBootstrap();
				return result;
			};
		};
		wrapHistoryMethod("pushState");
		wrapHistoryMethod("replaceState");
		window.addEventListener("popstate", scheduleBootstrap);
		window.addEventListener("hashchange", scheduleBootstrap);
	}
	if (typeof window !== "undefined" && window) {
		installObservableD3DevHelpers(window);
		installObservableD3DockBridge(window);
		createObservableD3RouteWatcher(window);
	}
	//#endregion
	exports.bootstrapWidgetVAOnObservableD3Page = bootstrapWidgetVAOnObservableD3Page;
	exports.installObservableD3DevHelpers = installObservableD3DevHelpers;
	exports.installObservableD3DockBridge = installObservableD3DockBridge;
	exports.startWidgetVAOnObservableD3Pages = startWidgetVAOnObservableD3Pages;
	return exports;
})({});
